# 换行一律 LF —— 入库与检出都是。
#
# 为什么要这份文件：门禁里的 `cargo fmt --all --check` 只认 `rustfmt` 的
# `newline_style`，而它是默认的 `Auto`（按每个文件自己的首个换行符走）。`Auto` 挡得住
# 「CRLF 检出的工作区整仓库 `--check` 全红」，但**拦不住一份带 CRLF 的新文件混进来**
# ——对每个文件照单全收，门禁不会响，统一的 LF 就这么被啃掉一块（挂单 `Q184`）。
# 堵这一手是仓库级的设置，不是 `rustfmt` 的旋钮。
#
# 主力机是 Windows（ADR-0018），`core.autocrlf` 与 `core.eol` 都没设过；`eol=lf` 把
# 检出形态也钉成 LF，免得换台机器门禁就换个结果。落这份文件时 HEAD 上 0 个 CRLF 文件，
# 所以它不重整任何既有文件。
*           text=auto eol=lf

# 界面内嵌的中文字体子集，别当文本碰。
*.ttf       binary

# ⚠️ **往仓库里加二进制夹具时，在这儿给它的扩展名补一行 `binary`。**
# 上面那条 `text=auto` 只靠「头 8000 字节里有没有 NUL」来认二进制——ROM 头、小压缩包、
# 某些图片 payload 头上未必有 NUL，认错了就会被当文本改写 0x0D/0x0A，夹具**静悄悄地坏掉**，
# 而且只在 Windows 上红，错误信息一个字都不会提到这份文件。
# （眼下仓库里一个签进来的二进制夹具都没有：测试夹具全是运行时现造的，
# `git ls-files --eol` 数下来 326 个文本文件全是 `i/lf w/lf`，非文本只有上面那个字体。）

# `cmd.exe` 读不好只有 LF 的批处理（多行块与 `goto` 标号会认不出来），它得留着 CRLF。
# 主力机是 Windows（ADR-0018），眼下没有这类文件，先把位子占上。
*.bat       text eol=crlf
*.cmd       text eol=crlf
