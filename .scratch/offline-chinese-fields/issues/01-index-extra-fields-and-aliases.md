# 01 — 中文离线索引多存几样，别名先端到端

**What to build:** 中文离线源撞上一个条目之后，那个条目的**别名**进**标题集合**——同一部作品的多个叫法，用户搜哪个都找得到。

为了做到这件事，本机那份中文离线索引要开始留住它一直丢掉的东西：条目的**简介**、以及 infobox 里的**类型**、**开发商**、**发行商**。这一票只**取到并存下来**，真正把它们产出去是后面几票的事——但存储、结构版本、索引这条路要在这一票里一次走通，别名就是走通它的那颗曳光弹。

别名是**变体**这一层的字段（与中文名同层，依据说的是「这个文件的正题撞上的」），所以这一票**不碰作品层的任何机制**。

结构版本要往上走一格。这份索引是**可再生**的：版本对不上就从本机那份原件整份重建，不走顺序迁移，也**不重新下载**。

**Blocked by:** 无 —— 可立即开工

**Status:** done

- [x] 跑一趟**离线档**刮削，撞上的变体在**变体锚点**上多出别名，且它们进了标题集合
      —— `crates/core/tests/scrape.rs` 的
      `撞上中文条目的变体在变体锚点上多出别名而且进了标题集合`：变体锚点上
      `中文离线源` 给中文名一条、`中文离线源·别名` 给别名两条（`魂斗羅`、`Probotector`），
      `title::fold` 折出来的集合里三个叫法都在，别名那两条的类型是**别名**。
      **票没想到的一处**：中立库 `scrape_value` 的去重键原本只到「锚点 × 字段 × 源」，
      一个源装不下几个别名——挂单 Q1 记着我怎么走的（去重键加上 `value`，老库无损换键，
      不加中立库结构版本）。
- [x] 撞不上中文条目的变体**一个新字段都不产出**
      —— 同一条测试的第六段：`FC/一堆/甲.zip` 在两个中文源上都是空的。
      单元测试 `别名那一路把条目的别名都交出来而且不重复中文名` 钉着「两路同生共死」。
- [x] 这一趟的网络请求数为 **0**
      —— 同一条测试的第一段：`scrape::run` 的网络句柄传的是 `None`，报告里的源清单只有
      本地源（`中文离线源`、`中文离线源·别名` 在，`ScreenScraper` 不在）。
      现成的对照 `离线档只用本地数据源` 仍然绿。
- [x] 索引里存得下简介、类型、开发商、发行商；结构版本已 +1
      —— `zh::store::SCHEMA_VERSION` 1 → 2；`subject` 多一列 `summary`，多值的三样落在新表
      `subject_fact`（带 `ord`，留住数据源的原次序）。测试
      `zh::store::tests::简介类型开发商发行商都留得住`、
      `zh::dump::tests::一条真实记录读得出简介类型开发商与发行商`。
      **简介一个字都不改**（连两头的全角空格都不掐，规格 18）——挂单 Q3。
- [x] 拿一份**旧结构版本**的索引打开，它**自动从本机原件重建**，没有网络请求、也没有要求用户重新下载
      —— `zh::sync::tests::结构版本一变就从本机那份原件重建_不下载不报错`：那条测试里
      **连 fetcher 都没有**。`Store::open` 不再报 `StoreError::Version`（那个变体删了，
      挂单 Q4），改成扫掉旧结构、把上一版的 dump 名与指纹留在 `Store::rebuilding()` 里，
      由新增的 `zh::sync::rebuild` 从缓存目录那份原件重读。本机没有原件时是
      `Rebuilt::NoOriginal`，如实说一句而不是硬报错
      （`本机没有那份原件时如实说一句而不是硬报错`）。
- [x] infobox 解析在解析处有内联单元测试，盖住：单值、多值块、顿号分隔、缺键、空值、键名带空格
      —— `crates/core/src/zh/dump.rs` 两条：
      `infobox_单值多值块与顿号三种写法都拆得开`（前三样，外加混着写时的去重）、
      `infobox_缺键空值与键名带空格`（后三样）。另有
      `发行与发行日期是两个键` 钉住 `|发行=` 不会被 `|发行日期=` 顶掉。
- [x] **输入指纹**盖住了新的解析——改了取哪些字段之后重跑，撞上过的锚点会**重采**而不是被缓存跳过
      —— 索引带一行 `zh::store::FIELDS`（写进 `meta`，`romcat zh sync` 也打出来），
      进 `scrape::zh` 的 `probe`。测试 `scrape::zh::tests::索引取了哪几样也进输入指纹`：
      同一份 dump、同一批条目，只把「取了哪几样」换成上一版，指纹就不一样。
- [x] 门禁命令全绿
      —— `cargo clippy --workspace --all-targets --all-features` 与
      `cargo test --workspace --all-features` 各跑一遍，见提交说明。
