# 06 — 工序段与识别这道工序

**What to build:** 库屏长出第三段——**工序**：这个库还差哪几道步骤。这一张立起工序段本身，加上第一道工序：**识别**。

那一行显示的是**还差多少**——"N 个**变体**连识别都还没跑过"——不是"上次几点跑的"。人要的是"下一步该干什么"，时间戳答不了这个问题：加了一块盘重扫之后，那个数自己就涨上去，你不必再自己推理"是不是该重跑识别了"。

**这个数已经有了**：**待确认队列**屏正用警告色画着它，命令行的队列报告印的也是同一份。这一张不另造一份，复用现成的那个。

点那一行的按钮，识别排上**任务台**——报得出跑到哪儿、按得下停下，跑完那一行的数字当场刷新、待确认队列自己重新列过。接任务台的办法与已接的那五种（差量预览、扫描、取数据源、刮削、同步）一模一样：核心那侧收一个任务句柄，界面这侧往台上排一趟；要写库的那几支写在**认领**那一步，因为台上那条线拿的是只读连接。

**这一张同时立起"工序进度"这个新模块**（眼下只含识别一支），07 与 08 各往里加一支。三道工序的度量收在一处，是为了工序段一次问得出全部状态，而不是调三个地方。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 库屏在"根"与"数据源"之后长出第三段，一道工序一行
- [x] 识别那一行显示"还有多少个变体连识别都还没跑过"
- [x] 那个数与待确认队列屏上、与命令行队列报告印的是同一个
- [x] 点那一行的按钮把识别排上任务台，报得出进度
- [x] 跑到一半按"停下"，按**停在半路**记账，与"跑完了"在任务台历史上分得开
- [x] 跑完之后那一行的数字当场刷新
- [x] 跑完之后待确认队列自己重新列过，不必手动点"重新列队列"
- [x] 这一趟正在跑时，那一行的按钮按不下去
- [x] 外置盘不在位时工序那几行照样看得见

## 证据

**新立的两个模块**：`crates/core/src/stage.rs`（**工序进度**，`Stages::survey` 一趟问出全部
工序还差多少）与 `crates/gui/src/stages.rs`（库屏上的**工序段** + 排识别那一趟活的入口）。
核心那一侧的长入口是 `identify::run_task`（收一个 `task::Handle`，与 `scan::scan` 同形）。

逐条：

1. **第三段**——`crates/gui/src/roots.rs::Screen::body` 末尾 `self.stages.ui(...)`；
   `crates/gui/tests/roots.rs::库屏在根与数据源之后长出工序那一段_识别那一行说的是还差多少`
   断言屏上真画出了「工序 · 1 道」与「识别」那一行。
2. **还差多少**——措辞在 `stage.rs::StageRow::render`（领域判断不在界面层，ADR-0005）；
   同一条测试断言屏上那一行逐字是「2 个变体连识别都还没跑过」。
3. **同一个数**——工序段、`triage::Queue::not_run`、`QueueReport::not_run` 三处同取
   `Catalog::not_run_count`，没有第二份算法。`crates/core/tests/stage.rs::识别那一行的数与待确认队列和命令行队列报告印的是同一个`
   （连命令行那份报告 `render_text()` 印出来的数一起断）＋
   `crates/gui/tests/roots.rs::工序段那个数与待确认队列屏上的是同一个`
   （顺带钉住「加了一块盘重扫那个数自己涨上去」，规格 28）。
4. **排上任务台、报得出进度**——`crates/core/tests/task.rs::识别一路报得出算完了几个变体而且跑完记成完成`
   （`task.progress().done` 走到变体总数）＋ 下面第 6 条那条 GUI 测试。
5. **停在半路**——`crates/core/tests/task.rs::识别按停之后在台上记成停在半路而且不说下一趟接着算`
   ＋ `crates/gui/tests/roots.rs::识别跑到一半按停在任务台历史上记成停在半路`。
   两条都断到那半句上：**识别没有断点**，那句话写的是「从头再算一遍」而不是「接着算」。
6. **数字当场刷新** ＋ 7. **队列自己重新列过**——
   `crates/gui/tests/roots.rs::点一下把识别排上任务台_跑完那一行的数字当场刷新_队列自己重新列过`：
   全程一次「重新列队列」都没点，跑完之后 `queue().identified()` 翻成真、`not_run()` 变 0。
   重列那一下在 `crates/gui/src/app.rs::poll_tasks`（只有窗口同时够得着两屏）。
8. **按钮按不下去**——`crates/gui/tests/roots.rs::这一趟正在跑的时候那一行的按钮按不下去`：
   **先拿一趟占位任务占住台**，识别稳稳排在队列里，于是这一条不靠「识别恰好还没跑完」
   那种挂钟彩票（挂单 `Q196` / `Q349`）。断的是屏上真画出了「跑着呢」，加上再按一次
   什么都不发生。
9. **盘不在位照样看得见**——`crates/gui/tests/roots.rs::外置盘不在位的时候工序那几行照样看得见`：
   把 fixture 目录删掉、`reload` 之后那一行的数与屏上那句话都还在（工序段一个字节都不读主库）。

**门禁**：`cargo xtask gate` 五条全绿（fmt 1s / check 19s / clippy 19s / **test 504s** /
doc 8s，墙钟约 9m11s，这棵树独占跑、`target/` 是热的）。`cargo test --workspace
--all-features` **1,739 条**——基点 `cee1c1b` 上是 1,726，差的正是本票新增那 13 条
（核心 `tests/stage.rs` 4 条 + `tests/task.rs` 2 条，界面 `tests/roots.rs` 7 条）。

## 挂单

这一趟记下 `Q416`–`Q424` 九条（`.scratch/PARKING-LOT.md`），一条都没停线：

- `Q416` **识别在任务线程里自己开一份连接写库**，与规格那句「要写库的那几支写在认领那一步」
  对不上——照的是扫描与刮削两条已经落地的先例（识别一路往库里写批，攒到认领那一步再写会
  卡住画帧那条线程）。**这一条要拿主意的人裁**：07 / 08 会跟着走同一条路。
- `Q417` 工序那一行的形状（两支的 `Behind` vs 每行都带「数 + 时刻」两格）——票 `07` 裁。
- `Q418` 界面上的识别整层关掉**模型推断**，连库里已经付过钱的答案也不用。
- `Q419` 「跑完自己重新列队列」只做识别这一支，扫描完不重列。
- `Q420` 识别没有断点：按停之后下一趟从头再算一遍（如实说了，没瞒）。
- `Q421` 票 `09` 那三处捷径怎么把按下的那一下递到 `App::start_stage`——留给 `09`。
- `Q422` 「退回上次跑的时刻」那条退路**有形状、没有数据通路**（`at` 眼下恒为 `None`）——
  识别这一支数得出来，不需要它；票 `07` 是第一个真要用退路的，那条通路跟它一起来。
- `Q423` 「没有 DAT 库就当场拒绝」这条守卫票与规格都没要求（不加的话整库会落成
  「未命中」并写进中立库，那是假结论不是失败）。
- `Q424` **没有任何东西在新代码上跑一遍词表**：`job_of` 又撞进**任务**那条的 `_Avoid_`
  里（这一趟改成了 `task_of`）。**与 `Q365` 互相指认**——那一条记的是票与规格上的
  同一个缺口，两条一起裁才划得出「词表要不要有一道门禁」。

## 给下三张票的接口

- **工序进度模块**：`romcat_core::stage`（`crates/core/src/stage.rs`）。**加一支要写五处**，
  `Stage` 那段文档逐条列着——其中两处不在核心库里（`romcat_gui::stages` 里排活那个 `match`、
  `romcat_gui::app::App::poll_tasks` 里那个 `match`）。**「这一支交不出数」的退路是
  `Behind::Unmeasured { at, why }`**，只对单支生效（`Stages::survey` 一行一支地折，
  一支退了别的支照旧报数——`crates/core/tests/stage.rs` 钉着这一条）。
- **排一趟工序的入口**：`romcat_gui::app::App::start_stage(romcat_core::stage::Stage)`。
  票 `09` 那三处捷径调它就行；它与库屏工序段那颗按钮排的是同一趟活
  （底下都是 `romcat_gui::stages::Section::start`）。


## 挂单裁决（第四轮收口，2026-09-09）

第四轮收口的范围是 `Q364`–`Q448`（记下来的 66 条）。**本票名下 9 条，全部 `settled`**——判据是**一条挂单有了主人就算收了，不是等它做完**。
分档：**记 5 ／ 岔 0 ／ 活 4**（记＝发现了一件事、没人欠动作；岔＝一次真的选择，已由拿主意的人当场裁；活＝一处改动，要一张票接）。
全文迁自 `.scratch/PARKING-LOT.md`，每条顶上多一行**裁决**。

### Q416 — 识别在任务线程里自己开一份连接写库，与规格那句「写在认领这一步」对不上

**裁决（第四轮收口，2026-09-09）：记** —— 这一条 2026-09-09 由拿主意的人当场裁了：**维持票 `06` 的做法，`07` 与 `08` 照同一条走**，规格那段已经跟着改成「写库走哪条，按写量分」。收口只是把它一并迁回。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 两份东西矛盾
- **在哪：** `.scratch/gui-self-sufficient/spec.md`「核心库：三个长入口接任务台」那一节
  （「要写库的那几支写在认领那一步」）；对面是 `crates/gui/src/roots.rs:294`（扫描）
  与 `crates/gui/src/scrape.rs:409`（刮削），两条已经落地的写库路走的都是
  「后台线程按文件路径自己开一份写得动的库」。新写的是
  `crates/gui/src/stages.rs::Section::start`。
- **为什么没停线：** 规格那句话描述的是**子库同步**那一支（台上拿的是只读连接、清单当
  产物交回来），而识别与扫描同类：它一路往中立库里写批（`write_batch` 默认 2,000 条），
  把四万六千条结论攒到认领那一步再写，会让画帧那条线程整个卡住——那正是这一屏要治的病。
- **这张票实际做了什么：** 照扫描与刮削的先例办：任务线程 `Site::open_file` 自己开一份，
  产物只带回那份命中率报告。两份连接同时写同一个文件由 SQLite 的 WAL 兜着，与扫描一样。
- **谁来裁：** 拿主意的人（若判定规格那句话是全称，票 `07` / `08` 也要跟着改）
- **状态：** settled（第四轮收口，2026-09-09）


### Q417 — 工序那一行的形状：两支的 `Behind`，还是每行都带「数 + 时刻」两格

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。票 `07` 是第一个真用退路的，**用上了、没要求改形状**——两支的枚举活过了它的第一个真用户。`Option` 两两组合出的四种里有两种无意义，那条理由也站住了。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 规格没说
- **在哪：** `crates/core/src/stage.rs` 的 `Behind`（`Left(u64)` / `Unmeasured { at, why }`）
- **为什么没停线：** 两条路画出来的那一行长得一样，翻案是一次编辑（改一个枚举加两处
  `match`），07 与 08 加自己那一支时也够得着。
- **这张票实际做了什么：** 选了**两支的枚举**：一行要么报数、要么退回时刻，
  由类型分开。另一条站得住的路是 `StageRow { remaining: Option<u64>, last_run: Option<i64> }`
  ——那样一行可以同时有数与时刻。没选它，是因为「有数又有时刻」这个状态谁都说不出该画
  哪一个，而 `Option` 两两组合出的四种里有两种是无意义的。
- **谁来裁：** 票 `07`（它是第一个真的要用退路的）
- **状态：** settled（第四轮收口，2026-09-09）


### Q418 — 界面上的识别整层关掉模型推断，连库里已经付过钱的答案也不用

**裁决（第四轮收口，2026-09-09）：活** —— 给核心库加 `Guessing::cached(&Answers)`（只用缓存、一个请求都不发），界面照它装配。这一条**直接顶着本轮的题目**：库里已经付过钱的答案原样躺着，人得回终端跑一趟 `romcat identify` 才捡得回来。价目表那个理由（0 价算出来的报告会印「花费 0.00 美元」）只挡得住**发请求**那一层，挡不住只读缓存。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/stages.rs::identify_run` 里那句
  `identify::model::Guessing::off()`；对面是 `crates/cli/src/main.rs:1766`
  （`catalog.model_answers()` → `model::Answers::build`，命令行**关着这一层也照旧用缓存**）
- **为什么没停线：** 那些答案原样躺在中立库里，一条都没丢——跑一次 `romcat identify`
  就全捡回来了。差别只是界面上跑出来的候选比命令行少一层。
- **这张票实际做了什么：** `Guessing::off()`，整层关着。另一条站得住的路是给核心库加一个
  `Guessing::cached(&Answers)`（只用缓存、一个请求都不发），界面照它装配。没选它，是因为
  这一层的价钱要从价目表里查，而界面上没有那几个旋钮；一份用 0 价算出来的计划会在报告里
  印出「花费 0.00 美元」，那比不印更糟（`pricing.toml` 开头写的就是这件事）。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）


### Q419 — 「跑完自己重新列队列」只做识别这一支，扫描完不重列

**裁决（第四轮收口，2026-09-09）：活** —— 扫描完不重列，于是队列屏屏头那个「还没识别 N 个」会**过期**——屏上那个数会说错话，撞规格第 31 条。队列本身（要人裁的那些）确实一条没变，但那个数是屏上的话。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/app.rs::poll_tasks` 里认领库屏那一趟之后那个 `match`
- **为什么没停线：** 扫描确实也会让队列屏屏头那个「还没识别 N 个」过期，但**队列本身
  （要人裁的那些）一条都没变**——那些变体连结论都还没有，进不了队列。
- **这张票实际做了什么：** 只在识别跑完之后重列。另一条路是扫描完也重列一次，那样屏头
  那个数当场就对；代价是真库上每扫完一个根就走一遍一万八千条。
- **谁来裁：** 收尾
- **状态：** settled（第四轮收口，2026-09-09）


### Q420 — 识别没有断点：按停之后下一趟从头再算一遍

**裁决（第四轮收口，2026-09-09）：活** —— 识别没有断点：按停之后下一趟从头再算一遍（`run` 起手就 `clear_identifications()`）。扫描有**断点**、同步有**清单**，识别两样都没有。如实说出来不骗人，但能力缺口是真的，而让识别可续跑是识别本身的一次改动，伸出那张票。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/identify.rs::run` 起手那句 `catalog.clear_identifications()`；
  这一趟新写的那句话在 `run_task` 的 `Handle::halfway` 里
- **为什么没停线：** 如实说出来就不骗人。扫描有**断点**、同步有**清单**，识别两样都没有，
  而这一票要的只是「按停记成停在半路」，那一档它本来就该落（它清过库，动过东西）。
- **这张票实际做了什么：** 那句「停在半路」明写「识别没有断点，下一趟从头再算一遍」，
  库屏与任务屏原样转出来。另一条路是让识别可续跑（只清这一趟要重算的那些），
  那是识别本身的一次改动，伸出这张票。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）


### Q421 — 票 `09` 那三处捷径怎么把按下的那一下递到 `App::start_stage`

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。票 `09` 自己放了记号（`queue::identify_shortcut`，五处共用一颗按钮），「记号该长什么样取决于 09 要在哪几处摆按钮」这条判断对了。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/app.rs::start_stage`（这一趟交出来的那个入口）；
  要用它的是 `crates/gui/src/queue.rs:514`、`:565`、`:851` 那三处空态文案
- **为什么没停线：** 入口已经在窗口那一层了，够得着；怎么从队列屏把「按下了」递上来是
  票 `09` 自己的活。
- **这张票实际做了什么：** 只交出 `App::start_stage(Stage)`，没有替队列屏放记号。
  队列屏那一侧照 `browse` / `sublibrary` 的先例放一个 `take_…` 记号、由 `App::route`
  取走就行——这一趟没先替它写，是因为记号该长什么样取决于 `09` 要在哪几处摆按钮。
- **谁来裁：** 票 `09`
- **状态：** settled（第四轮收口，2026-09-09）


### Q422 — 「退回上次跑的时刻」那条退路有形状、没有数据通路：`at` 眼下恒为 `None`

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。票 `07` 把「上次跑是几点」那条数据通路一起带进来了（`Catalog::titles_folded_at`）；识别这一支数得出来，它只在中立库读不动时才退，而那时也没有时刻可给。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 规格没说
- **在哪：** `crates/core/src/stage.rs:154`（`identify_row` 里那句 `at: None`）；
  规格 `.scratch/gui-self-sufficient/spec.md`「工序进度收成一处」那节的「退路已经认可」段，
  与「Testing Decisions」那句「退路也要有测试」
- **为什么没停线：** 退路那一支的**形状**立起来了，`render()` 也钉了测试
  （`crates/core/tests/stage.rs` 三条：画的是时刻不是零、没跑过时说「还没跑过」、
  一支退了别的支照旧报数）。缺的只是**一个真的会退的调用方**——识别这一支数得出来，
  它只在中立库读不动时才退，而那时也没有「上次跑的时刻」可给。
- **这张票实际做了什么：** `Behind::Unmeasured { at, why }` 里那个 `at` 是留给 07 / 08 的：
  谁先要用退路，谁把「上次跑是什么时候」那条数据通路一起带进来（识别这一支没有那个时刻
  可读——中立库里没记过「上次识别是几点」）。另一条路是这张票就把「上次跑的时刻」这张表
  加出来，那是规格明确不要的（「不为了整齐去加一张变更计数表」）。
- **谁来裁：** 票 `07`（它是第一个真的要用退路的）
- **状态：** settled（第四轮收口，2026-09-09）


### Q423 — 没有 DAT 库时识别当场拒绝，这条守卫票与规格都没要求

**裁决（第四轮收口，2026-09-09）：记** —— 不用做，守卫留着。没有它 `DatRepo::open` 会当场建出一份空 DAT 库、整库落成「未命中」写进中立库——那是一条**假结论**，不是一次失败。措辞落在哪一层由 `Q437` 那条岔路裁了（留在界面层，规矩已写进 ADR-0005）。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/src/stages.rs::identify_run` 开头那段；命令行那一侧同一条判断在
  `crates/cli/src/main.rs:1658`
- **为什么没停线：** 没有它的话 `DatRepo::open` 会**当场建出一份空的 DAT 库**，识别照跑，
  整库落成「未命中」并写进中立库——那是一条假结论，而不是一次失败。
- **这张票实际做了什么：** 加了守卫，如实拒绝并指向库屏上面那一段「数据源」，
  配一条 GUI 测试（`还没取回那份弹药时识别如实拒绝并说清为什么`）。
- **谁来裁：** 收尾（判定超范围的话，删掉守卫加那条测试就是一次编辑）
- **状态：** settled（第四轮收口，2026-09-09）


### Q424 — 没有任何东西在新代码上跑一遍词表：`job_of` 又撞进 `_Avoid_` 里

**裁决（第四轮收口，2026-09-09）：活** —— 库屏那两处旧账（`roots.rs` 的 `enum Job` 与 `Screen::job_of`）一个字没动。与 `Q365②` 一起才划得出「要不要一道扫 `_Avoid_` 的门禁」——一道 grep 挡得住本轮三次里的两次（`job_of`、「输出目录」），挡不住的那半（屏上的措辞）仍旧只能靠人念一遍。

- **来自：** 票 `gui-self-sufficient/06`
- **类别：** 工具缺陷
- **在哪：** `CONTEXT.md` 的**任务**条（`_Avoid_` 里逐字列着 `job`）；撞上的那处是
  `crates/gui/src/stages.rs` 里原先那个 `job_of`（这一趟已改成 `task_of`）；
  旧账在 `crates/gui/src/roots.rs` 的 `enum Job` 与 `Screen::job_of`
- **为什么没停线：** 改一个名字而已，而且是审查抓出来的，不是跑出来的。
- **这张票实际做了什么：** 新代码那处改成 `task_of` 并在文档里写明「名字不叫 `job_of`，
  词表那条 `_Avoid_` 列着它」；库屏那两处旧账**一个字没动**（不在这张票的范围里）。
- **谁来裁：** 收尾——**与 `Q365` 互相指认**：那一条记的正是「没有任何东西在票与规格上
  跑一遍词表」，这一条是它在**代码**上的同一个实例。两条一起裁才划得出「词表要不要有
  一道门禁（哪怕只是一条 grep `_Avoid_` 里那几个词的检查）」。
- **状态：** settled（第四轮收口，2026-09-09）

