# 05 — 开场：认领一个新主库

**What to build:** 在开场上认领一个新**主库**，全程不必开终端。一条龙向导三步：起主库名 → 选第一个**根** → 开扫。走完直接进主窗口，扫描已经排在**任务台**上跑着，你能立刻看进度、也能按停。

**名字撞了在第一步就拦下**——工作目录里已经有同名的库时当场说，而不是等你选完目录、按下开始才发现。

**领域判断一条都不许长在向导里**：加一个根要拦的三种情况（根名重复、与已有的根套在一起、圈进工作目录）全部调核心库现有的加根入口。向导重画 UI，但判断必须是同一个函数——两套判断只会分叉出两套行为（ADR-0005 的红线）。

**晚落盘**：起名与选根两步只在内存里攒，按下"开始扫描"才真的开中立库。中途关窗，磁盘上什么都不留——一份零根的空库对人没有用处，却会永久占着开场的一行。

**Blocked by:** 03

**Status:** done

- [x] 开场上能认领一个新主库，走完向导进主窗口
      —— `gui/tests/program.rs::在开场上认领一个新主库走完向导就进主窗口`：从一个一份库
      都没有的工作目录起，点开场抬头那颗「认领新主库」→ 起名 → 选根 → 按「开始扫描」，
      顶栏五屏当场画出来、标题写着人刚起的那个名字（**不是**带哈希的主文件名——原名
      这一趟真的落进了元数据表），而建出来的文件就落在 `workspace::catalog_path(工作目录,
      Slug::Named("我的主库"))` 那一处。入口在 `opening::Screen::workspace_ui`
      （票 `03` 留的那一行），向导本身是新模块 `romcat_gui::claim`。
- [x] 走完之后扫描已经排在任务台上，报得出进度、按得下停下
      —— `gui/tests/program.rs::走完向导之后扫描已经排在任务台上报得出进度也按得下停下`：
      进主窗口那一帧落在**任务屏**上，屏上写着「扫描 · 主库」「已用 …」与那颗「停下」；
      点下去，等台上收场，历史里正好留着这一趟。**排扫描没另造一条路**：走的是库屏那条
      现成的 `roots::Screen::scan`（断点、并发档、被按停时怎么记账全在它上面），
      由 `App::claim_first_root` 接上去。**保留**：落在任务屏而不是默认那一屏，
      与 ADR-0002 的关系挂单 `Q409`。
- [x] 起名那一步就拦下这个工作目录里已被占用的主库名
      —— `gui/tests/program.rs::起名那一步就拦下这个工作目录里已被占用的主库名`：撞名时
      当场说「已经有一份叫「我的主库」的主库了」，而且**还站在第一步**（屏上没有「选第一个
      根」）。判据是**工作目录里那个文件在不在**（`workspace::catalog_path` +
      `Slug::Named` 折出来的那一条），与按下「开始扫描」建出来的是同一个文件。
      **保留**：这一句判断长在向导里、核心库没有对应的函数，挂单 `Q411`。
- [x] 根名重复、与已有的根套在一起、圈进工作目录三种情况都被拦下，且用的是与库屏加根同一个判断
      —— **同一个判断**这一半是构造上的事实：库屏与向导调的是同一个
      `roots::add_root_from_fields`（本票把库屏 `Screen::add_root` 里那段「两个框 → 一次
      `roots::add_root` 调用」的折算提了出来），拦不拦得住与那句措辞全在
      `romcat_core::catalog::roots::add_root`。撞得到的两支各一条测试，两句话都逐字来自
      核心库：`向导里选的根圈进工作目录时被拦下说的是加根那一处的原话`（断「ADR-0004」
      与「与工作目录」）与 `向导里根名不能用时说的也是加根那一处的原话`
      （断「这个根名不能用」「键的分隔符」）。**保留**：「根名重复」「与已有的根套在一起」
      在向导里**结构上撞不到**——向导只加第一个根，试的那份库恒为零根，挂单 `Q414`。
      规格「Testing Decisions」明写这一层**不重复验领域规则本身**。
- [x] 不给根名时按目录自己的名字取
      —— `gui/tests/program.rs::向导里不给根名就按那个目录自己的名字取`：根名那个框空着，
      台上那一趟叫「扫描 · 甲盘」——就是那个目录自己的名字，也不是兜底那个词。
      **兜底那一下不在向导里**：它在 `roots::add_root_from_fields` 里，与库屏共用。
      **保留**：目录是 `/`、名字带非法字符那几种边角一条守卫都没加（核心库那一处已经
      有答案），挂单 `Q413`。
- [x] 走到第二步、第三步时中途关窗，工作目录里不多出任何文件
      —— `gui/tests/program.rs::向导走到第二步就放弃时工作目录里一个文件都不多`：走到
      第二步、两个框都填好，一路**关窗**（把 `Program` 扔掉）与按**「算了」**各来一趟，
      两趟之后那个工作目录**递归数下来仍旧是空的**；末了再走一遍向导，那个名字还空着
      （留下一份空库的话，人会被自己刚才那一趟拦在第一步）。晚落盘落在
      `claim::Wizard::claim`：加根那条判断先在一份**内存库**上走一遍，过了才
      `Catalog::open_named`（开库就是建库）。**保留**：「中途关窗」在测试里是「把
      `Program` 扔了」，挂单 `Q412`；先拦后建这个次序挂单 `Q408`。
- [x] 向导建出来的库与命令行扫出来的库一样：同一份现场、同一套路径锚，命令行接得上
      —— `gui/tests/program.rs::向导建出来的库与命令行扫出来的库一样命令行接得上`：
      向导走完、扫完，再用命令行那条「按名字找」（`Locate { library: Some("我的主库"), .. }`）
      开一次——开得出，`Site::library`（**路径锚**记的那个名字）与那份中立库的主文件名
      一模一样，`display_name()` 是人起的那个原名，库里真有扫进去的变体，
      连**断点**路径都与 `workspace::checkpoint_path(.., Slug::Named("我的主库"), "主库")`
      是同一条。走的是核心库那两条现成的路（`catalog_path` + `Catalog::open_named`），
      与 `romcat scan` 建库那一段同源。
- [x] 第二个根仍然从库屏加——向导只管第一个根，不做成第二条加根的路
      —— `gui/tests/program.rs::向导只管第一个根第二个根仍然从库屏加`：向导走完之后
      主窗口里**没有**「认领新主库」那颗按钮；第二个根从库屏那两个框加进去，屏上当场
      「根 · 2 个」、甲盘乙盘都在。`App::claim_first_root` 只在向导走完那一下走一趟，
      比库屏多做的只有「顺手把那趟扫描排上去」与「换到任务屏」两件。

## 收尾

**门禁**：`cargo xtask gate --keep-going` 在 `ticket/gui-self-sufficient-05` 上跑了一趟，
**五条全绿**——fmt 2s ／ check 69s ／ clippy 18s ／ test 805s ／ doc 8s。
`--all-features` **1,796 条**（70 个测试目标，0 失败），基点 `72c19be` 是 1,785，
差 11 条正是本票新增的 11 条（全在 `gui/tests/program.rs`，那一份从 16 条到 27 条）。
墙钟约 16 分钟，比基点上那 10m46s 长——同时另一趟合并门禁在主工作区上跑（队列宽度 2 的
设计点）。

**向导摆成什么形状、入口怎么进：**

入口是**开场抬头那一行**上那颗「认领新主库」（`opening::Screen::workspace_ui`，票 `03`
在代码里点名留的那个位子），紧挨着「工作目录 <路径>」——**认领进哪儿**正是这一下最要紧的
事。按下去，开场那张库表让位给向导（一条龙走到一半还摆着一排现成的库，只会让人在
「认领一个新的」与「开那一份」之间来回犹豫），换工作目录那一行同时收起来（向导第一步
查的重名是在**这个**目录里查的）。

向导本身是**两块界面 + 一颗按钮**：第一步起主库名（「下一步」当场查重名），第二步选第一个
根的目录与根名（两个框的提示字与库屏加根**共用同一对常量**），第三步「开扫」就是第二步上
那颗「开始扫描」——中间没有任何新信息，单摆一屏只让人多按一下确认（挂单 `Q406`）。
两步上都有「算了」，第二步还有「上一步」。走完交出一份刚开出来的**现场**加一个还没加上的
第一个根，由 `Program::ui` 换成主窗口（走 `opened_with`，那一份于是被记住）之后交给
`App::claim_first_root`：加根、排扫描、换到任务屏。

**晚落盘那条测试怎么验的：**

`gui/tests/program.rs::向导走到第二步就放弃时工作目录里一个文件都不多`。判据不是「库文件
在不在」，是**整个工作目录递归数一遍**（辅助函数 `底下有什么`，连子目录一起收，因为中立库
落在 `<工作目录>/catalog/` 底下，只数顶层的话建出来也看不见）。开场之前先断言那个工作目录
**本来就是空的**，然后两条路各走一趟：

1. **中途关窗**——点「认领新主库」、打上主库名、点「下一步」进第二步、把根目录与根名两个框
   都填好，**就是不按「开始扫描」**，直接把 `Program` 扔掉。再数一遍：仍旧是空的。
2. **按「算了」**——同一条路走到第二步，按下去回到那张表（屏上重新出现「还没有库，认领一个
   主库开始」）。再数一遍：仍旧是空的。

末了还有第三句：**再走一遍向导，那个主库名还空着**（「下一步」照旧进得到第二步）。这一句
钉的是「放弃过一趟之后没有留下任何痕迹」——留下一份零根空库的话，人重走一遍会在第一步被
自己刚才那一趟拦住，而且看不出为什么。

另有两条从背面钉着同一件事：`向导里选的根圈进工作目录时被拦下说的是加根那一处的原话` 与
`向导里根名不能用时说的也是加根那一处的原话`，都在**被拦下之后**比对整个工作目录与动手前
一模一样——**判断走在建库之前**（`Wizard::claim` 先在一份 `Catalog::open_in_memory` 的库上
把加根走一遍），不是建完再回头拦。落盘那一步自己也留了退路：`Catalog::open_named` 成功、
`Site::open_file` 却失败时（**沉淀库**是工作目录里共用的那一份，它可能开不动），把刚建出来
的那一份连 `-wal`／`-shm` 一起收掉。

**交给下游的三样：**

- **共用的加根接缝**：`romcat_gui::roots::add_root_from_fields(catalog, workspace, path, name)
  -> Result<LibraryRoot, String>`。「两个框 → 一次 `roots::add_root` 调用」的全部折算在它
  里头（目录空着、那不是目录、名字不填时按目录自己的名字取），拦不拦得住与那句措辞仍旧全在
  `romcat_core::catalog::roots::add_root`。库屏 `Screen::add_root` 与向导都调它——**谁再加
  一条加根的路都该调它**。`Screen::add_root` 顺带改成交出**加上去的那个根名**
  （`Option<String>`），排扫描要的正是这个名字。
- **向导那一段的形状**：`romcat_gui::claim`——`Wizard::new(workspace)` /
  `Wizard::ui(ui) -> Outcome`（`Going` / `Dropped` / `Done(Box<Claimed>)`）。
  `opening::Chosen` 多了一格 `first_root: Option<claim::FirstRoot>`：**挑中一份现成的库时
  它是 `None`**，刚认领出来的那一份才欠着这一下。
- **进主窗口之后那一下**：`App::claim_first_root(&claim::FirstRoot)`——加根、排扫描、换到
  任务屏。**它不是第二条加根的路**（验收第 8 条），只在向导走完那一下走一趟。

**挂单**：`Q406`–`Q415` 十条，全部 open——`Q406` 三步压成两块界面加一颗按钮、
`Q407` 派活的简报把 `Program::opened` 说成会记（**错不在票面也不在规格**，处置同 `Q366`）、
`Q408` 加根判断走两遍（先拦后建）、`Q409` 走完落在任务屏与 ADR-0002 的关系、
`Q410` 落盘之后那一小段失败落在库屏且没有测试钉着、`Q411` 「这个主库名被占了」长在向导里、
`Q412` 「中途关窗」在测试里的定义、`Q413` 根名按目录名取那几种边角一条守卫都没加、
`Q414` 验收第 4 条三种情况里两种结构上撞不到、
`Q415` 本轮第三次撞上词表 `_Avoid_`（而这一次是动手前避开的，前两次 `Q424`／`Q439` 都是
事后才发现——这条记在「该不该给词表加一道门禁」那笔账上）。


## 挂单裁决（第四轮收口，2026-09-09）

第四轮收口的范围是 `Q364`–`Q448`（记下来的 66 条）。**本票名下 10 条，全部 `settled`**——判据是**一条挂单有了主人就算收了，不是等它做完**。
分档：**记 10 ／ 岔 0 ／ 活 0**（记＝发现了一件事、没人欠动作；岔＝一次真的选择，已由拿主意的人当场裁；活＝一处改动，要一张票接）。
全文迁自 `.scratch/PARKING-LOT.md`，每条顶上多一行**裁决**。

### Q406 — 向导摆成两块界面加一颗按钮，而票与 ADR 说的是「三步」

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。「三步」说的是这件事有哪三段，不是「界面上得有三屏」；多摆一屏确认没有任何新信息，而这一步已经有「上一步」可回。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/claim.rs::Step`（两支：`Name` / `Root`）；对面是
  `docs/adr/0023` 与票面那句「一条龙向导三步：起主库名 → 选第一个根 → 开扫」
- **为什么没停线：** 「三步」说的是这件事有哪三段，不是「界面上得有三屏」；而第三段没有
  任何自己的输入。
- **这张票实际做了什么：** 起名一块界面、选根一块界面，「开扫」是选根那一步上那颗
  「开始扫描」。第二条路是再摆一屏确认（把库名、根目录、根名列一遍再按一次）——它站得住，
  多那一屏能在落盘之前给人最后一眼；没走是因为那一屏没有任何新信息，而这一步已经有
  「上一步」可回。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）


### Q407 — 派活的简报说走 `Program::opened(...)` 那一份就会被记住，可 `opened` 恰恰是不记的那一条

**裁决（第四轮收口，2026-09-09）：记** —— 不用做，票走对了（走 `opened_with`，`crates/gui/tests/program.rs` 钉着）。同 `Q366`：**错在派活的编排简报**，不在票面也不在规格——日后读这条的人别去票或规格里找那句话。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 票写错了
- **在哪：** **不在票面、也不在规格里**——错在**派这张票的编排简报**上（它的「票 `04`
  给你的：那份记忆」一段写着「走 `Program::opened(...)` 那条已有的路即可——它会记」）。
  日后读这条的人别去票或规格里找，那儿没有这句话。处置同本轮 `Q366`（简报把结构版本
  说成 4，实际是 7）。对面是 `crates/gui/src/program.rs::Program::opened`——记那一下在
  `opened_with` 里，`opened` 一个字都不记（它的文档还专门写着为什么：合成数据那一路
  没有中立库文件）。
- **为什么没停线：** 照票面写会静静地不记，而「认领完下一趟直接进它」是看得见的行为，
  一条测试就钉住了。
- **这张票实际做了什么：** 向导交出的现场走 `opened_with`（与开场挑中一份走的是同一条），
  记那一下白拿；`opened` 一个字没动。
  `crates/gui/tests/program.rs::向导建完库进主窗口之后那一份被记住下一趟直接进它` 钉着。
- **谁来裁：** 收尾
- **状态：** settled（第四轮收口，2026-09-09）


### Q408 — 加根那一条判断在向导里走两遍：先在一份内存库上拦一次，过了才落盘再加一次

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。先建后拦那条路上「删掉」得赶在连接放下之后、还要收拾 `-wal`/`-shm` 与刚建出来的沉淀库，失手一次留下的就是一份零根空库：它永久占着开场一行，人重走一遍还会在第一步撞上自己刚留下的那个名字。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/claim.rs::Wizard::claim` 与 `Wizard::在内存里试一遍`
  （两遍走的都是 `crates/gui/src/roots.rs::add_root_from_fields`）
- **为什么没停线：** 两遍走的是同一个函数（`roots::Screen::add_root`），拦下的判据与
  说出来的话都只有一处；多花的是一次内存库的开销。
- **这张票实际做了什么：** 先拦后建。第二条路是先建后拦、拦下时把刚建的中立库删掉——
  它站得住（少一份内存库），没走是因为那条路上「删掉」得赶在连接放下之后、还要收拾
  `-wal`/`-shm` 与刚建出来的沉淀库，而失手一次留下的就是一份零根空库：它永久占着开场
  一行，人重走一遍向导还会在第一步撞上自己刚留下的那个名字。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）


### Q409 — 向导走完落在任务屏，而 ADR-0002 说「打开工具就是待确认队列」

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。ADR-0002 说的是**默认那一屏**（`View::default()`，一个字没改）；刚建出来的库队列屏这时候只有一句空话，而人刚按下的那一下正在任务台上跑着。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/app.rs::App::claim_first_root`（末尾 `self.view =
  View::Tasks`）；对面是 `docs/adr/0002` 与 `crates/gui/src/app.rs::View` 上那句
  「打开工具就是它」
- **为什么没停线：** ADR-0002 说的是**默认那一屏**（`View::default()`，一个字没改）；
  这一处是认领走完那一下的落点，而票面第 2 条要的正是「立刻看进度、按得下停下」。
- **这张票实际做了什么：** 认领走完落在任务屏上。第二条路是照旧落在待确认队列上——
  它站得住（少一条特例），没走是因为一份刚建出来的库里一个变体都没有，那一屏这时候
  只有一句空话，而人刚按下的那一下正在任务台上跑着。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）


### Q410 — 「开始扫描」按下之后到进主窗口之间那一小段失败时，人落在库屏上；没有测试钉着

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。走到那儿要求「向导刚拦过一遍之后那个目录又被挪走」，从界面上按不出来；那句话照旧摆在库屏上，不吞。删库退回向导那条更贵——删库这件事整个仓库一处都没有。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/app.rs::App::claim_first_root` 里 `add_root` 交出 `None`
  那一支
- **为什么没停线：** 走到那儿要求「向导刚拦过一遍之后那个目录又被挪走」——从界面上按
  不出来，而那句话照旧摆在库屏上，不吞。
- **这张票实际做了什么：** 换到库屏，让那句话被看见，库留着（这时候它已经建出来了）。
  第二条路是把刚建的库删掉、退回向导——站得住，但删库这件事整个仓库一处都没有
  （开场连删库入口都不给：删错一份 1 GB 的库没有后悔药），不为一条按不出来的路开这个头。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）


### Q411 — 「这个主库名被占了」这条判断长在向导里，核心库没有这个函数

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。折文件名那条算法本身在核心库（`workspace::catalog_path` + `Slug::Named`），界面只多问一句 `exists()`；而那个 `catalog_exists` 只会有一个调用方，内容就是 `catalog_path(..).exists()`。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/claim.rs::Wizard::named`
  （`workspace::catalog_path(...).exists()`）
- **为什么没停线：** 票面明写「靠工作目录里那个文件在不在」，而折文件名那条算法本身
  是核心库的（`workspace::catalog_path` + `Slug::Named`），界面只多问一句 `exists()`。
- **这张票实际做了什么：** 在向导里问那一句。第二条路是往 `workspace` 里加一个
  `catalog_exists(workspace, slug)`——它站得住（ADR-0005 那条红线按字面读，「这个名字
  能不能用」是领域话），没走是因为那个函数只会有一个调用方，而它的全部内容就是
  `catalog_path(..).exists()`。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）


### Q412 — 「中途关窗」在测试里是「把 `Program` 扔了」，真正的关窗那条路开场这一态没有

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。那一条验的是**磁盘上有没有多出东西**，与程序怎么退出无关——向导手上从头到尾只有三串字。「开场那一态收不收关窗信号」是另一件事。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 规格没说
- **在哪：** `crates/gui/tests/program.rs::向导走到第二步就放弃时工作目录里一个文件都不多`；
  对面是 `crates/gui/src/app.rs::App::handle_close`（关窗那条路只长在主窗口那一态上）
- **为什么没停线：** 这一条验的是「磁盘上有没有多出东西」，而那与程序怎么退出无关——
  向导手上从头到尾只有三串字。
- **这张票实际做了什么：** 两条都验了：把 `Program` 扔掉（关窗），以及按「算了」。
  第二条路是给开场那一态也接上关窗那条路再验一遍——站得住，但那是「开场收不收关窗信号」
  这件事，不是这张票的。
- **谁来裁：** 收尾
- **状态：** settled（第四轮收口，2026-09-09）


### Q413 — 根名按目录名取时那几种边角，向导一条守卫都没加

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。规格「Testing Decisions」明写向导这一层不重复验领域规则本身，而它调的就是库屏加根那个函数（`add_root_from_fields`）；在向导里另说一套措辞就是第二套措辞。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/roots.rs::add_root_from_fields`（`file_name()` 取不出来时
  退成「主库」）与 `romcat_core::path::root_name`（名字不能用时那句话）
- **为什么没停线：** 这几种边角在库屏加根那条路上早就有答案，而向导调的就是那个函数；
  规格「Testing Decisions」明写向导这一层**不重复验领域规则本身**。
- **这张票实际做了什么：** 一条都没加：目录是 `/`（末级名字取不出来）退成「主库」，
  名字带非法字符时报 `RootNameError` 那句原话，两样都由那一处出。第二条路是在向导里
  为这几种各说一句更贴合「这是你第一个根」的话——站得住，但那就是第二套措辞。
- **谁来裁：** 收尾
- **状态：** settled（第四轮收口，2026-09-09）


### Q414 — 验收第 4 条那三种情况里，两种在向导里结构上撞不到

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。向导只加**第一个**根，它试的那份库恒为零根，「根名重复」「与已有的根套在一起」在那上面无从发生；让向导认领已有的库好把那两支撞出来，正是验收第 8 条明说不做的事。票面那条验收写宽了。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 票写错了
- **在哪：** 票 `05` 验收第 4 条（「根名重复、与已有的根套在一起、圈进工作目录三种情况
  都被拦下」）；对面是 `crates/gui/src/claim.rs::Wizard::在内存里试一遍`——向导只加
  **第一个**根，它试的那份库恒为零根。
- **为什么没停线：** 那三条问的都是「库里已经有哪些根」或「工作目录在哪」，而向导调的
  就是库屏加根那个函数（`roots::add_root_from_fields`）：撞得到的那两支撞了，撞不到的
  那两支不是没接上，是这条路上不存在。规格「Testing Decisions」也明写向导这一层
  **不重复验领域规则本身**。
- **这张票实际做了什么：** 撞两支验两支——「圈进工作目录」与「根名不能用」各一条测试，
  两句话都逐字来自核心库那一处；「根名重复」「与已有的根套在一起」在零根的库上无从发生，
  一条测试都没写。第二条路是让向导认领**已有**的库再加根，好把那两支撞出来——它站得住，
  但那正是验收第 8 条明说不做的事（向导不做成第二条加根的路）。
- **谁来裁：** 收尾
- **状态：** settled（第四轮收口，2026-09-09）


### Q415 — 本轮第三次撞上词表 `_Avoid_`，而这一次是动手前避开的

**裁决（第四轮收口，2026-09-09）：记** —— 不用做（`Step` 动手前就改成了 `Asking`）。它是**这条唯一防线生效的一次证据**——靠的是派活时那句「动手前把你要用的词在词表里过一遍」，不是任何自动的东西。门禁那一半进活那一档（与 `Q424` / `Q365②` 同一笔）。

- **来自：** 票 `gui-self-sufficient/05`
- **类别：** 工具缺陷
- **在哪：** `crates/gui/src/claim.rs::Asking`（向导那个私有枚举，原本叫 `Step`）；
  词表在 `CONTEXT.md` **工序**那一条，`_Avoid_` 里逐字列着「步骤」，而 `Stage` 在本仓
  已经是**工序**（`romcat_core::stage::Stage`）。
- **为什么没停线：** 改个私有枚举的名字，一次替换的事。
- **这张票实际做了什么：** 动手前照票面要求把要用的词在词表里过了一遍，`Step` 当场改成
  `Asking`（屏上照旧说「第一步」「第二步」——那是票面自己形容这条向导的话，不是工序）。
  **值得记的是差别**：这是本轮第三次撞上 `_Avoid_`——前两次是 `Q424` 的 `job_of` 与
  `Q439` 的「输出目录」，两次都是**事后**才发现的；只有这一次是动手前避开的，而它靠的
  是派活时那一句「动手前把你要用的词在词表里过一遍」，不是任何自动的东西。
  这条记在 `Q365`／`Q403`／`Q424`／`Q426` 那笔「票、规格、词表之间没有一致性检查」的
  账上：一道拿 `_Avoid_` 扫新标识符的门禁挡得住前两次，而它挡不住的部分（屏上的措辞）
  仍旧只能靠人念一遍。
- **谁来裁：** 拿主意的人
- **状态：** settled（第四轮收口，2026-09-09）

