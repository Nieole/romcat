# 02 — 把启动逻辑抬进一个可测的顶层类型

**What to build:** **行为一字不变的重构**，为 03 铺路。启动那条路——解析参数、定位要开哪份库、开出**现场**、进主窗口——现在摊在程序入口里，够不着测试。把它抬进一个持有两态（开场中 / 已开库）的顶层类型，入口只剩参数解析与开窗。

眼下那两态只用得上第二态，第一态是 03 要填的位子。这一张交付的是**接缝**：开场到主窗口那段过渡从此可测，而 03 就成了"给顶层类型加一个状态"这样一次容易的改动。

`App` 保持公开、拿的仍是一份已经开好的现场——**五屏一行都不改**。让 `App` 自己兜住"还没有库"，就得把现场变成可空的，那个可空会渗进五屏每一处判空（ADR-0023 否决过这条）。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 启动那条路抬进顶层类型，程序入口只剩参数解析与开窗
      —— `crates/gui/src/program.rs` 新增 `Program`，持两态（开场中 / 已开库）。
      `Program::start(&Locate)` 一条龙走完「定位要开哪份库 → 开出现场 → 进主窗口」，
      工作目录也由它自己从三种给法里折（挂单 `Q378`）。`main()` 现在只剩五句：
      `Args::parse()`、`--font-check`、实测那几条、`args.start()`、`run_native`。
      **带保留**：`--demo` 那一支（造合成现场、换标题里那个库名）仍留在 `main.rs` 的
      `Args::start` 里。它在 `demo` feature 之后、默认不编进交付物，抬进 `program`
      会让假数据在库里长出一个公开入口——**记在挂单 `Q379`**，那一支往哪儿去由票 `03`
      或收尾裁。`/code-review` 的 Spec 轴把这一条判为「半过」，判词与这里一致。
- [x] 三种给法（主库根 / 库名 / 直接给中立库文件）行为与改动前完全一致
      —— `crates/gui/tests/program.rs::三种给法开出来的是同一份库`：三条路各开一次，
      `--library` 与 `--catalog` 指着同一个文件时**开出来的窗口标题逐字相同**
      （两条路折出两个名字的话，同一份库裁出来的路径锚会记在两个名字下），
      主库根那条开出的是它自己那一份。改动前后的等价性另有一道人工核对：
      `/code-review` 的 Spec 轴逐段对读了 `b1f1670:crates/gui/src/main.rs` 与现在这一份，
      结论是三种给法、`--demo`、`--bench*`、`--font-check` 全部等价。
      `Args::workspace_dir` 收窄成 `#[cfg(demo)]` 是安全的：`given()` 为真时它本来就是
      转调 `Locate::workspace_dir`，而非 demo 的 `default_dir` 那一支不可达。
- [x] 一个参数都不给时，照旧报"说清要开哪份库"那句话并退出——**这一张不改这个行为**
      —— `NO_LIBRARY` 那五行字**逐字搬进 `program.rs`，一个字符都没改**。
      `crates/gui/tests/program.rs::一个参数都不给就说清要开哪份库` 钉着它，
      断言用的是**字面量而不是引那个常量**：引常量的话两边同一个出处，谁把那句话改成
      别的，两边一起变而测试照样绿；这一条要钉的正是「人看见的还是那句话」。
      **另跑过一次二进制探针**（不采信测试）：`./target/debug/romcat-gui` 不带参数，
      屏上五行与改动前逐字相同、**退出码 1**。
- [x] 现有 GUI 测试一行不改、全部通过
      —— **一行都没改**：`git diff --stat b1f1670 -- crates/gui/tests/` 里只有新增的
      `program.rs` 一个文件，既有九个测试文件与 `shared/mod.rs` 全是空 diff。
      **实测条数**（`-- --list` 数出来的，不是估的）：`cargo test -p romcat-gui
      --all-features -- --list` = **199 条**（既有 **195** + 本票新增 **4**），
      `cargo test -p romcat-gui --all-features` **199 条全过、0 失败**。
      全仓门禁那一趟 `cargo test --workspace --all-features` = **1,726 条全过、0 失败**。
- [x] 顶层接缝上有新测试：给一份现成的库，走完启动拿到主窗口，窗口标题写着那一份
      —— `crates/gui/tests/program.rs::给一份现成的库就走完启动进主窗口`：临时目录里
      现建一份中立库 → `Program::start` → 断言窗口标题里写着**那一份的主库名**
      （`Site::library`，也就是中立库的主文件名），再跑一帧断言顶栏上五屏的名字都画出来了。
      同一个文件另有三条：三种给法、不给参数、库不在。四条**全从公开接口进去、断言屏上
      看得见的东西**，一处内部字段都没戳——照 `tests/close.rs` 与 `tests/roots.rs` 的形态。
      **主库一个字节都不碰**：中立库一律现建在临时目录里（ADR-0004）。
- [x] `App` 与五屏的公开接口一处不动
      —— `git diff b1f1670 -- crates/gui/src/app.rs` **为空**，五屏那五个模块一个都没进
      diff。`App` 照旧拿一份**已经开好的现场**：让它自己兜住「还没有库」就得把现场变成
      可空的，那个可空会渗进五屏每一处判空（ADR-0023 否决过这条）。
      顺带记一条：`App` 那份 `impl eframe::App` 从此没有调用方了（`run_native` 收的是
      `Program`），但删掉它就是动 `App` 的公开接口，本票明令不动——**挂单 `Q382`**。

## 收尾（本票自己写）

**门禁**：`cargo xtask gate` 五条全绿，墙钟 11m59s（这一趟与票 `01` 那棵树的门禁并行跑，
比 `main` 上单跑的 8m36s 长，属正常）。五条各自的结果：

| 条 | 结果 |
|---|---|
| `cargo fmt --all --check` | 绿 |
| `cargo check --workspace`（默认特性，交付出去的那份） | 绿 |
| `cargo clippy --workspace --all-targets --all-features` | 绿，零告警 |
| `cargo test --workspace --all-features` | 绿，**1,726 条全过、0 失败** |
| `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --all-features --lib --bins` | 绿 |

改到的文件五个：`crates/gui/src/program.rs`（新）、`crates/gui/tests/program.rs`（新）、
`crates/gui/src/main.rs`、`crates/gui/src/lib.rs`、`crates/gui/src/site.rs`（只改两处措辞，
见 `Q381`）。

## 挂单

号段 `Q376`–`Q385` 用掉 7 个（`Q383`–`Q385` 空号，不复用）。全文在 `.scratch/PARKING-LOT.md`：

| 号 | 一句话 | 谁来裁 |
|---|---|---|
| `Q376` | 顶层类型叫 `Program`，不叫 `Startup` | 票 `03` |
| `Q377` | 两态摆成私有枚举，开场那一支挂 `#[expect(dead_code)]` 留位 | 票 `03` |
| `Q378` | 接缝的入参只有 `Locate`，工作目录由它自己折 | 票 `03` |
| `Q379` | 合成数据那一支留在 `main.rs`，没跟着抬进顶层类型 | 拿主意的人 / 票 `03` |
| `Q380` | 全仓测试条数那两处（README 与 `gui/Cargo.toml`）没跟着改 | 收尾 |
| `Q381` | `Locate` 上那句「不给就是合成数据」是反话，顺手改了措辞 | 收尾 |
| `Q382` | `App` 那份 `eframe::App` 实现没人用了，但删它会动公开接口 | 票 `03` / 收尾 |

## 给票 `03` 的接缝说明

顶层类型是 `romcat_gui::program::Program`；两态是一个**私有**枚举
`Stage::Opening`（开场中，这一趟只留位、不构造）与 `Stage::Opened(Box<App>)`（已开库）。

**接缝就是 `Program::start` 里的这一句**：

```rust
if !locate.given() {
    return Err(NO_LIBRARY.to_string());   // ← 票 03 把这一句换成交出 `Stage::Opening`
}
```

- `Opening` 那一支上挂着 `#[expect(dead_code, reason = …)]`——**一构造它，那条期待就落空，
  编译器当场提醒把那三行属性删掉**。
- `Program::window_title()` 在开场那一态返回 `"romcat"`（还没有库、也还没有屏），
  `Program::ui()` 在那一态什么都不画：两处都是留给 `03` 的位子。
- 开场交出一份现场之后换主窗口，走的是 `Program::opened(App::new(site, 工作目录))`
  ——那个构造器已经在了（合成数据那一路正用着它）。
- `Opened` 必须装箱：不装箱 `clippy::large_enum_variant` 当场红（`all = deny`）。


## 挂单裁决（第四轮收口，2026-09-09）

第四轮收口的范围是 `Q364`–`Q448`（记下来的 66 条）。**本票名下 7 条，全部 `settled`**——判据是**一条挂单有了主人就算收了，不是等它做完**。
分档：**记 6 ／ 岔 0 ／ 活 1**（记＝发现了一件事、没人欠动作；岔＝一次真的选择，已由拿主意的人当场裁；活＝一处改动，要一张票接）。
全文迁自 `.scratch/PARKING-LOT.md`，每条顶上多一行**裁决**。

### Q376 — 顶层类型叫 `Program`，不叫 `Startup`

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。票 `03` / `04` / `05` 都在这个名字上盖过东西了，「这个类型活得比启动长」这条判断成立——`Startup` 会让人以为它开完窗就退场，而那正是 `App` 的位置。

- **来自：** 票 `gui-self-sufficient/02`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/program.rs:67`（`pub struct Program`）、`crates/gui/src/lib.rs:99`
- **为什么没停线：** 票只说「顶层类型」，没起名；改名是一次 `sed`，翻案不出这张票。
- **这张票实际做了什么：** **叫 `Program`（程序本体），模块 `program`。** 另一条站得住的是
  `Startup` / `startup`——票面自己管这段叫「启动那条路」，名字直接对上。没选它，是因为
  这个类型**活得比启动长**：票 `03` 之后「换一份库」要回到开场，那时它是整个进程活着期间
  一直在的那个东西，而 `Startup` 会让人以为它开完窗就退场（现实里那正是 `App` 的位置）。
  词表里 `启动屏` / `首屏` 是**开场**的 `_Avoid_` 词，`Startup` 离它们只有一步。
  第三条 `Shell` 直接否掉：`lib.rs` 开头「界面壳」说的是整个 crate。
- **谁来裁：** 票 `gui-self-sufficient/03`（它是第一个在这个名字上盖东西的人）
- **状态：** settled（第四轮收口，2026-09-09）


### Q377 — 两态摆成**私有**枚举，开场那一支挂 `#[expect(dead_code)]` 留位

**裁决（第四轮收口，2026-09-09）：记** —— 不用做，两处都挑对了。票 `03` 一构造 `Opening`，`#[expect]` 那条期待自己就落空了（换成 `allow` 会一直躺着没人管）；私有那条纪律也守住了——本轮没有一条测试写成 `matches!(program, Stage::…)`。

- **来自：** 票 `gui-self-sufficient/02`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/program.rs:44`（`enum Stage`，`Opening` 那一支上的 `#[expect]`）
- **为什么没停线：** 三条路都编得过、都跑得绿，差别在测试能戳多深；改一处属性是一次编辑。
- **这张票实际做了什么：** **私有枚举 + `#[expect(dead_code, reason = …)]`。** 私有是为了
  **测试戳不进来**：这个仓库的 GUI 测试形态是「从公开接口进去、断言屏上看得见的东西」，
  枚举一公开，下一条测试就会写成 `matches!(program, Stage::Opened(_))`——那是在验内部状态。
  `#[expect]` 而不是 `#[allow]`：票 `03` 一构造 `Opening`，这条期待就落空，编译器当场提醒
  它把那三行删掉；`allow` 会一直躺在那儿没人管。另一条站得住的是**把 `Stage` 公开**
  （那样连 `#[expect]` 都不用，公开枚举的变体不算死代码），代价正是上面那条测试纪律。
  第三条 `Program { app: Option<App> }` 否掉：ADR-0023 明写可空会渗开，
  两态有名字才说得出「开场中」是什么。
  ⚠️ 顺带记一条**实测**：`Opened` 那一支**必须装箱**，不然 `clippy::large_enum_variant`
  当场红（`all = deny`）——两态一个几乎空、一个是整扇窗。
- **谁来裁：** 票 `gui-self-sufficient/03`
- **状态：** settled（第四轮收口，2026-09-09）


### Q378 — 接缝的入参只有 `Locate`，工作目录由它自己折，不再从 `Args` 递

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。票 `03` 照条目写下的代价自己拼了 `Locate` 交进来，没有回头加第二个构造器；`--catalog` 那条反推从此只有一个出处。

- **来自：** 票 `gui-self-sufficient/02`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/program.rs:80`（`Program::start(locate)`）；对面是
  `crates/gui/src/main.rs:255` 的 `Args::workspace_dir`（现在只剩合成数据与实测那几条要它）
- **为什么没停线：** 两条路在「开现成的库」这一支上折出来的工作目录**一模一样**
  （`Args::workspace_dir` 在 `given()` 为真时就是转调 `Locate::workspace_dir`），
  所以选哪条都不动行为。
- **这张票实际做了什么：** **`Program::start` 只收 `Locate`，工作目录自己折。** 另一条是
  `Program::start(locate, workspace)`——把工作目录当参数递进去，谁调谁负责。没选它，是因为
  那正是这一段原来的形状：`main.rs` 里 `Args::workspace_dir` 与 `Locate::workspace_dir`
  各算一遍，靠「`given()` 为真时两者相等」这条不成文的巧合对齐。收成一处之后
  `--catalog` 那条反推（`工作目录/catalog/某某.sqlite3`）只有一个出处。
  代价：**票 `03` 的开场要开一份库时，得自己拼一个 `Locate` 交进来**（它手上有的是一条
  中立库完整路径，正好是 `Locate.catalog`），或者那时再加一个收 `Site` 的构造器。
- **谁来裁：** 票 `gui-self-sufficient/03`
- **状态：** settled（第四轮收口，2026-09-09）


### Q379 — 合成数据那一支留在 `main.rs`，没跟着抬进顶层类型

**裁决（第四轮收口，2026-09-09）：记** —— 不用做。`program` 模块里一处 `demo` 都没有，那正是它留在 `main.rs` 的全部理由——「交付出去的客户端里不该有一个字节的假数据」。

- **来自：** 票 `gui-self-sufficient/02`
- **类别：** 票写错了
- **在哪：** `crates/gui/src/main.rs:214`（`#[cfg(feature = "demo")] fn start`）
- **为什么没停线：** 票面那句「程序入口只剩参数解析与开窗」说的是**交付出去的那条路**，
  而 `--demo` 是量数用的脚手架（`demo` feature 默认不编进来），留在入口不影响验收。
- **这张票实际做了什么：** **交付那条路整条抬进 `Program::start`，`--demo` 那一支留在
  `Args::start` 里**，它造一份合成现场、换掉标题里那个库名、再包成 `Program::opened(app)`。
  另一条是把它也抬进 `program` 模块（`#[cfg(feature = "demo")] Program::demo(rows)`）——
  站得住，入口会更干净。没选它，是因为那会让**默认不编进来的假数据**在库里长出一个
  公开入口，而 `demo` 模块存在的全部理由是「交付出去的客户端里不该有一个字节的假数据」。
  眼下这个形状：`program` 模块里一处 `demo` 都没有。
- **谁来裁：** 拿主意的人（或票 `03`，它会重写 `Args::start` 那一支）
- **状态：** settled（第四轮收口，2026-09-09）


### Q380 — 全仓测试条数那两处（README 与 `crates/gui/Cargo.toml`）这一趟没跟着改

**裁决（第四轮收口，2026-09-09）：记** —— **就地改掉了**三处陈旧的数（`cargo test --workspace [--all-features] -- --list` 实测于 `main` `a142612`）：全特征 **1,815 条 / 70 个目标**、不带 `--all-features` **1,692 条 / 63 个目标**、少跑 **123** 条；顺带把 README 那句票数从 `59/63` 改成 `87/90`。⚠️ **「没有任何东西钉住这两个数」是常设那一半，这是第三次记了**（`Q198`、`Q268`、这一条）——进活那一档。

- **来自：** 票 `gui-self-sufficient/02`
- **类别：** 路过发现，不在范围内
- **在哪：** `README.md` 与 `crates/gui/Cargo.toml` 的 `demo` feature 注释里那一对数
  （`main` 上是 1,722 / 1,600）
- **为什么没停线：** 那两个数是文档，改不改都不影响门禁——它们本来就没有任何东西盯着
  （挂单 `Q198` / `Q268` 记着这条）。
- **这张票实际做了什么：** **一个字没动那两处**，本票新增 4 条测试。另一条路是当场把两处
  改成这一趟实测的数——站不住的地方在于：波 1 三张票同时在各自的 worktree 里加测试，
  谁先写谁的数都会在合并那一刻就是错的，而 `Cargo.toml` 那段注释自己就写着
  「合并时由编排者复核」。
- **谁来裁：** 收尾（编排者合完波 1 之后量一次，两处一起改）
- **状态：** settled（第四轮收口，2026-09-09）


### Q381 — `Locate` 上那句「不给就是合成数据」是反话，这一趟顺手改了措辞

**裁决（第四轮收口，2026-09-09）：记** —— 不用做，维持。留一句与 ADR-0023 正相反的话（「不给就是合成数据」）挂在一个走不到的分支上更贵：下一个人读到的是「界面会自己造一份假的」。行为没动（不可达 + 只改文案）。

- **来自：** 票 `gui-self-sufficient/02`
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/site.rs:17`（`Locate` 的文档）与 `:77`（`open()` 的
  `(None, None)` 那一支返回的那句话）
- **为什么没停线：** 那一支**走不到**——三条调用路（`Program::start`、`--font-check`、
  实测那几条）都先问 `given()`。改的是一句反话，不是行为。
- **这张票实际做了什么：** **两处措辞都改了**：文档那行改成「一样都不给的那条路由
  `Program::start` 挡在前面」，返回的那句话去掉「不给就是合成数据。」那半句，并在上面
  写清这一支为什么留着（公开函数，谁都能空手调一次）。另一条路是**一个字不改、只记条目**
  ——票面写着「行为一字不变」，而这是第五个文件。没选它，是因为这一趟正是把
  「一样都不给」那条路收成一处的活：留着一句与 ADR-0023 正相反的话挂在死分支上，
  下一个人读到的是「界面会自己造一份假的」。**行为没动**（不可达 + 只改文案）。
- **谁来裁：** 收尾（若认为第五个文件超范围，回退这一处就是一次编辑）
- **状态：** settled（第四轮收口，2026-09-09）


### Q382 — `App` 那份 `eframe::App` 实现从此没人用，但删它会动 `App` 的公开接口

**裁决（第四轮收口，2026-09-09）：活** —— **并入 `Q390`（同一条）。** 两份 `impl eframe::App` 逐字相同，`App` 那份从票 `02` 起就没有调用方。当初挡着它的是票 `02` / `03` 各自「`App` 与五屏的公开接口一处不动」的硬约束——**那两张票都已 `done`，挡的人没了**。

- **来自：** 票 `gui-self-sufficient/02`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/src/app.rs:533`（`impl eframe::App for App`）；对面是
  `crates/gui/src/program.rs` 末尾那份同形的实现
- **为什么没停线：** 留着它不影响任何行为，也不报告警（trait 实现不算死代码）。
- **这张票实际做了什么：** **一个字没动 `app.rs`。** `eframe::run_native` 从此收的是
  `Program`，`App` 那份实现没有调用方了，两份实现逐字相同。另一条站得住的路是当场删掉
  `App` 那一份——站不住的地方在票面：「`App` 与五屏的公开接口一处不动」是本票的验收条目，
  而去掉一个 trait 实现正是动公开接口。
- **谁来裁：** 票 `gui-self-sufficient/03`（它动这一带时顺手删；或收尾时单独裁）
- **状态：** settled（第四轮收口，2026-09-09）

