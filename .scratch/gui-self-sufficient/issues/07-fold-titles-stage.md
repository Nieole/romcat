# 07 — 折标题这道工序

**What to build:** 工序段加第二行：**折标题**。点一下就跑，不必去终端。

那一行要说得出**还差多少**——方向是"标题集合变过、但还没重折的作品数"。这个度量**没有现成实现**，需要新造。

**退路是这一张的一部分，不是失败**：度量比预想的贵（比如要全表比对才算得出来），那一行就退回显示上次跑的时刻，**不为了整齐去加一张变更计数表**。退路只对这一支生效——不能因为这支退了，就把 06 里那个白拿的识别数字也降级成时刻。

跑完之后浏览屏上的**显示标题**跟着更新——那是这道工序起没起作用唯一看得见的地方。

**Blocked by:** 06

**Status:** done

- [x] 工序段上折标题一行 —— `Stage::FoldTitles` 进了 `Stage::ALL`（现在两项）。
      GUI 测试 `工序段上折标题一行_画的是上次跑的时刻` 在屏上读到「折标题」与「工序 · 2 道」；
      核心测试 `工序段一道工序一行_次序就是那张全部工序的名单` 钉住次序。
- [x] 那一行说得出还差多少；算不出来时显示上次跑的时刻，并且识别那一行不受影响
      —— **走的是退路**（票面认可的那一条）：度量要全表比对，实测 `title::fold` 一趟
      73–81 毫秒 / 2,000 个作品，折算真库 1.7 秒上下，而 `Stages::survey` 跑在画帧线程上
      （一帧 16 毫秒）；细账与另外两条拦路的理由在挂单 `Q426`。
      **「上次跑是几点」那条数据通路由这张票带进来**：`Catalog::titles_folded_at`
      （元数据表加一个键，纯加），**只有真跑了这道工序才打戳**（`title::run_task` 末尾，
      不是 `refold`——`scrape::zh::judge` 顺手折回集合那一下不算跑过这道工序，
      `顺手把集合折回来不算跑过这道工序` 钉着它，挂单 `Q428`）。
      库读不动与还没折过是两句话，不都画成「还没跑过」（`fold_titles_row`）。
      核心测试 `折标题那一支退回上次跑的时刻_而识别那一行照旧报数` 与 GUI 那两条
      各验一遍「识别那一行不受影响」。
- [x] 点一下把折标题排上任务台，报得出进度、按得下停下 —— `App::start_stage(Stage::FoldTitles)`
      是唯一入口（票 `09` 的捷径照走它）。长入口收把手落在核心库：`title::run_task`
      三步，`title::TASK_STEPS` 报总步数，界面那一侧加自己那一步「读优先级表」。
      **报得出进度**：`折标题这一趟报得出走到第几步`（核对 `steps` / `at` / 步名 /
      `fraction` 说得出话）＋ `折标题读不动优先级表时如实拒绝_并说清停在哪一步`
      （从任务台历史上读回步名——那句话取自把手上的进度）。
      **按得下停下**：`折标题按停时清空那一下压根没发生_标题集合一条都没少`
      （拿一个一开始就停着的把手，验它交出「被按停了」那一支且中立库一个字节都没动）
      ＋ `折标题排着队被撤掉时标题集合一条都没少`（排着队就被撤掉那一条）。
- [x] 跑完之后浏览屏上的显示标题跟着更新 —— `折标题跑完之后浏览屏上的显示标题跟着更新`：
      折之前退回作品名 `Contra`，折之后是盘上那个中文名 `魂斗罗`。
      `App::poll_tasks` 那个 `match` 走 `browse.refresh`（只有它连详情面板那一格一起重读）。
- [x] 收场四档在任务台历史上各画各的 —— 三档有测试：**跑完了**（`Ending::Done`）、
      **停了什么都没留下**（`Ending::Stopped`，上面那两条按停测试）、**出错了**
      （`Ending::Failed { step, why }`，坏优先级表那条）。
      ⚠️ **保留**：第四档**停在半路**这一支不会出现——写回是「清掉再写回」，
      最后一个停下点摆在写回**之前**，那一趟要么写完、要么一个字节都没写。
      挂单 `Q427` 记着这条与票面「验它按停在半路记账」的出入。
- [x] 库变了之后那个数跟着变（度量做得出来时） —— **括号里的前提不成立，这一支走了退路**。
      跟着变的是**时刻**：折过一趟之后那一行从「还没跑过」改口成「上次跑是 ⋯」
      （核心测试与 GUI 测试各钉一遍）。度量真被认下来时那一行该说的话已经钉在
      `折标题的度量真做出来时那一行说的是几个作品还没重折`。挂单 `Q426`。
