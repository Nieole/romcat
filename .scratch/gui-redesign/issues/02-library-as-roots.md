# 02 — 主库变成一组根

**What to build:** 在**库**屏上按「添加目录」加进第二块盘，按扫描，在任务台里看着它跑完——两个根的变体落进**同一份中立库**，从此一起管理。

**主库**不再是一个目录，是**一组根**。每个根有自己的名字、路径、上次扫描的时刻与结果。**变体的键**因此变成「根名 + 相对根 + 路径」。

库屏这一半还要摆出**数据源**：DAT 仓库、中文离线源、Switch 数据库各有多少条记录、上次什么时候取的、一键重取。根与数据源同屏，是因为它们是同一件事——**让识别能干活的原料**。新用户最容易卡的「扫完了怎么没认出来」，在这一屏上一眼看得见：某个源那一行是空的。

**这是唯一动数据模型、也是唯一要求重扫的一票。** 路径锚会全部失效（**内容锚不受影响**）。真库现在 0 条裁决、0 条人工纠正，这是它最便宜的一刻。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 库屏上加一个目录、按扫描，任务台里跑得完，变体进了同一份中立库
- [ ] 两个根的变体**互不覆盖**，各自的键带得出自己的根名
- [ ] 同一个根换了挂载点仍然被拦下（那道闸是防指错盘的，**按根生效**）
- [ ] 新根落在已有根内部时被拒绝，且说清为什么
- [ ] 新根落在工作目录里时被拒绝（中立库不许被圈进主库，ADR-0004）
- [ ] 移除一个根时明确告知会去掉多少变体
- [ ] 盘没挂上时这个根的**上次结果仍然看得见**
- [ ] 库屏列出三个数据源的记录数与上次取回时刻，能一键重取（走任务台）
- [ ] 还没取回的数据源被明确标出来
- [ ] 中立库结构版本 +1；旧版本按既有约定处理（可再生，删库重扫）
- [ ] 键的形状仍按 ADR-0020：NFC 归一，读盘用磁盘上的原始形态
- [ ] **主库一个字节都没写、没 touch 时间戳**
- [ ] 门禁命令全绿（带 `--all-features`）
