# 13 — 作品级主列表的翻页代价

**What to build:** 让浏览屏那张表在真库规模上翻得快——眼下一次翻页的代价与库有多大成正比，而且搜索框里打了字之后还要再贵一倍出头。

两条挂单说的是同一件事的两半：

- **Q64**：主列表按**聚合列**排序。「这个作品有几个变体、合计多大、横跨哪几个平台」要先看过它的每一个变体，于是 `GROUP BY` 折成行、再按聚合出来的列排序，**两步都没有索引接得住**。变体表那一层每条 `ORDER BY` 都有一条索引正好接住（`variant_bytes_key` 那几条），一次翻页是索引倒着扫；作品级这一条不是。
- **Q108**：搜索那三条命中路（名字 / 标题集合 / 简介）在 `WHERE` 里是三条 `OR`，给每一行多加两次索引探查。实测（dev profile，合成数据 46,483 个变体收敛成 3,596 行）：不搜 `work_page` 208 ms，`work 0`（只有别名命中）425 ms。

**内存那一半一点没退**（一扇窗还是 512 行，`MAX_PAGE` 还在），涨的是每次翻页的时间。这张票要治的只是时间。

**先量再改。** 眼下手上的数一半是 release、一半是 dev，两者不可直接比（Q64 的表是 release，Q108 的是 dev）。第一步是在同一个 profile、同一份数据上把两条一起量一遍，得出「慢的到底是 `GROUP BY` 还是 `ORDER BY`，还是那三条 `OR`」。

**几条能走的路**（不预先定死，量完再挑）：

- 给作品级那几样聚合结果**落一张物化表**（变体一变就失效重算），把翻页变回一次索引扫。
- 只给**默认排序**（按作品名）铺索引，别的列排序照旧慢——真库上人多半不换排序。
- 搜索那三条 `OR` 拆成三次 `UNION ALL` 再去重，让每一条各自吃到自己的索引。

**不许动的**：一扇窗 512 行那条线、`MAX_PAGE`、以及票 03 立的「按当前筛选」口径。**主库只读**（ADR-0004）。

**Blocked by:** 无 —— 可立即开工

**Status:** done

- [x] 同一个 profile、同一份数据上把 `work_total` / `work_page` 的代价量一遍，分得清慢在哪一步
  —— **release、同一台机器、同一份合成数据**（46,428 变体 → 10,978 行）。量法落成常设的
  `--bench-paging`（`crates/gui/src/bench.rs::paging`；命令行那一条与合成数据都在
  `demo` feature 之后，`bench` 模块本身照旧不带 feature 门——那是这个仓库既有的样子）。
  拆开各量一遍的结果：只扫变体表 2.4 ms → ＋`GROUP BY` 17.1 → ＋十列聚合 35.1 →
  ＋`ORDER BY` 67.3 → ＋年份那张 join 88.3；搜索那三条各自 18.8 / **51.9** / 30.4 ms。
  **四处都咬人，份额头一次量出来**，见 `docs/library-facts.md`「慢在哪：拆开各量一遍」。
- [x] 数进 `docs/library-facts.md`，与其他事实同一个格式、带日期
  —— 新一节「作品级主列表翻一页要多久（2026-09-06，票 `gui-redesign/13`）」，
  带改前改后对照表、拆解表、走了哪条路、以及**变慢的那一处**。
- [x] 真库规模（46,428 变体 → 10,978 行）上一次翻页的时间比现在明显短，报出前后两个数
  —— **默认那一行：取第一页 94.3 → 25.0 ms，取最后一页 95.2 → 32.2 ms；
  数总行数 17.5 → 6.4 ms。**（改前改后交替各三趟取中位数。）
- [x] 搜索框里打了字之后的翻页代价与不搜时在同一量级
  —— 改前 94 对 142 毫秒，改后 **25 对 58–84 毫秒**；一条都不命中那个**上界**
  114.6 → 30.3 ms。**保留一处**：筛得很窄又同时在搜时 15.3 → 21.7 ms（挂单 `Q155`）。
- [x] 内存那一半不退：一扇窗还是 512 行，`window().retained()` 不涨
  —— `table::SPAN` 一个字没动；`--bench-browse` 改前改后都是「滚一趟读库 0 次；
  内存里始终 472 行」（那一趟筛完剩 472 行，窗装不满）；
  `crates/gui/tests/table.rs` 那条「一千行与十万行内存里一样多」原样绿。
- [x] 挂单 Q64、Q108 标为 resolved，写清走了哪条路 —— 见下面两条的「**收尾裁决**」。
- [x] 门禁命令全绿（带 `--all-features`）
  —— clippy 无输出；`cargo test --workspace --all-features -j 1 -- --test-threads=2`
  一趟全量 **58 个 `test result: ok`、1,558 条通过、0 失败**（基线 1,557，这张票加了 1 条）。


## 挂单裁决

从 `.scratch/PARKING-LOT.md` 迁来。那份挂单是这一轮队列的**待办队列**，收尾时清空；
裁决之后条目迁回它所属的票，编号 `Qn` 留着占号、不复用。

### Q64 — 作品级主列表按聚合列排序，代价与库有多大成正比

- **来自：** 票 `gui-redesign/03`
- **类别：** 这一票新引入的代价（票只说「十万行级别下翻得动」，没说甩滚动条那一种）
- **在哪：** `crates/core/src/catalog/browse.rs::Catalog::work_page`
- **为什么没停线：** 这是「一个作品一行」这件事的**内在**代价，不是可以绕开的实现细节：
  「这个作品有几个变体、合计多大、横跨哪几个平台」要先看过它的每一个变体。变体表那一层
  每条 `ORDER BY` 都有一条索引正好接住（`variant_bytes_key` 那几条），一次翻页是索引
  倒着扫；作品级这一条要先 `GROUP BY` 折成行、再按聚合出来的列排序，两步都没有索引接得住。
  **内存那一半一点没退**（一扇窗还是 512 行，`MAX_PAGE` 还在），涨的是每次翻页的时间。
- **这张票实际做了什么：** 走「查询整个下推 + 每页再补两样」这条路——`GROUP BY` 与
  `ORDER BY` 全在 SQLite 里，「元数据齐不齐」与「最高置信度」这两样**排不了序**，
  所以只对这一页那几十行补（`fill_missing` / `fill_confidence`），不摊到全库上算。
  `work_total` 那一条也不连年份那张表——它只数行。实测（release，`--bench`）：

  | 主列表行数 | 常态滚动（每帧 3 行） | 一趟甩到底（每帧都读库） |
  |---|---|---|
  | 46,483 | 中位 **0.85 ms**（1,173 fps），240 帧读库 **1 次** | 中位 **114 ms** |
  | 100,000 | 中位 **0.74 ms**（1,344 fps），240 帧读库 **1 次** | 中位 **317 ms** |

  真库的形状比这两行都轻：46,483 个变体收敛成 3,596 行时，换一次筛选 **44 ms**、
  点开一行 **21 ms**、每帧中位 **3.26 ms**（`--bench-browse`）。上面那两行是**每个变体
  各自一行**的极端形状（合成数据一个作品都没认出来），也就是这条代价的上界。
- **值不值得治：** 常态滚动没问题，咬人的只有「拖着滚动条一路甩到底」那 3 fps。
  两条路：把窗口开大（读得少但一次读得更久，且现有测试拿「一千行与十万行内存里一样多」
  钉着窗口大小），或者甩的时候先画空行、松手再补（那是画法，不是查询）。
  两条都超出这张票。
- **谁来裁：** 票 `12`（布局可调与视觉一致性收尾）
- **票 `12` 裁定：不在这张票里做，升成新票 `gui-redesign/13`。** 它是**核心库的查询代价**，
  与「版式可调与视觉一致性」不沾边；而它与 `Q108`（搜索那三条命中路）是同一条代价线上的
  两半，分两张票各治一半只会各自量一遍。新票把两条合起来：先在同一个 profile、同一份数据上
  量清楚慢在 `GROUP BY` 还是 `ORDER BY` 还是那三条 `OR`，再挑路。
- **状态：** resolved（票 `gui-redesign/13`）
- **收尾裁决：走了三条路，一条否掉。** 量出来「`GROUP BY` 与 `ORDER BY` 两口临时 b 树
  加上年份那张 join」各占多少（15 / 32 / 21 毫秒，十列聚合另占 18），于是：
  1. **给分组键铺一条表达式索引** `variant_group`（`catalog::content`）——分组键的第二项
     是个 `CASE`，索引里没有那个表达式，于是四万多行只能整个塞进临时 b 树。
     把表达式原样写进索引，那口 b 树就没了。**票据说的「只给默认排序铺索引」形状不对**：
     排的是聚合出来的列，任何单表索引都接不住 `ORDER BY`；接得住的是 `GROUP BY`，
     而那条索引**对五种排法一起生效**。
  2. **取一页分两趟**：第一趟只挑「这一页是哪几行」（按哪一列排就多算哪一列），
     第二趟只给这几百行算聚合（`work_page_anchors` / `work_page_totals`）。
     `ORDER BY` 那口排序器搬得轻，同一份数据上 40.6 → 17.3 毫秒。
  3. **年份那张 `LEFT JOIN` 只在按年份排时才连**，画出来的那个并进
     `fill_scraped` 那一趟——它本来就在读同一张表、同一批锚点、同一批 subject。
  **否掉了票据的第一条（落一张物化表）**，理由是口径不是速度：屏上那一行的变体数是
  **按当前筛选算**的（票 `03` 立的口径），物化表只存得下「一条筛选都不加」那一份，
  一加筛选就要回落到活查询——治一种情形，背全套失效维护。
  **改前改后：取第一页 94.3 → 25.0 毫秒，取最后一页 95.2 → 32.2 毫秒。**

### Q66 — 浏览屏顶上那三个批量操作按钮还没画

- **来自：** 票 `gui-redesign/03`
- **类别：** 票之间的接缝（原型上有，这张票的验收里没有）
- **在哪：** `crates/gui/src/browse.rs::edit_panel`（作用范围那一行就是它们的位置）
- **为什么没停线：** 原型 `.scratch/gui-redesign/prototype.html` 的浏览屏抬头摆着
  「刮削选中…」「存成子库」「★ 收藏」三个按钮，而它们各自是票 `10` / `11` / `06`
  的正文。这张票的十条验收一个都没点到它们，票据也点名「详情面板里媒体那一块只留位置」。
- **这张票实际做了什么：** 把**它们要的那样东西**铺到位并钉住语义——
  `Catalog::scoped_variants` / `scoped_variant_total` 把「选中的这几行」展开成一串变体的键，
  界面上常驻一句「选中 N 条、作用于 M 个变体」，`Screen::batch_variants` 是三个按钮
  按下去该拿的那一批。按钮本身留给各自的票，**不提前画一个按了没反应的**。
- **顺带记一条给接它们的人：** `WorkAnchor::Work(id)` 里那个数是 `work` 表的行号，
  **重跑识别会把它换掉**（`clear_identifications` 整批删了重建）。眼下界面上还没有
  「重跑识别」这个按钮，所以咬不到人；哪张票把识别接上任务台，就要在那一趟跑完之后
  把浏览屏的选中与点开的那一行一起丢掉，否则批量操作会打到另一个作品头上。
- **谁来裁：** 票 `06` / `10` / `11` 各接一个
- **状态：** open
- **收尾裁决（第三轮）：settled，但不由本票收——裁决写在票 `gui-redesign/03` 末尾。**
  三个按钮已由票 `10` / `06` / `11` 各自画上（实测见票 `03` 那一节）；
  「顺带记一条给接它们的人」那段拆成挂单 `Q193`，归票 `parking-3/10`。
- **第二轮当时的裁决（留档）：不由本票做，原样留着 open。** 那句「由本票接着做」是迁条目时粘串了行
  （与票 `15` 上 `Q154` 记的是同一种错）：这张票的正文与七条验收从头到尾只说
  **翻页的时间**，一条都没点到那三个按钮，而这条挂单自己那行「谁来裁」写的就是
  票 `06` / `10` / `11`。这张票一个像素都没画。

### Q108 — 搜索那三条命中路让一次翻页贵了一倍出头，与 Q64 在同一条代价线上

- **来自：** 票 `gui-redesign/05`
- **类别：** 这一票新引入的代价
- **在哪：** `crates/core/src/catalog/browse.rs::Search::filter`（`WHERE` 里那三条 `OR`）
- **为什么没停线：** 常态还在可用范围里，而且它与 Q64 是同一件事的两半——作品级主列表
  本来就要扫全表再 `GROUP BY`，搜索只是给每一行多加两次索引探查。
- **实测**（**dev profile**，合成数据 46,483 个变体收敛成 3,596 行；Q64 那张表是 release，
  两者不可直接比）：

  | 搜索词 | 命中行数 | `work_total` | `work_page`（一页 512） |
  |---|---|---|---|
  | 不搜 | 3,596 | 107 ms | 208 ms |
  | `幻想`（名字命中） | 359 | 240 ms | 301 ms |
  | `work 0`（只有别名命中） | 10 | 238 ms | 425 ms |
  | `外星人`（一条都不命中） | 0 | 270 ms | 296 ms |

  最后一行是**上界**：三条 `EXISTS` 每一行都跑满了才得出「一条都没有」。
- **已经做掉的那点便宜：** 三条 `OR` 按**从便宜到贵**排（名字 `LIKE` → 标题集合 →
  简介），SQLite 的 `OR` 短路求值，名字命中的行根本走不到后两条。
  `title` 那条走主键 `(work, …)` 的前缀，`scrape_value` 那条走 `scrape_value_subject`
  索引——两条都不是全表扫。
- **值不值得治：** 眼下不治。真要治，路是**给搜索加一次防抖**（界面上打字每一个字符
  都换一次查询，而这一层每次换查询要跑三条全表查询：`work_total`、
  `scoped_variant_total`、`work_page`），那是**界面**那一侧的事，不是这条查询的事，
  而且它同样会让「换筛选」那条路一起受益。没在这一票加，因为票据没点它，
  而且防抖要定一个时长——那是个产品决定。
- **谁来裁：** 票 `12`（布局与一致性收尾）或收尾
- **票 `12` 裁定：不在这张票里做，与 `Q64` 合起来升成新票 `gui-redesign/13`。**
  两条是同一条代价线上的两半——作品级主列表本来就要扫全表再 `GROUP BY`，搜索只是给每一行
  多加两次索引探查。而眼下手上的两组数一半是 release 一半是 dev，**不可直接比**，
  新票头一件事就是在同一个 profile、同一份数据上重量一遍。
- **状态：** resolved（票 `gui-redesign/13`）
- **收尾裁决：走的是「三条各自吃到自己的索引」那条，但不是 `UNION ALL`。**
  重量一遍（release，同一份数据）之后，三条各自单独跑：名字 18.8、**别名 51.9**、
  简介 30.4 毫秒——**贵在它们是相关子查询**：`EXISTS (… WHERE t.work = work.name …)`
  与外层绑死，SQLite 只能逐个变体行去探一次，真库形状上那是 46,428 次。
  短路求值省不下多少：命中的行只占一成，其余九成三条全跑满。
  改法是把别名与简介两条写成**非相关子查询**（`work.name IN (SELECT …)`）——
  子查询与外层无关，一次算完存进一张临时索引，每一行只剩一次查表；
  查询计划从 `CORRELATED SCALAR SUBQUERY` 变成 `LIST SUBQUERY` ＋ `CREATE BLOOM FILTER`。
  **筛出来的是同一批行**（`靠简介命中的那一行变体数一个都不少` 等测试钉着）。
  名字那一条留着逐行判：它读的是 `variant.key`，本来就没有索引可吃。
  **改前改后**（`work_page` 一页 512）：搜「幻想」120.7 → 58.0、
  搜「Sakuhin 1」141.7 → 83.6、一条都不命中那个上界 114.6 → 30.3 毫秒；
  与不搜（25.0）**落回同一量级**。
  **有一处变慢**：筛得很窄又同时在搜时 15.3 → 21.7 毫秒——非相关子查询那笔扫表开销
  是固定的，与筛剩几行无关。记在挂单 `Q155`，`--bench-paging` 里留了常设探针。


## 挂单裁决（第二轮）

从 `.scratch/PARKING-LOT.md` 迁来；编号 `Qn` 留着占号、不复用。

### Q155 — 筛得很窄又同时在搜的时候，翻一页比改之前多花 6 毫秒

- **来自：** 票 `gui-redesign/13`
- **类别：** 这一票新引入的代价
- **在哪：** `crates/core/src/catalog/browse.rs::Search::alias` / `Search::description`
  （改成非相关子查询的那两条），以及 `Catalog::work_page_totals`（多出来的第二趟查询）
- **为什么没停线：** 它是这张票**换来的**那笔账的另一半，而账是划算的：
  搜索那三条从相关子查询改成非相关子查询之后，`title` 与 `scrape_value` 各要**扫一遍**
  把命中的名字收出来——那是**一次性开销，与筛剩几行无关**。不筛的时候它换掉了
  四万多次逐行探查（141.7 → 83.6 毫秒）；筛到只剩 2,329 行的时候，那笔固定开销就露出来
  （15.3 → 21.7 毫秒）。「取一页分两趟」也占其中一份：多一次查询往返。
- **这张票实际做了什么：** **照做，并把这一处写进 `docs/library-facts.md`**
  （「代价搬到哪儿去了」一节）与 `--bench-paging` 的常设探针
  （「按平台筛（SFC）+ 搜」那两行）——**以后谁再动搜索那三条，这一格会自己说话**。
  没为它加「筛得窄就换一种写法」那种分支：两条路都留在代码里，
  等于让「屏上这一批」有两种算法，而这一层从头到尾在防的正是这个。
- **谁来裁：** 收尾 / 下一张动搜索那一层的票
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q157 — 中立库结构版本没加 1，而这张票给 `variant` 加了一条索引

- **来自：** 票 `gui-redesign/13`
- **类别：** 规格没说（读起来像该加，按判据不该加）
- **在哪：** `crates/core/src/catalog/content.rs` 的 `variant_group`；
  判据在 `crates/core/src/catalog.rs` 的 `SCHEMA_VERSION` 文档「什么算「结构变了」」一节
- **为什么没停线：** 那一节写死了判据——**加 1 的判据是「旧数据会不会被读错」，
  不是「文件里多了点东西」**，并且点名「纯加表」这一档不加 1（票 13 的刮削五张表就是先例）。
  加一条索引比加一张表更轻：已有的表一列没动、一条语义没改，`CREATE INDEX IF NOT EXISTS`
  在打开时就把它补上，旧库照样打得开，拿旧版程序打开也照样能用。
  加 1 会逼所有人删库重扫 37 分钟，换不到任何东西。
- **这张票实际做了什么：** **没加**，版本留在 7。（交接说明里说「现在是 6」——
  那是过期了一版，`traversal_note` 那次已经把它顶到 7。）
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **noted** —— 核对过、不用改，留作记录。

### Q158 — 票 13 末尾「挂单裁决」一节把三条挂单都标成「由本票接着做」，而它只从两条立起来

- **来自：** 票 `gui-redesign/13`
- **类别：** 票写错了
- **在哪：** `.scratch/gui-redesign/issues/13-work-list-paging-cost.md`
  第 73、93、128 行——`Q64`（作品级主列表按聚合列排序）、`Q66`（浏览屏顶上那三个
  批量操作按钮还没画）、`Q108`（搜索那三条命中路）三条各带一句一模一样的
  「**收尾裁决：由本票接着做** —— 它就是从这条挂单立起来的」
- **为什么没停线：** 与票 `15` 上的 `Q154` 是同一种错：迁条目时粘串了行。
  这张票的正文与七条验收从头到尾只说**翻页的时间**，一条都没点到那三个按钮；
  而 `Q66` 自己那行「谁来裁」写的就是票 `06` / `10` / `11`。
- **这张票实际做了什么：** **只把 `Q64` 与 `Q108` 标了 resolved**，`Q66` 原样留着 open，
  并在它的「收尾裁决」那行写明为什么不由本票做。三个按钮一个像素都没画。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **noted** —— 核对过、不用改，留作记录。
