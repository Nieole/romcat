# 11 — 子库屏重做

**What to build:** 子库屏一台设备一张卡：目标路径、前端格式、容量条、待同步步数、同步按钮。**选择集只读展示**——要改按「改选择」，跳回浏览屏，这个子库的规则已经预填在筛选器里；调完按「更新到子库」原样带回。

现在这一屏一屏塞了八样东西（子库列表、名字/路径/格式/容量/类别表单、规则增删、例外记撤、差量步骤表、容量账、同步、删除确认），而且没有明显的先后顺序——用户的原话是「操作逻辑看不懂」。**根因是它把「选什么」和「送到哪」混在了一起。**

拆开之后：**选什么**归浏览屏，**送到哪**归这一屏。于是这一屏从八个概念降到三个：选哪台、配目标、排差量后同步。

容量条分三段看得见：**选中的、清单之外的、上限**。

**Blocked by:** 04

**Status:** done

- [x] 一台设备一张卡，目标路径、格式、容量、待同步步数一眼看得见
      —— `crates/gui/src/sublibrary.rs::card_ui`。卡头是名字 + 待同步步数 + 「改选择…」，
      第二行是「目标路径｜前端格式｜能力档案｜上限」，摊开之后依次是选择集、容量条、差量。
      **待同步步数没排过差量时说「还没排过差量」，不摆一个 0**——「一步都不用做」与
      「还不知道要做什么」是两件事。核对：
      `crates/gui/tests/sublibrary.rs::一台设备一张卡目标格式容量与待同步步数一眼看得见`。
- [x] 容量条三段（选中 / 清单之外 / 上限）各自标得出数
      —— 三个数由核心的 `romcat_core::sublibrary::Gauge` 算（ADR-0005），
      界面只画那根条子与三个图例。**超没超不在条子这儿算**：那由 `over_capacity`
      一处说了算，条子再算一遍就会长出「条子说满了、旁边那行字说没超」。
      **清单之外要目标在位才知道**，没排过差量时是 `None`——如实写成
      「—（排一次差量预览才知道）」而不是 0。核对：
      `crates/core/tests/sublibrary.rs::容量条三段各自说得清而且清单之外分得出没有与不知道`、
      `crates/gui/tests/sublibrary.rs::容量条三段各自标得出数`。
- [x] 点「改选择」跳到浏览屏，**筛选器里预填的是这个子库的规则**
      —— `Screen::edit_selection` 只放下一个记号，跳转由 `App::route` 走（只有窗口同时
      够得着两屏）。多条规则用 `Rule::any_of` 并成一条，与求值同一条口径（并集）。
      浏览屏那边 `Screen::begin_editing` **把整份筛选换掉**而不是叠一层——多一个档、
      多一条搜索词，人核对的就不是同一件事了。核对：
      `crates/gui/tests/sublibrary.rs::点改选择跳到浏览屏而且筛选器里预填的是这个子库的规则`
      （连带断言「屏上筛出来的 ＝ 这个子库选出来的」）。
- [x] 在浏览屏调完按「更新到子库」，规则**原样带回**，选出来的东西与屏上一致
      —— `library::Screen::update_sublibrary` → `Catalog::replace_rules`：
      **换掉读得懂的那几条，不是往上加**（加的话旧的还在，子库选出来的比屏上多）。
      为了让「原样」成立，`Rule::any_of` 单条与 `WorkQuery::to_rule` 单组各去掉了一层
      多余的括号（挂单 Q88，规则语言一个字没改）。核对：
      `crates/gui/tests/sublibrary.rs::在浏览屏调完更新到子库规则原样带回而且选出来的与屏上一致`
      ——先空跑一趟断言一个字没变，再真改一次断言「子库选出来的 ＝ 屏上筛出来的」。
- [x] 例外（含进来 / 排除掉）在浏览屏上手动加减，子库屏如实显示有几条
      —— 三个按钮（含进来 / 排除掉 / 撤掉）做在浏览屏详情面板的编辑栏里
      （`library::Screen::exception_ui`），**只在「改选择」那一趟里露面**：平常浏览时
      摆一个「排除掉」在那儿，人会问「排除出哪儿」。子库屏上那一行只数一数
      （`{N} 条例外：收入 {x}、排除 {y}`）。核对：
      `crates/gui/tests/sublibrary.rs::例外在浏览屏上加减子库屏如实显示有几条`
      ——连带断言例外真的改变了选出来的那一批，撤掉之后重新由规则说了算。
- [x] 排差量与同步走**任务台**：带进度、能停
      —— **排差量**那一半是票 01 接好的，这一票原样接着用：`tasks.queue` → `settle`，
      正排着时进度与「停下」摆在按钮旁边（人是在这一屏点的，不该逼他先切去任务屏）。
      **同步**那一半照旧走它自己那条后台线程：**带进度、能停两条都成立**
      （顶栏上「正在同步「X」，N 秒」＋ 按得动的「停下」），但它不出现在任务屏的历史里。
      **保留，记在挂单 Q89**，指向票 `12`。核对：
      `crates/gui/tests/sublibrary.rs::排差量预览进任务队列跑完之后留一条带耗时的历史`、
      `::排差量预览按停之后一个字节都没写而且再排一次照样排得出`、
      `::同步从这里触发而且只碰清单里记录过的文件`。
- [x] 差量步骤看得见，超长时能全部展开
      —— 收起来时卡上摆头 6 条（几百上千步铺满一屏会把底下的「同步」挤没了），
      旁边一个「全部展开」；摊开之后是那张**虚拟化**的表（`steps_table`，最多 2,000 行，
      再多在末尾说清还有多少条没列）。重排一次自动收回去（`invalidate`）。核对：
      `crates/gui/tests/sublibrary.rs::差量预览摆得出新增与净变化而且步骤全部展开得了`、
      `::这一屏画得出来_摊开与收起都不炸`（headless 一帧真的走一遍两种样子）。
- [x] 目标不许落在主库里（ADR-0004），落了当场拦下
      —— 判据在核心里（`sync::prepare::refuse_target_in_library`，与同步那一道是同一条），
      **拦在「存下来」这一步而不是等到点同步**：一个指着主库的子库定义放在库里，
      下一次点同步之前谁都不知道它错了。浏览屏的「存成子库」那条路也补上了同一道闸。
      核对：`crates/gui/tests/sublibrary.rs::目标落在主库里当场拦下`
      ——连带断言拦住之后中立库里一条定义都没写。
- [x] **目标设备一律用本地 fixture 目录模拟**，测试里绝不碰真实设备或 SD 卡
      —— `crates/gui/tests/sublibrary.rs` 全程 `temp_dir("gui-sub-card")`；fixture 主库
      也只往临时目录里写。实测那条路（`--bench-sublibrary`）同样是
      `demo::workspace()/目标设备-fixture`，而且开着现成的库时直接拒绝跑。
      新加的两个根那条 `crates/core/tests/sync.rs::两个根里同一条相对路径…` 也全在临时目录里。
- [x] 门禁命令全绿（带 `--all-features`）
      —— 三条都跑了（见提交信息与回报）。

## 顺带收掉的

- **挂单 Q57**（落点撞车）点名交给这一票。核对之后发现**那道闸早就在了**
  （`Desired::screen` 的 `RejectReason::Collision`，判的是落点不是原因，所以一组根这条
  新路天然被咬住，而且**撞上的一个都不放行**），**没有另加一份检查**。这一票补的是
  两处：撞车那句话说清了「两个根里同一条相对路径」这个原因并印**完整的键**；
  以及**这一屏原先根本不画 `plan.rejected`**——撞车、FAT32 的 4 GiB 上限、文件名
  不收的字符在窗口里全是隐形的，人只会看见「明明选中了却没传过去」。顺带把
  `plan.unsupported`（吃不下、转不了，照搬但要说出口，ADR-0017）也画上了。
  剥根名那条口径一个字没改。
- **挂单 Q68**：裁决是「不在子库屏摆第二个『存成子库』」——抬头只有「+ 新建」，
  建出来的是一台选择集为空的设备，要什么内容去浏览屏筛。
- **挂单 Q74**：裁决是「不给规则语言加『根』这一维，明确留给将来」，理由记在那一条里。
- 规则那一栏原先把读不懂的几条**印两遍**（一遍当正常的、一遍当坏的），这一票分开了。
- 路过看见的一处：`Unruly::Unwritable` 那句话里夹着 17 个字面空格（票 04 留下的一处
  断行没接上），印出来是「……「或」，                 或者没配对的括号」。补上续行反斜杠。

## 新开的挂单

`Q85`（裁剪建议在这一屏上按不动了）、`Q86`（读不懂的规则界面上删不掉了）、
`Q87`（「算一遍容量」还在画帧线程上，343 ms）、`Q88`（往返不许多套括号）、
`Q89`（同步没搬上任务台）、`Q90`（删子库留成按两下，而票把「删除确认」列在要拆的八样里）、
`Q91`（`/code-review high` 报的六条，六条当场收掉）。

## 收尾审查报了什么

`/code-review high` 报六条，**六条全收**，逐条记在挂单 `Q91` 里。最要紧的两条是这一票
**自己造出来的**：把选择集搬去浏览屏之后，子库屏缓着的差量与容量账没人来丢了——
例外一按就落库，人点「不改了」再切回子库屏，那份旧差量还在，而「同步」认的正是它。
收法是浏览屏每动一次就留个记号、`App::route` 每帧转告子库屏 `forget` 那一台。

审查**没跑门禁**（它的原话：树在审查期间动了三次，编译量的是旧快照）。三条门禁由实现者跑，全绿。
