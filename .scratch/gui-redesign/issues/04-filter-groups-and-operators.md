# 04 — 筛选器：嵌套分组与两个新运算符

**What to build:** 在浏览屏上用**可嵌套的条件组**筛库：每组选「全部满足 / 任一满足 / 都不满足」，组里可以再套组。筛到满意按「存成子库」，条件原样变成那个子库的**规则**。

**筛选器就是规则语言**，同一套东西两处用。这一票要做的是把它补齐：

- 新增两个运算符：`^ 以…开始`、`$ 以…结束`
- 子句之间从平铺的「且」改为**可嵌套的组**，三种连接
- 维度补上**收藏**，以及元数据齐了之后的开发商、发行商、简介

**旧的平铺规则要原样解析**——它等价于一个「全部满足」的顶层组，已存的子库不该因为这一票失效。

收掉挂账 D154（五个筛选之间只能「且」）。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 三种连接（全部 / 任一 / 都不满足）各自成立，且组可以嵌套至少两层
- [ ] `^以…开始` 与 `$以…结束` 各自成立
- [ ] **旧的平铺规则原样解析**，已存的子库照常可用
- [ ] 筛选结果的条数当场显示，与真正命中的条数一致
- [ ] 「存成子库」把当前条件原样变成规则，新建的子库选出来的东西与屏上一致
- [ ] 筛选器随时看得见、改得动，不藏在对话框里
- [ ] 规则语言在核心库里，命令行与界面共用同一套解析与求值
- [ ] 挂账 D154 标为 resolved
- [ ] 门禁命令全绿（带 `--all-features`）
