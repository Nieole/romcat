# 14 — 读不懂的子库规则要有一条界面上的处置路

**What to build:** 一条读不回来的子库规则，眼下在界面上**改不动也删不掉**——只能去命令行。给它一条界面上的路。

**怎么来的（挂单 Q86）：** 票 25 那版子库屏每条规则旁边有个「删」。票 11 把选择集整个改成**只读**（票的原话：「规则增删、例外记撤的控件要**搬走**」），规则从此在浏览屏的筛选器里改。而**读不懂的那几条搬不去浏览屏**：筛选器摆的是一棵读得懂的树，一条读不回来的原文在那儿没有位置。于是它们卡在中间——子库卡上照旧摆出来（红的，连它错在哪），可一个动它的办法都没有。

票 12 只补了半件事：卡上现在写明「读不懂的这 N 条**界面上改不动**：按序号去命令行删（`romcat sublibrary rule <子库> --remove <序号>`）」。**说清了下一步，但那一步还在命令行上。**

**为什么不能顺手加回一颗「删」：** 那会推翻票 11 立的「这一屏不选内容」。选择集在子库屏只读，是那一票把八个概念降到三个的做法本身；开一个口子，规则增删就会一条条回来。

**几条能走的路**（不预先定死）：

- 让**筛选器**摆得下一条读不回来的原文：一个「读不懂的原文」节点，只显示与删除，不参与求值。那样「改选择」这条路就完整了——去程带过去、回程原样带回或者被人删掉。
- 或者在子库卡上只给**删**这一个动作（不给改），并把它与「改选择」在屏上分开写清：一个是「换掉读得懂的那几条」，一个是「把这条坏的扔掉」。
- 或者给**浏览屏的筛选器**一个「这个子库还有 N 条读不懂的规则」的横幅，点进去处置。

**不许动的：** `replace_rules` 只换**读得懂**的那几条这条口径（`crates/core/tests/sublibrary.rs::换掉规则时读不懂的那几条原样留着` 钉着）——顺手把读不懂的一起删了，等于拿一次「改选择」悄悄清掉用户还没来得及修的东西。**例外是永久记住的**（ADR-0016），一并不许被顺手清掉。

**Blocked by:** 无 —— 可立即开工

**Status:** done

- [x] 一条读不懂的规则在界面上处置得掉（删掉，或者改对）
      —— 走的是**第三条路**：「改选择」跳过去之后，浏览屏**筛选栏顶上那条横幅**底下
      多一段「处置读不懂的那 N 条」（`crates/gui/src/browse.rs::Screen::broken_rules_ui`），
      每条印出序号、原文、错在哪，各跟一颗「扔掉这条」。**只给「扔掉」不给「改对它」**：
      改一条读不回来的原文要的是一个规则语言的文本框，那正是票 `11` 拆掉的东西；
      改对了的那一条走上面的筛选器重筛一遍、按「更新到子库」带回去，那条路本来就通。
      理由与代价全文记在挂单 `Q143`。
- [x] 处置它**不碰**同一个子库里读得懂的那几条与例外
      —— 那道闸**落在核心里**（ADR-0005）：
      `crates/core/src/catalog/sublibrary.rs::discard_broken_rule` **先读一遍再决定
      删不删**，读得懂的那几条它拒绝（`Discarded::Readable`），界面只递一个序号过去。
      与 `remove_rule`（按序号删任何一条，命令行走的那条）分开正是为了这个：
      两条路混在一起的话，一次「扔掉这条坏的」就能悄悄删掉一条好的。
      核对：`crates/core/tests/sublibrary.rs::扔掉读不懂的那一条不碰读得懂的那几条与例外`
      ——读得懂的那条被拒（库里一条都没少）、坏的那条扔得掉、
      **例外连同它那句话原样还在**（ADR-0016「永久记住」）、再扔一遍答「已经不在了」。
- [x] 「改选择」那条路照旧只换读得懂的那几条（原有那条核心测试仍绿）
      —— `replace_rules` 一个字没动。
      `crates/core/tests/sublibrary.rs::换掉规则时读不懂的那几条原样留着` 原样通过
      （12 条全绿），`crates/gui/tests/sublibrary.rs::选择集在这一屏上只读摆得出规则与例外`
      也原样通过。
- [x] 子库屏照旧不输入规则与例外（票 11 立的「这一屏不选内容」不破）
      —— `crates/gui/src/sublibrary.rs` **一个写的动作都没加**：卡上那几条照旧只读地
      摆着（红的、连它错在哪），改的只有末尾那句指路——从「界面上改不动：按序号去命令行删」
      改成「按上面『改选择』跳去浏览屏，筛选栏顶上那条横幅里逐条扔得掉」（命令行那条路
      写进了悬停，没丢）。测试里有一条**反向断言**钉着这件事：卡那一帧画出来的字里
      **不许出现「扔掉这条」**。
- [x] headless 测试钉住：卡上摆着一条读不懂的，走完界面上那条路之后它没了，别的没动
      —— `crates/gui/tests/sublibrary.rs::读不懂的规则在改选择那条横幅里扔得掉而且别的一条都没动`。
      断言看的是**这一帧真的画出来的字**（`画出来的字` 收 `Galley`，与版式那两处同一个写法）：
      卡上摆得出那条原文与「按上面『改选择』跳去浏览屏」那句话、卡上**没有**「扔掉这条」、
      横幅里摆得出原文与那颗按钮、按完之后卡上那条原文**没了**；
      跟着断言读得懂的那条原样在、例外连同那句话原样在、
      **这个子库选出来的一个都没变**（3 个：两个 SFC 加那条收入的例外）。
- [x] 挂单 Q86 标为 resolved，写清走了哪条路 —— 见下面那一节。新记三条：`Q143`
      （三条路选了哪条、另外两条为什么不走、代价是什么）、`Q144`（扔一条坏规则会把
      子库屏缓着的差量一起丢掉——**有意的保守**，理由与收法记在那一条）、
      `Q145`（「这一帧真画出来的字」那个助手三份测试各抄了一遍）。
- [x] 门禁命令全绿（带 `--all-features`）—— 三条都跑了，见提交信息与回报。

## 收尾审查报了什么

`/code-review high` 报三条（它没跑任何 cargo 命令——这台机器内存吃紧，门禁由实现者跑）。

- **medium · 扔一条坏规则会把缓着的差量预览一起丢掉** —— **留成这样**，代价与理由写进
  `discard_broken_rule` 的文档并记在挂单 `Q144`：那根记号通道只有一根，它挡的是票 `11`
  收尾审查报的「同步认的是旧差量」那一条（`Q91` 第 2 条）；另开一根轻通道等于给
  「真改过选择却没作废」留一个新入口，而那一类的代价是把过期的计划传上卡。
- **low · 读库出的错报成了「写不动」** —— 收了。`discard_broken_rule` 是**先读一遍再决定
  删不删**，头一个会失败的正是那趟读，那句话改成「中立库读写不动」。
- **low · `Absent` 那一支既不留记号也不清错** —— 收了。另一个窗口先删过一遍时，
  库里确实已经没有那一条了，子库屏那张卡照旧红着印它。三支现在共用同一句
  `self.touched = Some(name)`。
- 另有一条**不算缺陷**的提醒（「这一帧真画出来的字」那个助手三份测试各抄一遍）
  —— 收它要动另外两张票的测试文件，记在挂单 `Q145`。


## 挂单裁决

从 `.scratch/PARKING-LOT.md` 迁来。那份挂单是这一轮队列的**待办队列**，收尾时清空；
裁决之后条目迁回它所属的票，编号 `Qn` 留着占号、不复用。

### Q86 — 读不懂的规则在界面上删不掉了

- **来自：** 票 `gui-redesign/11`
- **类别：** 票没预见到的第三种情况
- **在哪：** `crates/gui/src/sublibrary.rs::selection_ui`、
  `crates/core/src/catalog/sublibrary.rs::replace_rules`
- **为什么没停线：** 上一版子库屏每条规则旁边有个「删」，这一票把选择集整个改成只读
  （票的原话：「规则增删、例外记撤的控件要**搬走**」）。而**读不懂的那几条搬不去
  浏览屏**——筛选器摆的是一棵读得懂的树，一条读不回来的原文在那儿没有位置。
- **这张票实际做了什么：** 读不懂的那几条在卡上**照旧摆出来**（红的，连它错在哪），
  并且写明「它没参与求值，『改选择』也不会碰它」。`replace_rules` 只换**读得懂**的
  那几条：顺手把读不懂的一起删了，等于拿一次「改选择」悄悄清掉用户还没来得及修的
  东西；而留着它们不改变这个子库选出什么（`LoadedSelection::from_stored` 早把它们
  挑出来另放了，`select` 根本看不见）。核对在
  `crates/core/tests/sublibrary.rs::换掉规则时读不懂的那几条原样留着` 与
  `crates/gui/tests/sublibrary.rs::选择集在这一屏上只读摆得出规则与例外`。
- **删它的路还在，只是在命令行上：** `romcat sublibrary rule rm <子库> <序号>`，
  而卡上印着的正是那个序号。
- **正常路径上长不出这种规则**：`add_rule` 只收已经读通了的 `Rule`。它们的来路是
  「人打开过那个 SQLite 文件」或者「换了一版程序、删掉了一个维度」
  （`LoadedSelection::from_stored` 的文档）——真库上眼下一条都没有。
- **谁来裁：** 票 `12`（版式与一致性收尾）——要么在卡上给这几条单独留一个「删掉这条」，
  要么就明确写成「走命令行」
- **票 `12` 裁定：屏上那一半做掉，界面上删得掉那一半升成新票 `gui-redesign/14`。**
  加回一颗「删」会推翻票 11 立的「这一屏不选内容」——那一票把八个概念降到三个靠的就是它，
  开一个口子，规则增删会一条条回来。而这张票的硬约束里点名写着**票 11 的「选择集只读」
  不许顺手改形状**。
- **屏上眼下多了一句**：「读不懂的这 N 条**界面上改不动**：按序号去命令行删
  （`romcat sublibrary rule <子库> --remove <序号>`），或者改对了再 `--add` 一条。」
  ——**看不出下一步**是这条挂单里最贵的那一半，它当场没了；而「那一步还在命令行上」
  由新票 14 收。
- **状态：** resolved
- **收尾裁决：** **本票做掉了，走的是三条路里的第三条** —— 「改选择」跳过去之后，
  浏览屏**筛选栏顶上那条横幅**底下多一段「处置读不懂的那 N 条」，每条各跟一颗
  「扔掉这条」（`crates/gui/src/browse.rs::Screen::broken_rules_ui`）。
  **子库屏上一个写的动作都没加**——票 `11` 立的「这一屏不选内容」一个口子都没开：
  卡上那几条照旧只读地摆着，改的只有末尾那句指路（从「去命令行删」改成
  「按上面『改选择』，横幅里逐条扔」；命令行那条路进了悬停，没丢）。
  另外两条路：往筛选树里塞一个「读不懂的原文」节点会给「屏上筛出来的 ＝ 子库选出来的」
  与「规则原样带回」各开一个洞（两条各钉着一条测试）；在子库卡上只给「删」正是这张票
  正文点名警告的那一颗。删掉它的那道闸落在核心里而不是界面上
  （`Catalog::discard_broken_rule` 先读一遍再决定删不删，读得懂的它拒绝）。
  理由与代价全文在挂单 `Q143`。
