# 05 — 搜索框与匹配质量排序

**What to build:** 在浏览屏顶上打几个字就找到那个游戏，**匹配得好的排前面**——以搜索词开头的排最前，含有的排后面，别名命中的再后，简介命中的最后。

权重**内置**，不暴露给用户配。用户日常干的是「找到这一批然后处理它们」，让人自己配权重是个很少用得上、却一直占着界面的东西。

搜索框**独立于筛选器**：它排序，筛选器筛集合。**排序不参与子库的规则**——子库要的是集合，不是顺序。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 搜索框输入之后，以该词开头的作品排在含有该词的前面
- [ ] 别名命中与简介命中排在标题命中之后，各自的次序确定
- [ ] 中文与拉丁文的搜索词都工作，繁简差异按现有匹配层的口径处理
- [ ] 搜索与筛选器**可以叠加**：先筛后搜，结果既满足条件又按质量排
- [ ] **排序不进子库的规则**——存成子库时只带条件不带顺序，有测试钉住
- [ ] 排序逻辑在核心库里
- [ ] 门禁命令全绿（带 `--all-features`）
