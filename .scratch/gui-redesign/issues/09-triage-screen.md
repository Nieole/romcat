# 09 — 待确认屏：批分组、下钻、样本、整批操作

**What to build:** 打开待确认屏，看见的不是一万八千行的表，而是**工具已经分好的几十批**。每批写着条数与**那句共同依据**，展开有随机样本，看完一键整批通过。

**设计前提是算出来的**：待裁决 18,241 个变体，按 5 秒一条算是 **25 小时**，逐条不是可行路径。但其中 **14,657 条（80.4%）只有一个候选**——那不是「选哪个」，是「对不对」，而「对不对」可以按批回答。

**二级分组：**
- **一级按依据形状**（源 / DAT / 置信度 / 哈希口径 / 候选数）。这一层分得动，是因为它就是「工具凭什么这么认为」——而整批通过时你验证的正是那句话。实测前 5 批盖住 12,223 条。
- **二级按目录或命名规律**。坏的那几条往往集中在某一个目录里，随机抽样未必抽得到，所以要能下钻。既有的三个轴（按目录 / 按候选作品 / 按命名规律）作为二级的可选分法。

**每一层都能整批过或整批拒**，用户自己决定下钻到多细。屏上常驻三样：**条数、共同依据、随机样本**（可换一组）。

**识别**与**刮削**产生的待确认在**同一条队列**里——真库里那 4,723 条中文离线源的候选本来就和 DAT 的候选同表。

多候选那 3,584 条（2–3 个 2,750、4–10 个 688、10 个以上 146）走逐条键盘流兜底。

**Blocked by:** 04、08

**Status:** done

- [x] 一级分批的批数与各批条数**与真库数据对得上** —— **保留**。真库那块盘不在这台
      机器上（挂单 `Q81`）。分批逻辑本身钉在两处假数据上：`core/src/triage/batch.rs`
      的单元测试与 `core/tests/triage.rs::一级分批按依据形状分而且各批加起来就是整个队列`
      （磁盘 fixture 走真的扫描 + 识别），界面那一侧是
      `gui/tests/queue.rs::打开看见的是分好的批而不是一万八千行的表`。钉住的不变量是
      **各批条数加起来恰好是队列的条数**，以及**每一批照它的形状折回选择器，选中的与
      卡片上写的是同一批**——这两条与库有多大无关。**真库上那几个数等用户跑一趟核对。**
- [x] 二级下钻的条数加起来等于它所属的一级 ——
      `core/tests/triage.rs::二级下钻的条数加起来等于它所属的一级` 与
      `gui/tests/queue.rs` 同名那条。**按目录那个轴一条只落一个组**，各组之和就是这一批
      （`Drill::adds_up`）；另外两个轴一条能落进好几组，屏上把那句话说出来而不是装作
      加得起来。另有一条回归测试
      `gui/tests/queue.rs::下钻只收窄整批操作不把别的批从屏上筛掉`：下钻**不借道选择器**，
      它只收窄整批操作的作用范围，一级那一列卡片与二级那张表一个字都不动
      （收尾审查抓到过一次相反的写法）。
- [x] 每批屏上有条数、共同依据、随机样本；「换一组样本」每次不同但都在批内 ——
      `Batch::count` / `Batch::why` / `batch::sample`。共同依据取的是这一批候选依据的
      **最长公共前缀**再退到最后一个句读（`共同依据是原话不是概括`）——是原话不是概括。
      换样本走等差数列 + 与条数互质的步长，**相邻两次一定不同**（转满一圈会回到原处，
      那是应该的）：`换一组样本每次不同但都在批内`，界面侧连换 8 次。
      `gui/tests/queue.rs::屏上常驻三样条数共同依据与随机样本` **看的是这一帧真的画出来的
      那些字**（走 egui 的 `Galley`），不是查队列里有没有这条数据。样本在**展开的那张卡片**
      上（票的正文：「每批写着条数与那句共同依据，**展开有随机样本**」），而**列完队列
      头一批默认就是展开的**——设计稿上就是这样，一张都不展开的话人打开这一屏一条样本
      都看不见。
- [x] 一级与二级**都能整批通过、整批拒绝** ——
      `Queue::plan_scope` 收下的是 `Scope`（一批，或它下面下钻出来的一组），两层同一条路。
      四种组合各有一条：`gui/tests/queue.rs::一级与二级都能整批通过而且撤得回来`、
      `整批拒绝记成认不出并且退出队列`、`二级下钻之后整批拒绝只作用于那一组`，
      核心库侧还有 `core/tests/triage.rs::整批拒绝走的是认不出那一档`。
      **一条候选都没有的与多候选的不给「整批通过」**（`Batch::passable`）：前者什么也没
      定下来，后者是替人挑了个没看过的答案——按钮是灰的，理由印在 hover 上。
- [x] 整批操作之后可以**按批整个撤回**（走票 08 那条路）——
      `Queue::undo` 本人，`undo_batch` 一个字节的 DAT 都不读。
      `core/tests/triage.rs::整批通过之后按批整个撤回`（撤完中立库与沉淀库两边都回原样、
      队列条数回到原样）、`gui/tests/queue.rs::一级与二级都能整批通过而且撤得回来`。
- [x] 置信度用颜色标出，四档（高 / 中 / 低 / 还没识别）含义与别处一致 ——
      四档收成核心库里的 `catalog::identify::Tier`，**前三档的词与 `Confidence::label`
      逐字一样**，第四档是「还没识别」（与 `library.rs` 眼下用的词一致）。
      `queue.rs::四档各画各的颜色不串` 钉住四档颜色两两不同。
      **跨五屏对齐留给票 12**（挂单 `Q83`）。第四档那个词「还没识别」与词表里同名那条
      不是一回事——跟着**别处**（`browse::WorkRow::confidence_label`）写，冲突记在挂单 `Q84`。
- [x] 逐条键盘流可用：切候选、通过、拒绝、跳过、撤销上一条 ——
      `gui/tests/queue.rs::逐条键盘流切候选通过拒绝跳过撤销上一条`，**敲的是真的键盘事件**
      （`egui::Event::Key` 进 `RawInput`）。`←→` 切候选、`Y` 过、`N` 拒、`空格`
      **先放着（一个字都不写库，断言钉着）**、`U` 撤销上一条（走的就是按批撤那条路）。
      「跳过」这个词在这里与识别结论那一档撞了名，界面上改叫「先放着」，记在挂单 `Q80`。
- [x] 逐条时看得见文件名、路径与候选的完整**依据** ——
      `queue::Screen::detail` 把文件名与路径分两行摆出来，候选逐条印出置信度、源、条目名
      与**完整依据**，眼下切到的那一条加粗。
      `gui/tests/queue.rs::逐条时屏上真的摆着文件名路径与候选的完整依据` **断言的是这一帧
      真的画出来的那些字**（走 egui 的 `Galley`）——查队列里有没有这条数据是恒真的废话，
      这一屏要证的是那几样摆出来了没有。
- [x] 识别与刮削的待确认在同一条队列里 ——
      `core/tests/triage.rs::识别与刮削的待确认在同一条队列里`：fixture 里摆一份名字撞得上
      **中文离线源**（刮削那一侧的数据源）的汉化版，跑一趟识别，它撞出来的候选与 DAT 的
      候选同表、同一条队列，**在这一屏上自己占一批**，卡片上写着「中文离线源 / dump-…」。
      界面侧 `gui/tests/queue.rs` 同名那条。
- [x] 裁决落**沉淀库**，删掉中立库重扫也不丢 —— 整批操作走的是既有的
      `triage::apply`（沉淀库 + 中立库两处一起写），既有的
      `core/tests/triage.rs::裁决钉在文件哈希上下一趟直接命中不再进队列` 与
      `换一份中立库换一个路径同一个文件照样直接命中` 钉着这一条，本票没有另开一条路。
- [x] 分批逻辑在核心库里，界面只画和转发（ADR-0005）—— 分批、二级下钻、抽样、
      作用范围、四档的账全在 `crates/core/src/triage/batch.rs` 与 `Queue` 上。
      屏底那句「前 N 批盖住多少」的**数也是核心库算的**（`batch::coverage` /
      `Queue::coverage`，`前几批盖住多少是核心库算出来的` 钉着），界面只把它们摆成一句话；
      「这一条标成四档里的哪一档」走 `Item::tier`，界面不再自己去看第一条候选。
      收尾审查抓到过这三处漏在界面里的算数，已经搬回核心库。
- [x] 门禁命令全绿（带 `--all-features`）—— clippy / test 两条见收尾记录。


## 挂单裁决

从 `.scratch/PARKING-LOT.md` 迁来。那份挂单是这一轮队列的**待办队列**，收尾时清空；
裁决之后条目迁回它所属的票，编号 `Qn` 留着占号、不复用。

### Q83 — 置信度那四档（`Tier`）眼下只有待确认屏在用

- **来自：** 票 `gui-redesign/09`
- **类别：** 与别的票的边界
- **在哪：** `crates/core/src/catalog/identify.rs::Tier`、`crates/gui/src/queue.rs::tier_color`
- **为什么没停线：** 票明说「**你这一票只管自己这一屏的四档颜色正确，跨屏对齐留给 12**」。
- **这张票实际走了哪条路：** 四档收成一个核心库里的类型 `Tier`（高置信 / 中置信 /
  低置信 / 还没识别），**前三档的词与 `Confidence::label` 逐字一样**；颜色从 `Visuals`
  里取（亮暗两套主题下同一串 RGB 不是同一个可读性），`queue.rs::四档各画各的颜色不串`
  钉住四档不串。**`library.rs` 与 `table.rs` 眼下还是各写各的一份**（`None => "还没识别"`
  加三个 `match` 分支），没去动——那是票 12 的活。
- **谁来裁：** 票 `gui-redesign/12`——把另外四屏也换到 `Tier` 与同一个取色函数上。
- **票 `12` 裁定：做掉，五屏对齐。** 那个**颜色**眼下只有 `romcat_gui::look::tier_color`
  一处写，那个**词**只有 `Tier::label` 一处写（`browse::WorkRow::confidence_label` 也改成
  转发给它）。换过来的三处：待确认屏（原来自己有一份 `tier_color`）、主列表那一栏
  （原来一个 `match work.confidence`）、浏览屏详情面板的变体行（原来手写「高 / 中 / 低」）。
- **五屏里真会标置信度的是浏览与待确认两屏**：库屏摆的是根与数据源、子库屏摆的是设备与
  差量、任务屏摆的是队列与历史，那三屏上没有一个变体级的结论可标。「五屏对齐」在那三屏上
  是**不许凭空造一个置信度出来**，不是「每屏都得标一个」。
- 核对在 `crates/gui/tests/layout.rs::置信度四档在两屏上是同一个词`（看的是**这一帧真的画
  出来的字**，并且钉住旧写法「高｜」「中｜」「低｜」一处都不剩）。
- **状态：** resolved
- **收尾裁决：** 已在队列进行中裁决过（见本条原有的状态行）。
