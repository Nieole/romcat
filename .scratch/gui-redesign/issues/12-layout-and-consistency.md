# 12 — 布局可调与视觉一致性收尾

**What to build:** 拖动面板之间的边界调整各区大小，关掉再打开还是那个样子。以及把**置信度**的颜色语言在五屏里对齐——同一档在哪一屏都是同一个颜色、同一个词。

面板边界的位置持久化在**工作目录**里，不是中立库——中立库整份可再生，界面偏好放进去会被某次重扫抹掉。

置信度四档：**高置信 / 中置信 / 低置信 / 还没识别**。「还没识别」是词表里那个第三态——既不是「未命中」（撞过没撞上，是结论），也不是「无判据」（拿不到可撞的东西）。

**Blocked by:** 03、09、11

**Status:** done

- [x] 浏览屏、待确认屏、子库屏的面板边界都拖得动
      —— 七条边界收成一份声明（`crates/gui/src/layout.rs::Boundary::ALL`：浏览屏
      筛选 / 浏览详情 / 浏览编辑 / 刮削面板，待确认屏 批量 / 裁决面板，子库屏 配目标），
      三屏各自的 `ui()` 都改走 `Boundary::show`。收之前那三块**底栏**一块都拖不动：
      `egui::Panel::bottom` 默认 `resizable(false)`。**光打开 `resizable` 还不够**——
      `egui::Panel` 存进 `PanelState` 的是**内容量出来的**尺寸，内容不占满地方就会把
      拖出来的宽度缩回去（挂单 `Q129`），所以 `Boundary::show` 头一件事是
      `take_available_width` / `take_available_height`。
      核对在 `crates/gui/tests/layout.rs::三屏的面板边界都拖得动`：**真的按下去、拖过去、
      松手**（一帧一件事，挤在一帧里 egui 只当成一次点击），七条边界每一条都落在目标
      ±4 点之内。
- [x] 拖动后的位置关掉再打开还在，**存在工作目录里不存在中立库里**
      —— 路径归核心库说（`romcat_core::workspace::gui_layout_path` ＝
      `工作目录/gui/layout.txt`，与 DAT 库、媒体池一样**不带 `Slug`**：面板拖到哪儿是
      这个人这块屏的事，跟着库分开存等于换一份库就把版式忘一次）。读写在
      `crates/gui/src/layout.rs`：几行「名字 = 数值」的纯文本，开头三句给人看的话写明
      **随手删得掉**、**不在中立库里**（中立库整份可再生，放进去会被某次重扫抹掉）。
      `crates/gui/tests/layout.rs::拖动后的位置存在工作目录里不存在中立库里` 断言那条路径
      落在工作目录之内、文件里有刚拖出来的那个数；
      `::关掉再打开还是那个样子` **新开一个 `App` 配一个新的 egui 上下文**（egui 自己那份
      内存里的尺寸表跟着旧窗口一起没了），拖出来的 430 点原样回来。
      **记的是「人拖到哪儿」，不是「这一帧画成多宽」**：`harvest` 只在**把手被松开**的那一帧
      记（`ctx.read_response(resize_id).drag_stopped()`）。照单全收会出事——面板的上限跟着
      窗口走，最大化时拖到 700、还原窗口那一帧被夹到 576，576 就会盖掉 700；而窗口尺寸本身
      不持久化（`eframe` 的 `persistence` 没开），每次开窗都要挨那一刀。
      回归钉在 `::窗口变小再变回来不会把拖出来的宽度削掉`（审查发现，挂单 `Q132` 第 1 条）。
      **拖的时候不写盘**：手松开那一帧才落（`App::ui` 里 `pointer.any_down()` 为假时），
      而且只在数真变了的时候写。写不进去时顶栏上写一句「版式存不下来：……」，不静默吞掉。
- [x] 拖到极限时不塌陷、不把某一区挤没
      —— 下限 `min_size` 拦一道；上限**两道一起夹**（`Boundary::cap`）：一是**整个窗口那一维的
      几成**（`share`；按窗口算而不是按「摆到它那一刻还剩多少」算，后者会让先摆的挤后摆的
      ——摊开刮削面板，底下那块元数据面板就自己矮一截），二是**「眼下还剩多少」减去
      `FLOOR`（120 点）**——那是兜底：不管前面吃掉多少，后面一定还剩得下 120 点。
      第二道是审查逼出来的（挂单 `Q132` 第 2 条）：光按几成算，最小窗口 480 高、刮削面板
      摊开、两块底栏都拖到头时，26（顶栏）＋192＋192，正中只剩 70 点。
      钉在三条上：`layout::tests::同一屏同一维上的几条加起来挤不没正中那块`（常态下第二道不必
      出手）、`layout::tests::一块接一块吃下去正中那块还剩得下地板那么多`（最小窗口上连着
      算一遍）、`tests/layout.rs::刮削面板摊开时两块底栏一起拖到底也挤不没正中那块`。
      `crates/gui/tests/layout.rs::拖到极限时不塌陷也不把正中那块挤没`：往里拖 −2000 点，
      三条边界都 `>= min` 且那三块的抬头照样画得出来；往外拖 5000 点，正中那张表还剩
      ≥ `FLOOR` 宽、≥ `FLOOR` 高。
      **保留一条**：面板窄不过它最窄的那个子控件（「筛选」那栏声明的下限 150，实测最窄
      217.25），所以 `min` 是「至少这么宽」而不是「拖到底就是这么宽」——记在挂单 `Q129`。
- [x] 置信度四档的颜色与文案在五屏里**逐字一致**
      —— 那个**词**只有 `Tier::label` 一处写，那个**颜色**只有
      `romcat_gui::look::tier_color` 一处写。换过来的三处：待确认屏（原来自己有一份
      `tier_color`）、主列表那一栏（原来一个 `match work.confidence`）、浏览屏详情面板的
      变体行（**原来手写「高 / 中 / 低」**，与另外两处不是同一个词）。核心库那一侧
      `browse::WorkRow::confidence_label` 也改成转发给 `Tier::label`，并多一个
      `WorkRow::tier()`。
      核对在 `crates/gui/tests/layout.rs::置信度四档在两屏上是同一个词`：看的是**这一帧真的
      画出来的字**，四个词两屏上都出现过，而旧写法「高｜」「中｜」「低｜」一处都不剩。
      **五屏里真会标置信度的是浏览与待确认两屏**——库屏摆的是根与数据源、子库屏摆的是设备
      与差量、任务屏摆的是队列与历史，那三屏上没有一个变体级的结论可标；「对齐」在那三屏上
      是**不许凭空造一个置信度出来**。这条口径写进了 `Tier` 的文档与挂单 `Q83` 的裁定。
- [x] 颜色不是唯一线索——每处都有文字标签，色觉障碍下仍读得出
      —— `look::tier_bar`（色条）**从不单独出现**，摆它的地方一定跟着 `tier_label`
      （`tier_tag` 把两样一起给出来）。收之前有一处失守：待确认屏逐条那张表的「候选」一栏
      是一个**光染了色的数字**，眼下画的是「3 · 高置信」，表头也从「候选」改成
      「候选 · 置信度」（`crates/gui/tests/layout.rs::每一处上了色的置信度都跟着那个词`
      钉住这两样）。
      还有一半是**字得先读得出来**：高置信那一档从 `selection.bg_fill` 换成
      `selection.stroke`——前者是给背景用的一串，当前景色用时对比度只有 **2.33（暗）/
      1.55（亮）**，换后是 **12.41 / 7.81**（`look::tests::高置信那一档在两套主题下都读得
      出来`）。这一改动动了票 `09` 定的那一串，理由与代价记在挂单 `Q131`；
      低置信在暗色主题上是 4.31，**差一点点没到 4.5**，那是 egui 自己的红，改它要连带动
      界面上所有的错误提示，不在这张票里（同挂单）。
- [x] 窗口标题写明当前开的是哪一份库与哪一屏
      —— `App::window_title()` ＝ `romcat — {库} — {屏}`，**换屏才发一条**
      `ViewportCommand::Title`（`App::retitle`），不是每帧发一条。合成数据那一路
      `main.rs` 另给一个名字（`App::set_library_label("合成数据（演示）")`）——假数据与真库
      在界面上长得一模一样，标题是唯一一直看得见的区分处。
      `crates/gui/tests/layout.rs::窗口标题写明哪一份库与哪一屏`（五屏逐个查那串字）
      与 `::换一屏就把窗口标题改掉`（查那条命令**真的发出去了**，而且没换屏时不重发）。
- [x] 键盘焦点在所有可操作件上都有可见状态
      —— 两条路各修一半。走 egui 按钮那条路的（按钮、勾选框、可选标签、文本框）：
      `look::install` 把 `widgets.active.bg_stroke` 换成主题自己的**强调色**
      （`selection.stroke`，也就是 `TextEdit` 拿到焦点时那一圈），于是四类控件拿到焦点长的
      是同一个样子；**宽度一个点都不动**——egui 把描边宽度反过来从按钮内边距里扣，加宽会让
      控件在拿到焦点那一帧缩一下，焦点在控件间跳时整排按钮跟着抖。
      自己画底色的（表格的**行**、缩略图那几格）走不了那条路，而它们**是点得中的**、
      Tab 走得到（`Sense::click` 自带 `FOCUSABLE`）——各自描一圈
      （`look::focus_ring`，接在 `table.rs`、`queue.rs` 两张表的行上；`media.rs` 那几格
      在自己那道边框里头再描一圈）。
      `crates/gui/tests/layout.rs`：`::拿到键盘焦点的控件画得不一样`（按一下 Tab，画出来的
      描边里多出那一串强调色）、`::自己画底色的可点件也描得出焦点那一圈`（跑的就是
      `focus_ring` 那条路）、`::五屏上按一下跳格键焦点都落得下去`（五屏各按一下 Tab，
      焦点都落得到某个控件上——「可见」的前提是走得到）。
      焦点那一圈**夹在滚动视口里**（`focus_ring` 收一个「看得见的那一块」，取它的竖直范围）
      ——表格是虚拟化的，最上那一行常只露半截，不夹的话那 2 点宽的上沿会画到表头上
      （挂单 `Q132` 第 3 条）。缩略图那一格的圈画在 `rect.shrink(2.0)` 上，**不顶掉**
      「池里没这个文件」那道警告边框。
      **保留一条**：「所有」是按**类**核对的（四类走按钮那条路的 + 表格行 + 缩略图格），
      不是逐个控件扫一遍——headless 里没有「枚举这一屏所有可聚焦件」的办法。
- [x] 门禁命令全绿（带 `--all-features`）
      —— `cargo clippy --workspace --all-targets --all-features -j 1`：`Finished`，
      0 error 0 warning。`cargo test --workspace --all-features -j 1 --no-run`：`Finished`。
      测试分包跑（这台机器内存吃紧）：**58 个测试二进制全部 `test result: ok`，
      累计 1,433 条通过、0 条失败**（gui 14 / 146，core 30 / 1,191，cli 14 / 96），
      带 `--skip 断点落在主库内直接拒绝开工`（那条在这台机器上挂死，挂单 `Q8`）。

## Comments

### 前面十一张票点名交给这一票裁的十一条挂单

裁定与理由全文在 `.scratch/PARKING-LOT.md` 各条的「**票 `12` 裁定**」段里，这儿只列去向。

**做掉（6 条）**

- `Q61` 界面上那个「库」屏的模块叫 `roots`，因为 `library` 被库浏览占着
  —— `library.rs` → `browse.rs`、`tests/library.rs` → `tests/browse.rs`、
  `View::Variants` → `View::Browse`、`App::library()` → `App::browse()` 等一并改名。
  `roots.rs` **不**跟着改名叫 `library.rs`（理由见挂单）。**resolved**
- `Q73` 左栏上下两半在做同一件事 —— 走「两半各管一段、在屏上说清」这条：上半抬头
  「一按就有的档」（探索，各带条数），下半从「筛选器」改成「条件组」（表达，值要自己打）。
  **resolved**
- `Q83` `Tier` 四档只有待确认屏在用 —— 五屏对齐做掉，见验收第 4、5 条。**resolved**
- `Q90` 「删掉这个子库」留成了按两下 —— **留着**（这一票之后规则与例外不在这一屏输入，
  一下点掉之后那几条手挑的例外再也重打不回来），但把那句悬停改成**数着说**：
  「要按两下：{N} 条规则与 {M} 条例外跟着一起没」。**resolved**
- `Q117` 「★ 收藏」在抬头、「取消收藏 + 自建合集」在左栏底下 —— 位置不动（原型是用户
  2026-09-04 确认过的），**两块互相指一句**：找不着是这条里真正的那一半。**resolved**
- `Q126` 缩略图那几格的尺寸照原型定死 —— 原型那条是 `minmax(78px, 1fr)`，实现只落了前半截；
  这一票**照原型把后半截补上**（`media::cell_size`，格子跟着详情面板的宽度长），
  不加「缩略图大小」滑块。**resolved**

**明确不做（1 条）**

- `Q59` 界面上加根只有输入框，没有系统的目录选择对话框 —— **不做**。这张票的硬约束里写着
  不新增 Rust 依赖，而 `rfd` 之类会带进 GTK/AppKit 的运行时依赖，与 ADR-0005 当初
  「单一二进制、无 WebView 依赖」那笔账是同一类问题。要做它得先为那笔依赖立一条 ADR。
  **closed（裁定：不做）**

**升成新票（4 条 → 3 张票）**

- `Q64` 作品级主列表按聚合列排序 ＋ `Q108` 搜索那三条命中路让翻页贵一倍出头
  → **票 `gui-redesign/13`**（两条是同一条代价线上的两半，而且眼下的两组数一半 release
  一半 dev、不可直接比）
- `Q86` 读不懂的规则在界面上删不掉了 → **屏上那一半做掉**（卡上现在写明「界面上改不动：
  按序号去命令行删 `romcat sublibrary rule <子库> --remove <序号>`」——**看不出下一步**是
  这条里最贵的那一半），**界面上删得掉那一半** → **票 `gui-redesign/14`**
  （加回一颗「删」会推翻票 11 立的「这一屏不选内容」，而那条是这张票的硬约束）
- `Q87` 「算一遍容量」还跑在画帧线程上 ＋ `Q89` 同步没进任务台
  → **票 `gui-redesign/15`**（子库屏上还剩两条长活没走任务台，一起搬才划算）

**顺带收的词表缺口（`Q49`/`Q77`/`Q80`/`Q84`/`Q99`）** → **票 `gui-redesign/16`**：
`docs/agents/domain.md` 的规矩是记下来交给 `/domain-modeling`，不是自己往词表里写；
四条一起交，因为「批」那两个意思与「跳过」那两个意思要一起看才分得开。

### 这一票新记的挂单（4 条，编号到 `Q132`）

- `Q129` 面板窄不过它最窄的那个子控件，而声明的 `min_size` 比它松（连带记下
  「内容不占满地方就拖不动」这条 egui 事实）
- `Q130` 版式偏好那个文件不带 `Slug`，两个 romcat 窗口同时开着会互相盖（最后松手的赢）
- `Q131` 高置信那一档换了颜色：`selection.bg_fill` → `selection.stroke`（对比度
  2.33 → 12.41），这改了票 `09` 定的那一串
- `Q132` `/code-review high` 报的五条，**五条当场收掉**（版式被窗口尺寸削掉、竖直方向上
  正中那块留不住、焦点圈画出滚动区、测试与演示窗口共用工作目录、还开着的挂单指向旧路径）

### 票据本身没写错

八条验收逐条对得上做出来的东西。唯一要说明的是第 4 条「在**五屏**里逐字一致」——
五屏里只有浏览与待确认两屏真会标置信度，另外三屏上没有变体级的结论可标。这不是票写错了，
是「对齐」在那三屏上的意思是**不许凭空造一个**；口径写进了 `Tier` 的文档。
