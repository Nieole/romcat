# 07 — 占位活那四种写法收成一种

**What to build:** 按停那类界面测试**等一个信号**而不是等六秒挂钟——机器忙的时候它不再假失败。

收挂单 `Q427`（另有旧账 `Q196` / `Q349` 记着同一件事）。

**Blocked by:** 无 —— 可立即开工

**Status:** done

⚠️ **这不是预防性洁癖，有案底**：那个写法**真的咬过一次**——早先一份「四百步 × 五毫秒」
正好两秒的版本，机器一忙就自己先结束、测试假失败（那处注释自己记着这件事）。

⚠️ **存量是四种写法散在五个文件**（一处四遍逐字相同的「三千步、每步睡两毫秒」，
另有抽成 helper 的、常量化的、又一种）。收成**一处共享的 helper**，放界面测试**现成的
共享夹具**里——三个文件已经在用它，第四个加一行引用即可。

⚠️ **只收占位活那四种。轮询那一族（六个文件）与等图那八处不动**——它们等的是
「后台线程真的跑完了」「图真的下下来了」，那个不确定性是**真的**，换成信号要改任务台的
公开面，那是另一件事。**占位活不一样：它假装在干活，而「干多久」本来就该由测试说了算。**

- [x] 那四处改完，全部走同一个共享 helper

  证据：共享夹具 `crates/gui/tests/shared/mod.rs` 末尾新加 `占位活`（`排上` / `照这样排上` / `id` / `按停` / `放行`），
  底下是一对 `mpsc` 信号（`一对信号` / `信号::发` / `等信号::等`）。grill `:162`–`:167` 那四种一处不剩：
  - `roots.rs` 四处 `3_000 × 2ms` 换成 `占位活::排上`：`这一趟正在跑的时候那一行的按钮按不下去`、`折标题排着队被撤掉时标题集合一条都没少`、
    `导出排着队被撤掉时一份元数据都没写出去`、`台上已经有一趟识别时再按队列屏那颗捷径_不会排第二趟`；
  - `sublibrary.rs` 的 `现场::占住位子` 删了，调它的六条测试直接用 `占位活::排上`；
  - `task.rs` 的 `占位步数` 与 `排一趟占位的` 删了，那一族走 `排上`（三处）或 `照这样排上`（两处：要先报进度再等的那条、要把「被按停」折进 `CollectionError` 的那条）；
  - `media.rs` 那个 `AtomicBool` 放行闸换成 `排上` 加 `放行`，文件头加了 `mod shared;`。

  `grep -rnE '0\.\.3_000|0\.\.40_000|占位步数|AtomicBool|fn 占住位子|fn 排一趟占位的' crates/gui/tests` 零命中。
  **保留**：`roots.rs` 的 `占住任务台`（七处，`check` 加睡 1 ms 等按停）与 `task.rs` 的 `排一趟停在原地的`（两处，睡 2 ms）不在 grill 清单里，代码没动。
  两处都没有挂钟窗口，可同一件事眼下仍是两种写法，见挂单 `Q741`。

- [x] 那个 helper **等一个信号**，测试想让它停就发信号

  证据：`占位活::排上` 的活体只有 `等着.等()`（`Receiver::recv`），一步不走、一毫秒不睡。`按停` 先走 `Board::stop` 再发信号——次序反了会时灵时不灵地记成失败，文档写着；
  `放行` 只发信号；丢掉 `占位活` 就等于发。信号选 `mpsc` 不选 `Condvar`：「发的那一头丢了，等着的那一头就醒」是通道自带的，测试半路炸了也不会把台子占到进程结束。
  钉它的是 `task.rs` 新加的两条，先红（`占位活` 不存在，编不过）再绿：
  - `占位活没收到信号就一直占着台子_按停之后记成已取消`：三十帧后它仍在台上、进度停在 0、排在后面那一趟仍排着；按停后记成已取消，后面那一趟完成。
  - `占位活放行了就收场_丢掉它也一样`：只放行记成失败，丢掉也收场。

  审查之后另加一道守卫：活若跑在排它的那条线程上，就当场交一句失败、不去等——任务台哪天改成就地跑活，测试是红，不是挂死。这一道没有测试钉着（公开面上造不出就地跑的 `queue`）。

- [x] 全仓新增**零处**睡毫秒式的占位活

  证据：`git diff 56ae948 -- crates` 的新增行里 `sleep|from_millis|from_secs|Instant` 零命中。
  `crates/gui/tests` 里剩下的 11 处 `thread::sleep`：八处是轮询（`from_millis(10)` 七处、`browse.rs:2557` 等进度报出来的 1 ms 一处），
  一处是 `等图`（`media.rs:162`），另两处是 `Q741` 那两个基点之前就在的等按停写法。

- [x] 那几条按停测试照旧绿，且不再有六秒的挂钟窗口

  证据：2026-09-14，这棵树，审查改完之后 `TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp cargo test -p romcat-gui --all-features -j 3 --no-fail-fast --test task --test sublibrary --test roots --test media`：
  task 10、sublibrary 31、roots 57、media 9，全过，`EXIT=0`。审查之前那一趟跑的是 `romcat-gui` 的全部测试目标，全绿。
  占位活自己不会收场，没有哪个时长让它先走；按停那几条收尾时仍用原来那一族轮询等台子空。
  顺手：`task.rs` 两条把「跑一帧就按停」换成现成的 `等到那一趟报出`。原来按早了，它在 `step` 上就交出 `Halted`，折进领域错误的那条路一次都没走到。
  **保留**：`任务跑着的时候别的屏照常画得出来` 末尾「按下之后不到 3 秒台上就空」那条上界断言留着，见挂单 `Q742`。

- [x] 轮询那一族与等图那八处**一个字没动**

  证据：
  - `from_millis(10)` 的计数在 roots / sublibrary / task / media / program 上，基点与现在逐一相同（1 / 2 / 2 / 1 / 1）；
  - `media.rs` 的 `from_secs(20)` 基点 9 处、现在 9 处；
  - diff 里没有一行碰 `等任务跑完`、`画到台上空了`、`等到那一趟报出`、`等图` 的签名或 `0..600`；
  - `crates/gui/src` 与核心库零改动。

**门禁**（2026-09-14，这棵 worktree，`TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp cargo xtask gate -j 3 --test-threads 3 --keep-going`，跑的时候全部改动都还没提交）：
六条全绿，`EXIT=0`。fmt 1s / glossary 0s / check 14s / clippy 18s / test 397s / doc 8s。
test 那一步共 73 个测试目标（`Running` 与 `Doc-tests` 合计），**1,973 条通过、0 条失败、2 条 ignored**（`magnitude.rs` 那两条量级测量）。
日志里那几行 `error: unresolved link` 出自 xtask 门禁测试自己造的「故意红」小 crate（`throwaway`），不是这棵树的文档。
glossary 印的范围是「merge base 就是 `HEAD`（56ae948）……只看未提交的改动」。本票的改动那时全在未提交里，一行没漏扫。
日志在 `/Users/nicoer/dev/game-wt/logs/slot-2-mc07-gate.log`，开头两行是这棵树的路径与分支。

**收挂单：** `Q427` 落在 `crates/gui/tests/shared/mod.rs` 的 `占位活`。它的正文在票 `gui-self-sufficient/07` 末尾，底下加了一行「落地（2026-09-14）」；结算索引那一行不动。
旧账 `Q196`（头一帧 < 1 秒）与 `Q349`（`等任务跑完` 那句六秒）说的是断言与轮询那两张挂钟彩票，不在这张票里收，条目没动。

## 挂单裁决（第五轮收口，2026-09-23）

从 `.scratch/PARKING-LOT.md` 迁来（`/settle`，范围 `Q449`–`Q1185`）。每条顶上多一行**裁决**：落哪一档、去了哪儿。
编号 `Qn` 留着占号、不复用。

### Q741 — 两处等按停的占位活（`占住任务台`、`排一趟停在原地的`）没并进共享的 `占位活`

- **裁决（第五轮收口，2026-09-23）：** **活** —— 无家可归（全仓没有一张开着的票占这片地），进 `/grill-with-docs`，束「门禁、测试与约定」。

- **来自：** 票 `machine-checks-premises/07`
- **类别：** 规格没说
- **在哪：** `crates/gui/tests/roots.rs` 的 `现场::占住任务台`（`loop { check; sleep(1ms) }`，七处调用，票 `gui-answers-all-six/04` 于 2026-09-14 加的，晚于 grill）；`crates/gui/tests/task.rs` 的 `排一趟停在原地的`（`loop { check; sleep(2ms) }`，两处调用，2026-09-08 起就在）。grill `:162`–`:167` 那份清单两处都没列
- **为什么没停线：** 两处等的都是按停，没有挂钟窗口，不会自己先收场、害测试假失败；并不并只动测试文件，不出这张票。
- **这张票实际做了什么：** 照 grill 清单只收四种，这两处的代码一行没动（`排一趟停在原地的` 文档里那句「与 `排一趟占位的` 的分工」因为那个函数删了，改指 `占位活`）。
- **另一条路：** 一起并进：`占住任务台` 那七处换成 `占位活::排上` 加 `按停`；`排一趟停在原地的` 换成 `占位活::照这样排上`，先报进度、再等信号。
- **建议留哪条：** 并进。票题是「收成一种」，而 `roots.rs` 眼下同一件事两种写法 7 : 4 并排，下一张照着库屏那份抄的票（`gui-looks-like-the-design/05`）多半抄到旧的那种；`Q427` 正文自己就写过「单改一处会分家」。改法是机械的，二十来行。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q742 — 按停那条测试的「按下之后不到 3 秒台上就空」还是一条挂钟断言，而占位活已经改成被信号叫醒

- **裁决（第五轮收口，2026-09-23）：** **岔** —— 实现走的就是条目建议的那条；追认：维持现状（2026-09-23 拿主意的人整批追认）。

- **来自：** 票 `machine-checks-premises/07`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/tests/task.rs` 的 `任务跑着的时候别的屏照常画得出来` 末尾那句 `按下之后过了 < Duration::from_secs(3)`
- **为什么没停线：** 它不是占位活，是一条上界断言；测试线程要被卡住三秒以上才会假失败。
- **这张票实际做了什么：** 这条断言和它那段注释一字没动。`按停` 先走 `Board::stop`、再发信号叫醒占位活之后，这个数量的只剩「那条线程收场之后任务台隔多久收得到」，不再量「活自己多久看一眼停下」。
- **另一条路：** 删掉它，只留「记成已取消」那条断言。这一族挂钟彩票（`Q196` / `Q349`）在这份文件里就只剩这一处。
- **建议留哪条：** 留。它仍给「按停之后台子会空」钉着一个上界；一趟线程收场是毫秒级的事，三秒有三个数量级的余量。真在门禁上红过一次再删。
- **谁来裁：** 收尾
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）