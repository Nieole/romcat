# 06 — 两条量级基准留在树里

**What to build:** 维护者跑一条命令就能复核折标题与导出的**量级**——它们还是不是差着两个
数量级。此前那两趟量完就删了，复核要照挂单里那段重新搭一遍。

收挂单 `Q426③`。

**Blocked by:** **05 — `docs/agents/` 多一份跑长活的规程**

**Status:** done

⚠️ **新开一份量级测量的测试文件**，两条都放它，都挂 `#[ignore]`。三条理由：
① 它们不是回归测试、是**量级测量**，混进主题测试文件会让读者以为它们在验行为；
② 两条共用「造一份 N 个变体的合成库」那套夹具，放一处才不重复搭；
③ `cargo test -- --ignored` 的产物因此是**一份读得懂的量级报告**。

⚠️ **不引 criterion、不建 `benches/`。** 那两趟量的是**数量级**（折算真库 1.7 秒与 2.0 秒，
对着一帧 16 毫秒的预算差两个数量级），不是要看 5% 的回归——criterion 那套统计是为后者准备的。

⚠️ **这是仓库第一条 `#[ignore]`，房规同时定，写进票 `05` 那份规程**：
`#[ignore]` 只用于**要人主动跑、且跑起来很贵**的东西；**不用于「暂时挂起的失败测试」**
——那种要么修要么删。

⚠️ **不进门禁。**

- [x] 那份新文件里两条基准都挂着 `#[ignore]`，平常一条都不跑

  证据：`crates/core/tests/magnitude.rs` 的 `整理标题一趟的量级` 与 `导出一趟的量级` 各挂 `#[ignore = "…"]`，理由里带跑法。
  2026-09-13 常规跑 `cargo test --workspace --all-features --test magnitude`：`1 passed; 0 failed; 2 ignored`。
  门禁 `test` 那一步不带 `--ignored`（`xtask/src/gate.rs` 的 `test`）。`grep -rn '#\[ignore' crates xtask` 里挂着的属性只有这两处，其余命中是文档里提到这个词。

- [x] `cargo test -- --ignored` 跑得起来，两条都报得出耗时与折算到真库的量级

  证据：`cargo test --workspace --all-features --test magnitude -- --ignored`，这台开发机（8 核）、debug 构建，三趟都 `2 passed`，
  不带 `--nocapture` 报告也印得出来（直接写 stderr）。合成库 4,000 个变体 / 4,000 个作品 / 10 个平台，`title::fold` 一趟交出 8,000 条叫法，
  收敛成 4,000 个条目、10 份元数据文件。括号里是「折到真库量级 5 万个变体的秒数 / 差几个数量级」（对一帧 16 毫秒）：

  | 哪一趟 | 整趟 | `title::fold` 3 趟 | `converge::run` 3 趟 | `transfer::export` 只排计划 2 趟 |
  |---|---:|---|---|---|
  | 2026-09-13 23:46，审查前的代码（被量的函数与之后一字不差） | 6.30 秒 | 90.0–96.5 ms（1.12–1.21 s / 1.9） | 173.7–177.1 ms（2.17–2.21 s / 2.1） | 312.0–316.2 ms（3.90–3.95 s / 2.4） |
  | 2026-09-14 00:06，最终代码 | 17.62 秒 | 265.3–310.5 ms（3.32–3.88 s / 2.4） | 467.5–538.8 ms（5.84–6.74 s / 2.6） | 797.8–945.1 ms（9.97–11.81 s / 2.9） |
  | 2026-09-14 00:07，最终代码；起跑前后一分钟负载 50.78 / 49.56，另有别的项目与兄弟 worktree 的测试在跑 | 7.76 秒 | 102.4–108.4 ms（1.28–1.35 s / 1.9） | 181.7–211.8 ms（2.27–2.65 s / 2.2） | 338.8–350.1 ms（4.24–4.38 s / 2.4） |

  三趟之间差出近三倍，全是机器忙不忙；差的数量级落在 1.9–2.9 之间，「差着两个数量级」这个结论三趟都成立。

  **保留**：折算乘的是量级 5 万、不是台账的确切变体数（挂单 `Q531`）；合成库识别过、整理过标题，与当初那两趟不是同一种库，`crates/core/src/stage.rs` 那两支文档里的合成库数已换成这份夹具量出来的（挂单 `Q532`）。

- [x] 两条共用同一套合成库夹具，没有第二份

  证据：`合成库::造` 是这份文件里唯一造库的函数，两条基准都调它。夹具本身由不挂 `#[ignore]` 的
  `合成库造几个变体就真有几个_每个都认出了作品` 钉着（12 个变体摊在 3 个平台上各 4 个、认出 12 部作品）：先红（骨架造出 0 个变体），再绿。

- [x] 全仓没有 criterion、没有 `benches/`

  证据：`grep -rni criterion`（`*.toml` `*.rs` `*.lock`，不含 `target/`）只命中新文件文档里那句「不引 criterion」；
  `find . -name benches -type d -not -path './target/*'` 零个。

- [x] `#[ignore]` 的房规写进了票 `05` 那份规程

  证据：`docs/agents/long-jobs.md` 末尾新增一节「`#[ignore]` 只挂要人主动跑、跑起来很贵的东西」：判据一句，挂之前核三条，跑法，以及「暂时红着的测试不挂」。
  **保留**：头一批两条一趟 6–18 秒，「很贵」那一半站得不稳，见挂单 `Q533`。

**门禁**（2026-09-14，这棵 worktree，`TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp cargo xtask gate -j 3 --test-threads 3 --keep-going`）：
五条全绿，`EXIT=0`。fmt 2s / check 25s / clippy 31s / test 564s / doc 10s。
test 那一步共 71 个测试目标（68 个测试二进制加 3 份 doc-tests），**1,833 条通过、0 条失败、2 条 ignored**。
ignored 的正是 `magnitude.rs` 那两条基准；这份文件是这一趟新多出来的测试目标，门禁里只跑夹具那一条。
日志在 `/Users/nicoer/dev/game-wt/logs/slot-4-mc06-gate.log`，开头两行是这棵树的路径与分支。

**收挂单：** `Q426③` 落在 `crates/core/tests/magnitude.rs`。`Q426`（票 `gui-self-sufficient/07`）与量的那一半出自的 `Q436`（票 `08`）
条目底下各加了一行「落地（2026-09-13）」；`crates/core/src/stage.rs` 那两支文档的合成库实测换成这份文件量出来的数，并指向它。
