# 02 — 门禁多一步扫词表

**What to build:** 接票的 agent 新写的标识符或屏上文案撞上词表 `_Gate_` 里的词，
**门禁当场报红**，并说清三样：**哪一行、命中哪个词、该用哪个正名**。它只看**新写的代码**，
所以两千处存量一处都不报。

收挂单 `Q365`（防法那一半）与 `Q424`（没有任何东西在新代码上跑一遍词表）。

收挂单 `Q513`：加上新的那一条时，票 `01` 证据里「列着名字的几处」一起补上它的名字（拿主意的人若裁成删清单，就照裁决删）。

**Blocked by:** **01 —（prefactor）「门禁几条」那个数从措辞里消失**、
**05 — `docs/agents/` 多一份跑长活的规程**

**Status:** done

⚠️ **范围：相对 `main` 的 merge base 以来的全部改动，含未提交的。** 接票的 agent 跑门禁时
改动一半已提交一半没有（它跑门禁正是为了决定要不要提交），只看一半都会漏。
**在 `main` 上直接干活时 merge base 就是 `HEAD`，于是退化成「只看未提交的」——这是对的，
但要往票 `05` 那份规程里补一段说明**，否则有人会以为门禁在 `main` 上失效了。

⚠️ **扫标识符与字符串字面量；不扫注释与文档注释**（那里是在**谈论**禁词）、
**不扫 `.scratch/`**、**不扫词表自己**。

⚠️ **词表从 `CONTEXT.md` 读，不在门禁里另抄一份。** 抄一份就是当场犯这一批要治的病；
而「哪些词严到可以报红」是领域判断，ADR-0024 说它只许有一处实现。

⚠️ **形状：新那条仍然是 cargo**（递归的 xtask 子命令），保住「门禁每一条都是 cargo 子进程」
那条形状。检查本体是纯函数：**（`_Gate_` 词表 × 改动的行）→ 命中**，单元测试直接打它。

⚠️ **加这一条会撞三条现成测试**：硬断言五条名字的那条、断言「不带 `--all-features` 的
只有一条」的那条、断言完整命令字面量的那条。三条都要跟着改。

⚠️ **把这道机器的价值如实写进它自己的文档**：它**一次也没能「重放」本轮三次事故**——
`job_of` 拦不住（一条正则分不开它与 `default_jobs`，后者指并发度、按词表豁免第 1 类不算违规）、
「输出目录」写在票与规格里而那侧不扫、`Step` 压根不在 `_Avoid_` 里（词表禁的是中文「步骤」）。
**它拦的是将来那些多字中文近义词。** 不写这一句，下一个人会以为它比实际强。

⚠️ **它咬得住每一次 push，不只是 agent**：门禁从 2026-09-08 起就在 GitHub Actions 上真跑。

- [x] 新写一个撞 `_Gate_` 的标识符，门禁**报红**，并说清哪一行、哪个词、该用哪个正名
  —— 纯函数那一半：`xtask/src/glossary.rs` 的 `新写的标识符撞词就报出哪一行哪个词该用哪个正名`（断言整个 `Hit`：行、词、正名、词表行号、落在标识符上）。退出码那一半：`xtask/tests/gate.rs` 的 `词表那一条撞词就红并说清哪一行哪个词该用哪个正名改掉就绿`，在丢弃 git 仓库上直接跑编出来的 `xtask glossary`：撞词退非零，输出里有 `crates/a/src/lib.rs:2`「近义一」「正名甲」；改掉就退 0。真仓库探针（2026-09-14，本分支，未跟踪的 `crates/core/src/zz_gate_probe.rs`，跑完即删）：印出 `crates/core/src/zz_gate_probe.rs:1  标识符里撞上「批次号」→ 该用「批」（CONTEXT.md:202）`，退出码 1；`批次号` 里套着的「批次」只报长的那个（`长词里套着的短词与同一行的重复只报一次`）。
- [x] 同一个词出现在**注释**里，**不报**
  —— `注释与文档注释里的同一个词不报`：`//`、`///`、`//!`、嵌套的 `/* /* */ */`、行尾注释、跨行的 `/** */` 都不报；同一份源码里代码中的那个词照报，字面量里的 `"// …"` 不当注释。上面那份探针第 2 行 `// 批次 只在注释里` 没报。
- [x] 同一个词出现在一份**没改动过**的文件里（已知存量），**不报** —— 这一条钉住「只扫新代码」
  —— `词表那一条扫分支上已提交未提交与新建的改动而存量一处不报`：丢弃仓库里 `old.rs` 带一处存量命中、分支上没动它，不报；而且 `main` 在分出去之后把 `old.rs` 那处改掉了——拿 `main` 的尖端比就会把它误读成新写的，这条钉的是比 merge base。纯函数层另有 `新写的标识符撞词就报出哪一行哪个词该用哪个正名` 的第 1 行（撞词但不在改动的行里）不报。真库存量：`crates/` 下含 `_Gate_` 词的 `.rs` 64 份、出现 218 处（含注释，`grep` 口径，2026-09-14）；全量门禁里这一条扫出 0 处（本票没碰 `crates/`）。
- [x] 撞 `_Gate_` 的**字符串字面量**照样报红
  —— `字符串字面量撞词照样报`：普通字符串、跨行字符串（报词真正所在的那一行）、带引号的 `r#"…"#`，以及 `'"'` 字符字面量不许把后面读成字符串。探针第 3 行 `"预演"` 印出 `字符串字面量里撞上「预演」→ 该用「差量预览」（CONTEXT.md:373）`。
- [x] `_Gate_` 里出现了它那条 `_Avoid_` 没有的词时，这一步**当场报错**（子集校验）
  —— 单元测试 `门禁词越出它那条近义词就当场报错`；退出码 `词表里门禁词越出它那条近义词时这一条当场红`（非零，输出里有 `CONTEXT.md:3` 与越界的词）。按审查补的同类：走了样的 `_Gate_` 行、空的 `_Gate_` 行、同一词条里第二行 `_Avoid_`、整份一个 `_Gate_` 词都读不出来，各一条测试，都当场报错——悄悄跳过就是假绿。`仓库里那份真词表读得通而且读得出门禁词` 钉着真 `CONTEXT.md`；全量门禁里这一条印「读出 33 个 `_Gate_` 词」（12 行 `_Gate_`）。
- [x] 在 `main` 上跑时只看未提交的改动，且这一行为写进了票 `05` 那份规程
  —— `词表那一条在_main_上只看未提交的改动`：`main` 上已提交的存量不报、未提交的报，范围是 `Scope::SinceMergeBase { on_base: true }`。规程：`docs/agents/long-jobs.md`「读回执」补了两段——在 `main` 上只看未提交的不是失效；`glossary` 那一条的「绿」可能是跳过。**保留**：分支刚开出来、还没有自己的提交时也是这个样子（全量门禁那一趟就是：「merge base 就是 `HEAD`（3976ff1）——站在 `main` 上，或者这条分支还没有自己的提交」），输出与规程都照实写了。
- [x] **在 CI 上也跑得起来**：`actions/checkout@v4` 默认浅克隆 1 层、merge base 求不出来——要么给它 `fetch-depth: 0`，要么这一步拿不到历史时**如实跳过并说明**（不许假装扫过了）
  —— 两条都做（挂单 `Q545`）：`.github/workflows/gate.yml` 的 checkout 给了 `fetch-depth: 0`；代码里浅克隆、找不到 `main`、不是 git 仓库时交 `Scope::Skipped`，印「跳过：<原因>。」与「这一趟一行代码都没扫——不是扫过了没撞上」，退 0。钉子：`词表那一条在浅克隆上如实跳过而不是假装扫过`（真的 `git clone --depth 1`）、`词表那一条找不到_main_时如实跳过`。**保留**：① 没在 GitHub Actions 上真跑过（本票不 push）；② 跳过退 0，门禁汇总表上仍记「绿」（挂单 `Q542`）；③ **推到 `main` 的那一趟 merge base 就是 `HEAD`，一行都不扫**，而这一轮是在本地合进 `main` 再推、不开 PR——票面「它咬得住每一次 push」在眼下的流程里不成立（挂单 `Q543`，已交拿主意的人）。
- [x] 门禁那三条现成测试跟着改完，全绿
  —— `门禁就是这几条而且顺序是从便宜到贵`：名字列表加 `glossary`（排在 `fmt` 后）；`每一条打印出来对得上_readme_那一段`：加 `cargo xtask glossary` 一行；`交付出去的那份配置还有一条在看着` **断言没动**——票面预计它会撞，实际不会：它除了「不带 `--all-features`」还按 `--workspace` 过滤，`glossary` 两个开关都不带；只把局部名 `五条` 改成 `门禁`，并补一句注释说清。两个带「五条」的测试名去掉了数（挂单 `Q544`；票 `01` 证据里引的旧名从此 grep 不到）。全量门禁里三条都绿（见下）。
- [x] 那道机器的文档里写明它拦不住本轮那三次事故里的哪几次
  —— `xtask/src/glossary.rs` 模块文档「它拦不住什么」：三次一次都拦不住——`job_of`（`Q424`，`job` 不在 `_Gate_` 里）、「输出目录」（`Q439`，写在票与规格里，这一条不扫 `.scratch/`）、`Step`（`Q415`，压根不在 `_Avoid_` 里）；并写明「它拦的是将来那些多字中文近义词」。同一份文档还写明推到 `main` 的那一趟 CI 一行都不扫。

**收挂单：**
- `Q365`（防法那一半）：落的是**代码那一侧**。`Q365` ② 原话是扫 `.scratch/**/*.md`，grill 收窄成只扫代码，于是「没有任何东西在票与规格上跑一遍词表」**仍然没有机器**。`.scratch/parking-3/issues/18-close-the-parking-lot.md` 那一条底下加了「落地（2026-09-14）」。
- `Q424`：`.scratch/gui-self-sufficient/issues/06-stages-section-and-identify.md` 那一条底下加了「落地（2026-09-14）」，写明 `job_of` 本身它拦不住、库屏那两处旧账不会让门禁红。
- `Q513`：照「留清单」补上 `glossary` 的名字——`README.md` 门禁块、`xtask/src/main.rs` 的 `--help`、`.github/workflows/gate.yml:3`、`xtask/src/gate.rs` 的 `Step::name`；`xtask/Cargo.toml` 与根 `Cargo.toml` 那两句列的是「盖着 `xtask` 的那几条」，各补一句 `glossary` 不看它；「唯一一条跑在默认特性上」（`gate.rs:20`、`README.md`）改成「唯一一条在默认特性上编整个工作区的」；`rust-toolchain.toml` 补上 `glossary` 要的是 git。挂单 `Q513` 标 `settled`。

**门禁与审查：** 全量门禁（`TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp cargo xtask gate -j 3 --test-threads 3 --keep-going`，2026-09-14，本分支上的改动）：fmt 3s／glossary 0s／check 7s／clippy 18s／test 713s／doc 16s，末行 `6 条全绿。`，`EXIT=0`；test 那一步 70 个测试目标、1,851 条通过、0 条失败（票 `01` 那趟是 1,832，多出的 19 条正是本票加的 13 条单元测试与 6 条集成测试）。日志 `/Users/nicoer/dev/game-wt/logs/slot-3-mc02-gate.log`（2,393 行，开头两行是本工作树与本分支）。`/code-review` 两轴回执开头列出的审查范围与自取的 14 个文件逐个对得上。按审查改了：两处注释里「字面量写真词会先红在自己身上」的理由（`xtask/` 不在扫描口径里，不成立）、`Q513` 落地行里挪了位的行号、`Hit` 改成直接带一个 `GateWord`、三个函数改名（`push_literal` / `push_raw_literal` / `in_scope`）、`gate.yml` 那段收成一句指针、一行都没扫时不再印「没撞上」；补了上面第 5 条那几条词表走样的测试，以及 `只在近义词里没进门禁词的词不报`。没改的：「空 `Report` 构造两遍」（两处范围不同，抽函数不划算）、「三份 toml 各补一句」（`Q513` 已裁留清单）；Spec 轴报的推 `main` 不扫、跳过记绿、测试改名，分别是挂单 `Q543`、`Q542`、`Q544`。
