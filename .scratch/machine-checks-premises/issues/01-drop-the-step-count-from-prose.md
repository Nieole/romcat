# 01 —（prefactor）「门禁几条」那个数从措辞里消失

**What to build:** 散文里不再写死门禁有几条。约**十五处**写着「门禁五条」——门禁跑手的
`about` 与那段长文档、门禁模块头部的注释、两个 `Cargo.toml`、`README.md` 三处、
GitHub Actions 那个 job 名——**其中一处已经漂成了「四条」**。全改成不带数的说法
（「门禁那几条」「每一条」）。

**这是 prefactor。** 做完之后，票 `02` 加第六条只需碰三条测试加一处那张条目表，
**不必再去改十五处散文**——而「改一处得改十五处」正是这一批要治的病本身。

收挂单 `Q380`（那一族没人盯着的数，第三次被记）。

**Blocked by:** 无 —— 可立即开工

**Status:** ready-for-agent

⚠️ **真正的钉子留着不动**：那条硬断言五条名字完整列表的测试，才是机器可核的断言。
门禁打印的那张汇总表**本来就是算出来的**（从那张条目表的长度取），也不动。
**只改散文里手抄的那个数。**

⚠️ 这个数**不进票 `03` 的生成机制**：要把生成机制铺进一个 GitHub Actions 的 job 名，
是拿大锤敲图钉；而那个数对读者本来就没有信息量——要知道跑哪几条就 `--list`。

- [ ] 全仓 `grep` 不到「门禁五条」「门禁四条」这类写死条数的说法
- [ ] 那条断言五条名字的测试**一个字没改**，照旧绿
- [ ] 门禁跑起来打印的汇总表照旧报得出总条数（它是算的，不是抄的）
- [ ] `--list` 照旧列得出跑哪几条
- [ ] 顺手记下改了哪几处，票 `02` 的人拿它当清单
