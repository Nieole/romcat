# 01 —（prefactor）「门禁几条」那个数从措辞里消失

**What to build:** 散文里不再写死门禁有几条。约**十五处**写着「门禁五条」——门禁跑手的
`about` 与那段长文档、门禁模块头部的注释、两个 `Cargo.toml`、`README.md` 三处、
GitHub Actions 那个 job 名——**其中一处已经漂成了「四条」**。全改成不带数的说法
（「门禁那几条」「每一条」）。

**这是 prefactor。** 做完之后，票 `02` 加第六条只需碰三条测试加一处那张条目表，
**不必再去改十五处散文**——而「改一处得改十五处」正是这一批要治的病本身。

收挂单 `Q380`（那一族没人盯着的数，第三次被记）。

**Blocked by:** 无 —— 可立即开工

**Status:** done

⚠️ **真正的钉子留着不动**：那条硬断言五条名字完整列表的测试，才是机器可核的断言。
门禁打印的那张汇总表**本来就是算出来的**（从那张条目表的长度取），也不动。
**只改散文里手抄的那个数。**

⚠️ 这个数**不进票 `03` 的生成机制**：要把生成机制铺进一个 GitHub Actions 的 job 名，
是拿大锤敲图钉；而那个数对读者本来就没有信息量——要知道跑哪几条就 `--list`。

- [x] 全仓 `grep` 不到「门禁五条」「门禁四条」这类写死条数的说法
  —— 派活给的正则 `grep -rnE '门禁[五四六]条|[五四六]条门禁|五条全绿|门禁那?五' --include='*.rs' --include='*.toml' --include='*.md' --include='*.yml'`（排除 `.scratch/`、`target/`、`.git/`）在 `c642dae` 上命中 17 行，改完 0 行（grep 退出码 1，2026-09-13）。那条正则抓不到的说法（「哪五条」「看全五条」「那五条」「四条绿没绿」「那四条命令」「另外两条是绿的」）另扫一遍，一起改了。**口径**：`.scratch/` 下的规格与票据是历史记录，不在范围里。**保留**：下面这些仍然 grep 得到「五条」「四条」，都是有意留的——受保护那三条测试的名字 `门禁就是五条而且顺序是从便宜到贵`、`五条打印出来对得上_readme_那一段` 与它们里头的局部名 `五条`（`xtask/tests/gate.rs:126`、`:139`）、「五行字面量」（`:157`），测试名里的数有断言钉着；讲 `README.md` 旧开发块的「四行命令」「四条命令」（`xtask/src/gate.rs:3`、`:203`，`xtask/tests/gate.rs:99`），见挂单 `Q511`；挨着名字数一部分的「`clippy` / `test` / `doc` 三条」（`gate.rs:15`、`:18`，`README.md:209`，`tests/gate.rs:134`）。
- [x] 那条断言五条名字的测试**一个字没改**，照旧绿
  —— `git diff c642dae -- xtask/tests/gate.rs` 只动了模块文档（11 行）、辅助函数 `这一条绿吗` 的局部名（86–87 行，挂单 `Q512`）和另两条测试里的注释（202、239 行）；`门禁就是五条而且顺序是从便宜到贵`、`交付出去的那份配置还有一条在看着`、`五条打印出来对得上_readme_那一段` 三个函数一个字没动。全量门禁里三条都是 `ok`（日志 2275、2263、2262 行）。
- [x] 门禁跑起来打印的汇总表照旧报得出总条数（它是算的，不是抄的）
  —— `xtask/src/main.rs` 里 `let 总条数 = gate::steps(limits).len()` 没动。全量门禁（`TMPDIR=… cargo xtask gate -j 3 --test-threads 3 --keep-going`，2026-09-13，本分支上的改动）汇总表：fmt 0s／check 17s／clippy 18s／test 405s／doc 6s，末行 `5 条全绿。`，`EXIT=0`；test 那一步 70 个测试目标、1,832 条通过、0 条失败。日志 `/Users/nicoer/dev/game-wt/logs/slot-3-mc01-gate.log`（2,369 行）。
- [x] `--list` 照旧列得出跑哪几条
  —— `cargo xtask gate --list` 印出五行命令，与改之前逐字相同（从 `cargo fmt --all --check` 到 `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --all-features --lib --bins`），退出码 0。
- [x] 顺手记下改了哪几处，票 `02` 的人拿它当清单
  —— 10 个文件、38 处（39 行），见下面「改了哪几处」。票面预测「约十五处、`README.md` 三处」，实测 `README.md` 六处；派活那条正则只抓得到其中 17 行。**保留**：票面说「做完之后，票 `02` 加一条只需碰三条测试加一处那张条目表」，这句**没有完全兑现**——列着名字的清单，以及对「每一条」「唯一一条」下的断言原样留着，票 `02` 加一条时还得看，逐处列在下面「加一条时还要看的」；挂单 `Q513`，已往票 `02` 追加「收挂单 `Q513`」。

**改了哪几处**（行号是改完之后的）：

| 文件 | 行 | 原来 → 现在 |
|---|---|---|
| `Cargo.toml` | 4 | 门禁五条照样盖着它 → 门禁那几条照样盖着它 |
| | 8 | 门禁五条里有四条显式带 `--workspace`（`fmt` 那条走 `--all`）→ 门禁那几条不是显式带 `--workspace` 就是走 `--all`（`fmt` 那条） |
| `rust-toolchain.toml` | 37 | 门禁四条里 → 门禁那几条里 |
| | 41 | 门禁四条一条都用不到它 → 门禁哪一条都用不到它 |
| `.cargo/config.toml` | 1 | 门禁四条的唯一定义 → 门禁那几条的唯一定义 |
| | 5 | 四条绿没绿才是 → 那几条绿没绿才是 |
| `.github/workflows/gate.yml` | 3 | 五条（fmt / …）的唯一定义 → 那几条（fmt / …）的唯一定义 |
| | 6 | 跑哪五条 → 跑哪几条 |
| | 32 | job 名 `门禁五条` → `cargo xtask gate`（改前查过：`required_status_checks` 与 `protection` 都是 404，rulesets 为 `[]`） |
| | 80 | 把五条的结论 → 把每一条的结论 |
| `README.md` | 118 | 那五条的唯一定义 → 那几条的唯一定义 |
| | 147 | `# 门禁五条：排版…` → `# 门禁那几条：排版…` |
| | 150 | 五条命令的定义 → 那几条命令的定义 |
| | 152 | 跑哪五条 → 跑哪几条 |
| | 159 | 要一趟看全五条 → 要一趟跑完每一条 |
| | 209 | 是五条里唯一跑在默认特性上的 → 是门禁里唯一一条跑在默认特性上的 |
| `xtask/Cargo.toml` | 9 | 门禁五条（fmt / …）→ 门禁那几条（fmt / …） |
| `xtask/src/lib.rs` | 1 | 那四条命令的唯一定义 → 那几条命令的唯一定义 |
| `xtask/src/main.rs` | 4 | `# 门禁五条，按机器给的资源跑` → `# 跑门禁，按机器给的资源跑` |
| | 6 | 只打印它会跑哪五条 → 哪几条 |
| | 17 | clap `about`「仓库杂活：门禁五条的唯一定义」→「仓库杂活：门禁的唯一定义」 |
| | 56 | `--list` 的帮助：哪五条 → 哪几条 |
| | 67 | 跑（或者只打印）门禁五条 → 门禁那几条 |
| | 88 | 只列跑过的两条，会被读成「另外两条是绿的」→ 只列跑过的，会被读成「没列出来的都是绿的」 |
| `xtask/src/gate.rs` | 1 | 门禁五条的唯一定义 → 门禁那几条的唯一定义 |
| | 4 | 现在五条只在下面 → 现在门禁的每一条只在下面 |
| | 11 | 看全五条 → 看全每一条 |
| | 20 | 它是五条里唯一跑在默认特性上的 → 它是门禁里唯一一条跑在默认特性上的 |
| | 37 | 四条里只有这一条从来没写过标准 → 那时门禁里只有这一条从来没写过标准（讲历史，挂单 `Q511`） |
| | 138 | `steps` 的文档：门禁五条 → 门禁那几条 |
| | 159 | 五条里唯一不带 `--all-features` 的一条 → 编整个工作区的那几条里，唯一不带 `--all-features` 的一条（原句漏算了同样不带它的 `fmt`） |
| | 169 | 五条里一条都没编 → 门禁里哪一条都没编 |
| | 171 | 五条全绿 → 门禁全绿 |
| | 230 | 门禁五条全绿 → 门禁照样全绿 |
| `xtask/tests/gate.rs` | 11 | 模块文档：五条的名字与形态 → 门禁每一条的名字与形态 |
| | 86–87 | 辅助函数 `这一条绿吗` 的局部名 `五条` → `门禁`（挂单 `Q512`） |
| | 202 | 四条门禁里只有它 → 那时门禁里只有它（讲历史，挂单 `Q511`） |
| | 239 | 门禁五条全绿 → 门禁照样全绿 |

**加一条时还要看的**（没改，但门禁的条目一变就可能跟着变；挂单 `Q513`）：

- 列着名字的清单：`README.md:147`、`xtask/src/main.rs:27`（`gate` 子命令的 `--help`）、`xtask/Cargo.toml:9`、`.github/workflows/gate.yml:3`、`xtask/src/gate.rs:86`（`Step::name` 的文档）。
- 对每一条或「唯一一条」下的断言：`Cargo.toml:8`、`rust-toolchain.toml:37`–`38`、`xtask/src/gate.rs:20`、`:159`、`README.md:209`。
- 受保护那三条测试的名字与局部名里的 `五条`（`xtask/tests/gate.rs:123`、`:126`、`:139`、`:154`、`:157`）——票 `02` 本来就要改这三条。

**收挂单：** `Q380` 本票收的是「门禁几条」这一个数：它不进生成机制，直接从措辞里拿掉，钉子是现成那条断言名字的测试和算出来的汇总表。`Q380` 本题的测试条数、测试目标数归票 `03`（生成并核对），真库那些数归票 `04`（带日期与出处）。`Q380` 那一条在 `gui-self-sufficient/02` 里已是 settled（第四轮收口），本票不去改那份票据。

**门禁与审查：** 全量门禁见上面第 3 条。`/code-review` 两轴（Standards、Spec）回执开头列出的审查范围，与自取的 12 个文件逐个对得上；按审查改了 job 名与 `about`（「那几条」在 Actions 界面和 `--help` 里单独出现时没有前文可指）、`main.rs:88` 与 `gate.rs:4` 的指代、`gate.rs:159` 的限定语，并补全了 `Q513` 的「在哪」。审查报的「Q513 与票 `02` 那行指向的证据还没写」就是这一段。
