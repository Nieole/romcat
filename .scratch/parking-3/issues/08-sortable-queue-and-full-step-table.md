# 08 — 队列表能排序、步骤表不截断

**What to build:** 两件界面上的事：

1. **队列那一屏点表头就换排序。** 真机上 16,656 条待确认，维护者想按容量或按结论找到
   该先动的那几条，现在只能靠选择器缩小范围——次序永远是变体的键。（挂账 `D153`）
2. **差量预览的步骤表列得全。** 真机量级上一次同步动上万个文件（实测合成数据 8,206 步），
   表列满 2,000 条就打住，想确认第 5,000 步是什么就得转去命令行。（挂账 `D158`）

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 队列那一屏点表头换排序，**在内存里排**——它的条目本来就在内存里（几千条），不为它改查询。（这与变体表那种下推到 `ORDER BY` 的路子不同，是有意的：数据在哪就在哪排。）
- [ ] 有一条界面测试断：点一下表头之后，屏上画出来的第一行换了。
- [ ] 差量预览的步骤表改成**虚拟化**的，去掉 2,000 条上限。计划整份本来就已经在界面状态里，表格组件本来就支持。
- [ ] 上面那四行汇总照旧留着。
- [ ] 有一条界面测试断：一份 8,000 步以上的计划，滚到第 5,000 步那一行摆得出来。
- [ ] 帧率不塌：虚拟化之后翻行的代价与总步数无关，有实测数字写进报告。
- [ ] 挂账 `D153` `D158` 标 `settled` 并迁回票 `rom-metadata-automation/24` 与 `25` 末尾「挂单裁决（第三轮）」一节。
