# 11 — 变体表那一列作品名排得了序

**What to build:** 维护者翻库时认的是**作品**（不是那些各路来源攒出来的文件名），
所以变体表里有一列作品名——但它排不了序也筛不了，因为它是从一张整份读进来的小表
（真库 9,226 条）里查的，不在查询的排序与筛选里。想按作品找就只能退回「键里含」那个框。
（挂账 `D161`）

**Blocked by:** 10（作品行的形状在 `10` 里刚动过）

**Status:** done

⚠️ **票正文的前提有一半是旧的，收尾时核对过**（挂单 `Q294`）：那一列作品名长在票 `25`
那一版的**变体表**上，票 `gui-redesign/03` 把那张表整个换成了**作品级主列表**——
作品名成了第一列、`WorkOrder::Name` 是默认排序且早就下推在 `ORDER BY` 里，
筛选走 `Dimension::Work` 也早就下推着。所以「排不了序也筛不了」在**主列表**上不成立；
`D161` 点名的那一处（**变体那一层的页查询**）倒是原封不动地还在，本票补的正是它。

- [x] 作品名**下推到页查询里**——排序与筛选一律下推到中立库（ADR-0005），不在内存里补。

      —— `Catalog::variant_browse_page`（`crates/core/src/catalog/browse.rs`）：
      一趟 `LEFT JOIN work ON work.id = variant.work_id`，`SELECT` 里多一列
      `work.name AS work_name`。排序落在 `VariantOrder::Work` 上（`ORDER BY` 拼的就是那个
      别名），筛选落在 `作品^…`（`Dimension::Work` → `catalog::filter`）。
      `variant_page` 与它**走同一条 SQL**（前者只是把作品名那一格丢掉），
      于是**这两个入口**筛的排的绝不会漂开。
      **总数那一条是另一条 SQL**（`variant_total` 只 `COUNT(*) FROM variant`，不连那张表），
      它与页内容数的仍是同一批，靠的是另一条依据：`work.id` 是 `INTEGER PRIMARY KEY`，
      一个变体最多连上一行，`LEFT JOIN` 放大不了行数——这句已写进 `VARIANT_BROWSE_FROM`
      的文档，连同「日后要再连一张表，先回答这一句还成不成立」。
      **`LEFT` 那半个字是要紧的**：还没认出作品的变体会被内连接**整批**筛掉，一个都不剩
      （真库上一千七百多个：作品 9,226 个，而主列表 10,978 行）。
      证据：`crates/core/tests/browse.rs::页查询自己带回作品名而不必整份读那张小表`
      ——只取**中间一页**（第 40 行起 20 行），前面那些行压根没取回来过，那一页每一格
      作品名都对得上 `work_names()` 说的。**突变检查过**：把 `VariantOrder::Work` 的列名
      改成 `key`（「什么都没干」的实现）当场红。

- [x] 另立一个**浏览专用的行类型**，与「变体表的一行」分开——后者被十几处构造着，直接给它加字段会波及一大片。

      —— `romcat_core::catalog::BrowseVariant`：
      `{ variant: VariantRow, work: Option<String> }`。形状照同一个模块里的 `WorkVariant`
      来——**装着**那一行，不把它的字段抄一遍。`VariantRow` 一个字段都没加：它是
      「`variant` 表的一行」，`identify` / `triage` / `scrape::local` / `adapter::converge`
      等十几处都在构造它、按这个意思读它，而作品名不在那张表上。
      `work` 那一格是 `Option`：`None` 是**还没认出作品**，不是「作品叫空字符串」——
      两件事混成一件，排序时它们就会插进真有作品的那一段里。

- [x] 点表头按作品名排序；筛选器里作品名也用得上。

      ⚠️ **照勾，但保留**：**表头那一半在票 `gui-redesign/03` 就落了**
      （`crates/gui/src/table.rs::Table::show` 的表头循环走 `WorkOrder::ALL`，
      `WorkOrder::Name` 那一档标签就是「作品」，再点一次翻方向），本票没动它，
      在这一条上的新增**只有测试**。而本票新加的 `VariantOrder::Work`
      **屏上没有任何表头可点**——`VariantOrder` 在 `crates/gui/src/` 里零出现，
      变体级的表在票 `gui-redesign/03` 之后就不在屏上了。所以规格 User Story 31
      「变体表里那一列作品名点表头就排序」**按字面没做到**，指到挂单 `Q297`。
      **筛选器那一半**走筛选面板那棵条件组（`Dimension::Work`，界面上那个下拉里就有
      「作品」，提示值 `火焰纹章`），一路下推成 `EXISTS (SELECT 1 FROM work w …)`。
      证据：`crates/gui/tests/browse.rs::筛选器里按作品名筛得动而且下推着筛`
      ——走的是屏上那条路（`browse::Screen::set_filter_rule`），断「筛掉了行」
      「筛完内存里还是那一扇窗（`retained() <= SPAN`）」「屏上剩的那几行画的就是那个作品」。
      条件里那个名字**从库里现取**，不写死。
      变体那一层同一件事：`crates/core/tests/browse.rs::按作品名筛也下推到库里`
      （总数与页内容筛的是同一批）。

- [x] 有测试断：按作品名排序之后屏上画出来的次序对。

      —— `crates/gui/tests/browse.rs::按作品名排完屏上画出来的次序就是作品名的次序`。
      看的是**这一帧真的画出来的字**（`shared::画出来的字`），比的是
      「屏上那一串 == 下推出来的那一页的前几行」，再断那一页自己确实按名字排好了；
      **期望从数据自己算出来**（把拿回来的名字自己排一遍），换一份同样合法的数据照样成立。
      两个方向都断，并断「翻了方向屏上真的变了」。中文那一段两边同口径：`ORDER BY`
      是 UTF-8 逐字节比，Rust 的 `str` 也是。
      **突变检查过**：把 `WorkOrder::Name` 的列名改成 `work_id` 当场红。
      对照：既有的 `五列都排得了序而且换排序真的换了次序` 断的只是「五种排法的头一行
      不全一样」，一次都没看屏上的字，在「排了、但排错了」上照样绿（挂单 `Q298`）。

- [x] 有测试断：没有作品的那些变体（作品那一格是空的）排序时的位置是确定的，不是随机漂。

      —— `crates/core/tests/browse.rs::按作品名排序是下推出来的而且作品那一格空着的一律排在末尾`：
      正反两个方向各一趟，断「第一个空的往后全是空的」「空的前面全是有作品的」，
      也就是空格子**连成一段、钉在表尾**，不跟着方向翻。做法是 `ORDER BY` 多一把
      `work_name IS NULL` 的键；这一档是本票现定的，另一条路（照 SQLite 默认）
      记在挂单 `Q295`。**突变检查过**：去掉那把键，正序那一趟当场红
      （SQLite 默认把 `NULL` 排最前，空的那一批跑到表头去了）。
      **主列表上不存在这一格**——那一屏还没认出作品的行画的是自己的键
      （`COALESCE(work.name, variant.key)`，与 `adapter::converge` 的 `Anchor::Loose`
      同一条口径），所以那儿没有「空格子往哪放」这个问题；新那条界面测试顺带断了
      **两种行摆在同一串里**（这一页上 `WorkAnchor::Work` 与 `Anchor::Loose` 都有）。

- [x] 帧率不塌：真库量级（10,978 行）上翻行的代价与总行数无关，实测数字写进报告。

      —— 判据是 `bench::FrameCost::reads`（**读了几次库**，不是秒表；票 `parking-3/01`
      拿掉过一条挂钟断言，票 `17` 立的这条路子）。
      实测（dev profile，同机另有两个 worktree 在跑，`demo::browse`）：
      **按作品名排（默认排序）滚 720 行——946 行读 1 次库、中位 19.20 ms；
      10,978 行读 1 次库、中位 19.27 ms；内存里始终 512 行。**
      读库次数一次不多、中位帧代价差 0.4%，两样都与总行数无关。
      ⚠️ **量法说清楚**：这两个数是在那条门禁测试里临时加一句 `eprintln!` 打 `FrameCost`
      得来的，量完当场撤掉——**没有常设入口，第二个人照不着复现**，也没落进
      `docs/library-facts.md`（挂单 `Q303`）。**读库次数那一半有常设断言守着，
      而且不是本票头一回量出来的**：`docs/library-facts.md:1269`（票 `parking-3/17`）
      已经记着「946 → 10,978 行，两边都是 1 次库读」；本票这一趟是**改完之后确认它没变**，
      不是新结论。
      门禁上守着它的是 `crates/gui/tests/browse.rs::翻行的代价按读了几次库算而且真库量级上一次不多`
      （`大的.reads <= 3` 且 `小的.reads == 大的.reads`），它跑的正是默认排序＝按作品名排。
      **那张 9,226 行的小表这一屏一次都没读过**：主列表那一列作品名从头到尾是页查询自己
      带回来的（`WORK_ANCHOR_COLUMNS` 里的 `COALESCE(work.name, variant.key)`），
      `crates/gui/` 里 `work_names` 零出现。`bench::BrowseCost::facets_ms` 那句
      「连表里那一列作品名靠的那张小表一起」是票 `25` 那一版的说法，本票一并改掉了。
      变体那一层同理：`variant_browse_page` 是一条查询，`work_names()` 一次都不调。

- [x] 挂账 `D161` 标 `settled` 并迁回票 `rom-metadata-automation/25`。

      —— `deferred.md` 的 `D161` 改成 `settled`（走 B），决定与证据迁进
      `.scratch/rom-metadata-automation/issues/25-gui-browse-and-sublibraries.md`
      末尾「挂账裁决（第三轮收口迁回）」一节，**原验收正文一个字没动**。

## 说明

「按标题排」是另一件事——那要的是标题选取的结果，SQL 里算不出来。这张票**只做按作品名排**，
标题那条路不在范围内，如实记在挂单里。

## 挂单

`Q294`（票正文那一列屏上已经没有了）、`Q295`（空格子排末尾是本票现定的）、
`Q296`（`platform` 那一列的 `NULL` 次序仍跟着方向漂）、
`Q297`（变体那一层排序屏上点不到，两个入口都没有生产调用方）、
`Q298`（既有那条五列排序断言仍然是弱的；**这一条另外收着「一直是绿的却从没真验过」
那一族的第五例与它添的那一层**——断言的两边同源就是恒真，指向票 `18` 收口）、
`Q299`（命令行导出曾为取一个作品名读整张作品表，顺手改掉了）、
`Q300`（按作品名排那一档背后没有索引，代价与别的变体列不是一个量级）、
`Q301`（只要变体行的调用方也跟着付了那条 join 的钱）、
`Q302`（「真库上一多半变体还没认出作品」这句话与 `library-facts.md` 对不上）、
`Q303`（帧率那两个毫秒数没有常设入口，复现不了）。

## Comments

**收尾（做这张票的人自己写）**

- **「按标题排」照旧不在范围内**，理由与票正文一致：显示标题是 `title::choose` 挑出来的，
  优先级表、语言、来源三样一起判，SQL 里算不出来。这张票做的是**作品名**那一列——
  它是 `work.name`，一列文字，`ORDER BY` 接得住。两者不要混。
- 动过的地方：`crates/core/src/catalog/browse.rs`（`VariantOrder::Work`、`BrowseVariant`、
  `variant_browse_page`、`order_clause`、模块文档）、`crates/core/src/catalog.rs`（再导出）、
  `crates/gui/src/main.rs`（字体自检连作品名一起过）、`crates/gui/src/bench.rs`（一句旧文档）、
  `crates/cli/src/main.rs`（`Q299`），加两份测试。
- **顺带改了模块文档里一句从这一趟起会变成假话的话**：`catalog::browse` 原先写着
  「变体表的每一条 `ORDER BY` 都有一条索引正好接住」——按作品名排那一档没有
  （`work_name` 是 join 出来的列，`variant` 上那几条 `(列, key)` 索引与 `work(name)`
  都接不住），它落到一口临时 b 树上。改成「按**自己那张表上的列**排时」，
  并在 `VariantOrder::Work` 上写明这一档的量级。记在挂单 `Q300`。
- **门禁四条全绿**（`cargo xtask gate --keep-going -j 6`，基于 `796a243`）：

  | | | |
  |---|---|---|
  | `fmt` | **绿** 2s | `cargo fmt --all --check` |
  | `clippy` | **绿** 20s | `cargo clippy --workspace --all-targets --all-features -j 6` |
  | `test` | **绿** 524s | `cargo test --workspace --all-features -j 6` |
  | `doc` | **绿** 8s | `cargo doc --workspace --no-deps -j 6` |

  **1,696 条通过 / 66 个测试目标，0 个 FAILED**（本票新增 5 条：核心 3、界面 2）。
  `任务六秒都没跑完` 那张挂钟彩票这一趟没撞上。
- `cargo doc` 告警**一条没多**：core 56 / gui 8，与动手前一样。
- **`/code-review` 抓到的，逐条处置**（两轴各一个子 agent，工作目录与 diff 口径写死；
  ⚠️ 派它们时最初给的是 `main..HEAD`，而 `main` 在本票开工后已走到 `05e6ace`
  （票 `parking-3/09` 合了进去），那个口径会把别人的改动倒过来一起报——当场改成
  `git diff HEAD` 重来）：
  - **筛选那条界面测试最后一句是循环论证**（Standards 硬伤）：`屏上` 本来就是拿这一页的
    名字当白名单滤出来的，所以「屏上每一行都对得上」恒真，屏上真画了别的行只会被静默
    滤掉。改成断**次序加前缀**（`屏上 == 这一页[..屏上.len()]`），并补一句
    「那一页装的行数与总数对不上」。
  - **模块小节把一张屏上已经没有的表说成现状**（Standards 硬伤）：补一段⚠️说清
    「说的是这个查询面，不是当下某一屏」。
  - **`VariantOrder::ALL` 的文档「界面照这个次序摆表头」是假话**（Standards 硬伤）：
    屏上摆的是 `WorkOrder::ALL`，这个枚举在 gui 里零出现。改成「可排的列的规范次序」。
  - **排序那条测试没走「从别的列点回作品名」**（Standards 判断题）：作品名是默认档，
    直接开跑等于只验了「默认恰好是对的」。改成先落在 `WorkOrder::Bytes` 上跑一帧，
    再切回 `Name`。
  - **`variant_total` 不连那张表，所以「同一条 SQL」不是总数对得上的真理由**
    （Standards 判断题 + Spec）：真理由是主键 join 不改行数，已写进
    `VARIANT_BROWSE_FROM` 的文档，并留下「再连一张表之前先回答这一句」。
  - **「真库上一多半变体还没认出作品」与 `library-facts.md` 对不上**（Spec）：
    真库约 3.8%。自己新写的三处已改，仓库既有的六处不动，记 `Q302`；
    `Q295` 那条决定的理由从「这一批很大」换成「它们在这一列上没有值可比」，决定不变。
  - **那两个毫秒数没有常设入口**（Spec）：量法与不可复现已写进验收第 6 条，记 `Q303`。
  - **`variant_page` 如今生产上也零调用方**（Spec）：并进 `Q297`。
  - 「`retained() <= SPAN` 由构造保证、担不起『没把全库读回来过』这句话」（Standards 小事）：
    测试文档改成如实说它拦的是「日后有人把窗换成整份读回来」。
  - 「那个辅助函数靠『一个行名这一帧只画一次』」（Standards 小事）：写进函数文档。

## 挂单裁决（第三轮收口，2026-09-08）

本票在实现路上记下的 **10 条**（`Q294`–`Q303`）由票 `parking-3/18` 收口裁完，条目全文迁自 `.scratch/PARKING-LOT.md`（那边的条目区已清空）。**本票的原验收正文一个字没动。**

分三档：**A** 已经做完只是没销 ／ **B** 不成立了 ／ **C** 仍然成立要开票。判据是**一条挂单有了主人就算收了，不是等它做完**。本票：**A 5 ／ B 1 ／ C 4**。

| 挂单 | 档 | 一句话 |
|---|---|---|
| `Q294` | **B** | 票正文说的那一列作品名，屏上已经没有了 |
| `Q295` | **A** | 作品那一格空着的行排在末尾，这一档是这张票现定的 |
| `Q296` | **C**<br>第四轮「变体那一层的排序：核心做完了，屏上点不到」 | `platform` 那一列的 `NULL` 次序仍跟着方向漂 |
| `Q297` | **C**<br>第四轮「变体那一层的排序：核心做完了，屏上点不到」 | 变体那一层排序屏上点不到：新入口与 `variant_page` 都没有生产调用方 |
| `Q298` | **A** | `五列都排得了序而且换排序真的换了次序` 那条断言仍然是弱的 |
| `Q299` | **A** | 命令行导出那条路曾为取一个作品名读了整张作品表 |
| `Q300` | **C**<br>第四轮「变体那一层的排序：核心做完了，屏上点不到」 | 按作品名排那一档背后没有索引，代价与别的变体列不是一个量级 |
| `Q301` | **A** | 只要变体行的调用方也跟着付了那条 `LEFT JOIN` 的钱 |
| `Q302` | **C**<br>第四轮「变体那一层的排序：核心做完了，屏上点不到」 | 「真库上一多半变体还没认出作品」这句话与 `library-facts.md` 对不上 |
| `Q303` | **A** | 帧率那两个毫秒数没有常设入口，复现不了 |

### Q294 — 票正文说的那一列作品名，屏上已经没有了

- **来自：** 票 `parking-3/11`
- **类别：** 票写错了
- **在哪：** `.scratch/parking-3/issues/11-work-name-column-sorts-and-filters.md:4`
  （「所以变体表里有一列作品名」）、`.scratch/parking-3/spec.md:34`、
  `.scratch/rom-metadata-automation/deferred.md` 的 `D161`
- **为什么没停线：** `D161` 点名的那一处**还在**（变体那一层的页查询 `Catalog::variant_page`
  交回来的还是 `VariantRow`，只带 `work_id`），B 路照原样做得下去；停线那三条一条都不成立。
- **这张票实际做了什么：** 照 B 路做，并把「屏上那一半」如实报成**已经在了**。
  原委：`D161` 是票 `25` 的 `/code-review` 处置（`4abb29c`）里记下的，那时浏览屏画的是
  **变体表**，那一列作品名是取回来之后拿 `Catalog::work_names()` 在内存里对出来的。
  票 `gui-redesign/03` 把那张表整个换成了**作品级主列表**（`crates/gui/src/table.rs`，
  一行一个作品），作品名成了第一列、`WorkOrder::Name` 是默认排序且下推在 `ORDER BY` 里，
  筛选走 `Dimension::Work`（`catalog::filter`）也早就下推着。于是票正文那句
  「排不了序也筛不了」在**主列表**上已经不成立，「想按作品找只能退回『键里含』那个框」
  说的也是变体表那一屏的 `VariantQuery::contains`，主列表上那是搜索框加筛选器。
  本票补的是没做完的那一半：变体那一层的页查询照旧不带作品名。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **B（不成立了）**
- **收尾裁决（第三轮收口，2026-09-08）：** **B** —— **这一批 124 条里唯一的 B。** 票正文说的那一列作品名**屏上早就没有了**——票 `gui-redesign/03` 把变体表整个换成了作品级主列表（`crates/gui/src/table.rs`，一行一个作品），作品名成了第一列、`WorkOrder::Name` 是默认排序且下推在 `ORDER BY` 里，筛选走 `Dimension::Work` 也早就下推着。于是票正文那句「排不了序也筛不了」在主列表上**已经不成立**，照票面字面做是去修一个不存在的屏。`D161` 点名的那一处（变体那一层的页查询）还在，B 路照原样做得下去，所以没停线。

### Q295 — 作品那一格空着的行排在末尾，这一档是这张票现定的

- **来自：** 票 `parking-3/11`
- **类别：** 规格没说
- **在哪：** `crates/core/src/catalog/browse.rs` 的 `VariantQuery::order_clause`
  （`ORDER BY work_name IS NULL, …`）
- **为什么没停线：** 两条路都站得住，翻案只动那一句 `format!`，全在本票里。
- **这张票实际做了什么：** **一律排末尾，正反两个方向都是**。另一条路是照 SQLite 默认
  （`NULL` 正序最前、倒序最后）——票只要求「位置是确定的，不是随机漂」，默认那套也确实
  是确定的。没走，理由是那套**跟着方向翻**：人点一下表头换个方向，**还没认出作品**的
  那一批就整批从表尾跳到表头，而它们在这一列上根本没有值可比——没有值的东西，
  位置不该由方向决定。「空格子待在表尾」是表格里通行的那一档，也让
  「翻到底就是还没认出作品的那些」成立。
  **理由不靠这一批有多大**：起初这条写的是「真库上一多半」，那句话与
  `docs/library-facts.md` 对不上（真库约 3.8%，见 `Q302`），换掉之后这条决定原样成立。
  代价是它与同一个函数里 `platform` 那一列的 `NULL` 次序不一致（见 `Q296`）。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 作品那一格空着的行**一律排末尾，正反两个方向都是**。不走 SQLite 默认的理由是那套跟着方向翻——没有值的东西，位置不该由方向决定。这条决定原先靠「真库上一多半」那句话撑着，`Q302` 把那句话推翻之后理由换掉了、决定原样成立。

### Q296 — `platform` 那一列的 `NULL` 次序仍跟着方向漂

- **来自：** 票 `parking-3/11`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/catalog/browse.rs` 的 `VariantQuery::order_clause`；
  既有测试 `crates/core/tests/browse.rs::平台未知的那些行照样排得进去` 断的正是
  「未知的排在最前」
- **为什么没停线：** 本票只碰作品那一列，`platform` 一个字没动；改它要连带改那条既有测试。
- **这张票实际做了什么：** 只给作品那一列加了 `work_name IS NULL` 那把键，`platform`
  照旧走 SQLite 默认。于是同一个 `ORDER BY` 里两列的 `NULL` 处置不一样——
  一列钉死在末尾，一列跟着方向翻。**平台未知**在词表里是一档真实内容（ADR-0011），
  按 `Q295` 那条道理它也该钉死；但那是既有行为，改它是另一件事。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— `platform` 那一列的 `NULL` 次序仍跟着方向漂，与 `Q295` 现定的那一档不一致——同一个 `ORDER BY` 里两列的 `NULL` 处置不一样。**平台未知**在词表里是一档真实内容（ADR-0011），按 `Q295` 那条道理它也该钉死；改它要连带改既有测试 `平台未知的那些行照样排得进去`。
  **去处：** 第四轮规格材料，「变体那一层的排序：核心做完了，屏上点不到」那一组。

### Q297 — 变体那一层排序屏上点不到：新入口与 `variant_page` 都没有生产调用方

- **来自：** 票 `parking-3/11`（`/code-review` Spec 轴点名要说清这一条）
- **类别：** 票写错了
- **在哪：** `crates/core/src/catalog/browse.rs` 的 `Catalog::variant_browse_page`；
  生产调用方只有 `crates/gui/src/main.rs` 的 `font_check`
- **为什么没停线：** 票点名要「另立一个浏览专用的行类型」并把作品名做成可排序列，
  这一半做完了、有测试钉着；「把变体表重新摆上屏」不在这张票的验收里。
- **这张票实际做了什么：** 落了核心那一半，并给它找了一个**真用得上的**生产调用方：
  字体那趟自检（`--font-check`）从前只过全部变体的**键**，而主列表第一列画的是**作品名**
  ——它来自 DAT 与中文离线源，跟键根本不是同一批字，屏上最显眼的那一列反而没人替它
  查过豆腐块。现在连作品名一起过，作品名由这条页查询顺路带回来，不为它整份读那张表。
  除此之外，屏上眼下没有变体级的表（见 `Q294`）。要么日后浏览屏加一档「按变体看」，
  要么这条入口就一直只服务自检与测试。
  **要说清的结论：** 规格 User Story 31「变体表里那一列作品名点表头就排序」**按字面没做到**
  ——本票新加的 `VariantOrder::Work` 屏上没有任何表头可点（`VariantOrder` 在
  `crates/gui/src/` 里零出现）。票里验收第 3 条能勾上，靠的全是票 `gui-redesign/03`
  的既有功能（主列表那一列），本票在那一条上的新增**只有测试**。
  **另一头**：`Catalog::variant_page` 如今生产上也零调用方了——原先唯一那个
  （字体自检）已改走新入口，剩下的只有测试。留着它是因为「只要变体自己那一行」这个
  问法本身没错，删它要连带改五处测试；真要治，该治的是让调用方各自去要它们真正要的
  东西，而不是留一个谁都不叫的名字（同 `Q301`）。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— **规格 User Story 31「变体表里那一列作品名点表头就排序」按字面没做到**——本票新加的 `VariantOrder::Work` 屏上没有任何表头可点（`VariantOrder` 在 `crates/gui/src/` 里零出现），票里验收第 3 条能勾上靠的全是票 `gui-redesign/03` 的既有功能，本票在那一条上的新增**只有测试**。另一头：`Catalog::variant_page` 如今生产上也零调用方了。与 `Q300` 是同一件事的两头。
  **去处：** 第四轮规格材料，「变体那一层的排序：核心做完了，屏上点不到」那一组。

### Q298 — `五列都排得了序而且换排序真的换了次序` 那条断言仍然是弱的

- **来自：** 票 `parking-3/11`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/tests/browse.rs::五列都排得了序而且换排序真的换了次序`
- **为什么没停线：** 本票另写了一条强的（`按作品名排完屏上画出来的次序就是作品名的次序`），
  旧那条照旧绿着、也照旧覆盖着另外四列，删它是净损失。
- **这张票实际做了什么：** **留着不动，另写一条**。旧那条断的是「五种排法的头一行不全一样」
  ——它在「压根没排」上会红，可在「排了、但排错了」上照样绿；而且它一次都没看屏上
  画出来的字，看的是又问了一遍库拿回来的那批行。新那条只覆盖作品名那一列：
  看这一帧真的画出来的字、期望从数据自己算出来、两个方向都断。
  另外四列（平台、变体数、容量、年份）眼下仍只有那条弱的守着。
- **这一族已经攒到第五例，判据是同一句**（收口时票 `18` 要把它们并起来说）：
  `15` 那条根本没拿适配器当被测对象、`16` 靠一个夹具格的巧合、`17` 靠一个 1,020 步周期的
  巧合、`08` 靠合成数据缺「跳过」整整一档、本票这一例靠**断言的两边是同一个来源**。
  **判据：换一份同样合法的数据，它还成立吗？**
  本票这一例比前四例更狠一点——**它连数据都不用换，本来就永远成立**：
  那条筛选界面测试原先最后一句是「屏上每一行都对得上那个作品名」，而「屏上」那一串
  本来就是拿这一页的名字当白名单滤出来的，屏上真画了别的行只会被静默滤掉。
  于是它不是「碰巧这批数据让它成立」，是**恒真**——一条永远绿的断言，看上去却像在验一件事。
  `/code-review` 的 Standards 轴抓出来的，已改成断**次序加前缀**
  （`屏上 == 这一页[..屏上.len()]`）：只有「顺序」与「前缀」这两样是白名单滤不掉的。
  **这一例给那一族添的那一层**：前四例问的是「这批数据是不是恰好让它成立」，
  这一例问的是**「这条断言的两边是不是同一个来源」**——同源的两边比出来的永远是真，
  换多少份数据都照旧。写测试时该多问一句：**我拿来验的那个东西，是不是我要验的那个
  东西自己算出来的？**
- **谁来裁：** 票 `parking-3/18`（收口时与 `15` `16` `17` `08` 那四例并起来说）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 改动已落地（断言改成断**次序加前缀**，`屏上 == 这一页[..屏上.len()]`）。**这一条的方法论价值最高**：它比同族前四例更狠——**连数据都不用换，本来就永远成立**（断言两边是同一个来源，「屏上」那一串本来就是拿这一页的名字当白名单滤出来的）。它给那一族添的那一问原样带进收口结论：**我拿来验的那个东西，是不是我要验的那个东西自己算出来的？**

### Q299 — 命令行导出那条路曾为取一个作品名读了整张作品表

- **来自：** 票 `parking-3/11`
- **类别：** 工具缺陷
- **在哪：** `crates/cli/src/main.rs`（`romcat export --prefer` 那一段）
- **为什么没停线：** 一处调用、五行，与本票同一个题目（那张 9,226 行的小表还整份读不读）。
- **这张票实际做了什么：** **顺手改掉了**——换成 `Catalog::work_of_variant`（一次索引查）。
  原样是 `catalog.work_names()` 之后 `names.get(&id)`：把整张作品表读进内存只为取其中
  一行，而 `work_of_variant` 的文档写的正是「单开一条而不是让调用方读一遍 `work_names`」。
  它在循环里，`--prefer` 给几次就读几遍。另一条路是留着不动、只记一条——没走，
  因为改动比记它还短，而且行为一字不变（还没认出作品时照旧退回变体自己的键）。
  **没有为它新加测试**：`--prefer` 那条路眼下没有测试守着，补一条是另一件事。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 顺手改掉了（`catalog.work_names()` + `names.get(&id)` → `Catalog::work_of_variant` 一次索引查），行为一字不变。没为它新加测试——`--prefer` 那条路眼下没有测试守着，补一条是另一件事。

### Q300 — 按作品名排那一档背后没有索引，代价与别的变体列不是一个量级

- **来自：** 票 `parking-3/11`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/catalog/browse.rs` 的 `VariantOrder::Work`；索引清单在
  `crates/core/src/catalog/content.rs`（`variant_platform_key` / `variant_rule_key` /
  `variant_files_key` / `variant_bytes_key` / `work_name`）
- **为什么没停线：** 变体那一层眼下屏上没有调用方（`Q297`），这份代价现在没人付；
  真要付的时候该先量再治，而量法（`--bench-paging`）已经有了。
- **这张票实际做了什么：** **不加索引，把它写进文档**。`work_name` 是 join 出来的列：
  `variant` 上那几条 `(列, key)` 索引接不住它，`work(name)` 那条也接不住（排的是变体
  不是作品），于是这一档落到一口临时 b 树上——量级与作品级主列表那一条同一档，
  不是别的变体列那种索引倒着扫。模块文档里原先那句「变体表的每一条 `ORDER BY` 都有
  一条索引正好接住」因此也改了，不然它从这一趟起就是句假话。
  另一条路是补一条覆盖索引——没走：SQLite 建不出跨表的索引，真要治得在 `variant` 上
  冗余一列作品名，那是拿写入与一致性换读取，而这一档现在一次都没人读。
  **没量过它到底多贵**：要量得先有调用方（同 `Q297`）。
- **谁来裁：** 拿主意的人（与 `Q297` 是同一件事的两头）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 按作品名排那一档背后**没有索引**（`work_name` 是 join 出来的列，`variant` 上那几条索引接不住，`work(name)` 也接不住），落到一口临时 b 树上，量级与作品级主列表同一档、不是别的变体列那种索引倒着扫。**没量过它到底多贵**——要量得先有调用方。与 `Q297` 是同一件事的两头。
  **去处：** 第四轮规格材料，「变体那一层的排序：核心做完了，屏上点不到」那一组。

### Q301 — 只要变体行的调用方也跟着付了那条 `LEFT JOIN` 的钱

- **来自：** 票 `parking-3/11`（`/code-review` Standards 轴的判断题）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/catalog/browse.rs` 的 `Catalog::variant_page`
  （改成走 `variant_browse_page` 再把作品名那一格丢掉）
- **为什么没停线：** 语义一字未变（`MAX_PAGE` 夹取、`limit == 0` 早返、越界出空表全在
  被调用方里，既有三条测试照旧覆盖），而 `variant_page` 眼下的调用方只剩测试。
- **这张票实际做了什么：** **两个入口共用一条 SQL**，代价是只要变体行的调用方白付一次
  主键 join 加一个 `Option<String>` 的分配。另一条路是让两个入口各拼各的 `SELECT`
  ——没走：那是拿「筛的排的会不会漂开」去换几个字节，而这一层最要紧的一条纪律正是
  「总数、页内容、两个入口筛的必须是同一批」，两条 SQL 就是两处可以各自改坏的地方。
  真要治，该治的是让 `variant_page` 消失（调用方要么本来就想要作品名，要么该走
  `Catalog::variant` 按键取一条），而不是把 SQL 拆成两份。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 两个入口共用一条 SQL，代价是只要变体行的调用方白付一次主键 join。理由终局：这一层最要紧的纪律是「总数、页内容、两个入口筛的必须是同一批」，两条 SQL 就是两处可以各自改坏的地方。

### Q302 — 「真库上一多半变体还没认出作品」这句话与 `library-facts.md` 对不上

- **来自：** 票 `parking-3/11`（`/code-review` Spec 轴抓出来的）
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/browse.rs:10`、`crates/core/tests/works.rs:6`、
  `crates/core/tests/filter.rs:115`、`crates/core/src/catalog/browse.rs:795` `:1471` `:2229`
  ——对面是 `docs/library-facts.md:183` 起那张表（变体 46,444、命中 30,024、
  **全部变体里 64.6%**）与 `:190`（作品 9,226 个），加上主列表 10,978 行
- **为什么没停线：** 这是仓库既有的说法，六处都不在本票的 diff 里；本票**沿用过它**，
  发现之后已把自己新写的三处改掉，剩下六处不是本票的活。
- **这张票实际做了什么：** **只改自己新写的那三处，剩下六处记下来不动**。
  算法：作品 9,226 个、主列表 10,978 行，差出来的一千七百多行一行一个变体，
  也就是**还没认出作品的变体约占 3.8%**——不是「一多半」。换个读法（按命中率）
  没命中的约三成半，也不是一多半。这个数在本票里是**承重的**（`Q295` 那条决定原先
  就靠它），所以必须换成算得出来的说法；换完之后那条决定照样站得住——理由从
  「这一批很大」改成「它们在这一列上根本没有值可比，位置不该由方向决定」。
  另一条路是六处一起改——没走：那是六个文件的文案改动，与本票一个字关系都没有，
  而且真要改该先确认 10,978 那个数本身是真库量的还是照真库形状摆的合成数据
  （`docs/library-facts.md:1127` 与 `.scratch/parking-3/spec.md:398` 读起来不完全一致）。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 「真库上一多半变体还没认出作品」这句话**六处**与 `docs/library-facts.md` 对不上——算出来约 **3.8%**（作品 9,226、主列表 10,978 行），换个读法（按命中率）没命中的约三成半，也不是一多半。本票只改了自己新写的三处。真要改六处，得先确认 10,978 那个数本身是真库量的还是照真库形状摆的合成数据（`library-facts.md:1127` 与 `spec.md:398` 读起来不完全一致）。
  **去处：** 第四轮规格材料，「变体那一层的排序：核心做完了，屏上点不到」那一组。

### Q303 — 帧率那两个毫秒数没有常设入口，复现不了

- **来自：** 票 `parking-3/11`（`/code-review` Spec 轴抓出来的）
- **在哪：** 票 `parking-3/11` 验收第 6 条写着的「中位 19.20 ms / 19.27 ms」；
  `crates/gui/tests/browse.rs::翻行的代价按读了几次库算而且真库量级上一次不多`
- **类别：** 路过发现，不在范围内
- **为什么没停线：** 验收要的那一半（「翻行的代价与总行数无关」）由**读库次数**守着，
  那一半有常设的门禁断言；毫秒数只是旁证。
- **这张票实际做了什么：** **照实说出量法，并把它标成不可复现**。那两个数是在那条门禁
  测试里临时加一句 `eprintln!` 打 `FrameCost` 得来的，量完当场撤掉——仓库里现成的两条
  命令都对不上这个口径（`--bench-scroll` 走 `demo::synthetic`，一个作品都不建；
  `--bench-browse` 走 `bench::browse`，报的是另一组数），所以没有第二个人能照着复现，
  也没落进 `docs/library-facts.md`（全仓的实测数都记在那儿）。
  另一条路是给 `--bench-scroll` 加一个「用 `demo::browse` 造数据」的旋钮，让这个口径有
  常设入口——没走：那是票 `17` 那一族的活（合成数据的形状），而本票的判据本来就该是
  `reads` 而不是秒表（票 `parking-3/01` 拿掉过一条挂钟断言）。
  **顺带说明**：`reads` 那一半也不是本票头一回量出来的——`docs/library-facts.md:1269`
  （票 `parking-3/17`）已经记着「946 → 10,978 行，两边都是 1 次库读」。本票这一趟是
  **改完之后再确认它没变**，不是新结论。
- **谁来裁：** 拿主意的人（与票 `17` 那一族是同一件事）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 那两个毫秒数**照实说出量法并标成不可复现**是对的——本票的判据本来就该是 `reads` 而不是秒表（票 `01` 刚拿掉过一条挂钟断言），而 `reads` 那一半由票 `17` 的 `Q357` 推到了真库量级（946 → 10,978 行，两边都是 1 次库读）。
