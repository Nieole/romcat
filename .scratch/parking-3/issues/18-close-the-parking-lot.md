# 18 — 挂单收口

**What to build:** 长期挂账那份文件归档关掉：以后不会再有一条问题因为「够不上票」而
无限期挂着。这一轮各票在实现路上新记下的挂单（`Q194` 起）也各自有归宿，翻任何一张票
都看得见它当初撞上什么、最后怎么裁的。

**Blocked by:** 01 02 03 04 05 06 07 08 09 10 11 12 13 14 15 16 17

**Status:** ready-for-agent

- [ ] `.scratch/rom-metadata-automation/deferred.md` **归档关掉**：只留 resolved 历史，表头写明**不再接新条目**，并说清为什么（够不上票的东西挂在那儿等于没人拍板）。归档前核对 `D153` `D157` `D158` `D161` `D162` `D163` 六条都已由票 `08` `10` `11` `12` `13` 收掉。
- [ ] 各票在实现过程中新记下的条目（`Q194` 起）各自有归宿：迁回、开成新票、或明确记着留到下一轮。分类只有三档：**A** 已经做完只是没销、**B** 不成立了、**C** 仍然成立要开票。
- [ ] `Q194` 起这一批**没有**升进长期挂账的条目——若最后发现有一条实在无处可去，那是一个要当场问维护者的信号，不是往 `deferred.md` 送的理由。
- [ ] 核对票 `01`–`17` 各自末尾那条「把本票的挂单标 `settled` 迁回原票」的验收都已勾上——迁回是各票收尾时自己的事，不在这张票里重做一遍。
- [ ] `.scratch/PARKING-LOT.md` 收尾一节补上第三轮的读数（`parking-3` 这 18 张票撞出几条 ÷ 18），接在第三轮收口那张表后面。
- [ ] 门禁全量跑一遍，最后一行抄进报告。

## 已经做掉的（2026-09-07，`/settle` 第三轮收口）

这张票原先还担着「把 `Q135`–`Q193` 那 21 条迁回、把条目区清空」。那批已经收了，
判据是**一条挂单有了主人就算收了，不是等它做完**——反过来读的话，最大的那条会把整批
摁在开着的状态，而 `Q194` 起的新条目在后面继续堆。

- [x] `Q135`–`Q193` 共 21 条全部有归宿，`.scratch/PARKING-LOT.md` 条目区清空，
      收尾一节写清了三条车道的分法、每条的去处、以及三轮的读数。
      **18 条落进本轮已经开着的票，0 条无家可归，0 条升进长期挂账。**
- [x] `Q191` 标 `settled` 迁回票 `gui-redesign/17`——那条重复的 `#[test]` 已由提交
      `bd4273d` 做掉（独立探针：`crates/core/tests/sync_run.rs` 现有 16 个 `#[test]`，
      没有相邻重复）。
- [x] `Q66` 主体标 `settled` 迁回票 `gui-redesign/03`——三个表头按钮已由票
      `gui-redesign/10` / `06` / `11` 画完（实测 `crates/gui/src/browse.rs`
      1039 / 1052 / 235 行），条目正文所在的票 `gui-redesign/13` 已改成指回票 `03`。
