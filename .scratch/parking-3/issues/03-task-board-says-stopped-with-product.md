# 03 — 任务台说得出「停了，留下了产物」

**What to build:** 维护者按停一趟已经往卡上写进去东西的同步，子库屏说「⚠️ 这一趟被你按停了」，
任务屏历史那一行**也**这么说——而不是记成「完成」。同一趟活不再在两屏上说两套话。

眼下核心库那对枚举只认三种收场（跑完 / 被按停 / 出错），说不出**「被按停，但留下了
可用的产物」**这第四种；扫描与同步被叫停时都返回「成功」，于是任务屏历史里那一行是
「完成」。（挂单 `Q153`）

**这是本轮的前置票**——票 `04` `05` `06` `09` 都要动它定下来的形状。

**Blocked by:** None — can start immediately

**Status:** done

- [x] 核心库那对近乎重复的收场枚举合流成**一条轴**，能表达四种收场：

  ```
  跑完了              → 有产物
  停了，什么都没留下   → 干净，可以当没跑过
  停了，留下了产物     → 有产物，且明说它是半截的
  出错了              → 停在哪一步、为什么
  ```

  （这个形状是从 `sync::execute::run` 与 `scan::scan` 两条真实路径上折出来的：两者都在
  被叫停时返回「成功」并把「被打断了」记在账上——ADR-0015 要求写过东西的活留下续跑的
  依据才停得干净，抛错会把清单丢掉。）

- [x] 同步与扫描被叫停时走**第三支**，不再折成「跑完了」。
- [x] 任务屏历史那一行如实画出这四种的区别，「停了，留下了产物」那一行说得出留下了什么（走了几步 / 落了几件）。
- [x] 子库屏那句回执与任务屏历史说的是**同一件事**——有测试对着「画出来的字」钉着两屏的措辞不打架。
- [x] 每一种收场都有测试：核心库那一侧断返回的是哪一支，界面那一侧断屏上画出来的是哪一句。
- [x] 挂单 `Q153` 标 `settled` 并迁回票 `gui-redesign/15`。

## 硬约束

- **不许抛错代替返回。** 写过东西的活抛出去等于把清单丢掉，下一趟会把这次放上去的东西当成「清单之外」，于是碰都不敢碰（ADR-0015）。
- 「剩余时间」不归这张票，归票 `04`；「按停之后措辞对不对得上」不归这张票，归票 `05`。这张票只把**轴**定下来。

## 收尾（2026-09-07）

**这条轴长什么样**（后面四张票接的就是它）：

```rust
pub enum Ending<T> {          // crates/core/src/task.rs
    Done(T),                  // 跑完了，产物在这儿
    Stopped,                  // 停了，什么都没留下——干净，可以当没跑过
    Halfway { product: T, left_behind: String },  // 停在半路：没走完就收了场，可交出了产物
    Failed { step: String, why: String },         // 出错了
}
```

`Done<T>` 没了，`Finished::ended` 是 `Ending<T>`，`Record::ending` 是
`Ending<()>`（`Ending::forget` 折的：产物归认领它的那一屏，历史只留账）。
**走哪一档、以及那句「留下了什么」整句，都由长入口自己说**——`Handle::halfway(…)`，
任务台在 `Board::settle` 里 `Ok(product)` 那一支问一次（那个 getter 不对外）。
`Ending::render` 折出来是「停在半路：{留下了什么}」——**「为什么收的手」不写死在这一层**，
因为下一批候选（连着失败太多次主动停了、刮削撞上配额）不是被按停的（`Q216` `Q217` `Q221`）。
分「停了」与「失败」的那道字符串比对**原样没动**（票 `05` 的活）。

### 逐条证据

- **合流成一条轴，四种收场** —— `crates/core/src/task.rs::Ending<T>`。
  测试：`task::tests::四种收场各画各的话`（四句去重之后仍是 4 条）、
  `task::tests::被按停却交出了产物的那一趟记成停在半路而不是完成`。
- **同步与扫描走第三支** —— `sync::execute::run` 在 `out.interrupted` 时报
  `task.halfway(留下了什么(out.touched()))`；`scan::scan` 在 `interrupted` 时报
  「走过 N 个条目，断点写下了」。界面那一层 `roots.rs` 里那道
  `Ok(outcome) if outcome.interrupted => Err(Halted.to_string())` **拿掉了**
  ——那份半份体检报告不再被丢掉（挂单 `Q214`）。
  测试（四条都**把活丢上任务台跑完再断它记成了哪一支**，断的不是那个侧信道）：
  `crates/core/tests/task.rs::同步按停时已经落下东西的那一趟在台上记成停在半路而且说得出落了几件`
  （用一份只读视图把那一下钉死在「读到第几个文件」上，不靠抢时间；断的是
  `Ending::Halfway`、`interrupted`、`touched() > 0`，以及那句话里的件数与 `touched()`
  **对得上**——不写死某个数）、
  `同步按停时一件都没落的那一趟也记成停在半路留下的只有清单`、
  `扫描被叫停时在台上记成停在半路而且说得出断点写下了`、
  `没设断点的那一趟被叫停时不许说断点写下了`（后两条断的是「断点写下了」/「没设断点」
  整半句——只断「断点」两个字的话，两支文案互换了照样绿）。
- **历史那一行画得出四种的区别、说得出留下了什么** ——
  `crates/gui/tests/task.rs::四种收场在任务屏历史里各画各的话`：对着**画出来的字**
  逐行断四句，其中那一行整整齐齐是
  「停在半路：按停时落了 1 件，清单记着到这儿为止目标上真实有什么」，
  而屏上「完成」只出现一次（四趟里只有一趟是真跑完的）。
- **两屏措辞不打架** ——
  `crates/gui/tests/sublibrary.rs::按停一趟同步之后子库屏与任务屏说的是同一件事`：
  真按「同步」再按「停下」，子库屏那句回执与任务屏历史都出现「按停」，
  任务屏那一趟自己那一行写着「停在半路」、**不含「完成」**。
  **保留一处**：这条测试里那一趟停在第一步之前（`touched() == 0`），
  屏上那句是「一件都没落，只重折了那份清单」——「落了 N 件」那半句的实测由核心那条
  `同步按停时已经落下东西的那一趟说得出落了几件` 钉着（它用一份只读视图把那一下
  钉死在「读到第几个文件」上，不靠抢时间）。
- **每一种收场都有测试** —— 核心：上面三条 + 既有的
  `被叫停时停在两步之间`（`Stopped`）、`失败的那一趟说得出哪一步为什么`（`Failed`）、
  `就地跑的那趟账照样进任务台`（`Done`）。界面：`四种收场在任务屏历史里各画各的话`
  一条覆盖四句，另有 `crates/gui/tests/roots.rs::界面上按停一趟扫描_…` 断扫描那一趟
  是 `Ending::Halfway` 且那句话里说得出「断点写下了」。
- **`Q153`** —— 已 `settled`，条目全文迁进
  `.scratch/gui-redesign/issues/15-sublibrary-long-runs-onto-the-board.md`
  的「挂单裁决（第三轮）」一节；`.scratch/PARKING-LOT.md` 的已收索引那一行跟着标了。

### 词表

`CONTEXT.md` 的「工具自己的状态」一节新增两条：**收场**（一条轴四档）与
**停在半路**。「停在半路」这个名字不是新起的——`Q153` 当年写下的代价那一段里
就是这么叫的（「停在半路，但留下了可用的产物」），刮削屏上也早就在这么说
（那句「这一趟停在半路：{配额用完 / 被封 / 断网}」）。词表把它收下、**写成理由无关的**
（`Q221`），并划清与「不留半截状态」的界：那说的是**那一步**干不干净，
半路说的是**整趟**只走了一段。

### 顺手收掉的一处

库屏那句回执原先把「停在半路」与「还排着队就被撤掉」并成同一句
「……断点已经写下，再按「重扫」从那儿接着跑」。改之前两档长得一样，那句话只能这么写；
轴分开之后它对**撤掉**那一种就是骗人的（压根没开跑，哪来的断点）。
`crates/gui/src/roots.rs::Screen::settle` 里那两支**拆开**了，撤掉那一趟改说
「还没轮到就被撤掉了。中立库与那块盘一个字节都没动」。

### 门禁四条（2026-09-08，`-j 6`）

| 门禁 | 结果 |
|---|---|
| `cargo fmt --all --check` | **绿** |
| `cargo clippy --workspace --all-targets --all-features` | **绿**，零告警（先红过一次：新写的测试里 `Box::new(BTreeMap::new())` 撞上 `clippy::box_default`，改成 `Box::default()`） |
| `cargo test --workspace --all-features` | **绿**，62 个测试目标、**1,639 条**全过 |
| `cargo doc --workspace --no-deps` | 退出码 0，**68 条**告警——与改动前**一条不多不少**，且没有一条指到本票改过的文件（那 68 条是票 `02` 的活，本票不碰） |

**一处保留**：全量测试头一趟在 `crates/gui/tests/media.rs:248::切换选中那一帧不解码`
上红过一次（`头一帧花了 1.02007901s`）。那是**负载假红**，不是这张票的事——同一棵树
安静单跑 `-- --test-threads=1` 当场绿（1.29 秒），第二趟全量跑（62 个目标、1,639 条）
一条都没红。那条挂钟断言归票 `parking-3/01`，正在飞行中，本票没碰它。

### 挂单

`Q214`–`Q222` 九条，见 `.scratch/PARKING-LOT.md` 的「## 条目」一节。
末一条 `Q222` 记的是收尾流程本身：`/code-review` 在 worktree 里认错工作区，
审成了主工作区的上一个提交；我另派两个只读 agent 对着本票的 diff 重跑，两份意见逐条落地。

## 挂单裁决（第三轮收口，2026-09-08）

本票在实现路上记下的 **9 条**（`Q214`–`Q222`）由票 `parking-3/18` 收口裁完，条目全文迁自 `.scratch/PARKING-LOT.md`（那边的条目区已清空）。**本票的原验收正文一个字没动。**

分三档：**A** 已经做完只是没销 ／ **B** 不成立了 ／ **C** 仍然成立要开票。判据是**一条挂单有了主人就算收了，不是等它做完**。本票：**A 5 ／ B 0 ／ C 4**。

| 挂单 | 档 | 一句话 |
|---|---|---|
| `Q214` | **A** | 票 `parking-3/03` 说「扫描被叫停时任务屏历史记成完成」，扫描那一半不成立 |
| `Q215` | **A** | 「停在半路」的判据是长入口自己报一句，不是任务台看「按过停下没有」 |
| `Q216` | **C**<br>第四轮「「停了」那条链剩下的段」 | 「连着失败太多次主动停了」与「有几步没做成」那两趟也留下了产物，仍记「完成」 |
| `Q217` | **A** | 刮削那条路上「被按停」有两个出口，其中一个照旧记成「完成」 |
| `Q218` | **C**<br>第四轮「零散（各自独立，不成组）」 | 被按停的扫描那份「到目前为止」的体检报告交回来了，可库屏只拿它说一句话 |
| `Q219` | **A** | 「留下了什么」是一句写在核心库里的中文，不是一组数 |
| `Q220` | **C**<br>第四轮「零散（各自独立，不成组）」 | `中立库落在本机而不是主库里` 那条测试每跑一次就在 `/tmp` 里留一个目录 |
| `Q221` | **A** | 「停在半路」这个词刮削屏上已经在用，指的是活自己收的手 |
| `Q222` | **C**<br>第四轮「门禁与工具链」 | `/code-review` 在 worktree 里认错工作区，审的是主工作区的上一个提交 |

### Q214 — 票 `parking-3/03` 说「扫描被叫停时任务屏历史记成完成」，扫描那一半不成立

- **来自：** 票 `parking-3/03`
- **类别：** 票写错了
- **在哪：** `.scratch/parking-3/issues/03-task-board-says-stopped-with-product.md` 第 7–8 行
  （「扫描与同步被叫停时都返回「成功」，于是任务屏历史里那一行是「完成」」）；
  改之前的 `crates/gui/src/roots.rs::Screen::scan`
  （`Ok(outcome) if outcome.interrupted => Err(Halted.to_string())`）
- **为什么没停线：** 票要做的事没变。核心那一侧确实两条都返回 `Ok`，但**界面那一层
  早就替扫描折了一道**：被中断的那一趟折成 `Halted` 那句话，于是它在任务屏历史里记的
  是「按停了」，不是「完成」。真记成「完成」的只有同步那一条（`Q153` 的原话说的也正是同步）。
  折那一下的代价是**那份「到目前为止」的体检报告被整个丢掉**——`Err(String)` 装不下产物。
- **这张票实际做了什么：** 把那道折**拿掉**：扫描被叫停时照旧交出 `Product::Scanned`，
  由 `scan::scan` 自己报一句 `Handle::halfway`，任务台据此记「停在半路」。
  于是三处说的是同一件事，而那份半份体检报告不再被丢掉。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 票写错了（「扫描与同步被叫停时都记成完成」，扫描那一半不成立——界面早就替扫描折了一道），已按实情做：把那道折拿掉，扫描被叫停时照旧交出 `Product::Scanned`，那份半份体检报告不再被丢掉。

### Q215 — 「停在半路」的判据是长入口自己报一句，不是任务台看「按过停下没有」

- **来自：** 票 `parking-3/03`
- **类别：** 规格没说
- **在哪：** `crates/core/src/task.rs::Handle::halfway` 与 `Board::settle` 里
  `Ok(product) => match handle.left()` 那一段
- **为什么没停线：** 两条路都站得住，翻案就是改 `settle` 里那三行。
- **这张票实际做了什么：** **走「长入口自己报」这条。** 另一条是任务台自己判
  ——`Ok(product)` 加上 `handle.stopped()` 就算停在半路，一句都不用长入口说。
  没选它的理由：**「按下停下那一刻正好跑完」的那一趟会被冤枉**。那时核心那一侧
  `interrupted` 是 `false`、子库屏回执一个字不提按停，而任务屏历史却写「停在半路」
  ——两屏又打架，正是这张票要收的那件事，只是掉了个个儿。代价是**新长入口忘了报那一句
  就会静默记成「完成」**：眼下报了的只有 `sync::execute::run` 与 `scan::scan` 两条，
  各有测试钉着；模块文档与 `Handle::halfway` 的文档都把这条义务写了出来。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 走「长入口自己报一句 `Handle::halfway`」这条，另一条（任务台看「按过停下没有」）会冤枉「按下停下那一刻正好跑完」的那一趟。代价（新长入口忘了报就静默记「完成」）写进了模块文档与 `Handle::halfway` 的文档，两条现有长入口各有测试钉着。

### Q216 — 「连着失败太多次主动停了」与「有几步没做成」那两趟也留下了产物，仍记「完成」

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/sync/execute.rs` 里 `out.gave_up = true; break;` 那条路，
  以及 `out.failures` 非空的那一趟；`crates/gui/src/sublibrary.rs::sync_notice`
  为这两样各印一句 `⚠️`
- **为什么没停线：** 这张票立的那一档是「**被按停**，可留下了产物」，票面与 `Q153`
  都只说按停。`gave_up` 与「有几步没做成」是另一件事：它们不是人按的，是活自己收的手。
- **这张票实际做了什么：** **只让 `interrupted` 那一档报「停在半路」。** 那两样照旧
  记「完成」，屏上靠子库屏那两句 `⚠️` 兜着——于是它们仍然是「两屏说两套话」的形状，
  只是这一次维护者点名要收的不是它们。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 「连着失败太多次主动停了」（`gave_up`）与「有几步没做成」那两趟仍然记「完成」，屏上靠子库屏两句 `⚠️` 兜着——**仍然是「两屏说两套话」的形状**，只是这一次维护者点名要收的不是它们。
  **去处：** 第四轮规格材料，「「停了」那条链剩下的段」那一组。

### Q217 — 刮削那条路上「被按停」有两个出口，其中一个照旧记成「完成」

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** 停在**步与步之间**的走 `crates/gui/src/scrape.rs` 里那几处手写的
  「按停了」，落进 `Ending::Failed`（那是 `Q151`／票 `parking-3/05` 的活）；
  停在**采集那一步里头**的走 `crates/core/src/scrape.rs:861`
  （`if context.cancel.is_cancelled() { interrupted = true; break; }` 然后 `Ok(Outcome)`）
  ——`Ok` 加上没报 `halfway` 就是 `Ending::Done`，历史写「完成」，而
  `crates/gui/src/scrape.rs::finished` 从头到尾不看 `outcome.interrupted`
  （命令行那一侧看的），屏上于是写「刮削跑完了」。识别那一层同理。
- **为什么没停线：** 票的验收点名的是「同步与扫描」两条。第一个出口的措辞是票
  `parking-3/05` 的活，这张票伸手进去只会跟它撞车；第二个出口要动的是刮削那份回执，
  与轴无关。
- **这张票实际做了什么：** 只把轴与两条路（同步、扫描）定下来。刮削那一侧的
  `Ending::Done` / `Ending::Halfway` 两支已经并成一条（产物照收），
  等票 `05` 把第一个出口改成由类型来的时候，一并决定 `scrape.rs:861` 那个出口
  该不该报 `halfway`——它形状上完全符合这一档（停了、交出了产物）。
- **谁来裁：** 票 `parking-3/05`
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 刮削那第二个出口由票 `05` 收掉了（`Q238`：`scrape::run` 那个出口现在报 `Handle::halfway`，记成 `Ending::Halfway`）。**识别那一层同一个形状**（`Ok` 加上没报 `halfway` 就是「完成」）仍然没碰，那半条落在 `Q238`。

### Q218 — 被按停的扫描那份「到目前为止」的体检报告交回来了，可库屏只拿它说一句话

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/src/roots.rs::Screen::settle` 里
  `(Ending::Halfway { .. } | Ending::Stopped, Job::Scan(_))` 那一支
- **为什么没停线：** 这是**多出来的机会**，不是坏掉的东西。改之前那份报告压根到不了
  界面（折成 `Err` 时丢了），现在它到了，只是没人看。
- **这张票实际做了什么：** 不动。这一支照旧只说「被按停了，断点已经写下」——
  「半份体检报告该怎么摆」是库屏自己的产品问题，不是这条轴的事。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 被按停的扫描那份「到目前为止」的体检报告现在到得了界面（改之前折成 `Err` 时丢了），可库屏只拿它说一句话。**是多出来的机会，不是坏掉的东西**——但「半份体检报告该怎么摆」是库屏自己的产品问题，今天没有人欠这个决定，也没有票接它。
  **去处：** 第四轮规格材料，「零散（各自独立，不成组）」那一组。

### Q219 — 「留下了什么」是一句写在核心库里的中文，不是一组数

- **来自：** 票 `parking-3/03`
- **类别：** 规格没说
- **在哪：** `crates/core/src/task.rs::Ending::Halfway::left`（一个 `String`）、
  `crates/core/src/sync/execute.rs::留下了什么`、`crates/core/src/scan/mod.rs` 那句
  `task.halfway(format!("走过 {} 个条目…"))`
- **为什么没停线：** 两条路都站得住，翻案只动这三处加它们的测试。
- **这张票实际做了什么：** **走「核心库出一句话」这条。** 另一条是让 `Halfway` 带一组
  结构化的数（落了几件 / 走了几步 / 断点有没有），界面自己排成话。没选它的理由：
  这张票要的就是**两屏说同一件事**，而话只有一份才不会漂——子库屏那句回执与任务屏历史
  那一行从此同源；何况「落了几件」与「走过几个条目」本来就不是同一组字段，硬凑一个结构
  等于给每条长入口留一片填不满的空格。代价是**核心库里多了给人看的中文措辞**
  （ADR-0005 说折算是领域逻辑，措辞算不算领域逻辑没定过）。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 走「核心库出一句话」这条，理由是这张票要的就是两屏说同一件事、而话只有一份才不会漂。代价（核心库里多了给人看的中文措辞，ADR-0005 没定过措辞算不算领域逻辑）写在条目里。

### Q220 — `中立库落在本机而不是主库里` 那条测试每跑一次就在 `/tmp` 里留一个目录

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/scan/mod.rs:2110` 那条单元测试里两处
  `crate::testing::temp_dir("catalog")` / `temp_dir("catalog2")`——两处都是
  **当场丢掉的临时值**：`temp_dir(…)` 返回的 `TempDir` 在那一句结束时就析构、
  目录被删掉，紧接着 `Catalog::open` 又 `create_dir_all` 把它建了回来，
  于是**再也没人删它**。这台机器上眼下攒了 224 个 `/tmp/romcat-catalog2-*`
- **为什么没停线：** 与这张票要动的东西毫无关系（我是在数 `/tmp` 里的残留时撞见的）。
  它也不会让测试变红——`assert!(path.exists())` 那一句恰恰因为「删了又建回来」才过。
- **这张票实际做了什么：** 不动。改法是把那两个 `TempDir` 绑成局部变量（`let _dir = …`），
  但那是别人的测试、别人的文件，顺手改进来只会让这张票的 diff 说不清自己在干什么。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— `crates/core/src/scan/mod.rs:2110` 那条测试每跑一次在 `/tmp` 留一个目录，而且**永远没人删**（`temp_dir(…)` 是当场丢掉的临时值，`TempDir` 析构删掉，`Catalog::open` 又 `create_dir_all` 建了回来）。票 `03` 记的是 224 个，**收口当天实测已经 246 个**。改法是两行（`let _dir = …`），它不会让任何东西红，所以除非有人特意去看，它会一直涨。
  **去处：** 第四轮规格材料，「零散（各自独立，不成组）」那一组。

### Q221 — 「停在半路」这个词刮削屏上已经在用，指的是活自己收的手

- **来自：** 票 `parking-3/03`
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/scrape.rs` 里那句 `" 这一趟停在半路：{}"`（印的是
  `crate::scrape::online::Halt`——配额用完、被封、退避、自设预算、断网）；
  与 `CONTEXT.md` 新收的词条「停在半路」
- **为什么没停线：** 两处说的是**同一件事**（没走完就收了场、却交出了产物），
  不是同名不同物——真要打架的只有「收手的理由」那半格。
- **这张票实际做了什么：** **把词条写成理由无关的**：「一趟活没走完就收了场，
  却交出了产物」，并明说收手的理由（人按的停下、活自己收的手）不改变这一档是什么。
  `Ending::render` 跟着**不再写死「按停时」**——那半句话由长入口自己给
  （`sync::execute::left_behind`、`scan::scan` 那句 `format!`）。另一条路是把词条限死成
  「被按停」，那样刮削屏那句话就得改口——可它那句是对的，改的该是词条。
  **刮削那一趟眼下仍然记成「完成」**，那是 `Q217`。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 词条「停在半路」写成**理由无关**的（「一趟活没走完就收了场，却交出了产物」），刮削屏那句原有的话是对的、改的是词条；`Ending::render` 跟着不再写死「按停时」。

### Q222 — `/code-review` 在 worktree 里认错工作区，审的是主工作区的上一个提交

- **来自：** 票 `parking-3/03`
- **类别：** 工具缺陷
- **在哪：** 这张票的收尾流程——`/code-review` 起的那个 fork 继承的是会话的 cwd
  （`/home/alex/workboox/romcat`），不是我干活的那个 worktree
  （`/home/alex/workboox/romcat-wt/03-outcome`）。它于是报「工作区干净」、退回去审
  `git diff HEAD~1`，交上来的八条发现全是主工作区上一个提交里那几份 Markdown 的事，
  与本票的 diff 一条都不沾。
- **为什么没停线：** 它只读，没写脏任何东西（主工作区已核过），而且**看得出来审错了**
  ——「工作区干净」这一句与本票十五个改过的文件当场对不上。
- **这张票实际做了什么：** **另派两个只读 agent 对着 diff 重跑**，一个看仓库标准与
  正确性、一个看规格符合性，提示里把 worktree 的绝对路径、`git -C <worktree>` 的取法、
  以及「不许 `checkout`/`restore`/`reset`/`stash`/`clean`、不许碰别的 worktree、
  不许 build」全写死。两份意见逐条落地了（拆开库屏那句合着写的回执、核心四条集成测试
  从「断侧信道」改成「断任务台记成了哪一支」、`render` 不再写死「按停时」、
  `left` → `left_behind` 并收成非公开）。**没有去改 `/code-review` 本身**：那是这套
  流程的工具，不是这个仓库的代码。
- **怎么防（给下一个人）：** 在 worktree 里收尾时，别指望 `/code-review` 自己找对地方
  ——要么调用时把 worktree 的绝对路径写进参数，要么直接照上面那套派两个只读 agent。
  判据只有一条：**它报的「审查范围」里得出现你那十几个改过的文件**，出现「工作区干净」
  就是审错了。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 与票 `10` 的 `Q288` 是同一条，**这一轮至少咬了两次**（票 `03` `10` 各一次）。补救办法两张票都用过、也都写清了判据（「它报的『审查范围』里得出现你那十几个改过的文件」）。工具本身不在这个仓库里（住在 `~/.claude/plugins/cache/` 的插件里，见 `Q365`），但**本仓库做得了的那一半在**：把「在 worktree 里怎么派审查、判据是什么」写进 `docs/agents/`。
  **去处：** 第四轮规格材料，「门禁与工具链」那一组。
