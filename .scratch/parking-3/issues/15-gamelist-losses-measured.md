# 15 — ES gamelist 先量再声明

**What to build:** ES gamelist 那份结构性损失清单从「**还没查过**」变成「查过了，是这几条」。
维护者据此知道把值交给这个格式存一趟会变成什么样。（挂单 `Q161`）

眼下它走的是默认的空清单——而空清单在文档与报告里定成「还没查过」，不是「这个格式什么都不丢」。

**Blocked by:** 14（词表把这个词定下来之后再写声明，免得声明里用的是一个后来要改的词）

**Status:** done

- [x] 照票 `14` 那条路子：**先量，再声明**。逐样往返实测，量到什么写什么。
      量的一律是 `库 → 文件 → 库`、**不给底本**那一趟（给了底本证的是档位那件事，不是这件）。
      量出来八条，落在 `crates/core/src/adapter/gamelist.rs::STRUCTURAL_LOSSES`：
      ① 值两端的空白（含 U+3000）被掐掉，`desc`、`x-` 扩展键与未知元素**不掐**，掐空了元素整条不写；
      ② 开发商 / 发行商 / 类型写了几条这件事按 `, ` 合成一条，与「一家名字里带逗号」分不开；
      ③ 字面上就叫 `unknown` 的那三样单独一条时整条消失；
      ④ 「这几个文件是同一部作品」这一层——摊成几个条目；
      ⑤ 一段式简介、标签、启动命令、工作目录这个格式没有对应元素；
      ⑥ 合集段整段不写；
      ⑦ 二十个规范资源槽里十一个没有元素，认得的九个也只写得下第一条路径；
      ⑧ `x-` 扩展键与未知元素一个键只写第一条值；键名带空白或 `/` 的键与值一起不见，
      带 `>` 的更坏——键被截断、值被前一段键名污染。
- [x] 每条声明都有一条往返测试钉着。八条对八个测试，全在
      `crates/core/src/adapter/gamelist.rs` 的 tests 模块：
      `值两端的空白被掐掉_而_desc_与扩展键不掐`、`多值字段合成一条_读回来分不出原先是几条`、
      `字面上就叫_unknown_的开发商整条消失`、`一个条目挂着几个文件_写出去就是几个条目`、
      `中立库有而这个格式没有元素的那几样整条不见`、`合集段整段不见`、
      `资源槽这个格式只认九个_一个槽也只写得下一条`、
      `扩展键与未知元素一个键只写第一条_键名带空白的整条不见`。
      每条测试既量往返、又钉那条声明里不许省的那句话（不掐的那三样、「分不开」、
      「整条」、「元数据每份都在」、丢了哪十一个槽……）。另加
      `结构性损失不看库里当下有什么`：一趟一样都没撞上，清单照样在。
- [x] **一个字节的行为都不改**——这张票只加声明与测试。
      `git diff main..HEAD` 里 `crates/` 的非测试改动只有三处：`gamelist.rs` 多了
      `STRUCTURAL_LOSSES` 这个 `const`、`Adapter for Gamelist` 多了 `structural_losses`
      这一个返回它的方法、`use` 多引一个 `StructuralLoss`。读、写、词法、`fold_game`
      一行没动。量到的损失一条都没去修——最该修的那条（键名带空白）只声明，记在挂单 `Q338`。
- [x] 空清单读作「还没查过」这条口径**不许弱化**：既有测试钉着「没声明结构性损失的格式
      一个字都不印」，量完之后那条测试要么仍然绿（换一个还没量过的格式当被测对象），
      要么被一条更强的替代——不许直接删掉。
      **两样都做了。** 两个真适配器现在都量过了，所以
      `crates/core/src/adapter/report.rs::没声明结构性损失的格式一个字都不印` 换了个
      **还没量过的格式**当被测对象（一个只在测试里存在、没覆写 `structural_losses` 的
      适配器），并从「渲染那一侧不印」加强到同时钉住三样：默认实现交出来的是空清单、
      导入报告一个字都不印、**导出报告**（用户在导出**之前**读的那一份）一样不印。
- [x] 报告与命令行输出**绝不印一句「无结构性损失」的假保证**。
      `crates/core/tests/gamelist.rs::导出前就说得出这个格式的结构性损失_而且导入那一侧说的是同一句`
      与 `crates/cli/tests/gamelist.rs::能力档位对用户可见` 各有一条
      `assert!(!…contains("无结构性损失"))`，`report.rs` 那条也加了同样一句。
      渲染那一侧本来就是「清单空就一个字不写」（`write_structural_losses`），一行没动。
- [x] 挂单 `Q161` 标 `settled` 并迁回票 `queue-followups/02`。
      迁在该票末尾「挂单裁决（第三轮）」那一节之后，带八条声明的清单与各自的测试名。

## 挂单裁决（第三轮）

这一趟新记了八条，全在 `.scratch/PARKING-LOT.md`：

- `Q334` —— `rating` 的 0.1 与 `releasedate` 补出来的月与日**没进**清单。照票 `14` 的裁法
  （同 `Q328`）不动 `LossyNote` 与 `StructuralLoss` 的分界。gamelist 这边比 Pegasus 还多
  一层：写那一侧自己就先四舍五入了，`库 → 文件 → 库` 连有损点都发不出来。
- `Q335` —— 行尾这一条**量不到**：我们的词法不规范化 `\r`，而 ES 家族都用的 pugixml 会。
  钉不住的声明就是照直觉列的，所以没写。它顺带说明我们读一份带 CRLF 的真 gamelist，
  与前端读它，拿到的值不一样。
- `Q336` —— 第 1 条（值两端的空白）是**读那一侧 trim** 的，不是格式词法逼出来的；
  照样声明了，`why` 里把归属说清。
- `Q337` —— 第 6 条（合集段）丢的是**一整段**而不是「一个值」；照样声明了，理由是
  「你的 `collection:` 那一段整段不会过去」正是维护者最该在导出前读到的一句。
- `Q339` —— 真的 1970 年 1 月 1 日表达不出来（`19700101T000000` 是这个格式的
  「没有日期」）。与第 3 条同一个形状，但它已经走 `LossyNote`，同 `Q334` 不动分界；
  顺带记了那条 `LossyNote` 的 `detail` 在这一格上说的是假话。
- `Q338` —— 第 8 条底下那个**该修的缺陷**：`x-` 键名带空白 / `/` 时写出去是个认不回来
  的标签，键与值一起消失；带 `>` 时键被截断、值被污染成一个看着正常的错值。
  本票只声明不修（硬约束），够开一张票。
- `Q340` —— **有底本**那一趟里 `merge_block` 根本不写 `game.unknown`，跨格式搬进来的
  未知键在导出后凭空消失。八条声明全是「不给底本」量的，说不到它；没追认成第九条声明，
  因为那是少填了一处、不是格式的边界。够开一张票。
- `Q341` —— 未知元素的键名与这个格式认得的元素同名时会静默串味（`unknown` 里有个
  `path`，写出去就是第二个 `<path>`）。与 `Q340` 同一处代码，一起看。

## `/code-review` 找出来的（都已落地）

在这个 worktree 上审 `main..工作树`。一条**必须改**、四条**建议改**，全部照办：

1. `crates/cli/tests/gamelist.rs` 那几条断言原先跑在**整份 `romcat adapters` 输出**上，
   而 Pegasus 那一节自己就含 `U+3000`、自己就含「分不开」——把 gamelist 的声明整条删掉
   照样绿。改成先抠出「ES-Gamelist 的结构性损失」那一节，再在节内断言。
2. 「与 Pegasus 那份清单差在哪」那段散文没跟着第 1 条一起改，还写着「`desc` 根本不掐」，
   与已经改好的「不掐的有三样」自相矛盾。改齐了。
3. 第 8 条比实测**窄**：词法扫标签名的终止符是空白、`/` 与 **`>`** 三个，而 `>` 那一格的
   坏法不一样也更坏——键被截断、值被前一段键名污染，拿回来是个**看着正常的错值**。
   实测 `x-我的>备注` 读回来是键 `我的`、值 `备注>值`。声明与测试都补了这一格，挂单
   `Q338` 也跟着改；顺带把判据从「合不合 XML 规范」纠正成「是不是词法的分隔符」
   （`x-我的&备注` 同样不合规范却原样回得来）。
4. `assert_eq!(Gamelist.structural_losses().len(), STRUCTURAL_LOSSES.len())` 是**恒真**的
   （函数体就是那个 const）。换成 `assert_eq!(…, 8)`——顺带钉住条数，删一条会红。
5. `合集段整段不见` 没量它真正改掉的那个值：这一趟 `FC/甲.zip` 读回来是 `甲.zip`。
   「不是丢了、是起了作用」只在**真的导入**里成立（靠 `rom_bases` 重新锚定）。补了断言，
   声明里也补上了这个前提。

另外两条「只是记一笔」开成了挂单 `Q340`、`Q341`；第 7 条（1970 哨兵）我在审查回来之前
已经自己量到并记成 `Q339`。审查确认：**一个字节的行为都没改**（`crates/` 里非新增的行
只有两行——`use` 多引一个类型、`report.rs` 测试里被替换的那一行）、公开文档里没有指向
私有项的 rustdoc 链接。

## 挂单裁决（第三轮收口，2026-09-08）

本票在实现路上记下的 **8 条**（`Q334`–`Q341`）由票 `parking-3/18` 收口裁完，条目全文迁自 `.scratch/PARKING-LOT.md`（那边的条目区已清空）。**本票的原验收正文一个字没动。**

分三档：**A** 已经做完只是没销 ／ **B** 不成立了 ／ **C** 仍然成立要开票。判据是**一条挂单有了主人就算收了，不是等它做完**。本票：**A 2 ／ B 0 ／ C 6**。

| 挂单 | 档 | 一句话 |
|---|---|---|
| `Q334` | **C**<br>第四轮「结构性损失那条分界与 gamelist 那处代码」 | `rating` 的 0.1 与 `releasedate` 补出来的月与日没进 gamelist 的清单 |
| `Q335` | **C**<br>第四轮「结构性损失那条分界与 gamelist 那处代码」 | 行尾这一条量不到：我们的词法不规范化，pugixml 会 |
| `Q336` | **A** | 「值两端的空白」这一条是适配器 trim 的，不是格式自己掐的 |
| `Q337` | **A** | 「合集段整段不见」算不算「一个值」的结构性损失 |
| `Q338` | **C**<br>第四轮「结构性损失那条分界与 gamelist 那处代码」 | `x-` 扩展键的键名里有空白，写出去是个坏标签，键与值一起没了 |
| `Q339` | **C**<br>第四轮「结构性损失那条分界与 gamelist 那处代码」 | `19700101T000000` 是这个格式的「没有日期」，于是真的 1970-01-01 表达不出来 |
| `Q340` | **C**<br>第四轮「结构性损失那条分界与 gamelist 那处代码」 | 有底本的那一趟里，中立库新增或改动的**未知元素**一个字都写不出去 |
| `Q341` | **C**<br>第四轮「结构性损失那条分界与 gamelist 那处代码」 | 未知元素的键名与这个格式认得的元素同名时会静默串味 |

### Q334 — `rating` 的 0.1 与 `releasedate` 补出来的月与日没进 gamelist 的清单

- **来自：** 票 `parking-3/15`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/gamelist.rs::STRUCTURAL_LOSSES` 的头部文档，
  与 `fold_game` 里 `rating` / `releasedate` 两个分支（推的是 `LossyNote`）；
  Pegasus 那一侧的同一条分界是 `Q328`
- **为什么没停线：** 票正文点名「照票 `14` 的裁法走：属于 `StructuralLoss` 的进清单，
  属于 `LossyNote` 那条既有路子的别去动分界，记挂单」——照着做就有归宿。
- **这张票实际做了什么：** 没进清单，在清单头部文档里点名说清楚「这不是全部天花板，
  这两族明确不在里面」，并指到本条。实测：库里 `rating` 是 `0.75`，往返回来是 `0.8`；
  只知道年份的 `1985` 往返回来是 `1985-01-01`——按**结构性损失**自己的定义两样都算。
  另一条路站得住：这一趟顺手声明进去，用户在 `romcat adapters` 上当场读得到。没走它，
  是因为那要动 `LossyNote` 与 `StructuralLoss` 的分界，而票 `queue-followups/02` 的验收
  正压在「两者分得很开」上，撤销它要伸到这张票之外（与 `Q328` 同一条理由）。
  **gamelist 这边还比 Pegasus 多一层，值得裁的时候一起看：** Pegasus 那族至少在读一份
  用户手写的文件时会发出有损点（`rating: 85` 那一行本来就没生效）；gamelist 这边
  `format_rating` 在**写**的那一侧自己就先四舍五入了，`库 → 文件 → 库` 这一趟
  **连有损点都发不出来**——文件里躺着的是 `0.8`，读回来也是 `0.8`，一个字都不说。
- **谁来裁：** 拿主意的人（与 `Q328` 一起）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— gamelist 这一侧的同一条分界（`rating` 的 0.75 → 0.8、只知年份的 `1985` → `1985-01-01`）。**gamelist 还比 Pegasus 多一层**：`format_rating` 在**写**的那一侧自己就先四舍五入了，`库 → 文件 → 库` 这一趟**连有损点都发不出来**——文件里躺着的是 `0.8`，读回来也是 `0.8`，一个字都不说。与 `Q328` `Q339` 一起裁。
  **去处：** 第四轮规格材料，「结构性损失那条分界与 gamelist 那处代码」那一组。

### Q335 — 行尾这一条量不到：我们的词法不规范化，pugixml 会

- **来自：** 票 `parking-3/15`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/gamelist.rs::lex`（不碰 `\r`），
  与 `STRUCTURAL_LOSSES` 头部文档里点名排除的第二族
- **为什么没停线：** 票要的是「先量，再声明」；量不到的东西照直觉写进清单，恰恰是票的
  硬约束禁止的那一样。
- **这张票实际做了什么：** 没声明，在清单头部文档里点名排除并指到本条。实测：
  `desc` 写 `"甲\r\n乙\r丙"`，我们自己往返回来 `\r` 一个不少。但 XML 规范 §2.11 要求
  解析器把 `\r\n` 与单个 `\r` 都规范成 `\n`，而 ES 家族四个代码库**都用 pugixml**
  （调研 §B.1），也就是说**真正的 ES-DE 读同一份文件会得到不一样的值**。
  另一条路站得住：按规范写一条声明，`why` 里引 XML 规范与 pugixml。没走它，是因为票的
  硬约束写死「每条声明配一条往返测试钉着」，而我们这一侧钉不住——写下去就是一条永远
  绿不了、也永远没人核过的话。**这条比声明缺一句更值得看：** 它意味着我们读一份带 CRLF
  的真 gamelist，与前端读它，拿到的值不一样。要修得动词法（`\r` 规范化），而那与
  「逐字节往返」直接打架，够开一张票。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— **我们读一份带 CRLF 的真 gamelist，与真正的 ES-DE 读同一份文件，拿到的值不一样**：XML 规范 §2.11 要求解析器把 `\r\n` 与单个 `\r` 都规范成 `\n`，而 ES 家族四个代码库都用 pugixml，我们自己的 `lex` 不碰 `\r`。没声明是对的（票的硬约束写死「每条声明配一条往返测试钉着」，而我们这一侧钉不住）。条目自己写着**够开一张票**——要修得动词法，而那与「逐字节往返」直接打架。
  **去处：** 第四轮规格材料，「结构性损失那条分界与 gamelist 那处代码」那一组。

### Q336 — 「值两端的空白」这一条是适配器 trim 的，不是格式自己掐的

- **来自：** 票 `parking-3/15`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/gamelist.rs::fold_game` 第一行
  （`let value = kid.text.trim().to_string();`），声明落在 `STRUCTURAL_LOSSES` 第 1 条
- **为什么没停线：** 声明与不声明都不动一个字节的行为，翻案只要改一个 `const` 与一条测试。
- **这张票实际做了什么：** **照样声明了**，并在 `why` 里把归属说清（「手改过的 gamelist 里
  元素文本两端带的常常是文件自己的缩进，单行那一族分不出它与值本身的空白」）。
  词表说的是「这个格式**自己**会把它改成什么样」，而 XML 是有界定符的——`<name>  甲  </name>`
  在规范上毫不含糊，掐掉两端是**读那一侧**的选择，Pegasus 那几条则是格式词法逼出来的。
  另一条路站得住：不声明，只记挂单，等有人裁定这一条到底算谁的。没走它，是因为用户要
  知道的是「把值交给这条路存一趟会变成什么样」，而这一趟真的会掐——不说才是那句
  「没人核过的保证」。有一半是格式的：`desc` 之所以是唯一的例外，是因为源码里它是
  `MD_MULTILINE_STRING`、别的元素是 `MD_STRING`（调研 §B.1 那张 18 个字段的表），
  这条分界不是我们划的。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 「值两端的空白」照样声明了，并在 `why` 里把归属说清（掐掉两端是**读那一侧**的选择，不是格式词法逼出来的）。理由：用户要知道的是「把值交给这条路存一趟会变成什么样」，不说才是那句「没人核过的保证」。有一半确实是格式的（`desc` 是 `MD_MULTILINE_STRING`、别的是 `MD_STRING`，这条分界不是我们划的）。

### Q337 — 「合集段整段不见」算不算「一个值」的结构性损失

- **来自：** 票 `parking-3/15`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/gamelist.rs::STRUCTURAL_LOSSES` 第 6 条；
  行为在 `render_fresh` / `render`（`entry.game()` 拿不到就跳过）
- **为什么没停线：** 同 `Q336`，翻案的代价全在这张票之内。
- **这张票实际做了什么：** **声明了。** 词条的原话是「把**一个值**交给某个格式存一趟」，
  而这一条丢的是**一整段**（合集的简介、短名与 `x-` 扩展键）。另一条路站得住：把它留给
  **能力档位**去说，或者干脆不说。没走它，是因为「导出前才看得见」这件事上，
  「你的 `collection:` 那一段整段不会过去」正是维护者最该在导出**之前**读到的一句；
  归到档位那四个词里说不出来（档位答的是能不能双向走）。实测：写出去文件里
  「红白机」「我的备注」一个字都没有，读回来一个合集段都没有；只有 `directory` 是
  **用掉了**的——`<path>` 相对它解析，写的时候剥掉了那一段。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 「合集段整段不见」声明了。词条原话说的是「把**一个值**交给某个格式存一趟」而这一条丢的是一整段——终局理由：「你的 `collection:` 那一段整段不会过去」正是维护者最该在导出**之前**读到的一句，而档位那四个词说不出来。

### Q338 — `x-` 扩展键的键名里有空白，写出去是个坏标签，键与值一起没了

- **来自：** 票 `parking-3/15`
- **类别：** 工具缺陷
- **在哪：** `crates/core/src/adapter/gamelist.rs::write_block` / `merge_block`
  （`write!(out, "<{EXTRA_PREFIX}{name}>…")`，键名一个字都没检查）
- **为什么没停线：** 票的硬约束写死「一个字节的行为都不改，量到损失就声明它，不要去修」。
- **这张票实际做了什么：** **只声明，没修**（`STRUCTURAL_LOSSES` 第 8 条）。实测：
  `x-` 键名是 `我的 备注` 或 `我的/备注` 时，写出去的 `<x-我的 备注>值</x-我的 备注>`
  不是一个认得回来的标签，词法当整行文本跳过，读回来 `extra` 与 `unknown` 都是空的——
  **键与值一起消失，一句话都不说**。**键名带 `>` 的更坏**（`/code-review` 找出来的）：
  `x-我的>备注` 读回来是键 `我的`、值 `备注>值`——键被截断、值被前一段键名污染，
  用户拿回来的是一个**看着正常的错值**，比一个空缺难发现得多。`&`、`<`、引号与数字开头的
  键名反而都回得来——判据是「是不是词法的分隔符」（`lex_start` 扫名字撞上空白、`/`、`>`
  就收尾），不是「合不合 XML 规范」。
  这条不是理论风险：**Pegasus 的 `x-` 键是自由文本**（`x-我的 备注` 完全合法，键名只被
  小写化），跨格式搬一趟正撞上。另一条路站得住：写的时候把键名里的空白换成 `_`，或者
  这种键干脆推一条 `LossyNote`，再不然写标签名那一步就该转义。没走它，是因为票的硬约束
  把「改行为」划在范围之外，而这几条出路都改行为、也都不止一个方向站得住。**够开一张票**，
  与 `Q340`、`Q341` 是同一处代码的三个方向，一起看。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— `x-` 扩展键的键名里有空白时**键与值一起消失、一句话都不说**；带 `>` 的更坏——键被截断、值被前一段键名污染，用户拿回来的是一个**看着正常的错值**。判据是「是不是词法的分隔符」，不是「合不合 XML 规范」。**不是理论风险**：Pegasus 的 `x-` 键是自由文本，跨格式搬一趟正撞上。条目自己写着**够开一张票**，与 `Q340` `Q341` 是同一处代码的三个方向。
  **去处：** 第四轮规格材料，「结构性损失那条分界与 gamelist 那处代码」那一组。

### Q339 — `19700101T000000` 是这个格式的「没有日期」，于是真的 1970-01-01 表达不出来

- **来自：** 票 `parking-3/15`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/gamelist.rs::parse_datetime`（`year == 1970 && month == 1
  && day == 1` 那个特判）与 `fold_game` 的 `releasedate` 分支
- **为什么没停线：** 与 `Q334` 同一条分界，照票 `14` 的裁法走就有归宿。
- **这张票实际做了什么：** **没进清单**，只记在这儿。实测：库里 `release` 是
  1970-01-01，写出去是 `<releasedate>19700101T000000</releasedate>`，读回来 `release` 是
  `None`——那是 ES-DE 的默认值（epoch 0），意思是「没有发行日期」。这与清单第 3 条
  （字面上就叫 `unknown` 的开发商）**是同一个形状**：这个格式的默认值本身是一个合法值，
  于是那个值表达不出来。另一条路站得住：把它跟第 3 条并成一条「默认值撞车」声明。
  没走它，是因为裁的口径是**机制**——第 3 条那一族一句话都不说（`push_non_empty`
  静默丢），而这一条**已经走 `LossyNote`**（读回来推一条 `Dropped`），动它就是动分界。
  **顺带记一笔：那条 `LossyNote` 的 `detail` 在这一格上说的是假话**——它写着
  「`releasedate` 只认 `%Y%m%dT%H%M%S`……别的写法解不出来」，可 `19700101T000000`
  正是那个写法，解不出来的原因是特判，不是格式不认。裁这一条时一起看。
- **谁来裁：** 拿主意的人（与 `Q334` 一起）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— `19700101T000000` 是这个格式的「没有日期」，于是真的 1970-01-01 表达不出来——与清单第 3 条（字面上就叫 `unknown` 的开发商）是同一个形状：格式的默认值本身是一个合法值。顺带记着**那条 `LossyNote` 的 `detail` 在这一格上说的是假话**（它写「别的写法解不出来」，可 `19700101T000000` 正是那个写法，解不出来的原因是特判）。与 `Q334` 一起裁。
  **去处：** 第四轮规格材料，「结构性损失那条分界与 gamelist 那处代码」那一组。

### Q340 — 有底本的那一趟里，中立库新增或改动的**未知元素**一个字都写不出去

- **来自：** 票 `parking-3/15`（`/code-review` 找出来的）
- **类别：** 工具缺陷
- **在哪：** `crates/core/src/adapter/gamelist.rs::merge_block`——`changed` 只由 `ORDER`、
  `assets`、`extra` 三处填，`game.unknown` 一次都没进去；而没有底本那一侧的
  `write_block` 是写它的
- **为什么没停线：** 这张票只加声明与测试，硬约束写死「一个字节的行为都不改」。
- **这张票实际做了什么：** 没修，也**没声明**，只记在这儿。八条声明全是「不给底本」
  量出来的（那才是「把值交给这个格式存一趟」的定义），一条都说不到这件事——这是
  **有底本**那条路上的行为不一致，不是格式的天花板。跨格式搬（Pegasus 的未知键 →
  一份已经存在的 gamelist）正会撞上：那个键在导出后凭空消失，而报告里的
  `kept_verbatim` 只数「基线里没人认领的段」，数不到它。另一条路站得住：把它当成
  第九条结构性损失声明出去。没走它，是因为声明的是**格式**做不到什么，而这一条是
  `merge_block` 少填了一处——声明它等于把一个该修的 bug 追认成格式的边界。**够开一张票。**
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 有底本的那一趟里，中立库新增或改动的**未知元素一个字都写不出去**（`merge_block` 的 `changed` 只由 `ORDER`、`assets`、`extra` 三处填，`game.unknown` 一次都没进去），而报告里的 `kept_verbatim` 只数「基线里没人认领的段」、数不到它。没声明是对的——**声明它等于把一个该修的 bug 追认成格式的边界**。条目自己写着**够开一张票**。
  **去处：** 第四轮规格材料，「结构性损失那条分界与 gamelist 那处代码」那一组。

### Q341 — 未知元素的键名与这个格式认得的元素同名时会静默串味

- **来自：** 票 `parking-3/15`（`/code-review` 路过发现）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/adapter/gamelist.rs::write_block` 原样写 `<{name}>`，
  而 `fold_game` 的 `match` 里 `path` / `name` / `desc` 那几个分支排在 `unknown` 之前
- **为什么没停线：** 读一份 gamelist 产生不出这种键（认得的元素名都被前面的分支接走了），
  眼下触不到。
- **这张票实际做了什么：** 没动，只记在这儿。跨格式搬进来的 `unknown` 里若有一个叫
  `path` 的键，写出去会是**第二个** `<path>`，读回来直接追加进 `files`——一个条目凭空
  多一个文件，一句话都不说。与 `Q340` 是同一处代码的两个方向，裁的时候一起看。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 未知元素的键名与这个格式认得的元素同名时会**静默串味**：跨格式搬进来的 `unknown` 里若有一个叫 `path` 的键，写出去是第二个 `<path>`，读回来直接追加进 `files`——一个条目凭空多一个文件，一句话都不说。与 `Q340` 是同一处代码的两个方向。
  **去处：** 第四轮规格材料，「结构性损失那条分界与 gamelist 那处代码」那一组。
