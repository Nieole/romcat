# 15 — ES gamelist 先量再声明

**What to build:** ES gamelist 那份结构性损失清单从「**还没查过**」变成「查过了，是这几条」。
维护者据此知道把值交给这个格式存一趟会变成什么样。（挂单 `Q161`）

眼下它走的是默认的空清单——而空清单在文档与报告里定成「还没查过」，不是「这个格式什么都不丢」。

**Blocked by:** 14（词表把这个词定下来之后再写声明，免得声明里用的是一个后来要改的词）

**Status:** ready-for-agent

- [ ] 照票 `14` 那条路子：**先量，再声明**。逐样往返实测，量到什么写什么。
- [ ] 每条声明都有一条往返测试钉着。
- [ ] **一个字节的行为都不改**——这张票只加声明与测试。
- [ ] 空清单读作「还没查过」这条口径**不许弱化**：既有测试钉着「没声明结构性损失的格式一个字都不印」，量完之后那条测试要么仍然绿（换一个还没量过的格式当被测对象），要么被一条更强的替代——不许直接删掉。
- [ ] 报告与命令行输出**绝不印一句「无结构性损失」的假保证**。
- [ ] 挂单 `Q161` 标 `settled` 并迁回票 `queue-followups/02`。
