# 14 — 词表收下「结构性损失」+ Pegasus 补齐

**What to build:** 「把值交给这个格式存一趟会变成什么样」这件事有了词表里的名字，
代码、文档与维护者说话时用的是同一个词。Pegasus 那份结构性损失清单说的是这个格式的
**全部**天花板，不是碰巧撞见的那两样。（挂单 `Q159` `Q160` `Q163`）

眼下词表里的**能力档位**四个词（只读、只写、双向、无损往返）答不了这件事，那条词条
已经窄了半格；而清单里只声明了「单个换行」与「值开头的空白」两条。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] `CONTEXT.md` 收一条新词条，与**能力档位**并列，说清两者的分界（一个说「这个格式能不能双向走」，一个说「走一趟之后值变成什么样」）。带 `_Avoid_` 行。
- [ ] 代码里的类型名与中文文档统一到这个词上——现在中文那一侧刻意说的是「这个格式**结构上**装不下什么」这一句话，不是一个词。
- [ ] Pegasus 补三条声明，**先量再声明**，行为一个字不改：
  - 值**末尾**的空白与开头那一样被掐掉（现在只声明了开头）
  - 值里**内容恰为 `.` 的一行**读回来成了段落分隔
  - 多值字段里某个值带换行的会被拆成多个值；多值里的**空串**写成 `.`、读回来是字面量 `.`
- [ ] 每条声明都有一条往返测试实测钉着。
- [ ] 清单长度**不许**被断言死（既有测试已经改成按关键字找，别退回去）。
- [ ] 挂单 `Q159` `Q160` `Q163` 标 `settled` 并迁回票 `queue-followups/02`。

## 硬约束

- **别顺手去改 ES gamelist 那个适配器**——它是另一个格式，归票 `15`。
- 词表**只是词表**：不放实现细节、不放取舍记录。
