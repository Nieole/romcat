# 09 — 整批收藏离开画帧线程

**What to build:** 维护者按下「全选 46,483 行 → ★ 收藏」，窗口不冻住——它像别的长活一样
排上任务台，看得见进度、按得停。实测眼下这一下在画帧那条线程上跑 **6.7 秒**。（挂单 `Q119`）

那 6.7 秒里的大头是：判据折算**一个变体问四次库**（成员、容器构成、容器状态、算过的哈希）。
真库上队列有 18,241 条要各算一次，所以这条不只是「全选」那一下的事。

> **收尾更正（两处）：** ① 收藏那一路每个变体其实是 **5–6 次**——上面那四次之外，
> `collection::apply` 自己还要一次 `Catalog::variant`，`landed` 的路径锚那一支再一次。
> ② 「大头是查询」这个判断在实测上不成立：批掉那些查询只省下不到 2 秒，其余是
> `identify::collect` 的纯 CPU。两条都记在挂单 `Q274`。

**Blocked by:** 03

**Status:** done

- [x] 识别那一层加一个**批量取判据**的入口：一趟几条查询取回整批，不是一条四次。
      → `identify::content_prints`。底下六条按键取一批的入口
      （`Catalog::variants_of` / `variant_members_of` / `container_files_of` /
      `container_status_of` / `entry_facts_of` / `content_hashes_of`），每 500 个键一段
      （`catalog::KEYS_PER_QUERY`）。

      **往返次数（折判据那一路）：** 全选 46,483 行 = **93 段 × 最多 6 条 = 558 条**；
      改之前是每个变体 5–6 次（成员是容器还是裸文件差一次），约 **23–28 万次**。
      **段同时管着内存**：一段开一份 `Bulk`、一段丢一份，所以峰值驻留与这一批多大无关
      （ADR-0005 那条「绝不把全库载入内存」）。

      **保留**：`collection::landed` 那一条**不在这 558 里**——内容锚每条还要一次
      `variants_with_content`，真库上那是约 46,483 次，本票没批量化它（挂单 `Q279`）。
      合成数据上锚基本都是路径锚，那一支由 `known` 短路成零次，所以下面那张实测表
      量不到它。
- [x] 单条那份改成批量那份的**特例**，两者共用同一个说法。**不许另写一遍**——那一层是「谁代表这个变体」的唯一说法，写两遍迟早漂开，而漂开的后果是同一个变体上的收藏与裁决钉在不同的东西上。
      → `content_print(catalog, variant)` 就是 `content_prints(catalog, [variant])`，
      `representative` / `ordered` 一个字都没有第二份；六条单键版全部改成对应批量版
      「一个键」那一档的特例，SQL 也只有一句。
      钉着的测试：`crates/core/tests/collection.rs::批量取判据与一条一条问_答的是同一份`
      （三档并排的 fixture 上，整批与一条一条逐个比，连拿不到判据那一个的 `None` 一起比）。
- [x] 队列填判据那一路改走批量入口，实测数字写进报告。
      → `triage::fill_prints` 与 `triage::plan_forget`（后者改成**先筛完再整批算**，
      筛掉的那些一次判据都不付）。实测 `--bench-queue --queue-rows 16656`：
      **排一次计划 263.4 ms（1,396 条）**。往返：那 1,396 条由约 4,200–5,600 次变成
      3 段 × 最多 5 条 = **15 次**；真库的 18,241 条由约 5.5–7.3 万次变成
      37 段 × 5 = **185 次**。
      **保留**：没有同机的「改之前」那个耗时（旧代码已不在树上），只有次数是精确的——
      见挂单 `Q274`。
- [x] 整批收藏排上任务台，跑完了按号认领——与差量预览一模一样的形状。
      → `collection` 拆成只读的 `plan`（收把手、报进度、按得停）与只写的 `commit`；
      `gui::task::Product::Planned` + `browse::Screen::settle_collection` 按号认领，
      写两份库在认领那一步（与同步那一趟 `Product::Synced` 同一个形状）。
      `collection::add` / `remove` 成了这两半接起来的那个特例。
      钉着的测试：`crates/core/tests/collection.rs::排一趟再落下去_与当场收藏那一下一个字不差`
      与 `整批收藏的读那一半停得下来_而且停下时一个字都没写`（后者断那句话**正是**
      `Halted` 交出来的那一句——任务台按它分「停了」与「失败」）。
- [x] 它要一份**落在磁盘上的库**（内存库分不出第二份连接）。内存库上走哪条路（退回同步 / 直接拒绝并说清楚）由实现者挑一条，**在挂单里写明走了哪条**。
      → 走**退回就地跑完**（`Board::run_here`），理由与代价写在挂单 `Q275`：
      同一屏上子库屏那两处（`preview` / `evaluate`）早就是这么走的，第三处换一种处置
      等于让「分不出第二份连接」在界面上有两个意思。**别的原因分不出来就直说**，
      不偷偷退回画帧线程。
- [x] 有一条界面测试断：这一下跑着的时候屏上有进度、有「停下」。
      → `crates/gui/tests/browse.rs::整批收藏排上任务台_跑着的时候屏上有进度也按得停`。
      它在**磁盘上**搭了一份小库（合成数据只活在内存里，走的是就地跑完那一支，
      台上留不下「正在跑的那一趟」），断的是**画出来的字**：任务名「放进「收藏」」、
      「停下」、以及走到哪一步（「折锚」）。
- [x] 实测重跑一遍（dev profile，同一份合成数据），新数字与 6.7 秒并排写进报告。
      → `--bench-browse --rows 46483`（`demo::browse(46_483)`，中立库在内存里，
      同机三跑取中位）。**尺子**是「不筛的全选展开」那一行：`Q119` 当年 40 ms，
      现在 45 ms，两次测量可比。

      | 这一步 | `Q119` 记的 | 本票之后 |
      |---|---|---|
      | 不筛的全选展开成 46,483 个键 | 40 ms | 44 ms |
      | 落一次整批收藏（展开 + 排锚 + 落两份库） | **6.7 s** | **4.8 s** |
      | 其中排锚那一半（真库上跑在画帧线程之外） | —— | 3.75 s |
      | **画帧那条线程上还剩** | 6.7 s | **约 1.05 s** |

      这两行从此**量得出来、不用手搭**（`crates/gui/src/bench.rs::browse` 末尾那一步）。
      **保留**：票说「那 6.7 秒的大头是判据折算一个变体问四次库」——实测在这份合成数据上
      批掉那些查询只省下不到 2 秒，其余 3.75 秒是 `identify::collect` 的纯 CPU。
      **这不是票写得不够细，是病因诊断错了**，记在挂单 `Q274`，拆法与两条出路在 `Q277`。
      ⚠️ **那几个数的口径两条得连着读**：① 全是 `-O0` 上量的，release 上「查询 : CPU」
      的配比很可能倒过来；② 这份合成数据一条 `content_hash` 都没有、容器构成是空的，
      真库上配比会不一样。**拿它去开票之前先补这两个数**（`Q277` 的收尾判据）。
- [x] 挂单 `Q119` 标 `settled` 并迁回票 `gui-redesign/06`。
      → 票 `gui-redesign/06` 末尾新开一节「挂单裁决（第三轮）」，`Q119` 连裁决与那张
      实测表落在那儿；票 `gui-redesign/15` 里寄放的那一份标了 `settled` 并指过去
      （它当年落在 15 上是迁条目时粘串了行，`Q154` 记着）。**原验收正文一个字没动。**

## 门禁

`cargo xtask gate --keep-going -j 6`，**四条全绿**（机器空着跑的，`13` 刚合并、
别的 worktree 已拆）：

| 条 | 结果 | 耗时 | 说的是 |
|---|---|---|---|
| `fmt` | 绿 | 1 s | `cargo fmt --all --check` |
| `clippy` | 绿 | 19 s | `cargo clippy --workspace --all-targets --all-features` |
| `test` | 绿 | 372 s | **1,656 通过 / 0 失败，66 个目标** |
| `doc` | 绿 | 16 s | `cargo doc --workspace --no-deps` |

- **`--all-features` 没省**，`--keep-going` 也带了：66 个目标、1,656 条，数对得上。
  `main` 上那一份是 1,681 条 / 66 个目标——本分支从 `4ab25c1` 切出，中间 `main` 又合进了
  六张票，少的那 25 条是它们带来的，不是这一票丢的。
- **那条挂钟彩票（`任务六秒都没跑完`，挂单 `Q349`）这一趟没红**：机器是空的。
- **`cargo doc` 那条是软的**：66 条既有 rustdoc 告警。头一趟跑出来是 66（`romcat-core`
  58 + `romcat-gui` 8），比既有的多两条——**都出在本票新写的那一段文档上**
  （`identify::content_prints` 的文档链到了私有的 `KEYS_PER_QUERY` 与 `Bulk`）。
  当场改成不带链接的代码体，`cargo doc` 重跑：**56 + 8 = 64**，与既有基线一致，
  **本票一条新告警都没添**。那 64 条是票 `parking-3/02` 的活，没顺手去清。
- 那一处改的是**一行文档注释**，所以改完只重跑了 `fmt`（干净）、`clippy`（绿）、
  `doc`（64 条，全绿）；`test` 那条的结果原样成立——一行注释改不动它。

## 本票留下的挂单

`Q274`–`Q283`，在 `.scratch/PARKING-LOT.md` 上：

| 号 | 一句话 |
|---|---|
| `Q274` | **票把病因诊断错了**：批掉那些查询只省下不到 2 秒（口径：`-O0`、合成数据） |
| `Q275` | 内存库上走的是**退回就地跑完**，与子库屏那两处同一条 |
| `Q276` | 写那一半仍在画帧线程上（约 1.05 秒）——中立库全程只有一个写者 |
| `Q277` | 那 3.75 秒的大头是 `collect` 的纯 CPU；两条出路（先试 B），开票前得先补 release 与真库两个数 |
| `Q278` | 一条 `IN` 问 500 个键是量级判断，不是量出来的 |
| `Q279` | `scrape::zh::resolve` 与 `landed` 的内容锚那一支仍是一条一条问 |
| `Q280` | 界面测试自己搭了一份**磁盘上**的小库，票 `16` 并夹具时该一起看 |
| `Q281` | 一屏一次只排一趟收藏：第二次按星当场说一句，不排队 |
| `Q282` | 取消收藏与自建合集那两下也一起搬上了任务台（票只点名了收藏） |
| `Q283` | `KEYS_PER_QUERY` 与占位符在同一个 crate 里有两份（另一份在 `catalog::scrape`） |

## 别顺手做的

- 增量投影与整份重建走**同一个展开**这条正确性修复不许退回去——它当初买回来 1.5 秒是有理由的：同一份内容存了两处时，不这么做屏上这一下与下一趟识别会给出两种样子。

## 挂单裁决（第三轮收口，2026-09-08）

本票在实现路上记下的 **10 条**（`Q274`–`Q283`）由票 `parking-3/18` 收口裁完，条目全文迁自 `.scratch/PARKING-LOT.md`（那边的条目区已清空）。**本票的原验收正文一个字没动。**

分三档：**A** 已经做完只是没销 ／ **B** 不成立了 ／ **C** 仍然成立要开票。判据是**一条挂单有了主人就算收了，不是等它做完**。本票：**A 8 ／ B 0 ／ C 2**。

| 挂单 | 档 | 一句话 |
|---|---|---|
| `Q274` | **A** | 票说那 6.7 秒的大头是「一个变体问四次库」，实测批掉那些查询只省下不到 2 秒 |
| `Q275` | **A** | 内存库上整批收藏走哪条路：**退回就地跑完** |
| `Q276` | **A** | 写那一半仍在画帧那条线程上：46,483 行约 1.05 秒 |
| `Q277` | **A** | 排锚那 3.75 秒的大头是 `collect` 的纯 CPU，不是查询 |
| `Q278` | **A** | 一条 `IN` 一次问 500 个键，这个数是量级判断不是量出来的 |
| `Q279` | **A** | 还有两处仍在一条一条问判据，这一票没跟着改 |
| `Q280` | **C**<br>第四轮「合成数据与夹具」 | 界面测试要一份**落在磁盘上**的库，于是这一票自己搭了一份 |
| `Q281` | **A** | 一屏一次只排一趟收藏：第二次按星当场说一句，不排队 |
| `Q282` | **A** | 取消收藏与自建合集那两下也一起搬上了任务台，票只点名了收藏 |
| `Q283` | **C**<br>第四轮「零散（各自独立，不成组）」 | `KEYS_PER_QUERY` 与占位符在同一个 crate 里有两份 |

### Q274 — 票说那 6.7 秒的大头是「一个变体问四次库」，实测批掉那些查询只省下不到 2 秒

- **来自：** 票 `parking-3/09`
- **类别：** 票写错了
- **在哪：** 票 `09-bulk-favorite-off-the-paint-thread.md` 第 6 行「那 6.7 秒里的大头是：
  判据折算**一个变体问四次库**」；实测处 `crates/gui/src/bench.rs::browse` 新加的
  「落一次整批收藏」那两行
- **实测**（dev profile，`demo::browse(46_483)`，中立库在内存里，同机三跑取中位；
  尺子是「不筛的全选展开」＝ 43–45 ms，挂单 `Q119` 那张表同一行是 40 ms，机器可比）：

  | 这一步 | `Q119` 记的 | 本票之后 |
  |---|---|---|
  | 不筛的全选展开成 46,483 个键 | 40 ms | 44 ms |
  | 落一次整批收藏（展开 + 排锚 + 落两份库） | **6.7 s** | **4.8 s** |
  | 其中排锚那一半（真库上在画帧线程之外） | —— | 3.75 s |
  | **画帧那条线程上还剩** | 6.7 s | **约 1.05 s** |

- **拆开来看**（临时插的计时，量完就撤了）：46,483 个变体上 `变体行 335 ms ｜ 取判据
  2.2 s ｜ 折锚 1.28 s`。取判据那 2.2 秒里 SQL 往返只剩四百多条，其余是
  `identify::collect` 为每个变体折 unit、折那句「为什么没判据」的纯 CPU（`-O0`）。
- **它错在哪一层：** **不是票写得不够细，是病因诊断错了**，而整张票的做法（加一个批量
  取判据的入口）正是照那个诊断开的方子。方子本身没白开——往返少了三个数量级，队列那一路
  跟着受益，而且「判据整批取」是「排上任务台」的前提。但**真正的大头一个字都没动**：
  剩下那 3.75 秒是 `identify::collect` 的纯 CPU，拆法与两条出路见 `Q277`。
- **为什么没停线：** 票要的两件事都成立——批量入口有了（**折判据那一路**的往返
  由约 23–28 万次变成 558 次，见下），这一下也离开了画帧线程（6.7 s → 约 1.05 s）。
  错的是「大头是查询」这个判断，而它不改变这张票该做什么。
- **那 558 次是怎么数的，以及它不含什么：** 一段 500 个键（`KEYS_PER_QUERY`），
  46,483 个变体是 **93 段**，一段最多 6 条查询（变体行、成员、容器构成、容器状态、
  条目、算过的哈希）＝ **558 条**。改之前是每个变体 5–6 次（成员是容器还是裸文件差一次），
  也就是约 23–28 万次。
  **`collection::landed` 那一条不在这 558 里**：内容锚每条还要一次
  `variants_with_content`，真库上判据基本都拿得到，那就是约 46,483 次——**本票没批量化
  它**，记在 `Q279`。合成数据上锚基本都是路径锚，那一支由 `known` 短路成零次，
  所以上面那张表量不到它。
- **这张票实际做了什么：** 照票做完两半，把实测数原样写进票的验收与这条挂单；
  **没有去追那 2.2 秒**（那要给 `collect` 开一条岔路，见 `Q277`）。
- ⚠️ **这几个数的口径，两条得连着读——不然会被拿去开一张不该开的票：**

  1. **全是 `-O0` 上量的**（dev profile；`libsqlite3-sys` 那份 C 也跟着 `-O0`）。
     debug 构建把**纯 CPU** 那一半放大得最厉害，而 SQL 往返里有一截是过不了优化器的
     固定开销——所以 release 上「查询 : CPU」的配比多半与这里完全不同，**很可能倒过来**。
     **这一票没量 release**（那要一趟全新的 release 构建）。
  2. **这份合成数据不是真库的形状**：它的变体**一条 `content_hash` 都没有**、容器构成
     也是空的，所以旧路上那几次查询次次落空、便宜得不正常，而每个变体都要付一句
     「穿不透」的 `format!`。真库上 `container_entry` 有 1,016,857 行、中立库在盘上
     而不是内存里——批量那一条省下的应当比这里多，也就是说
     **「大头不是查询」这个结论本身只在这份合成数据上量得住**。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— **这一轮「票面错误」里最典型的一条**：票说那 6.7 秒的大头是「一个变体问四次库」，实测批掉那些查询只省下不到 2 秒，其余 3.75 秒是 `identify::collect` 的纯 CPU。**不是票写得不够细，是病因诊断错了**，而整张票的做法正是照那个诊断开的方子——方子没白开（往返由约 23–28 万次变成 558 次，队列那一路跟着受益），但**真正的大头一个字都没动**。两条口径写死在条目里，不连着读就会被拿去开一张不该开的票：全是 `-O0` 上量的；这份合成数据不是真库的形状。

### Q275 — 内存库上整批收藏走哪条路：**退回就地跑完**

- **来自：** 票 `parking-3/09`
- **类别：** 规格没说（票把这条明写成「由实现者挑一条，在挂单里写明走了哪条」）
- **在哪：** `crates/gui/src/browse.rs::Screen::queue_collection` 那三支 `match`
- **走的是哪条：** **退回就地跑完**（`Board::run_here`），不是拒绝。
- **为什么是这条：** 同一屏、同一个条件（`CatalogError::NotOnDisk`）上，
  子库屏那两处（`Screen::preview`、`Screen::evaluate`）早就是这么走的。
  在第三处上换一种处置，等于让「分不出第二份连接」这件事在界面上有两个意思。
  拒绝还会把 `--demo` 那个演示窗口上的收藏整个关掉，而那一屏正是票
  `gui-redesign/06` 要给人看的东西。
- **为什么没停线：** 票把这条明写成「由实现者挑一条」，两条路都站得住，而翻案的代价
  只是改一支 `match`——留在这张票之内。
- **代价说清楚：** 合成数据上这一下仍然整段跑在画帧那条线程上（46,483 行约 4.8 秒）。
  **真库永远落在盘上**，走的是另一支。**别的原因分不出第二份连接就直说**，
  不偷偷退回画帧线程——那既僵住窗口又把真正的问题盖住了（与子库屏同一条）。
- **这张票实际做了什么：** 三支照抄子库屏那套，第三支那句话里点名说了是哪一下
  （加收藏 / 取消收藏）。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 内存库上整批收藏走**退回就地跑完**（`Board::run_here`），与子库屏那两处同一条。代价说清：合成数据上这一下仍然整段跑在画帧线程上（约 4.8 秒），而真库永远落在盘上、走的是另一支。

### Q276 — 写那一半仍在画帧那条线程上：46,483 行约 1.05 秒

- **来自：** 票 `parking-3/09`
- **类别：** 这一票新引入的代价
- **在哪：** `crates/core/src/collection.rs::commit`，由
  `crates/gui/src/browse.rs::Screen::settle_collection` 在**认领**那一步调
- **为什么没搬走：** **中立库全程只有一个写者**（`Catalog::read_only` 的第一条）。
  台上那条线拿的是只读连接，写不动；沉淀库那一半同理。这正是票
  `gui-redesign/15` 给同步那一趟定下的形状（`Product::Synced`：清单当作产物交回来、
  由认领它的那一屏落库），本票原样照抄。
- **为什么没停线：** 6.7 秒变成约 1.05 秒，而票要的是「窗口不冻住」的那条轴——
  剩下的这一秒里两份库各只有一个事务，中途停下留下的正是「屏上亮着而沉淀库里没有」
  那种半截状态，所以它本来就不该可停。
- **这张票实际做了什么：** 把可停的那一半（只读的排锚）搬上任务台，写那一半留在认领那步，
  并在 `commit` 的文档里写死「它不收把手，也停不下来」。
- **谁来裁：** 收尾（哪天这一秒也嫌长了，再问「一份可写的连接能不能交给台上那条线」）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 写那一半留在认领那步、不搬走——**中立库全程只有一个写者**，台上那条线拿的是只读连接。这正是票 `gui-redesign/15` 给同步定下的形状，本票原样照抄，并在 `commit` 的文档里写死「它不收把手，也停不下来」。

### Q277 — 排锚那 3.75 秒的大头是 `collect` 的纯 CPU，不是查询

- **来自：** 票 `parking-3/09`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/identify.rs::collect` —— 它为每个变体折出 unit，
  拿不到判据的那些还要折一句**为什么**（`blocked_unit` 那几处 `format!`）
- **为什么没收：** `content_prints` 只要「谁代表这个变体」那一份判据，那几句理由它一句
  都不看。可要省掉它们，就得给 `collect` 开一条「只要判据、不要理由」的岔路——而这条
  路上的东西正是本票明令不许写第二遍的那一层。理由那几句是**识别报告的正文**
  （命中率旁边那栏「为什么没判据」），不是可有可无的装饰。
- **那 3.75 秒具体花在哪**（临时插的计时，量完就撤了；46,483 个变体）：

  | 这一段 | 耗时 | 里头是什么 |
  |---|---|---|
  | 取变体行 | 0.34 s | 93 条 `variants_of`，加 46,483 个 `VariantRow` 的构造 |
  | **取判据** | **2.2 s** | SQL 往返只剩四百多条；其余全是 `collect` 逐个变体折 unit |
  | 折锚 | 1.28 s | 46,483 个 `Anchor` 的构造 + `touched` 那个 `BTreeSet<String>` |

  取判据那 2.2 秒里，**一个变体要干的活**是：一次 `ContainerKind::for_path`、一次
  `classify::classify`、一句 `format!`（「`.zip` 穿不透，而这一趟没为它付全量解压的
  代价」）、外加 `blocked_unit` 里三四个 `String`。乘以四万六千次。
- **为什么没停线：** 它已经不在画帧那条线程上了（`Q274` 那张表），慢的是后台；
  而票要的那条轴（「窗口不冻住」）已经成立。
- **这张票实际做了什么：** 量出来、拆开、记下来，**没动 `collect` 一个字**。
- **要收的话，两条路：**
  - **A** 让 `blocked_unit` 收一个**惰性**的理由（闭包或 `fmt::Arguments`）：
    `collect` 的签名要变，识别那一侧全都跟着变。
  - **B** 给 `Bulk` 加一档「**这一趟不要理由**」的开关，`collect` 里那几处 `format!`
    照它跳过。**B 不分岔**——同一条路、同一句「谁代表这个变体」，只是少干点活；
    本票明令不许写第二遍的是**那句说法**，不是那几句给人看的理由。**先试 B。**
- ⚠️ **开票之前必须先做的两件事**（口径同 `Q274`）：
  1. **补一个 release 的数。** 上面那张表是 `-O0` 量的，debug 把纯 CPU 放大得最厉害
     ——release 上「查询 : CPU」的配比很可能倒过来，那时这条挂单整个不成立。
  2. **在真库上量一遍。** 这份合成数据一条 `content_hash` 都没有、容器构成是空的，
     于是**每个**变体都走「穿不透」那一支、每个都付一次 `format!`。真库上容器构成有
     1,016,857 行，绝大多数变体走的是**另一支**（有内部文件、不折那句理由），
     那 2.2 秒的成分会完全不同。
- **谁来裁：** 票 `parking-3/18`（收口）—— **它要据此判断该不该开一张新票**。
  判据就是上面那两件事：两个数都补齐，而且 release 上、真库上大头**仍然**在 CPU，
  才值得开；只要有一样翻过来，这条就该按 `noted` 归档。
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— **收口当场裁：不开票，按 `noted` 归档。** 条目自己写死了判据：「两个数都补齐，而且 release 上、真库上大头**仍然**在 CPU，才值得开；只要有一样翻过来，这条就该按 `noted` 归档」。收口这一天两件前置**一件都没补齐**——真库不在开发机上（同 `Q304`），release 那个数没量。**没有实测支撑就不开票**，这正是这份挂单立的规矩，也是 `Q274` 那条口径提醒过的（debug 把纯 CPU 放大得最厉害，release 上配比很可能倒过来）。哪天真库上量过一趟，从这条挂单接着走——出路 B（给 `Bulk` 加一档「这一趟不要理由」）已经写好，条目里连「先试 B」都定了。

### Q278 — 一条 `IN` 一次问 500 个键，这个数是量级判断不是量出来的

- **来自：** 票 `parking-3/09`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/catalog.rs::KEYS_PER_QUERY`
- **为什么是 500：** 给 SQLite 的绑定参数上限留余量。这个仓库用的 `bundled` 是新版
  （上限 32,766），但那条上限历史上是 999。500 段与 32,000 段在这几张表上的往返次数
  差不到一个量级（46,483 个键：93 段 vs 2 段），而实测里往返已经不是大头（`Q274`）。
- **为什么没停线：** 两头都不难判——一个键那一档（`content_print` 那个特例）折出来的是
  `… IN (?1)`，与从前那句 `… = ?1` 走同一个索引、同样进 `prepare_cached`；
  四万个键那一档一段一条。中间那一段两条路都不难看。
- **这张票实际做了什么：** 定死一个数、把理由写进文档，**没有去量最优值**。
  它同时管着另外两样：「按下停下到真的停了」的粒度（一段一次 `task.check()`），
  以及 `identify::content_prints` 一次在内存里驻留多少行（一段开一份 `Bulk`、一段丢一份
  ——那是 ADR-0005 那条「绝不把全库载入内存」）。
- **同名的第二份：** `catalog::scrape` 里另有一个模块私有的 `KEYS_PER_QUERY`（900），
  连占位符也自己拼了一套。见 `Q283`。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— `KEYS_PER_QUERY = 500` 是量级判断不是量出来的，理由（给 SQLite 绑定参数上限留余量；往返已不是大头）写进文档。它同时管着按停粒度与「一次在内存里驻留多少行」（ADR-0005）。同名的第二份记在 `Q283`。

### Q279 — 还有两处仍在一条一条问判据，这一票没跟着改

- **来自：** 票 `parking-3/09`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/scrape/zh.rs::Rulings::resolve`（对每条内容锚上的变体各问
  一次 `content_print` 复核）；`crates/core/src/collection.rs::landed` 的
  `Anchor::Content` 那一支（每条内容锚一次 `variants_with_content`）
- **为什么没停线：** 两处的量级都不是变体数。`resolve` 走的是**人一条条裁出来的**那些
  裁决（几百到几千）；`landed` 那一支在真库上多数时候只回来一个键（就是它自己），
  而那一个由 `known` 免掉了复核——那正是票 `gui-redesign/06` 花 1.5 秒买回来的正确性。
- **这张票实际做了什么：** `landed` 的**路径锚**那一支认了 `known`（排一趟刚为它取过
  那一行，不必再问一次库），内容锚那一支一个字没动。`resolve` 一个字没动。
- **谁来裁：** 收尾（真库上量过 `landed` 那一支之后再说；批量化它要 `(crc32, size)` 的
  行值 `IN`，SQLite 3.15 起支持，两张表上都有现成的复合索引）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 两处仍在一条一条问判据，但**两处的量级都不是变体数**——`resolve` 走的是人一条条裁出来的几百到几千条，`landed` 那一支真库上多数只回来一个键、而那一个由 `known` 免掉了复核。今天不咬人；与 `Q277` 同一条口径：要动先在真库上量。批量化它的路子（`(crc32, size)` 的行值 `IN`）写在条目里。

### Q280 — 界面测试要一份**落在磁盘上**的库，于是这一票自己搭了一份

- **来自：** 票 `parking-3/09`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/tests/browse.rs::磁盘上的小库`
- **为什么非它不可：** 「跑着的时候屏上有进度、有『停下』」这条只在**真的排上台**那条路
  上成立，而合成数据只活在内存里、分不出第二份只读连接，走的是就地跑完那一支
  （`Q275`）——那一支跑完时台上已经没有「正在跑的那一趟」了，屏上什么都断不着。
- **为什么没停线：** 二十行的一份夹具，与同一份文件里那个 `三档并排的库` 同一个写法。
- **这张票实际做了什么：** 在 `crates/gui/tests/browse.rs` 里**只添不改**地加了这个夹具
  （票 `parking-3/16` 正在并那两份界面测试的夹具，不去动它已有的签名）。
- **要提醒的：** 票 `16` 把两份夹具并成一份时，这一个也该一起看——它与 `三档并排的库`
  差的只是「开在盘上还是开在内存里」。
- **谁来裁：** 票 `parking-3/16`
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 票 `16` 并夹具时**漏了第三份**：`crates/gui/tests/browse.rs::磁盘上的小库`——它与同一份文件里的 `三档并排的库` 差的只是「开在盘上还是开在内存里」。本票加它时是「只添不改」，明确提醒过票 `16` 一起看，而 `Q344` 并的是另外两份。
  **去处：** 第四轮规格材料，「合成数据与夹具」那一组。

### Q281 — 一屏一次只排一趟收藏：第二次按星当场说一句，不排队

- **来自：** 票 `parking-3/09`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/browse.rs::Screen::queue_collection` 开头那道
  `if self.collecting.is_some()`
- **走的是哪条：** **挡住并说一句**（「上一趟还在跑（收藏与合集一次只排一趟）。
  任务屏上看得见它走到哪儿了，也按得停。」），不排第二趟。
- **那句话里为什么不带动词：** 头一版写的是「上一趟{这一次按的那个动词}还在跑」——
  先按 ★ 排上一趟、再按 ☆，屏上会说「上一趟**取消收藏**还在跑」，而上一趟明明是加收藏。
  审查抓出来，改成不带动词。
- **为什么是这条：** 排锚那一趟算的是**排它那一刻**库里的样子。两趟一起排，后一趟算的
  是前一趟落库**之前**那份库，落下去就会把前一趟的账盖掉一部分——而这两趟人看着是
  「我按了两下星」。子库屏那两处（`previewing` / `evaluating`）也是这么挡的。
- **为什么没停线：** 挡住的那一下有话说，不是静静地什么都不做。
- **这张票实际做了什么：** 一道判断加一句话，没有做「排队等前一趟」那种更复杂的处置。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 一屏一次只排一趟收藏，第二次按星当场说一句、不排队。理由：排锚那一趟算的是排它那一刻库里的样子，两趟一起排后一趟会把前一趟的账盖掉一部分。那句话里不带动词是审查抓出来的（先 ★ 再 ☆ 会说「上一趟取消收藏还在跑」）。

### Q282 — 取消收藏与自建合集那两下也一起搬上了任务台，票只点名了收藏

- **来自：** 票 `parking-3/09`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/browse.rs` 的 `favorite` / `unfavorite` /
  `join_collection` / `leave_collection` 四个入口，四个都收 `&mut Tasks`
- **为什么：** 四下走的是**同一个** `collection::apply`（现在是
  `plan` + `commit`），代价一模一样——「全选 46,483 行 → ☆ 取消收藏」与加收藏一样贵。
  只搬一下的话，同一块面板上两颗按钮一颗会冻窗口、一颗不会，而它们看着是一对。
- **为什么没停线：** 是同一处改动的自然边界，不是另一件事。
- **这张票实际做了什么：** 四个入口的签名从 `(&mut Site)` 变成 `(&Site, &mut Tasks)`，
  调用处跟着改：`crates/gui/tests/browse.rs` 里**六处**（1835 / 1868 / 1900 / 1912 /
  1937 / 2007）加 `crates/gui/src/bench.rs` 里一处，全部只是把 `browse_and_site()`
  换成**早就存在的** `browse_site_and_tasks()`。**既有夹具的签名一个都没动**
  ——票 `parking-3/16` 那条边界守住了。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 四个入口一起搬上任务台是同一处改动的自然边界——四下走的是同一个 `collection::apply`，只搬一下的话同一块面板上两颗看着成对的按钮一颗会冻窗口、一颗不会。既有夹具的签名一个都没动，票 `16` 那条边界守住了。

### Q283 — `KEYS_PER_QUERY` 与占位符在同一个 crate 里有两份

- **来自：** 票 `parking-3/09`
- **类别：** 两份东西矛盾
- **在哪：** 本票新加的 `crates/core/src/catalog.rs::KEYS_PER_QUERY`（500）与
  `placeholders`；以及**早就在那儿**的 `crates/core/src/catalog/scrape.rs` 里那个模块私有的
  `KEYS_PER_QUERY`（900），它 `:1102` 一带自己拼了一套占位符
  （`repeat_n("?", chunk.len()).join(",")`）
- **为什么没停线：** 两份都对，只是各拼各的。合成一处要动刮削那一族的下推查询
  （900 与 500 谁让谁是个要量一遍才答得了的问题），而那不是本票的接缝——本票动的是
  判据那一层。
- **这张票实际做了什么：** **没有合并**。头一版的文档写了一句「按键取一批行的路子只有
  这一条」，审查指出那是假话，改成如实点名第二份并指到这条挂单。
- **收的话怎么收：** 把 `placeholders` 提成 `catalog` 那一层公用的（它已经在那儿了），
  `scrape.rs` 改用它；两个常量留不留两份，看量出来的结果——**先合并占位符那一半**，
  它一个数都不改。
- **谁来裁：** 收尾 / 下一张动 `catalog::scrape` 的票

- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— `KEYS_PER_QUERY` 与占位符在同一个 crate 里有两份（本票新加的 500，与 `catalog::scrape` 里早就在那儿的 900、还自己拼了一套占位符）。头一版文档写「按键取一批行的路子只有这一条」，审查指出那是假话、已改成如实点名。条目自己给了第一步：**先合并占位符那一半，它一个数都不改**。
  **去处：** 第四轮规格材料，「零散（各自独立，不成组）」那一组。
