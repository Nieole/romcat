# 05 — 按停之后到处都说「停了」

**What to build:** 维护者在界面上按一下「停下」，那一趟显示成**停了**，不是失败——
不会以为自己按坏了什么。中文离线源那 435 MB 下载按 Ctrl-C 之后**当场**收手，
不用等它下完。（挂单 `Q151` `Q150`）

眼下任务台分「停了」与「失败」的判据是**那句话正好是某一句**，界面刮削那几处交出去的
话与核心库定的差着字，于是按停被记成失败。

**Blocked by:** 03

**Status:** done

- [x] 分「停了」与「失败」的判据**由类型说话**，不再靠字符串比对。改完之后界面任何一处措辞改动都不可能把「停了」悄悄变成「失败」。 ——
  新立 `task::Cutoff { Halted, Failed(String) }` 当**一趟活交不出产物时的唯一出口**，
  `Job<T>` / `Board::queue` / `Board::run_here` 的错误类型从 `String` 换成它，
  `Board::settle` 改成 `Err(Cutoff::Halted) => Ending::Stopped` /
  `Err(Cutoff::Failed(why)) => Ending::Failed`——**那一段里一个字符串比对都没有了**，
  连 `handle.stopped()` 都不看了。
  **`Cutoff::Halted` 是个不带字段的支**：措辞根本不到任务台手上，改哪一处都动不了分档。
  同时**删掉 `impl From<Halted> for String`**——「把被按停洗成一句话」在类型上没有顺手的
  写法了，编译器会逼所有收把手的长入口换成 `Cutoff`（`sync::prepare`、
  `sublibrary::survey`、`sources::refetch`、界面那四条闭包）。
  测试：`crates/core/tests/task.rs::措辞与核心库那一句差着字的按停照旧记成停了`
  ——它交上来的是 `CollectionError::Halted`，那句话**与 `Halted` 那一句差着字**
  （测试自己 `assert_ne!` 钉着这一点），照旧记成 `Ending::Stopped`。
  **红过一次**：把 `From<CollectionError> for Cutoff` 临时改成「一律洗成
  `Failed(error.to_string())`」（也就是老那条路），这一条与下面第 3 条当场红，
  报的是「把按停记成了「失败：整批排锚按停了：…」」。
  裁决记在 `Q234` `Q235` `Q236`；仍留着的那条口子（有人特意先 `.to_string()`）记在 `Q237`。
  **收尾审查还揪出低一层的同一个形状**：`zh::sync::SyncError` 的
  `Fetch(#[from] FetchError)` 会把 `FetchError` 新长出来的 `Halted` 支一并吞进
  「取数失败」——随手 `?` 一下就把「按停了」洗成失败。已改：那一支去掉 `#[from]`，
  换成手写的 `From<FetchError> for SyncError` 把 `Halted` 路由到 `SyncError::Halted`
  （下载那一处原先手写的 `match` 跟着收掉了）。`dat` 与 `titledb` 那两个同形状的
  `SyncError` 眼下产不出那一支（它们走 `download`，`keep_going` 恒真），
  按票面的范围原样没动，记在 `Q242`。
  **还有第三环，收尾时补掉的**：头一版给 `Cutoff` 实现了 `Display`（`Halted` 那一支
  转发给 `Halted` 自己那句中文）——判据链上任何一处 `to_string()` 都能把「停了」
  重新折成一句可比的字符串，**而且这一次是刚建的那个类型自己递上去的**。
  已改成 `Cutoff` **不实现 `Display`、也不是 `std::error::Error`**：想要那句话得先
  `match` 一下（命令行那一处摊开写了），于是「收到的是哪一档」在源码上永远看得见。
  `Halted` 自己照旧有 `Display`（它是个 `Error`，领域错误枚举拿它当 `source`），
  只是那句话再也到不了任务台手上。
  三环串起来的那句话记在 `Q243`：**`Display` 与 `#[from]` 都是「转换」，
  而「停了」这个信息必须在每一次转换上都过不去**——只把 `String` 换成 `enum`
  只挡住了第一次转换。
- [x] 界面刮削那几处不再手写「按停了」，改由类型转换得来。 ——
  `crates/gui/src/scrape.rs` 那四处 `task.step("…").map_err(|_| "按停了".to_string())?`
  全删了，现在就是 `task.step("…")?`（`Halted` 经 `From` 折成 `Cutoff::Halted`）。
  `grep -n '"按停了".to_string()' crates/gui/src/` 一处都没有。
  **顺手收了 `Q217` 点名留给本票的第二个出口**：刮削走到 `scrape::run` 里被按停
  （或者联网源自己收的手：配额、凭据、断网）时照旧返回 `Ok`，从前长着「跑完了」的样子
  进任务台；现在那一支报一句 `Handle::halfway`，记成 `Ending::Halfway`，
  `finished()` 那句回执也不再说「刮削跑完了」。
  测试：`crates/gui/src/scrape.rs` 的
  `被按停的那一趟说得出留下了什么_而且不说跑完了` 与 `真跑完的那一趟不报停在半路`。
  识别那一层同一个形状，本票没碰，记在 `Q238`。
  **收尾审查在这一处抓到两句被改错的话，都已改**：一是 `finished()` 起头写「刮削按停了」
  ——那一趟记的是「停在半路」，而 `Ending::Stopped.render()` 正好就是「按停了」，
  两档撞脸（词表「收场」：四档四句）；改成起头一律「刮削停在半路」，
  「是被你按停的」还是「是它自己收的手」放到后半句。二是 `Panel::settle` 的
  `Ending::Stopped` 那支还写着「已经采到的那些留在中立库里」——这张票之后落到那一档的
  **只剩「还没走到 `scrape::run` 就被 `?` 出来」那个出口**，它一个锚点都没采，
  那句话成了骗人的；改成「这一趟还没开始采——中立库与媒体池一个字节都没动」。
- [x] 有一条界面测试断：按下停下之后，任务屏历史那一行写的是「停了」那一类，**不是**「失败」。这条测试要能在判据改回字符串比对时当场红。 ——
  `crates/gui/tests/task.rs::按下停下之后任务屏历史写的是停了那一类_不是失败`：
  往任务台上排一趟「放进「收藏」· 200 个变体」，`app.tasks_mut().stop(id)`
  （界面上那颗「停下」按的就是它），断 `history()[0].ending == Ending::Stopped`，
  再断屏上那一行正好是「按停了」、**没有任何一行带「失败」**。
  它交上来的是 `CollectionError::Halted`，测试开头先 `assert_ne!` 钉住「这句话与
  `Halted` 那一句差着字」——**判据回到字符串比对它就当场红**，上面那次实测报的是
  「按了停下，历史那一行却写成了「在「为 200 个变体折锚」这一步失败：…」」。
- [x] 中文离线源那 435 MB 下载接上中断信号：按下 Ctrl-C 之后在**当前这一块**读完就收手，不等整份下完。 ——
  `Fetcher` 上加了一条带默认实现的 `download_while(url, to, keep_going)`；
  `HttpFetcher` 覆盖它，走新的 `copy_while`：**一块 1 MiB（`DOWNLOAD_CHUNK`），
  每块之前问一次要不要收手**，收手时把那个 `.partial` 删掉（半截留着的话下一趟会
  以为原件已经在手边）。`zh::sync::sync` 把 `Context::cancel` 折成那个问法递下去，
  收手折成 `SyncError::Halted`（不是「取数失败」）。命令行的 Ctrl-C 与界面那颗「停下」
  接的是同一个信号（`run_zh_sync` 传的就是 Ctrl-C 那个 `CancelToken`），两边一并有了。
  **多久收手**：判据是**搬进去了多少字节**——
  `dat::fetch::那份_435_兆的原件按停之后在当前这一块读完就收手_不等整份下完`
  在一份 435,891,841 字节的合成正文上放行三块就收手，搬进去的**正好 3 MiB**
  （`assert_eq!(落点.搬进去了, 3 * DOWNLOAD_CHUNK)`），实测 **0.5–0.9 ms**，
  而同一份整份搬完是 **16.8–18.7 ms**（合成正文，内存速度）。
  收手的**上界是读完当前这一块**——1 MiB；换算到一条 20 MB/s 的线路上约 **50 ms**，
  而从前要等的是剩下那 430 MB，约 **21 秒**。
  接线那一头由 `zh::sync::那_435_兆的下载按停之后当场收手_不等它下完` 钉：
  **两头都跑**——按了停下的那一趟**一块都没搬**、缓存里不留半截、错误是
  `SyncError::Halted`；没按的那一趟 416 块一块不少地下完。
  实测把递下去的那个问法换成恒真，这一条当场红（「按了停下却还是搬了几块」）。
  一块 1 MiB 是**选的不是量的**，记在 `Q240`。
  收尾审查另补了一条：`copy_while` 换掉的 `io::copy` 本来会吞并重试
  `ErrorKind::Interrupted`，新循环补上了同一档——不补的话一个信号就会让按停显示成
  「读写 … 失败：Interrupted」，正是这条路要消灭的那种误报。
- [x] 取数那一侧的文档改准——现在写着「接得住的只是读那份原件那几分钟，下载那 435 MB 照样停不掉」，这张票之后那句不再成立。 ——
  `crates/core/src/sources.rs::refetch` 的文档改成「**中文离线源那一条整条接住了**
  ——下载那 435 MB 与随后读那份原件的几分钟都收中断信号，按下停下在**当前这一块**
  读完就收手，不等整份下完；缓存里也不留半截。另外两条还接不住（`dat::sync::run` /
  `titledb::sync::sync`），剩下那两段记在挂单 `Q62`」。
  跟着改准的还有三处：`crates/core/src/zh/sync.rs` 的模块文档（多了一段说下载也按同一条
  接住）、`crates/gui/src/roots.rs` 那颗「重取」按钮的悬停（从前一句「开跑之后停不下来
  ——底下那三个入口不收中断信号」现在分开说三条）、`crates/cli/src/main.rs` 那句中断
  回执（从前只说「停在两条记录之间」，那对下载那个出口不成立，改成「停下的地方是干净的：
  库里一个字都没写，缓存里也没留下半截原件」）。
  另外拆掉了一处**靠巧合成立的断言**：`zh::sync` 那条按停测试从前还断
  `error.to_string() == Halted.to_string()`，那是老判据的残留，现在断的是
  `matches!(error, SyncError::Halted(_))`。
- [x] 挂单 `Q151` `Q150` 标 `settled` 并迁回票 `queue-followups/03`。 ——
  两条原文迁进 `.scratch/queue-followups/issues/03-zh-rebuild-path.md` 末尾新开的
  「## 挂单裁决（第三轮）」一节，各带一条「收尾裁决（第三轮）：**settled**」；
  原验收正文一个字没动。`.scratch/PARKING-LOT.md` 的「已收索引」表里两行也标上了
  **settled** 与去处。**`Q151` 没走挂单里写的那个改法**（「那四处改成
  `.map_err(String::from)`」）——那条只把这一处对上，判据还是字符串比对；
  走的是换类型那条，理由写在迁回去的裁决里。

## 门禁

`cargo xtask gate --keep-going -j 6`，**四条全绿**：

| 条 | 结果 | 用时 | 命令 |
|---|---|---|---|
| fmt | 绿 | 1s | `cargo fmt --all --check` |
| clippy | 绿 | 10s | `cargo clippy --workspace --all-targets --all-features -j 6` |
| test | 绿 | 455s | `cargo test --workspace --all-features -j 6` |
| doc | 绿 | 8s | `cargo doc --workspace --no-deps -j 6` |

**66 个目标、1,702 条测试**（票 `13` 那趟是 1,681 / 66，中间 `09` 合了进来；
数是 `cargo test … -- --list` 数的，没有再跑一趟）。`cargo doc` 那 65 条既有告警
**回到基线**：core 56 / gui 8 / cli 1——cli 那一条（`sync::prepare` 既是函数又是模块，
`crates/cli/src/main.rs:4401`）在 `05e6ace` 上本来就有，核过。清它是票 `02` 的活。
挂钟彩票 `任务六秒都没跑完`（`Q349`）这一趟没撞上。

## 票面写错了什么

原话：

> 眼下任务台分「停了」与「失败」的判据是那句话正好是某一句，界面刮削那几处交出去的
> 话与核心库定的**差着字**，于是按停被**记成失败**。

**两半都不成立。** 实测：`crates/gui/src/scrape.rs` 那四处交出去的是
`"按停了".to_string()`，而 `Halted.to_string()` 是「按停下了：停在两步之间，
没留下半截状态。」——**它们确实差着字**，所以「差着字」这半句对；不对的是后半句的
适用范围。那四处只在**走不到 `scrape::run`** 的那一小段上生效（`run` 里那四步之间），
真正常见的按停走的是另一个出口：`scrape::run` 里 `if context.cancel.is_cancelled()
{ interrupted = true; break; }` 然后 `return Ok(报告)`——**任务台连比对的机会都没有**，
记成的是「**完成**」，不是「失败」（那正是挂单 `Q217` 记的那个出口）。

**病象说错了，病因说小了，而做法碰巧还是对的**：判据确实该由类型说话，而且不这么改的话
第二个出口也收不干净（`Ending::Halfway` 那一档要长入口自己报，靠字符串比对够不着）。
两个出口这一票都收了。

## 范围

- **只收中文离线源这一条下载。** 三条取数入口都停不下来（挂单 `Q62`）是同一件事的全部，另两条这张票不碰——如实记在挂单里，别顺手扩。

## 挂单裁决（第三轮收口，2026-09-08）

本票在实现路上记下的 **10 条**（`Q234`–`Q243`）由票 `parking-3/18` 收口裁完，条目全文迁自 `.scratch/PARKING-LOT.md`（那边的条目区已清空）。**本票的原验收正文一个字没动。**

分三档：**A** 已经做完只是没销 ／ **B** 不成立了 ／ **C** 仍然成立要开票。判据是**一条挂单有了主人就算收了，不是等它做完**。本票：**A 7 ／ B 0 ／ C 3**。

| 挂单 | 档 | 一句话 |
|---|---|---|
| `Q234` | **A** | 分「停了」与「失败」的判据搬进类型，`Board::queue` / `run_here` 的错误类型跟着换了 |
| `Q235` | **A** | 删掉了 `From<Halted> for String` |
| `Q236` | **A** | `Cutoff::Halted` 一个字都不带 |
| `Q237` | **A** | `From<String> for Cutoff` 仍然是一条能把「停了」洗成「失败」的路 |
| `Q238` | **C**<br>第四轮「「停了」那条链剩下的段」 | 刮削那第二个出口改报「停在半路」了；识别那一层同一个形状，这张票没碰 |
| `Q239` | **C**<br>第四轮「「停了」那条链剩下的段」 | `Fetcher::download_while` 的默认实现只在开工之前问一次；DAT 与 Switch 两条下载仍然停不掉 |
| `Q240` | **A** | 那 435 MB 的下载只接了中断，没接进度；一块 1 MiB 是选的，不是量出来的 |
| `Q241` | **A** | `scrape::Outcome` 加了 `Default`，只为界面那一侧的两条测试 |
| `Q242` | **C**<br>第四轮「「停了」那条链剩下的段」 | `dat::sync` 与 `titledb::sync` 的 `#[from] FetchError` 会把新长出来的 `Halted` 支吞成「取数失败」 |
| `Q243` | **A** | 「由类型说话」不是把 `String` 换成 `enum` 就完了：同一个形状从 `Display` 与 `#[from]` 两处又长了回来 |

### Q234 — 分「停了」与「失败」的判据搬进类型，`Board::queue` / `run_here` 的错误类型跟着换了

- **来自：** 票 `parking-3/05`
- **类别：** 票写错了
- **在哪：** `crates/core/src/task.rs`（新的 `Cutoff`、`Job<T>`、`Board::queue`、
  `Board::run_here`、`Board::settle`）
- **为什么没停线：** 票面点名要的就是这件事，而且要求「界面任何一处措辞改动都不可能把
  「停了」悄悄变成「失败」」——留在 `String` 那条通道上做不到这一条。
- **这张票实际做了什么：** 立了 `Cutoff { Halted, Failed(String) }` 当**一趟活交不出
  产物时的唯一出口**，`Job<T>` 的错误类型从 `String` 换成它，`settle` 改成按支分档。
  另一条路是留着 `String` 通道、把判据放宽（比如「按过停下就算停了」）——那条站得住，
  代价也只在这一处，但它会把「按下停下之后才真出错」那条错误文本整条丢掉
  （`crates/core/src/task.rs` 里那条测试钉着的正是它）。**选了换类型**：判据不再有
  任何字符串可比，措辞是什么都动不了它。
  ⚠️ **与票 `parking-3/04` 的撞车面**：这是**改了既有签名**，不是往里加。票 `04` 要动
  任务屏与进度那一层——`Progress` / `Live` / `Record` 一个字段都没动，`Board` 上动的
  只有 `queue` / `run_here` 那个闭包的错误类型与 `settle` 的判据。两边同一处的话，
  以这一条为准。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 立了 `Cutoff { Halted, Failed(String) }` 当一趟活交不出产物时的**唯一出口**，`Job<T>` 的错误类型换成它，`settle` 按支分档。选换类型而不是放宽字符串判据的理由：判据不再有任何字符串可比，措辞是什么都动不了它。

### Q235 — 删掉了 `From<Halted> for String`

- **来自：** 票 `parking-3/05`
- **类别：** 票写错了
- **在哪：** `crates/core/src/task.rs`（原先 `impl From<Halted> for String`）
- **为什么没停线：** 留着它，`Cutoff` 就只是一层新皮：任何一个还返回
  `Result<_, String>` 的长入口照旧能 `?` 一下把手，把「被按停了」洗成一句话——
  而洗完之后没有任何东西能把它认回来。
- **这张票实际做了什么：** **删掉它**，于是所有收把手、又停得下来的长入口被编译器逼着
  把错误类型换成 `Cutoff`（`sync::prepare`、`sublibrary::survey`、`sources::refetch`，
  以及界面那四条闭包）。另一条路是留着它、只在新写的地方用 `Cutoff`——那条改动小得多，
  代价也在本票之内，但「不可能」就退化成「记得别那么写」。**选了删**：票面那句
  「不可能」只有靠编译器才成立。
  命令行那一侧只受一处影响（`crates/cli/src/main.rs` 的 `prepare` 加了一句
  `.map_err(|why| why.to_string())`）——它没人按停下。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 删掉 `From<Halted> for String`，于是所有收把手又停得下来的长入口被**编译器**逼着换错误类型。留着它的话「不可能」就退化成「记得别那么写」，而票面那句硬验收要的正是前者。

### Q236 — `Cutoff::Halted` 一个字都不带

- **来自：** 票 `parking-3/05`
- **类别：** 规格没说
- **在哪：** `crates/core/src/task.rs` 的 `Cutoff::Halted`
- **为什么没停线：** 「停了」那一档在界面上要说的那句话由 `Ending::render` 出，
  长入口从来没有需要自己说的半句。
- **这张票实际做了什么：** 让它是个**不带字段的支**。另一条路是让它带一句给人看的话
  （`Halted(String)`），那样长入口能说「排锚那一趟按停了」之类——那条站得住，翻案只动
  一个变体与三处构造。**选了不带**：带了话，`settle` 那儿就又有可比的东西了，而这张票
  要拆的正是「有东西可比」。要说的话往 `CollectionError::Halted` 那种领域错误上写，
  它一路都在，只是**不参与分档**。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— `Cutoff::Halted` 做成不带字段的支——带了话 `settle` 那儿就又有可比的东西了，而这张票要拆的正是「有东西可比」。要说的话往领域错误上写，它不参与分档。

### Q237 — `From<String> for Cutoff` 仍然是一条能把「停了」洗成「失败」的路

- **来自：** 票 `parking-3/05`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/task.rs` 的 `impl From<String> for Cutoff`
- **为什么没停线：** 票面那句「**措辞**改动不可能把停了变成失败」已经成立——这条路要走
  上去得有人**写代码**先把 `Halted` 折成字符串再 `?` 上来，那不是改措辞。
- **这张票实际做了什么：** **留着它，并写进 `Cutoff` 的文档**（「真出错了：`Cutoff::failed`，
  或者把一句 `String` `?` 上来」）。留的理由是那几条长入口里几十处
  `.map_err(|error| format!("…"))?` 全靠它——去掉就得逐处改成 `Cutoff::failed`，
  纯噪音。另一条路是不给这个 `From`、只留 `Cutoff::failed`，那样「一句 `String` 进来
  一定是失败」这件事变成显式的；翻案代价是那几十处，全在本票之内。**选了留**：
  这条口子只在有人**特意**把 `Halted` 先 `.to_string()` 时才张开，而删掉
  `From<Halted> for String`（`Q235`）之后，那一下没有顺手的写法了。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 留着 `From<String> for Cutoff` 并写进文档。终局理由：这条口子只在有人**特意**把 `Halted` 先 `.to_string()` 时才张开，而 `Q235` 删掉 `From<Halted> for String` 之后，那一下没有顺手的写法了。

### Q238 — 刮削那第二个出口改报「停在半路」了；识别那一层同一个形状，这张票没碰

- **来自：** 票 `parking-3/05`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/src/scrape.rs` 的 `run`（`left_behind` 那一段）；
  没碰的是识别那一层（`Q217` 末句点名的「识别那一层同理」）
- **为什么没停线：** `Q217` 把这个出口留给了本票，本票收了；识别那一层不在票面上。
- **这张票实际做了什么：** 刮削走到 `scrape::run` 里被按停（或者联网源自己收的手：
  配额、凭据、断网）的那个出口，现在报一句 `Handle::halfway`，于是记成
  `Ending::Halfway` 而不是 `Ending::Done`；`finished` 那句回执跟着不再说「跑完了」。
  **识别那一层原样没动**——它不在票面的验收里，而且这张票已经在动任务台那一侧的判据，
  再伸一只手进去会把两件事搅在一起。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— 刮削那第二个出口收了（现在报 `halfway`），**识别那一层同一个形状原样没动**——`Ok` 加上没报 `halfway` 就是 `Ending::Done`，历史写「完成」。这是 `Q217` 末句点名的那一半，两张票都明说没碰。
  **去处：** 第四轮规格材料，「「停了」那条链剩下的段」那一组。

### Q239 — `Fetcher::download_while` 的默认实现只在开工之前问一次；DAT 与 Switch 两条下载仍然停不掉

- **来自：** 票 `parking-3/05`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/dat/fetch.rs` 的 `Fetcher::download_while` 默认实现；
  `crates/core/src/dat/sync.rs:603` 与 `crates/core/src/titledb/sync.rs:151` 仍走
  `download`
- **为什么没停线：** 票面的**范围**一节写死了「只收中文离线源这一条下载」，另两条如实
  记在挂单里、别顺手扩。
- **这张票实际做了什么：** 把 `download_while` 加成**带默认实现**的一条（默认只在开工
  之前问一次，之后一口气下完），只有 `HttpFetcher` 覆盖它、只有 `zh::sync::sync` 用它。
  另一条路是改 `download` 的签名、逼所有实现与调用点都接上信号——那条更彻底，翻案代价
  也只有五处，但它会把 `Q62` 的另两段一并做掉，越了本票的范围。**选了加一条带默认的**。
  ⚠️ 默认实现**不会在下载中途收手**，大件的实现必须自己覆盖它；这一句已经写进 trait
  的文档，但编译器不管。
- **谁来裁：** 票 `queue-followups/03`（`Q62` 剩下那两段）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— `Fetcher::download_while` 的默认实现只在开工之前问一次；**DAT 与 Switch 两条下载仍然停不掉**（`crates/core/src/dat/sync.rs:603`、`crates/core/src/titledb/sync.rs:151` 仍走 `download`）。票面范围写死了「只收中文离线源这一条」，这是 `Q62` 剩下那两段。
  **去处：** 第四轮规格材料，「「停了」那条链剩下的段」那一组。

### Q240 — 那 435 MB 的下载只接了中断，没接进度；一块 1 MiB 是选的，不是量出来的

- **来自：** 票 `parking-3/05`
- **类别：** 规格没说
- **在哪：** `crates/core/src/dat/fetch.rs` 的 `DOWNLOAD_CHUNK`；
  `crates/core/src/zh/sync.rs` 里那次 `download_while` 调用
- **为什么没停线：** 票面要的是「按下 Ctrl-C 之后在当前这一块读完就收手」，那一条已经
  成立；下载那几分钟里屏上说不说话是另一件事（读原件那一段本来就报进度）。
- **这张票实际做了什么：** 只接中断，**没有**把 `Context::progress` 也接进下载
  ——接的话要给 `download_while` 再加一个回调参数，那是第二件事。一块 **1 MiB**
  是在「每几毫秒问一次白花系统调用」与「一条慢线路上读完一块要好几秒」之间挑的，
  **没有实测依据**：实测的只有「收手时搬进去的字节数正好是放行过的那几块」
  （`dat::fetch` 那条测试）。换成 256 KiB 或 4 MiB 都站得住，翻案是改一个常量。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 只接中断、不接进度是本票范围内的终局取舍（接进度要给 `download_while` 再加一个回调参数，那是第二件事）。一块 **1 MiB** 是选的不是量出来的，翻案是改一个常量；量到的只有「收手时搬进去的字节数正好是放行过的那几块」。

### Q241 — `scrape::Outcome` 加了 `Default`，只为界面那一侧的两条测试

- **来自：** 票 `parking-3/05`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/scrape.rs` 的 `Outcome`
- **为什么没停线：** 纯加，一个既有调用点都没动。
- **这张票实际做了什么：** 给它 `#[derive(Default)]`，好让 `crates/gui/src/scrape.rs`
  那两条测试只摆要验的那一格（`interrupted` / `new_blobs`）。另一条路是在测试里手写
  全部十几个字段——那条也站得住，翻案就是把 derive 去掉，可那份手写的构造每加一个字段
  就要跟着改一次。**选了加 derive**，并在文档里写清它是「一趟什么都没干的刮削」。
- **谁来裁：** 收尾
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— 给 `scrape::Outcome` 加 `Default` 是纯加，一个既有调用点都没动；文档写清它是「一趟什么都没干的刮削」。

### Q242 — `dat::sync` 与 `titledb::sync` 的 `#[from] FetchError` 会把新长出来的 `Halted` 支吞成「取数失败」

- **来自：** 票 `parking-3/05`（收尾审查报的）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/dat/sync.rs` 与 `crates/core/src/titledb/sync.rs` 里那两个
  `SyncError` 上的 `Fetch(#[from] FetchError)`
- **为什么没停线：** **眼下产不出那一支**：这两条走的是 `Fetcher::download`，而
  `HttpFetcher::download` 递下去的是一个恒真的 `keep_going`，`FetchError::Halted`
  在这两条路上根本出不来。
- **这张票实际做了什么：** 只把 `zh::sync::SyncError` 那一处堵上了——`Fetch` 那一支
  去掉 `#[from]`，改手写 `From<FetchError> for SyncError` 把 `Halted` 路由到
  `SyncError::Halted`，于是那一层随手 `?` 一下也漏不掉（原先那段手写 `match` 跟着收掉了）。
  另两处**原样没动**：动它们等于替 `Q62` 剩下那两段先铺路，而票面的范围写死了「只收
  中文离线源这一条下载」。**它们是陷阱不是缺陷**：哪天有人给 DAT 或 Switch 那条接上
  中断信号，第一件事就得先把这两个 `#[from]` 换成手写的 `From`，不然「按停了」会
  悄悄变成「取数失败」——正是这张票在任务台那一层拆掉的形状，只是低了一层。
- **谁来裁：** 票 `queue-followups/03`（`Q62` 剩下那两段）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **C（仍然成立，归第四轮）**
- **收尾裁决（第三轮收口，2026-09-08）：** **C** —— `dat::sync` 与 `titledb::sync` 上那两个 `Fetch(#[from] FetchError)` **是陷阱不是缺陷**：眼下产不出 `Halted` 支，可哪天有人给这两条接上中断信号，第一件事就得先把 `#[from]` 换成手写的 `From`，不然「按停了」会悄悄变成「取数失败」——正是这张票在任务台那一层拆掉的形状，只是低了一层。与 `Q239` 是同一张票。
  **去处：** 第四轮规格材料，「「停了」那条链剩下的段」那一组。

### Q243 — 「由类型说话」不是把 `String` 换成 `enum` 就完了：同一个形状从 `Display` 与 `#[from]` 两处又长了回来

- **来自：** 票 `parking-3/05`
- **类别：** 规格没说
- **在哪：** `crates/core/src/task.rs` 的 `Cutoff`（那段「特意不实现 `Display`」的注释）；
  `crates/core/src/zh/sync.rs` 的 `SyncError::Fetch` 与它底下那条手写的
  `From<FetchError>`；同形状还没动的两处记在 `Q242`
- **为什么没停线：** 三处都在这张票要收的那条判据链上，翻案代价也全在本票之内。
- **这张票实际做了什么：** 这一条是**把 `Q234`–`Q242` 那一串串起来的那句话**，
  单记一条是因为它比任何一条单独的裁决都值钱。

  票面的活是「把一道**靠字符串比对**的判据换成由类型说话」。`Cutoff` 立起来、
  `Board::settle` 改成按支 `match` 之后，同一个错误形状**又从两个更低的地方长了回来**，
  两处都长得像便利设施：

  1. **`Display`。** 头一版给 `Cutoff` 实现了 `Display`（`Halted` 那一支转发给
     `Halted` 自己那句中文）。于是判据链上任何一处 `to_string()` 都能把「停了」重新
     折成一句可比的字符串——**而且这一次是刚建的那个类型自己递上去的**。
     已改：`Cutoff` **不实现 `Display`、也不是 `std::error::Error`**。想要那句话的人
     得先 `match` 一下（命令行那一处就是这么摊开写的），于是「我这儿收到的是哪一档」
     在源码上永远看得见。`Halted` 自己照旧有 `Display`——它是个 `Error`，领域错误枚举
     拿它当 `source`；关键是那句话**再也到不了任务台手上**，`Cutoff::Halted` 不带它。
  2. **`#[from]`。** `SyncError::Fetch(#[from] FetchError)` 把 `FetchError` 这一票新长
     出来的 `Halted` 支一并吞进「取数失败」。**一个 `?` 就完成了洗白，源码上一个字符
     都看不出来。** 已改：那一支去掉 `#[from]`，换成手写的 `From<FetchError>` 折支不
     折话。`dat` 与 `titledb` 那两个同形状的眼下产不出 `Halted` 支，按票面范围没动
     （`Q242`）——**记着它们是同一个形状，等哪天它们也长出那一支。**

  合起来是一句：**`Display` 与 `#[from]` 都是「转换」，而「停了」这个信息必须在
  每一次转换上都过不去。** 只把 `String` 换成 `enum` 只挡住了第一次转换。
  另一条路是留着 `Display` 与 `#[from]`、靠代码评审盯住——那条站得住（两处眼下都
  没人踩），翻案就是把两个 `impl` 加回来，可它把「不可能」降回「记得别那么写」，
  而票面那句硬验收要的正是前者。**选了都拆掉。**
- **谁来裁：** 收尾（票 `parking-3/18`）
- **状态：** settled —— 第三轮收口 2026-09-08，走 **A（已经做完，收口只是销号）**
- **收尾裁决（第三轮收口，2026-09-08）：** **A** —— **这一轮方法论产出最高的一条。** 它是把 `Q234`–`Q242` 串起来的那句话：「由类型说话」不是把 `String` 换成 `enum` 就完了——同一个错误形状从 `Display` 与 `#[from]` **两个更低的地方又长了回来**，两处都长得像便利设施。两处都拆掉了（`Cutoff` 不实现 `Display`、也不是 `std::error::Error`；`SyncError::Fetch` 去掉 `#[from]` 换手写的 `From`）。合起来一句：**`Display` 与 `#[from]` 都是「转换」，而「停了」这个信息必须在每一次转换上都过不去。**
