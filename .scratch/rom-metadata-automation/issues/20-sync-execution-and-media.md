# 20 — 同步执行与媒体导出

**What to build:** 把计划真正落到目标设备上。媒体从**媒体池**铺到目标：同卷且支持硬链接就链接，不支持就自动降级为复制——SD 卡通常是不支持链接的文件系统，这条降级路径是必需项而非优化。

子库内的路径一律相对于子库根目录，卡插到不同设备、盘符变化都不受影响。

**Blocked by:** 19

**Status:** ready-for-agent

- [ ] 执行严格按计划进行，不做计划之外的任何写入
- [ ] 删除前必须先呈现干跑预览
- [ ] 导出前探测目标文件系统，据此决定硬链接或复制
- [ ] 主库侧在支持硬链接的卷上使用硬链接，不重复占用空间
- [ ] 子库内元数据中的路径全部相对于子库根目录
- [ ] 同步可中断，中断后状态一致、可续跑
- [ ] 同步完成后清单更新为目标的真实状态
