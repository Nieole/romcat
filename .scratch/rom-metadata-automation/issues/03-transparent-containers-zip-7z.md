# 03 — 透明容器：zip 与 7z 零解压读取

**What to build:** 工具能不解压就读出 zip 与 7z 内部每个文件的 CRC-32、未压缩大小与文件名。体检报告因此能看见容器内部的真实构成，而不只是「这里有一个 3GB 的压缩包」。

这是整个项目性能可行性的支点：DAT 的每条记录都带大小与 CRC，因此第一命中层可以完全不解压。

**Blocked by:** 02

**Status:** ready-for-agent

- [ ] 不解压即可读出 zip 与 7z 内部文件的 CRC-32、未压缩大小与文件名
- [ ] 正确处理 zip 的通用标志位第 3 位——局部头部的 CRC 与大小为零时改读中央目录
- [ ] 正确处理 ZIP64 哨兵值
- [ ] 7z 按 solid block 分组读取，一块一趟流式喂多个 hasher，不对每个内部文件各解压一遍
- [ ] 提供流式部分解压能力：读取内部文件前若干字节后即可提前中止
- [ ] 体检报告能给出容器内部的文件数与构成
- [ ] 有测试覆盖 solid 与非 solid 两种 7z、以及存储与压缩两种 zip 条目
