# 01: 「改选择」不再把几条规则并成一条

**What to build:** 人在子库屏按「改选择」、到浏览屏调完按「更新到子库」，回来那一台的规则**不丢名字、不少条**。
按这一台**读得懂**的规则有几条分三档：

- **没有**：更新到子库时新添一条。
- **恰好一条**：只换这一条的条件，**名字与序号照旧**；读不懂的那几条原样留着。走的是与规则行上 ✎ 同一条逐条替换的路。
- **两条以上**：「改选择」变灰，旁边照 ADR-0005 修订段「带理由的拒绝」的形状写明「这一台有 N 条规则，用规则行上的 ✎ 逐条改」；按下去什么都不发生。

「把读得懂的规则整批删掉、写回一条无名规则」那个核心库函数从此没有调用方，删掉（命令行不走这条路）。
灰不灰的判断住界面层；「几条」这个数照旧取自核心库交出的选择集。

规格：`../spec.md`（`Q1184` 那一段）。收挂单 `Q1184`。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 先写一条在今天的 `main` 上红的界面测试：一台两条带名字的规则 → 改选择 → 更新到子库之后，两条与名字都还在（今天会只剩一条无名的）——修完之后这条改写成下面「两条以上」那一条
- [ ] 界面测试：一条带名字的规则 → 改选择 → 改条件 → 更新到子库 → 从中立库读回仍是一条，名字与序号照旧，条件换了
- [ ] 界面测试：一条读得懂 + 一条读不懂 → 同上，读不懂的那条原样留着
- [ ] 界面测试：两条带名字的规则 → 「改选择」灰着，理由里有「2 条」与 ✎；按下去不跳屏、不写库
- [ ] 界面测试：零条 → 改选择 → 更新到子库 → 新添一条
- [ ] 「改选择」→「不改了」照旧什么规则都不写
- [ ] 核心库那条「只换第几条规则，序号不变，别的规则一条不碰」照旧绿；整批替换函数已删、全仓无引用
- [ ] 子库屏那颗按钮的新状态重批截图基线（若有变），拿主意的人点头
- [ ] 门禁全绿（`Q479` 那五条本机已知红除外）
