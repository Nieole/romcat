# 02 — 标题队列也收中置信的中文名

**What to build:** 一条模糊匹配来的中文名落**中置信**时，它照旧进**标题集合**那条待裁的队列——
维护者看得见它、裁得了它。此前它一进库就退出了视线，可能直接当上**显示标题**，而他一眼都没看过。

收挂账 `D128`：模糊匹配来的中文名算中置信，退出了标题队列。

**Blocked by:** 无 —— 可立即开工

**Status:** done

门禁（2026-09-14，本分支工作树，`TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp cargo xtask gate -j 3 --test-threads 3 --keep-going`，日志 `/Users/nicoer/dev/game-wt/logs/slot-2-oc02-gate.log`）：fmt、glossary、check、clippy、test（72 份测试结果，1,943 条通过、0 条失败，405 秒）、doc，**6 条全绿**，`EXIT=0`。

⚠️ **判据改成与识别那一侧同一条：没人裁过就进队列，不看置信度。**
**否掉「让模糊匹配落低置信」那条路**——那是把**置信度这个描述性的量**当成「我希望它进哪个队列」
的控制开关，同一个病换个地方长。

- [x] 一条中置信的中文名进得了标题队列
  - 证据：`crates/core/tests/titles.rs` 的 `模糊匹配来的中置信中文名没人裁过就进得了队列`。fixture 里放一份文件名叫「塞尔达传说」的 Zelda，刮削那一步带一份只有这一条的中文离线索引。基点 `a10a4ce` 上红（`queue_examples` 为空），改后绿。同一条还断言：这份库一条都没人裁过，于是魂斗罗、盖亚、塞尔达三部全在队列里（`queue_works == 3`，从前一部都进不来）；报告那一节写着「没人裁过就进」。改动在 `crates/core/src/title/report.rs`：原来那句 `best.confidence == Confidence::Low` 换成 `!judged(catalog, best)?`。
- [x] 已经被人裁过的那些**不再**进队列（判据是「裁没裁过」，不是置信度）
  - 证据：两条测试，各自先红后绿。`人亲手写下的中文叫法裁过了不再进队列`：给盖亚写一条来源是裁决的中文叫法，盖亚出队列，中文覆盖数不变。`人肯定过的那一次模糊匹配带来的中文名裁过了不再进队列`：走 `scrape::zh::judge` 肯定那一次匹配，照真跑那样 `Rulings::resolve` 摊平后重刮，塞尔达出队列。
  - 「裁过」认哪两样、为什么不认压掉的叫法和识别那一侧的裁决，写在 `judged` 的文档与提交说明里。第二样用的是新添的 `scrape::zh::affirmed_title`，认法与那一次匹配自己的队列同一个（`matched_groups` 的 `confirmed`）。
  - 保留：人只裁了英文显示标题时，那条中文叫法照旧在队列里（挂单 `Q711`，`显示标题被裁成英文之后中文覆盖照旧算它` 钉着）；同一串字被几个变体叫着时只问集合那一行记着的变体（挂单 `Q712`）。
- [x] 低置信那些照旧进队列，行为不变
  - 证据：`中文名带置信度低的那些进得了队列` 的断言一个字没改，照旧绿（合集包名「塞尔达全集」是低置信，照旧在 `queue_examples` 里，`queue_works` 照旧变多）。只改了一句把低置信当成进队列理由的注释。
- [x] 置信度这个值本身**一个字没改**（没有为了让它进队列而调档）
  - 证据：`crates/core/src/title.rs` 的 `classify` 只改了注释，`Confidence::Medium` 那一行没动。两条测试断言模糊匹配那条仍是 `Confidence::Medium`：一次在进队列那条里，一次在人肯定过、出了队列之后。

收挂账 `D128`：照 grill 第 79 行的裁定，标题队列与识别那一侧同一判据（没人裁过就进）。`scrape/report.rs:769` 那句「中置信结论，照旧进待确认队列」补上「没人裁过就」，本票之后它才是实话。标题侧的队列眼下只是报告里一个数与十个例子，没有造一条能逐条裁的队列（挂单 `Q713`）。ADR-0002 与词表**待确认队列**的原话仍按置信度分档，与两侧的判据并排读会打架，ADR 与词表没动（挂单 `Q714`）。
