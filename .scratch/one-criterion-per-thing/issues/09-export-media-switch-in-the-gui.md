# 09 — 界面上那颗铺媒体的开关，按下之前说得出要付多少

**What to build:** 维护者在**工序**那一段的导出这一支上看见一个「一起铺媒体」的开关，
打开它时界面**先说清要付多少**（多少份、多大），他再决定按不按。

收挂账 `D64`（下半）。

收挂单 `Q584`：屏上那个数眼下是**上界**（落点上已经有的真铺时不重铺，见 `transfer::media_to_lay` 的文档）——照裁决说成「最多」，或者改报扣掉已有的。

**Blocked by:** **08 — 导出铺得出媒体（核心库那条路 ＋ 命令行开关）**

**Status:** done

⚠️ **收场四档的词以票 `gui-looks-like-the-design/03` 为准**（完成 / 已取消 / 部分完成 /
失败）：本票正文里若还写着「按停了」「停在半路」，实现与断言一律用新词；那张票没有阻塞、
可立即开工，两张不排同一波。

⚠️ **排波约束（硬的）**：本票动的是工序那一段的导出这一支，而
`.scratch/gui-answers-all-six/` 的**票 04（刮削进工序段）与票 05（导出「照写」出路）动同一处**。
**这三张不许排进同一波。**

⚠️ 开关**默认关着**。代价那句话要在**按下之前**说得出来，不是按下去之后才发现在拷贝。

- [x] 工序那一段导出这一支上有一个「一起铺媒体」的开关，默认**关着**
  - 证据：`crates/gui/tests/roots.rs::铺媒体开关默认关着_关着时导出与今天一模一样_一份媒体都不铺`——走 `headless` 收这一帧画出来的字，屏上有一整行 `stages::LAY_MEDIA`（「一起铺媒体」），`Section::lay_media()` 为 `false`。
  - 开关上的字与那句代价都是说得出名字的东西（`pub const stages::LAY_MEDIA`、`pub fn stages::media_cost`），各有断言按它们在屏上找，给库屏重排（票 `gui-looks-like-the-design/06`）留着。
  - 保留：开关只记在这一段上、不记进库，每次开窗都是关着的（挂单 `Q652`）。
- [x] 打开时，屏上**先说清**这一趟要铺多少份、多大
  - 证据：`打开铺媒体时_按下导出之前屏上先说清这一趟最多要铺几份多大`——**真点一下**开关（指针事件），不按导出，屏上画出 `media_cost(1, 19)`，那句里有「最多」「1 份」「19 B」，导出目录里一个文件都没有。
  - **按下之前**这一半靠一道闸：数没画出来时按导出（平常那颗与照写那颗都走 `Section::queue`），当场说一句、不排——`要铺多少还没算出来时按导出_当场说清_一份都不先铺`、`要铺多少算出来之后_那句按不下去的话收掉`。
  - 数是核心库那一处交的上界（`transfer::media_to_lay`，界面不另算，ADR-0024）；说成「最多」，收挂单 `Q584`（已标 `settled`）。
  - **不在画帧线程上算、不每帧重算**：打开开关时排一趟「算一遍要铺多少媒体」上任务台，读第二份只读连接，算完缓存（挂单 `Q651`）；缓存到库可能变了为止——这一段重读库、排上或跑完一道别的工序、改导出配置、离开库屏再回来（挂单 `Q654`；`跑完一道工序之后那句代价跟着重算_不拿旧数骗人`、`台上排着一道别的工序时按导出_那句代价作废_不排`、`改了导出的前端格式之后_那句代价重算一遍`、`离开库屏再回来_那句代价重算一遍`，最后这条同时断言「还在库屏上就不又排一趟」）。算的那一趟被撤掉时屏上说清、关掉再打开就重算：`要铺多少那一趟被撤掉时屏上说清_关掉再打开就重算`。算的那一趟整条只读，收场时不惊动浏览屏：`算要铺多少那一趟收场时浏览屏不重读`。
  - 保留：`media_to_lay` 在真库上要多久**没量过**；同在库屏上的根那一块排的扫描，排上时不作废（不会让屏上少报，见 `Q654`）。
- [x] 关着时，导出那一趟的行为与今天**一模一样**
  - 证据：`铺媒体开关默认关着_关着时导出与今天一模一样_一份媒体都不铺`——池里**真有**一张挂在魂斗罗上的封面，关着导一趟：导出目录里只多两份 `.metadata.pegasus.txt`、没有 `media/`、条目里一个 `assets.` 都没写；任务台历史那一趟叫「导出」、记「完成」；回执里没有「媒体」两个字。
  - 步数：`crates/gui/src/stages.rs` 的 `tests::导出那一趟声明的总步数与真走过的对得上_开着铺媒体也不走过头`，关着时最后停在「逐份写出去」、走过的等于声明的（`2 + ExportOptions::task_steps()`，关着时等于原来的 `2 + TASK_STEPS`）。
  - 原有的导出几条照旧绿（`roots.rs` 里「导出」相关的十条，含票 05 那几条照写）。
- [x] 铺媒体那一趟在**任务台**上与其余几步同一套看法（报得出跑到哪儿、按得下停下、四档**收场**分得开）
  - 报得出跑到哪儿：`export_run` 照 `ExportOptions::task_steps()` 声明总步数；上面那条单测开着时最后停在「把媒体铺出去」、走过的等于声明的，进度条不走过头。
  - 完成：`打开铺媒体之后导出那一趟真的铺出去_任务台记完成_回执说得出铺了几份`（`media/<前两位>/<哈希>.png` 就是池里那一份，条目写着 `assets.boxFront`，回执「铺出去 1 份」）。
  - 已取消：`开着铺媒体的导出排着队被撤掉时记成已取消_一份媒体都没铺`。
  - 部分完成：`铺媒体连着没铺成主动停了_任务台记部分完成_说得出铺到第几份`（「（共 12 份）」，铺出去的那一份留在盘上，屏上那句含 `record.ending.render()`）。
  - 失败：`开着铺媒体读不动优先级表时记成失败_一份媒体都没铺`（停在「读优先级表」）。
  - 收场的词一律从 `Ending::render` 出，这一层一个都没另写。
  - 保留：**人在「折媒体的落点」「把媒体铺出去」那两步跑着时按停**，界面这一层没有确定性的测试——导出里没有能把「停在第几份」钉死的接缝，而这一层递进去的是任务台那同一个把手；那条路由 `romcat-core` 钉着（`tests/sync_run.rs::导出铺媒体铺到一半按停_铺过的留在盘上_说得出铺了几份`、`tests/pegasus.rs::元数据写完媒体还没铺就按停_记的是没走完而且说得出媒体一份都还没铺`）。
- [x] 这一票只碰工序那一段导出这一支，不动票 04 / 05 的地盘
  - 证据：改动落在 `crates/gui/src/stages.rs`（导出那一支：`ExportKnobs::media`、`export_knobs`、`export_run`、开关与代价那一段、算要铺多少那一趟）、`crates/gui/src/task.rs`（`Product::MediaCounted` 一支）、`crates/gui/src/app.rs`（`poll_tasks` 先认算要铺多少那一趟，5 行）。刮削那一行（票 04）一行没动；照写（票 05）只经 `export_knobs` 带上同一个开关，`force_export` 与 `start` 一行没改——`照写那一趟带着同一颗铺媒体开关` 钉着。
  - `Section::queue` 多了两处：导出开着铺媒体、代价没画出来时挡下；排上别的工序时作废那句代价。前者是验收第 2 条的闸，后者是 `Q654`。
  - 库屏没重排：开关与代价画在导出配置那一行底下，原有几块的位置一格没挪。

## Comments

**收尾（2026-09-14）：** 收了挂账 `D64` 的下半（界面那颗开关）；上半（核心库那条路与命令行 `--media`）由票 08 收。收挂单 `Q584`（留上界、屏上说「最多」，已标 `settled`）。本票挂单 `Q651`–`Q655`。
审查（`/code-review`，两轴）之后改的：算要铺多少那一趟收场不再惊动浏览屏整页重读（`App::poll_tasks` 先认它）；排上别的工序时那句代价作废；数出来或关掉开关时收掉那句按不下去的话；挂单格式两处。没改的判断项：只读连接那三支 `match` 已是仓里第四份、铺媒体回执与命令行各写一份（`Q655`）。
窄跑（2026-09-14，带 `TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp`）：`cargo test -p romcat-gui --all-features --no-fail-fast` 全绿（lib 44、`roots.rs` 56，其余测试目标照旧），`EXIT=0`，日志 `/Users/nicoer/dev/game-wt/logs/slot-3-oc09-gui-tests-after-review.log`；`clippy -p romcat-gui --all-targets --all-features -D warnings`、`cargo xtask glossary`（4 份 `.rs` 新写 1180 行，没撞上）、`cargo doc -p romcat-gui`（`-D warnings`）全绿，日志 `slot-3-oc09-review-green.log`。全量门禁另起一个提交补在后面。

**全量门禁（2026-09-14，跑在票提交 `a9efe5c` 上，带 `TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp`）：** `cargo xtask gate -j 3 --test-threads 3 --keep-going`，日志开头两行是 `/Users/nicoer/dev/game-wt/slot-3` 与 `q5/oc-09-export-media-switch-gui`。fmt 1s、glossary 0s、check 12s、clippy 17s、test 350s、doc 4s，6 条全绿，`EXIT=0`。
- glossary 扫的是相对 `main` 的 merge base（`d411fdf`）以来的改动：4 份 `.rs` 里新写 1180 行，没撞上。
- test：71 个测试目标、1931 条通过、0 条失败、2 条 `#[ignore]`；门禁在第一个红的目标上就停，这一趟没红，全部都跑到了。
- 日志：`/Users/nicoer/dev/game-wt/logs/slot-3-oc09-gate.log`。

## 挂单裁决（第五轮收口，2026-09-23）

从 `.scratch/PARKING-LOT.md` 迁来（`/settle`，范围 `Q449`–`Q1185`）。每条顶上多一行**裁决**：落哪一档、去了哪儿。
编号 `Qn` 留着占号、不复用。

### Q651 — 「这一趟最多要铺多少」排在任务台上算，不在画帧线程上、也不在台外另开一条线程

- **裁决（第五轮收口，2026-09-23）：** **岔** —— 实现走的就是条目建议的那条；追认：维持现状（2026-09-23 拿主意的人整批追认）。

- **来自：** 票 `one-criterion-per-thing/09`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/stages.rs` `Section::count_media`、`media_cost_run`；`crates/gui/src/task.rs` `Product::MediaCounted`
- **为什么没停线：** 票 08 交出了那个数（`transfer::media_to_lay`），怎么算才不卡界面是界面这一层的事；几条路都一个字节不写。
- **这张票实际做了什么：** 打开开关时排一趟「算一遍要铺多少媒体」上任务台，后台读同一个库文件的第二份只读连接（与子库屏「算一遍容量」同一条路），算完缓存着、不每帧重算；数没出来之前按导出当场说一句、不排（`crates/gui/tests/roots.rs::要铺多少还没算出来时按导出_当场说清_一份都不先铺`）。`media_to_lay` 在真库上要多久**没量过**。
- **另一条路：** 在任务台外另开一条线程算：不进历史、不排在一趟几十分钟的扫描后面，画帧那条线程每帧只看一眼它交回来没有。
- **建议留哪条：** 留任务台。词表**任务台**说「排在台上的一次只跑一趟——这些活都在读同一份中立库、写同一块盘」，台外那条线程会与台上正在铺媒体的导出抢同一块盘，而且看不见、停不下；挂账 `D156` 把排差量预览搬上任务台图的正是「进度看得见、停得动」。代价是历史里多一条、台忙时要等——台忙时导出本来也得排在后面。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q652 — 铺媒体那颗开关只记在这一段上、不记进库：每次开窗都是关着的

- **裁决（第五轮收口，2026-09-23）：** **岔** —— 实现走的就是条目建议的那条；追认：维持现状（2026-09-23 拿主意的人整批追认）。

- **来自：** 票 `one-criterion-per-thing/09`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/stages.rs` `Section::lay_media`（与记进中立库元数据表的导出配置 `ExportSetup` 对照）
- **为什么没停线：** 票只说默认关着；两条路第一次开窗都是关着的。
- **这张票实际做了什么：** 开关只活在 `Section` 上，新建出来就是关着的（`crates/gui/tests/roots.rs::铺媒体开关默认关着_关着时导出与今天一模一样_一份媒体都不铺` 钉的就是这一句）；重开窗口、换一份库都会新建一份 `App` 连同这一段，于是回到关着——这一半是读代码得出的，没有测试单独钉。
- **另一条路：** 与导出去哪儿那两格一样记进中立库，下次开窗照上次的拨法。
- **建议留哪条：** 留不记。它要付的可能是几十 GiB 的复制，「默认关着」的理由（人多半只想要元数据，票 08）每次开窗都成立；记住的话，上个月为一次整理打开过它，这个月点一下导出就拷起来，而那句代价画在一个人这次没打算看的地方。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q653 — 代价还没画出来时导出那两颗按钮不变灰，按下去当场说一句、不排

- **裁决（第五轮收口，2026-09-23）：** **岔** —— 实现走的就是条目建议的那条；追认：维持现状（2026-09-23 拿主意的人整批追认）。

- **来自：** 票 `one-criterion-per-thing/09`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/stages.rs` `Section::queue` 开头那道闸（平常那颗「开跑」与「我看过了，照写」都走它）
- **为什么没停线：** 票只要「代价在按下之前说得出来」，没说按钮长什么样；两条路都不让没看过代价的一趟排上台。
- **这张票实际做了什么：** 闸在 `queue`，两颗按钮一个口径，屏上那一行写着「算出来之前导出按不下去」；按钮本身照旧画成按得下去的样子。
- **另一条路：** 开着铺媒体、数没出来时把导出那一行的「开跑」与照写按钮一起禁掉（`add_enabled`）。
- **建议留哪条：** 留现在这样：闸必须在 `queue`（捷径与照写都得挡住），而再加一层变灰要动工序表那一格与票 05 那颗按钮；变灰的按钮说不出为什么，按下去那一句说得出。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q654 — 那句代价什么时候过期：这一段重读库、这一段排上或跑完一道别的工序、改导出配置、离开库屏再回来

- **裁决（第五轮收口，2026-09-23）：** **岔** —— 实现走的就是条目建议的那条；追认：维持现状（2026-09-23 拿主意的人整批追认）。

- **来自：** 票 `one-criterion-per-thing/09`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/stages.rs` `Section::expire_media_cost` 与它的几处调用（`reload`、`queue`、`settle`、`set_export_setup`、`lay_media_ui` 里认出「离开过这一屏」的那一下）
- **为什么没停线：** 票只要「按下之前说得出来」、简报只禁「每一帧都重算」；过期的判据怎么定，几条路都一个字节不写。
- **这张票实际做了什么：** 上面几件事之一发生就作废那个数，开关开着时下一帧重排一趟去算，数重新出来之前导出按不下去；排上与跑完导出都不作废（它只写底本、时刻戳与导出目录，`media_to_lay` 读的变体、作品归属、媒体引用、媒体池一样都没动）；正在算时作废，交回来的那个数不用、再算一遍。钉它的是 `crates/gui/tests/roots.rs` 的 `跑完一道工序之后那句代价跟着重算_不拿旧数骗人`、`台上排着一道别的工序时按导出_那句代价作废_不排`、`离开库屏再回来_那句代价重算一遍`、`改了导出的前端格式之后_那句代价重算一遍`。**没盖住的一条**：同在库屏上的根那一块排的扫描，排上时不作废（扫完 `reload` 时才作废）——扫描加进来的变体还没有媒体引用，移除根只会让数变小，所以它不会让屏上**少报**。
- **另一条路：** 窗口那一层（`App::poll_tasks`、`App::route`）每认领一趟活、每收到一个「别的屏改了库」的记号就转告这一段作废：只在库真的变了时重算，不因为人切了一下屏就多排一趟。
- **建议留哪条：** 留现在这样。别的屏改库的出口不止任务台（裁决、合并作品、刮削面板收媒体各走各的路），窗口那一层得逐个接，漏接一个就是一个会**少报**的数——而少报正是危险的方向（屏上说几 MiB、按下去拷几十 GiB）；「离开过这一屏」不必认出口。代价是切屏回来多排一趟、历史里多一条。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q655 — 铺媒体那句回执界面与命令行各写一份，已经差着一截

- **裁决（第五轮收口，2026-09-23）：** **活** —— 无家可归（全仓没有一张开着的票占这片地），进 `/grill-with-docs`，束「核心库该多交一样东西、或把一个判据收到一处」。

- **来自：** 票 `one-criterion-per-thing/09`
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/stages.rs` `media_laid`；`crates/cli/src/main.rs` `export` 收场那几行 `eprintln!`（「媒体铺出去 … 份（硬链接 …、复制 …）」）
- **为什么没停线：** 两句说的是同一份 `MediaReport` 的同几格，数一个都不另算；只是话写了两份。
- **这张票实际做了什么：** 界面另写一句，比命令行那句多报「几份没铺成」（命令行只报被占着的）；命令行一行没动（本票只碰界面导出这一支）。钉它的是 `crates/gui/tests/roots.rs::打开铺媒体之后导出那一趟真的铺出去_任务台记完成_回执说得出铺了几份`。
- **另一条路：** 在核心库 `MediaReport` 上立一个一句话的渲染（与 `ExportReport::render_text` 同一处），界面与命令行都读它——这一次就把命令行那几行一起换掉。
- **建议留哪条：** 走另一条，留给下一张动命令行导出收场的票：两份话已经分叉了一次（没铺成的那几份命令行不说），再各改各的只会越差越远。这一次没走，是因为那要动 `crates/cli`，出了本票「只碰工序段导出这一支」的圈。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）