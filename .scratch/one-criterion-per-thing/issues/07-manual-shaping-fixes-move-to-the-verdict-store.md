# 07 — 成型的人工纠正搬进沉淀库

**What to build:** 维护者哪天撞上结构版本变了、按提示删库重扫，**他一条条人工纠正出来的成型
还在**。而且删库那句提示会**逐项说清还会丢什么**，他按下去之前知道代价。

收挂账 `D97`：中立库里还剩几样不完全可再生的东西。

**Blocked by:** 无 —— 可立即开工

**Status:** done

⚠️ **搬进沉淀库、单开一张表**，不另开第三份库文件（ADR-0001 修订）。进了沉淀库就白拿那一档的
两条保证：**走顺序迁移、永不要求删库**。中立库那张表**删掉**，读它的地方改成读沉淀库。

⚠️ **默认不随沉淀库导出。** 「它只对本机成立、而沉淀库可导出分享」不构成反对——
**那个先例已经有了**：**路径锚**那些行就住在沉淀库里、默认不导出。人工纠正走同一条。

⚠️ **另两样不搬**：**子库**的定义与**清单**、导入的前端快照留在中立库并接受重扫会丢
（它们重建得回来）——但**删库那句提示要把这两样逐项说出来**。

⚠️ 现成样板：成型那份测试里已经有一条验「人工纠正熬得过**重扫**」的，新那条是它的另一半
（**删库**重扫）。

- [x] 人工纠正**熬得过删库重扫**（删掉中立库文件、重扫一遍，它还在）
  - 证据：`crates/core/tests/shaping.rs::人工纠正熬得过删库重扫`（先红：编不过；删掉中立库文件连 `-wal` / `-shm`、重建、重扫，那个变体仍是 2 个文件、`manual`，报告里人工纠正 1 个）。
  - 真入口：`crates/cli/tests/shape.rs::人工纠正熬得过删掉中立库重扫`（`shape --merge` → 删掉 `catalog/` → `scan` → 报告里 `manual = 1`、`variants = 4`）。
  - **人读到那句提示时的那份库**：`crates/core/tests/shaping.rs::结构版本对不上的旧库_列出来那一下就把人工纠正救进沉淀库_照提示删库重扫之后还在`（关掉救援验过红：`files` 1 ≠ 2）。这一条是规格审查抓出来的——结构版本对不上的库开不进去，原先「开现场时搬一次」那条路走不到它（挂单 `Q726`）。
  - 旧中立库里攒着的：`…::旧中立库里记着的人工纠正_开现场时搬进沉淀库_撤掉的不再回来`（挂单 `Q721`）。
  - **保留**：界面扫描那一处取纠正没有测试单独钉（挂单 `Q723`）。
- [x] 它默认**不出现在**沉淀库的导出里
  - 证据：`crates/core/tests/shaping.rs::人工纠正默认不出现在沉淀库的导出里`（库里另放一条能分享的裁决作对照，断言它在导出里、纠正不在；别人导入之后一条纠正都没有）。
  - **保留**：到达即绿，没经过红——导出本来只折裁决与匹配裁决两张表，这条钉的是日后改格式时别顺手带上。`--include-path` 带不带见挂单 `Q722`。
- [x] 中立库里那张表没有了，读它的地方全部改完
  - 证据：中立库建表语句里删掉 `shaping_override`；`Catalog` 上那三个读写入口删掉，读写改走 `Store::{shaping_overrides, set_shaping_override, clear_shaping_override}` 与 `Site::shaping_overrides`——`shape::reshape`、扫描收尾、命令行 `shape` / `scan`、界面扫描；`remove_root` 那句删除拿掉（挂单 `Q724`）。全仓 grep `shaping_override` 只剩沉淀库、搬家那两个函数与文档。
  - 旧库里那张表**留着不删、不再读**，开现场时搬一次、记下「搬过了」（挂单 `Q721`）；不升中立库的结构版本。
- [x] 删库重扫那句提示**逐项**说清会丢什么（子库的定义与清单、导入的前端快照）
  - 证据：`CatalogError::Version`；`crates/core/tests/catalog_list.rs::结构版本对不上的库照列并说清是哪个版本对哪个版本` 断言那句话含「子库的定义与清单」「底本」「首选变体」「亲手加的叫法」。
  - **保留**：「导入的前端快照」照词表改叫**底本**（ADR-0001 修订段同改）。那句话多说了两样——**首选变体**与**亲手加的叫法**也住在中立库、删库会丢，是挂账 `D97` 数漏的人定的东西；本票只说不搬（挂单 `Q725`）。
- [x] 沉淀库那一侧走的是顺序迁移，旧的沉淀库文件打开就能用、不要求删
  - 证据：沉淀库第 6 条迁移；`crates/core/src/verdict.rs` 的 `第五版的老库带着裁决与压制升上来_人工纠正那张表是空的`（先红：编不过），按主库分开存取 `人工纠正按主库分开存得住也撤得掉`。
- 收挂账 `D97`：`.scratch/rom-metadata-automation/deferred.md` 的 `D97` 标做掉了，并注明数漏的两样（→ `Q725`）。
- 全量门禁（`cargo xtask gate -j 3 --test-threads 3 --keep-going`，带 `TMPDIR=/Users/nicoer/dev/game-wt/cs/tmp`）：6 条全绿——fmt 1s、glossary 0s、check 12s、clippy 25s、test 387s、doc 6s；test 那一步 72 个目标（69 个测试二进制加 3 份文档测试）合计 1962 passed / 0 failed；`EXIT=0`。日志 `/Users/nicoer/dev/game-wt/logs/slot-3-oc07-gate.log`。
- 挂单：`Q721`–`Q726`。
