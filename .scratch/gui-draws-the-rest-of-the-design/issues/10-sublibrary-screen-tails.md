# 10: 子库屏：示例名、合计注、例外「还有 N 个」

**What to build:** - 目标设置弹层：选择集空时「设备上的位置」照新建那一档拿示例名画。
- 子库卡片合计那一行旁补「不含元数据与转换」。
- 手动例外弹层摆满 6 行时补「还有 N 个，去浏览屏筛」，按下走「改选择」回程跳浏览屏。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q950`：见 `../grill.md` 里那一条。
收挂单 `Q814`：见 `../grill.md` 里那一条。
收挂单 `Q974`：见 `../grill.md` 里那一条。

**Blocked by:** 01、`no-wrong-deletes-or-platforms/01`（「改选择」回程先修好）

**Status:** ready-for-agent

- [ ] 先写红测试（界面）：选择集空的子库打开目标设置 → 「设备上的位置」那一格有示例路径（今天整格不画）
- [ ] 合计旁有那一句注
- [ ] 例外弹层搜到 10 部 → 只摆 6 行、写「还有 4 个」；按下跳到浏览屏且在编辑这个子库
- [ ] 子库屏相关截图基线重批；每张新截图基线先由编排者对稿自审，过关的再交拿主意的人点头
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
