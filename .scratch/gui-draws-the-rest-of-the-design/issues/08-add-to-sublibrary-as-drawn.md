# 08: 加入子库照稿：容量条、三格预估、原地叠目标设置、全选禁

**What to build:** 「加入子库」那一层照稿：
- 「加入到」每一台旁边补容量条；预估那一格改成三格等宽。
- 「新建子库…」：子库屏的目标设置弹层做成**两屏共用**，在浏览屏原地叠上；建完回到「加入到」并选中新那台，筛选与勾选都不丢。
- 全选筛选结果时「只加入勾选的」照稿禁掉，理由写「全选请用『作为规则加入』」。

⚠️ 05–09 都动浏览屏那一个大文件，并行派时错开。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q1183`：见 `../grill.md` 里那一条。
收挂单 `Q1185`：见 `../grill.md` 里那一条。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 先写红测试（界面）：全选之后「只加入勾选的」按不动、理由在屏上（今天按得动）
- [ ] 按「新建子库…」→ 浏览屏上叠出目标设置 → 建完回到「加入到」、新那台被选中、筛选条数不变
- [ ] 「加入到」每行有容量条；预估三格
- [ ] 子库屏上打开目标设置照旧可用（共用那一层两边都走得通）
- [ ] 加入子库弹层截图基线重批；每张新截图基线先由编排者对稿自审，过关的再交拿主意的人点头
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
