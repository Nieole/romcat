# 04: 详情页其余：收藏与合集行、标签吸顶、优先级按钮、识别依据帮助、平台全名

**What to build:** - 详情页头上收藏按钮与标签照稿；状态块补「合集」一行，连「加入合集…」（接现成弹层）与就地移出。
- 六面标签吸顶（头与标签分段摆）。
- 元数据帮助里「数据源优先级」换成行内按钮，打开优先级弹层并停在对应字段。
- 识别依据帮助字：删掉今天印在屏上、与 ADR-0002 对不上的那半句，后半句照 ADR-0002 改写。
- `platforms.toml` 补其余二十四个平台的官方英文全名（只动数据）。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q961`：见 `../grill.md` 里那一条。
收挂单 `Q1108`：见 `../grill.md` 里那一条。
收挂单 `Q926`：见 `../grill.md` 里那一条。
收挂单 `Q925`：见 `../grill.md` 里那一条。
收挂单 `Q922`：见 `../grill.md` 里那一条。
收挂单 `Q962`：见 `../grill.md` 里那一条。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 先写红测试（界面）：详情页上按收藏 → 状态变、沉淀库里有这一条；按「加入合集…」弹出现成那一层
- [ ] 往下滚动之后六面标签仍在视口顶上、按得到别的面
- [ ] 按「数据源优先级」→ 优先级弹层打开、停在当前字段
- [ ] 识别依据帮助字与 ADR-0002 一致（断言屏上没有旧那半句）
- [ ] 平台表里每个平台都有全名（测试逐项断言非空）
- [ ] 详情页截图基线重批；每张新截图基线先由编排者对稿自审，过关的再交拿主意的人点头
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
