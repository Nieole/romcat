# 02: 任务带副标题（八种长活照稿填）

**What to build:** 任务台与任务屏上每一趟长活名字底下有一行副标题，说清它在做什么（例如差量预览那一趟：「只读取库和设备上的目录，不写入文件」；扫描那一趟：扫的是哪个根）。
核心库给任务加一个**可选副标题槽**，排活的各个入口照稿各交一句。收口时 `Q835` 裁的「正在跑那张卡先不画副信息」随之作废——那一行就是这个槽。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q1028`：见 `../grill.md` 里那一条。
收挂单 `Q835`：见 `../grill.md` 里那一条。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 先写红测试（核心库）：排一趟带副标题的活 → 任务快照里读得回那一句
- [ ] 界面测试：稿上八种长活各排一趟，任务屏上每一张卡都有副标题，文字照稿
- [ ] 不带副标题的旧调用照常工作（槽是可选的）
- [ ] 任务屏与状态栏截图基线重批；每张新截图基线先由编排者对稿自审，过关的再交拿主意的人点头
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
