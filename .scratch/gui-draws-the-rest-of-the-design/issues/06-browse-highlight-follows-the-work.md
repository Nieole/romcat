# 06: 浏览屏键盘：高亮存作品身份，三处共用判断

**What to build:** 浏览屏的高亮改存**作品身份**，表格与卡片墙共用：卡片墙上下键挪得动、画选中态；表格与卡片墙来回切，高亮留在同一部作品上。
三处键盘入口共用的那三道判断（有弹层／有浮层／光标在输入框里就不接）抽成一个函数；焦点那条无障碍路（Tab + Enter）留着。

⚠️ 05–09 都动浏览屏那一个大文件，并行派时错开。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q1142`：见 `../grill.md` 里那一条。
收挂单 `Q1143`：见 `../grill.md` 里那一条。

**Blocked by:** 01

**Status:** ready-for-agent

- [ ] 先写红测试（界面）：卡片墙上按下键 → 高亮挪到下一部（今天不动）
- [ ] 表格上高亮第 3 部 → 切卡片墙 → 高亮仍是那一部
- [ ] 有弹层时按下键不动高亮；光标在搜索框里时同样不动
- [ ] Tab + Enter 仍能在卡片上走通
- [ ] 卡片墙截图基线重批；每张新截图基线先由编排者对稿自审，过关的再交拿主意的人点头
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
