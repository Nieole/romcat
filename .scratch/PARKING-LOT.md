# 挂单：实现时撞上、但没有为它停线的东西

**Status:** living（本轮队列全部票共用一份）
**Opened:** 2026-09-04
**Effort:** 本轮队列 = `offline-chinese-fields` 6 张 + `gui-redesign` 12 张

---

## 这份文件是什么

接票实现时总会撞上票没预见到、或者票写错了的东西。**这里面几乎没有一件该停下整条队列。**
它们落在这儿：**记下来、绕过去、接着走、收尾时一起裁。**

它不是项目的长期缺口清单（那是 `.scratch/rom-metadata-automation/deferred.md`，编号 `Dn`，全局连续）：

| | 挂账 `deferred.md` | 这份挂单 |
|---|---|---|
| 装的是 | **产品**里的取舍 | **执行**里冒出来的问题——票写错了、规格没说、两份东西互相矛盾 |
| 寿命 | 活得比这轮队列长 | **收尾时清空**——每条要么就地裁掉、要么升进 `deferred.md`、要么开成新票 |
| 谁读 | 下一个动这块产品的人 | 收尾那一步，和拿主意的人 |

**编号分两套，别混：** 这里用 `Q1、Q2…`；`deferred.md` 用 `D1、D2…`。
一条从这里升进 `deferred.md` 时才拿 `Dn` 号，且这边的 `Qn` 留着不复用。

---

## 停线——只有这三条

停线很贵：拿主意的人得把整个上下文重新装回脑子。门槛：

1. **接着做会写出一个错的东西，而且撤销它要伸到这张票之外。**
   典型：票里的指示与上游一条仍然生效的决定（ADR、词表、规格）相悖。
2. **动作不可逆，而且不止一个方向站得住。**
   典型：删掉或改名一个被很多处引用的东西——一个 id、一个字段、一份文件。
3. **前提根本不在，票做不下去。**
   典型：「在 X 上面盖 Y」，而 X 不存在。

其余一律：记在这儿，挑最站得住的那条路走，接着做。这包括：票里的数字与你实测的对不上、
验收条目读着含糊、票没想到的第三种情况、路过看见的无关缺陷、以及你确实拿不准的单条。

> ⚠️ **绕过去要留痕。** 条目记的是**你走了哪条路**，不是「有两条路」。
> 说不出自己做了什么的条目，不算一条条目。

---

## 条目格式

```markdown
### Q<N> — <一句话：这是什么>

- **来自：** 票 `<feature>/NN`
- **类别：** 票写错了 / 规格没说 / 两份东西矛盾 / 工具缺陷 / 路过发现，不在范围内
- **在哪：** 路径 + 行号或 id —— 要点得过去
- **为什么没停线：** 一句话
- **这张票实际做了什么：** 一句话 —— **必填**
- **谁来裁：** 拿主意的人 / 票 `NN` / 收尾
- **状态：** open
```

编号在整轮队列里连续往下走，**不要按票分节**——「来自」那行已经带了这个信息。

---

## 条目

<!-- 从 Q1 往下追加；不要重排，不要删 -->

### Q1 — 别名进不了标题集合，卡在中立库那张表的去重键上，不在存储

- **来自：** 票 `offline-chinese-fields/01`
- **类别：** 票写错了
- **在哪：** `crates/core/src/catalog/scrape.rs` 的 `scrape_value`（原主键
  `(anchor, subject, field, source)`）、`crates/core/src/scrape.rs` 的 `Harvest::value`
- **为什么没停线：** 票说「别名的存储已经有了，缺的只是端到端那条路」——中文索引那一侧确实
  齐了（`subject_name` 一直在存），可端到端那条路上真正拦住它的是**中立库**：一个
  「锚点 × 字段 × 源」只存得下**一个**值，于是一条条目的几个别名只有一个落得了库，
  其余的连同**依据**一起蒸发。这不是「换一条路走」的事，绕不过去。
- **这张票实际做了什么：** 把 `scrape_value` 的去重键加上 `value` 那一列（`Harvest::each`
  是集合字段那一条路，单值字段仍旧走 `value`、第一个胜出）。**没有加中立库的结构版本**：
  老库在 `catalog::scrape::rekey` 里**无损换键**——旧表整份搬过来，一行不丢一列不改，
  与 `identify::add_columns` 同一档的就地修补。加一格版本要用户删掉 780 MB 的库、重扫
  27 分钟、重跑 14 分钟识别，而这张表本来就整份可再生。
  **代价说清楚**：拿老版本的程序去开新库会在 `ON CONFLICT` 那一句上硬报错（不是静默出错）。
- **谁来裁：** 拿主意的人
- **状态：** settled —— **裁决（2026-09-04，用户）：项目还在开发阶段，用户手上一个真实的
  中立库都还没有**，要等这一批票做完才开始扫描。于是「免掉用户 27 分钟重扫」这笔账根本
  不存在，`rekey` 是为一个不存在的问题写的。按 `catalog::SCHEMA_VERSION` 自己那条判据
  （「改了已有表的列或含义才加 1」），改去重键正是该加 1 的那一档。**已在提交 `abd2b1c` 里摘掉 `rekey` 及其测试，中立库结构版本 4 → 5。**上一条报告里说的「旧版程序开新库
  会硬报错」那个兼容代价随之消失。往后所有票：中立库改结构一律加版本重建，不写迁移代码；
  **沉淀库仍走顺序迁移**，它不可再生，那条纪律不跟着放宽。

### Q2 — 别名与中文名同源同档，合成一路的话「显示标题」是按码位定的

- **来自：** 票 `offline-chinese-fields/01`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/title.rs` 的 `rank`（第四层才轮到源名次）、
  `crates/core/src/scrape/priorities.toml`
- **为什么没停线：** 中文名与别名都被 `title::classify` 判成「中置信的别名」，语言也一样，
  于是排序键前三层完全平手，最后按**字典序**定胜负——`合金彈頭7` 与 `合金弹头7` 谁当
  显示标题全看码位。那不是一条判据，而票只说了「别名要进集合」，没说它不该当显示标题。
- **这张票实际做了什么：** 别名另占一个源名 `中文离线源·别名`
  （`identify::fuzzy::ALIAS_SOURCE`，`scrape::zh::ChineseAliasSource`），在优先级表的
  「标题」那条链上**垫在最后一位**（`文件名` 之后），并且在 `title::classify` 里判成
  **低置信**——两样缺一不可，理由见 Q7 第 4 条（`title::rank` 里置信度排在那张表前面）。
  于是别名只进标题集合、只管搜得到，轮到它当显示标题的唯一情形是「这个变体一个标题都
  没有」——那时有出处的叫法总比空白强。集成测试
  `撞上中文条目的变体在变体锚点上多出别名而且进了标题集合` 钉着这条（拿**中文别名**断言）。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q3 — 简介掐两头空白会把数据源的排版吃掉

- **来自：** 票 `offline-chinese-fields/01`
- **类别：** 两份东西矛盾
- **在哪：** `crates/core/src/zh/sync.rs` 的 `entry_of`
- **为什么没停线：** 仓库里别的字段一律 `trim`，而规格 18 要求「简介里的换行、全角空格、
  以及数据源自带的排版原样保留」。Bangumi 的简介开头几乎都是两个全角空格（`　　以细腻的
  画风…`），而 `str::trim` 把 U+3000 当空白扫掉——照惯例写就违反规格。
- **这张票实际做了什么：** 简介**一个字都不改**，两头也不掐；只有整段都是空白时才算没有。
  `zh::sync` 与 `zh::store` 两处各有一条测试钉着那两个全角空格与中间那个换行。
- **谁来裁：** 收尾
- **状态：** open

### Q4 — 「结构版本 +1」原本的处置是报错让用户自己删库

- **来自：** 票 `offline-chinese-fields/01`
- **类别：** 规格没说
- **在哪：** `crates/core/src/zh/store.rs`（原 `StoreError::Version`）
- **为什么没停线：** 票的验收要「旧结构版本的索引自动从本机原件重建」，而现状是
  `Store::open` 直接返回 `StoreError::Version`，文案让用户「删掉它重新跑一次
  `romcat zh sync`」——那要重下 435 MB，正是这份库当初与中立库分家的理由的反面。
- **这张票实际做了什么：** `StoreError::Version` 换成了 `StoreError::TooNew`——只在「库比
  程序新」那一个方向报错（Q7 第 2 条）。旧版本这一侧改成：`Store::open` **一个字都不动**，
  只把上一版的 dump 名与指纹留在 `Store::rebuilding()` 里，交给新增的 `zh::sync::rebuild`
  从缓存目录那份原件重读一遍（`Rebuilt` 三态：不必重建 / 原件不在手边 / 重建好了）；
  换结构与写新数据是 `Store::replace` 那一个事务里的事。**这条路上没有发请求的地方**，
  测试里连 fetcher 都没有。
- **谁来裁：** 收尾
- **状态：** open

### Q5 — 重建折平台名要平台清单，而 `identify` / `scrape` 两个子命令没有 `--manifest`

- **来自：** 票 `offline-chinese-fields/01`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/cli/src/main.rs` 的 `heal_zh_store`、`IdentifyArgs`、`ScrapeArgs`
- **为什么没停线：** 就地重建要把数据源写的平台名折成本工具的平台名，那要一份平台清单；
  这两个子命令的参数表里没有 `--manifest`。加一个命令行开关是 CLI 表面的改动，
  而这张票的风险面本该只在刮削那一侧。
- **这张票实际做了什么：** `heal_zh_store` 收一个 `Option<&Manifest>`：`zh sync` 手上有
  自己那份就传进去（`--manifest` 指得了）；`identify` 与 `scrape` 传 `None`，那时按
  `ManifestArgs` 自己的老规矩解析（工作目录里那份 `platforms.toml`，没有就用内置的），
  **不加新开关**。用户若只在 `identify` / `scrape` 这两条路上要一份自定义清单，
  这一次自动重建折出来的平台名会与那份不一致——他重跑一次 `romcat zh sync --manifest
  <文件>` 就纠正回来了。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q6 — 规格要的「真机验收数字进 `docs/library-facts.md`」这张票交不出来

- **来自：** 票 `offline-chinese-fields/01`
- **类别：** 前提不在（但不挡这张票）
- **在哪：** `.scratch/offline-chinese-fields/spec.md` 的「真机验收」一节、
  `docs/library-facts.md`
- **为什么没停线：** 那要在用户那份 435 MB 的真索引上跑一趟并报数（撞上的变体数、
  各字段补上多少条、按平台的覆盖表）。实现环境里没有那份索引，而票 01 自己的验收条目
  里也没有这一条——它要的是「索引里存得下」。
- **这张票实际做了什么：** 一律用 fixture 测行为，不抄规格里那几个覆盖率数字当验收。
  真机数字留给拿主意的人跑一趟 `romcat zh sync` 之后从 `print_zh_stats` 那几行抄。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q7 — 审查抓出来的四条，一并在这张票里改掉了

- **来自：** 票 `offline-chinese-fields/01`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（但都是这张票自己引进来的）
- **在哪：** `crates/core/src/zh/store.rs`、`crates/core/src/zh/sync.rs`、
  `crates/cli/src/main.rs`、`crates/core/src/title.rs`
- **为什么没停线：** 四条都在这张票自己改动的范围里，改法唯一。
- **这张票实际做了什么：**
  1. **打开一份旧索引不再先扫后建**——`Store::open` 一个字都不动，换结构与写新数据是
     `replace` 那一个事务里的事，`schema_version` 最后才落盘。原先是先 `DROP`（连 `meta`
     一起）再盖新版本号，重建中途 Ctrl-C 就只剩一份空索引，而「从哪份原件重建」的线索
     没了——出路正是重下那 435 MB。
  2. **库比程序新的一律拒绝**（新增 `StoreError::TooNew`）。原先 `version != SCHEMA_VERSION`
     把降级也当成「该重建」，两个版本的程序交替跑会互相把对方的索引毁掉。
  3. **`zh sync --dry-run` 不重建**，并且**等着重建的那一份不许走「指纹没变就跳过」**
     （否则本机没有原件时，那份索引永远补不回来）。重建失败也不再让 `identify` /
     `scrape` 整条命令硬失败——如实说一句，这一趟少一层。
  4. **别名降成低置信**。`title::rank` 里置信度排在优先级表名次**前面**，所以光把
     `中文离线源·别名` 在 `priorities.toml` 里垫到最后拦不住它当显示标题（中置信压过
     一切低置信的中文叫法，包括这个变体自己的文件名）。降到低置信之后它与文件名同档，
     再由那张表分先后。两处的分工在两边都写清了。
     另外把集成测试那条断言换成**中文别名**（原先拿 `Probotector` 断言，它是拉丁字母、
     落在回退链最后一档，那条断言恒真）。
- **谁来裁：** 收尾
- **状态：** open

### Q8 — `scan::tests::断点落在主库内直接拒绝开工` 在这台机器上挂死，与这张票无关

- **来自：** 票 `offline-chinese-fields/01`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/scan/mod.rs:1746` 那条测试、`crates/core/src/path.rs:269`
  的 `normalize_existing`
- **为什么没停线：** 它**不是这张票碰出来的**，那两处代码一个字都没动过。我把 `HEAD`
  原样 `git archive` 到另一个目录、另一份 `target` 单独跑那一条，**照样挂死**。
  机器上还留着一个四小时前起的、卡在同一条测试上的 `romcat_core-*` 进程
  （起得比我这一场开工还早）。
- **成因（看代码看得出来）：** 测试把断点设在 `/lib/.romcat/checkpoint.json`、扫描根设成
  `/lib`，指望那道「断点落在主库内」的闸拦下来。可 `normalize_existing` 走的是**真文件
  系统**，而这台机器（以及一切 merged-usr 的发行版）上 `/lib` 是指向 `usr/lib` 的符号
  链接——它折出来的是 `/usr/lib/.romcat/checkpoint.json`；而扫描根那一侧走的是 `MemFs`
  的 `canonicalize`，折出来还是 `/lib`。两边对不上，闸不响，`scan` 于是真跑起来，
  带着 `interval: Duration::ZERO` 的断点、往一个写不进去的目录里写。
- **这张票实际做了什么：** 没碰它。门禁那条 `cargo test --workspace --all-features` 加了
  `-- --skip 断点落在主库内直接拒绝开工` 跑完，其余全绿；报告与提交说明里如实写着这一条。
- **谁来裁：** 拿主意的人（这条该单开一张票：那道闸在 merged-usr 的机器上本来就是失效的，
  挂死只是它顺带暴露出来的第二个症状）
- **状态：** open

### Q9 — 「变体层必须先于作品层采集」这条我没做成一条采集顺序约束

- **来自：** 票 `offline-chinese-fields/02`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape.rs` 的 `WorkVariant`（新增）与 `Plan::build`、
  `crates/core/src/scrape/zh.rs` 的 `ChineseSource::work_hit` / `work_probe`
- **为什么没停线：** 票与规格钉的是「撞在哪、挂在哪、谁先跑」，而规格自己写了
  「具体怎么把这件事接进计划的构建，交给实现」。照字面做成「引擎先跑完变体锚点、
  作品锚点读变体撞完的结果」会**在第二趟静默毁数据**：第一趟采完之后变体那一层整片
  命中输入指纹（`known.get(source.name()) == Some(&input)` 那一支），`collect` 一次
  都不跑，作品锚点手里就是空的——引擎于是把上一趟好好的类型当成「这个源改主意了」
  清掉（`forget_scraped` 那一支）。
- **这张票实际做了什么：** 作品锚点带的是**够撞一次的那几样输入**（`WorkVariant`：
  变体键、主文件键、平台、DAT 条目）而不是撞完的结果，作品层自己现撞一遍。于是这一层
  **与采集顺序和缓存状态都无关**，同一份库跑几趟结果一样；作品层的输入指纹
  （`work_probe`）盖住名下每个变体的那几样，名下多一个变体、换个平台、多撞出一条 DAT
  条目都会重采。**代价说清楚**：已确认的变体一趟里撞两遍（自己一遍、它所属的作品一遍），
  那是一次内存里的倒排查表，而且第二趟起两边都被指纹跳过。计划里变体仍旧排在作品前面
  （`Plan::build` 的两个循环），只是**没有任何东西依赖这个次序**了。
  **另一笔代价是票 03 的复审量出来的**：`Plan::build` 给每个已确认变体的 DAT 条目在
  `WorkVariant` 里再 clone 一份，与 `slot.entries` 并存——真库 30,024 个已确认变体，
  这一层内存大致翻倍。不是错误（计划本来就整份在内存里），但真库上值得量一次。
  集成测试 `类型落在作品锚点上而且同一条条目只留一份` 第七条钉着「再跑一趟类型还在、
  `forgotten` 是 0」。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q10 — `只在变体这一层说话` 那条测试被拆成了三条

- **来自：** 票 `offline-chinese-fields/02`
- **类别：** 票写对了，但要动已有的测试
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `tests`
- **为什么没停线：** 那条测试钉的正是这张票要推翻的事——中文离线源现在在两层都参加。
- **这张票实际做了什么：** 原来那条 `只在变体这一层说话` 删掉，换成三条：
  `作品那一层不自己撞名字读的是名下变体撞出来的条目`（作品层说话了，而且只有一份类型）、
  `名下一个变体都没撞上的作品什么都不产出`（含「名下一个变体都没有时无话可说」那一档，
  也就是原来那条断言的落点换了个地方）、`别名那一路不跟到作品那一层去`（别名源仍旧
  **只在变体这一层说话**，那道闸从「上一层源的 probe」挪成了它自己的一句
  `kind != Variant` —— 不挪的话，中文名那一路开始在作品层说话，别名就跟着被搬过去了）。
  另加两条：`两个变体撞到不同条目时取最多数`、`平票时取条目号最小的那一条而且与名下的
  次序无关`。
- **谁来裁：** 收尾
- **状态：** open

### Q11 — 一条条目写了好几个类型时，这张票只取头一个

- **来自：** 票 `offline-chinese-fields/02`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `ChineseSource::collect_work`
- **为什么没停线：** 票说「用最简单的**单值字段**（`游戏类型`）把这条路跑通」，而票 01
  已经把 infobox 里一个键装几个值的情形拆成了 `Entry::genres` 这个 `Vec`。两边对不上时
  该听谁的，票没说。
- **这张票实际做了什么：** 走单值那条路（`Harvest::value`），取 `genres` 里**头一个**
  ——数据源里的原次序，同一份库跑两次取的是同一个。理由：`Priorities::merge` 一个字段
  只回一个值，而集合那条路（`Harvest::each` 加 `title::fold`）眼下**只有标题走得通**，
  多写几条类型进库也没人读得出第二条，只会让「谁胜出」重新变成 Q2 那个字典序问题。
  拆好的那几个值仍旧躺在中文索引里，将来要给类型开一条集合的路，不必重新取数。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q12 — 优先级表里类型那条链，中文离线源垫在 `ScreenScraper` 之后

- **来自：** 票 `offline-chinese-fields/02`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/priorities.toml` 的 `类型` 那一节
- **为什么没停线：** 规格说在线源的定位收窄成「图」，但没说要把它在**类型**这一栏上
  降位；而这张表的规则 2 是「没列到的源排在全部列到的之后」，所以不明写就等于垫最后，
  必须做个决定。
- **这张票实际做了什么：** `["裁决", "Pegasus", "ES-Gamelist", "ScreenScraper", "中文离线源"]`
  ——与**标题**那条链上的相对位置一致（那条链上中文离线源也在 ScreenScraper 之后）。
  判据：中文离线源撞的是**名字**不是字节，是模糊匹配来的中置信结论；在线源那一边是按
  哈希确认的条目。**离线档里 ScreenScraper 根本不在场**，所以这个名次不影响用户跑
  `romcat scrape` 默认那一趟——这一栏由中文离线源填，集成测试第六条钉着
  `merged["类型"].source == "中文离线源"`。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q13 — 刮削报告那句「离线档补不上简介、类型与开发商」现在有三分之一是假的

- **来自：** 票 `offline-chinese-fields/02`
- **类别：** 两份东西矛盾（这张票自己造成的，但修它是票 06 的活）
- **在哪：** `crates/core/src/scrape/report.rs:8` 的模块文档
- **为什么没停线：** 报告文案归票 06（`06-report-and-coverage.md`），提前改就是把后面那
  张票的活抢了；而简介与开发商这两样**眼下确实还补不上**（票 03、票 04），整句改准要等
  那两张票落完。
- **这张票实际做了什么：** 报告那一侧**一个字都没动**。只把
  `priorities.toml` 里两处直接与这张票的产出打架的地方改准了：`类型` 那一节的说明，
  以及「眼下有哪些源」那张表里 `中文离线源` 与 `ScreenScraper` 两行。集成测试
  `报告说得出离线档补不上哪些字段` 跑的是 `Naming::off()` 那一趟（手边没有中文索引），
  所以它照旧点名「类型」，没有被这张票改到。
- **谁来裁：** 票 `06`
- **状态：** settled —— 票 06 把那一节整段改了：不再声称离线档补不上简介、类型或开发商，
  如实说**补不上的是图**，并说清那正是在线档存在的理由（见 Q45：那句话与「有没有空字段」
  无关，所以这一节改成无条件打印）。空着的字段照旧点名，但换了措辞——空说的是「没撞上」
  或者「索引还没取」，不是这一档做不到。同一句话在别处的最后两份副本也一起改准了：
  `crates/gui/src/library.rs` 那句「简介是离线源补不上的那一个」，以及 CLI 集成测试
  `不给档案就是离线档_一个网络请求都不发` 里钉着那句假话的断言。
  **台账 `docs/library-facts.md` 里那一节没改**，理由与处置见 Q46。

### Q14 — 新建的中文索引不写 `schema_version`，下次升版会在下完 435 MB 之后硬报错

- **来自：** 票 `offline-chinese-fields/02`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（是票 01 引进来的）
- **在哪：** `crates/core/src/zh/store.rs` 的 `prepare`（那句 `_ => {}`，约 295 行）；
  对照 `crates/core/src/catalog.rs:377` 那一侧的 `None` 分支是写版本号的
- **为什么没停线：** 它在**这张票一个字都没动过的文件**里——票 01 的 Q7 第 1 条把
  `prepare` 改成「打开一份旧索引什么都不毁」时，顺带删掉了原来
  `None => put_meta("schema_version", …)` 那一支（`git show 841a067^:…/store.rs` 的 181 行
  还有它），现在版本号只在 `replace` 末尾落盘。这张票的风险面按规格只该在刮削那一侧，
  而票的收尾指示是「只提交这张票动过的文件」。
- **这张票实际做了什么：** 没碰它，只把它记准：**症状**是「文件已建好、`replace` 还没跑过」
  的那一档（第一次 `romcat zh sync` 中途断网，或先跑了 `romcat zh find`）留下的库里
  `meta` 没有 `schema_version` 这一行；等 `SCHEMA_VERSION` 升到 3，`prepare` 读到 `None`
  → 既不走 `TooNew` 也不置 `rebuilding` → `CREATE TABLE IF NOT EXISTS` 对已存在的 v2 表
  一个字不改 → `reshape` 因为 `rebuilding.is_none()` 直接返回 → `replace` 在
  `INSERT INTO subject(… 新列 …)` 上硬报 `no column named`，而这一下**发生在下完
  435 MB 之后**——恰是票 01 的 Q4 想避免的那个失败。
  **改法就一行**：`_ => {}` 那一支里，`found` 为 `None` 时在 `execute_batch(SCHEMA)`
  之后补一句 `self.put_meta("schema_version", &SCHEMA_VERSION.to_string())?`。
- **谁来裁：** 拿主意的人（该单开一张票，或者并进票 03 那一趟顺手改掉）
- **状态：** settled —— 协调者在票 02 与票 03 之间的间隙改掉了，就是挂单里写的那一行：
  `prepare` 里 `found` 为 `None` 时，`execute_batch(SCHEMA)` 之后补写 `schema_version`。
  见提交 `abd2b1c`。

### Q15 — `romcat zh sync` 里先 heal 再 sync，远端有新版时那一趟解压是白干的

- **来自：** 票 `offline-chinese-fields/02`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（是票 01 引进来的）
- **在哪：** `crates/cli/src/main.rs` 的 `run_zh_sync`（约 4129 行那句 `heal_zh_store`）、
  `crates/core/src/zh/sync.rs:130` 的 `pending` 那道闸
- **为什么没停线：** 同 Q14，在这张票没动过的文件里；而且它只是**慢**，不出错。
- **这张票实际做了什么：** 没碰它，只记准：`sync()` 自己已经有 `pending` 那道闸——
  等着重建的那一份本来就不走「指纹没变就跳过」，会自己 `read_dump` + `replace` 一遍。
  先 heal 的话，heal 把本机那份旧 zip 整份解开读完（435 MB / 解开 960 MB，几分钟），
  紧接着 sync 发现 `latest.json` 是新的一版，下载新档再解一遍，**heal 那一趟的产物当场
  被覆盖**。只有指纹一样时 heal 才是有用的（heal 完 sync 就 skip 了）。两条出路：
  `run_zh_sync` 里干脆不 heal（交给 sync 自己那条路），或者把 heal 挪到「取到 latest、
  确认指纹没变」之后。`identify` / `scrape` 那两条路上的 heal 不受影响，那里没有 sync。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q16 — 中文离线源的输入指纹从来没盖住**剥离规则**

- **来自：** 票 `offline-chinese-fields/02`（复审的产出）
- **类别：** 路过发现，不在范围内（票 11 就有的洞）
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `variant_probe` 与 `work_probe`；
  `crates/core/src/filename.rs` 的 `Rules`
- **为什么没停线：** 两个指纹都盖了 dump、这一版取了哪几样字段、匹配参数，唯独没盖
  `naming.rules`。而**剥离规则「是配置不是代码，用户能补充自己遇到的模式」**
  （`CONTEXT.md` 的「剥离规则」词条）——换一份规则会改变**正题**，进而改变撞出来的条目
  与类型，缓存却会一口咬定「输入没变」而整条跳过。这不是这张票引进来的：
  `variant_probe` 那份清单票 11 就是这么写的，`work_probe` 只是照抄了同一份。补它要先给
  `Rules` 加一个 `fingerprint()`，那是 `filename` 那个模块的公开表面，而这张票的风险面
  按规格只该在刮削那一侧。
- **这张票实际做了什么：** 没补，照抄了变体那一层现有的那份清单——两层一致，将来补
  `Rules::fingerprint()` 时两处一起加一行就行。**眼下的出口是 `--refresh`**
  （`Source::probe` 的文档已经把「盖不住这个源自己的解析逻辑」那一条写在那儿了，
  换规则与换解析逻辑是同一类）。
- **谁来裁：** 拿主意的人（该单开一张票：它同时影响识别那一层与刮削这一侧）
- **状态：** open

### Q17 — 简介不跟着索引进内存，于是这一路多了一个 trait，「本地源永远 `Ok`」那条也跟着松了一格

- **来自：** 票 `offline-chinese-fields/03`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `Summaries`（新增）与 `collect_summary`、
  `crates/core/src/scrape.rs` 的 `RunContext::summaries`（新增）与 `Source` 的 trait 文档、
  `crates/cli/src/main.rs` 的 `open_zh_store`（新增）
- **为什么没停线：** 票 01 落下的存储把简介**特意留在库里那一列上**，不进
  `Store::load`（`load` 的文档：8.7 万条乘中位 338 字是九十来 MB 常驻，而撞名字不看
  简介）。于是刮削这一侧手里那份 `zh::Index` 上 `Entry::summary` **永远是空串**——
  照票 02 那条「从撞上的条目上取一格」的路直接写，跑出来一条简介都没有，而且不报错。
  改法唯一：要么把那九十来 MB 装进内存（推翻上一张票刚做的决定），要么给这一层一条
  按条目号回库里点名的路。
- **这张票实际做了什么：** 走后者。新增 trait `scrape::zh::Summaries`（`fn summary(id)
  -> Result<Option<String>, String>`），`zh::store::Store` 直接实现它；句柄由
  `RunContext::summaries` 传进来，`ChineseSource::with_summaries` 接上。**代价说清楚**：
  一趟刮削多出「作品锚点个数」那么多次 SQLite 点名（真库约 9 千次），换掉的是九十来 MB
  常驻。另有两处连带：
  1. **`Source` 的「本地源的 `collect` 永远返回 `Ok(())`」不再成立**——读那一列是会
     失败的。失败照 `Failure::Skip` 处置（**那一对不写库，下一趟再来**），不吞成
     「这条没有简介」；trait 文档与 `Source` 的文档两边都写清了，两条测试钉着。
  2. **「简介这条路在不在场」进了作品那一层的输入指纹。** 不进的话，用户把这条路接上
     之后重跑，缓存会一口咬定「输入没变」而整条跳过，那些简介永远补不上来。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q18 — 闸设在 4,000 字，这个数是我定的

- **来自：** 票 `offline-chinese-fields/03`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `DESCRIPTION_LIMIT`
- **为什么没停线：** 票与规格都写着「闸设在多少由实现定，但要说得出理由」。
- **这张票实际做了什么：** 定在 **4,000 字**（`char`，不是字节）。三条判据：
  数据源实测中位 338 字、最长 9,962 字，4,000 是中位数的 11 倍，**绝大多数条目一个字
  都不动**；用户手上那份手写元数据简介的中位是 1,354 字，工具产出的东西不该比手写那份
  短，所以闸要高出它一大截；4,000 个汉字 = 12 KB，**一条的上界是死的**，存储与导出都
  不会因为某一条超长条目变得不可用。**闸也进作品那一层的输入指纹**——把它调小了重跑，
  同一条简介该重新截一遍，而不是被缓存跳过。
  票里那几个覆盖率数字**没有拿来当验收**（同 Q6）：手边没有那份 435 MB 的真索引，
  行为一律用 fixture 测。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q19 — 「被截断的条目在报告里点得出名」按**记号**找，不按长度找

- **来自：** 票 `offline-chinese-fields/03`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `TRUNCATED_MARK` 与 `clamp`、
  `crates/core/src/catalog/scrape.rs` 的 `values_marked`（新增）、
  `crates/core/src/scrape/report.rs` 的 `truncated_descriptions`（新增）
- **为什么没停线：** 票要「被截断的条目在报告里点得出名」，而报告是**从中立库折出来
  的、不重跑一遍刮削**（ADR-0001）——所以「哪些被截了」必须是库里读得出来的事实，
  不能靠这一趟的内存。怎么读，票没说。
- **这张票实际做了什么：** 截断这件事**写进值本身**：正文截到闸上，末尾缀一句
  `……〔简介太长：原文 9,962 字，这里留了前 4,000 字。〕`。理由是**值才是导出到前端、
  用户真会读到的那一份**，依据只有回到工具里才看得见——前端里一段话戛然而止，用户没有
  任何办法分辨那是数据源本来就写到这儿，还是工具砍的。报告靠这个记号
  （`Catalog::values_marked`，SQL 的 `instr`）把锚点逐条点出来，文本报告里列前 20 个、
  其余只报个数，`--json` 那一份是全的。
  **不按长度找**：那条判据要求「截到多少字」这个常量与库里的值永远一致，而闸是会调的
  ——调完闸之后，上一趟按老闸截出来的那些行就点不出来了，而它们恰恰最该被点出来。
  另：依据里也写着这一句，两处都有。
- **谁来裁：** 收尾
- **状态：** open

### Q20 — 界面那条验收钉在 `one_line` 上，钉不到画出来的那一帧上

- **来自：** 票 `offline-chinese-fields/03`
- **类别：** 工具缺陷（egui 的，不是这个仓库的）
- **在哪：** `crates/gui/src/library.rs` 的 `one_line` 与 `values_ui`、
  `crates/gui/tests/library.rs` 的两条测试
- **为什么没停线：** 票要「带一条超长简介的库，界面上的列表仍然滚得动」。真正会被
  撑坏的是详情面板右栏那份**刮削来的元数据**清单——它画在一条 `ui.horizontal` 里，
  而**横排不折行**：4,000 个汉字排出来就是四五万点宽的一行，面板跟着长出一条横向
  滚动条。可 egui 的 `Label` 只画视口之内的文字（`ui.is_rect_visible`），而那一栏在
  底部面板（300 点高）里排在标题集合与首选变体之后——headless 的一帧里它**根本没被
  画出来**，量帧里最宽的那块文字也就量不到它。试过三条路都够不着：滚轮事件、把视口
  调高到 2000 点、挑一条没认出作品的变体好让上面两节缩成两行。
- **这张票实际做了什么：** 界面这一侧照改（`one_line`：换行折平、超过 60 字用省略号
  收住，**原文一个字没动**，整段挂在悬停里，用 `on_hover_ui` 让那个闭包只在真悬停时
  才跑）；验收拆成两条：`一条顶到闸上的简介收成一行画得下的那一截` 钉在
  `one_line` 这个纯函数上（因此把它改成 `pub`，文档里写明了为什么），
  `库里有一条顶到闸上的简介时列表照样滚得动` 钉表那一侧（内存里仍是一扇窗、
  240 行只读 2 次库）。**没有一条断言真的看着那一行被画出来** —— 这是这条验收的缺口，
  记在这儿。
- **谁来裁：** 拿主意的人（要补的话，得给详情面板那个 `ScrollArea` 开一个像
  `Screen::scroll_to` 那样的「只有量帧率时才设」的偏移口）
- **状态：** open

### Q21 — 简介那条链上，中文离线源垫在 `ScreenScraper` 之后

- **来自：** 票 `offline-chinese-fields/03`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/priorities.toml` 的 `简介` 那一节
- **为什么没停线：** 同 Q12：这张表的规则 2 是「没列到的源排在全部列到的之后」，
  不明写就等于垫最后，必须做个决定。
- **这张票实际做了什么：** `["裁决", "Pegasus", "ES-Gamelist", "ScreenScraper", "中文离线源"]`
  ——与**标题**、**类型**两条链上的相对位置一致。判据同 Q12：中文离线源撞的是名字不是
  字节，是模糊匹配来的中置信结论。**离线档里 ScreenScraper 根本不在场**，所以这个名次
  不影响 `romcat scrape` 默认那一趟，集成测试钉着 `merged["简介"].source == "中文离线源"`。
  连带把 `priorities.toml` 里两处与产出直接打架的地方改准了：`简介` 那一节的说明，
  以及「眼下有哪些源」那张表里 `中文离线源` 与 `ScreenScraper` 两行。
  **报告那一侧一个字都没动**——那句「离线档补不上简介、类型与开发商」归票 06（Q13），
  而开发商眼下确实还补不上（票 04）。
- **谁来裁：** 票 `06`
- **状态：** open

### Q22 — Pegasus 那一侧的**写出**把单值写成一行，简介里的换行会写坏那份文件

- **来自：** 票 `offline-chinese-fields/03`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（票 16 就有的洞，这张票让它变成常态）
- **在哪：** `crates/core/src/adapter/pegasus.rs:1121` 的 `write_attribute`；
  对照同一个文件里读那一侧的 `joined`（`:502`，`.` 折成 `\n\n`、普通换行折成空格）
  与 `classify`（`:377`）
- **为什么没停线：** 三条判据都不满足。**一、这个洞不是这张票开的**：`joined` 读进来
  的简介本来就带 `\n\n`（用户手写那份 Pegasus 元数据里每条简介都是续行写的），
  `romcat import` 之后再 `emit` 一趟，同样写出一份坏的；ScreenScraper 的 `synopsis`
  也带换行。**二、主库只读**（ADR-0004）——`emit` 写的是导出目标，不是用户那份原件。
  **三、这张票的硬约束里明写着「导出侧一行不改」**（规格：「这次改动的风险面只在刮削
  那一侧」），而修它正是往导出侧加风险。
- **这张票实际做了什么：** 没碰导出侧一行。**把自己那一处收掉了**：截断时缀的那句说明
  原本前面带一个空行（`{head}……\n\n〔简介太长…〕`），现在接在同一行上——数据源自带的
  换行是规格 18 要求原样留着的，而那个空行是我们自己加的，加了它只会给这个洞多开一处
  出口（空行会让 `collect` 的 `open = false`，那一段直接断在简介中间）。
  **症状写准**：`write_attribute` 单值走 `writeln!(out, "{key}: {}", values[0])`，
  值里的 `\n` 于是变成一行**顶格**的文字——`classify` 判它 `Malformed`，那一行里若有
  半角 `:`（简介里的 URL 很常见）还会被当成一个新属性键。
  **改法就在写那一侧**：值里有 `\n` 就写成 `key:` 加每行 `{INDENT}`，空行写成
  `{INDENT}.`——读那一侧的 `joined` 已经认得 `.` 了，两边正好对上，往返也就闭合了。
- **谁来裁：** 拿主意的人（该单开一张票：它同时管住 `import` 来的简介、在线源的简介
  与这张票产出的简介，而且是这一批发现里唯一会写坏一份文件的）
- **状态：** settled —— 协调者在票 03 与票 04 之间的间隙改掉了，就按这条记的改法：
  `write_attribute` 里「单值且不含 `\n`」才写成一行，否则一律走续行、空行写成 `.`。
  往返测试 `adapter::pegasus::tests::带换行的简介写成续行_读回来不是坏行也不冒出新键`
  钉着三件事：没有顶格的裸文字行、读回来没有 `Malformed`、简介里那个半角 `:`
  不冒出新键。见提交 `a9d3fe9`。
  **残留一条**：单个换行读回来折成空格（`joined` 的规矩），只有空行分隔的段落往返得回来。
  那是 Pegasus 这个格式自己的天花板，不是这里的疏漏——**该由能力档位说出口**
  （ADR-0003：「导出前就知道这个格式会丢掉什么」）。这一条另记 Q26。

### Q23 — 中文索引重建时，`reshape` 在 `replace` 那个事务之外提交

- **来自：** 票 `offline-chinese-fields/03`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（票 01 引进来的）
- **在哪：** `crates/core/src/zh/store.rs:316` 的 `reshape`、`:384` 起的 `replace`
- **为什么没停线：** 在这张票一个字都没动过的文件里，而这张票的风险面按规格只该在
  刮削那一侧。
- **这张票实际做了什么：** 没碰它，只记准。`reshape` 用 `execute_batch` 跑
  `DROP TABLE …; {SCHEMA}`，那是**自动提交**的；`replace` 之后才开自己的事务。
  于是 DROP 落盘与 8.7 万条 INSERT 提交之间有一个真实的窗口：升级后跑
  `romcat identify` 触发重建，在写入那几十秒里 Ctrl-C 或进程被杀，库里就只剩一份空的
  新结构表。**`schema_version` 最后才落盘这一点是对的**（票 01 的 Q7 第 1 条），所以
  缓存里那份 zip 还在时下一趟自己会补回来；zip 已经被清掉时才是死路——重下 435 MB，
  恰是这套设计要避免的结局。SQLite 的 DDL 是事务性的，把 DROP/CREATE 挪进同一个 `tx`
  就闭合了，模块文档那句「换结构与写新数据是同一个事务里的事」也就名副其实。
- **谁来裁：** 拿主意的人
- **状态：** settled —— 协调者在同一条缝隙提交里改掉了：`reshape` 改成收一个
  `&Transaction`，由 `replace` 在自己那个事务里调，`DROP` 与几万条 `INSERT` 一起落盘
  或一起回滚。见提交 `a9d3fe9`。

### Q24 — 「别名永远轮不到当显示标题」只在条目的中文名真是中文时成立

- **来自：** 票 `offline-chinese-fields/03`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（票 01 的 Q2 / Q7 第 4 条没盖住的那一角）
- **在哪：** `crates/core/src/title.rs:388` 的 `rank`、`:704` 那段注释、
  `crates/core/src/scrape/priorities.toml` 里标题那一节的 ⚠️
- **为什么没停线：** 这张票的硬约束里明写着「别名在变体锚点上、低置信、不当显示标题，
  别顺手动它」。
- **这张票实际做了什么：** 没碰它，只记准。`rank` 的桶是 `(语言, 类型)`，**排在置信度
  前面**：`(中文, 别名) = 3`、`(中文, 汉化组自取) = 4`、`(英文, 官方名) = 5`，
  而「别名 + 非中文」落到 `_ => 7`。中文数据源里对西方游戏常有 `name_cn` 就是一串拉丁字
  （`name_cn = "Minecraft"`）的条目——那时中文名那一路进 7 桶，而别名里那个中文叫法
  （`我的世界`）进 3 桶，于是**低置信的别名成了显示标题**，还压过 4 桶的汉化名。
  `title.rs:709` 与优先级表里那两段都断言这不会发生。两条出路：把判据改成「别名那一路
  一律垫到汉化组自取之下」，或者把这条限制如实写进那两处注释。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q25 — 中文索引就地重建时一句话都不打，用户看见的是一个像死掉了的进程

- **来自：** 票 `offline-chinese-fields/03`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（票 01 引进来的）
- **在哪：** `crates/cli/src/main.rs` 的 `heal_zh_store`（那句 `Rebuilt::Done` 的
  eprintln 在重建**跑完之后**才发）、`open_zh_store` 与 `run_zh_find` 两个调用点
- **为什么没停线：** 只是没说话，不出错；而且它在票 01 的代码里。
- **这张票实际做了什么：** 没碰它。这张票把 `load_zh_index` 拆成
  `open_zh_store` + `load`（刮削那一趟要留着库句柄读简介），`heal_zh_store` 的调用位置
  与时机**一个字都没变**。记准症状：`zh::sync::rebuild` 要读+解压 960 MB 再整份写库，
  几分钟起步，这期间标准错误上一个字都没有；而且这一趟不看取消令牌，`identify` 的
  Ctrl-C 管不到它。改法：开工**之前**先打一句「结构版本 X → Y，正在从本机那份原件重建，
  不会下载任何东西」，并把 `cancel` 传进去。
- **谁来裁：** 拿主意的人（与 Q15、Q23 同属「中文索引重建这条路」，该并成一张票）
- **状态：** open

### Q26 — Pegasus 存不下单个换行，这件事还没由能力档位说出口

- **来自：** 票 `offline-chinese-fields/03` 的 Q22 裁决（协调者）
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/pegasus.rs` 的 `write_attribute` 与 `joined`、
  `crates/core/src/capability.rs`（能力档位那一套）
- **为什么没停线：** Q22 的修复已经让写出来的文件**读得回来**了，这一条是它的残留：
  简介里单个 `\n` 读回来是一个空格，只有空行分隔的段落往返得回来。中文离线源的简介
  两种换行都有，所以真库上会有一批条目的简介往返一趟变了形。
- **这张票实际做了什么：** 只把它记准，没动能力档位。ADR-0003 说的正是「每个适配器
  标着能力档位：导出前就知道这个格式会丢掉什么」——单个换行折成空格该在那张表上有名字，
  而不是让用户自己发现。
- **谁来裁：** 拿主意的人（够单开一张小票；也可以并进票 06「报告说实话」那一趟，
  它本来就在改「这个格式补不上什么」的措辞）
- **状态：** open

### Q27 — 开发商与发行商拆成多条进库，可导出那一侧眼下只读得出一条

- **来自：** 票 `offline-chinese-fields/04`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `ChineseSource::collect_facts`（新增）、
  `crates/core/src/scrape.rs` 的 `Harvest::each` 文档、
  `crates/core/src/adapter/converge.rs` 的 `build_game`（**没动，问题在这儿**）
- **为什么没停线：** 票的验收明写着「顿号分隔的一行拆成**多条**」「多值块拆成**多条**」，
  而 Q11（票 02 定的：一条条目写了好几个类型时只取头一个）的理由恰恰是「多写几条进库
  也没人读得出第二条」。两条摆在一起时该听哪一条，票没说；而「导出侧一行不改」是这张票
  的硬约束，所以那一侧本来就不许在这一票里动。
- **这张票实际做了什么：** 照票走——`Harvest::each`，一个值一条。**与 Q11 不冲突**：
  那是票 02 拿类型跑通骨架时的最小做法，这一票的验收把话说反了，而说反的那一条是票。
  **代价说清楚，这也是这条挂单的全部内容**：中立库里一个作品的开发商现在可能有好几行，
  而导出那一侧 `build_game` 里那个 `pick(Field::Developer)` 只挑得出一行——挑的是
  `Priorities::pick` 第四层排序键定的那一行，而那一层比的是**值本身**，也就是**码位序**
  （前三层——表里的名次、采集时刻、源名——同一个源的几条值上完全平手）。于是
  `|开发= 科乐美、KCE东京` 导到前端里写的是 `KCE东京`：**换的不是次序，是换了一家公司**。
  结果是确定的（同一份库跑两次一样），但它不是「第一家开发商」。
  **`/code-review` 把这一条判成中等，我认下这个判词，但没有在这一票里修**：修它只有两条路，
  一条是让导出那一侧按集合读这两栏（`Game::developers` / `publishers` 本来就是
  `Vec<String>`，缺的只是把 `pick` 换成一次按字段的收集），另一条是把
  `zh::store` 那张 `subject_fact` 的 `ord`（数据源原次序，一直在库里躺着）一路带到
  `ScrapedValue` 上、折进 `Priorities::pick` 的第四层键。**两条都伸到「导出侧一行不改」
  这条硬约束之外**，而且后一条要动中立库的表与那层排序键——那层排序键管着全部字段，
  标题集合也在里面。
  **顺带把被这一条推翻的注释就地改准了**：`crates/core/src/zh/store.rs` 里 `subject_fact`
  那句「按字典序重排等于换一家公司」原本是在说明 `ord` 为什么必须存，而这一票之后，
  它描述的正是现在真的在发生的事——留着不改，下一个人会照着它推理出「导出取的是第一家」。
  集成测试 `开发商与发行商落在作品锚点上而且一个键写了几个值就拆成几条` 钉着库里那几条。
- **谁来裁：** 拿主意的人（够单开一张导出侧的小票；也可以并进将来「标题集合那一套推广到
  别的集合字段」那条线）
- **状态：** settled —— 协调者在票 04 与票 05 之间的间隙走了**这条挂单说的第一条路**：
  新增 `Priorities::pick_all`（先按同一套优先级选出**哪个源说了算**，再把那个源在这个
  字段上的话全都交出来，**不跨源合并**），`converge::build_game` 里开发商与发行商两栏
  改用它。`Game::developers` / `publishers` 本来就是 `Vec<String>`，导出侧的形状一直
  装得下，缺的只是这一步。两条单元测试钉着：`集合字段交出胜出那个源说的全部值而不是挑一条`、
  `集合字段不跨源合并_输的那个源一条都不带出来`。见提交 `79f67f3`。
  **没走第二条路**（把 `subject_fact.ord` 折进 `Priorities::pick` 的第四层排序键）：
  那层键管着全部字段、标题集合也在里面，为一个次序问题去动它不划算。
  **残留**：多个值之间仍是码位序，不是数据源原次序。**少一家比排错序坏得多**，先把
  「全都在」做到；原次序要不要一路带过来，仍留给拿主意的人。

### Q28 — 优先级表里开发商与发行商两条链，中文离线源垫在 `ScreenScraper` 之后

- **来自：** 票 `offline-chinese-fields/04`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/priorities.toml` 的 `开发商` 与 `发行商` 两节
- **为什么没停线：** 这张表的规则 2 是「没列到的源排在全部列到的之后」，所以不明写就等于
  垫最后，必须做个决定；而规格只说在线源的定位收窄成「图」，没说要在这两栏上给它降位。
- **这张票实际做了什么：** 两条链都把 `中文离线源` 加在 `ScreenScraper` 之后，与**标题**、
  **类型**、**简介**三条链上的相对位置一致（同 Q12、Q21）。判据也一样：中文离线源撞的是
  **名字**不是字节，是模糊匹配来的中置信结论；在线源那一边是按哈希确认的条目。
  **两条链的实际效果并不一样，这一点写进了表里的说明**：开发商那一栏 DAT 各家一条都
  给不出、`ScreenScraper` 在离线档里又不在场，所以默认那一趟由中文离线源填；而
  **发行商那一栏 TOSEC 是在场的、而且排在前面**，所以默认那一趟多半仍由 TOSEC 说了算，
  中文离线源只在 TOSEC 认不出那个文件时才轮得到（这个库里 16,420 个变体正是这个处境）。
  集成测试第六条钉着这两件事：`merged["开发商"].source == "中文离线源"`、
  `merged["发行商"].source == "TOSEC"`。
  **顺带**：Q13 记的那句「离线档补不上简介、类型与开发商」，票 04 之后**三样全部为假**。
  **报告里那句仍旧留给票 `06`**（那张票还要连带做按平台的覆盖表）；但同一句话的另外几处
  副本这张票就地改了——`crates/cli/src/main.rs` 的 `Scrape` 与 `--profile` 两行帮助
  （用户看得见，而票 03 落简介时改的也正是这一行）、`scrape.rs` 与 `online.rs` 两处模块
  文档、以及 `ScreenScraper` 那个源自己的文档。留着它们等票 06，等于让这一票的模块文档
  当场自相矛盾。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q29 — 「这一层产出哪几个字段」得单独进作品锚点的输入指纹，索引那一行盖不住

- **来自：** 票 `offline-chinese-fields/04`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `WORK_FIELDS`（新增）与 `work_probe`
- **为什么没停线：** 规格写着「新增的字段解析要跟着进指纹」，而票 02 与票 03 各自靠着
  别的东西满足了它：票 02 靠 `zh::store::FIELDS` 那一行跟着票 01 变了，票 03 靠新增的
  「简介：在/不在」那一格。**票 04 两样都不占**——开发商与发行商票 01 就已经存进库了，
  `FIELDS` 一个字都不变，匹配参数也没动。照原样交出去，票 03 那一版采过的作品会被
  `known.get(source.name()) == Some(&input)` 一口咬定「输入没变」而整条跳过，这两栏
  永远补不上来，而且**不报错**。
- **这张票实际做了什么：** 新增 `const WORK_FIELDS: [Field; 4]`（类型、简介、开发商、
  发行商），把它的字段名拼进 `work_probe`。于是「这一层取哪几样」自己就是一份指纹输入，
  下一次多接一个字段上来时不必再想一遍这件事。**这一行与真的产出对不对得上有测试钉着**
  （`这一层产出的字段与进指纹的那一行对得上`）——它只拦得住「产出了没写进这一行」，
  拦不住反过来，所以那个常量的文档里明写了「多产出一个字段这里要跟着加一行」。
- **谁来裁：** 收尾
- **状态：** open

### Q30 — `zh::store` 两处文档还写着「数据整批扫掉」，而 `a9d3fe9` 之后早就不扫了

- **来自：** 票 `offline-chinese-fields/04`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/zh/store.rs` 的 `Rebuilding` 文档（约 152 行）与
  `Store::open` 文档（约 206 行）
- **为什么没停线：** 两处说的都与实现相反——`prepare()` 撞上旧结构版本是**一个字都不动**
  地早返回，换结构挪进了 `replace` 那个事务（同一个模块的模块文档写的是对的：
  「旧数据一直留到新数据真的写进来那一刻」）。这是协调者那次挪动（Q23）漏改的旧话，
  与这张票要做的事没有关系，而这张票只该提交它自己动过的文件。
- **这张票实际做了什么：** 没改，只记在这儿。同一个文件里被**这张票**推翻的那一句
  （`subject_fact` 的 `ord`，见 Q27）就地改准了——那句是这一票自己造成的，这两句不是。
- **谁来裁：** 拿主意的人（与 Q15、Q23、Q25 同属「中文索引重建这条路」，该并成一张票）
- **状态：** settled —— 协调者在同一条缝隙提交里改准了两处：`Rebuilding` 那句改成
  「旧数据还在，换结构与写新数据是 `replace` 那一个事务里的事」，`Store::open` 那句
  拆成旧版本（一个字都不动、只记下重建要用的两样）与新版本（`TooNew` 报错）两条。
  这两句过期正是 `a9d3fe9` 留下的，所以由协调者收。见提交 `79f67f3`。

### Q31 — `romcat zh sync --full` 撞上等着重建的索引时，那份 435 MB 原件解两遍

- **来自：** 票 `offline-chinese-fields/04`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/cli/src/main.rs` 约 4156 行，`heal_zh_store` 与紧接着那句 `sync()`
- **为什么没停线：** `heal_zh_store` 先从本机原件整份重建一次（读 zip、写 8.7 万条），
  紧接着 `sync()` 因为 `--full` 永远不走「指纹没变就跳过」，把同一个文件又读一遍、
  `replace` 一遍。多花的是分钟级的白工，不是错误结果；而这是票 01 落下的代码，
  这张票不该顺手改另一张票的控制流。
- **这张票实际做了什么：** 没改，只记在这儿。修法一句话：`heal_zh_store` 那一步加个
  `!args.full` 的闸——`--full` 那一档本来就要整份重读，重建这一趟是纯白工。
- **谁来裁：** 拿主意的人（同上，并进「中文索引重建这条路」那张票）
- **状态：** open

### Q32 — Pegasus 往返会吃掉简介开头那两个全角空格，`trim_start` 把 U+3000 也当空白

- **来自：** 票 `offline-chinese-fields/04`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（与 Q26 同一族：Pegasus 这个格式的天花板）
- **在哪：** `crates/core/src/adapter/pegasus.rs` 的 `classify`（约 385 行）里那句
  `raw.trim_start()`，与 `write_attribute` 新写的续行（约 1130 行）
- **为什么没停线：** Q22 的修复让带换行的值**读得回来**了，这一条是它旁边的另一个洞：
  读续行时 `trim_start` 掐前导空白，而 Rust 把 U+3000 也算空白——于是简介开头那两个
  **全角空格**在「导出到 Pegasus 再读回来」这一趟里没了。规格 18 与挂单 Q3 为它在中立库
  与界面两侧都逐字保住了，唯独这一趟丢。这是 Q22/Q26 那条线上的事，与开发商发行商无关。
- **这张票实际做了什么：** 没改，只记在这儿。**这多半不是代码能修的**：前导空白与
  「续行的缩进」在这个格式里长得一模一样，分不开；该做的是让**能力档位**把它说出口
  （ADR-0003：导出前就知道这个格式会丢掉什么），与 Q26 那条「单个换行折成空格」并成一条。
- **谁来裁：** 拿主意的人（并进 Q26 那一张）
- **状态：** open

### Q33 — 「一条裁决管多大范围」落成沉淀库里的**第二张表**，不是 `Decision` 的第四档

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 规格没说
- **在哪：** `crates/core/src/verdict.rs` 的 `MIGRATIONS[1]`（新增）、`MatchVerdict`、
  `MatchIndex`、`MatchRow`
- **为什么没停线：** 票钉的是「裁决的粒度该是**这次匹配对不对**」与「裁决记的仍是**内容锚**」，
  没说它住在哪张表上；而这是个必须当场做的决定。
- **这张票实际做了什么：** 新增迁移**第 2 条**建 `match_verdict`，键是
  **「锚 × 源 × 条目号」**。**没有并进 `verdict` 那张表**：那张表一条锚只允许一个答案
  （`Decision` 是枚举、`put` 是覆盖，因为「这份内容是什么」只有一个答案），而一份内容上
  **可以有好几次匹配**——这个源撞出条目 4、那个源撞出条目 9，各裁各的。混进同一张表就得在
  唯一索引上二选一：要么把好几个源挤成一条，要么放开唯一性让「这是什么」也攒得出两条打架的
  记录。**沉淀库不可再生这条纪律一个字都没松**：走的是顺序迁移，一次都没要求删库，
  `库比程序新时如实拒绝而不是叫人删库` 那条测试照旧绿。
  连带：导出格式版本 **1 → 2**（多了 `matches` 那一栏）。**往前兼容**——第 1 版的文件照读，
  只是那一批是空的（`第一版的裁决文件照读只是没有匹配裁决` 钉着）；**往后如实拒绝**——
  一份第 2 版的文件里可能装着老程序读不出来的匹配裁决，静静丢掉比读不了更糟。
  **导出时盖第几版看内容**（`Export::version_for`，`/code-review` 报的第 5 条）：
  一条匹配裁决都没有的那一份仍旧盖 **1**。版本号在这个格式里的唯一作用是那道往后拒绝的
  闸，无条件盖 2 会让升级之后导出的**每一份**文件都被老程序拒收，而它其实一个字都读得懂。
  **装着新东西的才该拦下，空的不该。**
  **词表这一侧记一笔**：`CONTEXT.md` 里只有**裁决**，没有「匹配裁决」。按
  `docs/agents/domain.md`，词表里没有的概念要么是发明了新语言、要么是真缺口——
  我判它是**裁决的一种特化**（「在候选之间做出选择，或者判定都不对」，只是对象从识别的候选
  换成了刮削那一次匹配），所以没有自己去改词表，留给拿主意的人决定要不要立词条。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q34 — **肯定**裁决不「钉住」条目号，只在候选里把它排到最前

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `ChineseSource::hit` 里那个排序键
- **为什么没停线：** 票写着「对中文名下一次**肯定**裁决，同一次匹配带来的其余字段一并定下」
  与「裁决之后重跑刮削，结论稳定」，但没说**匹配器根本不再撞出这条条目**时该怎么办
  （换了一版 dump、调了门槛都会走到这一步）。
- **这张票实际做了什么：** **不凭空造匹配**。人说过「就是这条」的那个条目号，只在匹配器
  **交出来的那批候选里**排到最前（排序键是`（人说过就是它, 相似度）`）；匹配器压根没撞出它时，
  这个源照旧按算出来的那一条说话，而那一条重新进待确认队列。判据是这一票的硬约束
  **「匹配算法本身不动」**——凭空产出一条匹配器没给的结论，就是在算法之外造数据，
  而这个项目对「编数据」的态度是「宁可留空」（调研 §13.1）。
  测试 `人说过的那一条排在机器挑的前面` 钉着这一条的两半。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q35 — **否定**裁决划掉的是「这条条目」，不是「这个变体从此没有中文条目」

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `ChineseSource::hit` 里那道 `continue`
- **为什么没停线：** 票写「对中文名下一次**否定**裁决，同一次匹配带来的其余字段一并失效」，
  没说**下一顺位的候选**该不该顶上来。
- **这张票实际做了什么：** **顶上来**，而且顶上来的那一条仍旧是**中置信**、仍旧进待确认队列。
  判据是人只说了「4 不对」这一句，没说「这个文件没有中文条目」——后一句是另一种裁决，
  这一票没有为它开档。好处是连着裁几次就能把一个变体上错的候选一条条筛掉；代价是一次否定
  之后队列里可能立刻冒出另一条要裁的。测试
  `否定的是这一条条目不是这个变体从此没有中文条目`。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q36 — 否定裁决**就地清库**，作品那一层可能连带短暂少几栏

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `judge` 与 `clear_source`
- **为什么没停线：** 「一并失效」这四个字要求错的东西不许在库里多躺一秒——那些值带着一条
  指向人已经说了不对的条目的**依据**。
- **这张票实际做了什么：** **否定**那一档就地清掉三处：变体锚点上中文名与别名两个源的行、
  作品锚点上中文离线源的行（清作品那一层之前**先按条目号确认**那几栏真是这一次匹配说的——
  作品层的答案是名下变体数票数出来的，胜出的可能是另一条条目）。**肯定那一档一个字都不清**，
  值是对的，变的只是它们的依据，而那靠输入指纹在下一趟改写。
  **代价说清楚**：作品名下还有别的变体撞着同一条条目时，那几栏会一直**缺到下一趟
  `romcat scrape`** ——那一趟按剩下的变体重新数票，缺的会回来而且是对的。集成测试
  `一条否定裁决管住同一次匹配带来的全部字段` 的第五、六条把这两半都钉住了。
  **`/code-review` 报的第 2、3 条在这里收掉了**：作品那一层**只在「这个变体自己撞的
  就是这一条」时才清**（`Judged::from_variant`）。不加这道闸的话，拿一个撞的是别的条目
  的变体去裁作品那一层数票胜出的那条，会把那几栏清掉、而下一趟名下真正撞它的变体照旧
  投回来——用户看到的是「删了一次、然后什么都没变」，而命令行还报了「连作品那一层一起」。
  现在这一档一个字都不清，命令行明说「这条裁决管不到那一层，去裁那个变体」，
  报出来的条数也按**真删掉的那批**算。集成测试
  `裁一条这个变体自己没撞上的条目不会白清作品那一层`。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q37 — 内容锚**反查**变体走中立库一条新查询，连带加了两条索引

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/catalog/identify.rs` 的 `Catalog::variants_with_content`（新增）
  与索引 `content_hash_print`、`crates/core/src/catalog.rs` 的索引 `container_entry_print`、
  `crates/core/src/scrape/zh.rs` 的 `Rulings::resolve`
- **为什么没停线：** 票的第 6 条验收要求裁决钉在**内容锚**上，而刮削那一侧手里只有**变体的键**
  ——两头必须接得上。正着走（为每个变体取一次内容判据）是真库 46,444 次、每次两查，
  而**刮削那一侧本来就特意不走这条路**（`Plan::build` 里那句「变体这一层不带判据」，票 14 定的）。
- **这张票实际做了什么：** **反着走**：拿手里那几条裁决（人一条条裁出来的，量级几百到几千）
  去问中立库「谁装着这份内容」，两张表都问——裸文件的判据在 `content_hash`，透明容器里零解压
  就有的在 `container_entry`。查出来的那几个变体再用 **`identify::content_print` 复核一遍**：
  那一层才是「谁代表这个变体」的唯一说法，在这儿另写一遍迟早会漂开（不复核的话，两个变体里
  同一份说明文件会让裁决串到隔壁去）。
  两条索引都是 `CREATE INDEX IF NOT EXISTS`，**没有加 `catalog::SCHEMA_VERSION`**——
  中立库虽然可再生，但为两条白拿的索引让用户重扫 10 TB 不划算。代价：老库第一次打开时
  要建这两条索引。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q38 — 裁决那一格**无条件**进指纹，于是这一版第一次跑会把中文离线源那两路整片重采一遍

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `variant_probe` 与 `work_probe`
- **为什么没停线：** 不进指纹的话，人裁完重跑一趟，缓存会一口咬定「输入没变」而整条跳过——
  那正是票的第 5 条验收（「重跑刮削，结论稳定」）要拦的事。
- **这张票实际做了什么：** **无条件**把裁决那一格拼进两层的指纹（没人裁过时是空串），
  而不是「有裁决才拼」。后者省得下这一趟重采，但要读代码的人自己推演「非空→空会不会撞回
  老指纹」——而那种推演正是这一类缓存 bug 的来源（Q9 与 Q29 各栽过一次）。
  代价是这一版程序第一次跑时，中文离线源与别名那两路的全部锚点各重采一遍：**离线的、
  一个网络请求都不发**，真库上是几万次内存里的倒排查表。
- **谁来裁：** 收尾
- **状态：** open

### Q39 — 入口给在命令行（`romcat zh matches` / `judge`），界面那一侧一行都没动

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 规格没说
- **在哪：** `crates/cli/src/main.rs` 的 `ZhCommand::Matches` / `ZhCommand::Judge`
- **为什么没停线：** 票的验收说「**队列里**一个变体的待确认项看得出……」「在队列里下一次裁决」，
  而**界面正在被另一条队列整个重排**（`gui-redesign` 12 张票，`585c17f` 已经把主库改成一组根）。
  在旧屏幕上长一块这一票的 UI，下一张票就要拆掉。
- **这张票实际做了什么：** 领域那一半**全落在核心库**——`scrape::zh::matched_groups`
  （按条目号归堆，队列「看得出」靠它）与 `scrape::zh::judge`（落裁决 + 就地清库）；
  命令行给两条命令当入口。界面接不接、接在哪一屏，留给 `gui-redesign` 那条线——
  它接的时候一条领域逻辑都不用写（ADR-0005：GUI 只是窗口壳）。
- **谁来裁：** 拿主意的人（或 `gui-redesign` 那条线上的某一张票）
- **状态：** open

### Q40 — 刮削那一趟**打不开沉淀库就停下**，不再降级成「没人裁过」

- **来自：** 票 `offline-chinese-fields/05`（`/code-review` 的产出）
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/cli/src/main.rs` 的 `run_scrape` 里那段 `Rulings::resolve`
- **为什么没停线：** 我第一版写的是 `Err(_) => Rulings::none()`，理由写的是「沉淀库还不在
  （谁也没裁过）」——**而那条理由是假的**：`open_store` 建得出库，谁也没裁过时它建一份空的
  就成功了。真走到错误那一支的是别的原因，最典型的是**库比程序新**（两个版本的 romcat
  轮流在同一个工作目录上跑，`zh::store` 那段 `TooNew` 注释描述的正是这个场景），
  还有库损坏、工作目录只读。
- **这张票实际做了什么：** 改成**停下**。那几种情况下当成「没人裁过」跑下去，会把否定掉的
  中文名整片撞回来、把肯定过的依据改写回「一律进待确认队列」——正是沉淀库那条「库比程序
  新时如实拒绝、绝不将就」要拦的事；别处都停线，这里不该是唯一静悄悄降级的地方。
  错误话里点名「沉淀库里装着你的裁决，读不到就等于把裁过的那些当成没裁过」。
- **谁来裁：** 收尾
- **状态：** open

### Q41 — 作品级那四栏的章盖在**人裁过的那个变体**上，代表变体多了一层排序键

- **来自：** 票 `offline-chinese-fields/05`（`/code-review` 的产出）
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `ChineseSource::work_hit` 里挑代表那一段
- **为什么没停线：** 作品级那四栏的依据是从**代表变体**那一条上抄下来的（票 02 定的形状），
  而「这一次匹配盖没盖过章」正写在那句话的末尾。代表原本只按`（相似度, 变体键）`挑——
  人裁的是甲、而乙分数更高时，那四栏照旧写着「一律进待确认队列」，**一条裁决管住全部字段
  当场落空，而且不报错**。我原来的集成测试之所以绿，只是因为被裁的那个变体恰好在键序上
  排第一而分数平手。
- **这张票实际做了什么：** 排序键前面加一层「**人裁决过这一次匹配的排最前**」，
  相似度与变体键降为第二、第三层（后两层一个字都没动，它们保证的是「与摆进来的次序无关」）。
  单元测试 `作品那一层的章盖在人裁过的那个变体上而不是分数最高的那个` 专门裁**键更大**的
  那一个，钉住这一层真的在起作用。
  **数票那一层没动**：胜出哪条条目仍旧按票数多寡（票 02 定的），人裁过的那一条不因此
  多一票。理由是那是票 02 的骨架，这一票的硬约束是不改它的形状；代价是「人裁了甲的条目 4、
  而名下三个变体都撞条目 6」时，作品那一层仍旧是 6。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q42 — 「从中文离线源补上了多少条」只报一个数就会撒谎，这一节报的是三个

- **来自：** 票 `offline-chinese-fields/06`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/report.rs` 的 `ZhFieldRow`（新增）与
  `ScrapeReport::render_chinese`（新增）
- **为什么没停线：** 票要「列出这一趟从中文离线源补上的字段与各自条数」，而「条数」有三个
  都说得通的口径：落在几个**锚点**上、值一共几条、**合并之后**在几个锚点上胜出。
  Q28 已经量过：发行商那一栏 TOSEC 在场而且排在优先级链的前面，中文离线源常常一条都不
  胜出（这个库里只有 TOSEC 认不出的那 16,420 个变体才轮得到它）。只报贡献，报告就是在替
  一个几乎不出场的源邀功——而这张票的名字叫「报告说实话」。
- **这张票实际做了什么：** 三个都报，一行一个「源 × 字段」（别名那一路另占一行，它是
  另一个源名）。表下写明「**贡献与胜出是两回事**」，以及「值多于锚点是对的」（一个键写了
  几个值就拆成几条、一条条目给得出好几个别名）。集成测试
  `报告数得出中文离线源补上了哪几个字段各多少条` 钉着这两件事：开发商胜出 1、
  **发行商胜出 0**。
- **谁来裁：** 收尾
- **状态：** open

### Q43 — 按平台那张覆盖表的分母，票没说取哪一个

- **来自：** 票 `offline-chinese-fields/06`
- **类别：** 规格没说
- **在哪：** `crates/core/src/catalog/scrape.rs` 的 `source_by_platform` 与
  `SourceCoverage`（都是新增）、`crates/core/src/scrape/report.rs` 的 `ZhPlatformRow`
- **为什么没停线：** 「按平台列出中文离线源的覆盖」——覆盖是个比值，分母至少有三种取法：
  库里那个平台的变体数、这一趟采过的锚点数、撞了 DAT 的那些。而**作品那一层根本按平台
  归不动**（一部作品跨平台，那正是它挂在作品这一层的理由）。
- **这张票实际做了什么：** 分母取**库里那个平台的变体数**，分子是「身上有中文离线源给的值
  的变体数」，**只数变体这一层**——撞只发生在那儿（票 02 的骨架），作品级那几栏是顺着名下
  变体撞到的条目号推上去的，变体撞得上多少就是它们的上限。判据：报告是从中立库折出来的
  （ADR-0001），而「这一趟看了几个」是 `PlanCounts` 那一侧的口径；两个口径混进一张表，
  读的人无从判断 40% 说的是「四成撞上了」还是「采过的里头四成撞上了」。表下把分母是什么
  写出来了。**合计行单独存一格**（`zh_total`）而不是让读的人自己去加：`--json` 那一份里
  「撞上的变体数」是规格「真机验收」点名要的一个数，加出来的与印出来的万一对不上，谁也
  说不清哪个是真的。平台空着的那一行照样在表里（同 `chinese_by_platform`），
  单元测试 `按平台数覆盖时同一个变体上几条值只算一次而平台空着的那一行照样在` 钉着它。
- **谁来裁：** 收尾
- **状态：** open

### Q44 — 票说「这一趟」补上了多少，报告给的是「库里现在有多少」

- **来自：** 票 `offline-chinese-fields/06`
- **类别：** 票写错了（与 ADR-0001 相悖）
- **在哪：** `crates/core/src/scrape/report.rs` 的 `render_chinese`
- **为什么没停线：** 刮削报告**从中立库折出来、不重跑一遍刮削**（ADR-0001），它印的每个数
  都是全库口径——「按字段」那一节一直就是这样。真按「这一趟新增了几条」报，得在采集那一侧
  另攒一份计数，而**第二趟整片命中缓存**：同一份库连跑两趟，第二趟会报「补上了 0 条」，
  那才是真的误导。
- **这张票实际做了什么：** 两节都按全库口径报，措辞里不写「这一趟」——空的时候那句话是
  「**库里**一条都没有」。全篇唯一说「这一趟」的是网络请求那一句，而那一句确实是这一趟的
  事实（离线档的闸门查的是每个源自报的本地还是联网）。
- **谁来裁：** 收尾
- **状态：** open

### Q45 — 缺口那一节改成**无条件**打印：「补不上的是图」挂不到「有没有空字段」上

- **来自：** 票 `offline-chinese-fields/06`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/scrape/report.rs` 的 `render_text`（原来那一节是
  `if !self.gaps.is_empty()` 才打）
- **为什么没停线：** 验收第 2 条要缺口那一节「如实说明离线档补不上的是**图**」，而这句话
  与 `gaps` 空不空毫无关系：**图根本不是字段**，它永远不会出现在那份清单里（`gaps` 是
  `Field::all()` 里一个值都没采到的那些）。挂在原来那个条件上，字段都采齐的库反而一个字
  都看不到这句话——而那正是最该看到它的那种库。
- **这张票实际做了什么：** 这一节改成无条件打印（标题就叫「缺口」），
  里头两段：空字段那一段照旧（有就列、没有就说「没有」），**图那一段照打**，离线档与在线档
  各一套措辞。同时把那句假话删了——「本地数据源里没有这些东西。换 `--profile 在线` 跑一趟
  才补得上」换成「**这不等于「离线档补不上」**……先跑一次 `romcat zh sync`」。离线档那一段
  还印出**网络请求数是 0**，好让规格「真机验收」点名要的那个数报告自己说得出。
  CLI 的集成测试 `不给档案就是离线档_一个网络请求都不发` 跟着改了断言（它原本钉的正是
  那句假话）。
- **谁来裁：** 收尾
- **状态：** open

### Q46 — 真机那两条验收交不出来，而 `docs/library-facts.md` 里那一节还写着那句假话

- **来自：** 票 `offline-chinese-fields/06`
- **类别：** 前提不在（同 Q6）
- **在哪：** 票 06 的验收第 5、6 条；`docs/library-facts.md:395` 的
  「### 离线档补不上的三样」（在「## 刮削：离线档第一次跑（2026-09-01，票 13）」底下）
- **为什么没停线：** 那两条要在用户那份 435 MB 的**真索引**上跑一趟并报数，而实现环境里
  没有那份索引，`romcat zh sync` 要联网取。Q6 记的就是这件事，票 01 已经撞过一次。
- **这张票实际做了什么：** 交的是**报告报得出这些数的能力**（代码，加上用 fixture 钉住的
  测试），**一个数都没往台账里写**——`docs/library-facts.md` 是真库实测台账，往里写没跑过
  的数就是掺假；规格里 `dump-2026-09-01` 那几个覆盖率（94.2% / 99.7% / 83.8%）也没抄进来，
  规格自己写着「以实现时跑出来的数为准，别抄这里的」。
  **台账里那一节一个字没动**：它写着「开发商、类型、简介——本地数据源里没有」，票 02 至
  票 04 之后三样全部为假。这张票收到的指示是这份台账**只看不写**，所以留在那儿没碰；
  **跑真机那一趟顺手把它一起改掉**——新数正好接着写在同一节底下。
- **谁来裁：** 拿主意的人（跑一趟 `romcat zh sync` 再跑 `romcat scrape`，从报告里抄四样：
  `zh_total.matched`（撞上的变体数）、「中文离线源补上了什么」那张表、「中文离线源按平台的
  覆盖」那张表、以及缺口那一节印的**网络请求数 0**）
- **状态：** 部分 settled —— 协调者在票间隙把 `docs/library-facts.md` 那一节的**假话**
  改掉了（标题改成「离线档补不上的是**图**」，加一段 2026-09-04 的更正说明错因与错的
  代价，指向这六张票的提交区间），并明写「这一节的实测数字还欠着，一个数都不写」与
  补数的办法。**数字这一半仍然 open**：要用户在有真索引的机器上跑一趟 `romcat zh sync`
  + `romcat scrape` 才补得上。见提交 `114e94e`。

### Q47 — 缺口那一节改准之后，反过来把年份与汉化组也许给了中文离线源

- **来自：** 票 `offline-chinese-fields/06`（`/code-review` 的产出，判为中等）
- **类别：** 路过发现，不在范围内（是这张票自己引进来的）
- **在哪：** `crates/core/src/scrape/report.rs` 缺口那一节、`crates/core/src/scrape/zh.rs`
  的 `FIELDS`（新增）
- **为什么没停线：** 我把那句假话换成「标题、简介、类型、开发商、发行商、**年份**撞上
  一条中文条目就有……先跑一次 `romcat zh sync`」，而这一层**根本不产出年份**
  （`WORK_FIELDS` 只有类型、简介、开发商、发行商，变体那一层只有中文名）。`Field::all()`
  里还有**汉化组**，那一栏只有 TOSEC 的 `[tr zh <组>]` 给得出。于是一个没有 TOSEC 汉化
  命中的库跑一趟离线档，报告会把「汉化组」列进缺口、再叫用户去取 435 MB 的中文索引——
  **与这张票要修的那句假话是同一类，只是指向反了**。集成测试没抓到，是因为 fixture 里
  本来就有 `[tr zh]` 条目，汉化组从来不是缺口。
- **这张票实际做了什么：** 在 `scrape::zh` 上加一条公开的 `FIELDS`（中文名 + 那四样），
  报告按它把缺口分成两拨：给得出的那几栏照旧指路「先跑一次 `romcat zh sync`」，
  **年份与汉化组另说一句**「不在中文离线源的产出里，取索引补不上它们」，并说清各自的
  出处。两条测试钉着：`scrape::zh` 里 `报告用的那份字段清单与两层真的产出的对得上`
  （清单与两层真的产出漂开就红），`scrape::report` 里
  `中文离线源产出不了的那几栏不许被推去取索引`（直接摆一份缺口渲染一遍）。
- **谁来裁：** 收尾
- **状态：** open

### Q48 — `romcat zh judge --no` 那句「不会再撞回这条条目」把作品那一层也许了出去

- **来自：** 票 `offline-chinese-fields/06`（`/code-review` 的产出，判为低）
- **类别：** 路过发现，不在范围内（是票 05 引进来的）
- **在哪：** `crates/cli/src/main.rs` 的 `run_zh_judge` 末尾那两句
- **为什么没停线：** 那句话对**变体**成立（裁决进了输入指纹），对**作品**不成立——
  `zh::judge` 自己的文档写着「名下还有别的变体撞着同一条条目的话，那几栏会照样回来，
  而且是对的」。于是一个作品名下三个变体都撞条目 4、用户在其中一个上否掉，
  `cleared_work` 非零、命令行许诺「不会再撞回来」，下一趟刮削作品那四栏原样回来。
  已有的 `!from_variant` 那条警告管的是另一种情形，这一种它不响。
- **这张票实际做了什么：** 就地改准——头一句限定成「**这个变体**重跑不会再撞回这条条目」，
  并在 `cleared_work > 0` 时多打一句「**作品那一层可能自己回来**：那几栏按名下变体数票定
  ……要连作品一起否掉，就把那几个变体各裁一次」。**只改措辞，一条逻辑都没动**。
  这张票的名字是「报告说实话」，同一类的假话在同一轮里留着不改说不过去；代价是这一票
  多动了一个文件（`crates/cli/src/main.rs`）。
- **谁来裁：** 收尾
- **状态：** open

### Q49 — **任务**与**任务台**不在词表里

- **来自：** 票 `gui-redesign/01`
- **类别：** 规格没说（词表缺一条）
- **在哪：** `CONTEXT.md`（没有这两条）、`.scratch/gui-redesign/spec.md` 的五屏表与
  User Stories 56–61、`prototype.html` 的任务屏、`crates/core/src/task.rs`、
  `crates/gui/src/task.rs`
- **为什么没停线：** 规格与用户确认过的原型都把**任务**当一等概念用（五屏之一：
  排队、进度、可停、历史），而 `CONTEXT.md` 里「任务」只作为**待确认队列**的
  `_Avoid_` 出现过一次——那条避的是「别把待确认队列叫成任务列表」，不是「这个项目
  没有任务这个概念」。`docs/agents/domain.md` 说词表里没有的概念要么重想、要么记成缺口；
  这里显然是后者，而重新发明一个词只会让代码与规格、原型三处对不上。
- **这张票实际做了什么：** **照规格与原型的用法走**，模块、屏、类型一律叫任务
  （`romcat_core::task`、`View::Tasks`、`Board` = 任务台），并在这里记下缺口。
  词条本身留给 `/domain-modeling`——它要顺带说清任务与**待确认队列**的边界
  （一个是「工具替你跑的活」，一个是「等你裁的那一堆」），那不是这张票该顺手定的事。
- **谁来裁：** 收尾（交 `/domain-modeling` 补 `CONTEXT.md`）
- **状态：** open

### Q50 — 「再跑一次不重做已完成的部分」这条验收对**排差量预览**不成立

- **来自：** 票 `gui-redesign/01`（第 3 条验收）
- **类别：** 票写错了（准确说：那条判据是照会写东西的长入口写的）
- **在哪：** `.scratch/gui-redesign/issues/01-task-board.md` 第 17 行、
  `crates/core/src/sync/prepare.rs` 的 `prepare`
- **为什么没停线：** 排差量预览**整条只读**——它一个字节都不写（模块注释里那句
  「排计划这条命令一个文件都没写」是可以照字面核对的）。于是它没有「已完成的部分」
  可保住：停下等于什么都没发生，再排一次就是从头排一次（真机量级 343 ms）。
  「不重做已完成的部分」这条判据是给**扫描**（断点续跑）与**同步**（清单）准备的，
  它们在各自的票里接上任务台时才吃得到。
- **这张票实际做了什么：** 把这条验收落成它在只读长活上唯一说得通的含义——
  **停下之后状态是干净的，而干净是可以照字面核对的**：
  `crates/core/tests/task.rs::按停下之后一个字节都没动而且再排一次照样排得出`
  比对目标设备与工作目录里每个文件的路径、长度、修改时间，再排一次得到完整的一份；
  `crates/gui/tests/sublibrary.rs` 里同名那条走界面上那条一模一样的路。
  把手本身的「停在两步之间」由 `romcat_core::task` 的
  `被叫停时停在两步之间` 钉住（那条用的是一趟会数数的活，验得到「停下那一步一件事
  都还没做」）。
- **谁来裁：** 收尾
- **状态：** open

### Q51 — 停下的粒度是「两步之间」，最长那一步停不动

- **来自：** 票 `gui-redesign/01`
- **类别：** 规格没说
- **在哪：** `crates/core/src/task.rs` 的模块注释与 `Handle::step`、
  `crates/core/src/sync/prepare.rs` 里「折事实」那一步
- **为什么没停线：** 把手在两步之间生效，所以「按下停下」到「真的停了」之间最坏是
  **当前这一步的长度**。排差量预览十二步里最长的是折事实（`sublibrary::facts` 走一遍
  全库，挂账 D156 记着它是那 343 ms 的大头），于是最坏约两百毫秒——一次点击的等待，
  够用。要更细就得把把手往 `sublibrary::facts` 里传，而它有六处调用方（命令行、
  报告、三个测试、`prepare` 自己），为这一档收益改六处不划算。
- **这张票实际做了什么：** 走两步之间这条，把它写进 `task` 的模块注释与 `prepare` 里
  「折事实」那一步的行内注释，并给出更细粒度的办法：`Handle::cancel()` 把底下那个
  `CancelToken` 交下去——**扫描、识别、同步本来就收它**，所以接那几个长入口的票不必
  再造一遍轮子。
- **谁来裁：** 票 `02`（扫描）与票 `10`（刮削）——它们那两趟一步动辄几分钟，
  粒度够不够由它们说了算
- **状态：** open

### Q52 — 只活在内存里的中立库分不出第二份连接，那一趟就地跑

- **来自：** 票 `gui-redesign/01`
- **类别：** 工具缺陷（`rusqlite::Connection` 不是 `Sync`）
- **在哪：** `crates/core/src/catalog.rs` 的 `Catalog::read_only`、
  `crates/core/src/task.rs` 的 `Board::run_here`、
  `crates/gui/src/sublibrary.rs` 的 `Screen::preview`
- **为什么没停线：** 后台那条线程要的是**同一个文件的第二份只读连接**，而合成数据
  （`--demo` 与 `--bench*`）那份库只活在内存里，压根没有文件可开。这时候要么直说
  「排不了」、要么就地跑完。选就地跑：合成数据上这些活是几毫秒的事，而直说排不了会
  让演示与实测那两条路当场瘸掉——**实测量的正是这一趟花了多久**。
- **这张票实际做了什么：** `Board::run_here` 走同一套账（进历史、带耗时、认领方式
  一模一样），只是不开线程；`Screen::preview` 分不出只读连接时退到它。
  真库一律走 `Board::queue`（后台线程），`crates/gui/tests/sublibrary.rs` 的现场因此
  改成**落在磁盘上的中立库**——fixture 照真库的样子摆，才验得到那条真路。
  `crates/core/tests/task.rs::只活在内存里的库分不出第二份连接` 钉住这时要直说而不是
  悄悄开一份空库。
- **谁来裁：** 收尾
- **状态：** open

### Q53 — 这个仓库不是 `rustfmt` 格式的，而门禁里没有 `cargo fmt`

- **来自：** 票 `gui-redesign/01`
- **类别：** 路过发现，不在范围内
- **在哪：** 整个仓库；`rustfmt 1.9.0-stable` 会改动至少 11 个与这张票无关的文件
  （`crates/core/src/verdict.rs`、`zh/*`、`scrape/*`、`adapter/pegasus.rs`、
  `crates/core/tests/scrape.rs` 等），共 260 余行
- **为什么没停线：** 我顺手跑了一次 `cargo fmt --all`，它把那 11 个文件全改了
  （典型是把 `if a { 1 } else { 2 }` 这类单行式拆成四行）。**当场按 `HEAD` 逐个写回，
  工作区只留这张票动过的文件。** 门禁只有 clippy 与 test 两条，没有 fmt——
  所以仓库现在这个样子是有意的（人写的排版，中文注释的换行位置也照人读的节奏排），
  不是漏跑。
- **这张票实际做了什么：** 还原了那 11 个文件与 `crates/cli/src/main.rs` 里那一处
  无关改动；**这张票之后不再跑 `cargo fmt`**。记在这儿是为了让下一个人别重复这一下——
  它看着无害，实际会在 diff 里埋进两百多行与票无关的改动。
- **谁来裁：** 拿主意的人（要么把 `cargo fmt --check` 加进门禁并整仓库格式化一次，
  要么在 `CLAUDE.md` 里明写「别跑 fmt」——眼下两头不靠）
- **状态：** open

### Q54 — 任务屏没有「预计剩余」，而 User Story 57 要它

- **来自：** 票 `gui-redesign/01`
- **类别：** 规格没说（票的验收与规格的 story 对不上）
- **在哪：** `.scratch/gui-redesign/spec.md` 的 User Story 57、
  票 `01-task-board.md` 第 16 行（只要求「名字、进度、已用时间」）、
  `crates/gui/src/task.rs` 的 `running_ui`
- **为什么没停线：** 这张票的验收只要名字、进度、已用时间三样，而 story 57 还要
  **预计剩余**。眼下唯一接上来的长活是排差量预览（343 ms），给它算剩余时间是在
  几百毫秒上摆一个跳来跳去的数字——那比不给更坏。真正要剩余时间的是扫描
  （37.1 分钟）与刮削，而那两趟的剩余靠的是它们自己知道的东西（还剩几个文件、
  还剩几个请求），不是任务台猜得出来的。
- **这张票实际做了什么：** 把手上留好了位子——`Progress` 带 `done`/`total`
  （`Handle::tick` 报的那一对），进度条已经按它折算走了几成；**剩余时间那一格空着**，
  由接扫描与刮削的那两张票在有真分母的时候补。
- **谁来裁：** 票 `02`（扫描）与票 `10`（刮削）
- **状态：** open

### Q55 — 根名进键的**第一段**，而不是给中立库加一列

- **来自：** 票 `gui-redesign/02`
- **类别：** 数据模型取舍（票只说「变体的键变成根名 + 相对根 + 路径」，没说怎么落）
- **在哪：** `crates/core/src/path.rs` 的 `library_key` / `split_root`、
  `crates/core/src/catalog/roots.rs` 的模块文档、`docs/adr/0020` 的新修订段
- **为什么没停线：** 票的原话就是「**根名** + 相对根 + 路径」，读起来是**一串**而不是
  两列，两条路都站得住，但代价差一个数量级：中立库里有二十来张表以「条目的键」或
  「变体的键」当主键或外键（`entry`、`container`、`variant`、`variant_member`、
  `candidate`、`identification`、`content_*`、`scrape_value`、`media_*`、
  `sublibrary_manifest`、`title`…），**沉淀库**的路径锚还是 `(主库名, 变体的键)` 那一对。
  拆成两列要动到那份**不可再生**的库的结构。
- **这张票实际做了什么：** 走「进第一段」。改的只有 `crate::path` 里拆键的那几个函数
  （`platform_of_key` 从第一段挪到第二段、新增 `platform_dir_of_key` / `split_root`），
  以及三处按段拆键的调用点（`shape::scope_of`、`shape::climb`、
  `adapter::gamelist::media_placement`）。二十来张表一列没动，沉淀库结构一个字没改。
- **谁来裁：** 收尾（记着就好，不必回头改）
- **状态：** open

### Q56 — 沉淀库里换键之前落下的**路径锚**从此撞不上，一条也没删

- **来自：** 票 `gui-redesign/02`
- **类别：** 不可再生的数据受到影响（票说「路径锚会全部失效」，没说怎么处置）
- **在哪：** `crates/core/src/verdict.rs` 的模块文档「换键那一次」那一节
- **为什么没停线：** 票明说「路径锚会全部失效（**内容锚不受影响**）」，也明说真库现在
  **0 条裁决、0 条人工纠正**，所以这笔账的实际金额是零。三条路：删掉那些行、猜着往前
  挪一格、原样留着。**猜是最不该做的**——哪条旧键属于哪个根，沉淀库里没有任何判据说得
  出来（它只记着主库叫什么名字，记不着那时候只有一个根）；**删也不行**——那是不可再生
  的库，「删掉就没了」正是它与中立库唯一的区别。
- **这张票实际做了什么：** 原样留着，不删、不改、**不加迁移**（迁移只许往后追加，而这件
  事没有一条正确的 SQL 写得出来）。在 `verdict` 的模块文档里写清那几行是什么，免得日后
  有人把它们当脏数据清掉。内容锚一条都不受影响。
- **谁来裁：** 拿主意的人（如果哪天真有人带着旧沉淀库升上来，得有一条「把旧路径锚归到
  某个根名下」的命令；眼下没有这样的用户）
- **状态：** open

### Q57 — 子库落点与前端 `file:` 都**剥掉根名**，而票没说这一层

- **来自：** 票 `gui-redesign/02`
- **类别：** 票没预见到的第三种情况
- **在哪：** `crates/core/src/sync.rs` 的 `DesiredFile.path` 与 `Unsupported.path`、
  `crates/core/src/capability.rs` 的 `Conversion::path`、
  `crates/core/src/adapter/converge.rs` 的 `files:`
- **为什么没停线：** 子库里的布局本来就**照搬变体的键**（挂账 D79），键一带根名，卡上
  就多出一层 `主库/`——而前端认平台靠的正是**顶层那一级目录**（ADR-0013，ES-DE 的
  `es_systems.xml` 里 `<name>` 就是那个目录名）。照搬进去等于把每个平台目录降一级，
  卡上一个游戏都认不出来。
- **这张票实际做了什么：** 落点与 `file:` 一律 `path::relative_of_key`，卡上的形状与
  主库变成一组根之前一模一样；**源那一格照旧是完整的键**（读主库要靠它第一段查出那块
  盘在哪）。机器读的 `x-romcat-variant` 也保留完整的键——它要精确对回中立库里那个变体。
  **代价说清楚**：两个根里同一条相对路径会落在卡上同一个文件上。真库上两个根一个是
  ROM 一个是元数据，撞不上；真撞上时清单的主键 `(sublibrary, path)` 会把后来那条覆盖掉。
- **谁来裁：** 票 `11`（子库屏重做）——那时该在排计划那一步把撞车报出来
- **状态：** open

### Q58 — `--under` 的前缀从此带根名，`--library-root` 从此可以点名

- **来自：** 票 `gui-redesign/02`
- **类别：** 命令行的口径跟着键变了（票没提命令行）
- **在哪：** `crates/core/src/triage.rs` 的 `Filter::under`、
  `crates/cli/src/main.rs` 的 `--library-root`
- **为什么没停线：** 两处都是「键的形状变了，拿键当参数的地方跟着变」的直接后果。
  `--under FC` 变成 `--under 库/FC`——而**报告那张表印的就是新形状**（`Item::directory`
  与 `Filter::under` 是同一套判据，`Axis` 存在的理由正是这个），用户照着抄不会错。
  `--library-root` 原本是一条路径，一组根之后「换哪个根的位置」有了歧义。
- **这张票实际做了什么：** `--under` 不加兼容层（加了就得猜「FC 是根名还是平台名」，
  而猜错一次是「照这个改几百条」）；顺带白拿一条：`--under <根名>` 就是「整个这个根」。
  `--library-root` 改成可重复的 `[根名=]路径`，不点名时只在**这份库只有一个根**时算数，
  多于一个就直说要点名。`identify` / `scrape` / `names` 那几条的 `--root` 同理：
  只在至多一个根时换得动位置（`roots_for`）。
- **谁来裁：** 收尾
- **状态：** open

### Q59 — 界面上加根只有输入框，没有系统的目录选择对话框

- **来自：** 票 `gui-redesign/02`
- **类别：** 依赖取舍（原型画的是「+ 添加目录」按钮）
- **在哪：** `crates/gui/src/roots.rs` 的 `roots_ui`
- **为什么没停线：** 原生目录选择器要新增一个依赖（`rfd` 之类），而这张票的验收是
  「库屏上加一个目录、按扫描，任务台里跑得完」——输入框满足它。新依赖该单独裁一次：
  它会带进 GTK/AppKit 的运行时依赖，与 egui 选型当初「单一二进制、无 WebView 依赖」
  那笔账（ADR-0005）是同一类问题。
- **这张票实际做了什么：** 两个输入框（目录 + 根名，根名不填按目录名取）加一个按钮。
  路径不是目录、或者与已有的根/工作目录套在一起，都当场说清为什么。
- **谁来裁：** 票 `12`（版式与一致性）
- **状态：** open

### Q60 — 扫描时界面与后台**两个写者**对着同一个中立库文件

- **来自：** 票 `gui-redesign/02`
- **类别：** 并发（票只说「按扫描，在任务台里看着它跑完」）
- **在哪：** `crates/core/src/catalog.rs` 的 `Catalog::open`（`busy_timeout`）、
  `crates/gui/src/roots.rs::Screen::scan`
- **为什么没停线：** 排差量预览那一趟拿的是**只读**连接（`Catalog::read_only`），
  扫描不行——它要写。`rusqlite::Connection` 不是 `Sync`，交不过去，于是后台线程按文件
  路径自己开一份。WAL 允许一写多读，但两个写者撞上时默认**当场**返回 `SQLITE_BUSY`
  ——那会让用户在扫描期间按一下裁决就看见「数据库忙」。
- **这张票实际做了什么：** `Catalog::open` **与 `Catalog::read_only`** 各加一条
  `PRAGMA busy_timeout = 10000`：撞上就等，等不到才报。两份都要设——只读那一份是长活
  自己开的，默认超时是 0，扫描提交那一瞬间它会当场拿到「database is locked」
  （`/code-review` 报的第 9 条）。扫描一批写完就放手，界面那一侧的写都是几毫秒的事，
  十秒是很宽的余量。**没有拿锁把界面那一侧禁掉**——那会让「跑着的时候照常用别的屏」
  这条（User Story 59）在扫描这一趟上失效。
- **谁来裁：** 收尾（真机上扫 37.1 分钟期间做几次裁决，看看会不会撞上）
- **状态：** open

### Q61 — 界面上那个「库」屏的模块叫 `roots`，因为 `library` 这个名字被库浏览占着

- **来自：** 票 `gui-redesign/02`
- **类别：** 命名（规格把这一屏叫「库」，把变体表那一屏叫「浏览」）
- **在哪：** `crates/gui/src/roots.rs`（新的「库」屏）、
  `crates/gui/src/library.rs`（旧的**库浏览**屏，`View::Variants`）
- **为什么没停线：** 规格里五屏的名字是「库 / 浏览 / 待确认 / 子库 / 任务」，而仓库里
  `library.rs` 这个模块名已经归了库浏览。把 `library.rs` 改名成 `browse.rs` 要连带动
  `crates/gui/tests/library.rs` 与 `Cargo.toml` 里那条 `[[test]]`，而**票 03 正要重做
  那一屏**——改名与重做撞在一起，diff 会糊成一团。
- **这张票实际做了什么：** 新屏叫 `roots`（它主要那一半就是**根**），`View::Library`
  的标签是「库」。旧模块一个字没动。
- **谁来裁：** 票 `12`（票 `03` 已把用户看得见的那一半改掉，见下）
- **票 `03` 补记：** 重做那一屏时只改了**用户看得见的那半**——顶栏上那一屏现在叫
  「浏览」（`View::label`），文档里也改了口。模块名 `library.rs`、枚举名 `View::Variants`
  与测试文件名照旧：那三样一起改要连带动 `Cargo.toml` 里那条 `[[test]]`，而票 `03`
  本身已经把这一屏从里到外换了一遍，两件事叠在一个提交里 diff 就读不动了。
- **状态：** open

### Q62 — 三个数据源的「重取」没有细粒度进度，也没有 `--full`

- **来自：** 票 `gui-redesign/02`
- **类别：** 票没说到那一层（票只要求「能一键重取（走任务台）」）
- **在哪：** `crates/core/src/sources.rs::refetch`
- **为什么没停线：** `dat::sync::run`、`zh::sync::sync`、`titledb::sync::sync` 三个入口
  都**不收把手**（`task::Handle`），改它们的签名是三条独立的活，而这张票要的是「按一下、
  在任务台里看着它跑完」。粒度是**一整个源**：报一步、停在下一个文件之前。
- **这张票实际做了什么：** `sources::refetch` 报一步就下去跑，进度条画的是那条来回跑的
  条（`Progress::fraction` 说不出几成时的样子）。**限流与命令行同一个数**
  （`sources::THROTTLE`，500 毫秒）——在线源赌的是账号与 IP（ADR-0007），两处不能不一样。
  `--full`（整份重取）没往界面上放：那是一条要花很久、又不常用的路，命令行还在。
  **「停不下来」这件事如实说出口**（`/code-review` 报的第 3 条）：函数文档与界面上那个
  按钮的悬停提示都写明「排着队的停得掉，已经在下载的停不掉」，不摆一个按了没反应的
  承诺出来。
- **谁来裁：** 票 `10`（刮削面板）——那张票要给刮削报细粒度进度，顺手把这三条也接上
- **状态：** open

### Q63 — `/code-review` 报的十条，九条当场收掉、一条并进 `Q62`

- **来自：** 票 `gui-redesign/02` 的收尾审查（`/code-review high`）
- **类别：** 审查发现
- **在哪：** 见每一条后面的落点
- **为什么没停线：** 全部在这张票的范围之内，改动都小。
- **这张票实际做了什么：**
  1. **高 · `remove_root` 只删条目不删变体** —— 界面上那句「去掉 N 个变体」是假的，
     浏览屏与队列上会剩下指不着文件的幽灵行。补上按前缀删 `variant`、`variant_member`、
     `shaping_override`、`candidate`、`identification`、`model_answer`、
     `collection_variant`、`preferred_variant`、`title`、子库的例外与清单，
     以及刮削那三张表里 `subject = '变体'` 的那一半（作品那一层是别的根也在用的，不动）。
     测试 `移除一个根时说得出会去掉多少变体并且只去掉它自己那一支` 补了
     `root_stats(...) == RootStats::default()` 这条断言。
  2. **中 · 断点跨根共用一个文件** —— `workspace::checkpoint_path` 加根名那一段。
     不加的话扫乙盘会盖掉甲盘扫到一半的进度，`--resume` 还会撞出一句「扫描根对不上」
     让整趟失败。测试 `同一份库里两个根的断点互不覆盖`。
  3. **中 · `refetch` 收了把手却停不下来** —— 并进 `Q62`：文档与界面提示都改成如实说。
  4. **中 · 界面上「算了」按钮不取消** —— 它只清了一个局部变量，`self.removing` 永远
     挂着；一个只能前进不能后退的破坏性确认比不加确认更坏。
  5. **中 · 按下停下之后才真出错的那一趟被当成干净停止** —— 错误文本被整条丢掉，
     界面上却说「一个字节都没动」。判据改成「那句话正是 `Halted` 交出来的那一句」，
     测试 `按下停下之后才真出错的那一趟按失败记而不是按停了记`。
  6. **中 · `run_scan` 的 ADR-0004 守卫只守这一趟扫的那个目录** —— 这是主库变成一组根
     新开的口子：报告能落进**同一个主库的另一个根**里。改成遍历 `catalog.roots()`。
  7. **低 · `--library-root` 的 `=` 切分不校验左边** —— `/mnt/backup=2024/roms` 会凭空
     造出一个叫 `/mnt/backup` 的根。左边现在过 `path::root_name`。
  8. **低 · `set_root_path` 绕过加根的三道校验** —— 把摆位那一段抽成
     `roots::check_placement`，加根与改指两条路共用；**与自己比不算套在一起**，
     换挂载点照旧是正常操作。测试 `把一个已有的根改指到别处也要过摆位校验`。
  9. **低 · 只读孪生连接没设 `busy_timeout`** —— 并进 `Q60`。
  10. **低 · `Board::busy()` 漏掉待认领的那一格** —— `run_here` 就地跑完那一趟的产物
      要等到下一次有输入事件才交得出去。加 `Board::settled()`，界面据它多画一帧。
- **谁来裁：** 收尾
- **状态：** open

### Q64 — 作品级主列表按聚合列排序，代价与库有多大成正比

- **来自：** 票 `gui-redesign/03`
- **类别：** 这一票新引入的代价（票只说「十万行级别下翻得动」，没说甩滚动条那一种）
- **在哪：** `crates/core/src/catalog/browse.rs::Catalog::work_page`
- **为什么没停线：** 这是「一个作品一行」这件事的**内在**代价，不是可以绕开的实现细节：
  「这个作品有几个变体、合计多大、横跨哪几个平台」要先看过它的每一个变体。变体表那一层
  每条 `ORDER BY` 都有一条索引正好接住（`variant_bytes_key` 那几条），一次翻页是索引
  倒着扫；作品级这一条要先 `GROUP BY` 折成行、再按聚合出来的列排序，两步都没有索引接得住。
  **内存那一半一点没退**（一扇窗还是 512 行，`MAX_PAGE` 还在），涨的是每次翻页的时间。
- **这张票实际做了什么：** 走「查询整个下推 + 每页再补两样」这条路——`GROUP BY` 与
  `ORDER BY` 全在 SQLite 里，「元数据齐不齐」与「最高置信度」这两样**排不了序**，
  所以只对这一页那几十行补（`fill_missing` / `fill_confidence`），不摊到全库上算。
  `work_total` 那一条也不连年份那张表——它只数行。实测（release，`--bench`）：

  | 主列表行数 | 常态滚动（每帧 3 行） | 一趟甩到底（每帧都读库） |
  |---|---|---|
  | 46,483 | 中位 **0.85 ms**（1,173 fps），240 帧读库 **1 次** | 中位 **114 ms** |
  | 100,000 | 中位 **0.74 ms**（1,344 fps），240 帧读库 **1 次** | 中位 **317 ms** |

  真库的形状比这两行都轻：46,483 个变体收敛成 3,596 行时，换一次筛选 **44 ms**、
  点开一行 **21 ms**、每帧中位 **3.26 ms**（`--bench-browse`）。上面那两行是**每个变体
  各自一行**的极端形状（合成数据一个作品都没认出来），也就是这条代价的上界。
- **值不值得治：** 常态滚动没问题，咬人的只有「拖着滚动条一路甩到底」那 3 fps。
  两条路：把窗口开大（读得少但一次读得更久，且现有测试拿「一千行与十万行内存里一样多」
  钉着窗口大小），或者甩的时候先画空行、松手再补（那是画法，不是查询）。
  两条都超出这张票。
- **谁来裁：** 票 `12`（布局可调与视觉一致性收尾）
- **状态：** open

### Q65 — 「元数据齐不齐」与「最高置信度」的口径是这一票现定的

- **来自：** 票 `gui-redesign/03`
- **类别：** 票没说到那一层（票只写「每行看得见……元数据齐不齐、最高置信度」）
- **在哪：** `crates/core/src/catalog/browse.rs::WORK_FIELDS`、`Catalog::fill_confidence`
- **为什么没停线：** 两条都有现成的 ADR 给出方向，只是没人写死过具体那几样。
- **这张票实际做了什么：**
  - **齐不齐看五样**：年份、发行商、开发商、类型、简介（`WORK_FIELDS`）。**标题不算**
    ——叫法是个集合不是单值（`catalog::title`），缺不缺由标题集合自己答；**汉化组也不算**
    ——它挂在**变体**上（ADR-0012），拿它当作品级的缺口会让一整排原版游戏都显示成「缺」。
    锚点两支分开看：认出作品的看**作品**锚点，没认出来的看**变体**锚点，与
    `adapter::converge` 挑值时同一条岔路——于是屏上写着「齐」的那一行，导出时真填得满。
  - **最高置信度**看的是这一行**当前筛选下**那些变体上的**全部候选**（不只自动通过的那些），
    取最高的一档；一条候选都没有是 `None`，画成**还没识别**——那是独立的一档，
    不是「撞过没撞上」（ADR-0002）。
  - **年份**取「裁决优先，其次各源里最早的那一个」：裁决排在每个字段最前是优先级表的
    第一条规则（ADR-0001），而一部作品跨地区先后发行好几次，最早的那次才是它的年份。
    列表与详情面板走同一条取法（`Catalog::work_year`），两处写的是同一个数。
- **谁来裁：** 票 `10`（刮削面板要选「刮哪些字段」，那份字段清单该与这五样对齐）
- **状态：** open

### Q66 — 浏览屏顶上那三个批量操作按钮还没画

- **来自：** 票 `gui-redesign/03`
- **类别：** 票之间的接缝（原型上有，这张票的验收里没有）
- **在哪：** `crates/gui/src/library.rs::edit_panel`（作用范围那一行就是它们的位置）
- **为什么没停线：** 原型 `.scratch/gui-redesign/prototype.html` 的浏览屏抬头摆着
  「刮削选中…」「存成子库」「★ 收藏」三个按钮，而它们各自是票 `10` / `11` / `06`
  的正文。这张票的十条验收一个都没点到它们，票据也点名「详情面板里媒体那一块只留位置」。
- **这张票实际做了什么：** 把**它们要的那样东西**铺到位并钉住语义——
  `Catalog::scoped_variants` / `scoped_variant_total` 把「选中的这几行」展开成一串变体的键，
  界面上常驻一句「选中 N 条、作用于 M 个变体」，`Screen::batch_variants` 是三个按钮
  按下去该拿的那一批。按钮本身留给各自的票，**不提前画一个按了没反应的**。
- **顺带记一条给接它们的人：** `WorkAnchor::Work(id)` 里那个数是 `work` 表的行号，
  **重跑识别会把它换掉**（`clear_identifications` 整批删了重建）。眼下界面上还没有
  「重跑识别」这个按钮，所以咬不到人；哪张票把识别接上任务台，就要在那一趟跑完之后
  把浏览屏的选中与点开的那一行一起丢掉，否则批量操作会打到另一个作品头上。
- **谁来裁：** 票 `06` / `10` / `11` 各接一个
- **状态：** open

### Q67 — `/code-review` 报的四条，四条当场收掉

- **来自：** 票 `gui-redesign/03` 的收尾审查（`/code-review high`）
- **类别：** 审查发现
- **在哪：** 见每一条后面的落点
- **为什么没停线：** 全部在这张票的范围之内，改动都小。
- **这张票实际做了什么：**
  1. **中 · 点一下表头换排序就把选中静默清空** —— `sync_window` 拿整个 `WorkQuery`
     比相等，而它含 `order` / `descending`。人勾了两百行再按一下「容量」表头，筛出来的
     还是同一批行，选中却没了。判据拆成两条：**换排序**只丢高亮（那是个全序下标，
     重排之后会指到另一行身上），**换筛选**才连选中一起丢（全选说的「当前筛出来的
     这一批」已经不是同一批）。哪几个字段算筛选是查询面自己的事，所以判断落在核心库
     （`WorkQuery::same_filter`）。测试补在
     `crates/gui/tests/library.rs::多选与全选可用而且屏上看得见选中多少条`。
  2. **中 · 「作用于多少个变体」把读库错误吞成 0** —— `scoped_variant_total` 那一处
     `unwrap_or(0)` 恰好把它自己那段文档说要防的事做了出来：屏上写着「作用于 0 个变体」，
     按下去真会动一百多个。`scope` 改成 `Option<u64>`，数不出来就画「？」并把错落进
     `error`（`Screen::scope_label`）。
  3. **低 · 实测那一行把两个不同口径的数并在一起** —— `BrowseCost::rows` 是**没筛之前**
     的行数，`variants` 是**筛完之后**全选展开出来的变体数，`render()` 却把它们并成
     「N 行作品（M 个变体）」。改成与 `filtered` 配对：「全选展开范围 … ms（筛完那 N 行
     → M 个变体）」。
  4. **低 · 一条测试断言的其实是「一个变体的每条候选置信度都相同」** ——
     `assert_eq!(variant.confidence(), Some(candidate.confidence))` 摆在遍历全部候选的
     循环里。眼下只因合成数据每个变体的候选同档才过；真库上一个变体撞上一条高一条低
     是常态，那条会在**正确**的实现上炸。改成 `<=`（`Confidence` 的 `Ord` 里 `High` 最小，
     行上那一档是 `min`）。
- **审查另外核对过、确认不用改的：** `work_page` / `fill_confidence` 的参数绑定次序与
  SQL 文本里 `?` 的出现次序一致；`Confidence` 的 `Ord` 是 `High < Medium < Low`，
  `.min()` 取到的确实是最高那一档；`scoped_sql` 里 `COALESCE(work_id IN (…), 0)` 那层
  裹得对，全选减例外不会漏掉没认出作品的那些行；表格第一列那个复选框在 egui 的命中
  判定里排在单元格之后注册，会赢下点击，不会被行响应吃掉。
- **谁来裁：** 收尾
- **状态：** open

### Q68 — 「存成子库」这个按钮，票 04 与 Q66 各安排了一次

- **来自：** 票 `gui-redesign/04`
- **类别：** 票之间的接缝（两张票都写到了同一个按钮）
- **在哪：** `crates/gui/src/library.rs::save_panel` / `save_as_sublibrary`
- **为什么没停线：** 两处写的其实是同一件事的两半。Q66 说「存成子库」是票 `11` 的正文
  （那是照原型抬头那三个按钮分的）；而这张票的**验收第 5 条**是
  「『存成子库』把当前条件原样变成规则，**新建的子库选出来的东西与屏上一致**」——
  那句话没有能力做一半：不真的建出一个子库来，「与屏上一致」验不了。
- **这张票实际做了什么：** **按钮做在浏览屏上**，因为它是筛选器的出口不是子库屏的入口。
  折规则那一步在核心库（`WorkQuery::to_rule`），界面只把两个格子（名字、目标路径）
  交给它。前端格式默认 Pegasus、容量上限不设——那两样是**设备**的属性，去子库屏调。
  **重名不覆盖**：撞上一个已有的子库当场说清并且不写（`put_sublibrary` 是按名字更新的，
  `add_rule` 又是往上加一条，两条合起来等于悄悄改掉别人的目标路径再并上一批选中）。
- **留给票 11 的是反方向那一半**：「改选择」跳回浏览屏、规则预填进筛选器。核心库那条路
  已经铺好并且钉住了（`WorkQuery::from_rule`、`Rule::any_of`、
  `library::Screen::set_filter_rule`，`crates/core/tests/filter.rs::多条规则并成一条就是任一满足`）。
- **谁来裁：** 票 `11`（它重做子库屏时决定「存成子库」这个按钮要不要也在抬头摆一个）
- **状态：** open

### Q69 — 收藏这一维立起来了，值那条路留给票 06：界线划在这里

- **来自：** 票 `gui-redesign/04`（票据点名「维度补上收藏」）
- **类别：** 票之间的接缝
- **在哪：** `crates/core/src/sublibrary/rule.rs::Dimension::Favorite`、
  `crates/core/src/sublibrary.rs::VariantFacts::favorite`、
  `crates/core/src/catalog/filter.rs::favorite_any`
- **为什么没停线：** 票据把这一维派给了 04，把「在主列表里多选一批加收藏」那个动作
  派给了 06。两张票在这里交界。
- **界线就划在 `VariantFacts::favorite` 这一列上：**
  - **这张票（04）做的**：规则语言认得 `收藏`（`收藏=是` / `收藏=否` 解析得了）、
    内存那一侧算得动（`VariantFacts::favorite`）、SQL 那一侧也算得动
    （`filter::favorite_any`）、界面的维度下拉框里摆得出来。
  - **票 06 要做的**：把那一列**填上**——沉淀库里的成员关系、内容锚、以及主列表上
    「多选一批加收藏」那个动作。填的时候 `favorite_any` 那个常量谓词换成一条 `EXISTS`，
    `facts()` 多读一张表，**规则语言与界面一个字都不用改**。
- **眼下的行为是诚实的而不是假的**：中立库里还没有收藏表，所以每个变体的收藏值都是
  `否`——`收藏=是` 一个都选不中、`收藏=否` 全选中，而这两句话眼下**都是真的**。
  另外 `VariantFacts::has(收藏)` 特意取「真收藏了」而不是「两档都算有值」，于是
  选择集报告会点名说「这条规则引到的 `收藏` 在这份库里一条数据都没有」——
  选不出东西时说清是缺数据还是写错了。
- **谁来裁：** 票 `06`
- **状态：** open

### Q70 — 屏上有三样条件写不进规则：搜索框、识别状态、平台未知

- **来自：** 票 `gui-redesign/04`
- **类别：** 规格没说（验收第 5 条要求「原样变成规则」，但屏上有条件规则表达不了）
- **在哪：** `crates/core/src/catalog/browse.rs::Unruly` / `WorkQuery::to_rule`
- **为什么没停线：** 三样各有各的正当理由不进规则，而**挡住**是个立得住的答案：
  1. **搜索框**——搜索与筛选是两件事，搜索管的是排序、筛选管的是集合（票 `05` 的正文）。
  2. **识别状态**——那是这一趟的**进度**不是**内容**，重跑识别就变。一条按进度选内容的
     子库规则，下次同步选出来的会是另一批。
  3. **平台未知**——写成规则要把全部已知平台列一遍（`都不(平台=…)`），而平台清单一变
     那条规则就悄悄失效。
- **这张票实际做了什么：** `to_rule` 返回 `Result`，撞上这三样**当场挡住并说清是哪一条、
  以及怎么办**（`Unruly::advice`），**不是少写一条了事**——少一条，子库选出来的就比屏上
  多，而那正是这条约定要消灭的东西。测试
  `crates/core/tests/filter.rs::写不成规则的条件当场挡住而不是悄悄少写一条` 钉着。
- **哪张票想改，路是通的：** 给规则语言加一维 `识别` 就能收掉第 2 条（`VariantFacts` 多
  一列、`filter.rs` 多一个 `EXISTS`）。这一票没加，因为词表里的**维度**是「要什么内容」，
  加一维「进度」会让子库的规则语义分叉。
- **谁来裁：** 票 `05`（搜索那一半）/ 收尾
- **状态：** open

### Q71 — 括号：读得进来的比写得出去的宽，界线划在 `parse` 与 `build` 之间

- **来自：** 票 `gui-redesign/04`（这一条在收尾审查里改过一版，见下）
- **类别：** 规格没说（新语法与已有数据的边角）
- **在哪：** `crates/core/src/sublibrary/rule.rs`（模块文档那两节、`opens_group`、
  `matching`、`split_items`、`Clause::build`）
- **为什么没停线：** 组要嵌套就得有分界符，而作品名里带括号是常态
  （`Pocket Monsters (Japan)`），**而且未必配对**（`口袋妖怪(日` 这种截断的名字）。
- **走的是哪条路：** `(` **只在一项的开头**（前面那一段去掉空白之后是空的、或者正好是
  `都不`）才开一个组；别处的括号是值里的字，**配不配对都不影响这一层怎么切**。
  数括号那件事由 `opens_group` 一处说了算，`split_items` 与 `matching` 两处共用它
  ——两处规矩不一致的话，「切成几项」与「切在哪儿」会给出两个答案。
- **于是两条路宽窄不同，这是故意的：**
  - `Clause::parse`（**读库里躺着的**）宽：`作品~口袋妖怪(日` 照样读得懂，
    这一票之前存进去的规则一条都不许失效（验收第 3 条）。
  - `Clause::build`（**造一条新的**：界面点出来的、从筛选器折出来的）严：值里
    **不许有两侧带空白的连接词**，**括号得配对**。因为它造出来的东西马上要被
    `Rule::from_group` 印成一行字存进中立库，而印出去读回来必须还是它自己。
- **剩下的那点边角**：一个值里带落单 `)` 的**老**子句，若被手写进一个组里
  （`(作品~笑) 或 平台=GB)`），那半个括号会把组提前收尾。库里现有的规则都是平铺的，
  够不着这条路；新造的一律过 `build` 那道闸。
- **要根治得加转义**（`\(`）或者引号包值，两样都会让手写规则变难看。眼下真库里没有
  这种作品名，先不加。
- **谁来裁：** 收尾
- **状态：** open

### Q72 — 年份那一维在两个求值器里的「读不成数就扔掉」是两套写法对齐的

- **来自：** 票 `gui-redesign/04`
- **类别：** 执行里冒出来的（同一条语义要在 Rust 与 SQLite 里各写一遍）
- **在哪：** `crates/core/src/catalog/filter.rs::YEAR_VALUE` / `RATING_VALUE` 对
  `crates/core/src/sublibrary.rs::ScrapedInto::absorb`
- **为什么没停线：** 刮来的年份是字符串，里面有 `199X` 这种写法。内存那一侧是
  `value.trim().parse::<i32>()`——读不成就扔掉；SQL 那一侧 `CAST('199X' AS INTEGER)`
  会读成 `199`。两边不一样的话，屏上筛出来的与同步搬过去的就差那几条。
- **这张票实际做了什么：** SQL 那一侧加一条「**转回文字还是原样**」的闸
  （`trim(value) = CAST(CAST(trim(value) AS INTEGER) AS TEXT)`），正好把读不成整数的值
  挡在外面。`crates/core/tests/filter.rs` 的 fixture 里特意塞了一条 `199X`，
  六条年份规则各跑两遍比键。
- **还有一条对不齐的边角**：`+1990` 这种带正号的写法 Rust 读得出、这条闸读不出。
  真库里没有这种值（年份来自 DAT 与中文离线源，都是四位数字），不为它加一层。
- **评分那一维（`RATING_VALUE`）写全了但验不了**：眼下没有任何源产得出评分
  （挂账 D68），一条数据都没有。等 ScreenScraper 的凭据到位、值落进库那天，
  这一段要补一次同样的两边比键。
- **谁来裁：** 收尾（或者哪张票把评分接上来）
- **状态：** open

### Q73 — 左栏上下两半在做同一件事：五个一按就有的档，与那棵条件组

- **来自：** 票 `gui-redesign/04`
- **类别：** 票之间的接缝（布局与视觉收尾是票 `12`）
- **在哪：** `crates/gui/src/library.rs::filter_panel`
- **为什么没停线：** 平台 / 合集 / 语言 / 中文这四个档，条件组里都写得出来
  （`平台=GB`……）。两套并存，等于同一件事有两个地方点。
- **为什么眼下留着两套：** 那四个档**各自带着条数**（`Catalog::facets` 一次 `GROUP BY`
  问出来），而条件组里的值是打出来的——没有条数的下拉框，人只能一个个点开试。
  一按就有的那一半是**探索**，条件组是**表达**，两件事。而且把四个档折进条件组
  要重做票 03 的 `WorkQuery` 形状与它那一批测试，风险与收益不成比例。
- **代价说清楚**：「存成子库」时两半都要折进同一条规则（`WorkQuery::to_rule` 做的），
  多一处会漏的地方；`识别状态` 那一档还折不进去（Q70）。
- **谁来裁：** 票 `12`（布局与视觉收尾）——要么把四个档做成「点一下就往条件组里加一条
  子句」的快捷键，要么明确两半各管一段并在屏上说清。
- **状态：** open

### Q74 — 规则语言里**没有**按路径匹配的维度，所以票 02 那条键形状的改动咬不到它

- **来自：** 票 `gui-redesign/04`
- **类别：** 核对过的、确认不用改的（但下一个人会问同一个问题）
- **在哪：** `crates/core/src/sublibrary/rule.rs::Dimension`
- **为什么记下来：** 票 02 把**变体的键**改成了「根名 + 相对根的路径」，挂单 Q58 说
  「`--under` 那类前缀从此带根名」，而这张票的硬约束点名「规则语言里凡是按路径前缀
  匹配的地方都受这条影响」。**核对结果：一处也没有。** 十三个维度筛的是平台、语言、
  中文、合集、收藏、类型、作品名、年份、评分、体积、开发商、发行商、简介——
  没有一维读变体的键。ADR-0020 的 NFC 归一管的是**路径键**，规则里的值不是路径键
  （作品名来自 DAT 条目，平台名来自平台清单）。
- **但 `^ 以…开始` 让「按根筛」变成一个自然的期待**：`键^卡二/` 这种写法眼下写不出来
  ——**键**与**根**都不是维度。浏览屏上按路径找那条路走的是**搜索框**
  （`VariantQuery::contains`，按 `variant.key` 取子串），而搜索框进不了规则（Q70）。
  于是「把某一块盘上的东西存成一个子库」眼下只能靠平台或合集绕。
- **要加是件小事**：一维 `根`（或 `键`），`VariantFacts` 多一列、`filter.rs` 里一条
  `variant.key LIKE ?`。没在这一票加，因为票据没点它，而且加一维**路径**会把
  「规则选的是内容」这条语义撑开一个口子——路径是内容住在哪儿，不是内容是什么。
- **谁来裁：** 票 `11`（子库屏；「这块盘上的全部」是不是个真需求，那一屏最看得出来）
- **状态：** open

### Q75 — `/code-review high` 报的四条，四条当场收掉

- **来自：** 票 `gui-redesign/04` 的收尾审查
- **类别：** 审查发现
- **在哪：** 见每一条后面的落点
- **为什么没停线：** 全部在这张票的范围之内，改动都不大，而且第 1 条正是这张票立的那条
  硬约束（屏上筛出来的那批 ＝ 同步搬过去那批）被打破的地方。
- **这张票实际做了什么：**
  1. **高 · 界面搭出来的规则存进子库会变成另一条** —— `Clause::build` 对值不做任何
     检查，而 `Rule::from_group` 把值原样拼进那行文本。在「作品 ~」那格里打
     `RPG 且 平台=GB`，屏上按**一个**子句筛（求值走的是内存里那棵树），存进中立库那行字
     下次读回来却是**两个**子句；`生存 或 毁灭`、`Pocket Monsters (Japan` 更是直接读不
     回来，落成 `BrokenRule`，那个子库一条都选不中。**而且静默。**
     `Clause::build` 加两道闸（值里不许有两侧带空白的连接词、括号得配对，新增
     `RuleError::JoinerInValue`），`Clause::parse` 那条路**不加**——它读的是库里已经
     躺着的规则（见 Q71）。
     审查顺带发现的**回归**一并收掉：`split_items` 原先全层数括号，把这一票之前合法的
     `作品~口袋妖怪(日` 变成了 `UnbalancedParen`。改成只认**项开头**那个 `(`
     （`opens_group` 一处说了算，`matching` 与 `split_items` 共用），老规则原样读得懂。
     测试：`rule.rs::值里落单的括号不影响这一层怎么切`、`造一条新子句时挡住印出去会走样的值`、
     `造出来的子句套进组里印回去还是它自己`。
  2. **中 · `facet_clause` 只挡逗号** —— 合集名是用户自己起的，「送朋友的 或 备份」
     「口袋(日版」都合法，折成规则后与第 1 条同样的后果。新增 `Unruly::Unwritable`，
     把 `Clause::build` 那两道闸翻成一条说得出口的话。测试补在
     `crates/core/tests/filter.rs::写不成规则的条件当场挡住而不是悄悄少写一条`。
  3. **中 · 建子库不是原子的** —— `save_as_sublibrary` 先 `put_sublibrary` 再
     `add_rule`，后一步失败会留下一个**一条规则都没有**的子库（同步过去是空的），
     而那个名字从此被重名检查占着，人按原名重试还被挡。改成失败就
     `remove_sublibrary` 退回去，并在回执里说清名字还回来了没有。
  4. **低 · 评分那一维两个求值器会分家** —— `YEAR_VALUE` 有「转回文字还是原样」那道闸，
     `RATING_VALUE` 没有对应的一手：`CAST('优秀' AS REAL)` 给 `0.0`（落在 0–1 里！）、
     `1.5` 照收，而内存那侧的 `parse_rating` 两种都扔掉。于是 `评分<=0.5` 在 SQL 那侧
     会把一批评分写成非数字的变体选中。补上「里面得有个数字」与「落在 0–1 之间」两道闸，
     测试 `crates/core/tests/filter.rs::评分那一维读不成数与超出范围的值两边都得扔掉`
     手塞三条值验它（真库里眼下一条评分都没有，挂账 D68）。
- **审查另外核对过、确认不用改的：** 各维在取不到值时都落 `COALESCE(…,0)`、`!=` 用
  `NOT COALESCE(…)` 翻过来，与 `satisfies` 的骨架逐条对得上；大小写折叠两侧都是
  `to_ascii_lowercase` / SQLite `lower()`（这一处审查途中刚从 `to_lowercase` 改过来）；
  语言两头补逗号之后四个运算符对应四种 `instr`，`en` 撞不上 `Danish`；`negate()` 的
  拍平规则与 `render_group` 的加括号规则互为逆（手推了 8 种组合）；旧平铺规则的
  `split_items` 行为与老 `split_clauses` 一致。
- **谁来裁：** 收尾
- **状态：** open

### Q76 — 撤销的中立库那一半**跟着中立库活**：重跑过识别之后就只撤得回沉淀库那一半

- **来自：** 票 `gui-redesign/08`（裁决的撤销干净）
- **类别：** 取舍
- **在哪：** `crates/core/src/catalog/identify.rs` 的 `verdict_batch_shadow` 两张表与
  `Catalog::clear_identifications`；`crates/core/src/triage.rs::undo_batch` 的
  `Undone::catalog_rolled_back`
- **为什么没停线：** 这是挂账 D102 选项 A 里那条实际约束（「B 要 DAT 库在手边，而 `undo`
  眼下不要求」）**剩下的那一小半**，而这张票已经把主要那一半解掉了。
- **这张票实际走了哪条路：** 撤销要放回中立库的那一份，**在这一批落下之前就收起来**
  （`stash_conclusions`），于是撤销**一个字节的 DAT 都不读**——D102-A 那条约束在常见路径上
  不再成立。收起来的东西（结论、候选、变体身上那两条链接）**全部可再生**，所以它按中立库
  的规矩活：`clear_identifications` 把它一起清掉，也就是**重跑一趟识别，那几批的快照就没了**。

  那时 `undo --batch` **照样把沉淀库那一半撤干净**，只是中立库那一半回不去；命令行印
  「⚠️ 这一批之后跑过识别……再跑一趟 `romcat identify`」，界面上那一行同样印
  （`Undone::catalog_rolled_back` 一处说了算，`triage.rs` 与 `gui/queue.rs` 共用）。

  **没有把快照搬进沉淀库**，理由是身份：沉淀库装的是**不可再生**的东西，而快照里
  一条条候选是 DAT 撞出来的，重跑一遍就有。搬进去还会让那份「可导出分享」的库背上几万行
  与别人毫无关系的本机缓存。**批本身与它盖掉的旧裁决在沉淀库里**——那两样确实不可再生。
- **代价：** 「裁完 → 跑一趟识别 → 才发现裁错了」这条路上，撤销只回滚沉淀库，用户要多跑
  一趟识别。真机上那一趟是 27 分钟。**不是数据损失**，是时间。
- **谁来裁：** 收尾。若要连这一档也撤干净，那就是 D102 的选项 B（撤销时重算），
  要 DAT 库在手边——那是一张新票。
- **状态：** open

### Q77 — **批**这个词还没进词表

- **来自：** 票 `gui-redesign/08`
- **类别：** 词表缺口
- **在哪：** `CONTEXT.md` 没有「批」；代码里是 `verdict::Batch`、`triage::undo_batch`、
  命令行 `romcat triage batches`，界面上是「撤回第 N 批」
- **为什么没停线：** 与 D103 同类——`docs/agents/domain.md` 说撞上词表缺口时**记下来交给
  `/domain-modeling`**，不是自己往词表里写一条。
- **这张票实际走了哪条路：** 全程只用「批」这一个词，不造同义词（**不写**「批次」「批号」
  「事务」「操作」）。要避的同义词已经有先例：`CONTEXT.md` 的**扫描代号**词条里
  `_Avoid_` 就写着「批次号」。定义草稿：**批** ＝ 一次批量裁决落下的那些裁决，
  连它们各自盖掉的那条一起；**撤销以它为粒度**。
- **谁来裁：** 收尾，和 D103 剩下的词一起交给 `/domain-modeling`
- **状态：** open

### Q78 — `romcat triage undo` 一条命令两种粒度

- **来自：** 票 `gui-redesign/08`
- **类别：** 界面取舍
- **在哪：** `crates/cli/src/main.rs::run_triage_undo`
- **为什么没停线：** 两种粒度都得留着，而票只要求「按批撤」那一种。
- **这张票实际走了哪条路：** 给了 `--batch` / `--last` 就走**按批撤**（两边一起回去），
  否则走原来那条**按选择器忘掉**（只动沉淀库）。没有拆成两个子命令，理由是
  「撤销」在用户脑子里是一件事；两条路各自的出口都印在输出里，选择器那条还多印一句
  「要两边一起回去，按批撤」。
- **为什么不干脆让选择器那条也回滚中立库：** 它选中的可以是**任何一条**裁决——
  `triage import` 收下的别人那一份不属于本机任何一批，也就没有「落下之前是什么样」
  的快照可放回去。硬做的话只能靠重算，那又要 DAT 库在手边（见 `Q76`）。
- **谁来裁：** 收尾
- **状态：** open

### Q79 — `/code-review` 在**已经提交的**票 03 / 04 代码里发现三条，本票不动它们

- **来自：** 票 `gui-redesign/08` 的收尾审查（它按 `origin/main` 比，于是把本地已提交的
  `ca680d9`（票 03）与 `f75a56b`（票 04）也一起看了）
- **类别：** 路过看见的无关缺陷
- **为什么没停线：** 三条都不在这张票动过的文件里，而收尾纪律是**只提交这张票动过的文件**。
  审查在这张票自己的代码里报的那一条（撤销漏掉重复拷贝）已经当场收掉，见下面「本票已收」。
- **本票已收（不在这条挂单的范围里，记在这儿只为对得上审查那份报告）：**
  `undo_batch` 里「同一条锚上第二份拷贝」的判据原先写成「锚上是不是空的」，而这一批
  **盖掉过旧裁决**时，第一份撤完锚上留下的是那条**旧的**——第二份于是被当成「别人的账」，
  它的中立库结论留着一条指向已经不存在的裁决的「命中」，再也回不到队列。判据改成
  「锚上眼下这个样子是不是这一批自己刚留下的」（`current == row.before`）。
  回归测试 `core/tests/triage.rs::几份同内容的拷贝盖掉过旧裁决时撤销照样把它们全都放回队列`
  ——把判据改回旧写法它会失败，已实测。
- **留给别人的三条：**
  1. **高 · `crates/core/src/sublibrary/rule.rs:788`（票 04）** —— `split_items`（与 `matching`
     第 731 行同款）里 `(` 只在**项开头**才入栈（`opens_group`，那是票 04 为 Q75 的回归特意
     改的），但 `)` 只要 `starts.len() > 1` 就无条件出栈。于是**组里**任何一个落在值里的
     `)` 会提前把组关掉：`平台=GB 且 (作品~Pocket (Japan) 或 平台=GB)` 读回来是
     `Err(MixedJoin)`。后果与 Q75 第 1 条同一种——屏上筛出来那批与存进子库的那批不是同一批，
     而且**静默**（落成 `BrokenRule`）。既有测试只走顶层，那一层 `starts.len() > 1` 为假、
     落单的 `)` 正好被忽略，所以盖不到。修法：每一层记一下值里非结构的 `(` 有几个，
     `)` 先抵掉它们再出栈。
  2. **中 · `crates/core/src/sublibrary/rule.rs:691`（票 04）** —— `negate` 的
     `if inner.nodes.len() == 1 || inner.join == Join::Any` 把 `Join::None` 也拍平了：
     `都不(都不(平台=GB))` 读回来是 `Group{None,[平台=GB]}`，**意思正好反了**。
     拍平只在 `inner.join == Join::Any`、或者 `nodes.len() == 1` 且 `inner.join != Join::None`
     时成立。
  3. **低 · 三处给用户看的话里夹着一长串空格** —— `sublibrary/rule.rs:506`（9 个）、
     `catalog/browse.rs:826`（17 个）、`gui/src/library.rs:907`（25 个）：折行时漏了 `\`，
     那串空格原样印出去。前两条是票 04、第三条是票 03/04 那批。
- **谁来裁：** 收尾——这三条要么开成一张新票，要么由下一个动 `sublibrary/rule.rs` 的人顺手收。
- **状态：** 部分 settled —— **前两条（高：值里的括号提前关组；中：`negate` 把双重否定
  折成意思相反的单重否定）由协调者在票 08 与票 09 之间的间隙修掉了**，见提交 `<seam5>`。
  括号那条的改法：每一层记着「还欠几个值里的左括号」，成对跳过；`split_items` 与
  `matching` 两处用同一套数法（那份代码自己的注释就要求两处逐字一致）。双重否定那条：
  里面那层本身是「都不满足」时一律不拍平。两条各有一条回归测试
  （`值里带括号的作品名不会把组提前关掉`、`双重否定翻回来而不是折成单重否定`），
  改回旧写法都会失败。
  **第三条仍然 open**：三处用户可见文案里夹着一长串空格（`rule.rs:506`、
  `browse.rs:826`、`gui/library.rs:907`）——纯文案，留给下一个动这几处的人顺手收。
