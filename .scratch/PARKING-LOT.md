# 挂单：实现时撞上、但没有为它停线的东西

**Status:** living（本轮队列全部票共用一份）
**Opened:** 2026-09-05（第二轮；第一轮 2026-09-04 开，132 条已在收尾时清空并迁回票据）
**Effort:** 第二轮队列 = `queue-followups` 10 张（真机那张 `09` 是人工票，不排进来）+ `gui-redesign` 13–16 四张

**编号接着上一轮往下走，从 `Q133` 起。** 上一轮的 `Q1`–`Q132` 已经迁回各自的票据末尾
（「挂单裁决」一节），编号留着占号、**不复用**——票据里引用得回来。

---

## 这份文件是什么

接票实现时总会撞上票没预见到、或者票写错了的东西。**这里面几乎没有一件该停下整条队列。**
它们落在这儿：**记下来、绕过去、接着走、收尾时一起裁。**

它不是项目的长期缺口清单（那是 `.scratch/rom-metadata-automation/deferred.md`，编号 `Dn`，全局连续）：

| | 挂账 `deferred.md` | 这份挂单 |
|---|---|---|
| 装的是 | **产品**里的取舍 | **执行**里冒出来的问题——票写错了、规格没说、两份东西互相矛盾 |
| 寿命 | 活得比这轮队列长 | **收尾时清空**——每条要么就地裁掉、要么升进 `deferred.md`、要么开成新票 |
| 谁读 | 下一个动这块产品的人 | 收尾那一步，和拿主意的人 |

**编号分两套，别混：** 这里用 `Q1、Q2…`；`deferred.md` 用 `D1、D2…`。
一条从这里升进 `deferred.md` 时才拿 `Dn` 号，且这边的 `Qn` 留着不复用。

---

## 停线——只有这三条

停线很贵：拿主意的人得把整个上下文重新装回脑子。门槛：

1. **接着做会写出一个错的东西，而且撤销它要伸到这张票之外。**
   典型：票里的指示与上游一条仍然生效的决定（ADR、词表、规格）相悖。
2. **动作不可逆，而且不止一个方向站得住。**
   典型：删掉或改名一个被很多处引用的东西——一个 id、一个字段、一份文件。
3. **前提根本不在，票做不下去。**
   典型：「在 X 上面盖 Y」，而 X 不存在。

其余一律：记在这儿，挑最站得住的那条路走，接着做。这包括：票里的数字与你实测的对不上、
验收条目读着含糊、票没想到的第三种情况、路过看见的无关缺陷、以及你确实拿不准的单条。

> ⚠️ **绕过去要留痕。** 条目记的是**你走了哪条路**，不是「有两条路」。
> 说不出自己做了什么的条目，不算一条条目。

---

## 条目格式

```markdown
### Q<N> — <一句话：这是什么>

- **来自：** 票 `<feature>/NN`
- **类别：** 票写错了 / 规格没说 / 两份东西矛盾 / 工具缺陷 / 路过发现，不在范围内
- **在哪：** 路径 + 行号或 id —— 要点得过去
- **为什么没停线：** 一句话
- **这张票实际做了什么：** 一句话 —— **必填**
- **谁来裁：** 拿主意的人 / 票 `NN` / 收尾
- **状态：** open
```

编号**接着上一轮往下走**（第二轮从 `Q133` 起），不要按票分节——「来自」那行已经带了这个信息。

---

## 条目

<!-- 从 Q133 往下追加；不要重排，不要删 -->

### Q133 — 票说「执行层压根不比落点」，其实闸早就在，缺的只是大小写那一半

- **来自：** 票 `queue-followups/01`
- **类别：** 票写错了
- **在哪：** `crates/core/src/sync/execute.rs` 的 `place` / `landing`（提交 `3fa668c` 加的）
- **为什么没停线：** 判据是那条红测试，票的**归因**写偏了不影响要做的事。
- **这张票实际做了什么：** 照红测试做。实测那道闸在**大小写不敏感**的目标上本来就管用
  ——`real_path` 先拿 `root.join(key)` 试一次，不敏感的文件系统上它开得了别人那份，
  于是 `taken` 为真、新增被挡下（`大小写不敏感的目标上_清单之外只差大小写的文件不被顶掉`
  一直是绿的）。漏的是**敏感的盘**：`real_path` 只折 NFC（ADR-0020），大小写归文件系统
  自己认，ext4 上它答「空的」。所以这一票补的是「同一张卡换台机器插，行为得是同一个」，
  不是从零加一道闸；票里那句「两种情况都报告成功」只对一半。
- **谁来裁：** 收尾
- **状态：** open

### Q134 — 落点占用这道闸每一条新增都要现列一层目录，没做缓存

- **来自：** 票 `queue-followups/01`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/sync/execute.rs` 的 `occupied_by` / `folded_child`
- **为什么没停线：** 代价的**量级没变，常数翻了一倍**——`landing` 走的 `real_path`
  一直就是这样：直接拼那一下打不开就逐段列目录，于是每一条新增本来就要把落点那条路径
  上的每一层列一遍；`real_path` 每次现开一个 `DirCache::default()`，`occupied_by` 又
  不复用它，于是同一层现在被列两遍。
- **这张票实际做了什么：** 走「**不缓存、每次现列**」这条路。另一条路是在一趟 `run` 里
  按目录记一份折起来的 listing（`fs::DirCache` 那种记性），常数能减半；没走，是因为
  这道闸要防的正是「计划算完到真的改名之间，卡还插在机器上」，一份跑到一半就过期的
  listing 会把那道缝重新打开——而这条缺陷是十七张票里唯一会弄丢用户数据的那条。
  真要治代价，该治的是 `real_path` 与它共用一份 listing，那是整条链路的活，不是这一票的。
- **谁来裁：** 收尾（或并进「翻页代价」那张票一起量）
- **状态：** open

### Q135 — 「不敏感的卡上行为一致」这一半，开发机上造不出来

- **来自：** 票 `queue-followups/01`
- **类别：** 规格没说
- **在哪：** `crates/core/tests/sync_run.rs`（三条落点占用的测试）；
  `crates/core/src/sync/execute.rs` 的 `place` / `landing` / `occupied_by`
- **为什么没停线：** 两边判的现在是**同一个** `path::fold`，敏感那一边实测挡得住，
  不敏感那一边只会更容易挡住（`real_path` 直接拼那一下就开得了别人那份）。
- **这张票实际做了什么：** 照勾那一条验收，并写明保留。敏感的盘（这台是 ext4）
  由三条新测试实测；不敏感那一边要一个不敏感的挂载点才造得出来，而 `execute` 这一层
  直接吃 `std::fs` 与 `fs::RealFs`，没有能塞进一个假文件系统的接缝——加那道接缝是
  改这一层的形状，超出这一票。走的是「照勾 + 写明保留 + 开这一条」这条路，
  没有为它加接缝、也没有把验收条目改小。
- **谁来裁：** 拿主意的人（真卡上跑一趟，或者决定给 `execute` 加一道可注入的目标侧接缝）
- **状态：** open

### Q136 — 列不开的目录底下，折起来那半边闸整个失效

- **来自：** 票 `queue-followups/01`（`/code-review` 报的第 5 条）
- **类别：** 规格没说
- **在哪：** `crates/core/src/sync/execute.rs` 的 `folded_children`；
  模块文档七与 `sync::TargetState::unlistable_dirs`
- **为什么没停线：** 漏的那一格窄，而且**不丢数据**——逐字那半边不受影响
  （`real_path` 直接拼那一下在列不开的目录里照样开得了文件），于是大小写不敏感的目标
  上照样挡得住；剩下的只有「敏感的盘 + 列不开的目录 + 只差大小写的占用」，后果是在
  维护者那份**旁边**多写一份（敏感盘上两份本来就能并存），不是顶掉它。
- **这张票实际做了什么：** 走「**把洞写进文档、不补**」这条路。原先那句理由
  （「列不开的目录底下写不进去东西」）是错的——Unix 上 `0300` 的目录列不开却写得进
  ——已改掉，换成把这一格说清楚。没补，是因为补它要把「列不开」与「列开了没找到」
  分成两态从 `read_dir` 一路带到 `place`，那是改这一层形状的活；而模块文档七偏偏点名
  `unlistable_dirs` 是执行层这道闸的头号理由，所以这条得有人裁，不能只留在注释里。
- **谁来裁：** 拿主意的人（值不值得为这一格加一层「读不动是第三态」，ADR-0021 的口径）
- **状态：** open

### Q137 — 剥离规则的指纹**有意不盖 `中文源平台别名`**

- **来自：** 票 `queue-followups/04`
- **类别：** 规格没说
- **在哪：** `crates/core/src/filename.rs` 的 `Rules::fingerprint`；
  `crates/core/src/scrape/zh.rs` 的 `variant_probe` / `work_probe`
- **为什么没停线：** 票自己就给了判据——「指纹只认**会改变正题的那部分**」，
  而那张别名表一个字都不改正题：它只在**建索引**时用得着（`zh::sync`）。
- **这张票实际做了什么：** 走「**别名不进这份指纹**」这条路，并把理由写进了
  `Rules::fingerprint` 的文档与一条测试（`中文源平台别名不进这个指纹`）。理由是另一条线
  （提交 `0f3a480`）已经论证过的那一条：别名对刮削的全部影响都由**建索引那一刻折出来的
  那一串**（`zh::Index::platform_fold`，已进两层指纹）盖着；再盖一遍的话，
  「改了别名但还没重建索引」会让指纹变、可这一趟的输入一个字都没变——那是白重采一整趟。
  同理，`版本模式` / `容量模式` 两张表**逐字折小写、按内容排序再折**：它们只做「有没有
  一条对得上」的判断，而模式里的字面量本来就大小写不敏感，调换次序、改个大小写写法
  都不改正题。于是这份指纹的口径是「剥出正题那一套的语义」，
  **不是「这份规则文件的全部内容」**——名字叫 `fingerprint()` 而口径窄了一格，
  将来谁拿它去回答别的问题（比如「用户换过规则文件没有」）会答错。
- **谁来裁：** 拿主意的人（要么认下这个口径，要么把它改名成说得出口径的名字）
- **状态：** open

### Q138 — 交接说的基线「1,543 条通过」，实测是 1,528

- **来自：** 票 `queue-followups/04`
- **类别：** 票写错了（交接里的数，不是工单正文）
- **在哪：** 门禁 `cargo test --workspace --all-features -j 1 -- --test-threads=2`
- **为什么没停线：** 差的那 15 条不是这张票弄丢的——三个动过的文件里 `#[test]` 只增不减
  （26→31、37→39、52→53，共 +8），`git status` 里也只有这三个文件，而且零失败。
- **这张票实际做了什么：** 没去追那 15 条的出处（要么另一份 feature 组合、要么那次计数
  的口径不同），照实报自己量到的数：58 个 `test result: ok`、1,536 条通过、0 失败
  ——1,528 + 本票新增的 8 条。下一个接票的人别把这个差额当成自己弄丢的。
- **谁来裁：** 收尾（重新量一次基线，或者说清那次是怎么数的）
- **状态：** settled —— **是交接那个数错了，不是这张票弄丢的。** 协调者跑基线时把
  `sync_run` 单独跑了一遍（15 条）**又**跑了全量（58 个目标 / 1,528 条），然后把两段
  加起来报成「59 个目标 / 1,543 条」。**真实基线就是 58 / 1,528。** 分段跑之后累加，
  同一个目标会被数两遍——这一轮第二次栽在「判据本身不可靠」上（上一次是拿
  「grep 没输出」当通过）。往后报基线一律只认**一趟全量**那一段的数。

### Q139 — 票 05 点的那三处长串空格，开工时已经全被修掉了；同款的第四处在别的文件里

- **来自：** 票 `queue-followups/05`
- **类别：** 票写错了（原挂单 Q79 的行号与状态都停在半年前那一刻）
- **在哪：** `crates/core/src/sublibrary/rule.rs:506`、`crates/core/src/catalog/browse.rs:826`、
  `crates/gui/src/library.rs:907`（Q79 记的旧行号；`library.rs` 后来被票 12 的 Q61
  改名成 `browse.rs`），新收的那一处在 `crates/cli/src/main.rs:2051`
- **为什么没停线：** 三处都已经是绿的，没有可撤销的东西；票的实质（用户可见文案里不该
  夹一长串空格）反而在别处还差一处。
- **这张票实际做了什么：** 逐处核对过 `HEAD`，**三处都已由前面的提交顺手收掉**——
  `rule.rs` 那处在提交 `510c302`（造新子句那道闸那一趟，整句重写时补上了 `\`），
  另两处在票 12 那批界面重构里随文案改写一并补上。判据是实测：`\` 续行在 Rust 里会
  连换行带**后续全部前导空白**一起吃掉（当场编了个小程序验的），而这三处现在行尾都有 `\`。
  于是走「**按验收项的实质收一处真的**」这条路：写了个词法级扫描器（跳过注释、
  raw string，只看普通字符串里**没有** `\` 的字面换行）扫了全仓 `crates/**/*.rs`，
  唯一确凿的用户可见文案漏网是 `cli/src/main.rs:2051`（模型推断那段「没答回来」的提示，
  折行后 13 个空格原样印出去），补了那个 `\`。没有别处再漏。
- **谁来裁：** 收尾（这一条只是把账对上，不必再有人动手）
- **状态：** settled —— 三处早已绿，第四处已收。

### Q140 — 票 05 给的第一条路（别名整档垫到汉化名之下）两种读法都不成立，改走「另立判据」

- **来自：** 票 `queue-followups/05`（Q24 的裁决落地）
- **类别：** 票写错了（给的两条出路里，第一条解不了它要解的那条）
- **在哪：** `crates/core/src/title.rs` 的 `rank`（排序键）
- **为什么没停线：** 票自己写着「选哪条要看它对已有那批显示标题影响多大」，那就是让人
  去量；量完两条读法都被现有测试判死，第三条路不动任何已有结论。
- **这张票实际做了什么：** 把票给的那条路按两种读法各改一遍、各跑一遍标题那两组测试：
  - **读法一（`中文·别名` 与 `中文·汉化组自取` 互换名次，3 ↔ 4）**：测试全绿，
    但**根本解不了这条缺陷**——`name_cn` 是拉丁串时正名落在第 7 桶，中文别名从第 3 桶
    挪到第 4 桶照样赢。
  - **读法二（`中文·别名` 不再享有语言优先，落进「其余」那一桶）**：解得了，代价是
    **破三条测试**（`显示标题按中文英文日文文件名回退`、`排序标题独立生成而不是拿中文
    标题去排`、`中文名带置信度低的那些进得了队列`），它们钉的是「中文优先」这条回退链
    本身，而受连累的是**库里那 16,420 个只有文件名的变体**（`TitleKind::Alias` 的文档
    原话：「库里那些中文文件名多半是这一档」）——那是全库最大的一档。
  于是走**另立判据**那条路：在 `rank` 的排序键里，**裁决之后、语言与类型的档位之前**
  插一层，按来源认出 `中文离线源·别名` 这一路（`fuzzy::ALIAS_SOURCE`）整路垫底。
  这一层只动那一路，别的比较**逐条不变**（对所有别的来源它恒为 0），全量门禁
  58 / 1,537 / 0，只多了本票新增的那一条测试。断言这道闸管用的**四处**注释
  （`title::choose` 的回退链、`title::classify` 里那段、`priorities.toml` 标题那一节的
  ⚠️、`fuzzy::ALIAS_SOURCE` 的文档）全部改成了现在这套说法。
  **代价记在这儿**：`rank` 从此认得一个具体的**来源名字**，而它原来只按
  (语言, 类型, 置信度, 表里名次) 这些通用的面排序。同一路要是将来多出第二个源
  （比如别的数据源也给「那条条目还叫什么」），得记着回来加。真要做干净，得给
  `TitleRow` 添一列「这个名字说的是不是手上这个东西」，那是中立库的结构版本要动的活。
- **谁来裁：** 拿主意的人（认下这个按源名认的判据，还是排一张票把它升成 `TitleRow` 的一列）
- **状态：** open

### Q141 — 显示标题不再是中文的那一类作品，报告的中文口径与详情面板会各说一套

- **来自：** 票 `queue-followups/05`（收尾 `/code-review` 的产出）
- **类别：** 路过发现，不在范围内（这一票的直接后果，但对齐哪一侧是产品决定）
- **在哪：** `crates/core/src/title/report.rs:153`（`chinese_works` / `rival_works` /
  `queue_works` 三个计数的闸都是 `chosen.language == Language::Chinese`）、
  `crates/core/src/catalog/detail.rs:265`（详情面板走 `title::best_chinese`）、
  `crates/gui/src/browse.rs:2352` 那句「中文标题取的是「{}」」
- **为什么没停线：** 报告那一侧的口径**没变，变的是事实**——`chinese_works` 的文档写着
  「拿到中文显示标题的作品数」，那一类作品的显示标题现在确实不是中文了，如实少算才对；
  `queue_works` 同理（它盯的是「会被写进前端的那个标题可不可信」，而那个标题现在是
  中置信、有出处的正名，不是低置信的别名）。
- **这张票实际做了什么：** 走「**把契约写准，不动行为**」这条路。`best_chinese` 答的是
  「中文那一档的第一名」而不是「显示标题」，两者分开的**两种**情形（人裁过一个非中文的
  叫法；中文那一档里只剩「那条条目还叫什么」）已经写进它的文档。没有改 `best_chinese`
  的行为——那条别名**确实是**这个作品的一个中文叫法，界面要说得出「有」还是「没有」，
  用户也要搜得到（本票硬约束）。留着的那半是**界面文案**：面板那句「中文标题取的是
  「我的世界」」在第 2 种情形下会让人以为前端里能看到中文，而实际写进去的是
  `Minecraft`。要么面板改口（「这个作品有个中文叫法，但显示标题不是它」），要么报告
  多一栏「有中文叫法但显示标题不是中文的作品」。**没量过这一类在真库里有多大**——
  那要跑一趟真库（票 `queue-followups/09` 的活）。
- **谁来裁：** 拿主意的人（对齐哪一侧是产品决定），量交给票 `queue-followups/09`
- **状态：** open

### Q142 — 收尾审查在**别的票已提交的**代码里报了两条，本票不动

- **来自：** 票 `queue-followups/05` 的收尾审查（它按 `origin/main` 比，于是把本地已提交
  的 `a481964`（票 01）与 `229710a`（票 04）也一起看了）
- **类别：** 路过看见的无关缺陷
- **为什么没停线：** 两条都不在这张票动过的文件里，而收尾纪律是**只提交这张票动过的
  文件**。审查在这张票自己的代码里报的那一条（报告口径）已经就地收了，见 `Q141`。
- **这张票实际做了什么：** 没碰它们，原样记在这儿：
  1. **低 · `crates/core/src/sync/execute.rs:340`（票 01）** —— `AlreadyExists` 那道豁免
     比「落点被占」宽。`create_dir_all` 在「该建目录的位置上躺着一个文件」时返回的
     正是 `AlreadyExists`：卡上有个文件叫 `gb`（不是目录），要写 `GB/tetris.zip`，
     `occupied_by` 第二层 `read_dir` 失败后 `unwrap_or_default()` 答不出，`landing` 同理，
     于是走到 `create_dir_all` 才炸。这一整个平台目录下每一条新增都以同一句话失败，
     而 `consecutive` 一次都不涨、`gave_up` 永远不触发——正是 `GIVE_UP_AFTER` 那句
     「一个一个试过去只会把同一句错误印上几百遍」要防的场景。建议把判据收紧到
     「这一步是被 `place` 那道闸挡下来的」，别认 `io::ErrorKind`。
  2. **低 · `crates/core/src/sync/execute.rs:678`（票 01，与挂单 `Q134` 同一处）** ——
     `occupied_by` 把 `landing` 刚列过的目录又列一遍，两处不共享 `DirCache`。`Q134` 否掉
     的是**按整趟 `run` 缓存**（那会把 TOCTOU 那道缝重新打开）；把作用域收到**单次
     `place()` 调用之内**（`landing` 建、`occupied_by` 复用）两问相隔微秒，比现在分两次
     列还少一点过期窗口，常数减半。建议补进 `Q134` 的备选方案。
  审查另报的第 3 条（票 15 的验收正文 `.scratch/rom-metadata-automation/issues/15-title-set-and-display-sort.md:71` 还写着旧的「六层」）**没改**：那是一张已经完成、
  已归档的工单，改它的验收正文会让那张票的记录失真。活着的那几处抄本
  （`title/report.rs`、`docs/library-facts.md`、`priorities.toml`、`fuzzy.rs`、`zh.rs`、
  `tests/scrape.rs`）本票都已同步到七层。
- **谁来裁：** 收尾——两条要么开成新票，要么由下一个动 `sync/execute.rs` 的人顺手收
- **状态：** open

### Q143 — 读不懂的规则给的是「扔掉」这一个动作，不是「改对它」

- **来自：** 票 `gui-redesign/14`
- **类别：** 票给了三条路，选了第三条（理由在下面）
- **在哪：** `crates/gui/src/browse.rs::Screen::broken_rules_ui` /
  `::discard_broken_rule`、`crates/core/src/catalog/sublibrary.rs::discard_broken_rule`
- **走的是哪条路：** 票给的第三条——**浏览屏筛选栏顶上那条横幅**。「正在改子库「X」的
  选择集」那条横幅本来就印在那儿，读不懂的那几条跟着「改选择」整条带过去，摆在它底下、
  各给一颗「扔掉这条」。
- **有没有在「这一屏不选内容」上开口子：** **没有。** 子库屏一行写的代码都没加——
  卡上那几条照旧只读地摆着（红的、连它错在哪），改的只是末尾那句指路：从
  「界面上改不动，去命令行删」改成「按上面『改选择』跳去浏览屏，横幅里逐条扔得掉」。
  票 `11` 立的「规则增删、例外记撤的控件要搬走」一个字没动。
- **另外两条路为什么没走：**
  1. **给筛选器加一个「读不懂的原文」节点**（票给的第一条）——那棵树是
     「**读得懂**」这件事的具象：`Filter` 每个节点都是一条 `Clause`，`WorkQuery::to_rule`
     把整棵树折成一条规则带回子库。塞一个不参与求值、也折不进 `to_rule` 的异类进去，
     「屏上筛出来的 ＝ 子库选出来的」（票 `11` 验收第 3 条）与「规则原样带回」
     （第 4 条）两条口径都要各开一个洞，而它们各自钉着一条测试。
  2. **在子库卡上只给「删」**（票给的第二条）——它正是票正文点名警告的那一颗：
     开一个口子，规则增删会一条条回来。而且那一屏上删完之后人还得再跳一趟浏览屏
     去补一条对的，路反而更长。
- **为什么只给「扔掉」不给「改对它」：** 改一条读不回来的原文，要的是一个**规则语言的
  文本框**——那正是这一版界面拆掉的东西（票 `11` 把八个概念降到三个）。改对了的那一条
  走上面的筛选器重筛一遍、按「更新到子库」带回去，那条路本来就通。
  **代价：** 一条写了一半、只差一个括号的规则，眼下只能扔掉重筛，原文捡不回来
  （屏上印着它，人能照着抄）。真库上眼下一条这样的规则都没有
  （`add_rule` 只收读通了的 `Rule`）。
- **那道闸落在核心里而不是界面上：** `Catalog::discard_broken_rule` **先读一遍再决定
  删不删**，读得懂的那几条它拒绝（`Discarded::Readable`）。界面只递一个序号过去
  （ADR-0005）——不然一次「扔掉读不懂的」就能删掉一条好的，而屏上写的是另一回事。
- **谁来裁：** 收尾
- **状态：** open

### Q144 — 扔掉一条读不懂的规则会把子库屏缓着的差量与容量账一起丢掉，而它并不改变选出什么

- **来自：** 票 `gui-redesign/14` 的收尾审查（`/code-review high` 报的三条之一，medium）
- **类别：** 有意的保守，代价记下来
- **在哪：** `crates/gui/src/browse.rs::Screen::discard_broken_rule` 的 `self.touched`
  → `crates/gui/src/app.rs::App::route` → `crates/gui/src/sublibrary.rs::Screen::forget`
  → `::open` → `::invalidate`
- **是什么：** 扔掉一条**读不懂**的规则不改变这个子库选出什么（它本来就没参与求值），
  可它留下的那个记号是**通用**的那一根：`forget` 会把这一台缓着的差量预览与容量账
  一起丢掉。于是「排完一趟预览 → 跳去浏览屏扔掉一条坏规则 → 回来」要重排一趟，
  而大库上那一趟是走一遍全库折事实（343 ms 起，挂单 `Q87`）。
- **这张票实际做了什么：** **留成这样，没另开一根「只重读、不作废」的轻通道。**
  理由是severity不对称：那根通道挡的是票 `11` 收尾审查报的那一条（挂单 `Q91` 第 2 条）
  ——浏览屏一按就落库、子库屏还攥着旧差量，而「同步」认的正是它，按下去会把刚排除掉的
  那个传上卡。多一根轻通道就多一个「真改过选择却没作废」的入口，而那一类的代价是
  **把过期的计划传上卡**，比重排一趟贵得多。真库上眼下一条读不懂的规则都没有，
  这条路一年也走不了一次。
- **要收的话怎么收：** 子库屏那边分出 `reread`（只重读 `rules` / `broken` / `exceptions`，
  不碰 `prepared` / `evaluated`），浏览屏这边分出第二个记号；两根通道各自钉一条测试，
  并在 `forget` 的文档里写死「哪些改动只准走轻的那根」。
- **谁来裁：** 票 `gui-redesign/15`（子库屏那两条长活搬上任务台的那一票——差量重排的
  代价正是那一票在算的东西）
- **状态：** open

### Q145 — headless 里「这一帧真画出来的字」那个助手，三份测试各抄了一遍

- **来自：** 票 `gui-redesign/14` 的收尾审查（不算缺陷的一条提醒）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/tests/layout.rs::画出来的字`、`crates/gui/tests/queue.rs::画出来的字`、
  `crates/gui/tests/sublibrary.rs::画出来的字` —— 三份**逐字一样**
- **为什么没停线：** 不是缺陷。而收的话要动另外两张票的测试文件，
  而收尾纪律是**只提交这张票动过的文件**。
- **这张票实际做了什么：** 照着前两份原样抄了第三份，没动前两份。
- **要收的话怎么收：** 挪进 `romcat_gui::headless`（`headless::frame` 已经在那儿），
  三处测试各换成一行。下一个要断言「屏上写的是什么」的测试文件会变成第四份。
- **谁来裁：** 收尾，或者下一个动 `crates/gui/tests/` 的人顺手收
- **状态：** open

### Q146 — 「还没识别」裁成两个词：第二种用法叫「没有候选」，而五屏上仍印着旧词

- **来自：** 票 `gui-redesign/16`（接 `Q84`）
- **类别：** 两份东西矛盾
- **在哪：** `CONTEXT.md` 的**还没识别**与新收的**没有候选**两条；
  `crates/core/src/catalog/identify.rs` 的 `Tier::Unidentified::label()`（返回「还没识别」）
  与 `NOT_RUN_LABEL`（同样四个字）；`crates/core/src/catalog/browse.rs` 的
  `WorkRow::confidence_label` 与 `StateFilter::{label, from_label}`；印这一档的界面在
  `crates/gui/src/{table.rs,queue.rs,look.rs}` 与 `crates/gui/src/browse.rs:1870,1876,2133`。
  **票 16 的 `Q84` 给的是 `crates/gui/src/browse.rs:976,996`，那两处是 `load_detail` 与
  `status`，一个字都不印这一档**——本条按实际印的地方重记了一遍。
- **为什么没停线：** 票 16 把这一裁明写着交给实现者（`Q84`），而它的验收最后一条又明写
  「代码里一个名字都没改」——裁得动词表，动不了标签。
- **这张票实际做了什么：** **裁成两个词，不并成一个。** 词表里**还没识别**仍是「一个变体
  连识别都还没跑过」，另收一条**没有候选**＝「识别跑过了、却一条候选都没有」。
  并成一个的话，「跑过没跑过」这条界线就没词可说了，而它正是**命中率的分母**那条界线
  （ADR-0002 那套置信度与待确认队列的账，加上核心里 `not_run` 与 `Unidentified` 已经被
  刻意分开的 Rust 名字）；ADR-0021 立的正是「不可读是第三态、不许并进已变或已删」这条纪律
  ——识别这一侧不该反过来把两件事并回一个词。**代码一个字没动**：那一档打给用户的仍是
  「还没识别」，于是词表与界面眼下对不上。
- **它不是换个字符串那么简单：** `Tier::of` 收的是 `Option<Confidence>`，
  **`None` 同时装着「连识别都没跑过」与「跑过了、一条候选都没有」**——
  `crates/core/tests/works.rs` 里那批散落的行就是拿 `confidence == None` 断言
  「还没识别」的。直接把 `Tier::Unidentified::label()` 换成「没有候选」，
  连识别都没跑过的变体也会被印成「没有候选」，当场违背词表新收的那条。
  同一个坑还有一处：`StateFilter::from_label` 拿的是一个**裸字符串字面量**
  「还没识别」，不是 `NOT_RUN_LABEL`，换掉 `label()` 那一半而漏了它，筛选器的
  往返就静默断掉。
- **要收的话怎么收：** 票 `gui-redesign/17` —— 先让浏览与队列那一侧分得出这两件事
  （库里已经有 `not_run` 那份账），再各印各的词，五屏与 `spec.md` 一起改。
- **谁来裁：** 票 `gui-redesign/17`
- **状态：** open

### Q147 — 收了「跳过」，却没收它那三个同档兄弟，也没收「依据形状」

- **来自：** 票 `gui-redesign/16`
- **类别：** 路过发现，不在范围内
- **在哪：** `CONTEXT.md` 新收的**跳过**与**批**两条；
  `crates/core/src/catalog/identify.rs::State`（命中 / 未命中 / 无判据 / 跳过四档标签）、
  `crates/core/src/triage/batch.rs::Shape`（一级分批的键）
- **为什么没停线：** 票点名的是四个缺口，这几个词一个都不在名单上，而它们不收也读得通。
- **这张票实际做了什么：** **票点名之外只多收了一条：依据形状。** 头一版把它在**批**那条里
  写成「一组**依据**长得一样的待裁决变体」想绕开，那句话是**假的**——
  `triage::batch::Shape` 有两支，`Bare { state, reason }` 那支装的正是**一条候选都没有**的
  条目（真机上队列里的绝大多数），它们连一条**依据**都没有，按的是结论与理由。
  绕不开就别绕：收成一条**依据形状**，**批**那条回到票里的原话「一组依据形状相同的待裁决变体」。
  「命中 / 未命中 / 无判据」仍不收——它们没有「跳过」那个两个意思的毛病，靠**还没识别**条里
  那句对照就说得清。「补缺」「重采」也不另立词条，写在**采法**条内部，照**连接**那条
  把三种连接写在条内的先例。
- **谁来裁：** 下一次 `/domain-modeling`
- **状态：** open

### Q148 — 词表裁完了，代码里还有四处文档指着 `Q77` / `Q80` / `Q84` 说「词表眼下没收」

- **来自：** 票 `gui-redesign/16` 的收尾审查
- **类别：** 两份东西矛盾
- **在哪：** `crates/core/src/triage/batch.rs` 模块文档（「词表眼下两个都没收（挂单 Q77、Q80）」）、
  `crates/gui/src/queue.rs`（「先放着」那一处注释指着 `Q80`）、
  `crates/core/src/identify/report.rs`（指着 `Q84`）、
  `crates/core/src/catalog/identify.rs` 的 `Tier::Unidentified` 与 `NOT_RUN_LABEL` 两段文档
  （「两个用法的取舍记在挂单 `Q84` 上，交给 `/domain-modeling`」）
- **为什么没停线：** 票 16 的验收最后一条是「**代码里一个名字都没改**」，而这四处都在 `crates/` 底下。
  改注释不改名字，字面上过得去；但这一票的全部价值就是那十二张票攒下的一致性，
  为了四行注释去碰四个文件、再跑一趟全量门禁，赔的比赚的多。
- **这张票实际做了什么：** **一处没动，记在这儿。** 词表已经收了**批**（两个意思并排）、
  **跳过**与**先放着**、**还没识别**与**没有候选**，那四处文档从今天起是过时的：
  它们说的「词表没收」「交给 `/domain-modeling`」都已经不成立。
- **要收的话怎么收：** 顺手并进票 `gui-redesign/17`（它本来就要动 `catalog/identify.rs` 与那几屏），
  把这四处改成指向 `CONTEXT.md` 的对应词条。
- **谁来裁：** 票 `gui-redesign/17`
- **状态：** open

### Q149 — `zh sync` 断网时不再顺手把等着重建的那份索引补回来

- **来自：** 票 `queue-followups/03`
- **类别：** 票没说的第三种情况
- **在哪：** `crates/cli/src/main.rs::run_zh_sync`（从前那句 `heal_zh_store`）
- **为什么没停线：** 这是票正文点名的那个顺序（「先取到远端版本、确认指纹没变，才就地
  重建」）的必然后果，而**离线重建那条路一条都没少**。
- **这张票实际做了什么：** **走的是「`run_zh_sync` 干脆不 heal」那条**（挂单 `Q15` 摆出的
  两条出路之一，另一条是「把 heal 挪到取到 latest 之后」）。挑它是因为 `zh::sync::sync`
  自己那道闸本来就把「等着重建的那一份」排除在「指纹没变就跳过」之外，取数那一趟顺手
  就把重建做了——挪 heal 等于在同一条路上再摆一份同样的判断。代价：`aux/latest.json`
  取不回来（断网、闸门拦下）时，`zh sync` 现在整条失败，而从前会先把索引就地重建好。
  离线重建仍有四条路：`identify`、`scrape`、`zh find` 与界面刮削，它们走
  `heal_zh_store` / `open_zh`，一个网络请求都不发。真要让 `zh sync` 断网时也补，
  得在「取 latest 失败」那一支上补一句回落——那是「网络不通时这条命令该怎么收场」的
  口径问题，不是这一票的正题。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q150 — 下载那 435 MB 仍然停不掉：Ctrl-C 要等它下完才生效

- **来自：** 票 `queue-followups/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/zh/sync.rs::sync` 里那句 `fetcher.download`；
  `dat::fetch::Fetcher` 那个接缝不收中断信号
- **为什么没停线：** 票的验收说的是「**重建期间**按得停」，而重建那一段（读+解 960 MB，
  时间的大头）现在停得下来了。
- **这张票实际做了什么：** 只把**读原件**那一段接上了中断信号
  （`zh::sync::Context::cancel`，每读一条看一眼）。下载那一段照旧：按下 Ctrl-C 之后要等
  那 435 MB 下完，才在读第一条记录之前收手。**没有假装它停得下来**——
  `sources::refetch` 的文档已经改成「`zh::sync::sync` 接得住的只是读那份原件那几分钟，
  下载那 435 MB 照样停不掉」。与挂单 `Q62`（三条取数入口都停不下来）是同一件事的一部分。
- **谁来裁：** 挂单 `Q62` 那条活
- **状态：** open

### Q151 — 界面刮削那一趟把「按停了」折成一句对不上的话，任务台会把它记成失败

- **来自：** 票 `queue-followups/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/src/scrape.rs` 四处 `task.step("…").map_err(|_| "按停了".to_string())`
  （695、698、720、728 行），排活的地方在同文件 404 行
- **为什么没停线：** 早在这张票之前就是这样，与「中文索引重建这条路」无关。
- **这张票实际做了什么：** **没碰它，只在核心库这一侧把口径对上。** 任务台分
  「停了」与「失败」的判据是**那句话正是 `Halted` 交出来的那一句**
  （`task::Board::settle`），差一个字就记成失败。这一票新开的那条停下之路两处都对得上：
  `zh::sync::SyncError::Halted` 是 `#[error(transparent)]`，`sources::refetch` 也不再往
  前头加「取中文离线源失败：」。gui 那四处交的是 `"按停了"`，与
  `Halted.to_string()`（「按停下了：停在两步之间，没留下半截状态。」）差着字——
  界面上按一下「停下」，那一趟会显示成失败，用户会以为自己按坏了什么。
  改法一句话：那四处改成 `.map_err(String::from)`（`Halted` 有 `From<Halted> for String`）。
- **谁来裁：** 拿主意的人 / 下一张动那几屏的票
- **状态：** open

### Q152 — 票说「`facts` 已经能收 `task::Handle`」，其实那个函数的签名里没有把手

- **来自：** 票 `gui-redesign/15`
- **类别：** 票写错了
- **在哪：** 票 `15-sublibrary-long-runs-onto-the-board.md` 第 9 行与它末尾 `Q87` 那条的
  「要收掉是件小事」一句；`crates/core/src/sublibrary.rs::facts`（签名是
  `facts(catalog: &Catalog)`，一个把手都不收）；`crates/core/src/sync/prepare.rs`
  那句 `task.step("折事实")` 就在 `facts(catalog)` **之前**
- **为什么没停线：** 票要的事情没变——「算一遍容量」照样搬得上任务台，只是「一句话的事」
  这个估计偏了：接把手的地方不在 `facts` 里头，而在**调它的那一层**。
- **这张票实际做了什么：** 在核心里新开一条长入口 `sublibrary::survey(catalog, 子库们, task)`
  ——折事实一步、一台设备一步，进度与「停下」都报在这一层，界面那一侧于是与
  `preview` 一个形状（排一趟、按号认领）。**没有给 `facts` 加把手**：它是一趟顺读，
  中途停下没有第二种停法，加进去只是让每个调用方多传一个参数。
- **代价说清楚：** 折事实那一趟（真机量级 343 毫秒的大头）**仍然是不可细分的一步**，
  所以「按下停下」到「真的停了」最坏就是这一整趟。它整条只读，停在哪儿都是干净的。
- **谁来裁：** 收尾（要么就这样，要么哪天真库上那 343 毫秒变成几秒了再说）
- **状态：** open

### Q153 — 被按停却留下了产物的那一趟，任务台历史里记成「完成」

- **来自：** 票 `gui-redesign/15`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/task.rs::Board::settle`（`Ok(product)` 一律折成
  `Ending::Done`）；撞上它的两条是 `sync::execute::run` 与 `scan::scan`
- **为什么没停线：** 这不是这一票**弄坏**的：扫描早就这样——它被叫停时那几处写的是
  `let _ = task.step(…)`，照旧把断点写完、返回 `Ok(ScanOutcome { interrupted: true })`，
  于是任务台历史里那一行是「完成」。
- **这张票实际做了什么：** **照扫描那条走**：同步被叫停时也返回 `Ok`，把
  `interrupted` 记在账上。**不能抛 `Halted`**——写过东西的活得留下续跑的依据才停得干净，
  而同步那份依据就是**清单**（ADR-0015）：抛出去的话清单丢了，下一趟会把这次放上去的
  东西当成「清单之外」，于是碰都不敢碰。子库屏那句回执照旧把
  「⚠️ 这一趟被你按停了」说出口（`sync_notice`），所以人不会以为它整趟传完了。
- **代价说清楚：** **同一趟活在两屏上说两套话**——子库屏说「被你按停了」，任务屏的历史
  说「完成」。要收掉得在核心那一层立一种新收场（「停在半路，但留下了可用的产物」），
  那是 `Done<T>` 与 `Ending` 两个枚举一起动，扫描、刮削、取数据源几屏跟着改。
- **谁来裁：** 拿主意的人 / 下一张动任务台那一层的票
- **状态：** open

### Q154 — 票 15 末尾「挂单裁决」一节把四条挂单都标成「由本票接着做」，而它只从两条立起来

- **来自：** 票 `gui-redesign/15`
- **类别：** 票写错了
- **在哪：** `.scratch/gui-redesign/issues/15-sublibrary-long-runs-onto-the-board.md`
  第 49、74、100、140 行——`Q54`（任务屏没有「预计剩余」）、`Q87`、`Q89`、
  `Q119`（全选 46,483 行收藏要 6.7 秒）四条各带一句一模一样的
  「**收尾裁决：由本票接着做** —— 它就是从这条挂单立起来的」
- **为什么没停线：** 票的正文与八条验收只说两件事——同步上任务台（`Q89`）、
  「算一遍容量」上任务台（`Q87`），而且验收第 7 条点名的也只有这两条。
  那句话在 `Q87`／`Q89` 上是真的（这张票正是从它们俩立起来的），
  在 `Q54`／`Q119` 上是迁条目时粘串了行。
- **这张票实际做了什么：** **只把 `Q87` 与 `Q89` 标了 resolved。**
  `Q54`（**预计剩余**——它要的分母在扫描与刮削那两张票手里）与 `Q119`
  （收藏那 6.7 秒——那是浏览屏与 `identify` 那一层的事，与子库屏无关）
  两条**原样留着 open**，「谁来裁」那行没动。
- **谁来裁：** 收尾
- **状态：** open

### Q155 — 筛得很窄又同时在搜的时候，翻一页比改之前多花 6 毫秒

- **来自：** 票 `gui-redesign/13`
- **类别：** 这一票新引入的代价
- **在哪：** `crates/core/src/catalog/browse.rs::Search::alias` / `Search::description`
  （改成非相关子查询的那两条），以及 `Catalog::work_page_totals`（多出来的第二趟查询）
- **为什么没停线：** 它是这张票**换来的**那笔账的另一半，而账是划算的：
  搜索那三条从相关子查询改成非相关子查询之后，`title` 与 `scrape_value` 各要**扫一遍**
  把命中的名字收出来——那是**一次性开销，与筛剩几行无关**。不筛的时候它换掉了
  四万多次逐行探查（141.7 → 83.6 毫秒）；筛到只剩 2,329 行的时候，那笔固定开销就露出来
  （15.3 → 21.7 毫秒）。「取一页分两趟」也占其中一份：多一次查询往返。
- **这张票实际做了什么：** **照做，并把这一处写进 `docs/library-facts.md`**
  （「代价搬到哪儿去了」一节）与 `--bench-paging` 的常设探针
  （「按平台筛（SFC）+ 搜」那两行）——**以后谁再动搜索那三条，这一格会自己说话**。
  没为它加「筛得窄就换一种写法」那种分支：两条路都留在代码里，
  等于让「屏上这一批」有两种算法，而这一层从头到尾在防的正是这个。
- **谁来裁：** 收尾 / 下一张动搜索那一层的票
- **状态：** open

### Q156 — 合成数据造得出真库的形状了，但 `--bench-browse` 与那七个测试还在二十个作品上

- **来自：** 票 `gui-redesign/13`
- **类别：** 票之间的接缝
- **在哪：** `crates/gui/src/demo.rs::browse`（薄壳，仍然是 `WORKS.len()` ＝ 20 个作品）
  与 `browse_shaped`（这张票新开的，作品数由调用方定）
- **为什么没停线：** 主列表那条查询的代价跟着**分出来多少组**走，而
  `demo::browse(46428)` 只收出 3,596 行——真库是 10,978 行。量翻页要的是后者，
  所以这张票开了 `browse_shaped`。但 `--bench-browse` 与 `crates/gui/tests/` 里那七个
  测试都吃 `demo::browse`，它们钉着的是**别的**东西（帧率、面板、媒体、版式），
  换掉形状要连它们的断言一起重定，而那与这张票要治的时间无关。
- **这张票实际做了什么：** **只让 `--bench-paging` 走 `browse_shaped`**，
  默认 `--bench-works 7407`（加上 1/13 那批还没识别的正好 10,978 行）。
  `demo::browse` 一个字节的行为都没变，那七个测试与 `--bench-browse` 原样。
- **谁来裁：** 收尾 / 下一张动界面实测的票
- **状态：** open

### Q157 — 中立库结构版本没加 1，而这张票给 `variant` 加了一条索引

- **来自：** 票 `gui-redesign/13`
- **类别：** 规格没说（读起来像该加，按判据不该加）
- **在哪：** `crates/core/src/catalog/content.rs` 的 `variant_group`；
  判据在 `crates/core/src/catalog.rs` 的 `SCHEMA_VERSION` 文档「什么算「结构变了」」一节
- **为什么没停线：** 那一节写死了判据——**加 1 的判据是「旧数据会不会被读错」，
  不是「文件里多了点东西」**，并且点名「纯加表」这一档不加 1（票 13 的刮削五张表就是先例）。
  加一条索引比加一张表更轻：已有的表一列没动、一条语义没改，`CREATE INDEX IF NOT EXISTS`
  在打开时就把它补上，旧库照样打得开，拿旧版程序打开也照样能用。
  加 1 会逼所有人删库重扫 37 分钟，换不到任何东西。
- **这张票实际做了什么：** **没加**，版本留在 7。（交接说明里说「现在是 6」——
  那是过期了一版，`traversal_note` 那次已经把它顶到 7。）
- **谁来裁：** 收尾
- **状态：** open

### Q158 — 票 13 末尾「挂单裁决」一节把三条挂单都标成「由本票接着做」，而它只从两条立起来

- **来自：** 票 `gui-redesign/13`
- **类别：** 票写错了
- **在哪：** `.scratch/gui-redesign/issues/13-work-list-paging-cost.md`
  第 73、93、128 行——`Q64`（作品级主列表按聚合列排序）、`Q66`（浏览屏顶上那三个
  批量操作按钮还没画）、`Q108`（搜索那三条命中路）三条各带一句一模一样的
  「**收尾裁决：由本票接着做** —— 它就是从这条挂单立起来的」
- **为什么没停线：** 与票 `15` 上的 `Q154` 是同一种错：迁条目时粘串了行。
  这张票的正文与七条验收从头到尾只说**翻页的时间**，一条都没点到那三个按钮；
  而 `Q66` 自己那行「谁来裁」写的就是票 `06` / `10` / `11`。
- **这张票实际做了什么：** **只把 `Q64` 与 `Q108` 标了 resolved**，`Q66` 原样留着 open，
  并在它的「收尾裁决」那行写明为什么不由本票做。三个按钮一个像素都没画。
- **谁来裁：** 收尾
- **状态：** open

### Q159 — 「结构性损失」是这张票新起的词，`CONTEXT.md` 的词表里还没有它

- **来自：** 票 `queue-followups/02`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter.rs` 的 `StructuralLoss`（`Capability` 那一段后面）、
  `CONTEXT.md` 的**能力档位**词条（第 207 行）
- **为什么没停线：** `docs/agents/domain.md` 给的正是这条路——「要用的概念词表里没有……
  是真缺口（记下来交给 `/domain-modeling`）」。自己往词表里加一条，等于替
  `/domain-modeling` 那一趟先把词定死了。
- **这张票实际做了什么：** **没动 `CONTEXT.md`。** 类型名照 `Capability` / `Lossy` /
  `Preserved` 的样子用英文，中文文档与用户可见的话里一律说「这个格式**结构上**装不下什么」
  ——是一句话，不是一个自造的术语。词表要不要收「结构性损失」这个词条，留给 `/domain-modeling`。
  眼下的词表把**能力档位**定成「只读、只写、双向、无损往返」四个词，而这四个词答不了
  「把值交给这个格式存一趟会变成什么样」，词条那一句因此已经窄了半格。
- **谁来裁：** 拿主意的人（够开一张词表小票，或并进下一趟 `/domain-modeling`）
- **状态：** open

### Q160 — 值**末尾**的空白与开头那一样被掐掉，这张票只声明了开头那一条

- **来自：** 票 `queue-followups/02`
- **类别：** 路过发现，不在范围内（与 Q32 同族）
- **在哪：** `crates/core/src/adapter/pegasus.rs` 的 `classify`：顶格那一行走
  `right.trim()`（**两端**都掐），续行走 `trimmed.trim_end()` 取值
- **为什么没停线：** 票的正文、验收与标题一路点的都是「简介**开头**的前导空白」，
  中文离线源的常态也正是开头那两个全角空格。末尾的空白极少带语义，
  而多声明一条等于替一件没人量过重要性的事占住用户的注意力。
- **这张票实际做了什么：** **只声明了票点名的那两样。** `STRUCTURAL_LOSSES` 第二条写的是
  「值**开头**的空白」，没顺手扩成「值两端的空白」——扩了就与票的验收对不上，
  也没有哪份数据源为它作证。行为一个字没改（本来就是 `trim`）。
- **谁来裁：** 拿主意的人（并进 Q159 那张词表小票也行）
- **状态：** open

### Q161 — ES gamelist 的结构性损失还没人查过，它的清单是空的

- **来自：** 票 `queue-followups/02`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/adapter/gamelist.rs`（没有 `structural_losses` 的实现，
  走 `crates/core/src/adapter.rs` 里那个默认的空清单）
- **为什么没停线：** 票的硬约束写明「**别顺手去改 ES gamelist 那个适配器**——它是另一个
  格式，有没有同类损失是另一回事；真发现了记挂单」。
- **这张票实际做了什么：** **一个字都没动 gamelist。** 默认实现返回空清单，而空清单
  在文档与报告里都定成**「还没查过」而不是「这个格式什么都不丢」**：报告对空清单
  一个字都不印（有测试 `没声明结构性损失的格式一个字都不印` 钉着），
  `romcat adapters` 也只列有声明的那些——绝不印一句「无结构性损失」的假保证。
- **谁来裁：** 拿主意的人（要查就单开一张，照这张票的做法：先量，再声明）
- **状态：** open

### Q162 — 挂单 Q26 把「能力档位那一套」指到 `crates/core/src/capability.rs`，那是另一样东西

- **来自：** 票 `queue-followups/02`
- **类别：** 票写错了
- **在哪：** `.scratch/queue-followups/issues/02-capability-says-what-pegasus-drops.md`
  第 32 行，Q26 的「在哪」写着「`crates/core/src/capability.rs`（能力档位那一套）」
- **为什么没停线：** 交接说明里给对了——`assert_capability` 在
  `crates/core/src/adapter.rs:570`。两个名字只差一个字：`capability.rs` 装的是
  **能力档案**（目标设备吃得下什么、目标存储放得下什么，ADR-0017），
  **能力档位**（只读 / 只写 / 双向 / 无损往返，ADR-0003）在 `adapter.rs` 里。
- **这张票实际做了什么：** **一个字都没动 `capability.rs`。** 改的是 `adapter.rs`、
  `adapter/pegasus.rs`、`adapter/report.rs`、`adapter/transfer.rs` 与命令行那一处。
- **谁来裁：** 收尾
- **状态：** open

### Q163 — Pegasus 还有一族没声明的结构性损失：值里那一行 `.`、以及多值字段里的换行与空串

- **来自：** 票 `queue-followups/02`（`/code-review` 的产出）
- **类别：** 路过发现，不在范围内（与 Q26 / Q32 / Q160 同一族：Pegasus 这个格式的天花板）
- **在哪：** `crates/core/src/adapter/pegasus.rs` 的 `write_attribute`（多值那一枝把
  `line.trim().is_empty()` 与「这一行内容真是 `.`」写成同一串字节）与 `joined`
- **为什么没停线：** 票的正文、验收与标题一路只点两样——单个换行与开头的空白。
  这一族撞上的概率比那两样低几个数量级：简介里单独一行只写一个 `.` 的中文条目，
  离线源里一条都没见着。
- **这张票实际做了什么：** **只声明了票点名的那两样**，这一族一条都没写进
  `STRUCTURAL_LOSSES`。但**把门留开了**：新测试原先那句
  `assert_eq!(STRUCTURAL_LOSSES.len(), 2, "就这两条")` 已经改成按关键字找那两条，
  将来补第三条不会连测试一起红。具体三样（都实测过）：
  1. 值里**内容恰为 `.` 的一行**读回来成了段落分隔（`"甲\n.\n乙"` → `"甲\n\n乙"`）；
  2. 多值字段（`files` / `genres` / `tags`，走 `values.clone()` 不走 `joined`）里
     某个值带 `\n` 的，会按行拆成**多个值**；
  3. 同上，多值里的**空串**写成 `.`，读回来是字面量 `"."`。
  另外**顺手改准了一句**：第一条声明原先写「空行分隔的段落**照旧原样往返**」，
  实测只在**恰好一个**空行上成立——一个 `.` 读回来固定还原成 `\n\n`，连着 n 个空行
  （原文 n+1 个换行）读回来是 2n 个换行。声明已改成「**一个**空行分隔的段落原样回得来，
  连着的空行每多一个多长出一个换行」，并加了断言钉住（`简介往返一趟("甲\n\n\n乙")`
  == `"甲\n\n\n\n乙"`）。**这不是行为改动**，`write_attribute` 与 `joined` 一行没动。
- **谁来裁：** 拿主意的人（要补就单开一张，照这张票的做法：先量，再声明）
- **状态：** open

### Q164 — 匹配裁决**不进「批」**，按批撤销那条路管不到它；走回来那一下是「再裁一次」

- **来自：** 票 `queue-followups/06`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/queue.rs` 的 `judged_text` 那句收尾话；核心库那一侧是
  `scrape::zh::judge`（写 `verdict::Store::put_match`）与 `triage::apply`
  （写 `put_batch`）——**两条路一次都没交汇**
- **为什么没停线：** 票写着「撤销走既有那条按批撤销的路，别另造一套」。**照做了**：
  这一票一行撤销代码都没写。但那条路（票 `gui-redesign/08`）撤的是一**批识别裁决**
  ——`verdict` 表加中立库那份快照；匹配裁决落的是**另一张表**（`match_verdict`，
  挂单 Q33 定的形状），`triage::apply` 从来不碰它。要让它也按批撤，得先把匹配裁决
  纳进批——那是核心库的形状改动，正是这张票禁止的「另造一套」。
- **这张票实际做了什么：** **不另造，也不假装它撤得掉。** 界面上那颗「撤回第 N 批」
  一个字都没动，只管识别那一批；匹配裁决落下之后那一行账**明说**它不在那条路上，
  并给出真正的走回路：**再裁一次**（同一条锚上 `put_match` 后一条盖掉前一条，
  `Judged::fresh` 为假），否定时清掉的值**下一趟 `romcat scrape` 采得回来**
  （人裁过之后那条条目在候选里排最前，Q34）。测试
  `gui/tests/queue.rs::匹配裁决不另起一条撤销路` 钉住三样：裁完沉淀库里一批都没建、
  屏上不冒出撤回按钮、再裁一次是覆盖而不是攒出第二条。
- **谁来裁：** 拿主意的人（要它也能按批撤，那是一张新票：把匹配裁决纳进 `verdict_batch`）
- **状态：** open

### Q165 — 归堆那一侧多交一栏**依据**：`MatchedValue.evidence`

- **来自：** 票 `queue-followups/06`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `MatchedValue`（新增 `evidence` 一栏）
- **为什么没停线：** 验收第 1 条要「堆上写着条目号与**依据**」，而 `matched_groups`
  原先只交出锚点、字段、值、源——依据那句话留在库里没带出来。
- **这张票实际做了什么：** **在核心库那一侧补一栏**，值原样从 `ScrapedValue::evidence`
  搬过来（`matched_groups` 本来就为了取条目号读着它）。**没有让界面自己再去库里捞一遍**
  ——捞回来还得再按条目号认一遍谁属于这一堆，而那正是归堆这件事本身（ADR-0005：
  第二处认法长在界面里，迟早与核心库那一处漂开）。命令行那两条命令一个字都没改，
  它照旧不印依据（`romcat zh matches` 一屏要摆好几堆，整句依据会把它冲垮）。
- **谁来裁：** 收尾
- **状态：** open

### Q166 — 界面上这个入口只在**逐条**那一屏；浏览屏上看见误撞，仍旧得先切过来

- **来自：** 票 `queue-followups/06`
- **类别：** 票写错了（正文与验收对不齐）
- **在哪：** 票 `.scratch/queue-followups/issues/06-match-verdict-in-triage-screen.md`
  第 5 行说「用户在**浏览屏**上看见一条误撞」，而八条验收从头到尾说的都是**待确认屏**
- **为什么没停线：** 验收是判据，正文那句是背景。而且两者不矛盾——看见与裁决本来就
  可以在两屏：**误撞看得见**是在浏览屏（那儿摆着刮削字段值与它们的来源），
  **裁得动**这一票接在待确认屏的详情面板上（那一屏本来就是「一次看一个变体」的地方，
  而匹配裁决的粒度正是一个变体）。
- **这张票实际做了什么：** **只做待确认屏那一侧**，而且只在**逐条**那一屏——分批那一屏
  一张卡片管着上千个变体，匹配裁决钉在**一个变体的内容**上，摆在卡片上就是给整批人
  按一颗只对一个人生效的按钮。浏览屏那一侧一行没动：用户在那儿看见误撞，要么切到
  待确认屏（那个变体还在队列里的话），要么照旧走 `romcat zh judge`。
- **谁来裁：** 拿主意的人（要把同一块接进浏览屏的详情面板，那是一张新票——
  那一屏还能裁**已经识别完、早就不在队列里**的变体，这一票够不着它们）
- **状态：** open

### Q167 — **肯定**那一档屏上那一堆不当场改口：那句「已由人裁决确认」在**依据**里，下一趟刮削才改写

- **来自：** 票 `queue-followups/06`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/gui/src/queue.rs` 的 `judged_text` 里肯定那一支；核心库那一侧是
  `zh::Match::evidence_confirmed`（由下一趟 `scrape` 重新采集时写进依据）
- **为什么没停线：** 这是既有行为，票的硬约束里写着「肯定那一档一个字都不清」。
  `MatchGroup::confirmed` 是**从依据那句话里读回来的**（`zh::is_confirmed`），而依据
  要等下一趟刮削才改写——于是按完「就是这条」，屏上那一堆照旧写着「还等着裁」。
- **这张票实际做了什么：** **不改行为，改的是把这件事说出口。** 账那一行明写
  「屏上那一堆仍写着『还等着裁』——那句话在依据里，下一趟 `romcat scrape` 才改写」，
  测试 `就地下一次肯定裁决那一堆的值一个字都不清` 钉着这句话在屏上。
  **没有让界面自己去沉淀库问一句「这条裁过没有」**：那要界面自己折出内容锚
  （`identify::content_print`）再去 `find_match`，是把领域判断搬进 `crates/gui`。
- **谁来裁：** 拿主意的人（要屏上当场改口，正路是让 `matched_groups` 顺带把沉淀库
  那一侧的态一起交出来——核心库的活，一张新票）
- **状态：** open

### Q168 — 合成数据里**只有那两个变体有 `entry` 行**，别的变体折不出内容锚

- **来自：** 票 `queue-followups/06`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/src/demo.rs` 的 `queue_with_zh`（新写的那两行 `EntryRecord`）
- **为什么没停线：** 它只影响合成数据。`demo::queue` 原先给每个非「无判据」的变体都
  摆了 `content_hash` 行，注释也写着「钉在内容上」——**可一条 `entry` 行都没有**，
  而 `identify::content_print` 先问 `entry_fact`，答「压根没有」就退到路径锚。
  于是 `--demo` 打开的队列里，**每一条**详情都写着「裁决钉在：路径——只在本机成立」。
- **这张票实际做了什么：** **只给那两个变体补了 `entry` 行**（这一票的验收要「锚在
  内容锚上」，没有它一条都验不了），并把那句注释改准。别的 16,654 条照旧没有——
  补全要多写一万六千行 entry，而那会改到别的屏上摆着的数（计划书里「钉在内容上的
  多少条」当场从 0 变成一万多），不是这一票该顺手动的。
- **谁来裁：** 收尾（或者下一个动 `demo::queue` 的人顺手补全）
- **状态：** open

### Q169 — 那一堆一屏摆不下：断言靠**真的滚轮事件**滚一趟才看得见

- **来自：** 票 `queue-followups/06`
- **类别：** 取舍
- **在哪：** `crates/gui/tests/queue.rs` 的 `详情滚一趟`；面板高度在
  `crates/gui/src/layout.rs` 的 `DECIDE`（默认 268 点）
- **为什么没停线：** 一堆匹配是六个字段、每条各带一整句依据，而详情那一栏默认
  268 点高——摆不下是必然的，egui 也不画视口之外的东西。挂单 Q20 记过同一件事
  （浏览屏那条 4,000 字的简介），那次是绕开屏幕改测单元函数；这次没绕。
- **这张票实际做了什么：** **测试滚真的滚轮**：指针停在那块面板里，
  连发十二次 `MouseWheel`，每一帧收那一帧真的画出来的字（`表格里画多少行文本输入框
  都是那几个` 也是这么滚一趟的）。**断言的仍旧是画出来的那一帧**，不是「库里有没有」。
  代价是这几条测试证的是「滚到那儿看得见」，不是「打开就看得见」——而人要看这一堆
  本来就得先把光标停到那一条上，滚一下是同一个动作里的事。
- **谁来裁：** 拿主意的人（要「打开就看得见」，那是版式的活：给这一堆一块自己的地方，
  或者把每一堆折起来只留标题——一张新票）
- **状态：** open

### Q170 — 「这一帧画出来的字」那个助手收成一处：`tests/shared/mod.rs`

- **来自：** 票 `queue-followups/07`
- **类别：** 取舍
- **在哪：** `crates/gui/tests/shared/mod.rs`；原先三份抄本在 `tests/queue.rs`、
  `tests/layout.rs`、`tests/sublibrary.rs`，这一票要用第四次
- **为什么没停线：** 收成一处是本票分内的事（票里点名），而且四处重复比三处更该收。
- **这张票实际做了什么：** 助手挪进 `tests/shared/mod.rs`，四份测试各写一行
  `mod shared;`。**摆在子目录里**：`tests/` 底下每个 `.rs` 都是一个独立的测试二进制，
  共用的东西直接放那儿会变成一个一条测试都没有的二进制。三份已有测试**断言的内容
  一个字没动**——只删了各自那份抄本与它的文档，统一后的文档把三处的「为什么」并了进去。
  没有挪进 `crates/gui/src`（`headless` 那种「只为测试存在的普通模块」在核心库有先例）：
  这一票只该改测试，往库里加一个只有测试用的公开函数是另一件事。
  **滚轮那个助手没一起收**：`tests/browse.rs` 的 `元数据栏滚一趟` 与 `tests/queue.rs` 的
  `详情滚一趟` 眼下是两份近似抄本，这一票收的是 `画出来的字`——第三份出现时一起收
  （`/code-review` 顺带报的）。
- **谁来裁：** 收尾
- **状态：** open

### Q171 — 票把两条验收记混了：钉在纯函数上的是「收成一行」那条，不是「列表滚得动」那条

- **来自：** 票 `queue-followups/07`
- **类别：** 票写错了
- **在哪：** `.scratch/queue-followups/issues/07-assert-what-the-frame-drew.md` 开头那句；
  两条验收在 `crates/gui/tests/browse.rs`
- **为什么没停线：** 票要的事（把断言改成看这一帧画出来的字）没有歧义，错的只是它指的
  是哪一条。`库里有一条顶到闸上的简介时列表照样滚得动` 钉的是**表**那一侧
  （`bench::scroll` 的翻页代价、内存里那一扇窗），一条纯函数断言都没有；钉在
  `browse::one_line` 上的是另一条 `一条顶到闸上的简介收成一行画得下的那一截`
  ——挂单 Q20 的原文分得很清楚。
- **这张票实际做了什么：** **改的是真钉在纯函数上的那一条**（`一条顶到闸上的简介收成
  一行画得下的那一截`）；`库里有一条顶到闸上的简介时列表照样滚得动` 一个字没动，
  它本来就在断言跑出来的结果。
- **谁来裁：** 收尾
- **状态：** open

### Q172 — `browse::one_line` 照旧公开，理由换了一个：有一条断言只有从函数那一侧看得见

- **来自：** 票 `queue-followups/07`
- **类别：** 取舍
- **在哪：** `crates/gui/src/browse.rs` 的 `one_line`（文档那一段）
- **为什么没停线：** 这一票只改测试。可它把「收成一行画得下的那一截」那条验收从
  `one_line` 挪到了画出来的那一帧上，而那个函数的文档正写着「它是公开的，因为这条验收
  就钉在它上面：headless 的一帧里根本够不着它」——话已经不成立了，留着会把下一个人
  骗回原地。
- **这张票实际做了什么：** **只改了那段文档**（一行代码都没动、可见性也没动）：写明
  「收成一行」那一半现在钉在画出来的那一帧上、够得着它靠的是指针停进那一栏再滚真的
  滚轮；公开的理由换成剩下那条断言——`one_line("原样画得下的一句") == None`
  说的是「不动就不必换一份字符串出去」，屏上画的是同一串字，看那一帧看不出来。
- **谁来裁：** 收尾（要把它收成 `pub(crate)`，得先把那条断言搬进 `crates/gui/src`
  里的单元测试——那是另一张票的活）
- **状态：** open
