# 挂单：实现时撞上、但没有为它停线的东西

**Status:** living（本轮队列全部票共用一份）
**Opened:** 2026-09-07（第三轮；第二轮 2026-09-05 开、第一轮 2026-09-04 开，
`Q1`–`Q132` 与第二轮迁走的 32 条都已清空并迁回票据）
**Effort:** 第三轮队列 = `.scratch/parking-3/issues/` 18 张

**本轮收口的范围（2026-09-07 裁定）：`Q135`–`Q193`，共 21 条**（其中 19 条彼时还 open）。
范围在动手之前定死：这条线之后记下来的（`Q194` 起）属于下一轮，**不会被这一轮吸进来**。

**编号一路往下走，第三轮从 `Q194` 起。** 之前的编号已经迁回各自的票据末尾
（「挂单裁决」一节），编号留着占号、**不复用**——票据里引用得回来。

⚠️ **本轮起不再往 `deferred.md` 送任何东西**（票 `18` 会把它归档关掉）。每条条目只有两个
去处：**迁回原票**，或者**开成一张票**。分类只有三档：**A** 已经做完只是没销、**B** 不成立了、
**C** 仍然成立要开票。

---

## 这份文件是什么

接票实现时总会撞上票没预见到、或者票写错了的东西。**这里面几乎没有一件该停下整条队列。**
它们落在这儿：**记下来、绕过去、接着走、收尾时一起裁。**

它不是项目的长期缺口清单（那是 `.scratch/rom-metadata-automation/deferred.md`，编号 `Dn`，全局连续）：

| | 挂账 `deferred.md` | 这份挂单 |
|---|---|---|
| 装的是 | **产品**里的取舍 | **执行**里冒出来的问题——票写错了、规格没说、两份东西互相矛盾 |
| 寿命 | 活得比这轮队列长 | **收尾时清空**——每条要么就地裁掉、要么升进 `deferred.md`、要么开成新票 |
| 谁读 | 下一个动这块产品的人 | 收尾那一步，和拿主意的人 |

**编号分两套，别混：** 这里用 `Q1、Q2…`；`deferred.md` 用 `D1、D2…`。
一条从这里升进 `deferred.md` 时才拿 `Dn` 号，且这边的 `Qn` 留着不复用。

---

## 停线——只有这三条

停线很贵：拿主意的人得把整个上下文重新装回脑子。门槛：

1. **接着做会写出一个错的东西，而且撤销它要伸到这张票之外。**
   典型：票里的指示与上游一条仍然生效的决定（ADR、词表、规格）相悖。
2. **动作不可逆，而且不止一个方向站得住。**
   典型：删掉或改名一个被很多处引用的东西——一个 id、一个字段、一份文件。
3. **前提根本不在，票做不下去。**
   典型：「在 X 上面盖 Y」，而 X 不存在。

其余一律：记在这儿，挑最站得住的那条路走，接着做。这包括：票里的数字与你实测的对不上、
验收条目读着含糊、票没想到的第三种情况、路过看见的无关缺陷、以及你确实拿不准的单条。

> ⚠️ **绕过去要留痕。** 条目记的是**你走了哪条路**，不是「有两条路」。
> 说不出自己做了什么的条目，不算一条条目。

---

## 条目格式

```markdown
### Q<N> — <一句话：这是什么>

- **来自：** 票 `<feature>/NN`
- **类别：** 票写错了 / 规格没说 / 两份东西矛盾 / 工具缺陷 / 路过发现，不在范围内
- **在哪：** 路径 + 行号或 id —— 要点得过去
- **为什么没停线：** 一句话
- **这张票实际做了什么：** 一句话 —— **必填**
- **谁来裁：** 拿主意的人 / 票 `NN` / 收尾
- **状态：** open
```

编号**接着上一轮往下走**（第二轮从 `Q133` 起），不要按票分节——「来自」那行已经带了这个信息。

## 第二轮收尾（2026-09-06）

第二轮 14 张票跑完，挂单从 57 条收到 **17 条**：

- **32 条迁回它们所属的票**（票据末尾「挂单裁决（第二轮）」一节）：23 条 `settled`（决定已落进代码并有测试钉着）、7 条 `noted`（核对过不用改）、Q145（已被 Q170 做掉）、Q146（由票 `gui-redesign/17` 承接）。
- **1 条升进 `deferred.md`**：Q141 → **D163**（报告口径与详情面板该对齐哪一侧，是产品方向，没有票可接）。
- **剩下这 17 条是真待办**，按主题分成六组，是下一轮 `/to-spec` 的材料：

| 组 | 挂单 | 一句话 |
|---|---|---|
| 结构性损失清单补齐 | Q159 Q160 Q161 Q163 | Pegasus 还有一族没声明、值末尾空白、gamelist 没查过、词表缺「结构性损失」 |
| 「停下」从核心到任务台一致 | Q150 Q151 Q153 | 下载停不掉、按停的话折错记成失败、按停留下产物记成完成 |
| `sync/execute.rs` 落点闸收尾 | Q135 Q136 Q142 | 不敏感盘验不了、列不开的目录那一格、审查报的两条 |
| 门禁与工具链 | Q187 Q188 Q189 Q191 | 门禁没有机器执行的地方、没钉住 rustfmt 版本、`cargo doc` 68 条告警、`main` 上两处本来就不 fmt-clean |
| 夹具与合成数据 | Q156 Q168 Q177 Q192 | `bench-browse` 还在 20 作品、`demo` 只有两个变体有 entry 行、队列卡片缺一句悬停、两份界面测试各搭一份几乎一样的小库 |

> **票 `gui-redesign/17` 跑完之后**：「还没识别」裁词那一组（`Q148`）收掉了，四处文档
> 改成指向 `CONTEXT.md`；新添 `Q190`（分辨机制走了哪条路，已裁）与 `Q191`（`main` 上
> 本来就不 fmt-clean 的两处，并进门禁那一组）与 `Q192`（两份界面测试的夹具重复，
> 并进夹具那一组）。真待办 **18 条**。

**编号继续往下走，下一个空号是 `Q193`。**

> 第三轮开工时 `Q193` 已由 `Q66` 拆出，与这 18 条一起进了下面的第三轮收口，下一个空号是 `Q194`。

---

## 第三轮收口（2026-09-07）

**范围：`Q135`–`Q193`，21 条。** 全部有归宿，条目区清空。

**判据：一条挂单有了主人就算收了，不是等它做完。** 反过来读——「做完才算收」——最大的
那条会把整批一直摁在开着的状态，而新条目在后面继续堆。这份挂单上一轮之所以到过三位数，
就是这么来的。

### 三条车道

| 车道 | 条数 | 是什么 |
|---|---|---|
| **记录** | 3 | 撞见一个事实，没有谁欠一个决定 |
| **岔路** | 0 | 条目里一条都没剩——`/to-spec` 写第三轮规格时已经替每条挑了路（见下表「去处」列） |
| **工作** | 18 | 是一处改动，不是一个选择；要一张票兜着 |

### 已收索引

| 条目 | 一句话 | 车道 | 去处 |
|---|---|---|---|
| `Q135` | 「不敏感的卡上行为一致」这一半，开发机上造不出来 | 工作 | 票 `parking-3/06` —— 给 `sync::execute` 加一道可注入的目标侧接缝 |
| `Q136` | 列不开的目录底下，折起来那半边闸整个失效 | 工作 | 票 `parking-3/07` —— 「读不动」立成第三态，照 ADR-0021 |
| `Q142` | 收尾审查在别的票已提交的代码里报了两条 | 工作 | 票 `parking-3/07` —— 落点判据收紧、单次判定内共享目录缓存。第 3 条（已归档票的验收正文还写「六层」）**核过不改**：改它会让那张票的记录失真 |
| `Q148` | 四处文档还指着裁掉的挂单说「词表眼下没收」 | 记录 | 已由票 `gui-redesign/17` 收干净（实际是五处，裁决在该票末尾） |
| `Q150` | 下载那 435 MB 仍然停不掉 | 工作 | 票 `parking-3/05` |
| `Q151` | 界面刮削把「按停了」折成一句对不上的话 | 工作 | 票 `parking-3/05` |
| `Q153` | 被按停却留下产物的那一趟，历史里记成「完成」 | 工作 | 票 `parking-3/03` —— **settled**，条目已迁回票 `gui-redesign/15` |
| `Q156` | `--bench-browse` 与那七个测试还在二十个作品上 | 工作 | 票 `parking-3/17` |
| `Q159` | 「结构性损失」这个词还没进词表 | 工作 | 票 `parking-3/14` |
| `Q160` | 值**末尾**的空白没声明 | 工作 | 票 `parking-3/14` |
| `Q161` | ES gamelist 的结构性损失清单是空的 | 工作 | 票 `parking-3/15` |
| `Q163` | Pegasus 还有一族没声明的结构性损失 | 工作 | 票 `parking-3/14` |
| `Q168` | 合成数据里只有那两个变体有 `entry` 行 | 工作 | 票 `parking-3/17` |
| `Q177` | 队列卡片缺一句「命令行上是什么」的悬停 | 工作 | 票 `parking-3/16` |
| `Q187` | 「门禁」没有任何机器执行的地方 | 工作 | 票 `parking-3/01` —— **settled**，裁决迁回票 `queue-followups/11` 末尾 |
| `Q188` | 没有任何东西钉住是哪一版 `rustfmt` | 工作 | 票 `parking-3/01` —— 落 `rust-toolchain.toml`，钉具体版本。**settled**，裁决迁回票 `queue-followups/11` 末尾 |
| `Q189` | `cargo doc` 68 条 rustdoc 告警 | 工作 | 票 `parking-3/02` |
| `Q190` | 「跑过没跑过」分辨落在哪儿 | 记录 | 已裁：给行加标记、`Tier` 四档不动（票 `gui-redesign/17`） |
| `Q191` | `main` 上两处不 fmt-clean，其一是重复的 `#[test]` | 记录 | **已做掉**：提交 `bd4273d` 删了那条重复的 `#[test]`。实测 `crates/core/tests/sync_run.rs` 16 个 `#[test]`，没有相邻重复 |
| `Q192` | 两份界面测试各搭了一份几乎逐字相同的夹具 | 工作 | 票 `parking-3/16` |
| `Q193` | 作品锚点里的那个 id 会被重跑识别换掉 | 工作 | 票 `parking-3/10` —— **已 `settled`**，迁回票 `gui-redesign/03` |

**工作那一道产出了什么：** 18 条全部落进 `.scratch/parking-3/issues/` 里**已经开着的** 12 张票
（`01` `02` `03` `05` `06` `07` `10` `14` `15` `16` `17`，外加 `18` 兜 `Q191`）——这一轮
**没有为挂单新开任何票**，也**没有一条无家可归**。那 12 张票每一张末尾都已经自带
「把本条标 `settled` 迁回原票」的验收条，迁回是各票收尾时自己的事。

**升进长期挂账的：0 条。** 本轮起 `deferred.md` 不再接新条目（票 `parking-3/18`）。

### 量一量：每张票撞出几条

**每张票撞出几条** = 一轮效力记下的条目数 ÷ 它手上的票数。对着 **1** 读：1 就是平均
每张票撞上一件它没预见到的事。

| 效力 | 记下的条目 | 票数 | 每张票撞出几条 |
|---|---|---|---|
| `offline-chinese-fields` | 48（`Q1`–`Q49` 一带） | 6 | **8.0** |
| `gui-redesign` | 101（`Q50`–`Q132` 一带 + `Q143`–`Q158`、`Q190`–`Q192`） | 17 | **5.9** |
| `queue-followups` | 44（`Q133`–`Q142`、`Q159`–`Q189` 一带） | 11 | **4.0** |
| 合计 | 193 | 34 | **5.7** |

喂进这一轮范围的是后两条：`queue-followups` **4.0**、`gui-redesign` **5.9**。

**读数：全线远在 1 以上。** 票是在地形还没探明之前写的，缺的那半份规格落在了这儿。
三轮的落差 **8.0 → 5.9 → 4.0** 是同一个方向：一路在降，但还差着四倍。这是冲
`/to-tickets` 的信号，不是冲这份文件——那几批更该先走一趟 `/wayfinder`。
第三轮（`parking-3`，18 张）的读数要等它跑完才有；下一轮收口时补上，这一串才是这个仓库
自己的刻度。

---

## 条目

<!-- 从 `Q194` 往下追加；不要重排，不要删。清空于第三轮收口（2026-09-07） -->

_空。_（第三轮收口当天的状态；`Q194` 起见下。）

### Q194 — 门禁跑手落成独立的 `xtask` crate，而不是 `romcat` 的一条子命令

- **来自：** 票 `parking-3/01`
- **类别：** 规格没说
- **在哪：** `xtask/Cargo.toml`、`.cargo/config.toml`、根 `Cargo.toml` 的 `members`
- **为什么没停线：** 票只说「仓库里一个真实可跑的子命令，承载它的东西不进交付出去的二进制」，
  没说是**谁的**子命令；两条路都撤得回来，改动全在这张票之内。
- **这张票实际做了什么：** 走**独立 crate + cargo 别名**这条路（`cargo xtask gate`）。
  另一条路是 `romcat gate`，藏在一个默认关掉的 feature 后面——它站得住（`romcat-gui` 的
  `demo` 就是这个先例），但它与票自己那句「不进交付出去的二进制」相抵：门禁四条的定义会住在
  交付 crate 的源码树里，而**门禁一律用 `--all-features` 跑**，于是那个 feature 在门禁里
  永远是开的，`romcat --help` 上会长出一条只给维护者看的命令。`xtask` 那条没有这一层：
  `romcat` 与 `romcat-gui` 都不依赖它。顺带把 `xtask` 放进 workspace `members`
  （而不是 `exclude`），于是门禁四条照样盖着门禁跑手自己——**门禁跑手自己不守门禁**
  是最难查的一类漏。
- **谁来裁：** 已裁（本票）
- **状态：** settled

### Q195 — `.github/workflows/gate.yml` 在这台机器上验不了；GUI 那串系统库是**量出来**不需要的

- **来自：** 票 `parking-3/01`
- **类别：** 前提不在（但票做得下去）
- **在哪：** `.github/workflows/gate.yml`
- **为什么没停线：** 票明确要求落这份 workflow；「验不了」不等于「写不了」，而且它只调一句
  `cargo xtask gate`，那一句本机跑过、四条全绿。
- **这张票实际做了什么：** 照官方配方写、逐条注明依据，**不猜**。唯一真有风险的那一格
  ——Linux 上要不要给 egui/eframe 装一串 X11 / wayland / xkbcommon 的 dev 包——**量掉了**：
  `ldd target/debug/romcat-gui` 只链到 `libc` / `libm` / `libgcc_s` / `ld-linux`
  （winit 与 wgpu 那一层是运行时 `dlopen`），而门禁跑的界面测试全是 headless、不开窗，
  于是 workflow 里**一个 apt 步骤都不写**，并把这条实测写在文件里。缓存那段照 cargo 官方
  CI 配方（`registry/index` + `registry/cache` + `git/db` + `target/`，**不含**
  `registry/src`）。剩下没验到的是：YAML 语法、action 版本、缓存命中率——这个仓库
  **没有 remote、没有 runner，这份文件从未被任何 CI 实跑过**。
- **谁来裁：** 拿主意的人（第一次真的推上去时）
- **状态：** open

### Q196 — 界面媒体那条「头一帧 < 1 秒」的挂钟断言，落到门禁上就是一张彩票

- **来自：** 票 `parking-3/01`
- **类别：** 路过发现，不在范围内——但门禁一落地它就成了门禁的问题
- **在哪：** `crates/gui/tests/media.rs`，测试 `切换选中那一帧不解码`（改动前第 248 行）
- **为什么没停线：** 一行断言，改动可逆，而且同一个测试里**已经有两条结构性判据**在说同一件事。
- **这张票实际做了什么：** **把那条挂钟断言拿掉**，留下 `ready() == 0`（头一帧一张图都没解出来）
  与 `busy() > 0`（活确实排出去了）这两条结构性判据，并在注释里写清实测与「要量时间去 `--bench`」。
  **实测把它的性质钉死了**：同一棵树、同一份代码——`--test-threads=6` 且机器上另有三个工作区
  在冷编译时，头一帧 1.82s、**红**；`--test-threads=2` 那趟（当时**照样**有别的工作区在编译）
  **绿**；机器安静下来单跑三趟**全绿**。它量的是这条测试**抢不抢得到 CPU**，不是这段代码
  有没有在画帧线程上解码——而 CI 的 runner 比这台又慢又忙，我落的 `gate.yml` 第一次真跑就会撞上。
  另两条路：① 把上限放宽到 5s / 10s——同一类彩票，只是中奖率变低，而**罕见的 flake 比常见的更坏**
  （不可复现、且慢慢把人对门禁的信任磨掉）；② 让门禁对红掉的测试自动安静重跑一次——那是
  让门禁说谎，还把最坏耗时翻倍。**推荐就是已走的这条**：门禁上的判据换成确定性的，
  时间交给这个仓库**本来就有**的地方（`--bench` / `--bench-browse`，`demo` feature 后面）。
  **代价说清**：从此没有任何东西拦「头一帧因为解码以外的原因卡住」，那一格是空的。
- **谁来裁：** 拿主意的人 / 票 `parking-3/16` `17`（它们动这一族界面测试时，可以顺手把那一格
  补在 bench 那一侧）
- **状态：** open

### Q197 — `rust-toolchain.toml` 的 `profile = "minimal"` 是票没要求、我加的

- **来自：** 票 `parking-3/01`
- **类别：** 规格没说
- **在哪：** `rust-toolchain.toml`
- **为什么没停线：** 一行，删掉即回退。
- **这张票实际做了什么：** 加了 `profile = "minimal"`，并显式点名 `components = ["rustfmt", "clippy"]`
  （那两个不是 `minimal` 自带的）。票专门要求「写清副作用：第一次进目录可能触发一次下载」，
  而 `minimal` 直接把那个副作用**做小**——与默认 `default` 差的那一份是 `rust-docs`
  （离线标准库文档），门禁四条一条都用不到。另一条路是不写 `profile`、跟默认走，代价是
  每台新机器多下那几十 MB。**带保留**：这台机器上 1.98.1 早就装着（而且是按 `default` 装的），
  落这份文件**没有触发任何下载**，所以「省下多少」是按 rustup 对 profile 的定义推的，
  **不是量出来的**。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q198 — 测试条数住在**三处**，没有任何东西盯着它们同步

- **来自：** 票 `parking-3/01`
- **类别：** 路过发现
- **在哪：** `README.md`（「现在到哪一步了」与「⚠️ 门禁必须带 `--all-features`」两处）、
  `crates/gui/Cargo.toml` 的 `demo` feature 注释
- **为什么没停线：** 改一处改三处是十秒钟的事；漏改也不会让任何东西红。
- **这张票实际做了什么：** 三处一起按实测改准——`--all-features` **1,635 条**、不带
  **1,530 条**、**少跑 105 条**（两边同一口径：`cargo test --workspace [--all-features] -- --list`；
  原先写的是「少跑 101 条，1,480 而不是 1,581」），并在两处写上「这个数会随票涨，
  **以 `cargo xtask gate` 自己的输出为准**」。另一条路是让门禁把这个数打出来再与文档比对，
  **没走**：那要门禁去解析自己子进程的输出，为一个注释里的数字加一条会自己坏掉的机器判据，
  换来的确定性不值这个价。
- **谁来裁：** 收尾
- **状态：** open

### Q199 — `cargo +stable xtask gate` 绕得过 `rust-toolchain.toml` 的钉，而门禁本地不报自己跑的是哪一版

- **来自：** 票 `parking-3/01`
- **类别：** 路过发现，不在范围内（收尾审查提的、判为「不必改」的两条之一）
- **在哪：** `xtask/src/gate.rs` 的 `cargo()`；`.github/workflows/gate.yml`「装工具链」那一步
- **为什么没停线：** 要绕得先自己在命令行上打一个 `+stable`——那是**显式**绕过，不是默默漂；
  而 CI 那一格本来就打印了版本。
- **这张票实际做了什么：** **没有**给门禁加工具链横幅，走的是「钉住 + CI 打印」这条路。
  `cargo()` 取的是环境变量 `CARGO`（cargo 起门禁跑手时写进去的是**它自己**那份的绝对路径），
  于是四条子进程用的必定与起门禁的那条工具链是同一条；正常调用下 `rust-toolchain.toml`
  钉得住。另一条路是让 `run()` 开头打一行 `rustc --version` / `cargo fmt --version`
  （CI 那一步已经这么做了），本地也就当场看得见跑的是哪一版——五行的事。**没加**：
  它防的是一个要人主动打 `+stable` 才成立的场景，而真跑歪了，门禁第一条
  `cargo fmt --all --check` 的 diff 会立刻把不一致喊出来。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q214 — 票 `parking-3/03` 说「扫描被叫停时任务屏历史记成完成」，扫描那一半不成立

- **来自：** 票 `parking-3/03`
- **类别：** 票写错了
- **在哪：** `.scratch/parking-3/issues/03-task-board-says-stopped-with-product.md` 第 7–8 行
  （「扫描与同步被叫停时都返回「成功」，于是任务屏历史里那一行是「完成」」）；
  改之前的 `crates/gui/src/roots.rs::Screen::scan`
  （`Ok(outcome) if outcome.interrupted => Err(Halted.to_string())`）
- **为什么没停线：** 票要做的事没变。核心那一侧确实两条都返回 `Ok`，但**界面那一层
  早就替扫描折了一道**：被中断的那一趟折成 `Halted` 那句话，于是它在任务屏历史里记的
  是「按停了」，不是「完成」。真记成「完成」的只有同步那一条（`Q153` 的原话说的也正是同步）。
  折那一下的代价是**那份「到目前为止」的体检报告被整个丢掉**——`Err(String)` 装不下产物。
- **这张票实际做了什么：** 把那道折**拿掉**：扫描被叫停时照旧交出 `Product::Scanned`，
  由 `scan::scan` 自己报一句 `Handle::halfway`，任务台据此记「停在半路」。
  于是三处说的是同一件事，而那份半份体检报告不再被丢掉。
- **谁来裁：** 收尾
- **状态：** open

### Q215 — 「停在半路」的判据是长入口自己报一句，不是任务台看「按过停下没有」

- **来自：** 票 `parking-3/03`
- **类别：** 规格没说
- **在哪：** `crates/core/src/task.rs::Handle::halfway` 与 `Board::settle` 里
  `Ok(product) => match handle.left()` 那一段
- **为什么没停线：** 两条路都站得住，翻案就是改 `settle` 里那三行。
- **这张票实际做了什么：** **走「长入口自己报」这条。** 另一条是任务台自己判
  ——`Ok(product)` 加上 `handle.stopped()` 就算停在半路，一句都不用长入口说。
  没选它的理由：**「按下停下那一刻正好跑完」的那一趟会被冤枉**。那时核心那一侧
  `interrupted` 是 `false`、子库屏回执一个字不提按停，而任务屏历史却写「停在半路」
  ——两屏又打架，正是这张票要收的那件事，只是掉了个个儿。代价是**新长入口忘了报那一句
  就会静默记成「完成」**：眼下报了的只有 `sync::execute::run` 与 `scan::scan` 两条，
  各有测试钉着；模块文档与 `Handle::halfway` 的文档都把这条义务写了出来。
- **谁来裁：** 收尾
- **状态：** open

### Q216 — 「连着失败太多次主动停了」与「有几步没做成」那两趟也留下了产物，仍记「完成」

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/sync/execute.rs` 里 `out.gave_up = true; break;` 那条路，
  以及 `out.failures` 非空的那一趟；`crates/gui/src/sublibrary.rs::sync_notice`
  为这两样各印一句 `⚠️`
- **为什么没停线：** 这张票立的那一档是「**被按停**，可留下了产物」，票面与 `Q153`
  都只说按停。`gave_up` 与「有几步没做成」是另一件事：它们不是人按的，是活自己收的手。
- **这张票实际做了什么：** **只让 `interrupted` 那一档报「停在半路」。** 那两样照旧
  记「完成」，屏上靠子库屏那两句 `⚠️` 兜着——于是它们仍然是「两屏说两套话」的形状，
  只是这一次维护者点名要收的不是它们。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q217 — 刮削那条路上「被按停」有两个出口，其中一个照旧记成「完成」

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** 停在**步与步之间**的走 `crates/gui/src/scrape.rs` 里那几处手写的
  「按停了」，落进 `Ending::Failed`（那是 `Q151`／票 `parking-3/05` 的活）；
  停在**采集那一步里头**的走 `crates/core/src/scrape.rs:861`
  （`if context.cancel.is_cancelled() { interrupted = true; break; }` 然后 `Ok(Outcome)`）
  ——`Ok` 加上没报 `halfway` 就是 `Ending::Done`，历史写「完成」，而
  `crates/gui/src/scrape.rs::finished` 从头到尾不看 `outcome.interrupted`
  （命令行那一侧看的），屏上于是写「刮削跑完了」。识别那一层同理。
- **为什么没停线：** 票的验收点名的是「同步与扫描」两条。第一个出口的措辞是票
  `parking-3/05` 的活，这张票伸手进去只会跟它撞车；第二个出口要动的是刮削那份回执，
  与轴无关。
- **这张票实际做了什么：** 只把轴与两条路（同步、扫描）定下来。刮削那一侧的
  `Ending::Done` / `Ending::Halfway` 两支已经并成一条（产物照收），
  等票 `05` 把第一个出口改成由类型来的时候，一并决定 `scrape.rs:861` 那个出口
  该不该报 `halfway`——它形状上完全符合这一档（停了、交出了产物）。
- **谁来裁：** 票 `parking-3/05`
- **状态：** open

### Q218 — 被按停的扫描那份「到目前为止」的体检报告交回来了，可库屏只拿它说一句话

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/src/roots.rs::Screen::settle` 里
  `(Ending::Halfway { .. } | Ending::Stopped, Job::Scan(_))` 那一支
- **为什么没停线：** 这是**多出来的机会**，不是坏掉的东西。改之前那份报告压根到不了
  界面（折成 `Err` 时丢了），现在它到了，只是没人看。
- **这张票实际做了什么：** 不动。这一支照旧只说「被按停了，断点已经写下」——
  「半份体检报告该怎么摆」是库屏自己的产品问题，不是这条轴的事。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q219 — 「留下了什么」是一句写在核心库里的中文，不是一组数

- **来自：** 票 `parking-3/03`
- **类别：** 规格没说
- **在哪：** `crates/core/src/task.rs::Ending::Halfway::left`（一个 `String`）、
  `crates/core/src/sync/execute.rs::留下了什么`、`crates/core/src/scan/mod.rs` 那句
  `task.halfway(format!("走过 {} 个条目…"))`
- **为什么没停线：** 两条路都站得住，翻案只动这三处加它们的测试。
- **这张票实际做了什么：** **走「核心库出一句话」这条。** 另一条是让 `Halfway` 带一组
  结构化的数（落了几件 / 走了几步 / 断点有没有），界面自己排成话。没选它的理由：
  这张票要的就是**两屏说同一件事**，而话只有一份才不会漂——子库屏那句回执与任务屏历史
  那一行从此同源；何况「落了几件」与「走过几个条目」本来就不是同一组字段，硬凑一个结构
  等于给每条长入口留一片填不满的空格。代价是**核心库里多了给人看的中文措辞**
  （ADR-0005 说折算是领域逻辑，措辞算不算领域逻辑没定过）。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q220 — `中立库落在本机而不是主库里` 那条测试每跑一次就在 `/tmp` 里留一个目录

- **来自：** 票 `parking-3/03`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/scan/mod.rs:2110` 那条单元测试里两处
  `crate::testing::temp_dir("catalog")` / `temp_dir("catalog2")`——两处都是
  **当场丢掉的临时值**：`temp_dir(…)` 返回的 `TempDir` 在那一句结束时就析构、
  目录被删掉，紧接着 `Catalog::open` 又 `create_dir_all` 把它建了回来，
  于是**再也没人删它**。这台机器上眼下攒了 224 个 `/tmp/romcat-catalog2-*`
- **为什么没停线：** 与这张票要动的东西毫无关系（我是在数 `/tmp` 里的残留时撞见的）。
  它也不会让测试变红——`assert!(path.exists())` 那一句恰恰因为「删了又建回来」才过。
- **这张票实际做了什么：** 不动。改法是把那两个 `TempDir` 绑成局部变量（`let _dir = …`），
  但那是别人的测试、别人的文件，顺手改进来只会让这张票的 diff 说不清自己在干什么。
- **谁来裁：** 收尾
- **状态：** open

### Q221 — 「停在半路」这个词刮削屏上已经在用，指的是活自己收的手

- **来自：** 票 `parking-3/03`
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/scrape.rs` 里那句 `" 这一趟停在半路：{}"`（印的是
  `crate::scrape::online::Halt`——配额用完、被封、退避、自设预算、断网）；
  与 `CONTEXT.md` 新收的词条「停在半路」
- **为什么没停线：** 两处说的是**同一件事**（没走完就收了场、却交出了产物），
  不是同名不同物——真要打架的只有「收手的理由」那半格。
- **这张票实际做了什么：** **把词条写成理由无关的**：「一趟活没走完就收了场，
  却交出了产物」，并明说收手的理由（人按的停下、活自己收的手）不改变这一档是什么。
  `Ending::render` 跟着**不再写死「按停时」**——那半句话由长入口自己给
  （`sync::execute::left_behind`、`scan::scan` 那句 `format!`）。另一条路是把词条限死成
  「被按停」，那样刮削屏那句话就得改口——可它那句是对的，改的该是词条。
  **刮削那一趟眼下仍然记成「完成」**，那是 `Q217`。
- **谁来裁：** 收尾
- **状态：** open

### Q222 — `/code-review` 在 worktree 里认错工作区，审的是主工作区的上一个提交

- **来自：** 票 `parking-3/03`
- **类别：** 工具缺陷
- **在哪：** 这张票的收尾流程——`/code-review` 起的那个 fork 继承的是会话的 cwd
  （`/home/alex/workboox/romcat`），不是我干活的那个 worktree
  （`/home/alex/workboox/romcat-wt/03-outcome`）。它于是报「工作区干净」、退回去审
  `git diff HEAD~1`，交上来的八条发现全是主工作区上一个提交里那几份 Markdown 的事，
  与本票的 diff 一条都不沾。
- **为什么没停线：** 它只读，没写脏任何东西（主工作区已核过），而且**看得出来审错了**
  ——「工作区干净」这一句与本票十五个改过的文件当场对不上。
- **这张票实际做了什么：** **另派两个只读 agent 对着 diff 重跑**，一个看仓库标准与
  正确性、一个看规格符合性，提示里把 worktree 的绝对路径、`git -C <worktree>` 的取法、
  以及「不许 `checkout`/`restore`/`reset`/`stash`/`clean`、不许碰别的 worktree、
  不许 build」全写死。两份意见逐条落地了（拆开库屏那句合着写的回执、核心四条集成测试
  从「断侧信道」改成「断任务台记成了哪一支」、`render` 不再写死「按停时」、
  `left` → `left_behind` 并收成非公开）。**没有去改 `/code-review` 本身**：那是这套
  流程的工具，不是这个仓库的代码。
- **怎么防（给下一个人）：** 在 worktree 里收尾时，别指望 `/code-review` 自己找对地方
  ——要么调用时把 worktree 的绝对路径写进参数，要么直接照上面那套派两个只读 agent。
  判据只有一条：**它报的「审查范围」里得出现你那十几个改过的文件**，出现「工作区干净」
  就是审错了。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q284 — 同名异作在**作品**那一层仍旧只有一行

- **来自：** 票 `parking-3/10`
- **类别：** 两份东西矛盾
- **在哪：** `crates/core/src/identify.rs` 的 `Projector::work`、
  `crates/core/src/catalog/content.rs` 的 `work` 表注释；票 `parking-3/10` 验收第 6 条
  「真的同名异作…不被并成一部」
- **为什么没停线：** 票自己那个括号（「两条不同的发行版线」）就是这条路；而挂账 **D53**
  （`deferred.md:331`）早已把这件事裁过一遍，状态 `resolved —— 维持原样`，原话是
  「同名在中立库里本来就已经是同一个作品了，锚点只是跟着它；真要分开，该分的是识别
  那一层的去重键」。翻它不是这张票的事。
- **这张票实际做了什么：** 走 D53 那条已裁的口径——**作品名就是作品的身份**。复用按名字
  来，同名的两部真作品在**作品**那一层共用一行；**分得开落在发行版那一层**，两条发行版线
  各自成行（测试 `同名的两条发行版线不会被并成一条`：一个作品、两条 `release`、
  FC 与 SFC 各一，重跑一趟也不多攒出第三条）。`work.name` 上**没有补 `UNIQUE`**，
  门留着：人工给其中一部改个名（一条裁决就够）之后两行并存，而重跑识别既不把它们并成
  一行、也不抹掉裁决那一行（测试 `同名异作插得进两行而重跑识别不抹掉裁决造的那一行`）。
  另一条路是给作品加消歧键（年份 / 平台世代），要同时改刮削锚点、`converge`、
  浏览行的分组与筛选四处——远超这张票，也正是 D53 说的「该分的是识别那一层的去重键」。
- **谁来裁：** 票 `parking-3/11`（它接作品行的形状）
- **状态：** open

### Q285 — 被叫停的那一趟识别不收「没人再指着」的作品行

- **来自：** 票 `parking-3/10`
- **类别：** 规格没说
- **在哪：** `crates/core/src/identify.rs` 的 `run()` 末尾那句 `if !interrupted`；
  `Catalog::drop_unheld_identified_works`
- **为什么没停线：** 两条路都留下一个说得清的窗口，选的这条窗口更小、而且不会把这张票
  刚治好的病重新引进来。
- **这张票实际做了什么：** 收尾那一遍**只在跑完整一趟之后**跑。被叫停时不收——那时还有
  变体没轮到，收了等于把它们的作品连 `id` 一起扔掉，正是本票要治的那件事。
  **审查在这儿抓到一个洞，一并补了**：起初的写法是「重新成型那一遍是安全网」，
  而它其实是漏洞——`clear_identifications` 起手把 `variant.work_id` 整列摘空，
  于是被叫停之后接着重扫（常规动作），`drop_unheld_works`（判据「还有没有人指着」、
  **不看来路**）会把整库的作品连同 `origin = 裁决` 的那些一起扫掉。补法是**不再整列摘空**：
  变体身上那两条链接改由 `write_identifications` **逐条覆盖**（认不出来时写的是空），
  于是「这一趟认不出来了」与「这一趟还没轮到它」在库里分得开。
  钉着它的测试：`crates/core/tests/identify.rs::被叫停之后重扫再跑一趟作品锚点照样指得中`
  （把那句 `UPDATE variant SET work_id = NULL` 加回去当场红）。
  **剩下的代价**：叫停之后到下一趟跑完之前，库里可能留着已经没人指着的作品行，报告里
  「识别建出来的作品数」在那个窗口里偏高。
- **谁来裁：** 下一个动识别中断路径的人
- **状态：** open

### Q286 — `origin = 裁决` 的作品行，识别这一趟一行都不收

- **来自：** 票 `parking-3/10`
- **类别：** 规格没说
- **在哪：** `Catalog::drop_unheld_identified_works` 里那句 `WHERE origin = ?1`
- **为什么没停线：** 票自己写着「重跑识别本来就只清『识别自己造的』那些行」。
- **这张票实际做了什么：** 收尾只收 `origin = 识别`，而且**来路这一列只升不降**
  （`Projector::work`：识别认领到一行 `origin = 裁决` 时一个字都不动它）。
  **只升不降那一半是审查抓出来的**：起初的写法是「这一趟头一次认领就照本趟重写来路」，
  而识别与裁决在同一趟里各认领一次，**中断正好可以落在两次之间**——那一行会永久停在
  「识别」，下一趟跑完就被收掉，`Q286` 承诺的东西在那条路上失效。顺带也省掉真库上
  9,226 次「写的还是原值」的 `UPDATE`。
  **代价**：一条裁决从沉淀库里删掉之后，它投影出来的那一行会一直挂着「裁决」这个来路
  （报告里「识别建出来的作品数」因此少数一个），而且识别一趟都收不掉它，直到下一次
  **重新成型**（`drop_unheld_works`，判据「还有没有人指着」、不看来路）把它收掉。
  审查核过这条的下游：刮削 subject 与导出收敛都从变体反查作品名，幽灵行不会烧配额、
  也不会长出浏览行，只有 `crate::title` 会给它开一个空标题集合。
  走这条的理由是「识别不替沉淀库做主」；反过来（识别也收裁决那一行）会让 `Q284` 说的
  那扇门在下一趟识别里自动关上。
- **谁来裁：** 下一个动作品来路那一列的人
- **状态：** open

### Q287 — 界面测试「切换选中那一帧不解码」的 1 秒闸在这台机器上压线

- **来自：** 票 `parking-3/10`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/tests/media.rs:248`
- **为什么没停线：** 与本票无关（`romcat-gui` 一个字没动），但它会让门禁第三条随机变红，
  下一个人会以为是自己改的。
- **这张票实际做了什么：** 量了一遍就走，一个字没改。同一条协议各跑 8 趟：`main`
  **过 2 败 6**，本票分支 **过 1 败 7**——两边一样。头一帧实测都落在 **1.00–1.11 秒**，
  而闸是 1 秒。变量是**机器负载**：量的时候另一个 worktree（`14-glossary`）正在跑
  `cargo test --workspace --all-features -j 6`；机器闲下来时两边都过（各 5 趟全过，
  本分支上 `--test media` 整份 9/9）。
- **谁来裁：** 票 `parking-3/01`
- **状态：** **已有归宿 —— 票 `01` 已经收掉（挂单 `Q196`），本条留作实测证据。**
  票 `01` 把那条断言从「花了多久」换成「解没解码」，理由与这里量到的一模一样：
  落到门禁上它是一张彩票。那条改动已合并进 `main`，而本票分支基于 `bd7b967`、
  比 `01` 早，**所以本票这一趟全量测试仍会撞上这条假红，不修**（`main` 上已经不是
  这个样子了）。上面那 16 趟是这条挂单最值钱的部分：`01` 只跑了 2 趟就下的判断，
  这里把它坐实了。

### Q288 — `/code-review` 在 worktree 里会认错工作区，审的是主工作区

- **来自：** 票 `parking-3/10`
- **类别：** 工具缺陷
- **在哪：** `/code-review` 技能派出去的 fork —— 它的 `cwd` 是主工作区
  `/home/alex/workboox/romcat`（在 `main` 上、工作区干净），不是接票 agent 的 worktree
- **为什么没停线：** 它是只读的，没写坏任何东西；但它交回来的是一份**看起来正常、
  其实审的是别人已经提交的代码**的回执——不核对的话会被当成自己这张票过了审查。
- **这张票实际做了什么：** 它自己的回执里写着「`git diff HEAD` 是空的（HEAD ==
  origin/main，工作区干净）」，于是改去审 `main` 上已提交的那批（票 `gui-redesign/17`
  加挂单收口的 Markdown）。我停不掉它（不归我所有），**另派两个只读 agent 对着自己的
  diff 重跑两轴**（Standards / Spec），并在提示里写死绝对路径、写死禁止
  `git checkout/restore/reset/stash/clean`、禁止改文件、禁止跑 `cargo`。
  那两趟一共抓出 2 条高（来路会被中断写坏、被叫停后重扫会把作品扫光）与十几条文档
  与口径问题，全部在本票内收掉了——**这条补救是必须的，不是保险**。
  推荐的防法：在 worktree 里接票时**不要直接 `/code-review`**，改成自己派只读 agent
  并把 worktree 的绝对路径写死在提示里。
- **谁来裁：** 门禁与工具链那一组（票 `parking-3/01`）／下一个在 worktree 里接票的人
- **状态：** open

### Q289 — 主列表那一栏印「高置信」时，`identified` 这一列白算

- **来自：** 票 `parking-3/10`（`/code-review` 认错工作区那一趟，对着 `main` 审出来的，
  见 `Q288`）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/catalog/browse.rs:794`（`WorkRow::identified` 的文档末句）
  与同文件 `confidence_label()`
- **为什么没停线：** 是票 `gui-redesign/17` 的活，代码已经在 `main` 上，与本票无关。
  本票一个字都没改它。
- **这张票实际做了什么：** 只把它记下来，不动。原话：`identified` 的文档说
  「**只要还剩一个没跑过，这一行就说还没识别**」，而 `confidence_label()` 先看
  `confidence.is_none()`，只有一条候选都没有时才去问 `identified`。场景：一部作品挂
  10 个变体，识别跑到第 1 个就被打断而那一个高置信命中——屏上印「高置信」，
  没有任何字告诉人底下还有 9 个连识别都没跑过，`identified` 在这一行上等于白算。
  两种收法：把文档末句限定成「一条候选都没有时」，或让这一栏在 `!identified` 时补个记号。
- **谁来裁：** 票 `gui-redesign/17`

### Q324 — 值**末尾**的空白立成了独立一条，没有并进「开头」那一条

- **来自：** 票 `parking-3/14`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/pegasus.rs::STRUCTURAL_LOSSES` 第 2、3 条
- **为什么没停线：** 票正文点名「补三条声明」，并把值末尾的空白列成其中一条，照着做就有归宿。
- **这张票实际做了什么：** 立成独立的第三条。另一条路站得住：把「开头」那条改写成
  「值**两端**的空白」合成一条，清单短一条、用户少读一行。没走它，是因为两头避不开的
  **理由不一样**——开头是「与续行的缩进长得一模一样，分不开」，末尾是「这个格式没有
  引号一类的界定符，根本写不出来」；并成一条就得挑一个理由丢掉，而 `why` 那一栏存在的
  意义正是「说得出为什么，才不会被当成待修的 bug」。翻案的代价是改一个 `const` 与
  一条测试，全在这张票之内。
- **谁来裁：** 收尾
- **状态：** open

### Q325 — 词表条目里没写「空清单读作**还没查过**」这条口径

- **来自：** 票 `parking-3/14`
- **类别：** 规格没说
- **在哪：** `CONTEXT.md`「结构性损失」条目；口径本体在
  `crates/core/src/adapter.rs::Adapter::structural_losses` 的默认实现文档里
- **为什么没停线：** 票的硬约束写着「词表**只是词表**：不放实现细节、不放取舍记录」，
  这条口径更像后者。
- **这张票实际做了什么：** **先按「词表只是词表」把口径留在了代码文档里，`/code-review`
  之后改了主意、写进词表。** 改的理由：初稿那句「每个**适配器**逐条声明，导出前就看得见」
  是**假的**——`Adapter::structural_losses` 默认 `&[]`，`gamelist` 一条都没声明，而词表
  是所有人都会读到的那一处。定稿写成「要由**适配器**逐条声明出来，导出前**才**看得见
  ……**清单是空的意思是「还没查过」**，不是「这个格式什么都不丢」」。这句是**这个词的
  意思**（空清单不等于没损失），不是实现细节，所以不算破「词表只是词表」。
  另一条路站得住：只把「每个适配器」改成「要由适配器」，口径仍旧不进词表——票 `15`
  那条「不许弱化」就还是只钉在一个默认实现的文档注释上。翻案的代价是删一句话。
- **谁来裁：** 票 `parking-3/15` / 收尾
- **状态：** open

### Q326 — 两个测试名里的「两样」改了，闭合票的证据指针因此失准

- **来自：** 票 `parking-3/14`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/tests/pegasus.rs:314`
  （原名 `导出前就说得出这个格式会丢掉哪两样_而且导入那一侧说的是同一句`）与
  `crates/core/src/adapter/pegasus.rs` 里的
  `档位声明的那两样是实测出来的_不是写死的一句话`；两个名字都被
  `.scratch/queue-followups/issues/02-capability-says-what-pegasus-drops.md` 第 29、33、50 行
  当作验收证据引着（三处，不是两处——第 50 行那处是 `/code-review` 找出来的）
- **为什么没停线：** 名字与实际断言对不上是**这张票造成的**——清单从两条变成了五条。
- **这张票实际做了什么：** 两个都改了名（`……的结构性损失……`、
  `换行与前导空白那两条是……`）。另一条路站得住：留着旧名字不动，闭合票那两行证据就
  还指得过去。没走它，是因为一个断言五条清单的测试叫「会丢掉哪两样」是**主动误导**，
  而 `.scratch` 里的指针失准只要读一眼这条挂单就接得上。翻案的代价是两处改名。
- **谁来裁：** 收尾
- **状态：** open

### Q327 — 量出两样票里没写的，塞进了已有声明而不是各立一条

- **来自：** 票 `parking-3/14`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/pegasus.rs::STRUCTURAL_LOSSES` 第 4、5 条的 `becomes`
- **为什么没停线：** 两样都是同一条机制走到头的那一格，在同一条 `becomes` 里说得完。
- **这张票实际做了什么：** 逐样往返实测时量到票里没写的两样——**整个值就是一行 `.`** 的，
  读回来是空的、**那个字段整条消失**（票只说「读回来成了段落分隔」）；**多值里只有
  一个值且它是空的**，整条属性消失（票只说「读回来是字面量 `.`」）。两样都并进已有声明的
  `becomes`：`.` 那一样进第 4 条，「掐空了字段就消失」那一样进第 3 条——`/code-review`
  指出它**不走 `.` 那条路**（`write_attribute` 取的是单值分支，写的是 `genre: `，
  根本没写出 `.`），初稿把它挂在第 5 条上是把机制说错了，已改。断言在
  `值里那一行点读回来成了段落分隔`、`值末尾的空白与开头那一样被掐掉`、
  `多值字段装不下值里的换行也装不下空串` 三条里。另一条路站得住：各立一条独立声明，
  清单变七条、每一样都有自己的 `why`。没走它，是因为它们与母条同一个成因，拆开会让用户在
  `romcat adapters` 里读到七行而其中两行的 `why` 与上一行几乎逐字相同。翻案的代价是
  拆两条 `const` 项，全在这张票之内。
- **谁来裁：** 收尾
- **状态：** open

### Q328 — 「全部天花板」这句话在 Pegasus 上还差一族：`rating` `players` `release`

- **来自：** 票 `parking-3/14`
- **类别：** 票写错了
- **在哪：** 票 `parking-3/14` 第 3–5 行「Pegasus 那份结构性损失清单说的是这个格式的
  **全部**天花板」；实物在 `crates/core/src/adapter/pegasus.rs::fold_game` 的
  `players` / `release` / `rating` 三个分支（推的是 `LossyNote`），与
  `crates/core/src/adapter.rs` 模块文档「Pegasus 自己就是有损的」那一段
- **为什么没停线：** 票的三条 bullet 点名的就是**值的文本形状**那一族（末尾空白、
  那一行 `.`、多值里的换行与空串），三条都做完了；这一族不在票的范围里。
- **这张票实际做了什么：** 没动，只记在这儿。`rating` 写 `85` 被静默丢弃、`players`
  的 `1-4` 存成 `4`、`release` 写 `1985` 读回来是 `1985-01-01`——按
  `StructuralLoss` 自己的定义（「把一个值交给这个格式存一趟，它自己会把值改成什么样」）
  这三样都算，可它们眼下只走**有损点**（`LossyNote`）那条按行数得出来的路，
  `romcat adapters` 在导出前一个字都不说。所以「全部天花板」这句在补完这一族之后
  仍然不成立。另一条路站得住：这一趟顺手把它们也声明进 `STRUCTURAL_LOSSES`。没走它，
  是因为那要动 `LossyNote` 与 `StructuralLoss` 的分界（同一件事会在两处各说一遍，
  而票 `queue-followups/02` 的验收正压在「两者分得很开」上），撤销它要伸到这张票之外。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q329 — 动了票 `queue-followups/02` 立的那条「值开头的空白」声明

- **来自：** 票 `parking-3/14`
- **类别：** 规格没说
- **在哪：** `crates/core/src/adapter/pegasus.rs::STRUCTURAL_LOSSES` 第 2 条
- **为什么没停线：** 改的是**这句话说得准不准**，格式的行为一个字没动，撤销它只要把
  `what` 与 `becomes` 改回去。
- **这张票实际做了什么：** 票只叫我「补三条声明」，没叫我动已有的。可量末尾那一半的
  时候顺带量出来：`classify` 对续行是先 `trim_start` 再 `trim_end`，掐的是**那一行**
  的两头，不是整个值的两端——`"甲\n    乙"` 与 `"甲  \n乙"` 往返回来都是 `"甲 乙"`，
  行内的缩进也没了。原话「值**开头**的空白」只覆盖了值的头一行，读的人会以为多行值
  中间那些行的缩进留得住。所以把第 2、3 条一起改成「值里**每一行**开头 / 末尾的空白」，
  并加了 `简介往返一趟("甲\n    乙")` 与 `简介往返一趟("甲  \n乙")` 两条断言钉着。
  另一条路站得住：只补第 3 条、第 2 条原样不动，票的范围就一个字没越——代价是清单里
  并排两条，一条说「值开头」一条说「每一行末尾」，而它们其实是同一个 `trim` 的两头。
  （这一样是 `/code-review` 找出来的；我初稿把它写成了「掐的是**整个值**的末尾」，正好说反。）
- **谁来裁：** 收尾

- **状态：** open

### Q344 — 并夹具时统一了两份原本不一样的那两格：候选的成员键与平台

- **来自：** 票 `parking-3/16`
- **类别：** 规格没说
- **在哪：** `crates/gui/tests/shared/mod.rs::候选`（原处：`crates/gui/tests/queue.rs::有一个连识别都没跑过的库`
  与 `crates/gui/tests/browse.rs::三档并排的库`）
- **为什么没停线：** 票明写「断言一条不改、结果一条不变」，而这两格**一条断言都不撑**：
  换过去之后两份测试原样跑绿（`queue` 33 条、`browse` 41 条）。
- **这张票实际做了什么：** 队列那份把候选的 `member_key` 写成**空串**、把 `platform` 一律
  写成 `"SFC"`（连那个落在 FC 上的变体也是）；浏览那份写的是变体自己的主成员键。
  并进来时统一取**变体自己的**那一格——单文件变体撞上的就是它那一个成员，候选说的平台
  与变体所在的平台是同一个。另一条路站得住：把这两格开成 `shared::候选` 的参数，
  两处各写各的，字节级一模一样。没走它，是因为那会把一处随手填的空串固化成共用模块的
  公开接口，而它一条断言都不撑，往后每个用这个搭子的人都得先问一句「这一格该填什么」。
  翻案的代价是给 `shared::候选` 加两个参数，全在这张票之内。
- **谁来裁：** 收尾
- **状态：** open

### Q345 — 「翻行会看得出来的延迟」这道门本票没接，明确留给票 `parking-3/17`

- **来自：** 票 `parking-3/16`（票 `parking-3/01` 的 `Q196` 路由过来的那半条：
  「『翻行会看得出来』的延迟从此门禁里没人把门，路由给票 `16`/`17`」）
- **类别：** 规格没说
- **在哪：** `crates/gui/tests/browse.rs:1057` —— `assert!(cost.reads <= 2)`；
  对照 `crates/gui/src/bench.rs::FrameCost::reads`
- **为什么没停线：** 本票正文没有这一条，两件正事都做完了。
- **这张票实际做了什么：** **没接，但把该走哪条路定下来了。** 本票并出来的那份夹具是
  **三个变体的手搭小库**，它存在的理由正是「同一张表上几档并排」——给它加一个量得出规模
  的档位，等于在 `demo::browse_shaped` 旁边另起一条互相打架的量级路子。
  而**不打彩票的那把尺子这个仓库已经有了**：`bench::scroll` 交回的 `FrameCost::reads`
  （这一趟读了几次库），`crates/gui/tests/browse.rs::库里有一条顶到闸上的简介时列表照样滚得动`
  断的就是它（`cost.reads <= 2`，240 行滚下来只许跨一次窗）——**它数的是次数不是毫秒，
  与机器忙不忙无关**，正是 `Q196` 拿掉那条挂钟断言时缺的那种判据。缺的只是它跑在**真库
  量级**上，而「换量级」与「重定基线」正是票 `17` 明写的两条验收。
  另一条路站得住：本票顺手把那条 `reads` 断言抄一份到真库量级上。没走它，是因为量级一换
  基线就得跟着重定，在票 `17` 之前先钉一个数，它到了要么推翻要么绕开。
  翻案的代价是加一条测试，全在票 `17` 之内。
- **谁来裁：** 票 `parking-3/17`
- **状态：** open

### Q346 — 共用模块整份压掉了 `dead_code`，不是逐项挂

- **来自：** 票 `parking-3/16`
- **类别：** 规格没说
- **在哪：** `crates/gui/tests/shared/mod.rs` 顶上那句 `#![allow(dead_code)]`
- **为什么没停线：** 压掉的是一条 warn 级 lint；`clippy --workspace --all-targets
  --all-features` 照旧零告警，退出码一次都没变过。
- **这张票实际做了什么：** `tests/shared/mod.rs` **整份**编进每一个 `mod shared;` 它的测试
  二进制（眼下四个：`queue` `browse` `layout` `sublibrary`），而搬进来的那几个搭子只有
  `queue` 与 `browse` 用得上——在 `layout` 与 `sublibrary` 那两个二进制里它们就是死代码。
  加了模块级 `#![allow(dead_code)]`，并在注释里写清这条告警在这儿说的不是「这段代码没人用」
  而是「这个二进制没用它」。另一条路站得住：逐项挂 `#[allow(dead_code)]`，将来真有一样
  彻底没人用时还看得出来。没走它，是因为搭子是成套的（`档` / `变体` / `候选` / `小库`
  四样连着走），逐项挂等于在每一样头上重复同一句话；而这个模块本来就是「谁用得上谁拿」
  的地方。翻案的代价是把一行 `#![allow]` 拆成几条 `#[allow]`，全在这张票之内。
- **谁来裁：** 收尾
- **状态：** open

### Q347 — 「悬停里写着什么」从「这儿验不了」变成验得了，靠的是从画出来的字里找位置

- **来自：** 票 `parking-3/16`
- **类别：** 规格没说
- **在哪：** `crates/gui/tests/shared/mod.rs::那一段画在哪儿` 与 `::悬停在`；
  对照 `crates/gui/tests/browse.rs` 末尾那句「悬停里跟着说的那一句**这儿验不了**」
- **为什么没停线：** 加的全在测试这一侧，界面与核心库一个字没动。
- **这张票实际做了什么：** 票要一条「断那句悬停摆得出来」的界面测试，而 `browse.rs` 末尾
  的原话是悬停「这儿验不了：它在 `on_hover_ui` 里，而这几帧没有指针，egui 一个字都不画」。
  本票把它变成验得了的：**从这一帧画出来的 `Galley` 里找那段字画在哪儿**（`Shape::Text`
  自带原文与位置），把指针停过去，再不动地跑 90 帧跨过 egui 那 0.5 秒的悬停延迟——
  `RawInput::time` 留空时 egui 每帧自己往前推 `predicted_dt`（1/60 秒），所以这道闸
  **是帧数不是挂钟，不打彩票**（与 `Q196`、`Q349` 那一类正相反）。
  另一条路站得住：把控件的 `Rect` 从 `Screen` 上漏出来给测试。没走它，是因为那要为一条
  测试在界面层开一个只有测试用得着的出口，而「屏上写的是什么」这条既有接缝本来就带着位置。
  翻案的代价是删两个搭子、改一条测试，全在这张票之内。
  **第三条路是收尾审查提出来的**：在测试的 `ctx` 上把 `tooltip_delay` 调成 0，两三帧就够，
  而且把 egui 那两个内部默认值整个从测试的前提里拿掉（哪天 0.5 改成 0.8 就不用重算 90）。
  没走它，是因为那样一来「屏上那句话到底要等多久才出来」从此没人看着——而这一条本来
  就是在验一句**悬停**。90 帧那个数的代价量过了：整条测试 1.2 秒跑完，已写进注释。
  **`browse.rs` 末尾那句话没跟着改**：它对它自己那一条仍然成立（那几帧确实没有指针），
  改它要动别人的验收正文。
- **谁来裁：** 收尾
- **状态：** open

### Q348 — 票与规格里的「批次卡片」用的是词表明令避开的那个词

- **来自：** 票 `parking-3/16`
- **类别：** 票写错了
- **在哪：** 票 `.scratch/parking-3/issues/16-shared-gui-fixture-and-card-hover.md` 标题、
  第 8 行、第 17 行；规格 `.scratch/parking-3/spec.md` 第 158、276、302 行。
  对照 `CONTEXT.md` 词条 **批** 的 `_Avoid_`：「批次、批号、批次号、桶、事务、操作」
- **为什么没停线：** 是措辞，不是范围——那张卡片是哪一张、要挂什么，票说得一清二楚。
- **这张票实际做了什么：** **代码里不跟**：新测试叫
  `crates/gui/tests/queue.rs::卡片悬停说得出这一批在命令行上是什么`，界面那句注释与
  文档一律只写「一批」「卡片」。**票与规格的正文一个字没改**——改它会让那两份记录失真
  （票 `parking-3/14` 的 `Q328` 已经立过这个先例：票写错了就记下来，不去改票）。
  另一条路站得住：顺手把票与规格里那六处改掉。没走它，是因为规格是这一轮的成文记录，
  实现者改它等于替写规格的人改口。翻案的代价是六处字符串替换。
  这条对着词表本身也是一个信号：`_Avoid_` 挡得住代码，挡不住写票的人——**没有任何东西
  在票与规格上跑一遍词表**。
- **谁来裁：** 拿主意的人
- **状态：** open

### Q349 — 门禁里还有第二张挂钟彩票：`sublibrary.rs` 那句「任务六秒都没跑完」

- **来自：** 票 `parking-3/16`（路过发现，不在范围内）
- **类别：** 工具缺陷
- **在哪：** `crates/gui/tests/sublibrary.rs:226` ——
  `排着队的那一趟同步撤得掉而且撤完目标与工作目录一处不差` 里那句 `任务六秒都没跑完`
- **为什么没停线：** 与本票动的东西无关（`sublibrary.rs` 只用共用模块的 `画出来的字`，
  那一样一个字没改），而且重跑就绿。
- **这张票实际做了什么：** **没动，只把它记下来连同实测。** 本票把四个用共用模块的测试
  文件一趟跑完（`queue` `browse` `layout` `sublibrary`）时它红了一次：
  「任务六秒都没跑完」，同一条单独重跑当场绿（11.13 秒跑完，其中等任务那一段远在 6 秒内）。
  当时机器上有四个 worktree 同时在编。这与 `Q196` 是**同一类**东西——票 `parking-3/01`
  把 `crates/gui/tests/media.rs` 那条「头一帧 < 1 秒」的挂钟断言换成了「解没解码」，
  理由是它落到门禁上是一张彩票；**这一条是同一张彩票的另一张票根，还在门禁里**。
  没顺手改，是因为 `sublibrary.rs` 眼下正是票 `parking-3/03` 在动的那几份之一
  （核心库的收场枚举与任务屏），去改它就是跟一个正在飞行的改动抢同一个文件。
  怎么改有现成的样板：判据从「花了多久」换成「跑完没跑完」——那一趟同步的**收场**
  是库里读得回来的事实，不是一个秒数。
  **旁证（编排者给的，来自票 `parking-3/10`）：** 那张票为 `media.rs` 那条挂钟断言做过
  16 趟对照实测——`main` 与它自己的分支各 8 趟，**过 2 败 6 / 过 1 败 7**；等机器闲下来
  **9 趟 9 过**。判据就是「单跑就绿、并发就红」，本条实测的样子与它一模一样
  （四文件一趟跑红一次，同一条单独重跑 11.13 秒当场绿），**同一套判据直接适用**。
  这一条同时是个反证：票 `parking-3/01` 拿掉 `media.rs` 那一条**不等于**这类问题解决了
  ——同一张彩票在门禁里还剩这一张票根。
- **谁来裁：** 票 `parking-3/18`（第三轮收口时接它；与 `Q196` 同一批看）
- **状态：** open

### Q350 — 队列那份夹具从前共用 `demo::workspace()`，破的是同一份文件头上自己立的规矩

- **来自：** 票 `parking-3/16`（收尾审查抓的第 1 条）
- **类别：** 工具缺陷
- **在哪：** `crates/gui/tests/queue.rs::有一个连识别都没跑过的库` 递给 `App::new` 的那个
  工作目录；规矩写在同一份文件第 25 行，另有六份界面测试（`browse` `task` `close`
  `table` `media` `layout`）各写了一遍
- **为什么没停线：** 那条测试断的是 `contains`、不是行数，眼下咬不到人。
- **这张票实际做了什么：** **改掉了**，换成同文件其余测试共用的那个 `工作目录()`。
  原话是「**不共用 `demo::workspace()`**：那是 `--demo` 那个演示窗口用的目录，而窗口会往
  里写**版式偏好**（面板拖到哪儿）……而断言数的正好是行数」——而这一处是整套界面测试里
  **唯一**破例的地方。它是从旧夹具继承来的（一个埋在函数体里的字面量），本票恰恰是它
  变成 `shared::小库` 一个**具名参数**、并在那个参数的文档里把这条规矩又写了一遍的时刻：
  文档立的规矩被它自己唯一的调用方当场破掉。改完 `queue` 33 条原样跑绿。
  另一条路站得住：一个字不动，只在那一行补一句「为什么这一条例外」——票明写「结果一条
  不变」，而换工作目录严格说是动了夹具的行为。没走它，是因为**说不出那个例外的理由**：
  这一处没有任何东西需要演示窗口那个目录。翻案的代价是把一个函数参数改回去。
  留给票 `17` 的那半句：它要把这份夹具推到真库量级、断言基线也要重排，那时共用演示
  窗口的目录就不再是「眼下咬不到人」了。
- **谁来裁：** 收尾
- **状态：** open
