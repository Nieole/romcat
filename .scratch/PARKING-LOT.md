# 挂单：实现时撞上、但没有为它停线的东西

**Status:** living（本轮队列全部票共用一份）
**Opened:** 2026-09-07（第三轮；第二轮 2026-09-05 开、第一轮 2026-09-04 开，
`Q1`–`Q132` 与第二轮迁走的 32 条都已清空并迁回票据）
**Effort:** 第三轮队列 = `.scratch/parking-3/issues/` 18 张

**本轮收口的范围（2026-09-07 裁定）：`Q135`–`Q193`，共 21 条**（其中 19 条彼时还 open）。
范围在动手之前定死：这条线之后记下来的（`Q194` 起）属于下一轮，**不会被这一轮吸进来**。

**编号一路往下走，第三轮从 `Q194` 起。** 之前的编号已经迁回各自的票据末尾
（「挂单裁决」一节），编号留着占号、**不复用**——票据里引用得回来。

⚠️ **本轮起不再往 `deferred.md` 送任何东西**（票 `18` 会把它归档关掉）。每条条目只有两个
去处：**迁回原票**，或者**开成一张票**。分类只有三档：**A** 已经做完只是没销、**B** 不成立了、
**C** 仍然成立要开票。

---

## 这份文件是什么

接票实现时总会撞上票没预见到、或者票写错了的东西。**这里面几乎没有一件该停下整条队列。**
它们落在这儿：**记下来、绕过去、接着走、收尾时一起裁。**

它不是项目的长期缺口清单（那是 `.scratch/rom-metadata-automation/deferred.md`，编号 `Dn`，全局连续）：

| | 挂账 `deferred.md` | 这份挂单 |
|---|---|---|
| 装的是 | **产品**里的取舍 | **执行**里冒出来的问题——票写错了、规格没说、两份东西互相矛盾 |
| 寿命 | 活得比这轮队列长 | **收尾时清空**——每条要么就地裁掉、要么升进 `deferred.md`、要么开成新票 |
| 谁读 | 下一个动这块产品的人 | 收尾那一步，和拿主意的人 |

**编号分两套，别混：** 这里用 `Q1、Q2…`；`deferred.md` 用 `D1、D2…`。
一条从这里升进 `deferred.md` 时才拿 `Dn` 号，且这边的 `Qn` 留着不复用。

---

## 停线——只有这三条

停线很贵：拿主意的人得把整个上下文重新装回脑子。门槛：

1. **接着做会写出一个错的东西，而且撤销它要伸到这张票之外。**
   典型：票里的指示与上游一条仍然生效的决定（ADR、词表、规格）相悖。
2. **动作不可逆，而且不止一个方向站得住。**
   典型：删掉或改名一个被很多处引用的东西——一个 id、一个字段、一份文件。
3. **前提根本不在，票做不下去。**
   典型：「在 X 上面盖 Y」，而 X 不存在。

其余一律：记在这儿，挑最站得住的那条路走，接着做。这包括：票里的数字与你实测的对不上、
验收条目读着含糊、票没想到的第三种情况、路过看见的无关缺陷、以及你确实拿不准的单条。

> ⚠️ **绕过去要留痕。** 条目记的是**你走了哪条路**，不是「有两条路」。
> 说不出自己做了什么的条目，不算一条条目。

---

## 条目格式

```markdown
### Q<N> — <一句话：这是什么>

- **来自：** 票 `<feature>/NN`
- **类别：** 票写错了 / 规格没说 / 两份东西矛盾 / 工具缺陷 / 路过发现，不在范围内
- **在哪：** 路径 + 行号或 id —— 要点得过去
- **为什么没停线：** 一句话
- **这张票实际做了什么：** 一句话 —— **必填**
- **谁来裁：** 拿主意的人 / 票 `NN` / 收尾
- **状态：** open
```

编号**接着上一轮往下走**（第二轮从 `Q133` 起），不要按票分节——「来自」那行已经带了这个信息。

## 第二轮收尾（2026-09-06）

第二轮 14 张票跑完，挂单从 57 条收到 **17 条**：

- **32 条迁回它们所属的票**（票据末尾「挂单裁决（第二轮）」一节）：23 条 `settled`（决定已落进代码并有测试钉着）、7 条 `noted`（核对过不用改）、Q145（已被 Q170 做掉）、Q146（由票 `gui-redesign/17` 承接）。
- **1 条升进 `deferred.md`**：Q141 → **D163**（报告口径与详情面板该对齐哪一侧，是产品方向，没有票可接）。
- **剩下这 17 条是真待办**，按主题分成六组，是下一轮 `/to-spec` 的材料：

| 组 | 挂单 | 一句话 |
|---|---|---|
| 结构性损失清单补齐 | Q159 Q160 Q161 Q163 | Pegasus 还有一族没声明、值末尾空白、gamelist 没查过、词表缺「结构性损失」 |
| 「停下」从核心到任务台一致 | Q150 Q151 Q153 | 下载停不掉、按停的话折错记成失败、按停留下产物记成完成 |
| `sync/execute.rs` 落点闸收尾 | Q135 Q136 Q142 | 不敏感盘验不了、列不开的目录那一格、审查报的两条 |
| 门禁与工具链 | Q187 Q188 Q189 Q191 | 门禁没有机器执行的地方、没钉住 rustfmt 版本、`cargo doc` 68 条告警、`main` 上两处本来就不 fmt-clean |
| 夹具与合成数据 | Q156 Q168 Q177 Q192 | `bench-browse` 还在 20 作品、`demo` 只有两个变体有 entry 行、队列卡片缺一句悬停、两份界面测试各搭一份几乎一样的小库 |

> **票 `gui-redesign/17` 跑完之后**：「还没识别」裁词那一组（`Q148`）收掉了，四处文档
> 改成指向 `CONTEXT.md`；新添 `Q190`（分辨机制走了哪条路，已裁）与 `Q191`（`main` 上
> 本来就不 fmt-clean 的两处，并进门禁那一组）与 `Q192`（两份界面测试的夹具重复，
> 并进夹具那一组）。真待办 **18 条**。

**编号继续往下走，下一个空号是 `Q193`。**

> 第三轮开工时 `Q193` 已由 `Q66` 拆出，与这 18 条一起进了下面的第三轮收口，下一个空号是 `Q194`。

---

## 第三轮收口（2026-09-07）

**范围：`Q135`–`Q193`，21 条。** 全部有归宿，条目区清空。

**判据：一条挂单有了主人就算收了，不是等它做完。** 反过来读——「做完才算收」——最大的
那条会把整批一直摁在开着的状态，而新条目在后面继续堆。这份挂单上一轮之所以到过三位数，
就是这么来的。

### 三条车道

| 车道 | 条数 | 是什么 |
|---|---|---|
| **记录** | 3 | 撞见一个事实，没有谁欠一个决定 |
| **岔路** | 0 | 条目里一条都没剩——`/to-spec` 写第三轮规格时已经替每条挑了路（见下表「去处」列） |
| **工作** | 18 | 是一处改动，不是一个选择；要一张票兜着 |

### 已收索引

| 条目 | 一句话 | 车道 | 去处 |
|---|---|---|---|
| `Q135` | 「不敏感的卡上行为一致」这一半，开发机上造不出来 | 工作 | 票 `parking-3/06` —— 给 `sync::execute` 加一道可注入的目标侧接缝 |
| `Q136` | 列不开的目录底下，折起来那半边闸整个失效 | 工作 | 票 `parking-3/07` —— 「读不动」立成第三态，照 ADR-0021 |
| `Q142` | 收尾审查在别的票已提交的代码里报了两条 | 工作 | 票 `parking-3/07` —— 落点判据收紧、单次判定内共享目录缓存。第 3 条（已归档票的验收正文还写「六层」）**核过不改**：改它会让那张票的记录失真 |
| `Q148` | 四处文档还指着裁掉的挂单说「词表眼下没收」 | 记录 | 已由票 `gui-redesign/17` 收干净（实际是五处，裁决在该票末尾） |
| `Q150` | 下载那 435 MB 仍然停不掉 | 工作 | 票 `parking-3/05` |
| `Q151` | 界面刮削把「按停了」折成一句对不上的话 | 工作 | 票 `parking-3/05` |
| `Q153` | 被按停却留下产物的那一趟，历史里记成「完成」 | 工作 | 票 `parking-3/03` |
| `Q156` | `--bench-browse` 与那七个测试还在二十个作品上 | 工作 | 票 `parking-3/17` |
| `Q159` | 「结构性损失」这个词还没进词表 | 工作 | 票 `parking-3/14` |
| `Q160` | 值**末尾**的空白没声明 | 工作 | 票 `parking-3/14` |
| `Q161` | ES gamelist 的结构性损失清单是空的 | 工作 | 票 `parking-3/15` |
| `Q163` | Pegasus 还有一族没声明的结构性损失 | 工作 | 票 `parking-3/14` |
| `Q168` | 合成数据里只有那两个变体有 `entry` 行 | 工作 | 票 `parking-3/17` |
| `Q177` | 队列卡片缺一句「命令行上是什么」的悬停 | 工作 | 票 `parking-3/16` |
| `Q187` | 「门禁」没有任何机器执行的地方 | 工作 | 票 `parking-3/01` |
| `Q188` | 没有任何东西钉住是哪一版 `rustfmt` | 工作 | 票 `parking-3/01` —— 落 `rust-toolchain.toml`，钉具体版本 |
| `Q189` | `cargo doc` 68 条 rustdoc 告警 | 工作 | 票 `parking-3/02` |
| `Q190` | 「跑过没跑过」分辨落在哪儿 | 记录 | 已裁：给行加标记、`Tier` 四档不动（票 `gui-redesign/17`） |
| `Q191` | `main` 上两处不 fmt-clean，其一是重复的 `#[test]` | 记录 | **已做掉**：提交 `bd4273d` 删了那条重复的 `#[test]`。实测 `crates/core/tests/sync_run.rs` 16 个 `#[test]`，没有相邻重复 |
| `Q192` | 两份界面测试各搭了一份几乎逐字相同的夹具 | 工作 | 票 `parking-3/16` |
| `Q193` | 作品锚点里的那个 id 会被重跑识别换掉 | 工作 | 票 `parking-3/10` —— **已 `settled`**，迁回票 `gui-redesign/03` |

**工作那一道产出了什么：** 18 条全部落进 `.scratch/parking-3/issues/` 里**已经开着的** 12 张票
（`01` `02` `03` `05` `06` `07` `10` `14` `15` `16` `17`，外加 `18` 兜 `Q191`）——这一轮
**没有为挂单新开任何票**，也**没有一条无家可归**。那 12 张票每一张末尾都已经自带
「把本条标 `settled` 迁回原票」的验收条，迁回是各票收尾时自己的事。

**升进长期挂账的：0 条。** 本轮起 `deferred.md` 不再接新条目（票 `parking-3/18`）。

### 量一量：每张票撞出几条

**每张票撞出几条** = 一轮效力记下的条目数 ÷ 它手上的票数。对着 **1** 读：1 就是平均
每张票撞上一件它没预见到的事。

| 效力 | 记下的条目 | 票数 | 每张票撞出几条 |
|---|---|---|---|
| `offline-chinese-fields` | 48（`Q1`–`Q49` 一带） | 6 | **8.0** |
| `gui-redesign` | 101（`Q50`–`Q132` 一带 + `Q143`–`Q158`、`Q190`–`Q192`） | 17 | **5.9** |
| `queue-followups` | 44（`Q133`–`Q142`、`Q159`–`Q189` 一带） | 11 | **4.0** |
| 合计 | 193 | 34 | **5.7** |

喂进这一轮范围的是后两条：`queue-followups` **4.0**、`gui-redesign` **5.9**。

**读数：全线远在 1 以上。** 票是在地形还没探明之前写的，缺的那半份规格落在了这儿。
三轮的落差 **8.0 → 5.9 → 4.0** 是同一个方向：一路在降，但还差着四倍。这是冲
`/to-tickets` 的信号，不是冲这份文件——那几批更该先走一趟 `/wayfinder`。
第三轮（`parking-3`，18 张）的读数要等它跑完才有；下一轮收口时补上，这一串才是这个仓库
自己的刻度。

---

## 条目

<!-- 从 `Q194` 往下追加；不要重排，不要删。清空于第三轮收口（2026-09-07） -->

### Q284 — 同名异作在**作品**那一层仍旧只有一行

- **来自：** 票 `parking-3/10`
- **类别：** 两份东西矛盾
- **在哪：** `crates/core/src/identify.rs` 的 `Projector::work`、
  `crates/core/src/catalog/content.rs` 的 `work` 表注释；票 `parking-3/10` 验收第 6 条
  「真的同名异作…不被并成一部」
- **为什么没停线：** 票自己那个括号（「两条不同的发行版线」）就是这条路；而挂账 **D53**
  （`deferred.md:331`）早已把这件事裁过一遍，状态 `resolved —— 维持原样`，原话是
  「同名在中立库里本来就已经是同一个作品了，锚点只是跟着它；真要分开，该分的是识别
  那一层的去重键」。翻它不是这张票的事。
- **这张票实际做了什么：** 走 D53 那条已裁的口径——**作品名就是作品的身份**。复用按名字
  来，同名的两部真作品在**作品**那一层共用一行；**分得开落在发行版那一层**，两条发行版线
  各自成行（测试 `同名的两条发行版线不会被并成一条`：一个作品、两条 `release`、
  FC 与 SFC 各一，重跑一趟也不多攒出第三条）。`work.name` 上**没有补 `UNIQUE`**，
  门留着：人工给其中一部改个名（一条裁决就够）之后两行并存，而重跑识别既不把它们并成
  一行、也不抹掉裁决那一行（测试 `同名异作插得进两行而重跑识别不抹掉裁决造的那一行`）。
  另一条路是给作品加消歧键（年份 / 平台世代），要同时改刮削锚点、`converge`、
  浏览行的分组与筛选四处——远超这张票，也正是 D53 说的「该分的是识别那一层的去重键」。
- **谁来裁：** 票 `parking-3/11`（它接作品行的形状）
- **状态：** open

### Q285 — 被叫停的那一趟识别不收「没人再指着」的作品行

- **来自：** 票 `parking-3/10`
- **类别：** 规格没说
- **在哪：** `crates/core/src/identify.rs` 的 `run()` 末尾那句 `if !interrupted`；
  `Catalog::drop_unheld_identified_works`
- **为什么没停线：** 两条路都留下一个说得清的窗口，选的这条窗口更小、而且不会把这张票
  刚治好的病重新引进来。
- **这张票实际做了什么：** 收尾那一遍**只在跑完整一趟之后**跑。被叫停时不收——那时还有
  变体没轮到，收了等于把它们的作品连 `id` 一起扔掉，正是本票要治的那件事。
  **审查在这儿抓到一个洞，一并补了**：起初的写法是「重新成型那一遍是安全网」，
  而它其实是漏洞——`clear_identifications` 起手把 `variant.work_id` 整列摘空，
  于是被叫停之后接着重扫（常规动作），`drop_unheld_works`（判据「还有没有人指着」、
  **不看来路**）会把整库的作品连同 `origin = 裁决` 的那些一起扫掉。补法是**不再整列摘空**：
  变体身上那两条链接改由 `write_identifications` **逐条覆盖**（认不出来时写的是空），
  于是「这一趟认不出来了」与「这一趟还没轮到它」在库里分得开。
  钉着它的测试：`crates/core/tests/identify.rs::被叫停之后重扫再跑一趟作品锚点照样指得中`
  （把那句 `UPDATE variant SET work_id = NULL` 加回去当场红）。
  **剩下的代价**：叫停之后到下一趟跑完之前，库里可能留着已经没人指着的作品行，报告里
  「识别建出来的作品数」在那个窗口里偏高。
- **谁来裁：** 下一个动识别中断路径的人
- **状态：** open

### Q286 — `origin = 裁决` 的作品行，识别这一趟一行都不收

- **来自：** 票 `parking-3/10`
- **类别：** 规格没说
- **在哪：** `Catalog::drop_unheld_identified_works` 里那句 `WHERE origin = ?1`
- **为什么没停线：** 票自己写着「重跑识别本来就只清『识别自己造的』那些行」。
- **这张票实际做了什么：** 收尾只收 `origin = 识别`，而且**来路这一列只升不降**
  （`Projector::work`：识别认领到一行 `origin = 裁决` 时一个字都不动它）。
  **只升不降那一半是审查抓出来的**：起初的写法是「这一趟头一次认领就照本趟重写来路」，
  而识别与裁决在同一趟里各认领一次，**中断正好可以落在两次之间**——那一行会永久停在
  「识别」，下一趟跑完就被收掉，`Q286` 承诺的东西在那条路上失效。顺带也省掉真库上
  9,226 次「写的还是原值」的 `UPDATE`。
  **代价**：一条裁决从沉淀库里删掉之后，它投影出来的那一行会一直挂着「裁决」这个来路
  （报告里「识别建出来的作品数」因此少数一个），而且识别一趟都收不掉它，直到下一次
  **重新成型**（`drop_unheld_works`，判据「还有没有人指着」、不看来路）把它收掉。
  审查核过这条的下游：刮削 subject 与导出收敛都从变体反查作品名，幽灵行不会烧配额、
  也不会长出浏览行，只有 `crate::title` 会给它开一个空标题集合。
  走这条的理由是「识别不替沉淀库做主」；反过来（识别也收裁决那一行）会让 `Q284` 说的
  那扇门在下一趟识别里自动关上。
- **谁来裁：** 下一个动作品来路那一列的人
- **状态：** open

### Q287 — 界面测试「切换选中那一帧不解码」的 1 秒闸在这台机器上压线

- **来自：** 票 `parking-3/10`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/gui/tests/media.rs:248`
- **为什么没停线：** 与本票无关（`romcat-gui` 一个字没动），但它会让门禁第三条随机变红，
  下一个人会以为是自己改的。
- **这张票实际做了什么：** 量了一遍就走，一个字没改。同一条协议各跑 8 趟：`main`
  **过 2 败 6**，本票分支 **过 1 败 7**——两边一样。头一帧实测都落在 **1.00–1.11 秒**，
  而闸是 1 秒。变量是**机器负载**：量的时候另一个 worktree（`14-glossary`）正在跑
  `cargo test --workspace --all-features -j 6`；机器闲下来时两边都过（各 5 趟全过，
  本分支上 `--test media` 整份 9/9）。
- **谁来裁：** 票 `parking-3/01`
- **状态：** **已有归宿 —— 票 `01` 已经收掉（挂单 `Q196`），本条留作实测证据。**
  票 `01` 把那条断言从「花了多久」换成「解没解码」，理由与这里量到的一模一样：
  落到门禁上它是一张彩票。那条改动已合并进 `main`，而本票分支基于 `bd7b967`、
  比 `01` 早，**所以本票这一趟全量测试仍会撞上这条假红，不修**（`main` 上已经不是
  这个样子了）。上面那 16 趟是这条挂单最值钱的部分：`01` 只跑了 2 趟就下的判断，
  这里把它坐实了。


### Q288 — `/code-review` 在 worktree 里会认错工作区，审的是主工作区

- **来自：** 票 `parking-3/10`
- **类别：** 工具缺陷
- **在哪：** `/code-review` 技能派出去的 fork —— 它的 `cwd` 是主工作区
  `/home/alex/workboox/romcat`（在 `main` 上、工作区干净），不是接票 agent 的 worktree
- **为什么没停线：** 它是只读的，没写坏任何东西；但它交回来的是一份**看起来正常、
  其实审的是别人已经提交的代码**的回执——不核对的话会被当成自己这张票过了审查。
- **这张票实际做了什么：** 它自己的回执里写着「`git diff HEAD` 是空的（HEAD ==
  origin/main，工作区干净）」，于是改去审 `main` 上已提交的那批（票 `gui-redesign/17`
  加挂单收口的 Markdown）。我停不掉它（不归我所有），**另派两个只读 agent 对着自己的
  diff 重跑两轴**（Standards / Spec），并在提示里写死绝对路径、写死禁止
  `git checkout/restore/reset/stash/clean`、禁止改文件、禁止跑 `cargo`。
  那两趟一共抓出 2 条高（来路会被中断写坏、被叫停后重扫会把作品扫光）与十几条文档
  与口径问题，全部在本票内收掉了——**这条补救是必须的，不是保险**。
  推荐的防法：在 worktree 里接票时**不要直接 `/code-review`**，改成自己派只读 agent
  并把 worktree 的绝对路径写死在提示里。
- **谁来裁：** 门禁与工具链那一组（票 `parking-3/01`）／下一个在 worktree 里接票的人
- **状态：** open

### Q289 — 主列表那一栏印「高置信」时，`identified` 这一列白算

- **来自：** 票 `parking-3/10`（`/code-review` 认错工作区那一趟，对着 `main` 审出来的，
  见 `Q288`）
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/catalog/browse.rs:794`（`WorkRow::identified` 的文档末句）
  与同文件 `confidence_label()`
- **为什么没停线：** 是票 `gui-redesign/17` 的活，代码已经在 `main` 上，与本票无关。
  本票一个字都没改它。
- **这张票实际做了什么：** 只把它记下来，不动。原话：`identified` 的文档说
  「**只要还剩一个没跑过，这一行就说还没识别**」，而 `confidence_label()` 先看
  `confidence.is_none()`，只有一条候选都没有时才去问 `identified`。场景：一部作品挂
  10 个变体，识别跑到第 1 个就被打断而那一个高置信命中——屏上印「高置信」，
  没有任何字告诉人底下还有 9 个连识别都没跑过，`identified` 在这一行上等于白算。
  两种收法：把文档末句限定成「一条候选都没有时」，或让这一栏在 `!identified` 时补个记号。
- **谁来裁：** 票 `gui-redesign/17`
- **状态：** open
