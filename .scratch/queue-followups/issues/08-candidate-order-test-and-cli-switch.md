# 08 — 候选按可信程度排：补测试，并给命令行按依据形状选的开关

**What to build:** 待确认队列那一层的「候选按可信程度排」**一条测试都没有**，眼下靠人肉复核。这一票给它补上。

同时把**按依据形状选一批**的开关补进命令行——待确认屏已经按依据形状分批，命令行却选不出同一批，两边拿到的东西不一致。

**Blocked by:** 无 —— 可立即开工

**Status:** done

- [x] 「候选按可信程度排」有测试钉着，次序是确定的
      —— **三条**，各钉一段（为什么要分三条见挂单 Q173）：
      `crates/core/src/identify.rs::tests::候选先按可信程度排再论平台与源的先后` 直接拿
      私有的 `rank` 比三层：中文离线源**中置信**那条排在 No-Intro **低置信**那条前面
      （可信程度压过源）、平台对得上的压过源的先后、都一样时才按
      `No-Intro → Redump → TOSEC → MAME → GoodNES` 论。
      `crates/core/tests/identify.rs::中立库交回候选的次序就是按可信程度排的那一个`
      走完整一趟识别再从中立库读回来：`库/FC/谁也不认得.zip` 上摆两条，**高置信那条出自
      GoodNES（源排第五）、中置信那条出自 No-Intro（源排头一个），而且中置信那份 DAT
      先写进去**——两种排法给出相反的答案，交回来的必须是 `[GoodNES 高置信, No-Intro
      中置信]`。`crates/core/tests/triage.rs::一级分批的键取的是最可信的那条候选`
      在队列里钉分批的键与 `candidates[0]` 是同一个。
      **变异实测两次**：把 `rank` 里置信度那一层换成常量 0，第 1、2 条当场红；
      把整条 `sort_by` 拿掉（Q82 说的旧写法「只剩写入顺序」），第 2、3 条当场红
      （`left: [("GoodNES", Medium), ("No-Intro", Medium)]`）。
- [x] 命令行选得出与界面同一批（按**依据形状**）
      —— `romcat triage list / decide / undo` 都多了一个可重复的 `--shape`
      （`crates/cli/src/main.rs::TriageFilterArgs`）。认那串字的活**在核心库里**：
      `triage::batch::Shape::selector` / `Shape::parse` 那一对，命令行只负责把它塞进
      `Filter::shape`（ADR-0005）。报告多一张「按依据形状」表把每一批连它那串字一起印出来
      （`QueueReport::by_shape`），`triage list` 末尾再给一行整条可粘贴的 decide 命令，
      **这一趟的选择器一样不少地带上**——库选择器走 `TriageCommonArgs::选择器()`，
      队列选择器走新加的 `TriageFilterArgs::选择器()`（挂单 Q175、Q178：只带 `--shape`
      的话，那张表上的条数是筛过之后数出来的，而命令跑的是全库那一批）。
      端到端：`crates/cli/tests/triage.rs::报告印出来的那串字照抄一条就选中同一批`
      ——从报告 JSON 里逐条抄 `selector` 跑回去，`selected` 与那一批的 `count` 一个数都不差
      （2 条那批与 1 条那批，加起来正是队列的 3 条）；
      `依据形状写岔了当场说清该怎么写` 钉住写岔了不是静悄悄选中零条；同一条测试里另跑一趟
      `--name 甲`，断言印出来的命令里有 `--name '甲'`。
      段界那条认法**只有头一段没有闭合词表兜底**，所以另有一条
      `crates/core/src/triage/batch.rs::tests::源那一段里不许出现分隔符` 把前提钉在真的
      源名上（内置数据源清单 ＋ 代码里写死的那四个），哪天有人加一个带 ` / ` 的源名当场红
      （挂单 Q180）。
- [x] 界面与命令行选出来的是同一批，有测试对照
      —— `crates/gui/tests/queue.rs::界面点开的那一批与命令行按同一串字选出来的一条不差`：
      屏上**逐批**点开（合成数据分出 4 批以上，两支都有），拿 `Queue::members(&scope)`
      收下界面这一侧盖住的键；再把同一个形状折成那串字、`Shape::parse` 认回来、走
      `triage::survey` 从中立库重折一遍队列，两个键列表必须一模一样。
      **不是空对空**：每一批都断言 `!界面这批.is_empty()`。
- [x] 门禁全绿（`--all-features`）
      —— `cargo clippy --workspace --all-targets --all-features` 干净；
      `cargo test --workspace --all-features -- --test-threads=2`：
      **58 个 `test result: ok`、1,581 条通过、0 失败**（基线 1,571 条 ＋ 这一票新增 10 条）。


## 挂单裁决

从 `.scratch/PARKING-LOT.md` 迁来。那份挂单是这一轮队列的**待办队列**，收尾时清空；
裁决之后条目迁回它所属的票，编号 `Qn` 留着占号、不复用。

### Q82 — 一级分批的键取「第一条候选」，不取「置信度最高的那条」

- **来自：** 票 `gui-redesign/09`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/triage/batch.rs::Shape::of`
- **为什么没停线：** 两条路里只有一条自洽，而它就是这张票要的那条。
- **这张票实际走了哪条路：** 取 **`candidates[0]`**。理由是「整批通过」在领域里就是
  `DecisionSpec::Pick(1)`——它采用的正是第一条。取「置信度最高的那条」的话，卡片上那句
  共同依据说的是 A、按下去做的是 B，而**整批通过时人验证的正是那句话**，那句话就成了假的。
- **它挂着的那条线：** 候选从中立库出来是**按写入顺序**（`candidates_of` 的
  `ORDER BY id`），也就是识别当初产出它们的顺序。眼下第一条实际上就是最可信的那条
  （`identify::rank` 排过），但**没有一条测试钉住这件事**。哪天识别改了写入顺序，
  分批的键会跟着变，而没人会发现。
- **顺带：** 「**按依据形状**」这个选择器只有界面上点得到——`Filter::shape` 在命令行上
  没有对应开关（`crates/cli/src/main.rs::TriageFilterArgs::build` 那里留了注释）。
  它要一串「源 / DAT / 置信度 / 哈希口径 / 候选数」的写法，那是另一张票的事。
- **谁来裁：** 收尾——要么给「候选按可信程度排」补一条测试，要么开一张票让命令行也能按形状选。
- **状态：** 本票做完了 —— **两件都做了**，不是二选一。测试三条（见验收第 1 条，
  变异实测两次都红过）；`--shape` 落在 `TriageFilterArgs` 上，那串字的折算在核心库里
  （`Shape::selector` / `Shape::parse`），报告与 `triage list` 末尾把它印出来给人抄。
  这条挂单**从这里起不再 open**。
- **收尾裁决：** **由本票接着做** —— 它就是从这条挂单立起来的。


## 挂单裁决（第二轮）

从 `.scratch/PARKING-LOT.md` 迁来；编号 `Qn` 留着占号、不复用。

### Q173 — 「可信程度」那一层在**队列里**钉不住：高置信的早自动通过走了

- **来自：** 票 `queue-followups/08`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/identify.rs::rank`；`crates/core/src/triage.rs::in_queue`
  （`row.accepted == 0`）
- **为什么没停线：** 票要的事（钉住「候选按可信程度排」）做得下来，只是钉不成一条测试
  ——待确认队列里**看不见高置信**：DAT 命中的候选 `confidence: High` 与 `accepted: true`
  是同一件事（`identify::candidate_of`），而自动通过的一律不进队列。剩下的中置信与低置信
  里，源的先后与可信程度**从不冲突**（低置信只出自模糊匹配与模型推断，那两个源本来就排
  在最后），拿它们比出来的次序两种排法给出同一个答案——那是一条恒真的断言。
- **这张票实际走了哪条路：** **三条测试分工**，各钉一段：
  1. `identify::tests::候选先按可信程度排再论平台与源的先后`（单元，不走库）——直接拿
     `rank` 比，可信程度、平台对不对得上、源的先后三层各有一对反着摆的候选。
  2. `tests/identify.rs::中立库交回候选的次序就是按可信程度排的那一个`——走完整一趟识别
     再从中立库读回来：**高置信那条出自 GoodNES（源排第五）、中置信那条出自 No-Intro
     （源排头一个）**，而且中置信那份 DAT **先写进去**。两种排法给出相反的答案。
  3. `tests/triage.rs::一级分批的键取的是最可信的那条候选`——在**队列里**钉分批的键与
     第一条候选是同一个，并把那一批折成 `--shape` 再选一遍。
  变异实测两次：把 `rank` 的置信度那一层拿掉，第 1、2 条当场红；把整条 `sort_by` 拿掉
  （Q82 说的「旧写法：写入顺序」），第 2、3 条当场红。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q174 — `--shape` 那串字的语法：段序照票走，段界**不靠数分隔符**

- **来自：** 票 `queue-followups/08`
- **类别：** 规格没说
- **在哪：** `crates/core/src/triage/batch.rs::Shape::selector` / `Shape::parse`
- **为什么没停线：** 两处对这串字的段序说法不一样，而两种都自洽——挂单 Q82 写的是
  「源 / DAT / **置信度 / 哈希口径** / 候选数」，`crates/gui/src/queue.rs::Picks::shape`
  那条注释举的例子是「MAME / gameboy.xml / **含头 / 中置信** / 单候选」。
- **这张票实际走了哪条路：** **照 Q82 的段序**（置信度在前），理由是它是这张票的正文；
  顺手把 GUI 那条注释里的例子改成同一个段序，并写明命令行那一侧收得下但走的是「照着抄」
  而不是手打。另外定了两条：
  - **段界不靠数分隔符。** DAT 的名字与那句「为什么没定下来」都可能自带 `/`，而认错一段
    的后果不是报错、是**静悄悄选中另一批**。所以源取头一段、置信度 / 哈希口径 / 候选数
    取**末三段**（三个都是闭合词表），中间原样拼回去当 DAT；理由那一支第三段往后整个是
    理由。有一条单元测试专门喂带 `/` 的 DAT 名与理由。
  - **`没有候选` 那一档当场报错**，不当成一个选中零条的形状：屏上根本折不出这串字，
    写出来只可能是手打错了。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q175 — 光有开关没用：报告不印出来，命令行上根本敲不出一串合法的依据形状

- **来自：** 票 `queue-followups/08`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/triage/report.rs`（`ShapeRow`、`by_shape`、`shape_table`）；
  `crates/cli/src/main.rs::run_triage_list` 末尾那一行
- **为什么没停线：** 票只说「命令行选得出与界面同一批」，加一张表不与它相悖。但只加开关的话
  「命令行选得出」只在**旁边开着界面**时成立——那五段里有 DAT 全名与哈希口径，没人凭空
  打得出来。三个轴那几张表早就是这么办的（`Axis::selector` 印在表头上，标签就是开关的值）。
- **这张票实际走了哪条路：** 报告多一张 **按依据形状** 表，每一批两行：一行是条数、占比与
  那句共同依据，下一行是 `--shape '…'`（**加单引号**，那串字里有空格）。分批走的是与屏上
  同一个函数（`batch::batches`），所以两边是同一批。另外在 `triage list` 末尾印一行**整条
  可粘贴的命令**——挑的是最大的那一批里**按批答得了的**（`Batch::passable`，`--pick 1`
  只在候选恰好一条时说得成立），并拿 `TriageCommonArgs::选择器()` 把这一趟用的库选择器
  带上，不带的话粘到别处开的是另一份库。端到端那条测试照抄报告 JSON 里的 `selector`
  跑回去，选中的条数与报告上写的那一批一个数都不差。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q176 — `triage/batch.rs` 模块文档里「词表两个都没收」那句已经过时

- **来自：** 票 `queue-followups/08`
- **类别：** 路过发现，不在范围内
- **在哪：** `crates/core/src/triage/batch.rs` 模块文档「批这个词在这个 crate 里有两个意思」
  那一节末句
- **为什么没停线：** 一句文档。但它写着「词表眼下两个都没收（挂单 Q77、Q80）」，而词表
  在 `1dfd13d` 已经把**批**与**一批变体**都收了——留着会把下一个人骗去重开一条挂单。
- **这张票实际走了哪条路：** 就地改成指向 `CONTEXT.md` 的「批」那一条（连「一批变体」
  这个叫法一起）。代码一行没动。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q178 — 报告末尾那条可粘贴命令原先只带 `--shape`，条数与它真选中的对不上

- **来自：** 票 `queue-followups/08`（`/code-review` 抓的第 1 条）
- **类别：** 工具缺陷（这一票自己写出来的）
- **在哪：** `crates/cli/src/main.rs::run_triage_list` 末尾那一行
- **为什么没停线：** 就地改掉了。
- **这张票实际走了哪条路：** 那张「按依据形状」表是从 `survey.items` 上算的，而
  `survey.items` **已经过这一趟的选择器筛过**。原先那一行只接了 `--shape` 与库选择器，
  于是 `romcat triage list --under 库/FC` 会印「从最值钱的那一批下手（120 条）」而命令
  跑的是**全库**那一批；`--state 跳过` 那一路更糟——报告印得出跳过那一批，抄走却因为
  默认三档里没有它而选中零条。**改法：给 `TriageFilterArgs` 也加一个 `选择器()`**
  （照 `TriageCommonArgs::选择器()` 的样子），把 `--under` / `--platform` / `--state` /
  `--name` / `--candidate-work` / `--key` 一样不少地折回去；`--shape` 那一段不带，
  它由报告上每一行自己给。端到端测试里加了一趟 `--name 甲`，断言印出来的命令里有
  `--name '甲'`。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q179 — 理由恰好是空串的那一批，折出来的 `--shape` 自己认不回来

- **来自：** 票 `queue-followups/08`（`/code-review` 抓的第 3 条）
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/triage/batch.rs::Shape::parse` 头一句 `text.trim()`
- **为什么没停线：** 够不到——理由的几个产地（`Skip::recorded()`、`读不动：{error}`、
  `blocked[0]`、`VERDICT_UNKNOWN_REASON`）一个都给不出空串；而且这条路失手的样子是
  **当场报「认不出结论」**，不是静悄悄选中另一批。
- **这张票实际走了哪条路：** **留着 `trim()`，把这条写进 `parse` 的文档**（那串字是从报告上
  抄来的，行尾多个空格是常事，剪掉两头的空白值这个价）。`Shape::Bare { reason: Some("") }`
  折出来的字末尾那个分隔符会被剪掉，认回来时报错。要真堵死，得让 `Shape::of` 把空串理由
  折成 `None`——那是改**一级分批的形状**，票 09 明说不许，所以没动。
- **谁来裁：** 收尾（真要堵，跟着「形状可不可以改」那件事一起裁）
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q180 — 段界那条认法只有**头一段**没有兜底：源名里出现 ` / ` 会静悄悄选中零条

- **来自：** 票 `queue-followups/08`（`/code-review` 抓的第 2 条）
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/core/src/triage/batch.rs::Shape::selector` / `Shape::parse`
- **为什么没停线：** 眼下的源名一个都不含 ` / `，而且加得上一道真的闸。
- **这张票实际走了哪条路：** 审查建议的「认完再折一遍比对」**堵不住这一条**——
  `selector(parse(x)) == x` 对错分法照样成立（源短一截、DAT 长一截，拼回去是同一串字），
  两个不同的形状映到同一串字，字面上分不出来。所以改成**把前提钉在数据上**：
  新增单元测试 `源那一段里不许出现分隔符`，遍历内置数据源清单
  （`dat::registry::Registry::builtin().sources()`）加代码里写死的那四个
  （`fuzzy::SOURCE`、`model::SOURCE`、`switch::SOURCE_TITLEDB`、`SOURCE_CONTAINER`），
  逐个断言不含 ` / `；同时把这条前提写进 `selector()` 的文档。哪天有人往
  `sources.toml` 里加一个带 ` / ` 的源名，这条测试当场红。DAT 与理由不受约束——
  中间那一段是整取的，自带 `/` 照样认得回来（有测试）。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。
