# 06 — 匹配裁决接进待确认屏

**What to build:** 在**待确认屏**上看得出「这几个字段来自同一次匹配」（同一个条目号），并就地下**肯定**或**否定**裁决——一次裁决管住那一次匹配带来的全部字段。

眼下这套东西领域那一半全在核心库（按条目号归堆、落裁决、就地清库），入口却只有命令行两条命令。用户在浏览屏上看见一条误撞，得切到终端、把变体键拷过去才裁得了。

**界面只画和转发**（ADR-0005），一条领域逻辑都不用写。**撤销走既有的那条按批撤销的路**，别另造一套。

**Blocked by:** 无 —— 可立即开工

**Status:** done

- [x] 待确认屏上看得出哪几个字段来自同一次匹配，堆上写着条目号与依据

  逐条那一屏的详情面板底下多了一块「中文离线源那几次匹配」：一堆一个**条目号**，
  堆上写着还等着裁还是已裁过、几个字段一条裁决全管，每条值底下跟着它那一整句**依据**
  （核心库那一侧多交一栏 `MatchedValue::evidence`，挂单 Q165）。归堆一个字都没在界面里
  算——`scrape::zh::matched_groups` 交什么就画什么。
  `gui/tests/queue.rs::待确认屏上看得出哪几个字段来自同一次匹配`：断言的是**这一帧真的
  画出来的字**（`Galley`），钉住条目号 `12345`、那一整句依据、变体层的别名「幻想傳說」、
  作品层的类型「角色扮演」、五个字段名与那两颗按钮。**变异实测**：把依据从那行
  `format!` 里拿掉，这条当场红。一堆六个字段一屏摆不下，所以断言收的是**滚一趟真滚轮
  事件**画出来的那几帧（挂单 Q169）。

- [x] 就地下**否定**裁决，同一次匹配带来的全部字段一并失效

  `gui/tests/queue.rs::就地下一次否定裁决同一次匹配带来的全部字段一并失效`：
  按下之后 `Judged::cleared >= 6`、`cleared_work > 0`，`matched_groups` 在**那个变体**
  与**名下另一个变体**上都读回空（作品那一层一并清了）；沉淀库里那一条 `accepted` 为假、
  条目号对得上、**备注跟着那一下记进去了**。屏上那一堆当场没了、那句账留着
  （否则人按完什么都看不见）。清库一行都没写在界面里——`scrape::zh::judge` 干的。

- [x] 就地下**肯定**裁决，下一趟不被分数更高的候选顶掉

  `gui/tests/queue.rs::就地下一次肯定裁决那一堆的值一个字都不清`：`cleared == 0`、
  `matched_groups` 与裁之前**逐条相等**，沉淀库里那一条 `accepted` 为真、源是
  `中文离线源`、条目号对得上。
  **保留**：「下一趟不被顶掉」那一半的判据在核心库，这一票没有重跑一趟刮削去验它——
  钉住它的是既有测试 `core/src/scrape/zh.rs::人说过的那一条排在机器挑的前面` 与
  `作品那一层的章盖在人裁过的那个变体上而不是分数最高的那个`（票
  `offline-chinese-fields/05` 的 Q34 / Q41）。界面这一侧证到「落下的正是那条肯定裁决」为止。
  另外：屏上那一堆**当场不改口**（那句「已由人裁决确认」在依据里，下一趟刮削才改写），
  账那一行明说了这件事——挂单 Q167。

- [x] 界面与命令行走**同一条**裁决路径，两边结果一模一样

  两边调的**就是同一个函数** `scrape::zh::judge`（命令行那侧是 `run_zh_judge`，
  `crates/cli/src/main.rs`）。`gui/tests/queue.rs::界面与命令行裁同一条匹配落下的东西
  一模一样`：同一份合成数据、同一条备注，一边按界面那颗按钮、一边照命令行那样直接调，
  比三样——交回来的 `Judged` 相等、清完之后作品那一层剩下的东西相等、沉淀库里
  `all_matches()`（抹掉时刻）相等。

- [x] 裁决落**沉淀库**、锚在**内容锚**上，删掉中立库重扫也不丢

  上面两条测试都断言 `Judged::anchor` 是 `Anchor::Content` 且 `is_shareable`。
  为此给合成数据里那两个变体补了 `entry` 行——`identify::content_print` 先问
  `entry_fact`，没有那一行就退到路径锚（挂单 Q168）。
  **保留**：「删掉中立库重扫也不丢」是核心库那条路的既有行为，钉住它的是
  `core/tests/scrape.rs::裁决记的是内容锚换台机器与改过名字之后仍然认得出`；
  界面这一侧只证到「锚是内容锚、可导出分享」。

- [x] 撤销走既有那条按批撤销的路

  **一行撤销代码都没写。**「撤回第 N 批」（票 `gui-redesign/08`）一个字没动。
  而那条路撤的是一**批识别裁决**，匹配裁决落的是沉淀库里另一张表（挂单 Q33 定的形状），
  `triage::apply` 从来不碰它——所以这一票**不假装它撤得掉**：账那一行明说它不在那条
  路上，并给出真正的走回路（再裁一次覆盖前一条；清掉的值下一趟刮削采得回来）。
  `gui/tests/queue.rs::匹配裁决不另起一条撤销路` 钉住三样：裁完一批都没建、屏上不冒出
  撤回按钮、再裁一次是覆盖而不是攒出第二条。这条取舍记在**挂单 Q164**。

- [x] `crates/gui` 里没有新长出领域逻辑

  界面这一侧新增的代码只做三件事：把 `matched_groups` 交来的那几堆画出来、把按下的
  那一下转给 `judge`、把交回来的 `Judged` 折成一句话。三个判断词全部来自核心库
  （`MatchGroup::confirmed`、`MatchGroup::from_variant`、`Judged` 那几栏），
  界面里**一次都没有**自己按条目号归堆、自己判该清什么、自己折内容锚。
  唯一动到核心库的是给 `MatchedValue` 补一栏 `evidence`（挂单 Q165）——那正是为了
  不让界面自己再去库里捞一遍、再认一遍条目号。

- [x] 门禁全绿（`--all-features`）

  `cargo clippy --workspace --all-targets --all-features -j 1` → `Finished`；
  `cargo test --workspace --all-features -j 1 --no-run` → `Finished`；
  `cargo test --workspace --all-features -j 1 -- --test-threads=2` → **58 个
  `test result: ok`、1,571 条通过、0 失败**（基线 58 / 1,563，新增 8 条）。


## Comments

**`/code-review high` 报的 5 条全部就地处理**（没有一条留成挂单）：

1. **中 · 验收第 4 条那条比较是空断言**（`gui/tests/queue.rs`）——`matched_groups` 收的是
   **变体键**，我把**作品名**递了进去，于是两边都读回空 vec，恒真。改成直接读作品锚点上
   剩下的行（`scraped_values`），**并先钉住裁之前那几栏不是空的**，再比两边清完之后
   剩下什么；顺带比了那个变体身上剩下的东西，并断言账里 `cleared >= 6`、`cleared_work > 0`
   ——「比的得是真清过的那一趟」。
2. **中 · 合成的那一次匹配够不着中置信**（`gui/src/demo.rs`）——`year: Check::Unknown`
   加 `exact: false` 时 `zh::Match::strong` 为假，`ChineseSource::hit` 真跑一趟会当场跳过它：
   这份合成数据代表的匹配在真库里一个字段都不会产出，而屏上就写在它旁边的那句
   「中置信，不自动通过」于是成了假话。改成 `year: Check::Agrees`（条目本来就写着 1995），
   并把那一次匹配抽成 `那一次匹配()` 加一条单元测试
   `demo::tests::合成的那一次中文匹配够得着中置信` 钉住它，顺带钉住依据里那句
   「一律进待确认队列」还在——**置信度口径一个字都没放松**。
3. **低 · 账那句收尾话无条件贴**（`gui/src/queue.rs::judged_text`）——肯定那一档
   `cleared == 0`，前一句刚说「一个字都不清」，后一句却说「清掉的值下一趟采得回来」；
   而否定那一档它与按钮说明（「重跑刮削也不会再撞回这条条目」）读起来相反。按档分开说：
   肯定那一支到「后一条盖掉前一条」为止，否定那一支把前提说全——
   「清掉的那些值要回来，得先改判成『就是这条』，再跑一趟 `romcat scrape`」。
4. **低 · `--queue-rows` 小时作品名与文件名对不上**（`gui/src/demo.rs`）——作品名硬写成
   `QUEUE_WORKS[0]`，而 `--demo --queue-rows 50` 上头两个够格的变体根本不是 n = 0、1
   （命中那一档按比例缩到零）。改成**从入选那条的下标反推**，并要求那两个**挨着**且
   落在同一部作品底下（`records.windows(2)`）。回归测试
   `队列缩到很小时那一堆的作品名照旧对得上文件名` 走 50 / 200 / 2,000 三个规模，
   **变异实测**：改回硬写第 0 个，它当场红。
5. **低 · 读不动时每帧重查一次，还把别的错误盖死**（`gui/src/queue.rs::refresh_matches`）
   ——失败分支原先把缓存置 `None`，于是下一帧再问一次注定失败的库，那句「中立库读不动」
   把屏上别的话（「裁不下去：……」）每帧盖掉一次。改成**失败也缓下来**（缓成空的），
   重试跟着光标与版号走、不跟着帧走。

另外把审查顺带指出的两处**写进文档**（不是行为改动）：`refresh_matches` 的缓存钥匙里那个
版号**不是单调的**（`Queue::reload` 会把它归零再推回 1），这份缓存作数靠的是「按批裁决那
条路一个字都不碰刮削那张表」；以及 `judged` 那本账只有「重新列队列」才清，撤回 / 放回 /
逐条裁那三条路不清它——账仍旧是真的，所以留着比抹掉好。


## 挂单裁决

从 `.scratch/PARKING-LOT.md` 迁来。那份挂单是这一轮队列的**待办队列**，收尾时清空；
裁决之后条目迁回它所属的票，编号 `Qn` 留着占号、不复用。

### Q39 — 入口给在命令行（`romcat zh matches` / `judge`），界面那一侧一行都没动

- **来自：** 票 `offline-chinese-fields/05`
- **类别：** 规格没说
- **在哪：** `crates/cli/src/main.rs` 的 `ZhCommand::Matches` / `ZhCommand::Judge`
- **为什么没停线：** 票的验收说「**队列里**一个变体的待确认项看得出……」「在队列里下一次裁决」，
  而**界面正在被另一条队列整个重排**（`gui-redesign` 12 张票，`585c17f` 已经把主库改成一组根）。
  在旧屏幕上长一块这一票的 UI，下一张票就要拆掉。
- **这张票实际做了什么：** 领域那一半**全落在核心库**——`scrape::zh::matched_groups`
  （按条目号归堆，队列「看得出」靠它）与 `scrape::zh::judge`（落裁决 + 就地清库）；
  命令行给两条命令当入口。界面接不接、接在哪一屏，留给 `gui-redesign` 那条线——
  它接的时候一条领域逻辑都不用写（ADR-0005：GUI 只是窗口壳）。
- **谁来裁：** 拿主意的人（或 `gui-redesign` 那条线上的某一张票）
- **状态：** open
- **收尾裁决：** **由本票接着做** —— 它就是从这条挂单立起来的。
  **做完了**：入口落在待确认屏**逐条**那一屏的详情面板上（分批那一屏一张卡片管着上千个
  变体，而匹配裁决钉在**一个变体的内容**上）；界面一条领域逻辑都没长
  （见上面第 7 条验收）。浏览屏那一侧仍旧只有命令行——记在**挂单 Q166**。


## 挂单裁决（第二轮）

从 `.scratch/PARKING-LOT.md` 迁来；编号 `Qn` 留着占号、不复用。

### Q164 — 匹配裁决**不进「批」**，按批撤销那条路管不到它；走回来那一下是「再裁一次」

- **来自：** 票 `queue-followups/06`
- **类别：** 规格没说
- **在哪：** `crates/gui/src/queue.rs` 的 `judged_text` 那句收尾话；核心库那一侧是
  `scrape::zh::judge`（写 `verdict::Store::put_match`）与 `triage::apply`
  （写 `put_batch`）——**两条路一次都没交汇**
- **为什么没停线：** 票写着「撤销走既有那条按批撤销的路，别另造一套」。**照做了**：
  这一票一行撤销代码都没写。但那条路（票 `gui-redesign/08`）撤的是一**批识别裁决**
  ——`verdict` 表加中立库那份快照；匹配裁决落的是**另一张表**（`match_verdict`，
  挂单 Q33 定的形状），`triage::apply` 从来不碰它。要让它也按批撤，得先把匹配裁决
  纳进批——那是核心库的形状改动，正是这张票禁止的「另造一套」。
- **这张票实际做了什么：** **不另造，也不假装它撤得掉。** 界面上那颗「撤回第 N 批」
  一个字都没动，只管识别那一批；匹配裁决落下之后那一行账**明说**它不在那条路上，
  并给出真正的走回路：**再裁一次**（同一条锚上 `put_match` 后一条盖掉前一条，
  `Judged::fresh` 为假），否定时清掉的值**下一趟 `romcat scrape` 采得回来**
  （人裁过之后那条条目在候选里排最前，Q34）。测试
  `gui/tests/queue.rs::匹配裁决不另起一条撤销路` 钉住三样：裁完沉淀库里一批都没建、
  屏上不冒出撤回按钮、再裁一次是覆盖而不是攒出第二条。
- **谁来裁：** 拿主意的人（要它也能按批撤，那是一张新票：把匹配裁决纳进 `verdict_batch`）
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q165 — 归堆那一侧多交一栏**依据**：`MatchedValue.evidence`

- **来自：** 票 `queue-followups/06`
- **类别：** 规格没说
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `MatchedValue`（新增 `evidence` 一栏）
- **为什么没停线：** 验收第 1 条要「堆上写着条目号与**依据**」，而 `matched_groups`
  原先只交出锚点、字段、值、源——依据那句话留在库里没带出来。
- **这张票实际做了什么：** **在核心库那一侧补一栏**，值原样从 `ScrapedValue::evidence`
  搬过来（`matched_groups` 本来就为了取条目号读着它）。**没有让界面自己再去库里捞一遍**
  ——捞回来还得再按条目号认一遍谁属于这一堆，而那正是归堆这件事本身（ADR-0005：
  第二处认法长在界面里，迟早与核心库那一处漂开）。命令行那两条命令一个字都没改，
  它照旧不印依据（`romcat zh matches` 一屏要摆好几堆，整句依据会把它冲垮）。
- **谁来裁：** 收尾
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q166 — 界面上这个入口只在**逐条**那一屏；浏览屏上看见误撞，仍旧得先切过来

- **来自：** 票 `queue-followups/06`
- **类别：** 票写错了（正文与验收对不齐）
- **在哪：** 票 `.scratch/queue-followups/issues/06-match-verdict-in-triage-screen.md`
  第 5 行说「用户在**浏览屏**上看见一条误撞」，而八条验收从头到尾说的都是**待确认屏**
- **为什么没停线：** 验收是判据，正文那句是背景。而且两者不矛盾——看见与裁决本来就
  可以在两屏：**误撞看得见**是在浏览屏（那儿摆着刮削字段值与它们的来源），
  **裁得动**这一票接在待确认屏的详情面板上（那一屏本来就是「一次看一个变体」的地方，
  而匹配裁决的粒度正是一个变体）。
- **这张票实际做了什么：** **只做待确认屏那一侧**，而且只在**逐条**那一屏——分批那一屏
  一张卡片管着上千个变体，匹配裁决钉在**一个变体的内容**上，摆在卡片上就是给整批人
  按一颗只对一个人生效的按钮。浏览屏那一侧一行没动：用户在那儿看见误撞，要么切到
  待确认屏（那个变体还在队列里的话），要么照旧走 `romcat zh judge`。
- **谁来裁：** 拿主意的人（要把同一块接进浏览屏的详情面板，那是一张新票——
  那一屏还能裁**已经识别完、早就不在队列里**的变体，这一票够不着它们）
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q167 — **肯定**那一档屏上那一堆不当场改口：那句「已由人裁决确认」在**依据**里，下一趟刮削才改写

- **来自：** 票 `queue-followups/06`
- **类别：** 票没想到的第三种情况
- **在哪：** `crates/gui/src/queue.rs` 的 `judged_text` 里肯定那一支；核心库那一侧是
  `zh::Match::evidence_confirmed`（由下一趟 `scrape` 重新采集时写进依据）
- **为什么没停线：** 这是既有行为，票的硬约束里写着「肯定那一档一个字都不清」。
  `MatchGroup::confirmed` 是**从依据那句话里读回来的**（`zh::is_confirmed`），而依据
  要等下一趟刮削才改写——于是按完「就是这条」，屏上那一堆照旧写着「还等着裁」。
- **这张票实际做了什么：** **不改行为，改的是把这件事说出口。** 账那一行明写
  「屏上那一堆仍写着『还等着裁』——那句话在依据里，下一趟 `romcat scrape` 才改写」，
  测试 `就地下一次肯定裁决那一堆的值一个字都不清` 钉着这句话在屏上。
  **没有让界面自己去沉淀库问一句「这条裁过没有」**：那要界面自己折出内容锚
  （`identify::content_print`）再去 `find_match`，是把领域判断搬进 `crates/gui`。
- **谁来裁：** 拿主意的人（要屏上当场改口，正路是让 `matched_groups` 顺带把沉淀库
  那一侧的态一起交出来——核心库的活，一张新票）
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

### Q169 — 那一堆一屏摆不下：断言靠**真的滚轮事件**滚一趟才看得见

- **来自：** 票 `queue-followups/06`
- **类别：** 取舍
- **在哪：** `crates/gui/tests/queue.rs` 的 `详情滚一趟`；面板高度在
  `crates/gui/src/layout.rs` 的 `DECIDE`（默认 268 点）
- **为什么没停线：** 一堆匹配是六个字段、每条各带一整句依据，而详情那一栏默认
  268 点高——摆不下是必然的，egui 也不画视口之外的东西。挂单 Q20 记过同一件事
  （浏览屏那条 4,000 字的简介），那次是绕开屏幕改测单元函数；这次没绕。
- **这张票实际做了什么：** **测试滚真的滚轮**：指针停在那块面板里，
  连发十二次 `MouseWheel`，每一帧收那一帧真的画出来的字（`表格里画多少行文本输入框
  都是那几个` 也是这么滚一趟的）。**断言的仍旧是画出来的那一帧**，不是「库里有没有」。
  代价是这几条测试证的是「滚到那儿看得见」，不是「打开就看得见」——而人要看这一堆
  本来就得先把光标停到那一条上，滚一下是同一个动作里的事。
- **谁来裁：** 拿主意的人（要「打开就看得见」，那是版式的活：给这一堆一块自己的地方，
  或者把每一堆折起来只留标题——一张新票）
- **状态：** open
- **收尾裁决（第二轮）：** **settled** —— 这条的决定已经落进代码，并有测试钉着。

---

## 挂单裁决（第三轮）

从 `.scratch/PARKING-LOT.md` 第三轮收口迁来。**本票的验收正文一个字没动。**

### Q168 — 合成数据里只有那两个变体有 `entry` 行

- **条目正文在** `.scratch/PARKING-LOT.md` 第三轮收口的已收索引表（去处：票 `parking-3/17`）。
- **收尾裁决（第三轮）：settled** —— 由票 `parking-3/17` 落地。
  本票当初为了让那一堆中文字段裁得出内容锚，只给中文离线源撞上的那**两个**变体补了
  `entry` 行；代价是 `--demo` 打开的队列里其余一万六千多条详情**每一条**都写着
  「裁决钉在：路径——只在本机成立」。现在 `demo::queue_with_zh` 给**每个**变体都写
  `entry` 行，于是拿得到内容判据的那些（除「无判据」那一档外的全部）折得出内容锚。
- **本票那两条断言的意思没变，力度变了**：`Judged::anchor` 是 `Anchor::Content` 这件事
  从前是「全库唯一的两个特例」，现在是「常态里的一条」——那两个变体不再靠一份专门为
  它们摆的数据才成立。
- **数变了：** 界面上「钉在内容上的多少条」从 **0** 变成 **12,119**
  （16,656 减去无判据那 4,537 条——那一档拿不到内容判据，只钉得住本机路径，
  那正是它落进那一档的原因）。
- **钉着它的测试：**
  - `crates/gui/tests/queue.rs::拿得到判据的那些钉在内容上而不是每一条都退到路径`
    ——两种锚都在屏上真的画出来过。
  - `crates/gui/tests/queue.rs::裁决即时写进沉淀库并从队列移除` 补了两条断言：
    `[ACG汉化组]` 那 129 条整批 `content_anchored`、`path_anchored` 为 0
    （补 `entry` 行之前是 2 与 127）。
