# 04 — 剥离规则进变体层与作品层的输入指纹

**What to build:** 改过文件名**剥离规则**之后重跑刮削，**真的重采**。

剥离规则决定从文件名里剥出什么**正题**，正题变了撞出来的中文条目就可能变。可两层的**输入指纹**都没盖住它，缓存一口咬定"输入没变"，整条跳过——用户调完规则重跑，什么都不会重采，而且不报错。

**这与已经做掉的那件事不是同一件。** 另一条线刚把**建索引那一刻的平台折叠表**盖进了指纹（治的是"补一条平台别名重建之后不重采"），那条提交自己写着"规则集的指纹因此不必加"。剥离规则改的是**正题**，那一路照旧漏。

**Blocked by:** 无 —— 可立即开工

**Status:** done

- [x] 规则集算得出一个指纹
      —— `Rules::fingerprint()`（`crates/core/src/filename.rs`）：FNV-1a 折的是**编译完
      那份规则的语义**，不是文件的字节。测试 `filename::tests::补一条剥离规则指纹就变`
      （补一条正题噪音词指纹就变，且钉住剥出来的正题真的从 `合金弹头7甲组特供版` 变成
      `合金弹头7`——不然那句只是在比两个字符串）。
- [x] **变体层**与**作品层**的采集记录都盖住它
      —— `scrape::zh` 的 `variant_probe` 与 `work_probe` 各多一行。测试
      `scrape::zh::tests::剥离规则进两层的输入指纹`：两层各一条 `assert_ne`。
      **别名那一路跟着盖住**：`ChineseAliasSource::probe` 转发 `inner.probe`
      （测试 `别名那一路不跟到作品那一层去` 照旧绿）。
- [x] 改过剥离规则重跑，撞上过的锚点**真的重采**而不是被缓存跳过
      —— 集成测试 `补一条剥离规则之后刮削重采而不是整片复用旧记录`
      （`crates/core/tests/scrape.rs`）：**同一份索引，只有规则换了**（dump、取的字段、
      折出来的那张表、匹配参数一个字都没变），变体锚点的标题从 `魂斗罗` 重采成
      `斗罗大陆`、作品锚点的类型从 `ACT` 重采成 `RPG`。
- [x] 规则没改时照旧整片跳过——加这道判据不让每一趟刮削都从头来
      —— 同一条集成测试第四段：规则一个字没改的第三趟 `reused_probes` **大于**第二趟。
      单元测试那一侧另有两条 `assert_eq`（同一份规则、两层都是同一个指纹）。
- [x] 指纹只认**会改变正题的那部分**：给规则文件加一行注释不触发全库重采
      —— `filename::tests::指纹算的是规则的语义不是文件的字节`：注释、空行、`版本模式`
      表的次序、以及 `V#*` 与 `v#*` 这种大小写写法都不改指纹（那张表只做「有没有一条
      对得上」的判断，而模式的字面量本来就大小写不敏感）。另有 `中文源平台别名不进这个指纹`
      ——那张表只在建索引时用得着，它对刮削的全部影响由 `zh::Index::platform_fold` 盖着
      （挂单 Q137 记着这个口径）。反过来那一半由 `一条词恰好等于下一张表的表名也分得开`
      钉着：表名与内容在指纹里用**不同的收尾符**，不然归类不同的两份规则会撞回同一串。
- [x] 门禁全绿（`--all-features`）
      —— clippy `--workspace --all-targets --all-features`：0 条。
      `cargo test --workspace --all-features -- --test-threads=2`：58 个 `test result: ok`、
      **1,536 条通过、0 失败**。（交接里写的基线是 1,543，实测口径对不上，挂单 Q138。）
      `/code-review` 报的四条低危全部改掉：表名与内容的收尾符分开、模式表折小写、
      指纹改成编译时算一次存在 `Rules` 上（一趟刮削要问它十万次量级）、一处注释方向写反。


## 挂单裁决

从 `.scratch/PARKING-LOT.md` 迁来。那份挂单是这一轮队列的**待办队列**，收尾时清空；
裁决之后条目迁回它所属的票，编号 `Qn` 留着占号、不复用。

### Q16 — 中文离线源的输入指纹从来没盖住**剥离规则**

- **来自：** 票 `offline-chinese-fields/02`（复审的产出）
- **类别：** 路过发现，不在范围内（票 11 就有的洞）
- **在哪：** `crates/core/src/scrape/zh.rs` 的 `variant_probe` 与 `work_probe`；
  `crates/core/src/filename.rs` 的 `Rules`
- **为什么没停线：** 两个指纹都盖了 dump、这一版取了哪几样字段、匹配参数，唯独没盖
  `naming.rules`。而**剥离规则「是配置不是代码，用户能补充自己遇到的模式」**
  （`CONTEXT.md` 的「剥离规则」词条）——换一份规则会改变**正题**，进而改变撞出来的条目
  与类型，缓存却会一口咬定「输入没变」而整条跳过。这不是这张票引进来的：
  `variant_probe` 那份清单票 11 就是这么写的，`work_probe` 只是照抄了同一份。补它要先给
  `Rules` 加一个 `fingerprint()`，那是 `filename` 那个模块的公开表面，而这张票的风险面
  按规格只该在刮削那一侧。
- **这张票实际做了什么：** 没补，照抄了变体那一层现有的那份清单——两层一致，将来补
  `Rules::fingerprint()` 时两处一起加一行就行。**眼下的出口是 `--refresh`**
  （`Source::probe` 的文档已经把「盖不住这个源自己的解析逻辑」那一条写在那儿了，
  换规则与换解析逻辑是同一类）。
- **谁来裁：** 拿主意的人（该单开一张票：它同时影响识别那一层与刮削这一侧）
- **状态：** done（本票补上了 `Rules::fingerprint()`，两层各加一行）
- **收尾裁决：** **由本票接着做** —— 它就是从这条挂单立起来的。
