# 11: 多碟变体同步时生成 .m3u

**What to build:** 多碟变体**同步到卡上时**多生成一份 `.m3u` 播放列表，清单照管（移出子库时一起清掉）。导出到主库那一侧不生成（ADR-0004）。适配器能力里声明支不支持播放列表。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q1046`：见 `../grill.md` 里那一条。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 先写红测试（真盘同步那份测试）：一个两碟变体同步后卡上有 `.m3u`、内容按碟序列出两份，且进了清单
- [ ] 移出子库后 `.m3u` 跟着删掉
- [ ] 导出到主库不生成 `.m3u`
- [ ] 不支持播放列表的前端不生成
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
