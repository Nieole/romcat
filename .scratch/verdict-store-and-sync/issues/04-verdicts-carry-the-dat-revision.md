# 04: 裁决带上修订；卡带头版本号进依据

**What to build:** 合并作品、以及一切「定成这个发行版」的裁决，带上 DAT 的**修订**：沉淀库裁决多一格，导出格式升一版（旧版照旧导入得回）。合并之后详情页「版本」那一格照旧写着修订。
卡带头与光盘头里读出来的版本号**不接**「第几版」那条链，只作为修订的一份证据写进依据与详情（词表**第几版**条已补）。

⚠️ 动沉淀库的顺序迁移清单：与 01→02→03→04→08→12 这条链上的票串着做，不并行。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q1010`：见 `../grill.md` 里那一条。
收挂单 `Q994`：见 `../grill.md` 里那一条。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 先写红测试：一个撞上 `(Rev 1)` 的变体合并进另一个作品 → 详情页「版本」那一格仍是修订（今天退回「—」）
- [ ] 沉淀库导出新格式带修订；旧格式文件导入得回、修订为空
- [ ] 卡带头版本号出现在依据里（标明是头里读的），「版本」那一格不因它变化；没撞 DAT 的变体那一格照旧「—」
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
