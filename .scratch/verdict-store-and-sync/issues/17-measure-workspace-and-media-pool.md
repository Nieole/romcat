# 17: 占用量尺、补宽高时长、划掉自动检查开关

**What to build:** - 核心库立「量工作目录与媒体池各件多大」一面，设置屏改报字节（不再报条数）。
- 任务台上一趟活，把老库里入过池、缺宽高时长的媒体补上；与量尺复用同一次遍历，不放在开库路上。
- 设置屏删掉「每周自动检查更新」两颗开关（设计稿一起改），屏上那句现话留着。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q1064`：见 `../grill.md` 里那一条。
收挂单 `Q995`：见 `../grill.md` 里那一条。
收挂单 `Q1062`：见 `../grill.md` 里那一条。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 先写红测试（核心库）：造一个工作目录与媒体池 → 量尺报出的各件字节数对得上
- [ ] 缺宽高时长的媒体跑过那一趟之后补上；已有的不动
- [ ] 那一趟排在任务台上、按得停
- [ ] 设置屏报字节、没有自动检查开关（界面测试 + 截图基线重批）
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
