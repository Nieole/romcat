# 07: 手动例外按作品折，写入带时刻

**What to build:** 子库的手动例外按**作品**折一层：同作品同方向并成一行，行尾一颗撤整组，可展开看逐份。分组判断在核心库，不动表结构。
例外写入加一个带显式时刻的入口（照「标记已导出」那个带时刻入口的先例），截图夹具改用它，那两张基线不再随机器忙闲变。

规格：`../spec.md`；已裁定的形状与核查见 `../grill.md`。

收挂单 `Q973`：见 `../grill.md` 里那一条。
收挂单 `Q1014`：见 `../grill.md` 里那一条。

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] 先写红测试（核心库）：一个作品三份变体各落一条例外 → 分组那一问交出一组三份
- [ ] 界面测试：例外表一行一个作品；按行尾撤整组 → 三条都没了；展开看得到逐份
- [ ] 子库例外那两张截图基线用定值时刻，连跑三遍画得一样
- [ ] 例外弹层截图基线重批，拿主意的人点头
- [ ] 门禁全绿（本机 `sync_run` 那五条已知红除外，`Q479`）
