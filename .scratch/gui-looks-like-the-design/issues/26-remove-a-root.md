# 26 — 移除一个根

**What to build:** 维护者移除主库里的一个**根**，在按下去之前就知道代价：会从库里去掉多少
**变体**、多少**作品**会因此消失、哪个子库会少多少；也知道**裁决**、收藏与合集为什么留得住。
盘上的文件一个字节都不动。

**Blocked by:** 06

**Status:** done

⚠️ **变体的键是「根名 + 相对路径」**（词表**根**）：所以移除一个根只影响它自己那一支；
裁决锚在**内容锚**上，重新添加这个根之后仍旧有效——这两句要说给用户听。

- [x] 移除前逐条说明：去掉多少变体、多少作品消失、子库少多少、导出条目少多少
- [x] 说明裁决、收藏与合集保留，重新添加这个根即可恢复
- [x] 要勾选确认才能按下移除
- [x] 移除后根列表、左栏徽标与工序那几个数跟着变
- [x] 盘上的文件没有被读写以外的任何改动（测试断言目标目录快照不变）

## 门禁

`cargo xtask gate --throttle`，**六步全绿**，整套 **2,336 项全过、0 失败**（2 项 `ignored`
是本来就挂着的量级测量）——`slot-3-gl26-gate-3.log`：

| 步 | 用时 |
|---|---|
| fmt | 2s |
| glossary | 1s（扫新写的那几行，没撞上 `_Gate_` 的词） |
| check | 5s |
| clippy | 25s |
| test | 1,217s |
| doc | 7s（`RUSTDOCFLAGS="-D warnings"`） |

**截图门**：一共动了**四张**——
- `library/remove-root-{light,dark}`（新）：这一票的弹层；
- `library/scanned-{light,dark}`（重出）：那一行的按钮从「移除」改成「移除…」，版式一个像素没动。

**子库那一批一张都没动**（`git status crates/gui/tests/snapshots/sublibrary/` 是空的）：全仓三颗
危险按钮里，子库屏那两颗永远是能按的那一档，只有库屏「移除根」会禁用。改禁用样式之后整套
73 张里 **71 张一个像素没变、只有那 2 张变**（`slot-3-gl26-snap-danger-dry.log`）。

带更新出图之后**不带更新连跑两遍都绿**（73/73 × 2），门禁里那一趟也是 73/73。

## 顺带治好的一处既有毛病：暗色下按不动的危险按钮看着像能按

**拿主意的人 2026-09-20 定在这张票里一并改**（收尾自审时发现的，不是这一票引进的）。

稿上禁用只有一条规则——`.btn[disabled]{opacity:.45}`，**照背景调淡**，egui 也正是这么干的。
这条规则在浅色里成立、在暗色里不成立，因为两套主题的 `lo` 站在面板的**两侧**：

| | 令牌 `lo` | 与面板比 | 调淡一半之后（实测基线上量的） | 看着像 |
|---|---|---|---|---|
| 浅色 | `#B0392F` | 比白面板**暗** | `#D09693` 淡粉 | 按不动 ✓ |
| 暗色 | `#EC7C70` | 比深面板**亮** | `#834E4C` **仍是一整块实心色** | 能按 ✗ |

所以按不动那一档换的不是浓淡，是**份量**：实心的危险色底换成**危险色的浅底**（令牌 `lo-soft`）
加危险色的字——也就是 [`look::chip`] 那一档 `Tone::Bad` 的搭配。实测改后：

| | 改前 | 改后 | 面板底 |
|---|---|---|---|
| 浅色 | `#D09693` | `#F5EAEA` | `#FFFFFF` |
| 暗色 | `#834E4C` | `#291F22` | `#161A21` |

两套主题都从「一整块实心色」变成「贴着面板的浅底」，一眼分得出；字留在危险色那一族里，
人看得清眼下按不动的是哪一颗。egui 那层调淡照旧盖在上面。

**这一处只有一支实现**（`look::danger_button`，全窗口只有它回答「危险按钮什么颜色」）。
全仓三颗危险按钮：库屏「移除根」（这一票，会禁用）、子库屏「删除子库」与「移除这条规则」
（**永远是能按的那一档**，`Button::new` 默认 `enabled: true`）——所以**只有这一票那两张基线动了**，
整套 73 张里另外 71 张一个像素没变（`slot-3-gl26-snap-danger-dry.log`：71 passed / 2 failed）。

**两条测试钉着**：`look` 的 `按不动的危险按钮换成浅底而不是把实心底调淡`（颜色逐项取自令牌，
并断言能按与按不动**不许撞脸**）、`tests/dialog.rs` 的
`页脚上按不动的危险按钮真的按不下去_拨成能按就按得下去`（真发指针事件去点它）。
两条都做过「真的红」：把 `enabled` 忽略掉、把两档改成同一套颜色，各自当场炸。

## 拿主意的人裁过的四处（稿上没画或与稿有出入）

1. **算这笔账当场算，不排任务台**（编排者 2026-09-20）。真库上量到超过 1 秒就改排任务台，
   触发条件与量法记在挂单 `Q983`。
2. **零的那几档**（2026-09-20）：一部作品都不消失时第一条改说「浏览里一行都不会消失」；
   一台子库都不少、一条导出条目都不少时，那两条**整条不画**。另外**这个根还没扫过**时第一条
   直说「中立库里一条记录都没有」——两轴审查挑出来的，稿与前一处裁定都没盖到这一种。
3. **子库那一行数的是选择集**（2026-09-20），照稿；不数清单。理由与代价记在 `Q983` 同一条。
4. **移完那句回执照稿**（2026-09-20），末尾补「（沉淀库一条都没动）」保住词表的词。

票面 ⚠️ 那**两句都在屏上**：「裁决、收藏、合集按文件内容记录，会保留……」照稿；
「别的根**一条都不少**：变体的键是「根名 + 相对路径」……」是稿上没有、票面点名要的那一句
（两轴审查的 Spec 轴挑出来缺了它）。
