# 11 — 表格按列排序

**What to build:** 维护者点表头就能按作品名、平台、变体数、容量、年份排序，年份未知的
那些永远排在最后。

**Blocked by:** 09

**Status:** ready-for-agent

⚠️ **票面原先把这件事写成了从零造，那是错的**（实现时核出来，2026-09-21 编排者改）。
**票 09 与 `parking-3/11` 已经做完的**：核心库 `WorkOrder` 五档下推 `ORDER BY`、
表格五个可点表头、排序箭头（默认那一种排法不画箭头）、点第二次换方向、卡片工具条的
排序下拉写回同一个 `WorkQuery`。**本票真正缺的只有下面标着「新」的那三条。**

⚠️ **作品名按排序标题排**，不按显示标题——中文按编码排出来是乱的，词表**排序标题**
那一条正是为这件事立的。

- [ ] 五列都排得动（票 09 已有），点第二次换方向（票 09 已有），**〔新〕第三次回到默认顺序**
      ——⚠️ **「作品」那一列只有两态**：默认那一种排法就是它正着排，所以它的第三态与第一态
      是同一个（第一下就翻成倒着，第二下回默认）。这不是漏做，是这一列本来就没有第三种状态。
- [ ] **〔新〕作品名按排序标题排**；**〔新〕年份未知的在两个方向上都排最后**
- [ ] 卡片视图与表格共用同一个排序（票 09 已有：卡片工具条写的就是同一个 `query.order`）
- [ ] **〔新〕搜索时默认按匹配程度排，显式选了排序则以它为准**

收挂单 `Q802`：认不出作品的那一行屏上主栏画的是正题（票 09），按「作品」排却还按变体的键排。
**2026-09-21 拿主意的人裁定：物化一列。** `work` 表加一列 `sort_title`（走
`add_columns`，不动 `SCHEMA_VERSION`），折标题那一趟顺手算下来
（`title::write_sort_keys` → `title::choose` 的 `Chosen::sort`），
`ORDER BY COALESCE(work.sort_title, UPPER(COALESCE(work.name, variant.key)))`。
**票面原先那句「定排序标题时连这几行排什么一起定」的去向写得不对**：`Q802` 自己记的
是「谁来裁：收尾」；而真正卡住的也不是那几行怎么排，是**排序标题根本不落库、下推不到
`ORDER BY`**。认不出作品的那些行照旧排自己的键（第三层退路），与票 09 一致。

这一路引进两笔新账，各记一条挂单：**优先级表一改排序键就旧了、得重折一趟**（`Q1092`）、
**没折过标题的库退回作品名**（`Q1093`）。
