# 17 — 「疑似同一作品」的判断落在核心库

**What to build:** 维护者在浏览与作品详情里看到「这两个作品可能是同一个」的建议与**理由**，
可以直接合并，也可以说「不是同一个」让它以后不再提示。判断本身**只在核心库一处**。

**Blocked by:** 16

**Status:** done

⚠️ **ADR-0024：一个判断只有一处实现。** 界面只展示理由并发起合并——界面里自己比一遍
名字、比一遍年份，就是第二处判断，迟早与核心库那一处漂开。

判据的三类线索（设计稿里已经写出来的）：不同数据库对同一部作品的**命名差异**、
**平台与年份一致**、**中文离线源里两个名字指向同一条条目**。阈值由核心库定。

- [x] 判断在核心库一处实现，命令行与界面读同一处
  - 新模块 `crates/core/src/triage/same_work.rs`（与票 16 的 `triage::merge` 同一族：
    那一处答「怎么合」，这一处答「该不该合」）。入口两个、**实现一段**：
    `survey(catalog, store, library)` 与 `survey_apart(catalog, dismissed)`
    （后者给跑在后台那条线程上的库体检用，沉淀库那一半在排活那一下就读好）。
  - 命令行 `romcat triage same-work`（`crates/cli/src/main.rs::run_triage_same_work`）
    与界面 `crates/gui/src/browse/suspicion.rs` 读的是同一处。
  - 钉着它的是 `crates/gui/tests/suspicion.rs::建议卡上的理由逐条列出来_逐字就是核心库交回来的那几句`：
    把核心库当场算出来的那几句与**屏上画出来的字**逐句比。界面若自己长出一套说法，这一条当场红。
- [x] 建议带得出理由，屏上逐条列出来
  - `same_work::Clue`（三档）与 `Clue::sentence()`：那几句话长在核心库，界面只印。
  - 屏上走 `look::impact` 一条一行（浏览屏侧边详情与作品详情页概览左栏打头，照设计稿 `suggHTML`）。
  - 证据：基线 `crates/gui/tests/snapshots/browse/suspicion-{light,dark}.png`，加上面那条测试。
- [x] 「不是同一个」记下来，之后不再提示
  - ⚠️ **票面原来写的是「记为裁决」，那是错的**（原样报回，已就地改掉，挂单 `Q1127`）：
    它落的是**沉淀库**里自己那张表 `not_same_work`（迁移 9，键是主库标识加那一对作品名，
    排过序；撤掉不删行），**不是一条裁决**——`Decision` 三档都是内容级/变体级的，装不下
    「这两个作品不是同一个」，而作品级的记录正是词表**合并作品**条里 2026-09-13 裁定暂缓的那一样。
    屏上那句回话因此不缀设计稿那句「（记为裁决）」（挂单 `Q1120`，与 `Q1032` 同一条理由）。
  - 核心库测试 `crates/core/tests/same_work.rs::说过不是同一个之后不再提_撤掉又回来`
    与 `说过不是同一个只管这一份主库`；界面测试
    `crates/gui/tests/suspicion.rs::按不是同一个之后那一对不再提_那句回话不说记为裁决`
    （里头有一条**反向断言**：屏上**没有**「记为裁决」）。
  - ⚠️ **保留：界面上撤不掉**，只有命令行 `romcat triage same-work --not-same A B --undo`
    撤得掉（挂单 `Q1121`，与词表**自足**相悖，已说清推给谁）。
- [x] 浏览屏的「整理建议」分面按它给数；数与列出来的组对得上
  - 左栏那一簇（设计稿 `#dup-chip`）一颗标签「疑似同一作品 N 组」，数是 `survey` 的条数；
    按下去 `WorkQuery::suspected` 收窄到那几对牵着的作品（新 `Unruly::Suspected`：
    它与「识别结论」同一个处境，只用于浏览、不写进规则，屏上写着那句说明）。
  - 钉着它的是 `crates/gui/tests/suspicion.rs::左栏整理建议那颗标签的数出自核心库_按下去之后表里只剩那一对`：
    标签写 `1 组`，按下去表里只剩那一对两行，不相干的第三个作品不在。
- [x] `crates/gui` 里没有新长出领域判断（测试断言用的是核心库交回来的结果）
  - `browse/suspicion.rs` 一条判断都没有：哪两个疑似、凭什么、那几句话，全从
    `same_work` 取；那张卡上的作品名走 `title::choose`（挑显示标题那一处），也不是新判断。
  - 断言方式见验收第 1 条那条测试：拿核心库当场算出来的 `reasons()` 与屏上的字逐句比。

收挂单 `Q957`：库屏库体检「疑似同一作品」那一格（`crates/gui/src/health.rs`，票 27）眼下只画「—」，点进去只在屏上说一句；这张票的判断立起来之后，那一格的数照它画，点进去落到浏览屏的「整理建议」上（票 27 验收最后一条）。

- [x] **`Q957` 已收**（挂单那一条标了 `settled`）：那一格的数跟着**库体检那一趟**回来
  （`survey_apart` 在后台那条线程上跑），**有建议**时点进去跳到浏览屏、左栏那颗标签按下去；
  一条都没有时照旧不跳、只说一句（跳过去是一张空表，正是那条挂单当初的判据）。
  扫描那一趟不算这一项，那一格回到「—」、小字「重新体检后才有」（挂单 `Q1125`）。
  钉着它的是 `crates/gui/tests/health.rs::疑似同一作品那一格的数照核心库画_点进去落到浏览屏的整理建议上`。

## 阈值是什么（核心库定的那一句）

**平台得有交集**（硬条件），而且**至少有一条立得了案的线索**：

| 线索 | 立得了案吗 | 为什么 |
|---|---|---|
| 命名撞上（两边各有一个叫法，`zh::key_of` 归一之后是同一串字） | 立得了 | 两个数据库对同一部作品的命名差异，识别因此建成了两个作品 |
| 同一条中文条目（两边的名字在中文离线源里指向同一条） | 立得了 | 那是「这两个名字是同一部作品的两种叫法」的直接证据 |
| 平台与年份一致 | **立不了** | 同一年同一平台的游戏成百上千；只当佐证 |

**名字那一条只认「归一之后一字不差」，不按相似度**：库里现成的 `zh::similarity` 在这一层会把
同系列的两部作品配成一对（`超级机器人大战J` 与 `R` 是 0.88）。中文离线源那一侧撞错了，代价是
人在队列里多看一眼；这一层给的建议**人是要按下去合并的**，撞错就是把两部游戏并成一个。
两边名字确实不一样的那一类（TOSEC 的 `Pocket Monster - Red Version` 与 No-Intro 的
`Pocket Monsters - Aka`）交给中文条目那一条去连——设计稿举的正是这一例。

## 复用而不是另起一套（ADR-0024）

| 要的东西 | 从哪一处取 |
|---|---|
| 名字归一与相似度 | `zh::key_of` / `zh::similarity` / `zh::alnum_of` |
| 这个锚点撞上了哪条中文条目 | `zh::entry_in` + `scrape::zh` 那组源名；新的 `scrape::zh::entry_marks` 只是 `matched_groups` 的全库那一份 |
| 一个作品有哪些叫法（**DAT 条目名本来就在里面**） | `title::work_titles`（`title::fold` 把自动通过的候选那条条目名折成官方名称，源就是那个数据库的名字） |
| 一个作品屏上写着的年份 | `Catalog::work_year_of`；那句 SQL 收进了 `scraped_year` 一处，与主列表那一列、详情面板那一格同一个取法 |
| 屏上那个作品叫什么 | `title::choose`（挑显示标题那一处） |
| 「那个作品还剩几个变体」「怎么合」 | 票 16 的 `triage::merge` 一族，一个字没改 |

## 与设计稿不同的几处（各自一条挂单）

| 哪儿 | 稿上 | 这一票 | 挂单 |
|---|---|---|---|
| 「不是同一个」那句回话 | 「……（记为裁决）」 | 不缀那个括号 | `Q1120` |
| 卡上的作品名 | 「某某」 | 《某某》（全仓写法） | `Q1124` |
| 体检那一格刚扫完 | 稿上只有一个态 | 回到「—」，小字「重新体检后才有」 | `Q1125` |

## 门禁

`cargo xtask gate --keep-going`（日志 `/Users/nicoer/dev/game-wt/logs/slot-2-gl17-gate1.log`，
3,158 行）：**六步全绿，`EXIT=0`**——`fmt` 5s、`glossary` 3s、`check` 44s、`clippy` 46s、
`test` 1,357s、`doc` 9s。`test` 那一步 **84 个测试二进制、2,510 项、0 失败**；
`glossary` 扫了 `crates/` 下 20 份 `.rs` 里新写的 2,226 行、没撞上
（词表那一条新词条加完之后另跑过一趟，照旧没撞上）。
截图门**不带更新连跑两遍**，两趟各 101 过 0 红。

## 截图

**新出两张**：`browse/suspicion-{light,dark}` —— 左栏「疑似同一作品 1 组」按下去、表里只剩那一对、
侧边详情里摆着那张建议卡（凭什么两条、两颗按钮）。合成数据里那一对是
`Pocket Monsters - Aka (Japan)`（精灵宝可梦 红）与 `Pocket Monster - Red Version (Japan)`
（口袋妖怪 红）——**只有这一态摆它**，别的态照旧是五个作品八行，那几张基线的内容一个字没动。

**重出 26 张**，差异逐张取过包围盒：

- `browse/{rows,cards,covers,empty}-{light,dark}` 与 `merge/step{1,2}-{light,dark}`：
  包围盒一律 `x 208–415`、`y 464–758`，**只落在左栏**——新长出来的「整理建议」那一簇，
  以及被它顶下去的「条件组」那一段。
- `library/{health,health-duplicates,platfix,shaping-doubts,shaping-merge,shaping-split}-{light,dark}`：
  包围盒一律 `x 1011–1119`、`y 682–718`，**只落在疑似同一作品那一格**（由「—」改成「0 组」）。
  `library/folded-{light,dark}` 那一格摆得更低，包围盒 `x 1011–1046`、`y 747–762`。

**自己量的对齐**（量的是基线图；按 brief，galley 矩形不当对齐证据）：

- 建议卡**撑满内容区整宽**：卡片 `x 1001–1263`，与它底下那张变体卡、那个「判定依据」框
  逐像素同宽（同为 `1001–1263`）。
- 卡上一圈**虚线**：上边那一道覆盖 `x 1001–1262`，右端比框沿短 1 点——那是
  `look::dashed_outline` 收尾落在空当上的正常样子（`433e119` 那几行注释）。
- 理由那一列**立成了一列**：两枚圆点左缘都在 `x = 1015`（逐像素相等）；三行文字
  （第一条两行、第二条一行）左缘分别是 `1042 / 1043 / 1042`——差的那 1 点是字形墨迹
  （「中」与「平」起笔不同），版式框是同一个：三行都由 `look::impact` 一处排，
  缩进是 `impact_column + item_spacing.x`（挂单 `Q1042`）。

## Comments

**2026-09-22（票 17 收尾）** 开了挂单 `Q1120`–`Q1127`（八条），收了 `Q957`。
词表加了 **疑似同一作品** 一条（`CONTEXT.md` 内容层级那一节，挨着**合并作品**）。
