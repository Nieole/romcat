# 06 — 库屏照稿重排，工序补齐六行

**What to build:** 维护者在库屏上看到完整的主干六步（扫描、识别、刮削、整理标题、裁决、
导出），每一行说得出「还差多少」，顶上一行直接告诉他下一步该做什么；根、数据源、
导出设置三块照设计稿排版，都收得起来。

**Blocked by:** 01、02、03；刮削那一行由 `gui-answers-all-six/04` 提供。
（截图那一条要等 `05` 立起截图门，但不必等它才开工。）

**Status:** ready-for-agent

⚠️ **工序段这一段同时有四张票在动，不许排进同一波**：`gui-answers-all-six/04`（刮削那一行）、
`/05`（导出照写与时刻戳）、`one-criterion-per-thing/09`（导出这一支上的铺媒体开关）与本票。
建议顺序：`gui-answers-all-six/04` → `/05` → `one-criterion-per-thing/09` → 本票收尾重排。

⚠️ **别把前面几张写上去的话抹掉**：刮削那一行的口径说明（「这个数不是按当前刮削旋钮
算的」）、导出那一支的铺媒体开关与代价说明，重排时**一句都不许丢**。
2026-09-14 拿主意的人定：刮削口径照稿逐字换成『统计的是没有任何刮削结果的变体』。

收挂单 `Q554`：刮削那一行的口径眼下画在工序表格底下一行、另挂在那个数的悬停里（没塞进那一格——令牌里没有正文宽度）；照稿重排时定它挨着那一行画在哪儿，那句话取 `romcat_core::stage::SCRAPE_BASIS`，一个字不改。

收挂单 `Q694`：库屏「添加目录」那一行仍然只能贴路径；照稿把根那一块排成「添加根…」时，那颗按钮调 `crate::pick::directory`（票 `gui-answers-all-six/01` 的薄层），交回来的交给库屏自己那条加根的路（`roots::Screen::add_root`），不另写一条。

⚠️ **加一支工序要改五处**，清单写在核心库工序枚举上方的文档里——补扫描与裁决两行照那份
清单逐条改，不另起一套。

⚠️ **算不出「还差多少」的那两支**（整理标题、导出）退回显示**上次跑的时刻**，
并在屏上说明白——词表**工序**那一条写着这条退路。

- [x] 工序六行齐，顺序与设计稿一致，每行报得出数或退回时刻
  - 证据：核心 `crates/core/tests/stage.rs::工序六道_次序与叫法照设计稿`（`Stage::ALL` 的叫法逐字是设计稿那六个）、`扫描那一行数的是还没完整扫过一趟的根_加一个根那个数跟着涨`、`裁决那一行的数与待确认队列说的是同一个_裁过的不再算`；界面 `crates/gui/tests/roots.rs::工序六行齐_次序照设计稿_每行报得出数或退回时刻`（六个名字按次序画在屏上，每一行那句话都在屏上，整理标题与导出两行说「还没跑过（这一趟算不出还差多少：……）」）。补的两支照 `Stage` 上方「加一支要写五处」清单逐条改，清单第 5 条照实补全了界面那几个 `match`。
  - 保留：扫描那一行数的是**根**（从没扫过的 + 上次部分完成的），不数盘上变了多少——挂单 `Q821`；裁决那一行要读沉淀库才与待确认队列屏同一个数——挂单 `Q822`。
- [x] 顶部「下一步：……」指向该做的那一行，按钮点得动，跑完自动指向下一行
  - 证据：核心 `下一步指向头一道还没做完的工序_做完一道就往下挪`（扫描 → 识别 → 刮削 → 整理标题 → 裁决 → 导出 → 都做完）；界面 `顶上那一行下一步指向该做的那一道_按钮点得动_跑完自动指向下一道`（真点「开始扫描」与三趟「运行」（识别、刮削、整理标题），每趟跑完顶上自己换到下一道；「去处理」换到待确认队列屏。按钮上的字第二段照稿改，挂单 `Q881`）、`在待确认队列屏上裁完一批再回库屏_裁决那一行跟着变`。扫描那一道交库屏自己那条扫描的路（`roots::Screen::take_scan`），不另写一份。
  - 保留：算不出还差多少的两道（整理标题、导出）**跑过就不再指它**——挂单 `Q823`。
- [x] 根、数据源、导出设置三块照稿排版，可折叠且下次打开还记得
  - 证据：界面 `根_数据源_导出设置三块收得起来_关掉再打开还是收着的`（点标题栏收起、换一个窗口本体与 egui 上下文再打开还收着、再点摊开也记得）；`crates/gui/src/layout.rs` 单元测试 `收着的那几块写出去再读回来是同一份_面板边界那几行照旧读得回来`，`crates/gui/tests/layout.rs` 13 条照旧绿。两栏（左工序、右三块），颜色、圆角、内边距、间距取令牌；「添加根…」在屏头（收挂单 `Q694`）。第二段照稿（挂单 `Q890`）：根那一块里不再摆贴路径的表单，屏头「添加根…」直接弹系统的选目录窗口、选中就加、根名取目录名（界面 `屏头添加根_选中一个目录就加成一个根_根名取目录名`、`屏头添加根_加不上时核心库那句话画在屏上_任务历史不多一条`、`屏头添加根_取消或弹不出选择窗口时屏上说去哪儿办`）；导出设置那一块逐项照稿（挂单 `Q887`，`导出设置照稿_没有记下那颗_目录选中且格式挑过就当场记下`）。
  - 保留：两栏 `1.25 : 1` 的比例是界面里的常量，令牌里没有这一格——挂单 `Q824`；工序那几行没画设计稿的序号圆点与「做完」「在等」两种样式，留给第二段对着截图量——挂单 `Q825`。
- [x] 还没扫描时每一块都有空态并写明下一步
  - 证据：界面 `还没扫描时每一块都有空态并写明下一步_一个根都没有时按扫描只在屏上说`（工序段「下一步：扫描」且后面几行说「等待扫描完成」（第二段照稿改，挂单 `Q827`）、根那一块指「添加根…」、数据源那一块说扫描时就可以先下载、导出设置那一块说先选一次；一个根都没有时按「开始扫描」只在屏上说、任务历史不多一条）；核心 `前面有一道没做完时_后面那几行一律说在等它`。
- [x] 刮削那一行的口径说明、导出那一支的铺媒体开关与代价说明在重排之后仍在屏上
  - 证据：界面 `工序段上刮削一行_数与口径都画在屏上`、`重排之后刮削口径挨着刮削那一行画_铺媒体开关挨着导出那一行画`（收挂单 `Q554`：口径画在那个数底下一行）、`打开铺媒体时_按下导出之前屏上先说清这一趟最多要铺几份多大`，票 07 的被拒提示 `还没选过格式与目录时点导出_当场说清而不是默默不动_任务历史不多一条`、`记着的前端格式这一版没有时点导出_当场说清去哪儿重选_任务历史不多一条`、`还没取回那份弹药时按识别_屏上说清为什么与去哪儿取_任务历史不多一条` 照旧绿（指路那一句改指「导出设置」「数据源」那一块，ADR-0005 修订段同步改）。全量门禁（`/Users/nicoer/dev/game-wt/logs/slot-4-gl06-gate.log`）fmt / glossary / check / clippy / test 绿（test 75 个目标、2029 条通过、0 条失败）；doc 那一步红在 `triage::pending_count` 的公开文档链到两个私有项，改成不带链接的反引号之后单跑 doc 绿（`/Users/nicoer/dev/game-wt/logs/slot-4-gl06-doc2.log`）。
- [ ] 工序行第二行的小字照稿补齐：识别那一行的命中 / 未命中 / 无判据 / 跳过分布与来自已问过的模型答案的候选数，整理标题那一行的作品数与有中文标题的作品数，导出那一行上次导出的条目数，其余几行照稿逐行对。每个数由核心库一个查询函数交出来、配单元测试，界面只画；不升结构版本交不出来的数停下回报（拿主意的人 2026-09-14 要求，挂单 `Q882`）
- [ ] 这一屏的基线图入库（票 05 之后）
