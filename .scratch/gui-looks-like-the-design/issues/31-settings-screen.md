# 31 — 统一的设置页

**What to build:** 维护者在一个地方找得到所有设置：外观与启动、工作目录与媒体池、数据源与
更新、ScreenScraper 账号与配额、刮削与导出的默认值、ffmpeg、快捷键、关于。⌘/Ctrl+, 打得开。

**Blocked by:** 30；「给主库改名」那一条的核心能力由 `no-mute-spots-opening-a-catalog/02` 提供。

**Status:** done

⚠️ **配额那一段要把话说重**（ADR-0007）：未识别 ROM 的配额撞穿会连账号带 IP 一起永久封禁；
工具只对已确认的条目发请求并默认限流。

⚠️ **补一处设计稿没画、别处也没人接的落点：给已经建好的主库改名。**
`no-mute-spots-opening-a-catalog/02` 把这件事在核心库做成了（元数据表那一族键各一对具名
函数，改名是第一个用例），但它票面明写界面入口不在它的验收里——按 ADR-0023 的判据，
「就在这台机器上、就现在、就这一份库」的动作界面上得有落点，所以接在这一屏的「常规」一节。

⚠️ **改名不动路径锚**：锚认的是**主库标识**，改的是**主库原名**。屏上要把这句说出来，
免得人以为改名会让裁决对不上。

⚠️ **导出撞上外部修改这一条不给「自动覆盖」的开关**：它丢掉的是人的一次手改，只能当场
点「照写」（那条路在 `gui-answers-all-six/05`）。

收挂单 `Q782`：数据源优先级那一层弹层（`crates/gui/src/priority.rs` 的 `Editor`，票 30）眼下
只从刮削面板「调整优先级…」打开。「数据源」一节的优先级入口调同一个 `Editor::open`、叠同一层
弹层，别另写一份；保存后照 `crates/gui/src/app.rs` 浏览屏那一支（`priority_mut().take_saved()` →
`set_priorities` → `refresh`）把新表换进浏览屏。刮削面板那颗留不留，挂单 `Q782` 等拿主意的人裁；
没裁之前两处并存。

- [x] 八节齐，左边一列切得动，⌘/Ctrl+, 打得开 —— 八节照稿（`settings::Section::ALL`），左边那一列
      是 `selectable_label`（稿上的实现说明写的就是它）；`⌘/Ctrl+,` 在 `App::shortcuts`，两道门抄逐条
      那一处（有弹层开着不接、光标在文本框里不接）。钉着的：`tests/settings.rs::八节都在左边那一列上切得动`、
      `::按一下打开设置那一对键就换到设置屏`。设置是**第六屏**：`View::Settings`、左栏「后台」那一组
      紧跟任务、不带计数（`tests/layout.rs::导航在左栏_三组六个入口照稿排好_…`）。
- [x] 常规一节还给得了「启动时直接打开上次使用的主库」 —— **稿上那六颗开关里，真接得上的
      是这一颗**：状态在 `crate::recent`，落点是版式文件那份通用键值面。拨了记得住，
      而且**启动那条路真的听它**（`Program::start_with` 与屏上读的是同一句
      `settings::open_last`——各判一次迟早出现「屏上说关着、启动照样开」）。
      钉着的：`tests/settings.rs::启动那颗开关拨一下就记进版式文件`、
      `::关掉之后启动先进开场而不是直接开上次那份`。另外五颗见挂单 `Q1062` / `Q1063` / `Q1065`。
      「外观」那三档同样是真的（记在同一份版式文件里，挂单 `Q1073`）。
- [x] 常规一节里给得了主库改名 —— 走核心库那一处判据（`Catalog::set_library_name`：空白、重名、
      目录列不开三种，拒的话也是它说的，界面一个字不编）。改完 `Site::display_name` 当场是新名字
      （开场屏与报告印的就是它），窗口标题与左栏那张卡也跟着换（`Screen::take_renamed` → `App`）。
      屏上常驻 `settings::RENAME_NOTE`：「改名不动路径锚：认库靠的是主库标识……裁决照旧对得上」。
      钉着的：`tests/settings.rs::改名收不收由核心库说了算`、`::改名那一格常驻着改名不动路径锚那句话`。
- [x] 工作目录与媒体池 —— 路径印的是窗口交进来的那一段短写（`~/.local/share/romcat`，
      **绝不印这台机器上的绝对路径**，`tests/settings.rs::设置屏上不印这台机器上那条绝对路径` 钉着）；
      媒体池那条路问核心库要（`workspace::media_pool_dir`，不自己拼「/media」），**单独换不了**
      就照实说一句（挂单 `Q1076`）；
      更换入口走 `gui-answers-all-six/01` 那条薄封装（`pick::directory`，不另写），挑回来的先过
      `Roots::refuse_writing_into`（ADR-0004，判据在核心库），过了整份退回开场（挂单 `Q1072`）；
      屏上常驻 `settings::WORKSPACE_NOTE`：「工作目录不能放在主库的根之内」。
      ⚠️ **占用明细核心库今天一个字节都算不出**（挂单 `Q1064`）：那张表改报**条数**，
      底下一句「每一样占多少空间还算不出来」。
- [x] 数据源 —— 更新时间与条数取 `sources::survey`（与库屏同一份）；优先级入口调的是票 30 那同一个
      `Editor::open`、叠同一层弹层（收挂单 `Q782`），保存后照浏览屏那一支换进浏览屏
      （`App::settle_settings`）。⚠️ **「自动检查更新」核心库没有这条路**（挂单 `Q1062`）：不画假开关，
      改一句「还不会自动检查更新：要更新的时候，去「库」那一屏数据源那一块按「全部更新」」。
- [x] ScreenScraper —— 一枚「账号已给／没给」的标（`Credentials::from_env`），
      封禁提醒是**与刮削面板同一句**的 `scrape::QUOTA_WARNING`，摆在警示框里（`tests/settings.rs::配额那段警示与刮削面板是同一句`）。
      ⚠️ **账号存不下来、连接测不了、两条配额留不住**（挂单 `Q1063`）：账号只认启动前那四个
      环境变量，配额只在一趟联网刮削跑着时报得出——三样都照实写，一个假输入框都不摆。
- [x] 刮削与导出的默认值 —— 导出那一节照 `Catalog::export_setup` 只读地摆出这个库设过的格式与
      导出目录（改它去库屏那一块，**不另开第二处编辑入口**）；「导出时铺媒体」照挂单 `Q652`
      已裁的那条（只记在那一段上、不记进库）写成一句说明，**不摆开关**；「撞上外部修改」照票面
      只摆一句说明、不给自动覆盖的开关。⚠️ 刮削那几个旋钮今天一个都不落盘（挂单 `Q1065`）。
- [x] 工具 —— ffmpeg 状态与「重新检测」真的探（新加 `preview::probe`，**判据在核心库**，ADR-0024），
      没有它时写明「照常用：视频显示成占位图标，仍旧点得开」，影响的只是预览帧
      （`tests/settings.rs::没有ffmpeg时说清影响的只是视频预览帧`）。**「没装」与「装了探不动」
      分两档说**，分那一刀的是核心库（`Missing::is_no_ffmpeg`）。
      ⚠️ **路径还指不了别的**（挂单 `Q1066`）：指了今天也没人读。
- [x] 快捷键表与关于 —— ⚠️ **票 14 还没开工**（挂单 `Q1060`）：那唯一一份由这张票造出来
      （`crates/gui/src/keys.rs`），摆成票 14 直接复用的形状，义务已追进票 14 的正文。
      表上**只列今天按得动的九条**（挂单 `Q1061`）。关于里有版本、库文件结构版本（`SCHEMA_VERSION`）
      与数据源名册；⚠️ 许可没有结构化字段，只说得出「以各家自己的说明为准」（挂单 `Q1067`）。
- [x] 这一屏的基线图入库 —— **八节各一对，共十六张**：
      `settings/{general,workspace,sources,scrape,export,tools,keys,about}-{light,dark}`。
      跟着机器变的三样都钉死了：工作目录那一段短写、数据源那张表的时刻、探 ffmpeg 那个程序名
      与 ScreenScraper 账号给没给（`probe_with` / `pin_account` / `pin_clock`）。
      ⚠️ **左栏多了「设置」这一项，另外 74 张带左栏的旧基线也全部重出**（差的只有那一行：
      摘掉那一项重跑，81 张一张不红，验过）。不带更新连跑两遍：89 过、89 过。
