# 19 — 子库屏照稿重排

**What to build:** 维护者在子库屏上一台设备一张卡：目标路径、前端格式与**能力档案**写在
卡头，下面是**选择集**里的每一条**规则**（名称、条件、命中多少、多大）与去重后的合计，
再下面是**容量条**。空间不够时只给删减建议。

**Blocked by:** 01、02、03。（截图那一条要等 `04`。）

**Status:** ready-for-agent

⚠️ **容量条三段**（词表）：**选中**（只问中立库，卡不在手边也算得出）、**清单之外**
（要设备在位才知道，没排过差量预览时是「未知」而**不是零**）、**上限**。
超没超不在条子这儿算。

⚠️ **超限只给建议，绝不自动删减**（ADR-0016）：删减建议表要说清每项释放多少、累计多少、
排除到哪一项就放得下、全排除也还差多少。

- [ ] 一台设备一张卡，卡头写清路径、前端格式、文件系统与能力档案
- [ ] 规则列表逐条显示名称、条件、命中数与大小；合计写明去掉了多少重复
- [ ] 容量条三段照词表画，「未知」画成未知而不是零
- [ ] 超限时给出删减建议表（四个数齐），排除记为这个子库的手动例外
- [ ] 还没有子库时是空态加一颗「新建子库」，不是两台示例设备
- [ ] 这一屏的基线图入库（票 04 之后）
