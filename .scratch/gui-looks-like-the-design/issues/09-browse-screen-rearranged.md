# 09 — 浏览屏照稿重排

**What to build:** 维护者在浏览屏上看到「左筛选 / 中表格 / 右详情」三栏，表格是主角；
认不出作品的那一行如实显示**正题**与从尾部截断的路径，而不是一长串原始路径。

**Blocked by:** 01、02、03。（截图那一条要等 `05`，但不必等它才开工。）

**Status:** ready-for-agent

⚠️ **认不出作品那一行是这张票的重点**：真库里 46,444 个变体有一万六千多没挂上作品，
设计稿此前没画过它，实现只好直接印路径。正解是**正题 + 未关联作品标签 + 从尾部截断的
相对路径**（测量宽度后从左侧删字补「…」）。

- [x] 三栏照稿排版，边界拖得动、收得起来、下次打开还记得
  - 收起、关掉再打开还收着、展开回到原宽：`crates/gui/tests/browse.rs::左右两栏收得起来_关掉再打开还收着_展开回到原来那么宽`。收起状态记在工作目录那份版式文件里（`layout.rs` 的 `Layout::seed` / `harvest` / `flush`），没有另开存储。
  - 拖得动、记得住：`crates/gui/tests/layout.rs::三屏的面板边界都拖得动`、`关掉再打开还是那个样子`，门禁 layout 13 条全绿。底栏挪进正中那一栏之后，这几条原样通过。
  - ⚠️ **保留**：
    - 收起后的窄条不写「N 个条件」（挂单 `Q806`，交票 12）。
    - 底下那块编辑面板与搜索框还留在正中那一栏（挂单 `Q804`，交票 15）。
- [x] 认不出作品的那一行照上面的形状显示，路径从尾部截断
  - 测试 `crates/gui/tests/browse.rs::认不出作品的那一行画正题与未关联作品标签_路径从尾部截断画得下`。它断言这一帧画出来的是：
    - 主栏是正题 `超级机器人大战R`，两行都挂「未关联作品」。
    - 整条原始路径不出现。
    - 副行是「…」加长路径的尾巴，文件名那一截还在，外框落在格子的裁剪矩形之内（是截到画得下，不是画出去被裁掉）。
    - 画得下的短路径一个字都不删。
  - 正题由核心库剥：`WorkRow::title(&Rules)` → `Rules::parse_main_key`（挂单 `Q801`）。
  - ⚠️ **保留**：按「作品」排与搜索照旧按键（挂单 `Q802`，交票 11）。
- [x] 侧边详情：封面或字卡、平台年份变体、判定依据、变体列表、媒体
  - 测试 `crates/gui/tests/browse.rs::侧边详情摆封面或字卡_平台年份变体数_判定依据_变体列表与媒体`，分两种情况断言：
    - 有封面：头上贴图，屏上有「未关联作品的变体」、正题、「GBA · 年份未知」「1 个变体」「判定依据」（候选依据摊在栏里）、「变体 1 个」「媒体 · 1 件」。
    - 没封面：头上是带 SFC 平台色的字卡；「判定依据」写核心库挑的「识别跑过了，一条候选都没有」。
  - 封面由核心库挑（`Catalog::cover_of`）。
  - ⚠️ **保留**：字卡调色比例写成常量、没进令牌（挂单 `Q808`）。
- [x] 列表每行开头显示封面的开关；没有封面显示平台色块
  - 测试 `crates/gui/tests/browse.rs::列表每行开头显示封面的开关_有封面贴封面_没封面画平台色块`：
    - 默认关着时表里不贴图。
    - 打开之后，有封面那一行正题左边贴图。
    - 没封面那一行正题左边是 SFC 平台色块，块里写着「SFC」。
  - ⚠️ **保留**：
    - 开关不记下次（挂单 `Q803`，交票 10）。
    - 认出作品的那一行仍是一行字（挂单 `Q807`）。
- [x] 筛不出东西时有空态，并给一颗「清除筛选」
  - 测试 `crates/gui/tests/browse.rs::筛不出东西时说清楚并给一颗清除筛选_按下去表就回来`：
    - 筛空时屏上有「没有符合当前筛选条件的作品。」与「清除筛选」，表头还在。
    - 按下去之后平台那一档清掉，那一行回来，空态那句收掉，「列出非游戏资产」没被拨回收起。
  - ⚠️ **保留**：
    - 「清除筛选」不清搜索框（挂单 `Q805`）。
    - 空态那句不带条件数（挂单 `Q806`）。
- [x] 与票 08（非游戏资产默认不列出）动的是同一处列表过滤，两张不排同一波
  - 票 08 已先合进 main（`10f4e81`），这一票从 `ee318bd` 开工，没有同波。
  - 那颗开关照旧在、照旧管用：`crates/gui/tests/browse.rs::非游戏资产默认不列出_屏上说收起了几个_开关打开后列出来并标着` 门禁绿。
- [ ] 这一屏的基线图入库（票 05 之后）

**第一段门禁**（2026-09-14）：`cargo xtask gate -j 3 --test-threads 3 --keep-going` 结果如下，日志在 `/Users/nicoer/dev/game-wt/logs/slot-2-gl09-gate.log`。

| 步骤 | 耗时 | 结果 |
|---|---|---|
| fmt | 1s | 绿 |
| glossary | 1s | 绿 |
| check | 19s | 绿 |
| clippy | 25s | 绿 |
| test | 666s | 绿 |
| doc | 11s | 绿 |

测试 2,022 条通过、0 失败、2 条忽略。基线那一格等票 05 的截图门合进 main 再做（第二段），所以 `Status` 暂不改。
