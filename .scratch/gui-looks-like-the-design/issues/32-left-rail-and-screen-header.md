# 32 — 左栏导航与屏头：窗口外壳照稿

**What to build:** 维护者打开主窗口，导航固定在左栏：顶上是当前主库和「切换主库」，
下面按「整理 / 输出 / 后台」分组列出各屏入口，每个入口带一个计数；栏底是已保存的裁决数和
收起按钮，收起后变成窄条。顶栏不再有：各屏的标题、说明和本屏动作都在该屏自己的屏头里
（标题、副标题、右侧动作、下边一道线），屏体内边距照稿。

**Blocked by:** 01、02、03、04。（截图那一条要等 `05`。）

**Status:** ready-for-agent

⚠️ **这张票是切票时漏掉的**：`spec.md` 实现决定三「左栏导航：六个入口各带计数徽标，底部是
新手引导入口、已保存裁决数与收起按钮。顶栏不再承载导航与动作」，用户故事 3、5，以及测试决定
「导航在左栏且六项齐」，都没有落到 01–31 里任何一张。拿主意的人看 06/09/20/25 四屏候选图时
指出「页面大的结构都与设计稿差异很大」，才补开这一张。

⚠️ **只动外壳，不动各屏内部**：各屏眼下往顶栏里画的那一段（`<屏>::Screen::status`）先原样
挪进该屏屏头的右侧，不改各屏文件里的写法。照稿摆进屏头的活由各屏自己的票接手
（06 库、09 浏览、18 待确认、20 子库、25 任务、31 设置）。

⚠️ **设置屏还没有**（票 31）：「设置」入口等 31 补上；这一张只摆已经存在的五屏。

⚠️ 底部状态栏在票 25 的分支上：稿里左栏纵贯到底（`.rail{grid-row:1/3}`），状态栏只在右侧主区
下面。25 与本票谁后合进 `main`，谁照这个摆法解冲突。

- [ ] 左栏宽取令牌 `rail-width`；顶上主库切换卡（库名 + 「切换主库」），替掉顶栏那颗「换一份库」
- [ ] 分组「整理」（库、待确认、浏览）、「输出」（子库）、「后台」（任务），每项带等宽计数；任务跑着时「任务」那一项带强调色圆点（截图里不动）
- [ ] 栏底：已保存 N 条裁决、收起按钮；收起成 `rail-collapsed` 宽的窄条，关掉再打开还是收着的
- [ ] 顶栏去掉；每屏一个屏头（标题、副标题、右侧放该屏原来在顶栏的那一段、下边线），屏体内边距照稿
- [ ] 测试钉住：导航在左栏、入口齐、点入口切屏、收起记得住；开场那几张基线一像素不变
- [ ] 主窗口外壳的基线图入库（左栏展开、收起 × 浅暗）
