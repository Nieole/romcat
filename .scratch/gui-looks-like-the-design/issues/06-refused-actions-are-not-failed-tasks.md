# 06 — 操作被拒只弹提示，不记为失败任务

**What to build:** 维护者在前置条件不满足时按下按钮（例如还没下数据源就点识别），
屏上只给一句「为什么不行 + 去哪儿办」，任务历史里不多一条失败任务——历史只记录真正
跑过的任务。

**Blocked by:** 无 —— 可立即开工

**Status:** ready-for-agent

⚠️ **真跑过的失败照旧记为失败**（例如下载超时）。这张票分开的是「压根没开跑」与
「跑了没成」。

- [ ] 缺前置条件时屏上出现那句提示，写明原因与去处
- [ ] 任务历史行数不变（测试断言这一帧画出了提示，且历史没多一条）
- [ ] 真跑过的失败仍旧进历史，收场是「失败」
- [ ] 现有那几处被拒的路径全部改完（工序、根、子库、导出）
