# 03 — 界面上的说法与词表对齐

**What to build:** 维护者在屏上读到的每一句都是他用得上的话：按钮是动词、收场四档与
文档里是同一套词，屏上不再出现写给开发者的解释、`**` 与 ADR 编号。

收挂单 `Q451`：认领向导起名框的提示字「主库名，例如「主库」」——「主库名」在词表**主库标识**条的 `_Avoid_` 里，框里要人起的是**主库原名**（改它会动 `crates/gui/tests/program.rs` 里五处按这串字找框的 `打字`）。
**已收：** 提示字改成「主库原名，例如「主库」」（`crates/gui/src/claim.rs` `NAME_HINT`），五处 `打字` 跟着改；挂单 `Q451` 标 `settled`。

**Blocked by:** 无 —— 可立即开工

**Status:** done

要改的字（词表 2026-09-11 与 09-13 两轮裁定）：认领新主库 → **添加主库**；
折标题 → **整理标题**；按停了 → **已取消**；停在半路 → **部分完成**；
在访达中显示 → **在文件系统中打开**。

⚠️ **收场四档只有一处落字**（`Ending` 那个渲染），界面与命令行都读它——别在界面里再写一份。

⚠️ 断言旧字串的测试要跟着改，那是这张票的一部分；注释与测试名里的旧词不必全改。

⚠️ **写新文案之前先过一遍词表的 `_Gate_` 那 12 行**：`machine-checks-premises/02` 会给门禁
加一条，新写的 `.rs` 里标识符与**字符串字面量**撞上 `_Gate_` 词就当场报红（只扫新代码）。
开场（启动屏、欢迎页、首屏、库选择器）、工序（步骤、阶段、流水线）、任务台（作业、
后台任务、任务列表、线程池）、差量预览（预演、确认页）、批（批次、批号）、媒体池
（图片目录、资源库）都在里面——这批观感票新写的文案量最大，撞上的机会也最大。

⚠️ **本票要改的那几个词恰好没进 `_Gate_`**（「认领」在**添加主库**的 `_Avoid_` 里但没有
`_Gate_` 行，收场四档同理），所以机器不会替你找，逐处 grep 是这张票自己的活。

- [x] 上面五组说法逐处改完，源码里只在注释里还留着旧词
  - 证据：扫 `crates/{gui,core,cli}/src` 的字符串字面量（跳过注释），「认领新主库 / 折标题 / 按停了 / 停在半路 / 主库名，例如」只剩测试模块里的断言说明、SQL 建表注释，和 `sync/execute.rs` 的「只重折了那份清单」（说的是清单，不是整理标题）。「在访达中显示」基点里一处都没有，无可改。
  - 屏上：开场按钮与向导标题「添加主库」、空库那句「还没有库，添加一个主库开始」、工序 `Stage::label` 与浏览屏入口「整理标题」、工序段那一行「还没重新整理」。钉它的是 `crates/gui/tests/program.rs`（29 过）、`browse.rs`（48 过）、`roots.rs`（31 过）、`crates/core/tests/stage.rs` 里断言新词的那几条。
  - **保留：** 测试里断言失败时的说明、测试名、大多数临时目录名留着旧词（挂单 `Q575`）；命令行报告里的「已中断」没改（`Q574`）。
- [x] 收场四档与词表逐字一致，命令行那一侧跟着变
  - 证据：`crates/core/src/task.rs::四种收场各画各的话` 逐字钉住 `完成` / `已取消` / `部分完成：…` / `失败：…`（带步骤的写成 `失败：在「X」这一步，…`）；`crates/gui/tests/task.rs::四种收场在任务屏历史里各画各的话` 钉任务屏历史那一行。基点上这两条红（`slot-1-gl03-red.log`），改后绿。
  - 词只在 `Ending::render` 落：各屏拿着收场结果的通知一律调它（审查后改的，`Q573`）；命令行扫描、识别、刮削、`zh sync`、中文索引重建五处收场那一句拼 `Ending` 调 `render`——基点上命令行一处都没读它。
  - **保留：** 失败那档的句式是这张票定的（`Q571`）；手上没有 `Ending` 的几处（被按停时的错误文案、刮削与同步回执、根那一行上次扫描）自己写词（`Q573`）；命令行报告里的「已中断」「这一趟被中断了」没改（`Q574`）。
- [x] 一条测试钉住：屏上不出现 `**`，也不出现 `ADR-`
  - 证据：三条，走 `headless` 收这一帧真画出来的字——`crates/gui/tests/layout.rs::五屏上画出来的字里没有星号也没有文档编号`、`program.rs::开场与添加主库向导上画出来的字里没有星号也没有文档编号`、`queue.rs::停在中文离线源那一条上依据里没有星号也没有文档编号`。基点上红：「Library 上画出了「**」」、待确认屏依据里画出了 `**`（`slot-1-gl03-red.log`，`EXIT_shots=101`）；改后绿（`slot-1-gl03-green.log`，`EXIT_shots=0`；`slot-1-gl03-nofailfast.log` 里三条都 ok）。
  - 屏上的 `**` 大半是核心库写的依据与报错：那些字面量一并去掉了，`zh::is_confirmed` 同时认带星号的旧记号（`zh.rs::旧库里带星号的确认记号照样认得出`）。
  - **保留：** 悬停与出错才画的那几句机器看不见，靠逐处 grep（`Q576`）；库里已存的旧依据要等下一趟识别、刮削才换（`Q572`）。
- [x] 屏上不再出现模块名、内部结构与「下推到中立库」这类写给开发者的话
  - 证据（逐处改）：浏览屏「全部下推到中立库」「三元组」「`Catalog::discard_broken_rule`」「挂单 Q117 / Q70」「票 10 / 11 / 06」；列表「排序一律下推到中立库，而它是补在页上的」；子库屏「票 `gui-redesign/11`」「折一次事实」；库屏「`CONTEXT.md` 的根」「挂单 Q62」；刮削面板「「锚点 × 字段 × 源」三元组」「跑在画帧那条线程之外」；浏览屏与子库屏开不出只读连接那句「要在画帧那条线程之外跑」；核心库画到屏上的「（ADR-xxxx）」「票 09」「调研 D.3.3」。
  - **保留：** 刮削面板那颗「加入任务队列」没改（`Q577`）。
- [x] 门禁全绿（`--all-features`）
  - 证据：`/Users/nicoer/dev/game-wt/logs/slot-1-gl03-gate2.log`（`/code-review` 那批改动落定之后跑的一趟）五步全绿：fmt、check、clippy、test（71 个测试目标，1858 过、0 挂、2 条 `#[ignore]`）、doc，`EXIT=0`。
  - 前一趟 `slot-1-gl03-gate.log` 的 test 那一步红在 `-p romcat-core --test works`（期望原文还带 `**`）；`slot-1-gl03-nofailfast.log` 补跑时又挂两条 `crates/gui/tests/sublibrary.rs`（断言旧通知字）。三条都只改了测试期望。
  - **保留：** 基点 `e2fd820` 早于门禁新加的 `cargo xtask glossary`，这一步没跑；新增 `.rs` 行本票自己按 `_Gate_` 那 12 行扫过，零命中，合并时编排者在 `main` 上补扫。
