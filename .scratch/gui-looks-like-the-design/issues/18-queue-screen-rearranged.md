# 18 — 待确认屏照稿重排

**What to build:** 维护者在待确认屏上先看到三个数（能整批处理的、有多个候选的、没有候选的），
然后是按**依据形状**分好的批：每批写明共同依据、给随机样本、可整批通过或拒绝；落下的
每一批都在**裁决记录**里，能撤销。

**Blocked by:** 01、02、03；撤任意一批由 `gui-answers-all-six/06` 提供。
（截图那一条要等 `05`。）

**Status:** ready-for-agent

⚠️ **裁决记录那一块的能力由 `gui-answers-all-six/06` 建**（从沉淀库列批、撤任意一批、
被后来的批盖住时拦下）——本票**只重排它**，不重写一份撤销逻辑。两张动同一屏，不排同一波。

收挂单 `Q625`：裁决记录眼下是顶栏一颗开关打开的一块浮窗（`queue::Screen::records_window`，
`egui::Window`），照稿定成抽屉（要不要在 `layout` 里加第八条边界）或别的样子；列批、撤销、放回
走的 `Screen::undo` / `Screen::redo` / `refresh_records` 不动。

收挂单 `Q661`：裁决记录那一块若照稿定成一层弹层（而不是抽屉），走 `crate::dialog` 那一套，
不再自己摆 `egui::Window`。

⚠️ **屏头那几个数取现成的那一处**（队列屏、命令行报告、工序段共用同一句），不许另造一份数。

⚠️ **「一批变体」与「一批裁决」措辞必须分得开**（词表**批**那一条）：前者是一级分批、
还没落库，后者是撤销的粒度。屏上两处不许混用同一个词。

⚠️ **中文离线源那几次匹配**照设计稿排在逐条那一屏的详情里：一堆一个条目号，写明这一次
匹配带来哪几个字段与整句依据，一次裁决管住全部。行为不改（票 `queue-followups/06` 已经
做完），这张票只重排。

- [ ] 屏头三个数与批列表照稿排版；能整批处理的排在前面
- [ ] 每批写明共同依据、给五条随机样本并能换一批
- [ ] 整批通过、整批拒绝；落下的批照 `gui-answers-all-six/06` 那套列在裁决记录里（本票不重写撤销）
- [ ] 撤销被后来的批盖住时，把那句话原样画出来，说清是哪一批盖的
- [ ] 逐条模式：候选卡片、键盘（← → / Y / N / 空格 / U）、匹配那一堆照稿排
- [x] 卡片只按依据形状各段画，不写死只装识别那一种（标题待裁眼下没有能逐条裁的队列，见 `Q713`）
  - 证据：`crates/gui/src/queue.rs` 的 `batch_head` / `shape_line` 只照 `Shape` 各段排（有候选的「源 / DAT / 哈希口径 / 候选数」，一条候选都没有的是 `Shape::label`），第二行放 `Batch::evidence` 或理由，一个字都不认识别那一侧的专名；`crates/gui/tests/queue.rs::批卡照稿_卡头是形状各段与共同依据_展开后判定依据是那句原话_样本旁边换一组`。
  - 票面原写「给 OC02 那类标题待裁条目留位置」查不实：标题待裁那一侧眼下只是报告里一个数与十个例子，没有能逐条裁的队列（`one-criterion-per-thing/02` 与挂单 `Q713`），屏上没有东西可接；协调人 2026-09-15 定改写成上面这一格。
- [ ] 没有待确认项时的空态；这一屏的基线图入库
