# 25 — 任务屏照稿重排

**What to build:** 维护者在任务屏上看到正在跑的那一趟（进度、已用、约剩、按得下停）、
等待中的队列，以及历史——历史里每一行写明四档**收场**中的哪一档，停下的那一趟写清保留了
什么、下次从哪儿继续。

**Blocked by:** 01、02、03。（截图那一条要等 `05`。）

**Status:** ready-for-agent

⚠️ **一次只跑一趟**（词表**任务台**）：它们读写同一份中立库、同一块盘，屏上把这条说出口。

⚠️ 四档收场的词由票 03 统一，这里只负责画得对：完成 / 已取消 / 部分完成 / 失败。

- [x] 正在跑那一张卡：进度、已用时间、约剩时间、当前在做什么、停下按钮
  - 证据：`crates/gui/tests/task.rs::正在跑的那一张卡写着进度已用约剩在做什么还有停下`（四步走完一步、第二步走了一半，屏上逐字「进度 38%」「2/4 挨个文件过一遍（3/6）」「停下」，另有「已用 」「约剩 」两段，还有「一次只跑一趟」那一句）；约剩走核心库现成的 `Live::remaining`，`走了一半时屏上那一句写着约剩多少`、`算不出还剩多久的那一帧屏上一个剩余时间的字都没有` 两条照旧绿。
  - 门禁：`/Users/nicoer/dev/game-wt/logs/slot-5-gl25-gate-3.log` 六步全绿，test 那一步 76 个目标、2042 条通过、2 条 `#[ignore]`，`EXIT=0`（合 main `1cce976` 之后，库屏那处修在 `5132ab1`）。
- [x] 等待队列可以移除某一项
  - 证据：`crates/gui/tests/task.rs::等待中的每一趟各有一颗移除_按下只撤掉那一趟`（台上占住，排两趟，屏上两颗「移除」；按头一颗只撤头一趟，排在后面的照旧排着，正在跑的那一趟不动）；走核心库现成的 `Board::stop`（`还没轮到的那趟撤得掉`）。
  - **保留：** 撤掉的那一趟照任务台原有的账在历史里记一条「已取消」——设计稿里移除不进历史；那一条交回排它的那一屏，库屏四条「排着队被撤掉记成已取消」钉着，推翻出票，没改（写在提交 `cdac15c` 说明里）。
- [x] 历史四列（任务、结果、耗时、时间），结果四档逐字与词表一致
  - 证据：`crates/gui/tests/task.rs::四种收场在任务屏历史里各画各的话`（表头连着四格「任务 / 收场 / 耗时 / 时间」；四趟四个词「完成」「已取消」「部分完成」「失败」各正好一行；耗时、时间两列画的是任务台记下的那两个数）；核心库 `task::tests::四档的词与那半句说明分得开`、`四种收场各画各的话`（`render` 逐字不变）、`历史里每一条都记着什么时候收的场`。
  - **保留：** 第二列表头画「收场」不画「结果」——「结果」是词表**收场**那一条的避用词（挂单 `Q832`，拿主意的人裁）；「时间」那一列画 UTC 的「年-月-日 时:分」，与库屏、开场同一个排法（挂单 `Q831`）。
- [x] 部分完成那一行写清留下了什么、下次从哪儿继续
  - 证据：说法全由核心库长入口给（`Handle::halfway`），界面只画 `Ending::detail`。扫描、同步两句补上了后半句：`crates/core/tests/task.rs::扫描被叫停时在台上记成停在半路而且说得出断点写下了`（「续跑从断点接着走」）、`没设断点的那一趟被叫停时不许说断点写下了`（「下一趟得从头扫」）、`同步按停时已经落下东西的那一趟…`（「下一趟同步照这份清单接着来，落过的不再重落」）、`同步按停时一件都没落的那一趟…`；识别、导出、刮削三句本来就说了（`识别按停之后在台上记成部分完成而且说得出下一趟接着算剩下的`）。屏上那一行：`四种收场在任务屏历史里各画各的话` 断说明画在任务名底下。
  - **保留：** 扫描那句写「续跑」（扫描那一侧的词、命令行是 `--resume`），库屏按钮叫「重扫」，没对齐；命令行收场那一句读的也是它，跟着变了。
- [x] 任务跑着的时候其他屏照常用；状态栏那一条与任务屏说的是同一件事
  - 证据：`crates/gui/tests/task.rs::任务跑着的时候别的屏照常画得出来`（原有）、`任务跑着的时候别的屏照常用_底下状态栏说的与任务屏是同一件事`（浏览屏上照常有行、底下状态栏「扫描 · 主库 · 38% · 约剩 …」；点那一句去任务屏，卡上「进度 38%」，**同一帧里状态栏与卡上的约剩逐字相等**——两处读同一份任务台快照，`task::Screen` 按帧号记着；工作目录定得死 `App::set_workspace_label`）。状态栏挂在 `app.rs` 只加不改。
  - **保留：** 状态栏让每一屏正文矮 30 点，合 main（票 05 的按钮加高）之后库屏「一起铺媒体」底下那一句被挤出视口，`roots.rs` 挂六条；修在 `5132ab1`（那一句一换就滚进视口，三条测试读字前先空跑一帧），根因与探针数写在那个提交里。
- [ ] 没有任务时的空态；这一屏的基线图入库（票 05 之后）
  - 空态那一半：`crates/gui/tests/task.rs::台上什么都没有时三块各说一句空态`（「眼下没有任务在跑。」「在「库」屏点一道工序的按钮，那一趟就排到这里。」「没有等待中的任务。」「历史里还没有任务。」，空台上没有「停下」「移除」）。基线图那一半在第二段做，等拿主意的人点头再勾。
