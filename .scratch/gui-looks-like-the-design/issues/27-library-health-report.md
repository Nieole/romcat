# 27 — 库体检：概要与明细

**What to build:** 维护者在库屏上看到一份**库体检**概要：重复拷贝、目录与内容平台不符、
成型存疑、不可读、未纳入管理的目录、附属文件落单、非游戏资产、疑似同一作品——每一格点
进去看明细。它是只读检查，只报告、不处理。

**Blocked by:** 06

**Status:** done

收尾说明（2026-09-20）：2026-09-16 另一个会话在这棵树上记了两件事，拿主意的人 2026-09-20 各裁了一条——

- **完整导出那条 P1 本票修好了**：`Limits::FULL_DUPLICATE_PATHS_PER_GROUP` 原先是一万条，一组超过一万份就漏掉第 10001 份路径。它给的理由「断点里存着这份索引」不成立（断点 `scan::checkpoint::Checkpoint` 里只有没扫完的目录与耗时），统计一律由 `Catalog::aggregate` 从中立库**按键顺序**折出来，索引只在那一趟里活着。这一趟把那道闸撤了（常量留着，值改成不设上限），命令行 `romcat report --dump-duplicates` 与界面「导出清单…」共用同一条路、同一份字节。测试 `crates/core/src/report/duplicates.rs::组内份数超过原先那道一万条的上限也写全`（10,001 份，每一份都在，不说「另有」）。
- **非游戏资产照 2026-09-15 的口径改回**：只数平台目录里的（`placement.in_scope()` 那道闸），挂单 `Q956` 已裁。
- 此前门禁里 `layout` 那两条红是并发共用版式偏好文件的竞争，串行 `cargo test -p romcat-gui --features demo --test layout` 22 条全过；`sync_run` 那 5 条是本机既有失败（挂单 `Q479`）。`Q952` 保持 open，没动。

收尾审查（2026-09-20，Standards + Spec 两轴，基点 `main` `f6575c4`）：

- **照改了**：非游戏资产的判据文案补上「只数平台目录底下的」（`Finding::criterion`，与 `Q956` 的口径对齐）；`重复拷贝每一份都带着中立库的键…` 那条测试改成按体检那一趟的上限收，报告那一侧的截断真的发生（原先默认上限 10 与报告展示的 10 相等，`truncate` 是空操作，钉不住）；撤掉一万条上限那段注释写明「折统计那一趟把每个键都拿在手上」是这张票自己刚加的，不是既成事实；界面与测试的模块文档改掉「对主库跑一遍只读扫描」那句（体检只折中立库）；两处逐字相同的等任务台循环收进 `tests/shared::等任务台空了`（原先是五千万次自旋，没有挂钟含义）；一条断言的失败消息与它断的相反，改过来。
- **两条岔路口拿主意的人 2026-09-20 裁了**：① 判据文案统一到核心库一处——明细弹层那段说明改成「核心库的判据（`Finding::criterion`）+ 界面自己的政策句」，格子小字也从同一处出（核心库新增 `Finding::hint`，判据的短写法，措辞照旧），`health-duplicates` 浅暗两张因此变字、重出；② 扫描那一趟的每组路径上限**维持不放开**，「与命令行同一份字节」只在点过「重新体检」之后成立，票面、`health.rs` 与 `Q959` 都写明了这条边界。
- **记着没动的**：`ShapingDoubtSummary::by_kind` / `StrandedSummary::by_kind` 眼下没有读者；成型存疑与附属文件落单那两套累加结构逐字同形；`DUPLICATE_ROWS = 50` 与其余几格的虚拟化列表两种做法；索引真被截断时屏上不说（默认 200 万键，真库够用）。
- `refuse_writing_into_library` 挪进核心库时判据由 `is_inside` 换成 `is_inside_place`（先落成真实路径、再折成可比形态）：这是有意收严的，`comparable_text` 的文档里写着不折形态时 ADR-0004 这道红线在 Windows 上拦不住；判据本身由 `path.rs` 里 `is_inside_place` 那几条测试钉着。

⚠️ **数不在界面里算**：报告本来就有给界面吃的那一份形态，界面只画它。

⚠️ **重复拷贝只发现并报告，绝不自动删除**（词表）；主库只读（ADR-0004），要清理请人自己
去文件系统里做。同一个作品的两个不同转储**不算**重复拷贝。

- [x] 概要八格，数字来自核心库的报告；还没扫描时是空态
  - 空态：`crates/gui/tests/health.rs::还没扫描时库屏底下有库体检那一块_说清扫描完成后才生成报告`，标题栏「库体检」「只读检查，只报告、不处理」「重新体检」与空态那句「扫描完成后生成体检报告。」逐字照稿。
  - 八格的数：`crates/gui/tests/health.rs::扫过之后八格的数照核心库的报告画_疑似同一作品在识别完成前写识别完成后才有`，期望的数照盘上摆的东西写（重复拷贝 1 组、可腾出 2 KiB，成型存疑 1 处，未纳入管理的目录 1 个，附属文件落单 1 个，非游戏资产 1 个）。数出自 `HealthReport::finding_count`，扫完一个根就用扫描交回的那份报告（拿主意的人 2026-09-15 答岔路口 8）。
  - 收起记在工作目录的版式文件里：`crates/gui/tests/health.rs::库体检那一块收得起来_关掉再打开还是收着的`（`layout::FOLD_HEALTH`）。
  - 缺数三格在核心库补上，见最后一条。
  - ⚠️ **保留**：
    - 未纳入管理的目录小字照实写「不在任何平台目录下，不进识别与刮削」，不照稿写「扫描时跳过」，也不画映射下拉（拿主意的人 2026-09-15 答岔路口 10，ADR-0011 修订段）。
    - 不可读那一格小字照词表写「文件名拿得到，元数据读不到」（挂单 `Q960` 已裁）。
    - 格子里的数用等宽常规体（拿主意的人 2026-09-15 答第二轮岔路口 3）。
- [x] 每一格点进去有明细：路径、原因、数量，能在文件系统里打开
  - 测试 `crates/gui/tests/health.rs::点其余几格_明细里是路径与原因_路径旁边能在文件系统中打开_未纳入管理的目录不画映射下拉`：
    - 附属文件落单的明细里有 `GBA/汉化/火焰之纹章.sav` 与原因「存档 · 同一目录里找不到同名的主文件」。
    - 路径旁有「在文件系统中打开」（`open` `=5.4.4`，盘不在位按不下去）。
    - 未纳入管理的目录只有目录名、没有路径，那一行不画「在文件系统中打开」。
  - 行、原因、数量出自核心库 `HealthReport::finding_rows` / `render_finding`，单元测试 `crates/core/src/report.rs::每一格的明细_路径原因数量都在_样例截断时说清另有几个`。
  - 行多了用虚拟化列表（令牌 `health-list-max-height`），样例截断时底下照实写「另有 N 个」：`crates/gui/tests/health.rs::扫描交回的报告每类只留十个样例_重新体检那一趟列全_导出的清单也全`（挂单 `Q959` 已裁）。
  - ⚠️ **保留**：疑似同一作品那一格不开明细，见倒数第二条。
- [x] 重复拷贝按可腾出的空间排序，每组列得出全部路径，屏上写明不会自动删除
  - 测试 `crates/gui/tests/health.rs::点重复拷贝那一格_明细按可腾出的空间排_展开一组列出全部路径_写明不会自动删除`：
    - 明细表四列照稿「内容 / 平台 / 份数 / 单份大小」，「逆转裁判.gba」（可腾出 16 KiB）排在「魂斗罗.zip」（2 KiB）前头。
    - 点一行摊开三份路径，照票 09 写「主库 · GBA/备份二/逆转裁判.gba」（`table::root_and_path`）。
    - 屏上有「绝不自动删除」。
    - 判据照实写「同名且同大小」，加一句「动手前请自己核一眼」（拿主意的人 2026-09-15 答岔路口 3）。
  - 核心库每组带名字、平台与中立库的键，截断时键跟着路径一起截：`crates/core/src/report.rs::每组重复拷贝带着文件名与它横跨的平台_跨平台的组平台一个不落`、`重复拷贝每一份都带着中立库的键_与路径一一对应_报告截断时键跟着路径一起截`。
  - ⚠️ **保留**：
    - 词表写「同一个内容锚」，实现是「同名且同大小」（挂单 `Q952`，拿主意的人来裁）。
    - 表里摆可腾出空间最多的前 50 组，底下写「另有 N 组 · 按可腾出的空间从大到小排列」；导出的清单是每一组（`Q959` 已裁）。
    - 一组横跨几个平台时按顿号并列，放不下尾部截断加悬停（`Q953` 已裁）。
- [x] 明细能导出成一份清单
  - 测试 `crates/gui/tests/health.rs::明细导出成清单_重复拷贝与命令行同一份字节_其余几格是核心库的文本明细_落点在主库里就拒并说原因`：
    - 重复拷贝那一份与 `romcat report --dump-duplicates` 同一份字节。
    - 附属文件落单那一份等于核心库的 `HealthReport::render_finding`。
    - 落点在主库里就拒，一个字节都不写，屏上写「落在主库内」。
  - 拒写的判断从命令行挪进核心库，两边共用：`crates/core/src/path.rs::工具写出去的文件落在主库里就拒_说清是哪个文件_落在别处放行`、`crates/core/src/catalog/roots.rs::工具写出去的文件落在任何一个根里都拒_一个根都不在就放行`。
  - 按钮照稿写「导出清单…」（词表「清单」条补了一句），「导出清单…」幽灵按钮在左、「关闭」主按钮在右（`Q958` 已裁）：`crates/gui/tests/dialog.rs::退出那一颗摆在右边的写法_退出那一颗靠右画成主按钮_其余几颗靠左是幽灵按钮_退出键照旧等于按它`。
  - 「重新体检」那一趟列全，导出也全（`HealthReport::build_full`）：`crates/gui/tests/health.rs::扫描交回的报告每类只留十个样例_重新体检那一趟列全_导出的清单也全`、`crates/core/src/report.rs::体检那一趟列全_未纳入管理的目录不再只留前十五个_默认的报告照旧只留前十五个`。
  - 组内份数再多也写全（2026-09-20 修掉原先一万条那道闸）：`crates/core/src/report/duplicates.rs::组内份数超过原先那道一万条的上限也写全`。
  - ⚠️ **边界（拿主意的人 2026-09-20 定）**：「与命令行同一份字节」**只在点过「重新体检」之后成立**——那一趟每组路径不设上限；扫完一个根交回的那一份用扫描的默认上限（每组 10 条），一组超过 10 份时导出的清单照实写「另有 N 份没记下路径」。扫描那一趟不放开（挂单 `Q959` 记着这条边界，`health.rs` 的模块文档与 `export_picked` 的文档也写着）。
  - ⚠️ **保留**：系统保存对话框本身测不了（同 `tests/pick.rs`），选中之后那一半递固定路径进去验。
- [x] 「重新体检」排进任务台，跑的时候概要上说明白正在体检
  - 测试 `crates/gui/tests/health.rs::开窗后头一次进库屏有扫过的根_自动排一趟体检_跑着时说正在体检_收场后写上次体检的时刻_一次只跑一趟`：
    - 开窗后头一次进库屏、有扫过的根却没有报告，自动排一趟「库体检 · 全部根」。
    - 排着时标题栏写「正在体检…」；再按「重新体检」不排第二趟。
    - 收场后写「上次体检 09-14 08:00」，任务历史里只有一趟；有报告了不再自动排。
  - 用只读连接跑（`health::Section::check`）。
  - ⚠️ **保留**：还没扫描时「重新体检」灰着按不下去，悬停说明原因（拿主意的人 2026-09-15 答第二轮岔路口 5）。
  - 真库计时是估的，没实测：debug 构建的命令行在合成库上 2 万个文件 0.30 秒、8 万个文件 1.24 秒，大致成线性，照真库 215,964 个文件推约 3.3 秒。没算容器里面的条目，release 没量（`/Users/nicoer/dev/game-wt/logs/slot-4-gl27-timing.log`）。
- [x] 疑似同一作品那一格的数来自票 17（判断在核心库），票 17 之前这一格显示为「识别完成后才有」——**界面里不许自己算一个**
  - 测试 `crates/gui/tests/health.rs::点疑似同一作品那一格_不跳屏也不开明细_只在屏上说一句_识别跑完之后那一格与那一句都改说实话`，与 `扫过之后八格的数照核心库的报告画_…`：
    - 那一格只画「—」，识别没跑完写「识别完成后才有」。
    - 识别跑完改写「暂时还给不出这一项」（拿主意的人 2026-09-15 答岔路口 9，措辞 `Q960` 已裁）。
      ⚠️ **这一句已被票 17 换掉**（2026-09-22，挂单 `Q957` 已 `settled`）：那一格的数现在由核心库
      给（`triage::same_work`），识别做完之后画「N 组」，零那一档小字写「没有待处理的建议」；
      识别没做完照旧画「—」/「识别完成后才有」，这一条没变。
    - 界面里没有算数的代码。
- [x] 那一格点进去落到浏览屏的整理建议上
  - ⚠️ **移交票 17**：拿主意的人 2026-09-15 答岔路口 9——判断在票 17、浏览屏也还没有「整理建议」可落，这一趟点进去不跳屏、不开明细，只在库体检那一块里说一句（挂单 `Q957`，票 17 票面末尾追加了「收挂单 `Q957`」）。
  - 测试 `crates/gui/tests/health.rs::点疑似同一作品那一格_不跳屏也不开明细_只在屏上说一句_识别跑完之后那一格与那一句都改说实话` 钉着现状：点完还在库屏，没有「库体检 · 疑似同一作品」弹层，屏上写「识别完成后才有疑似同一作品的建议」。
- [x] **范围变大**（2026-09-15 拿主意的人定）：缺数的三格这一趟在核心库补上——**成型存疑**、**附属文件落单**两条新判据立在核心库（`shape::shaping_doubts` / `shape::stranded_companions`，词表补了两条），非游戏资产补上数；不可读补样例路径（穿不透不并进来）；重复拷贝每组补名字与平台（明细四列照稿，跨平台先按顿号并列，挂单 `Q953`）。每条判据单元测试正例、反例各至少一个
  - 成型存疑（`crates/core/src/shape/doubt.rs`）：
    - 正例 `同一目录里只差碟片标记却各自成了变体的_是多碟没合在一起`、`目录树变体里直接躺着几个各自独立的镜像或容器的_是一个目录被当成了一个变体`。
    - 反例 `规则已经合上的_名字碰巧像的_分在两个目录的_人工纠正过的_都不算多碟存疑`、`正常的目录树转储不算存疑_根下只有一个包或者只有转储自己的数据文件`。
  - 附属文件落单（`crates/core/src/shape/stranded.rs`）：
    - 正例 `同目录里没有同名主文件的存档与补丁落单`、`同名主文件在同一平台的另一个目录时_说出是哪个目录`。
    - 反例 `同目录里有同名主文件的不算落单_主文件是透明容器也算`、`进了变体的_范围之外的_不是存档补丁的_都不算落单`。
  - 从中立库折进报告：`crates/core/tests/shaping.rs::报告带着成型存疑与落单的附属文件_数与样例都从中立库折出来`。
  - 非游戏资产、不可读样例：`crates/core/src/report.rs::非游戏资产数的是平台目录里_bios_目录底下的文件_游戏与未纳入管理的目录不算`、`不可读的样例路径跟着个数一起报_空文件与穿不透的容器不算进来`。
  - 词表 `CONTEXT.md` 补「成型存疑」「附属文件落单」两条；票 29 票面末尾追加「收挂单 `Q951`」。
  - ⚠️ **保留**（挂单，拿主意的人来裁）：
    - `Q954`：「一个目录被当成一个变体」认哪几种文件是各自独立的内容。
    - `Q955`：落单认哪几种存档，记忆卡不算。
    - `Q956`：非游戏资产只数平台目录里的。
- [x] 这一屏的基线图入库
  - `crates/gui/tests/snapshots/library/` 下 10 张，提交 `9fdcb4d`，拿主意的人 2026-09-15 点头：
    - 新图 4 张：`health-{light,dark}`、`health-duplicates-{light,dark}`。
    - 变动 4 张：`empty-{light,dark}`、`folded-{light,dark}`，库体检那一块长出来了。
    - 没变 2 张：`scanned-{light,dark}`。
  - 出图的是 `crates/gui/tests/snapshot.rs::库屏_体检_{浅色,暗色}`、`库屏_重复拷贝明细弹层_{浅色,暗色}`。库屏扫过的那几张开窗前先体检一趟（`先体检一趟`），自动排的那一趟就不会在拍图时跑着。
  - `/Users/nicoer/dev/game-wt/logs/slot-4-gl27-snap-2.log`：带更新出库屏 10 张 10 过，不带更新整份截图门连跑两遍，45 过、45 过。
  - **2026-09-20 重出 `health-duplicates` 那一对**：判据文案统一到核心库之后弹层那段说明变字（拿主意的人当天裁的第 1 条）。变的只有那一段：与上一版逐像素比，浅色、暗色的差都落在 `x 303–971 · y 284–314` 那一条内，弹层没变高、表格与页脚一个像素没动；格子小字措辞原样搬进核心库，八格那两张与空的、三块收起四张都没变。先不带更新验一遍（57 过、那一对如期红），带更新只重出这一对（2 过），再不带更新连跑两遍（59 过、59 过）。日志 `slot-4-gl27-final-snap-verify.log`、`-update.log`、`-check.log`。

**终版门禁**（2026-09-20，跑在合过 `main` `57bec44` 的这棵树上，HEAD `f19df2d`）：
`TMPDIR=… CARGO_INCREMENTAL=0 cargo xtask gate -j 3 --test-threads 3 --keep-going`，日志
`/Users/nicoer/dev/game-wt/logs/slot-4-gl27-final-gate-2.log`，末行 `EXIT=0`。

| 步骤 | 耗时 | 结果 |
|---|---|---|
| fmt | 1s | 绿 |
| glossary | 0s | 绿（范围：相对 `main` 的 merge base `57bec44` 以来，28 份 `.rs` 里新写的 3831 行，没撞上） |
| check | 20s | 绿 |
| clippy | 33s | 绿 |
| test | 603s | 绿 |
| doc | 4s | 绿 |

六条全绿。这一趟里与这张票相关的几个测试目标：核心库 lib 1,004 过、`shaping` 13 过、`fixture_library` 13 过、`sync_run` 26 过；界面 `health` 9 过、`dialog` 9 过、`layout` 22 过、`snapshot` 59 过、lib 66 过。

## 挂单裁决（第五轮收口，2026-09-23）

从 `.scratch/PARKING-LOT.md` 迁来（`/settle`，范围 `Q449`–`Q1185`）。每条顶上多一行**裁决**：落哪一档、去了哪儿。
编号 `Qn` 留着占号、不复用。

### Q951 — 成型存疑的判据由票 27 立在核心库，票 29「同结构的其他几组列在库体检里」照它读

- **裁决（第五轮收口，2026-09-23）：** **记** —— settled，票 29 已照读。已落地：票 gui-looks-like-the-design/29

- **来自：** 票 `gui-looks-like-the-design/27`（拿主意的人 2026-09-15 答：缺数三格这一趟把两条新判据也立起来）
- **类别：** 规格没说
- **在哪：** `crates/core/src/shape/doubt.rs` 的 `shaping_doubts`；报告 `HealthReport::shaping_doubts`；票 29 验收最后一条
- **为什么没停线：** 判据只在核心库一处（ADR-0024），票 29 还没开工，照读就行。
- **这张票实际做了什么：** 立了两种成型存疑（多碟没合在一起、一个目录被当成一个变体），报告带数与样例；词表补「成型存疑」条；票 29 票面末尾追加了「收挂单 `Q951`」。
- **另一条路：** 票 29 自己立判据，27 那一格等 29 做完再有数。
- **建议留：** 走了的这条——库体检那一格不必等纠正界面。
- **谁来裁：** 收尾
- **状态：** settled（2026-09-21，票 `gui-looks-like-the-design/29` 收了）——票 29 照读那一处，
  没另立一份判据：「同结构的另有 N 处」那个数取的是核心库按种类分的那一格
  （`ShapingDoubtSummary::by_kind`，票 27 留着、此前没有读者的那一样），屏上说的是
  「列在库体检的『成型存疑』里——逐处确认，这一下只改这一处」。
  票 29 这一趟只往那条判据上加了一句「**已经自成一个变体的那一份不算**」
  （拆开过的那一处不再存疑，与「人工纠正过的多碟不再存疑」同一条道理）。

### Q952 — 重复拷贝：词表写「同一个内容锚」，实现与屏上是「同名且同大小」

- **裁决（第五轮收口，2026-09-23）：** **岔** —— 裁定：词表改成实际口径「同名且同大小」（2026-09-23 拿主意的人）。CONTEXT.md「重复拷贝」条已就地改。

- **来自：** 票 `gui-looks-like-the-design/27`（拿主意的人 2026-09-15 答：屏上照实写「同名且同大小」，加一句「动手前请自己核一眼」；词表与实现不一致记挂单）
- **类别：** 两份东西矛盾
- **在哪：** `CONTEXT.md`「重复拷贝」条；`crates/core/src/scan/aggregate.rs` 的 `record_duplicate_candidate`（键是「字节数 + 小写文件名」，挂账 D3）；`crates/core/src/report/duplicates.rs` 模块文档
- **为什么没停线：** 屏上怎么写已经答了；词表跟实现走还是实现跟词表走，不在这张票里定。
- **这张票实际做了什么：** 核心库的判据与词表都没动；界面照实写判据（那一格等其余岔路口答完再画）。
- **另一条路：** A 词表改成「同名且同大小」照实现；B 实现改成按内容锚判（要读字节或借识别那一侧的哈希）。
- **建议留：** A 眼下就改（词表与代码互顶时文档是决定，但顶不动时改文档本身，`docs/agents/domain.md`）；B 另开一张票。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q953 — 重复拷贝明细一组横跨几个平台时，平台那一列怎么写

- **裁决（第五轮收口，2026-09-23）：** **记** —— 已裁维持顿号并列。已落地：票 gui-looks-like-the-design/27（health::duplicates_ui）

- **来自：** 票 `gui-looks-like-the-design/27`（拿主意的人 2026-09-15 答：明细四列照稿，核心库给每组补名字与平台；跨平台列成岔路口，先按顿号并列做）
- **类别：** 规格没说
- **在哪：** `romcat_core::report::DuplicateGroupStats::platforms`（核心库交按名排的平台列表）；界面明细表平台那一列
- **为什么没停线：** 先按顿号并列，改一处。
- **这张票实际做了什么：** 核心库每组交出名字（头一份的文件名，大小写照原样）与平台列表（没认出平台的是顶层目录名，库根下散文件是「（平台未知）」）；界面按「、」并列。
- **另一条路：** A 顿号并列全部；B 只写第一个加「等 N 个」；C 写「跨平台」、悬停看全部。
- **建议留：** A；平台超过三个时退到 B。
- **谁来裁：** 拿主意的人
- **状态：** 已裁：顿号并列，维持现状；平台那一格放不下时尾部截断加悬停（拿主意的人 2026-09-15 答）。落在 `health::duplicates_ui` 平台那一格（`cell` 截断自带悬停）

### Q954 — 成型存疑「一个目录被当成一个变体」认哪几种文件是各自独立的内容

- **裁决（第五轮收口，2026-09-23）：** **记** —— 两条别路各有明确误报/漏报，现状居中。记下，无事可做

- **来自：** 票 `gui-looks-like-the-design/27`（立判据时）
- **类别：** 规格没说
- **在哪：** `crates/core/src/shape/doubt.rs` 的 `independent_content`
- **为什么没停线：** 判据里的一个参数，改一处，单元测试跟着改。
- **这张票实际做了什么：** 透明容器、压缩镜像、`.iso`、清单里只可能属于某一个平台的扩展名算；`.bin`、`.sfo` 不算（PSV 转储根下的 `eboot.bin`、PS3 的 `PARAM.SFO` 是转储自己的数据）；分卷非入口段不另算。
- **另一条路：** A 凡是内容（含全部裸文件）都算——转储根下的 `.bin` 会误报；B 只认透明容器与压缩镜像——根下躺着几个 `.iso` 的合集认不出来。
- **建议留：** 走了的这条。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q955 — 附属文件落单认哪几种存档，光盘记忆卡算不算

- **裁决（第五轮收口，2026-09-23）：** **记** —— 记忆卡金手指算进来全是误报。记下，无事可做

- **来自：** 票 `gui-looks-like-the-design/27`（立判据时）
- **类别：** 规格没说
- **在哪：** `crates/core/src/classify.rs` 的 `SAVE_EXTENSIONS`
- **为什么没停线：** 一张表。
- **这张票实际做了什么：** `sav srm eep fla sra dsv rtc`；`.mcr` / `.mcd` 记忆卡不算（一张卡装几个游戏，不跟某一个主文件同名）；金手指不算。
- **另一条路：** 记忆卡、金手指（`.cht`）一起算。
- **建议留：** 走了的这条——那几种与主文件同名不是约定，算进来全是误报。
- **谁来裁：** 拿主意的人
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q956 — 非游戏资产那一格只数平台目录里的，未纳入管理的目录下的 `bios/` 不算

- **裁决（第五轮收口，2026-09-23）：** **记** —— 已裁并复裁落地。已落地：票 gui-looks-like-the-design/27（2026-09-20 改回）

- **来自：** 票 `gui-looks-like-the-design/27`（非游戏资产补数时）
- **类别：** 规格没说
- **在哪：** `crates/core/src/scan/aggregate.rs` 的 `Aggregate::record_file`（`placement.in_scope()` 那一闸）；`report::NonGameAssetSummary`
- **为什么没停线：** 一道闸，改一处。
- **这张票实际做了什么：** 判据照旧只在 `classify::non_game_asset`；报告只数落在平台目录里的文件（稿上那一格小字「入库但不导出」：未纳入管理的不入库）。
- **另一条路：** 全库都数，未纳入管理的目录里的也算。
- **建议留：** 走了的这条。
- **谁来裁：** 拿主意的人
- **状态：** 已裁：只数平台目录里的，维持 2026-09-15 的口径（拿主意的人 2026-09-20 复裁）。中间 2026-09-16 有另一个会话把那道 `placement.in_scope()` 的闸拆掉、改成整库都数（提交 `5d1fa0b`，理由写的是 ADR-0011 修订），2026-09-20 照裁定改回，单元测试的名字与期望值一并回到 2 个 / `512 KiB + 2 KiB`

### Q957 — 库体检「疑似同一作品」那一格点进去落到浏览屏「整理建议」，等票 17 接上

- **裁决（第五轮收口，2026-09-23）：** **记** —— 票 17 已收，那一格接上整理建议。已落地：票 gui-looks-like-the-design/17

- **来自：** 票 `gui-looks-like-the-design/27`（拿主意的人 2026-09-15 答岔路口 9）
- **类别：** 规格没说
- **在哪：** `crates/gui/src/health.rs` 的 `SAME_WORK` 那一格（`face` 与 `body_ui` 点了那一格那一支）；票 27 验收最后两条；票 17
- **为什么没停线：** 判断在票 17，那张票还没开工；浏览屏也还没有「整理建议」可落。
- **这张票实际做了什么：** 那一格只画「—」；识别没跑完写「识别完成后才有」，跑完写「暂时还给不出这一项」；点进去不跳屏、不开明细，只在库体检那一块里说一句。票 17 票面末尾追加了「收挂单 `Q957`」。
- **另一条路：** 点进去跳到浏览屏（不带筛选），在那一屏上说一句。
- **建议留：** 走了的这条——跳过去一个建议都看不到，比留在原地说一句更让人摸不着头脑。
- **谁来裁：** 收尾
- **状态：** settled（第五轮收口，2026-09-23，见顶上裁决）
### Q958 — 体检明细弹层页脚：「关闭」靠左、「导出清单…」靠右，设计稿是反过来摆的

- **裁决（第五轮收口，2026-09-23）：** **记** —— 已裁照稿并落地。已落地：票 gui-looks-like-the-design/27（dialog::Footer::dismiss_on_right）

- **来自：** 票 `gui-looks-like-the-design/27`（明细弹层照稿接进弹层框架时）
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/health.rs` 的 `Section::dialog_ui`（`Footer::new(Button::new("关闭", …)).button(Button::new(EXPORT, …))`）；设计稿 `DLG.health` 的 `foot`（左边幽灵按钮「导出清单…」，右边主按钮「关闭」）；弹层框架 `crate::dialog`（票 04 定：退出那一颗靠左，Esc 等于按它）
- **为什么没停线：** 两个按钮的字与动作都照稿，差的只是左右；改哪一边都只动这一处或弹层框架的一个新写法。
- **这张票实际做了什么：** 照弹层框架摆：「关闭」靠左（Esc 等于按它），「导出清单…」靠右；「关闭」不是主按钮。
- **另一条路：** A 照稿：弹层框架加一种写法——退出那一颗画在右边、当主按钮，左边摆一颗不是退出的；B 现状。
- **建议留：** A——稿上这一层没有「往前走」的那一颗，关掉就是这一层唯一的出口，放在右边当主按钮读得顺；框架加的那一种写法以后「快捷键表」这类只读弹层也用得上。
- **谁来裁：** 拿主意的人
- **状态：** 已裁：照稿——「导出清单…」幽灵按钮在左、「关闭」主按钮（填色）在右；弹层框架加一种写法（`dialog::Footer::dismiss_on_right`、`Button::ghost`），已有弹层照旧（拿主意的人 2026-09-15 答）

### Q959 — 体检明细列多少：重复拷贝那张表只摆前 50 组；其余几格照报告里的样例上限（每类 10 个），导出清单也只有这些

- **裁决（第五轮收口，2026-09-23）：** **记** —— 已裁 A 并落地。已落地：票 gui-looks-like-the-design/27（report::duplicates）

- **来自：** 票 `gui-looks-like-the-design/27`（明细弹层与导出清单）
- **类别：** 规格没说
- **在哪：** `crates/gui/src/health.rs` 的 `DUPLICATE_ROWS`；核心库 `scan::aggregate::Limits::max_examples`（每类样例 10 个）、`report::TOP_UNMAPPED_DIRS`（未纳入管理的目录列 15 个）；`HealthReport::render_finding`
- **为什么没停线：** 数都照实说（「另有 N 组」「另有 N 个没列出」），改的是上限，不是判据。
- **这张票实际做了什么：** 重复拷贝：表里摆可腾出空间最多的前 50 组，底下说另有几组；导出的是完整明细（每一组、记下的每一份，与命令行同一份字节）。其余几格：弹层与导出都只有报告里留下的样例，底下说另有几个没列出。
- **另一条路：** A 体检那一趟把 `max_examples` 放开（比如一万），其余几格的明细与清单也列全——报告 JSON 跟着变大；B 现状；C 重复拷贝那张表按滚动只画看得见的几行、列全部。
- **建议留：** A 只放开体检那一趟（命令行 `romcat report` 照旧 10 个），「明细能导出成一份清单」才名副其实；C 暂不做。
- **谁来裁：** 拿主意的人
- **状态：** 已裁：A，只放开「重新体检」那一趟，其余几格列全、导出也全，命令行照旧；弹层里行多了照稿用虚拟化列表，底下「另有 N 个」照实写；重复拷贝表照稿，底下写「另有 N 组 · 按可腾出的空间从大到小排列」（拿主意的人 2026-09-15 答）
- **边界（拿主意的人 2026-09-20 定）：** 「与命令行同一份字节」**只在点过「重新体检」之后成立**。扫完一个根交回的那一份用扫描的默认上限（每组 10 条路径），一组超过 10 份时导出的清单照实写「另有 N 份没记下路径」；扫描那一趟**不放开**（放开要在每次扫描的统计里多占与内容文件数同量级的内存）。票面、`crates/gui/src/health.rs` 的模块文档与 `Section::export_picked` 的文档都写明了这条边界。
- **收尾（2026-09-20）：** 「导出也全」当时并没有真的全：`Limits::FULL_DUPLICATE_PATHS_PER_GROUP` 是一万条的有界索引，一组超过一万份就漏掉第 10001 份路径（2026-09-16 那一轮把这条记成 P1）。它给的理由「断点里存着这份索引」不成立——断点（`scan::checkpoint::Checkpoint`）里只有没扫完的目录与耗时，统计一律由 `Catalog::aggregate` 从中立库按键顺序折出来。这一趟把那道闸撤了（常量留着，值改成不设上限），命令行与界面共用同一条路、同一份字节；测试 `report::duplicates::tests::组内份数超过原先那道一万条的上限也写全`

### Q960 — 体检那几格的小字与设计稿不一样的两句：不可读、疑似同一作品

- **裁决（第五轮收口，2026-09-23）：** **记** —— 已裁并落地。已落地：票 gui-looks-like-the-design/27

- **来自：** 票 `gui-looks-like-the-design/27`（八格照稿画小字时）
- **类别：** 两份东西矛盾
- **在哪：** `crates/gui/src/health.rs` 的 `face`；设计稿 `HEALTH`
- **为什么没停线：** 一句话的措辞，改一处。
- **这张票实际做了什么：** 不可读那一格稿上是「文件名拿得到，内容读不出来」，词表「不可读」说的是**元数据**读不到（「内容读不出来」是穿不透那一类），照词表写「文件名拿得到，元数据读不到」，明细说明同理。疑似同一作品识别跑完之后写「暂时还给不出这一项」（岔路口 9 让写一句实话，措辞是这张票定的）。成型存疑那一格小字照稿「多碟没合在一起、目录拆错」没改——「目录拆错」泛指目录成型错了，与判据「一个目录被当成一个变体」不矛盾。
- **另一条路：** 不可读照稿写「内容读不出来」；疑似同一作品换一句（比如「还没有判断这一项」）。
- **建议留：** 走了的这条。
- **谁来裁：** 拿主意的人
- **状态：** 已裁：不可读小字照词表写「文件名拿得到，元数据读不到」；疑似同一作品识别后写「暂时还给不出这一项」；成型存疑小字照稿留「多碟没合在一起、目录拆错」（拿主意的人 2026-09-15 答）
