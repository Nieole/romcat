# ROM 元数据自动化

把一批以 Pegasus 方式维护的模拟器 ROM 资源，从手工维护变成自动识别、自动刮削、可在主流前端格式间互转的库。

## Language

### 内容层级

**作品**:
跨平台、跨地区的同一个游戏概念，如"幻想传说"。刮削来的简介、封面默认挂在这一层。
_Avoid_: 游戏、Game、Title

**发行版**:
某个平台、某个地区的一次官方发行，通常对应 No-Intro / Redump 数据库里的一条记录。
_Avoid_: 版本、Version、ROM

**变体**:
磁盘上一份实际可玩的东西。原版转储、民间汉化版、英化版、UnDUB 版、同人移植与 homebrew 都是变体。变体**通常**指向它所基于的发行版；同人移植与 homebrew 没有任何官方发行版，直接挂在作品下——**没有发行版链接这件事本身就告诉识别管线：不要拿它去撞 DAT**。一个变体不等于一个文件——它可以是一个主文件加若干附属文件，也可以是一整个目录。
_Avoid_: Hack、补丁版、Dump、ROM 文件

**官中版**:
原厂发行的官方中文版本，它的译名是权威译名。**在有独立序列号的卡带与光盘世代**（PS1 的 `SLPS`、PS3 的 `BCAS`），它是一个独立的**发行版**，和日版、美版平级，能被官方数据库精确命中。**在数字世代**（Switch 上港服与美服 69.4% 共用同一 TitleID），中文只是同一发行版的**语言属性**，不另成发行版。
_Avoid_: 中文版、官方汉化

**汉化版**:
民间对某个发行版打补丁产生的中文版本，是一个**变体**。改过字节，对不上任何官方数据库。
_Avoid_: 中文版、汉化补丁

**首选变体**:
一个作品收敛成前端条目后，默认启动的那一个。规则是 汉化 > 官中 > 日版 > 其他，可被裁决覆盖。它与标题来源无关——标题始终优先取官中的官方译名。
_Avoid_: 默认版本、主文件

**平台**:
变体所属的硬件系统。用户的资源已按平台分目录，所以平台由目录给出，是扫描的输入；但目录只是强先验，文件内容可以推翻它。
_Avoid_: 系统、主机、机种

**合集**:
用户自定义的一组游戏，与平台正交。「我通关过的」「适合双人玩的」是合集，「SFC」是平台。当前库里两者恰好重合，模型上必须分开。
_Avoid_: 分类、Collection、目录

**附属内容**:
真正的追加内容（剧情包、曲包、角色包），归属某个作品、有自己的名字与元数据，但不能独立运行。入库但不导出为前端条目。判定依据是能否独立运行——XBLA、WiiWare、VC、DSiWare、minis 都是普通游戏，不是附属内容。
_Avoid_: DLC、追加包、扩展

**补丁**:
把一个变体变换成另一个变体的指令文件，如汉化补丁、修正补丁。归属某个作品或发行版，自己不可运行。入库但不导出为前端条目，识别管线要认出它并跳过 DAT 匹配——拿补丁去撞 DAT 必然落空，会一路掉到模型推断白烧一遍。
_Avoid_: 汉化包、补丁包、DLC

**非游戏资产**:
磁盘上必须存在、模拟器需要、但本身不是游戏的东西，如 MAME 的 BIOS set 与 device set。入库但永不导出。
_Avoid_: 系统文件、依赖

**成型规则**:
某个平台声明的、把磁盘上散落的文件聚成变体的办法。规则会出错，所以人工纠正聚合结果是一等公民功能。
_Avoid_: 扫描规则、分组策略

**主文件**:
一个变体里用来代表它、也用来交给模拟器启动的那个文件，如 `.cue`、`.iso`、`PS3_GAME` 所在目录。其余是附属文件。
_Avoid_: 入口文件、启动文件

**透明容器**:
`.zip` / `.7z` / `.rar` / `.zst` 这类只是包装、不改变内容身份的归档。识别要看内部的字节，容器本身的哈希毫无意义。分卷压缩的多个分卷合起来才是一个容器。**不是每一种都穿得透**——zst 的内部构成只能靠完整解压读出来，代价差 500–900 倍（ADR-0014）。
_Avoid_: 压缩包、归档

**裸文件**:
既不是透明容器也不是压缩镜像，直接以原始形态躺在磁盘上的内容文件。
_Avoid_: 原始文件、未压缩文件

**内部资源**:
一个变体内部的文件，本身没有独立身份——目录树转储里的音频、贴图、封包数据都属此类。它们不是变体、不是附属内容，识别管线不为它们产生候选。
_Avoid_: 游戏数据、资源文件、未归类

**穿透**:
**零解压**读出**透明容器**内部构成这件事——内部文件的名字、大小与可得的校验和。zip 与 7z 把这张表明文存在容器头里，所以穿得透；**zst 没有这个东西**，同样这三样只能靠完整解压读出来，产出的形状不变（校验和那一栏为空），变的只是代价。
_Avoid_: 解包、解压、展开

**穿不透**:
容器文件本身读得到，但内部构成读不出来（格式不认、需要密码、编解码器不支持、缺外部字典）。**它与不可读是平行的两件事，不是同一件**：不可读说的是文件的元数据取不到，穿不透说的是文件好好的、只是里面看不进去。**也与「还没读过」不是同一件**：那说的是压根没试（zst 要 `--zst` 才读）。三者的处置完全不同，混为一谈会让扫描做出错误的判断。
_Avoid_: 解压失败、损坏、不可读

**压缩镜像**:
`.chd` / `.cso` / `.pbp` / `.rvz` 这类格式。它**不是**包装——它就是变体本身的形态，模拟器直接读它。绝不要把它当容器去穿透解包。
边界要看**模拟器读不读得了**：`.nsz` / `.xcz` 虽然也是「压缩过的镜像」，但没有任何 Switch 模拟器读得了它们，识别读它们的路子又与未压缩的一模一样，所以归**裸文件**而不是这里。
_Avoid_: 压缩包、镜像文件

**中立库**:
工具自己的事实来源，能表达所有前端格式的并集。Pegasus 等前端的元数据文件只是它的导入源与导出目标。
装的全是**工具自己算出来的**东西，因此**整份可再生**——扫描、成型、识别、刮削全能重跑，
结果一模一样。所以它的结构一变就让用户删库重扫，那是省下一整套迁移代码的便宜买卖。
**每个主库一份。** 不可再生的东西一旦放进来，这条就重新失效——那时该问的是「它是不是本该住在[[沉淀库]]」。
_Avoid_: 数据库、缓存、索引

### 两个阶段

**识别**:
判定一个磁盘文件对应哪个发行版、是不是变体。输入是文件本身（哈希、头部、内部序列号），不是文件名。
_Avoid_: 匹配、识别游戏

**刮削**:
在识别结论的基础上，去数据源取标题、简介、封面等元数据与媒体。识别错了，刮削再准也是错的。
_Avoid_: 抓取、爬取、Scrape

**收敛**:
导出时把同一作品的多个变体合并成前端里的一个条目。当前策略是作品级收敛：一个条目、多个文件、默认启动首选变体。
_Avoid_: 合并、去重、聚合

**介质**:
主库所在的存储，按**延迟特征**分档而不是按接口分。同一块盘的不同区域可能落在不同档上——扫描前探一次，据此定并发。
_Avoid_: 硬盘、磁盘、设备

### 识别的产物

**候选**:
识别过程为一个变体给出的一个可能的发行版，附带置信度与依据。一个变体可以有多个候选。
_Avoid_: 匹配结果、猜测

**依据**:
一条候选是怎么来的——命中了哪个数据库的哪条记录、匹配上了哪个字段。没有依据的候选无法事后复核。
_Avoid_: 来源、理由、evidence

**待确认队列**:
置信度不足以自动通过的候选的集合，由人工批量裁决。它是工具的主界面。
_Avoid_: 任务列表、待办、审核列表

**裁决**:
人工在候选之间做出选择，或者判定"都不对"。裁决结果会反过来成为后续识别的依据。
_Avoid_: 确认、审核、标注

**平台标识**:
平台自己给一次发行印的号。卡带与光盘世代是**序列号**（PS1 的 `SLPS-02170`、PSP 的 `ULJM-05800`、卡带丝印的游戏码），数字世代是 **TitleID**。两者不可混为一谈——那正是 ADR-0019 那道世代裂缝的两侧。
_Avoid_: 游戏 ID、编号

**壳子**:
同一张逻辑光盘外面套的那一层——`.rvz` / `.wbfs` / `.cso` / `.pbp`。剥掉壳子看见的是同一张盘，所以它不改变内容的身份。
_Avoid_: 封装、外层、容器

**锚点**:
一条结论挂在三层内容层级的哪一层。刮削结论挂在作品名或变体的键上；裁决挂在**内容锚**（那份字节）或**路径锚**（那个位置）上。**换了锚点就是换了一条结论管多大范围。**
_Avoid_: 主键、挂载点、key

**汉化组**:
做出某个**汉化版**的那个团体。自动识别只做到发行版级（ADR-0008），汉化组与第几版靠文件名启发加人工**裁决**补齐。
_Avoid_: 汉化团队、译者

**还没识别**:
一个变体连识别都还没跑过。它既不是「未命中」（那是撞过没撞上，是结论），也不是「无判据」（那是拿不到可撞的东西）。三者混在一起，命中率就失真。
_Avoid_: 未处理、待识别

**沉淀库**:
**用户亲手定的东西**住的地方：裁决、收藏、人工纠正。把文件哈希直接钉到发行版、汉化组与版本上，
补的正是 TOSEC 与 No-Intro 覆盖不到的中文汉化部分，可导出分享。
它与[[中立库]]唯一的区别是**不可再生**——删掉就没了。因此它**全局一份**（几块盘共用）、
键是**内容锚**（换机、改名、挪目录都还认得出）、走**顺序迁移永不要求删库**。
_Avoid_: 用户数据库、自定义映射

**剥离规则**:
把文件名里的汉化组名、版本后缀、以及「完美版」「最终版」「中文版」这类噪音词剥掉的办法。是配置不是代码，用户能补充自己遇到的模式。
_Avoid_: 清洗规则、正则

**正题**:
一个文件名剥完噪音之后剩下的那串字，拿去做模糊匹配的就是它。
_Avoid_: 净名、主标题、干净名字

**中文离线源**:
本机那份中文条目数据，**撞上之后整套字段都拿得到**：中文名、别名、简介、类型、开发商、发行商、年份。
离线使用，不消耗任何在线配额。**图不在里面**——那是**在线源**唯一不可替代的地方。
_Avoid_: 中文数据库、Bangumi、中文名索引

### 格式互转

**适配器**:
把某一种前端格式与中立库对接的自包含模块，负责该格式的读、写与字段映射。
_Avoid_: 转换器、插件、驱动

**底本**:
导入前端元数据时保存下来的那份**原文件完整副本**。**无损往返**靠它做基线——中立模型表达不了或格式会吞掉的部分，导出时从它还原。
_Avoid_: 快照、原件、备份

**能力档位**:
一个适配器对自己能做到什么程度的显式声明：只读、只写、双向、无损往返。对用户可见。
_Avoid_: 支持级别、功能等级

**无损往返**:
把一个格式的文件读进中立库再写回去，结果与原文件等价——包括中立模型建模不了的未知字段、扩展键与注释。
_Avoid_: 双向同步、完整支持

### 标题

**标题集合**:
一个作品或发行版的全部叫法，每条带语言、地区、来源与类型（官方名 / 译名 / 别名 / 汉化组自取的名）。中立库里标题永远是集合，不是单值。
_Avoid_: 名称、别名列表

**显示标题**:
导出到某个前端时实际写进去的那一个标题。按中文译名 > 官方英文名 > 日文原名 > 文件名的优先级选出。
_Avoid_: 主标题、正式名称

**排序标题**:
供前端排序用的标题，与显示标题分开。中文显示标题按码位排等于乱排，所以要另行生成（拼音首字母或官方英文名）。
_Avoid_: sort key、索引名

### 媒体

**媒体池**:
按内容哈希存放全部封面、截图、视频的中央仓库，位于本机。各前端的媒体目录由它铺设而来，文件名不是媒体的主键。
_Avoid_: 图片目录、资源库、缓存目录

### 子库与同步

**主库**:
保存原始形态全部资源的仓储，**只读**。用户从不直接在它上面游玩。
它是**一组根**，不是一个目录：几块盘、几个目录都可以加进同一个主库，扫完收进**同一份中立库**统一管理。
_Avoid_: 库、总库、仓库

**根**:
主库里的一个被扫描的目录。它有自己的**名字**，**变体的键**由「根名 + 相对路径」构成——
所以加一个根不影响已有的键，挪动一个根的位置也只改它自己那一支。
_Avoid_: 扫描目录、库路径、挂载点

**子库**:
从主库挑选一部分导出到某个目标设备（通常是掌机）形成的派生库。持久实体，带自己的选择集、目标路径、前端格式、格式转换规则与容量上限。
_Avoid_: 导出目录、设备库、分库

**选择集**:
一个子库要包含哪些东西的定义。
_Avoid_: 列表、白名单

**规则**:
选择集里可重放的那部分，按平台、语言、评分、年份、体积、标签等条件描述要什么。主库新增的内容会自动落入符合条件的规则。
_Avoid_: 过滤器、条件、查询

**例外**:
选择集里手动增删的那部分，处理规则表达不了的个人口味。**优先于规则**，永久记住。
_Avoid_: 覆盖、白名单、黑名单

**收藏**:
用户给库里的东西打的一个标记，落**沉淀库**、锚在**内容锚**上（删掉中立库重扫还在、改名挪目录还认得出）。它是**规则的一个维度**（`收藏=是`），与「合集」同一套成员关系——合集是一组自己起名的，收藏是那个默认的一组。**它不是[[用户状态]]**：用户状态是前端里玩出来的历史（游玩次数、通关状态），那个明确不在管理范围内。
_Avoid_: 星标、喜欢、favorite

**维度**:
**规则**能筛的那些面：平台、语言、中文、合集、收藏、类型、作品、年份、评分、体积、开发商、发行商、简介。一条规则是一棵**组**，组里摆着**子句**与更多的组。
_Avoid_: 字段、属性

**子句**:
规则里的一个判断，形如「平台=GB,GBA」「作品^口袋」。运算符有九个：`=` `!=` `~`含有 `^`以…开始 `$`以…结束 `<=` `<` `>=` `>`。
_Avoid_: 条件、表达式

**组**:
规则里一批**子句**（或更多的组）加上它们之间的**连接**。组可以嵌套，写成括号：`平台=GB 且 (作品^口袋 或 类型~RPG)`。**浏览屏上的筛选器就是一棵组**——筛到满意存成子库，它原样变成那个子库的规则。
_Avoid_: 括号、子表达式、条件块

**连接**:
一个**组**里那几项怎么算数，三种：`全部满足`（写 ` 且 `）、`任一满足`（写 ` 或 `）、`都不满足`（写 `都不(…)`）。同一层里不许既写「且」又写「或」——要混就套括号。
_Avoid_: 逻辑运算符、与或非、布尔

**差量预览**:
同步前必须呈现的变更清单：将新增什么、删除什么、各占多少空间、净变化多少。它是同步的前置条件，不是可选功能。
_Avoid_: 预演、dry run、确认页

**清单**:
某个子库上次导出的完整记录。它是增量同步的依据，也是工具在目标设备上的行为边界——清单之外的文件工具一律不碰。
_Avoid_: 快照、索引、记录

**能力档案**:
一个子库所用前端与模拟器核心能吃哪些容器与镜像格式的声明。它决定导出时每个平台转成什么格式。内置预置档案，可手动覆盖。
_Avoid_: 设备配置、兼容性表

**同步**:
把子库的目标状态与目标设备的实际状态对齐的过程。三方对比：主库该有的 / 清单记录的 / 目标上实际有的。
_Avoid_: 导出、推送、拷贝

### 库的状况

**库体检**:
对主库跑一遍只读扫描得到的报告：有什么、多少、什么形态、哪些不该在这。它的覆盖面大于识别的覆盖面——未纳入管理的目录照样出现在报告里，那是人工整理的依据。
_Avoid_: 扫描报告、盘点

**不可读**:
文件名拿得到、但元数据读不到的条目。它既不是「已变」也不是「已删」，是如实记录的第三种状态。
_Avoid_: 损坏、失败、跳过

**遍历**:
走完一次主库留下的那条**记录**——什么时候走的、走了多久、看见多少。与「库里有什么」分开记。
**扫描**是那个**动作**，遍历是它的记录；命令、模块、选项一律叫扫描，落库的那一行才叫遍历。
_Avoid_: 跑一遍

**扫描代号**:
一次遍历的编号。中断续跑沿用同一个，收尾时靠它判断哪些条目「这次没见到」。
_Avoid_: 扫描 ID、批次号

**基线**:
上一次遍历记下的那份三元组，增量判断拿它作比。
_Avoid_: 快照、上次结果、缓存

**重复拷贝**:
**同一份内容**（同一个内容锚）存在多份，同一个主库内或跨主库都算。只发现并报告，绝不自动删除。
**同一个作品的两个不同转储不算重复拷贝**——那是两个**变体**，删掉任何一份都是真丢东西。
_Avoid_: 冗余、副本、去重

### 工具自己的状态

**现场**:
一份开好的中立库、一份**沉淀库**、以及这份主库在路径锚里叫什么名字——三样捆在一起才够干活。
_Avoid_: 上下文、会话、环境

**DAT 条目**:
一份 DAT 里的一条记录。它通常对应一个**发行版**，但不总是——TOSEC 会把同一次发行的不同转储各记一条，MAME 的 software list 记到芯片一级。**「一条 DAT 条目等于一个发行版」这个假设会出错。**
_Avoid_: DAT 记录、game

### 边界之外

**用户状态**:
收藏、游玩次数、游玩时长、通关状态——用户在前端里玩出来的历史。明确不在工具管理范围内，导出时只从快照原样搬运，绝不生成、绝不覆盖。
_Avoid_: 游玩数据、个人数据
