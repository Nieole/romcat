# ROM 元数据自动化

把一批以 Pegasus 方式维护的模拟器 ROM 资源，从手工维护变成自动识别、自动刮削、可在主流前端格式间互转的库。

## Language

### `_Avoid_` 是什么意思

每个词条底下那行 `_Avoid_` 列的是**这个概念的近义词**：**别拿那些词去指这个概念**。
它**不是**「这个词元禁止出现在仓库里」——按后一种读法，这个仓库有约两千处「违规」，
其中包括一个用户天天敲的旗标（2026-09-10 实测：236 个禁词、标识符命中 2,581 处声明点、
字面量命中 4,842 处，假阳占七成上下）。

**三类不算撞 `_Avoid_`：**

1. **外部格式与硬件的专名。** DAT 里的 `<game>` / `<rom>` 元素、`gamelist.xml`、Pegasus 的
   `collection:` 字段、`GameBoy`、卡带头里的 game code、Bangumi 的 dump 文件——
   它们指的就是那样东西本身，压根没在指**作品**或**变体**。
2. **已经公开的旗标名、库列名与协议字段。** `--workspace`、`--dry-run`、`evidence` 那几列。
   改它们是**破坏性变更**，而 `_Avoid_` 是命名指导、不是破坏性变更的授权。
   新起的名字照旧躲开这些词。
3. **那个词恰好是另一条正名的一截。** 「容器」在**透明容器**里、「库」在**主库** / **中立库** /
   **子库** / **沉淀库**里、「拷贝」在**重复拷贝**里、「确认」在**待确认队列**里——
   整词是正名时不算撞。**散文里当普通词用也不算**（`fn 重复拷贝明细也不许写进主库` 是句子，
   不是命名）。

⚠️ **这三类是豁免，不是漏洞。** 拿不准的时候问一句：**我是在给这个概念起名吗？**
是，就躲开 `_Avoid_`；不是（在引用一个外部名字、在说一句话），就照实写。

### `_Gate_` 是词表对门禁的授权

有些词条底下还有一行 `_Gate_`。它是 `_Avoid_` 的**子集**，意思是
**「这几个词严到可以让门禁当场报红」**——不是第二份禁词表。

一个词进 `_Gate_` 要同时满足三条：① **不是任何正名的一截**；② **不是外部格式或硬件的专名**；
③ **在散文里不会当普通词用**。三条缺一条就只留在 `_Avoid_` 里——那一行照旧管着命名，
只是没有机器替人念。

**明确没进 `_Gate_` 的，连理由一起记着**，免得日后有人以为是漏了：

- `key` `evidence` `workspace` `dry run` —— **公开的旗标名、库列名与协议字段**（上面第 2 类）。
  改它们是破坏性变更。
- `game` `Game` `Title` `Version` `ROM` `Collection` `Dump` `favorite` `shape` ——
  **外部格式与硬件的专名**（第 1 类）：DAT 的 `<game>` / `<rom>`、Pegasus 的 `collection:`、
  `SCHEMA_VERSION`、Bangumi 的 dump、`GameBoy`。
- `job` —— 一条正则分不开 `job_of`（真该拦）与 `default_jobs`（指并发度，按上面第 1 类豁免）。
  **它是这条限制最诚实的一处代价**：2026-09-10 那次真撞上的正是 `job_of`，而这道门禁拦不住它。
- 光板 `库` `目录` `游戏` `结果` `状态` —— 第 3 类：它们是别的正名的一截，也在散文里当普通词用。

**门禁只扫新写的代码**（相对 `main` 的 merge base 以来的全部改动，含未提交的），
不扫存量、不扫注释、不扫 `.scratch/`。理由与实测数在
`.scratch/machine-checks-premises/grill.md` 里。

### 内容层级

**作品**:
跨平台、跨地区的同一个游戏概念，如"幻想传说"。刮削来的简介、封面默认挂在这一层。
_Avoid_: 游戏、Game、Title

**发行版**:
某个平台、某个地区的一次官方发行，通常对应 No-Intro / Redump 数据库里的一条记录。
_Avoid_: 版本、Version、ROM

**变体**:
磁盘上一份实际可玩的东西。原版转储、民间汉化版、英化版、UnDUB 版、同人移植与 homebrew 都是变体。变体**通常**指向它所基于的发行版；同人移植与 homebrew 没有任何官方发行版，直接挂在作品下——**没有发行版链接这件事本身就告诉识别管线：不要拿它去撞 DAT**。一个变体不等于一个文件——它可以是一个主文件加若干附属文件，也可以是一整个目录。
_Avoid_: Hack、补丁版、Dump、ROM 文件

**官中版**:
原厂发行的官方中文版本，它的译名是权威译名。**在有独立序列号的卡带与光盘世代**（PS1 的 `SLPS`、PS3 的 `BCAS`），它是一个独立的**发行版**，和日版、美版平级，能被官方数据库精确命中。**在数字世代**（Switch 上港服与美服 69.4% 共用同一 TitleID），中文只是同一发行版的**语言属性**，不另成发行版。
_Avoid_: 中文版、官方汉化

**汉化版**:
民间对某个发行版打补丁产生的中文版本，是一个**变体**。改过字节，对不上任何官方数据库。
_Avoid_: 中文版、汉化补丁

**首选变体**:
一个作品收敛成前端条目后，默认启动的那一个。规则是 汉化 > 官中 > 日版 > 其他，可被裁决覆盖。它与标题来源无关——标题始终优先取官中的官方译名。
_Avoid_: 默认版本、主文件

**平台**:
变体所属的硬件系统。用户的资源已按平台分目录，所以平台由目录给出，是扫描的输入；但目录只是强先验，文件内容可以推翻它。
_Avoid_: 系统、主机、机种

**合集**:
用户自定义的一组游戏，与平台正交。「我通关过的」「适合双人玩的」是合集，「SFC」是平台。当前库里两者恰好重合，模型上必须分开。建法是**在浏览屏上多选一批、给它起个名字**——一个合集就是它那些成员，成员一条不剩它就没了。成员关系落**沉淀库**、锚在**内容锚**上，与[[收藏]]同一套（收藏是那个默认的一组）；中立库里那两张表是它的**投影**，重扫后照沉淀库重建。
_Avoid_: 分类、Collection、目录

**附属内容**:
真正的追加内容（剧情包、曲包、角色包），归属某个作品、有自己的名字与元数据，但不能独立运行。入库但不导出为前端条目。判定依据是能否独立运行——XBLA、WiiWare、VC、DSiWare、minis 都是普通游戏，不是附属内容。
_Avoid_: DLC、追加包、扩展

**补丁**:
把一个变体变换成另一个变体的指令文件，如汉化补丁、修正补丁。归属某个作品或发行版，自己不可运行。入库但不导出为前端条目，识别管线要认出它并跳过 DAT 匹配——拿补丁去撞 DAT 必然落空，会一路掉到模型推断白烧一遍。
_Avoid_: 汉化包、补丁包、DLC

**非游戏资产**:
磁盘上必须存在、模拟器需要、但本身不是游戏的东西，如 MAME 的 BIOS set 与 device set。入库但永不导出。
_Avoid_: 系统文件、依赖

**成型规则**:
某个平台声明的、把磁盘上散落的文件聚成变体的办法。规则会出错，所以人工纠正聚合结果是一等公民功能。
_Avoid_: 扫描规则、分组策略

**主文件**:
一个变体里用来代表它、也用来交给模拟器启动的那个文件，如 `.cue`、`.iso`、`PS3_GAME` 所在目录。其余是附属文件。
_Avoid_: 入口文件、启动文件

**透明容器**:
`.zip` / `.7z` / `.rar` / `.zst` 这类只是包装、不改变内容身份的归档。识别要看内部的字节，容器本身的哈希毫无意义。分卷压缩的多个分卷合起来才是一个容器。**不是每一种都穿得透**——zst 的内部构成只能靠完整解压读出来，代价差 500–900 倍（ADR-0014）。
_Avoid_: 压缩包、归档

**裸文件**:
既不是透明容器也不是压缩镜像，直接以原始形态躺在磁盘上的内容文件。
_Avoid_: 原始文件、未压缩文件

**内部资源**:
一个变体内部的文件，本身没有独立身份——目录树转储里的音频、贴图、封包数据都属此类。它们不是变体、不是附属内容，识别管线不为它们产生候选。
_Avoid_: 游戏数据、资源文件、未归类

**穿透**:
**零解压**读出**透明容器**内部构成这件事——内部文件的名字、大小与可得的校验和。zip 与 7z 把这张表明文存在容器头里，所以穿得透；**zst 没有这个东西**，同样这三样只能靠完整解压读出来，产出的形状不变（校验和那一栏为空），变的只是代价。
_Avoid_: 解包、解压、展开

**穿不透**:
容器文件本身读得到，但内部构成读不出来（格式不认、需要密码、编解码器不支持、缺外部字典）。**它与不可读是平行的两件事，不是同一件**：不可读说的是文件的元数据取不到，穿不透说的是文件好好的、只是里面看不进去。**也与「还没读过」不是同一件**：那说的是压根没试（zst 要 `--zst` 才读）。三者的处置完全不同，混为一谈会让扫描做出错误的判断。
_Avoid_: 解压失败、损坏、不可读

**压缩镜像**:
`.chd` / `.cso` / `.pbp` / `.rvz` 这类格式。它**不是**包装——它就是变体本身的形态，模拟器直接读它。绝不要把它当容器去穿透解包。
边界要看**模拟器读不读得了**：`.nsz` / `.xcz` 虽然也是「压缩过的镜像」，但没有任何 Switch 模拟器读得了它们，识别读它们的路子又与未压缩的一模一样，所以归**裸文件**而不是这里。
_Avoid_: 压缩包、镜像文件

**中立库**:
工具自己的事实来源，能表达所有前端格式的并集。Pegasus 等前端的元数据文件只是它的导入源与导出目标。
装的全是**工具自己算出来的**东西，因此**整份可再生**——扫描、成型、识别、刮削全能重跑，
结果一模一样。所以它的结构一变就让用户删库重扫，那是省下一整套迁移代码的便宜买卖。
**每个主库一份。** 不可再生的东西一旦放进来，这条就重新失效——那时该问的是「它是不是本该住在[[沉淀库]]」。
_Avoid_: 数据库、缓存、索引

### 两个阶段

**识别**:
判定一个磁盘文件对应哪个发行版、是不是变体。输入是文件本身（哈希、头部、内部序列号），不是文件名。
_Avoid_: 匹配、识别游戏

**刮削**:
在识别结论的基础上，去数据源取标题、简介、封面等元数据与媒体。识别错了，刮削再准也是错的。
_Avoid_: 抓取、爬取、Scrape

**采法**:
一趟**刮削**怎么采、跑多久，两档：**补缺**（只采还没采过的那些——[[输入指纹]]没变的这一趟不动）与**重采**（绕过输入指纹全部重来）。**它管的是跑多久、花多少配额，不管显示哪个值**——「有值了但我想换一个」是另一件事，不靠重采。
_Avoid_: 刮削模式、扫法、刷新、强制刷新

**输入指纹**:
一个源对一个**锚点**这一轮的全部输入折成的那一串字。与上一轮一样，这一趟就**不再采它**，所以它**必须盖住一切会改变结果的输入**——漏了一样，那一样变了也不会重采。它盖不住的只有一样：源自己的读法变了，那正是**重采**的出口。
_Avoid_: 缓存键、校验和、哈希、版本号

**收敛**:
导出时把同一作品的多个变体合并成前端里的一个条目。当前策略是作品级收敛：一个条目、多个文件、默认启动首选变体。
_Avoid_: 合并、去重、聚合

**介质**:
主库所在的存储，按**延迟特征**分档而不是按接口分。同一块盘的不同区域可能落在不同档上——扫描前探一次，据此定并发。
_Avoid_: 硬盘、磁盘、设备

### 识别的产物

**候选**:
识别过程为一个变体给出的一个可能的发行版，附带置信度与依据。一个变体可以有多个候选。
_Avoid_: 匹配结果、猜测
_Gate_: 匹配结果、猜测

**依据**:
一条候选是怎么来的——命中了哪个数据库的哪条记录、匹配上了哪个字段。没有依据的候选无法事后复核。
_Avoid_: 来源、理由、evidence

**待确认队列**:
置信度不足以自动通过的候选的集合，由人工批量裁决。它是工具的主界面。
_Avoid_: 任务列表、待办、审核列表

**裁决**:
人工在候选之间做出选择，或者判定"都不对"。裁决结果会反过来成为后续识别的依据。
_Avoid_: 确认、审核、标注

**依据形状**:
「工具凭什么这么认为」长成什么样。有候选的看它那条候选：出自哪个源、撞的哪份 DAT、什么置信度、按哪套哈希口径、一共几个候选；一条候选都没有的看它的**识别结论**与理由。**待确认队列一级分批的键就是它**——整批通过时人验证的正是那句共同**依据**。
_Avoid_: 分组键、分批键、shape
_Gate_: 分组键、分批键

**批**:
两个意思，靠**装的是什么**分开。**一批变体**是待确认屏一级分批的那一批——一组[[依据形状]]相同的待裁决变体，它还没落任何库；**一批裁决**是一次批量裁决落下的那些裁决，连它们各自盖掉的那条一起——**撤销以它为粒度**，撤掉就把盖掉的那条放回去。两者在按下「整批通过」那一刻一一对应（这一批变体落成那一批裁决），但不是同一个东西。
_Avoid_: 批次、批号、批次号、桶、事务、操作
_Gate_: 批次、批号、批次号

**先放着**:
逐条裁决时把一条推后：「这一条我等会儿再看」。**一个字都不写库**，它仍在待确认队列里，下一轮还会撞见。它不是[[跳过]]——那是一档识别结论。
_Avoid_: 跳过、忽略、略过、稍后再说

**平台标识**:
平台自己给一次发行印的号。卡带与光盘世代是**序列号**（PS1 的 `SLPS-02170`、PSP 的 `ULJM-05800`、卡带丝印的游戏码），数字世代是 **TitleID**。两者不可混为一谈——那正是 ADR-0019 那道世代裂缝的两侧。
_Avoid_: 游戏 ID、编号

**壳子**:
同一张逻辑光盘外面套的那一层——`.rvz` / `.wbfs` / `.cso` / `.pbp`。剥掉壳子看见的是同一张盘，所以它不改变内容的身份。
_Avoid_: 封装、外层、容器

**锚点**:
一条结论挂在三层内容层级的哪一层。刮削结论挂在作品名或变体的键上；裁决挂在**内容锚**（那份字节）或**路径锚**（那个位置）上。**换了锚点就是换了一条结论管多大范围。**
_Avoid_: 主键、挂载点、key

**汉化组**:
做出某个**汉化版**的那个团体。自动识别只做到发行版级（ADR-0008），汉化组与第几版靠文件名启发加人工**裁决**补齐。
_Avoid_: 汉化团队、译者

**还没识别**:
一个变体连识别都还没跑过。它既不是「未命中」（那是撞过没撞上，是结论），也不是「无判据」（那是拿不到可撞的东西）。三者混在一起，命中率就失真。**它也不是[[没有候选]]**——那是另一条轴上的事：识别跑过了，只是一条候选都没有。
_Avoid_: 未处理、待识别、没有候选

**没有候选**:
一个变体识别**跑过了**，却一条候选都没有。它不是「猜得不准」，是**一个字都没说**，因此那一格没有置信度可标。它与[[还没识别]]差着整整一趟识别：那一条连跑都还没跑过，库里连它的**识别结论**都没有。它也不是四档结论里的任何一档——**命中 / 未命中 / 无判据 / 跳过**各自都可能一条候选都没有。
_Avoid_: 还没识别、未识别、零候选、无结果

**跳过**:
一档**识别结论**：这个变体**不该撞 DAT**——补丁，以及没有发行版链接的变体。它不是「撞过没撞上」，混进未命中会把命中率说低。**逐条裁决里推后一条不叫跳过**，那叫[[先放着]]。
_Avoid_: 忽略、排除、不处理、先放着

**沉淀库**:
**用户亲手定的东西**住的地方：裁决、收藏、人工纠正。把文件哈希直接钉到发行版、汉化组与版本上，
补的正是 TOSEC 与 No-Intro 覆盖不到的中文汉化部分，可导出分享。
它与[[中立库]]唯一的区别是**不可再生**——删掉就没了。因此它**全局一份**（几块盘共用）、
键是**内容锚**（换机、改名、挪目录都还认得出）、走**顺序迁移永不要求删库**。
_Avoid_: 用户数据库、自定义映射

**剥离规则**:
把文件名里的汉化组名、版本后缀、以及「完美版」「最终版」「中文版」这类噪音词剥掉的办法。是配置不是代码，用户能补充自己遇到的模式。
_Avoid_: 清洗规则、正则

**正题**:
一个文件名剥完噪音之后剩下的那串字，拿去做模糊匹配的就是它。
_Avoid_: 净名、主标题、干净名字

**中文离线源**:
本机那份中文条目数据，**撞上之后整套字段都拿得到**：中文名、别名、简介、类型、开发商、发行商、年份。
离线使用，不消耗任何在线配额。**图不在里面**——那是**在线源**唯一不可替代的地方。
_Avoid_: 中文数据库、Bangumi、中文名索引

### 格式互转

**导出**:
把中立库写成某个前端能读的元数据，铺在主库上——**只写元数据文件，一个 ROM 都不搬**。写到哪个目录由人指定，而那个目录在语义上是**主库根的替身**：元数据里的 `file:` 相对它自己所在的目录解析，而中立库的键相对主库根，所以把导出来的文件放进主库根，路径直接就对。**整库级、作品级收敛，不挑选**。**人指定的那个目录叫「导出目录」**，别叫「输出目录」——那是这一条 `_Avoid_` 的第一个词，而票与规格已经照它写错过一轮。
**它不是[[同步]]**：同步是挑一批、转格式、搬到目标设备，对齐的是**子库**与那台设备上的实际状态；导出让前端直接对着主库读，没有目标设备，也没有**清单**那条行为边界。
_Avoid_: 输出、写出、生成元数据、推送

**适配器**:
把某一种前端格式与中立库对接的自包含模块，负责该格式的读、写与字段映射。
_Avoid_: 转换器、插件、驱动
_Gate_: 转换器、插件、驱动

**底本**:
导入前端元数据时保存下来的那份**原文件完整副本**。**无损往返**靠它做基线——中立模型表达不了或格式会吞掉的部分，导出时从它还原。
_Avoid_: 快照、原件、备份

**能力档位**:
一个适配器对自己能做到什么程度的显式声明：只读、只写、双向、无损往返。对用户可见。**它答不了「存一趟之后值变成什么样」**——那是**结构性损失**。
_Avoid_: 支持级别、功能等级

**无损往返**:
把一个格式的文件读进中立库再写回去，结果与原文件等价——包括中立模型建模不了的未知字段、扩展键与注释。
_Avoid_: 双向同步、完整支持

**结构性损失**:
把一个值交给某个格式存一趟，这个格式**自己**会把它改成什么样——要由**适配器**逐条声明出来，导出前才看得见。**与能力档位分工不同**：档位答「这个格式能不能双向走、写回去一不一样」，这一条答「走一趟之后值变成什么样」。它挂在**格式**上，与手上这份库里当下有没有这样的值无关；**清单是空的意思是「还没查过」**，不是「这个格式什么都不丢」。
_Avoid_: 有损、丢字段、降级、精度损失、兼容性说明

### 标题

**标题集合**:
一个作品或发行版的全部叫法，每条带语言、地区、来源与类型（官方名 / 译名 / 别名 / 汉化组自取的名）。中立库里标题永远是集合，不是单值。
_Avoid_: 名称、别名列表

**显示标题**:
导出到某个前端时实际写进去的那一个标题。按中文译名 > 官方英文名 > 日文原名 > 文件名的优先级选出。
_Avoid_: 主标题、正式名称

**排序标题**:
供前端排序用的标题，与显示标题分开。中文显示标题按码位排等于乱排，所以要另行生成（拼音首字母或官方英文名）。
_Avoid_: sort key、索引名

### 媒体

**媒体池**:
按内容哈希存放全部封面、截图、视频的中央仓库，位于本机。各前端的媒体目录由它铺设而来，文件名不是媒体的主键。
_Avoid_: 图片目录、资源库、缓存目录
_Gate_: 图片目录、资源库、缓存目录

### 子库与同步

**主库**:
保存原始形态全部资源的仓储，**只读**。用户从不直接在它上面游玩。
它是**一组根**，不是一个目录：几块盘、几个目录都可以加进同一个主库，扫完收进**同一份中立库**统一管理。
_Avoid_: 库、总库、仓库

**主库标识**:
认出「这是哪一个主库」的那个短名。**中立库**的主文件名是它，**路径锚**里那个键也是它。
由人起的那个名字折出来；没起过名字时由主库根的路径折出来（于是换个挂载点就会变——
**那正是该起名字的理由**）。
**它不是给人看的名字。** 人看的那个叫**主库原名**，记在中立库自己的元数据里，
开场屏与报告上印的都是它。两样都在，是因为一个要**稳**（换了挂载点也认得出同一份库），
另一个要**认得出**（人看得懂那是哪块盘）。
_Avoid_: slug、库 ID、主库名

**主库原名**:
人给这个**主库**起的那个名字，建库那一趟落进**中立库**的元数据里；没起过名字时退回主库根的
末级目录名。开场屏、报告、窗口标题上印的都是它。
**它不是标识**——认库靠的是[[主库标识]]，改原名不动任何一条路径锚。
_Avoid_: 库名、显示名、别名

**根**:
主库里的一个被扫描的目录。它有自己的**名字**，**变体的键**由「根名 + 相对路径」构成——
所以加一个根不影响已有的键，挪动一个根的位置也只改它自己那一支。
_Avoid_: 扫描目录、库路径、挂载点

**子库**:
从主库挑选一部分导出到某个目标设备（通常是掌机）形成的派生库。持久实体，带自己的选择集、目标路径、前端格式、格式转换规则与容量上限。
_Avoid_: 导出目录、设备库、分库

**选择集**:
一个子库要包含哪些东西的定义。
_Avoid_: 列表、白名单

**规则**:
选择集里可重放的那部分，按平台、语言、年份、体积等条件描述要什么。主库新增的内容会自动落入符合条件的规则。
**`评分` 这一维立着，但眼下没有源**（中立库里还没有哪个源在写它）——写得出规则，筛不出东西。
**没有 `标签` 这一维**：「汉化」那件事本仓已经有自己的表达（[[中文标记]]那一族），再造一个标签维是把同一件事说两遍。
_Avoid_: 过滤器、条件、查询

**例外**:
选择集里手动增删的那部分，处理规则表达不了的个人口味。**优先于规则**，永久记住。
_Avoid_: 覆盖、白名单、黑名单

**收藏**:
用户给库里的东西打的一个标记，落**沉淀库**、锚在**内容锚**上（删掉中立库重扫还在、改名挪目录还认得出）。它是**规则的一个维度**（`收藏=是`），与「合集」同一套成员关系——合集是一组自己起名的，收藏是那个默认的一组。**它不是[[用户状态]]**：用户状态是前端里玩出来的历史（游玩次数、通关状态），那个明确不在管理范围内。
_Avoid_: 星标、喜欢、favorite

**维度**:
**规则**能筛的那些面：平台、语言、中文、合集、收藏、类型、作品、年份、评分、体积、开发商、发行商、简介。一条规则是一棵**组**，组里摆着**子句**与更多的组。
_Avoid_: 字段、属性

**子句**:
规则里的一个判断，形如「平台=GB,GBA」「作品^口袋」。运算符有九个：`=` `!=` `~`含有 `^`以…开始 `$`以…结束 `<=` `<` `>=` `>`。
_Avoid_: 条件、表达式

**组**:
规则里一批**子句**（或更多的组）加上它们之间的**连接**。组可以嵌套，写成括号：`平台=GB 且 (作品^口袋 或 类型~RPG)`。**浏览屏上的筛选器就是一棵组**——筛到满意存成子库，它原样变成那个子库的规则。
_Avoid_: 括号、子表达式、条件块

**连接**:
一个**组**里那几项怎么算数，三种：`全部满足`（写 ` 且 `）、`任一满足`（写 ` 或 `）、`都不满足`（写 `都不(…)`）。同一层里不许既写「且」又写「或」——要混就套括号。
_Avoid_: 逻辑运算符、与或非、布尔

**差量预览**:
同步前必须呈现的变更清单：将新增什么、删除什么、各占多少空间、净变化多少。它是同步的前置条件，不是可选功能。
_Avoid_: 预演、dry run、确认页
_Gate_: 预演、确认页

**清单**:
某个子库上次导出的完整记录。它是增量同步的依据，也是工具在目标设备上的行为边界——清单之外的文件工具一律不碰。
_Avoid_: 快照、索引、记录

**容量条**:
子库屏上一台设备那张卡上的三段：**选中**（选择集选出来那批变体一共多大——只问中立库，卡不在手边也算得出来）、**清单之外**（目标上工具没放过的文件一共多大——要目标在位才知道，没排过**差量预览**时它是「还不知道」而不是零）、**上限**（这个子库记着的容量上限）。**超没超不在它这儿算**——那由一处说了算，条子只画。
_Avoid_: 进度条、空间条、占用图

**落点撞车**:
不止一份内容要落到目标设备上同一条路径。两条路走到这里：转换把两个不同的容器解成了同名内容；或者两个**根**里同一条相对路径——子库里的落点一律剥掉根名（不剥的话卡上多一层，而前端认平台靠的是顶层那一级目录）。**撞上的一个都不放行**：留一个放行等于随排序决定谁赢，下一趟排序变了赢家就换人，卡上那份会莫名其妙地改内容。**差量预览**里报出来，与「放不进目标」的其他几条（超过单文件上限、文件名不收的字符）同一栏。
_Avoid_: 冲突、重名、覆盖

**能力档案**:
一个子库所用前端与模拟器核心能吃哪些容器与镜像格式的声明。它决定导出时每个平台转成什么格式。内置预置档案，可手动覆盖。
_Avoid_: 设备配置、兼容性表

**同步**:
把子库的目标状态与目标设备的实际状态对齐的过程。三方对比：主库该有的 / 清单记录的 / 目标上实际有的。
_Avoid_: 导出、推送、拷贝

### 库的状况

**库体检**:
对主库跑一遍只读扫描得到的报告：有什么、多少、什么形态、哪些不该在这。它的覆盖面大于识别的覆盖面——未纳入管理的目录照样出现在报告里，那是人工整理的依据。
_Avoid_: 扫描报告、盘点

**不可读**:
文件名拿得到、但元数据读不到的条目。它既不是「已变」也不是「已删」，是如实记录的第三种状态。
_Avoid_: 损坏、失败、跳过

**遍历**:
走完一次主库留下的那条**记录**——什么时候走的、走了多久、看见多少。与「库里有什么」分开记。
**扫描**是那个**动作**，遍历是它的记录；命令、模块、选项一律叫扫描，落库的那一行才叫遍历。
_Avoid_: 跑一遍

**扫描代号**:
一次遍历的编号。中断续跑沿用同一个，收尾时靠它判断哪些条目「这次没见到」。
_Avoid_: 扫描 ID、批次号

**基线**:
上一次遍历记下的那份三元组，增量判断拿它作比。
_Avoid_: 快照、上次结果、缓存

**重复拷贝**:
**同一份内容**（同一个内容锚）存在多份，同一个主库内或跨主库都算。只发现并报告，绝不自动删除。
**同一个作品的两个不同转储不算重复拷贝**——那是两个**变体**，删掉任何一份都是真丢东西。
_Avoid_: 冗余、副本、去重
_Gate_: 冗余、副本

### 工具自己的状态

**工作目录**:
工具自己那些东西住的地方：**中立库**、**沉淀库**、DAT 库、**中文离线源**、**媒体池**、扫描的断点、界面的版式偏好，全在这儿。**一样都不落在主库里**——落进去会被当场拦下（ADR-0004）。位置按 `$ROMCAT_HOME` → `%APPDATA%\romcat` → `$XDG_DATA_HOME/romcat` → `~/.local/share/romcat` 这条链定，也可以每条命令单指。
换一个工作目录等于换一整套工具状态，**主库一个字节都不动**。
_Avoid_: 数据目录、配置目录、家目录、workspace
_Gate_: 数据目录、配置目录、家目录

**现场**:
一份开好的中立库、一份**沉淀库**、以及这份主库的[[主库标识]]——三样捆在一起才够干活。
_Avoid_: 上下文、会话、环境

**自足**:
界面上答得出「扫完了，然后呢」——主干**六步**每一步在界面上都有落点，而命令行只管两类活：**挂后台跑很久**与**换一台机器**（ADR-0023）。
**判据一句话**：凡是「就在这台机器上、就现在、就这一份库」的动作，界面上都得有落点。于是「这件事界面做不到，人得回终端」本身就是一条**缺陷**，不是一次取舍——按范围推掉它的时候要说清推给谁。
_Avoid_: 自洽、完备、全功能、GUI 优先、够用

**开场**:
五屏之外的那一屏：挑一份**现场**开进去，或者认领一个新主库。它交出现场之后自己退场——五屏都建立在「库一定在」这个前提上，开场就是守住那个前提的地方。
**通常见不到它**：上次开的那一份记着，下次直接开进主窗口；只在那一份打不开了（挪走、删了、结构版本对不上）或人主动要换库时才出现（ADR-0023）。
_Avoid_: 启动屏、欢迎页、首屏、库选择器
_Gate_: 启动屏、欢迎页、首屏、库选择器

**上次开的那份**:
上一趟真的开进主窗口的那份**现场**，记在**工作目录**那条链折出来的一份程序级配置里。
它是**开场**通常见不到的原因：下次启动直接开进主窗口。
**只记真的开进去了的那一份**——不论它是开场挑的还是命令行参数指的；合成数据那一路不记。
明确说了工作目录、而记着的那份不在那儿时，**不听它的**（换一个工作目录等于换一整套工具状态）。
_Avoid_: 最近打开、历史记录、会话恢复、上次的库

**任务**:
工具替你跑的一趟长活：扫一遍主库、跑一趟识别、采一趟刮削、排一次**差量预览**、同步一个子库。它报得出跑到哪儿了、按得下停下、跑完在历史里留一条。**它不是[[待确认队列]]**——队列是等你裁的那一堆，任务是工具替你跑的那一趟。
_Avoid_: 作业、后台任务、操作、job
_Gate_: 作业、后台任务

**任务台**:
**任务**排队与开跑的地方：一条队列、一个正在跑的位子，加上跑完那些留下的历史。**排在台上的一次只跑一趟**——这些活都在读同一份中立库、写同一块盘，同时跑两趟只会互相抢。
_Avoid_: 任务列表、任务队列、线程池
_Gate_: 任务列表、任务队列、线程池

**工序**:
一个库要走的那几道步骤：扫描、识别、刮削、折标题、裁决、**导出**。**工序是那道步骤，[[任务]]是跑那一趟**——点一道工序的按钮，排一趟任务上台。
库屏上一道工序一行，显示的是**还差多少**（「N 个变体连识别都还没跑过」），不是上次几点跑的：人要的是「下一步该干什么」，时间戳答不了这个问题。
**算不出「还差多少」的那几支退回显示时刻**：折标题与导出的那个数只有跑一遍那道工序才算得出来（折算真库 1.7 与 2.0 秒），而那一行是画帧那条线程上现算的——开窗一次、改根一次、每趟工序跑完一次，那几下会各卡上一两秒。退回时刻答的是**这道工序上次几点跑的**（真跑过才算，顺手折回来那一下不算），而不是「手上这份集合有多新」。这是退路，不是这一行想说的话——度量真做出来时它换回报数。
_Avoid_: 步骤、阶段、流水线、pipeline
_Gate_: 步骤、阶段、流水线、pipeline

**收场**:
一趟**任务**是怎么结束的，**一条轴四档**：跑完了（有产物）、停了什么都没留下（干净，可以当没跑过）、[[停在半路]]、出错了（说得出停在哪一步、为什么）。四档在**任务台**历史上各画各的话——两档撞脸的话，那句「跑了 X 秒」就成了骗人的话。
_Avoid_: 结果、状态、结局

**停在半路**:
一趟活**没走完就收了场，却交出了产物**——那是四档[[收场]]里的第三档。会写东西的活（扫描、同步）被叫停时照旧要把这一趟收完：扫描的**断点**、同步的**清单**是下一趟接着跑的依据，抛错会把它们丢掉（ADR-0015）。所以它交出来的产物长得跟跑完的那一份一模一样，而**留下了什么由那个长入口自己说**（按停时走过几个条目、落了几件）——只有它知道自己写没写过东西。
**收手的理由不改变这一档是什么**：人按的「停下」是一种，活自己收的手（刮削撞上配额、同步连着失败太多次）是另一种，两种留下的都是「下一趟得接着来」的东西。
**它与「不留半截状态」不是一回事**：停下来的那一步仍然是干净的（停在两步之间），半路说的是**整趟**只走了一段。
_Avoid_: 中断、半截、失败、完成

**DAT 条目**:
一份 DAT 里的一条记录。它通常对应一个**发行版**，但不总是——TOSEC 会把同一次发行的不同转储各记一条，MAME 的 software list 记到芯片一级。**「一条 DAT 条目等于一个发行版」这个假设会出错。**
_Avoid_: DAT 记录、game

### 边界之外

**用户状态**:
**前端里**那份玩出来的历史：游玩次数、游玩时长、通关状态，以及前端自己那份收藏（Pegasus 的 `favorites.txt`、ES 的 `gamelist.xml`）。明确不在工具管理范围内，导出时只从快照原样搬运，绝不生成、绝不覆盖。**与[[收藏]]同名不同物**：那一个长在沉淀库里、锚在内容锚上，是用户对着这个库做的一次挑选（ADR-0006 的修订段）。
_Avoid_: 游玩数据、个人数据
